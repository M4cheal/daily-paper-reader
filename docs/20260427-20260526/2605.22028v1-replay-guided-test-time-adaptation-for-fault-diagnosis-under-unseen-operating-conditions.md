---
title: Replay-guided Test-time Adaptation for Fault Diagnosis Under Unseen Operating Conditions
title_zh: 基于回放引导的测试时自适应方法用于未知工况下的故障诊断
authors: "Yakun Wang, Pengyu Han, Zeyi Liu, Xiao He, Dongming Cai, Hongshuo Zhao"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22028v1"
tags: ["query:tta"]
score: 9.0
evidence: 在线测试时自适应用于故障诊断，结合重放引导
tldr: 工业系统常变工况导致故障诊断模型性能下降，现有方法无法动态适应。本文提出离线域泛化与在线测试时自适应框架，离线通过对抗学习提取域不变特征，在线采用双记忆重放机制，存储高置信伪标签并与历史数据重放，实现适应分布变化并缓解遗忘。在真实电机数据集上取得竞争性能，为动态工况下故障诊断提供有效自适应方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22028v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 441, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22028v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1769, \"height\": 1068, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22028v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1803, \"height\": 1621, \"label\": \"Table\"}]"
motivation: 现有方法无法动态适应连续变化的工况，模型在未见过工况下性能严重下降。
method: 离线对抗学习提取域不变特征，在线双记忆重放机制选择性存储高置信伪标签并与历史数据重放。
result: 在真实电机数据集上取得竞争性能，优于现有方法。
conclusion: 提出的框架有效解决了动态工况下故障诊断的自适应问题，具有良好的泛化能力。
---

## 摘要
在现代工业系统中，机械设备常常在负载和速度不断变化的动态环境下运行。因此，基于深度学习的故障诊断模型在未知工况下由于复杂的数据分布偏移而经常遭受严重的性能退化。由于现有方法主要依赖静态离线训练，它们缺乏动态适应这些持续变化的能力。为了解决这一问题，本文提出了一种结合离线域泛化（DG）和在线测试时自适应（OTTA）的集成框架。首先，通过对抗学习提取域不变特征，离线获得具有初步泛化能力的模型。在线阶段，开发了一种双记忆回放机制。通过选择性存储高置信度的在线伪标签样本，并将其与历史离线数据一起回放，该模型有助于适应变化的数据分布，并减少对先前学习知识的遗忘。在实际电机数据集上的实验表明，所提出的方法在考虑的未知工况下取得了有竞争力的性能。

## Abstract
In modern industrial systems, machinery frequently operates under dynamic environments with continuously varying loads and speeds. Consequently, deep learning-based fault diagnosis models often suffer from severe performance degradation under unseen operating conditions due to complex data distribution shifts. Since existing methods predominantly rely on static offline training, they lack the capability to dynamically adapt to these continuous variations. To address this issue, an integrated framework combining offline domain generalization (DG) and online test-time adaptation (OTTA) is proposed. Initially, a model with preliminary generalization capability is obtained offline by extracting domain-invariant features via adversarial learning. During the online phase, a dual-memory replay mechanism is developed. By selectively storing high-confidence online pseudo-labeled samples and replaying them with historical offline data, the model facilitates adaptation to changing data distributions and helps reduce forgetting of previously learned knowledge Experiments on a real-world motor dataset show that the proposed approach achieves competitive performance under the considered unseen operating conditions.

---

## 论文详细总结（自动生成）

# 基于回放引导的测试时自适应方法用于未知工况下的故障诊断

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代工业系统中，机械设备常在负载、转速等动态变化的工况下运行，导致训练好的深度学习故障诊断模型在“未见工况”（unseen operating conditions）下因数据分布偏移而性能严重下降。
- **现有局限**：传统方法大多基于静态离线训练（如域泛化DG），模型参数固定，无法动态适应在线数据流的持续分布变化；而仅依赖在线更新又容易灾难性遗忘先前学到的知识。
- **整体含义**：本文旨在将离线域泛化（获得初步泛化能力）与在线测试时自适应（OTTA，动态跟踪分布漂移）相结合，提出一种能持续适应未知工况、同时保留历史知识的故障诊断框架。

## 2. 论文提出的方法论

### 核心思想
- 离线阶段：通过对抗学习（Domain-Adversarial Neural Network, DANN）提取域不变特征，建立鲁棒的初始模型。
- 在线阶段：构建双记忆结构（离线记忆库 + 在线记忆库），利用高置信度伪标签筛选新样本，并通过联合回放（replay）机制平衡对新分布的适应和对旧知识的保持。

### 关键技术细节
1. **离线训练**：
   - 模型由特征提取器 \( F \)、故障分类器 \( G_f \)、条件分类器 \( G_c \) 组成。
   - 优化目标：最小化故障分类损失 \( L_f \)，同时通过梯度反转层（GRL）最大化条件分类损失 \( L_c \)，获得域不变特征。
   - 整体目标：\(\max_{\theta_{G_c}} \min_{\theta_F, \theta_{G_f}} L_f - \lambda L_c\)。
   - 训练完成后，从离线数据中为每个工况/故障类别随机选取一批样本构建离线记忆库 \( M_{off} \)。
2. **在线阶段**：
   - 对于每个到达的未标注样本 \( x_t \)，用当前模型 \( H_{t-1} \) 预测置信度 \( \tilde{C}_{t-1} \) 和伪标签 \( \tilde{y}_{t-1} \)。
   - 若 \( \tilde{C}_{t-1} \) 超过预设阈值，则将 \( (x_t, \tilde{y}_t) \) 加入在线记忆库 \( M_{on} \)（容量K，FIFO淘汰策略）。
   - 定义联合记忆库 \( M_{all} = M_{off} \cup M_{on} \)。
   - 从 \( M_{all} \) 中采样batch，用交叉熵损失 \( L_{ce} \) 更新模型参数：  
     \[ L_{ce} = -\frac{1}{|M_{all}|} \sum_{(x_i, \tilde{y}_i) \in M_{all}} \sum_{c=1}^{N_f} \mathbf{1}(\tilde{y}_i = c) \log H(x_i)^{(c)}. \]
   - 每次在线更新迭代30个epochs。

### 算法流程（文字说明）
1. 离线阶段：使用源域多工况标注数据训练DANN，保存特征提取器和故障分类器，初始化 \( M_{off} \) 和 \( M_{on} \)（初始样本从 \( M_{off} \) 随机选取）。
2. 在线阶段循环：接收新样本 → 计算置信度和伪标签 → 若高置信则加入 \( M_{on} \) → 从 \( M_{all} \) 采样 → 用交叉熵损失更新模型 → 继续处理下一时刻样本。

## 3. 实验设计

- **数据集**：自行搭建的三相异步电机实验台（2.2 kW），包含8通道传感器（三轴振动、三相电流、转矩、键相），采样率12.8 kHz。共24种故障配置（正常、转子/定子异常、偏心、轴承缺陷、复合故障等）。数据经滑动窗口（1024长度，步长64）切分，每batch 128×6144。
- **场景划分**：
  - 离线训练：15 Nm 和 40 Nm 两个负载条件，每类故障556个样本。
  - 在线流：依次经过 20 Nm（未见）、30 Nm（未见）、15 Nm（已知）、40 Nm（已知），每种条件持续10580个样本。
  - 三种故障类型：动态偏心、静态偏心、电压不平衡。每种故障在序列的30%、50%、70%处注入（即健康→故障状态切换）。
- **对比方法**：
  1. **Baseline**：标准MLP，仅用交叉熵训练，无域泛化或更新。
  2. **Without Update**：离线DANN模型，在线不更新。
  3. **Without Replay**：在线更新时仅使用 \( M_{on} \)（无离线回放）。
  4. **Proposed**：完整框架（DANN + 双记忆回放）。
- **评价指标**：平均准确率 ± 标准差（Acc ± SD），每个条件5次独立重复。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量或训练时长。仅提及使用Adam优化器，离线学习率1e-3（50 epochs），在线学习率1e-4（每步30 epochs）。模型为全连接MLP（层数较少），算力需求较低。
- 因此无法评估具体的计算成本或可扩展性。

## 5. 实验数量与充分性

- **实验规模**：
  - 3种故障类型 × 3个注入位置（30%/50%/70%）= 9个主要实验场景。
  - 每个场景下比较4种方法，且每方法5次独立重复，共约180次试验（表I）。
  - 还提供了实时准确率曲线（图2，9个子图），直观展示各方法随时间变化的行为。
- **消融分析**：通过Without Update和Without Replay分别验证了离线DG和在线回放机制的必要性，消融实验设计合理。
- **充分性评价**：
  - ✅ 考虑了多种故障类型和注入时机（模拟不同严重度）。
  - ✅ 涵盖了已知与未知工况，检验了泛化与抗遗忘能力。
  - ⚠️ 仅在单一电机数据集上验证，缺乏跨设备或公开基准（如CWRU、XJTU等）的对比，通用性存疑。
  - ⚠️ 未对阈值、记忆库容量K等超参数进行敏感性分析。

## 6. 论文的主要结论与发现

- 提出的 **离线DG + 在线OTTA + 双记忆回放** 框架在大多数实验设置下取得了最高或接近最高的准确率，且标准差较小（例如在动态偏心场景下，Proposed方法在未知工况中可达0.99+，而Without Replay仅0.43-0.53）。
- 离线DANN提供了良好的初始域不变特征（Without Update比Baseline好），但静态模型在未知工况下仍然随分布漂移而性能下降。
- 无回放机制（Without Replay）会导致严重的灾难性遗忘，尤其是在动态偏心任务中。
- 双记忆回放（联合离线+在线）有效平衡了适应新分布与保留旧知识，实现了稳健的持续诊断。

## 7. 优点

- **方法创新性**：首次将离线域泛化与在线测试时自适应系统性地结合用于故障诊断，并利用回放机制缓解遗忘，问题设定贴近工业实际。
- **实验设计细致**：考虑了多种故障类型、不同注入位置（即故障发生时机），以及已知/未知工况的交替出现，能较好地模拟工业在线场景。
- **消融实验充分**：通过Without Update和Without Replay清晰地证明了离线DG和回放策略各自的贡献。
- **结果有说服力**：在性能严重下降的困难场景（如动态偏心、70%注入）下，Proposed方法仍能保持高精度且低方差。

## 8. 不足与局限

- **实验覆盖不足**：仅在一个自建电机数据集上测试，未在公共轴承/齿轮箱数据集（如CWRU、Paderborn等）上验证，结论的泛化性有待确认。
- **实时性考虑不足**：在线每步更新30个epochs，可能无法满足工业实时故障诊断的低延迟要求（如毫秒级响应）。未讨论计算开销或推理时间。
- **超参数敏感性未分析**：置信度阈值、在线记忆库容量K、回放批次大小等关键参数的影响未给出。
- **伪标签质量风险**：依赖模型初始性能，若早期预测错误则可能将错误样本加入在线记忆库，导致错误累积。文中未讨论错误积累或异常检测机制。
- **未与其他OTTA方法对比**：如TENT、SHOT等通用OTTA方法，缺乏与领域内最新OTTA算法的横向比较。
- **未讨论标签分布变化**：假设故障类别固定，但实际中可能有新类别出现或类别比例剧烈变化。

（完）
