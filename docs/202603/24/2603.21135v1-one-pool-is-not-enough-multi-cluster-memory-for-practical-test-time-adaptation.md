---
title: "One Pool Is Not Enough: Multi-Cluster Memory for Practical Test-Time Adaptation"
title_zh: 一个池子是不够的：面向实用测试时自适应的多聚类内存
authors: "Yu-Wen Tseng, Xingyi Zheng, Ya-Chen Wu, I-Bin Liao, Yung-Hui Li, Hong-Han Shuai, Wen-Huang Cheng"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21135v1"
tags: ["query:tta"]
score: 10.0
evidence: 针对流式数据的实用测试时间自适应多簇内存
tldr: 针对实际测试时自适应（PTTA）中测试流具有多模态且非独立同分布的特性，本文指出传统的单内存池设计存在结构性缺陷。作者提出了多聚类内存（MCM）框架，通过轻量级像素级统计描述符将样本组织成多个聚类，并引入相邻聚类合并（ACC）和均匀聚类检索（UCR）机制。实验证明，MCM在多个基准数据集上显著提升了现有TTA方法的性能，特别是在复杂分布下表现优异，确立了内存组织在PTTA中的关键地位。
selection_source: fresh_fetch
motivation: 现有的TTA方法普遍采用单内存池设计，无法有效应对实际测试流中固有的多模态分布特性。
method: 提出MCM框架，利用像素级统计描述符进行多聚类管理，结合相邻聚类合并以控制内存，并通过均匀检索确保自适应过程中的分布平衡。
result: "在CIFAR、ImageNet-C和DomainNet等数据集上，MCM使现有TTA方法的准确率最高提升了12.13%，且在复杂标签空间下增益更明显。"
conclusion: 内存组织结构是提升实际测试时自适应性能的关键设计维度，多聚类设计能显著改善分布平衡和模式覆盖。
---

## 摘要
测试时自适应（TTA）仅利用无标签测试数据，在推理阶段使预训练模型适应分布偏移。在实用测试时自适应（PTTA）设置下，测试流具有时间相关性且非独立同分布（non-i.i.d.），内存已成为稳定自适应不可或缺的组件，然而现有方法普遍将样本存储在单个非结构化池中。我们证明了这种单聚类设计与 PTTA 根本不匹配：流聚类分析揭示了测试流本质上是多模态的，其最优混合成分数量始终远超一个。为了弥补这一结构性差距，我们提出了多聚类内存（MCM），这是一个即插即用的框架，利用轻量级像素级统计描述符将存储的样本组织成多个聚类。MCM 引入了三个互补机制：基于描述符的聚类分配以捕获不同的分布模式；相邻聚类整合（ACC）通过合并最相似的时间相邻聚类来限制内存使用；以及均匀聚类检索（UCR）以确保自适应过程中所有模式的平衡监督。在 CIFAR-10-C、CIFAR-100-C、ImageNet-C 和 DomainNet 上与三种当代 TTA 方法集成后，MCM 在所有 12 种配置中均实现了持续改进，在 ImageNet-C 上提升高达 5.00%，在 DomainNet 上提升高达 12.13%。值得注意的是，这些收益随分布复杂度的增加而扩大：具有更大多模态性的更大标签空间从多聚类组织中获益最多。基于高斯混合模型（GMM）的内存诊断进一步证实，MCM 保持了近乎最优的分布平衡、熵和模式覆盖，而单聚类内存则表现出持续的不平衡和渐进的模式丢失。这些结果确立了内存组织作为实用测试时自适应的关键设计维度。

## Abstract
Test-time adaptation (TTA) adapts pre-trained models to distribution shifts at inference using only unlabeled test data. Under the Practical TTA (PTTA) setting, where test streams are temporally correlated and non-i.i.d., memory has become an indispensable component for stable adaptation, yet existing methods universally store amples in a single unstructured pool. We show that this single-cluster design is fundamentally mismatched to PTTA: a stream clusterability analysis reveals that test streams are inherently multi-modal, with the optimal number of mixture components consistently far exceeding one. To close this structural gap, we propose Multi-Cluster Memory (MCM), a plug-and-play framework that organizes stored samples into multiple clusters using lightweight pixel-level statistical descriptors. MCM introduces three complementary mechanisms: descriptor-based cluster assignment to capture distinct distributional modes, Adjacent Cluster Consolidation (ACC) to bound memory usage by merging the most similar temporally adjacent clusters, and Uniform Cluster Retrieval (UCR) to ensure balanced supervision across all modes during adaptation. Integrated with three contemporary TTA methods on CIFAR-10-C, CIFAR-100-C, ImageNet-C, and DomainNet, MCM achieves consistent improvements across all 12 configurations, with gains up to 5.00% on ImageNet-C and 12.13% on DomainNet. Notably, these gains scale with distributional complexity: larger label spaces with greater multi-modality benefit most from multi-cluster organization. GMM-based memory diagnostics further confirm that MCM maintains near-optimal distributional balance, entropy, and mode coverage, whereas single-cluster memory exhibits persistent imbalance and progressive mode loss. These results establish memory organization as a key design axis for practical test-time adaptation.

---

## 论文详细总结（自动生成）

这是一份关于论文《One Pool Is Not Enough: Multi-Cluster Memory for Practical Test-Time Adaptation》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在实用的测试时自适应（PTTA）场景中，测试数据流通常具有时间相关性和非独立同分布（non-i.i.d.）的特性。现有的 TTA 方法普遍采用“单聚类内存（SCM）”设计，即所有样本存储在一个无结构的池中。
*   **研究动机**：作者通过流聚类分析发现，测试流本质上是**多模态**的（最优聚类数 $K \gg 1$），而单池设计隐含了单模态假设。这种结构性失配导致内存无法公平代表所有分布模式，容易出现模式丢失和分布倾斜，限制了模型的自适应效果。

### 2. 论文提出的方法论：MCM 框架
MCM 是一个即插即用的结构化内存框架，核心思想是将样本按分布模式组织成多个聚类。
*   **像素级统计描述符**：使用图像通道级的均值和方差作为轻量级描述符，能够有效捕捉光照、噪声、模糊等领域偏移特征。
*   **基于描述符的聚类分配**：新样本到达时，计算其描述符与现有聚类中心的欧氏距离。若距离超过阈值 $\tau$ 则创建新聚类，否则归入最近聚类。
*   **相邻聚类合并（ACC）**：当聚类数量达到上限 $K_{max}$ 时，合并时间上相邻且描述符最相似的两个聚类。这利用了 PTTA 流的时间相关性，在限制内存的同时保留分布多样性。
*   **均匀聚类检索（UCR）**：在自适应训练阶段，从每个聚类中抽取相同数量的样本。这确保了梯度更新受到所有分布模式的平衡监督，防止模型被主导模式误导。

### 3. 实验设计
*   **数据集与场景**：
    *   **CIFAR-10-C / CIFAR-100-C / ImageNet-C**：包含 15 种损坏类型，模拟连续的领域偏移。
    *   **DomainNet**：跨领域的物体识别任务（Real $\to$ Clipart/Painting/Sketch）。
*   **Benchmark 与对比方法**：
    *   将 MCM 集成到三种代表性 TTA 方法中：**RoTTA**、**PeTTA** 和 **ResiTTA**。
    *   对比了 TENT、CoTTA、LAME、NOTE、TRIBE、NEO 等十余种主流 TTA 算法。
*   **评估指标**：分类错误率（%）、内存质量诊断指标（不平衡率、分布熵、模式覆盖率）。

### 4. 资源与算力
*   **硬件平台**：实验在一台 **NVIDIA RTX 4090 GPU** 上完成。
*   **运行效率**：论文提供了运行耗时对比。例如在 CIFAR-100-C 上，MCM 集成后的运行时间（约 1049s）与单池内存相当甚至更优，证明了其轻量化设计的有效性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个数据集和 3 种基准方法的 12 种组合配置，均表现出一致的性能提升。
*   **消融实验**：详细验证了 ACC、UCR 模块的独立贡献，对比了不同描述符（CNN 特征 vs. 像素统计）和合并策略（全局合并 vs. 随机合并等）。
*   **长期稳定性**：进行了 **20 轮循环 TTA 实验**，验证模型在长期运行下是否会崩溃。
*   **充分性评价**：实验设计非常全面且客观，通过 GMM 诊断框架从数学层面解释了性能提升的原因，而非仅仅依赖准确率数字。

### 6. 论文的主要结论与发现
*   **结构胜过容量**：单纯增加单池内存的容量对性能提升微乎其微，而通过 MCM 进行结构化组织能显著降低错误率（平均降低 2.96%，最高达 12.13%）。
*   **复杂度增益**：分布越复杂（标签空间越大、多模态性越强），MCM 的优势越明显（如在 ImageNet-C 和 DomainNet 上增益最大）。
*   **内存质量**：MCM 能在整个自适应过程中保持近乎最优的分布平衡和完整的模式覆盖，而 SCM 会出现严重的模式丢失。

### 7. 优点
*   **理论支撑扎实**：通过流聚类分析（BIC 准则）量化了 PTTA 的多模态本质，为方法论提供了科学依据。
*   **即插即用**：不改变原有 TTA 方法的损失函数或架构，易于集成到现有系统中。
*   **高效性**：采用像素级统计量而非深度特征进行聚类，计算开销极低，适合实时推理。

### 8. 不足与局限
*   **描述符偏差**：像素级统计量主要捕捉低级视觉偏移（如颜色、噪声），对于高级语义偏移或复杂的几何变换可能不够敏感。
*   **任务覆盖**：目前主要集中在图像分类任务，尚未在目标检测、分割或多模态任务（如视觉语言模型）中验证。
*   **超参数依赖**：虽然作者证明了阈值 $\tau$ 具有一定的鲁棒性，但在极端不同的数据分布下，可能仍需微调聚类上限 $K_{max}$。

（完）
