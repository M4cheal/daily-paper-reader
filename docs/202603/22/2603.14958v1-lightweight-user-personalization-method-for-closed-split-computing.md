---
title: Lightweight User-Personalization Method for Closed Split Computing
title_zh: 针对封闭式拆分计算的轻量级用户个性化方法
authors: "Yuya Okada, Takayuki Nishio"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14958v1"
tags: ["query:tta"]
score: 8.0
evidence: 针对封闭系统中数据分布偏移的轻量级自适应框架
tldr: 针对封闭式拆分计算中因数据偏移、通信不稳定和隐私扰动导致的性能下降问题，本文提出了SALT（拆分自适应轻量级微调）框架。该框架通过在客户端引入紧凑的适配器来优化冻结头部网络的中间表示，无需修改原始模型架构或增加通信开销。实验证明，SALT在提升用户个性化准确率的同时，显著降低了训练延迟，并在高丢包率和噪声环境下表现出极强的鲁棒性，为边缘云协作推理提供了高效的适配方案。
selection_source: fresh_fetch
motivation: 旨在解决封闭拆分计算环境下，因用户数据分布偏移、网络不稳定及隐私保护需求导致的推理性能下降挑战。
method: 引入一种名为SALT的轻量级客户端适配器，通过精炼冻结头部网络的中间表示来实现模型自适应，而无需访问或修改原始模型参数。
result: "实验显示SALT将CIFAR-10的个性化准确率提升至93.8%，降低了60%以上的训练延迟，并在高丢包率和噪声注入下保持了高准确率。"
conclusion: SALT框架证明了通过轻量级中间表示微调，可以在不增加通信负担的情况下，有效提升拆分计算系统的个性化水平和环境鲁棒性。
---

## 摘要
拆分计算（Split Computing）通过将深度神经网络划分为边缘端头部和服务器端尾部，实现了边缘设备与云端之间的协同推理，从而降低了延迟并限制了原始输入数据的暴露。然而，在实际部署中，由于用户特定的数据分布偏移、不可靠的通信以及面向隐私的扰动，推理性能往往会下降，特别是在模型架构和参数不可访问的封闭环境中。为了应对这一挑战，我们提出了 SALT（拆分自适应轻量级微调），这是一种针对封闭式拆分计算系统的轻量级自适应框架。SALT 引入了一个紧凑的客户端适配器，用于优化由冻结的头部网络生成的中间表示，从而在不修改头部或尾部网络、也不增加通信开销的情况下实现有效的模型自适应。通过仅修改训练条件，SALT 支持多种自适应目标，包括用户个性化、通信鲁棒性和隐私感知推理。在 CIFAR-10 和 CIFAR-100 上使用 ResNet-18 进行的实验表明，SALT 比传统的重新训练和微调具有更高的准确率，同时显著降低了训练成本。在 CIFAR-10 上，SALT 将个性化准确率从 88.1% 提高到 93.8%，同时将训练延迟降低了 60% 以上。此外，SALT 在 75% 的丢包率下仍能保持 90% 以上的准确率，并在注入噪声的情况下保持高准确率（在 sigma = 1.0 时约为 88%）。这些结果表明，SALT 为现实世界的拆分计算系统提供了一个高效且实用的自适应框架。

## Abstract
Split Computing enables collaborative inference between edge devices and the cloud by partitioning a deep neural network into an edge-side head and a server-side tail, reducing latency and limiting exposure of raw input data. However, inference performance often degrades in practical deployments due to user-specific data distribution shifts, unreliable communication, and privacy-oriented perturbations, especially in closed environments where model architectures and parameters are inaccessible. To address this challenge, we propose SALT (Split-Adaptive Lightweight Tuning), a lightweight adaptation framework for closed Split Computing systems. SALT introduces a compact client-side adapter that refines intermediate representations produced by a frozen head network, enabling effective model adaptation without modifying the head or tail networks or increasing communication overhead. By modifying only the training conditions, SALT supports multiple adaptation objectives, including user personalization, communication robustness, and privacy-aware inference. Experiments using ResNet-18 on CIFAR-10 and CIFAR-100 show that SALT achieves higher accuracy than conventional retraining and fine-tuning while significantly reducing training cost. On CIFAR-10, SALT improves personalized accuracy from 88.1% to 93.8% while reducing training latency by more than 60%. SALT also maintains over 90% accuracy under 75% packet loss and preserves high accuracy (about 88% at sigma = 1.0) under noise injection. These results demonstrate that SALT provides an efficient and practical adaptation framework for real-world Split Computing systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SALT (Split-Adaptive Lightweight Tuning)** 的框架，旨在解决拆分计算（Split Computing）在实际部署中面临的性能退化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：拆分计算将深度神经网络（DNN）分为边缘端的“头部”和云端的“尾部”，以平衡计算负载、降低延迟并保护原始数据隐私。
*   **核心问题**：在实际应用中，推理性能常因**用户数据分布偏移**（异构性）、**通信环境不稳定**（丢包）以及**隐私保护扰动**（注入噪声）而显著下降。
*   **挑战**：现有的模型自适应方法（如微调）通常假设模型参数是可访问且可修改的。但在现实中，许多模型以加密二进制、受限 API 或硬件加速器固件的形式存在（即“封闭模型”），导致传统微调方法失效。

### 2. 方法论：SALT 框架
*   **核心思想**：在不触动原始“头部”和“尾部”网络参数的前提下，在客户端引入一个极轻量级的**可训练适配器（Adapter）**。该适配器通过修正头部网络输出的中间表示（Latent Features）来补偿环境带来的失真。
*   **关键技术细节**：
    *   **残差结构**：采用 $z' = z + S(z)$ 的形式，其中 $z$ 是原始中间特征，$S(z)$ 是适配器生成的修正向量。这种设计保证了在不需要自适应时，模型能退化为原始状态。
    *   **维度保持**：适配器输出与输入维度完全一致，因此不会增加通信带宽负担。
    *   **统一自适应目标**：通过改变训练时的模拟条件（如模拟丢包、注入高斯噪声），同一个 SALT 架构可以分别实现个性化、通信鲁棒性和隐私增强三个目标。
*   **算法流程**：采用拆分学习（Split Learning）范式。客户端计算特征并经过 SALT 修正后发送至服务器；服务器计算损失并回传梯度；客户端仅更新 SALT 适配器的参数，头部和尾部网络保持冻结。

### 3. 实验设计
*   **数据集**：CIFAR-10 和 CIFAR-100。
*   **场景模拟**：
    1.  **用户个性化**：从全量类别中选取 5 类作为特定用户的任务空间。
    2.  **通信鲁棒性**：模拟 0% 到 75% 的随机丢包率。
    3.  **隐私感知**：在特征中注入不同强度（$\sigma$ 从 0 到 1.5）的高斯噪声以抵御模型逆向攻击（Inversion Attacks）。
*   **Benchmark 与对比方法**：
    *   **Original**：无自适应的原始拆分模型。
    *   **Retrain/Fine-tuning**：假设模型开放，重新训练或微调头部网络（作为性能上限参考）。
    *   **SALT (Insertion)**：另一种非残差结构的插入式适配器变体。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA Quadro RTX 6000** GPU。
*   **网络环境**：模拟带宽固定为 **80 Mbit/s**。
*   **训练细节**：使用 Adam 优化器，学习率 1e-3，batch size 128，训练 100 个 epoch 或直至验证集损失平稳。
*   **模型复杂度**：适配器包含约 443K 参数，计算量为 28.39 MMACs，远小于 ResNet-18 头部网络的开销。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流数据集上进行了测试，涵盖了 4 个不同的拆分点（BeforeBlock1 到 AfterBlock3），并针对丢包率和噪声强度进行了多组参数扫描。
*   **客观性与公平性**：每组实验重复 10 次并给出 95% 置信区间。对比实验考虑了训练延迟（计算+通信时间），不仅关注准确率，还关注了实际部署成本，实验设计较为全面且符合工业界“封闭模型”的真实约束。

### 6. 主要结论与发现
*   **高性能与低成本**：在 CIFAR-10 上，SALT 将准确率从 88.1% 提升至 93.8%，且训练延迟比传统的头部重训降低了 **60% 以上**。
*   **极强的鲁棒性**：在高达 **75% 的丢包率**下，SALT 仍能维持 90% 以上的准确率，而原始模型在 50% 丢包时即崩溃。
*   **隐私与效用平衡**：在注入强噪声（$\sigma=1.0$）以使逆向攻击失效（SSIM 降至极低）的同时，SALT 仍能保持约 88% 的分类准确率，有效解决了隐私保护带来的性能损失。

### 7. 优点
*   **非侵入性**：无需访问原始模型内部结构或参数，非常适合商业 AI 服务和受保护的硬件环境。
*   **通用性**：一个框架通过简单的训练条件切换即可应对数据偏移、网络波动和隐私需求。
*   **通信友好**：不改变传输特征的维度，对现有拆分计算协议完全兼容。

### 8. 不足与局限
*   **实验覆盖面**：实验主要基于 ResNet-18 和 CIFAR 系列小图数据集，尚未在更大型的模型（如 Transformer、大语言模型）或更高分辨率的数据集（如 ImageNet）上验证其扩展性。
*   **应用限制**：虽然适配器很轻量，但在极度资源受限的 IoT 设备上，额外的卷积层仍可能带来微小的能耗增加。
*   **理论深度**：论文更多侧重于实证结果，对于适配器如何从信息论角度修复受损特征的理论解释相对较少。

（完）
