---
title: "M2Diff: Multi-Modality Multi-Task Enhanced Diffusion Model for MRI-Guided Low-Dose PET Enhancement"
title_zh: M2Diff：用于 MRI 引导的低剂量 PET 增强的多模态多任务增强扩散模型
authors: "Ghulam Nabi Ahmad Hassan Yar, Himashi Peiris, Victoria Mar, Cameron Dennis Pain, Zhaolin Chen"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09075v1"
tags: ["query:tta"]
score: 8.0
evidence: 用于MRI引导的PET增强的多模态多任务增强扩散模型
tldr: 本研究针对低剂量PET成像质量不佳的问题，提出了一种名为M2Diff的多模态多任务扩散模型。该模型通过分别处理MRI和低剂量PET扫描，提取模态特定特征，并利用分层特征融合技术将结构与功能信息有效整合，从而实现高质量的标准剂量PET重建。在健康及阿尔茨海默症脑部数据集上的实验证明，该方法在定性和定量评估上均优于现有技术，为临床低剂量成像提供了有效方案。
selection_source: fresh_fetch
motivation: 现有的多模态PET增强方法在融合MRI和PET特征时容易出现特征稀释，难以充分利用模态特定的结构和功能信息。
method: 提出M2Diff模型，采用多任务框架分别提取MRI和LD PET特征，并通过分层融合机制指导扩散模型生成高质量PET图像。
result: 在健康人群和阿尔茨海默症患者的脑部数据集上，该模型在图像质量和定量指标上均取得了领先的表现。
conclusion: M2Diff通过解耦模态特征提取与分层融合，有效提升了MRI引导下的低剂量PET图像重建精度。
---

## 摘要
正电子发射断层扫描（PET）会使患者暴露于辐射中，虽然可以通过降低剂量来减轻辐射，但代价是图像质量下降。这使得低剂量（LD）PET 恢复成为一个活跃的研究领域。先前的研究主要集中在利用深度学习从 LD PET 扫描和/或多模态扫描（如 PET/CT 或 PET/MRI）中恢复标准剂量（SD）PET。虽然这些研究通过在单任务模型中进行条件化来整合多模态信息，但此类方法可能会限制提取模态特定特征的能力，从而可能导致早期特征稀释。尽管最近的研究已开始整合病理信息丰富的数据，但在有效利用多模态输入以重建多样化特征方面仍存在挑战，特别是在异质患者群体中。为了解决这些局限性，我们提出了一种多模态多任务扩散模型（M2Diff），该模型分别处理 MRI 和 LD PET 扫描以学习模态特定特征，并通过分层特征融合将其融合以重建 SD PET。这种设计能够有效整合互补的结构和功能信息，从而提高重建的保真度。我们在健康人群和阿尔茨海默病患者的大脑数据集上验证了模型的有效性。M2Diff 在这两个数据集上均取得了优越的定性和定量性能。

## Abstract
Positron emission tomography (PET) scans expose patients to radiation, which can be mitigated by reducing the dose, albeit at the cost of diminished quality. This makes low-dose (LD) PET recovery an active research area. Previous studies have focused on standard-dose (SD) PET recovery from LD PET scans and/or multi-modal scans, e.g., PET/CT or PET/MRI, using deep learning. While these studies incorporate multi-modal information through conditioning in a single-task model, such approaches may limit the capacity to extract modality-specific features, potentially leading to early feature dilution. Although recent studies have begun incorporating pathology-rich data, challenges remain in effectively leveraging multi-modality inputs for reconstructing diverse features, particularly in heterogeneous patient populations. To address these limitations, we introduce a multi-modality multi-task diffusion model (M2Diff) that processes MRI and LD PET scans separately to learn modality-specific features and fuse them via hierarchical feature fusion to reconstruct SD PET. This design enables effective integration of complementary structural and functional information, leading to improved reconstruction fidelity. We have validated the effectiveness of our model on both healthy and Alzheimer's disease brain datasets. The M2Diff achieves superior qualitative and quantitative performance on both datasets.

---

## 论文详细总结（自动生成）

这是一份关于论文《M2Diff: Multi-Modality Multi-Task Enhanced Diffusion Model for MRI-Guided Low-Dose PET Enhancement》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：正电子发射断层扫描（PET）在临床中至关重要，但其电离辐射对患者（尤其是儿童和需多次复查者）存在风险。降低剂量（Low-Dose, LD）虽能减小辐射，但会导致图像噪声剧增、对比度下降，影响诊断。
*   **研究动机**：
    *   **特征稀释问题**：现有方法多采用单任务模型，将 MRI 和 PET 简单拼接作为输入，这限制了模型提取模态特定特征的能力，容易导致特征在早期阶段被稀释。
    *   **病理异质性挑战**：在阿尔茨海默症（AD）等具有高度结构和代谢变异性的患者群体中，现有模型难以准确重建细微的病理特征（如局部代谢减低）。
    *   **扩散模型的局限**：标准扩散模型在 PET 恢复中易出现体素强度估计不足和边缘模糊问题。

### 2. 方法论
M2Diff 提出了一种基于**改进去噪扩散概率模型（IDDPM）**的多模态多任务框架：
*   **核心思想**：通过“解耦编码-分层融合-共享解码”的策略，分别提取 MRI 的解剖结构特征和 LD PET 的功能强度特征，并利用多任务学习确保两者在重建过程中的互补性。
*   **关键技术细节**：
    *   **双编码器结构**：设置两个独立的编码器分支，分别处理 T1 加权 MRI 和 LD PET，避免了输入端的特征稀释。
    *   **分层特征融合（HFF）**：在解码器的每个阶段，通过线性变换将两个编码器的特征投影到共享空间，进行通道拼接和非线性集成，实现多尺度特征共享。
    *   **多任务学习与双解码器**：模型包含两个解码分支，分别尝试从 PET 路径和 MRI 路径重建标准剂量（SD）PET。最终输出是两个分支预测结果的平均集成。
    *   **损失函数**：结合了图像恢复损失（MSE）和**偏置一致性正则化项（Bias Loss）**，后者用于强制两个任务分支的预测结果保持一致。
    *   **MRI-free 推理策略**：引入类似“无分类器引导”的训练技巧，使模型在测试阶段缺失 MRI 时仍能通过 PET 路径独立工作。

### 3. 实验设计
*   **数据集**：
    1.  **DaCRA 数据集**：包含 27 名受试者的同步 PET/MRI 数据，模拟了 100 倍（x100）和 20 倍（x20）的剂量减少因子（DRF）。
    2.  **ADNI 数据集**：包含正常、轻度认知障碍和阿尔茨海默症患者的数据，用于验证模型在复杂病理变异下的鲁棒性。
*   **Benchmark 与对比方法**：
    *   **非扩散模型**：CycleWGAN, Pix2PixHD, PT-WGAN, Multi-branch UNet。
    *   **扩散模型**：CDM, IDDPM, DDPM-PETMR, DiffusionMTL。
*   **评估指标**：PSNR（峰值信噪比）、NMSE（归一化均方误差）、SSIM（结构相似性）、LPIPS（感知相似性）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A40 GPU**。
*   **训练参数**：训练 100 个 epoch，Adam 优化器，学习率 1e-4，扩散步数 $T=1000$。
*   **模型规模**：M2Diff 参数量为 **268.99M**。
*   **耗时**：
    *   训练：约 1217.84 秒/epoch（Batch Size = 1）。
    *   推理：重建一个完整受试者的 3D 体积约需 **88 秒**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同性质的数据集（健康 vs. 病理）上进行了验证。
    *   针对不同剂量水平（x100, x20）进行了对比。
    *   **消融实验**：设计了 6 种变体（V1-V6），验证了 T1 引导、多任务框架、HFF 模块、对称解码器等设计的必要性。
    *   **统计分析**：进行了配对 t 检验，证明 M2Diff 在多数指标上具有统计学意义的显著提升（p < 0.001）。
    *   **表征分析**：利用 **CKA（中心核对齐）** 分析了编码器和解码器的特征相似性，客观证明了模型“先解耦后融合”的有效性。
*   **评价**：实验设计全面、客观，对比了当前最先进的多种模型，且涵盖了从定量指标到定性视觉效果的深度分析。

### 6. 主要结论与发现
*   **性能领先**：M2Diff 在 SSIM、PSNR 和 LPIPS 指标上均优于所有对比基准。
*   **病理保留**：在 ADNI 数据集上，M2Diff 能更准确地恢复额叶和颞叶等关键区域的代谢分布，保留了具有诊断意义的低代谢模式。
*   **融合优势**：CKA 分析显示，编码器成功学习了模态特定特征（低相似度），而解码器成功实现了特征对齐（高相似度）。
*   **鲁棒性**：即使在 MRI 缺失的情况下，通过部分 MRI 引导训练的模型仍能优于纯 PET 训练的基准模型。

### 7. 优点与亮点
*   **架构创新**：通过多任务学习和分层融合解决了多模态数据融合中的特征稀释难题。
*   **感知质量高**：相比 GAN 类方法，M2Diff 减少了伪影和过度平滑；相比传统扩散模型，其边缘和细节更清晰。
*   **灵活性**：支持 MRI 缺失时的推理，增强了临床适用性。
*   **理论支撑**：引入 CKA 相似性分析，从表征学习角度解释了模型的内部工作机制。

### 8. 不足与局限
*   **维度限制**：目前模型基于 **2D 切片** 运行。虽然通过多视角（轴向、冠状、矢状）堆叠和傅里叶小波变换合成 3D，但仍非原生 3D 卷积，可能存在层间不连续性。
*   **数据依赖**：训练阶段仍高度依赖配对的 PET-MRI 数据。
*   **临床验证**：虽然定量指标优异，但尚未经过放射科医生的盲测或临床诊断效用（如病灶检出率）的正式评估。
*   **计算开销**：作为参数量最大的模型（268.99M），其推理时间（88s）虽在可接受范围内，但仍高于轻量级网络（如 UNet）。

（完）
