---
title: "SPEGC: Continual Test-Time Adaptation via Semantic-Prompt-Enhanced Graph Clustering for Medical Image Segmentation"
title_zh: SPEGC：通过语义提示增强的图聚类实现医学图像分割的持续测试时自适应
authors: "Xiaogang Du, Jiawei Zhang, Tongfei Liu, Tao Lei, Yingbo Wang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11492v1"
tags: ["query:tta"]
score: 10.0
evidence: 用于医学图像分割的连续测试时自适应 (CTTA)
tldr: 针对医学图像分割中持续测试时自适应（CTTA）存在的误差累积和性能退化问题，本文提出了SPEGC框架。该方法通过语义提示增强机制注入全局上下文信息，并利用基于最优传输的可微分图聚类求解器提取高阶结构化表示，从而引导模型在不断变化的目标域中进行稳健自适应。实验证明，该方法在多个医学影像基准上显著提升了分割精度和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的CTTA方法常依赖不可靠的伪标签，导致误差在持续变化的域中不断累积并引发性能崩溃。
method: 设计了语义提示增强机制以注入全局上下文，并结合基于最优传输的可微分图聚类来提取高阶结构特征引导模型更新。
result: 在两个医学图像分割基准上的实验结果表明，SPEGC的性能优于目前最先进的持续测试时自适应方法。
conclusion: SPEGC通过增强特征表示和利用结构化一致性，有效解决了医学影像在动态临床部署中的域偏移挑战。
---

## 摘要
在医学图像分割任务中，训练数据与测试数据之间由于数据采集差异导致的领域间隙（domain gap）严重阻碍了预训练模型在临床实践中的部署。持续测试时自适应（CTTA）旨在使预训练模型能够适应不断变化的无标签领域，为解决这一问题提供了一种有效的方法。然而，现有的 CTTA 方法通常依赖于不可靠的监督信号，引发了错误累积的自我强化循环，最终导致灾难性的性能下降。为了克服这些挑战，我们提出了一种用于医学图像分割的语义提示增强图聚类（SPEGC）持续测试时自适应方法。首先，我们设计了一种语义提示特征增强机制，利用解耦的共性与异质性提示池将全局上下文信息注入局部特征，从而减轻其在领域偏移下对噪声干扰的敏感性。其次，基于这些增强特征，我们设计了一个可微图聚类求解器。该求解器将全局边稀疏化重新表述为一个最优传输问题，使其能够以端到端的方式将原始相似度矩阵提炼为精细的高阶结构化表示。最后，这种鲁棒的结构化表示被用于指导模型自适应，确保预测在聚类层面保持一致，并动态调整决策边界。大量实验表明，SPEGC 在两个医学图像分割基准数据集上优于其他最先进的 CTTA 方法。源代码可在 https://github.com/Jwei-Z/SPEGC-for-MIS 获取。

## Abstract
In medical image segmentation tasks, the domain gap caused by the difference in data collection between training and testing data seriously hinders the deployment of pre-trained models in clinical practice. Continual Test-Time Adaptation (CTTA) aims to enable pre-trained models to adapt to continuously changing unlabeled domains, providing an effective approach to solving this problem. However, existing CTTA methods often rely on unreliable supervisory signals, igniting a self-reinforcing cycle of error accumulation that culminates in catastrophic performance degradation. To overcome these challenges, we propose a CTTA via Semantic-Prompt-Enhanced Graph Clustering (SPEGC) for medical image segmentation. First, we design a semantic prompt feature enhancement mechanism that utilizes decoupled commonality and heterogeneity prompt pools to inject global contextual information into local features, alleviating their susceptibility to noise interference under domain shift. Second, based on these enhanced features, we design a differentiable graph clustering solver. This solver reframes global edge sparsification as an optimal transport problem, allowing it to distill a raw similarity matrix into a refined and high-order structural representation in an end-to-end manner. Finally, this robust structural representation is used to guide model adaptation, ensuring predictions are consistent at a cluster-level and dynamically adjusting decision boundaries. Extensive experiments demonstrate that SPEGC outperforms other state-of-the-art CTTA methods on two medical image segmentation benchmarks. The source code is available at https://github.com/Jwei-Z/SPEGC-for-MIS.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SPEGC: Continual Test-Time Adaptation via Semantic-Prompt-Enhanced Graph Clustering for Medical Image Segmentation》** 的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：医学图像分割模型在临床部署时，常因不同扫描仪、采集协议导致的“领域偏移”（Domain Shift）而性能下降。
*   **核心问题**：持续测试时自适应（CTTA）要求模型在不断变化的无标签目标域流数据中进行在线更新。现有方法（如熵最小化）在面对剧烈偏移时，容易产生错误累积（Self-reinforcing error）和灾难性遗忘（Catastrophic forgetting），导致模型崩溃。
*   **研究动机**：利用数据内在的高阶结构化信息（图聚类）来提供比像素级伪标签更稳健的监督信号。

### 2. 方法论
SPEGC 框架主要由两个核心模块组成：
*   **语义提示特征增强（SPFE）**：
    *   **不确定性采样**：利用 MC Dropout 评估预测的不确定性，筛选出低不确定性的稳定节点构建图。
    *   **解耦提示池**：设计了“共性提示池”（捕获跨域共享语义）和“异质性提示池”（捕获域特有风格）。通过注意力机制检索全局上下文并注入局部特征，增强特征对噪声的抵抗力。
*   **可微图聚类求解器（DGCS）**：
    *   **最优传输（OT）建模**：将图的边稀疏化和聚类问题重新表述为带熵正则化的最优传输问题。
    *   **Sinkhorn 迭代**：利用 Sinkhorn 算法以端到端可微的方式，从原始噪声相似度矩阵中提炼出反映语义一致性的高阶结构化矩阵 $S^*$。
*   **损失函数**：
    *   **图一致性损失 ($L_G$)**：利用提炼的结构引导模型预测，确保结构相似的节点具有一致的语义输出。
    *   **聚类损失 ($L_C$)**：约束共性提示池的学习，确保模型保留核心跨域知识，缓解遗忘。

### 3. 实验设计
*   **数据集**：
    *   **视网膜底像分割（OD/OC）**：包含 RIM-ONE, REFUGE, ORIGA 等 5 个不同中心的域。
    *   **息肉分割**：包含 BKAI, CVC-ClinicDB, ETIS, Kvasir 等 4 个中心的域。
    *   **3D 医学影像**：在 M&MS 心脏 MRI 数据集上验证了扩展性。
*   **Benchmark 与对比方法**：
    *   以 **ResUNet-50** 为骨干网络。
    *   对比了 6 种 SOTA 方法：SAR (熵最小化), DomainAdaptor (BN 统计), NC-TTT (噪声对比), VPTTA (视觉提示), GraTa (梯度对齐), TTDG (图匹配)。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA RTX 3090 GPU** 上运行。
*   **效率指标**：
    *   特征池大小为 3 时，FLOPs 为 5.8G，推理时间约为 0.521s/img，显存占用约 3276MB。
    *   特征池扩大到 15 时，计算量会激增至 120.3G，推理时间延长至 4.774s/img。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单源到多目标的顺序自适应实验。
    *   **长程 CTTA 实验**：进行了 5 轮连续循环测试，验证抗遗忘能力。
    *   **混合分布偏移实验**：模拟更复杂的随机多域流场景。
*   **消融实验**：详细拆解了图聚类、不确定性采样、两种提示池及损失函数的贡献。
*   **超参数分析**：对聚类类别数 $Z$、提示数 $M$、特征池大小等进行了敏感性分析。
*   **评价客观性**：采用 Dice (DSC)、Emax、Sα 及 ASSD 等多维度指标，并提供了均值与标准差，实验设计严谨且充分。

### 6. 主要结论与发现
*   **性能领先**：SPEGC 在视网膜和息肉分割任务上均显著优于现有 SOTA 方法（如在视网膜任务上平均 DSC 达到 84.37%）。
*   **抗遗忘与抗累积**：在长程 CTTA 测试中，SPEGC 的性能退化（1.27%）远低于 GraTa（3.52%）等方法，证明了结构化引导的稳定性。
*   **结构化优势**：在息肉分割等目标细小、背景复杂的任务中，基于图结构的自适应比基于熵的方法更不容易陷入局部错误。

### 7. 优点（亮点）
*   **端到端可微**：通过最优传输理论解决了图聚类在深度学习中难以微分优化的难题。
*   **特征解耦**：通过共性和异质性提示池的设计，既实现了新域的快速自适应，又保留了旧域的知识。
*   **高阶监督**：不再依赖脆弱的像素级伪标签，而是利用群体一致性进行自我调节。

### 8. 不足与局限
*   **计算开销**：图构建和 Sinkhorn 迭代的计算复杂度随节点数量呈二次方增长，这限制了在极高分辨率或超大 Batch 场景下的应用。
*   **超参数敏感性**：聚类数量 $Z$ 的选择对结果有一定影响，不同解剖结构的图像可能需要预先调整该参数。
*   **实时性挑战**：虽然在 3090 上能运行，但相比于简单的 BN 统计更新，其推理延迟较高，可能影响极高实时要求的临床场景。

（完）
