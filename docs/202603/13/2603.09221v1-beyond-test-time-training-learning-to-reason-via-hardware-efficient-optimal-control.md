---
title: "Beyond Test-Time Training: Learning to Reason via Hardware-Efficient Optimal Control"
title_zh: 超越测试时训练：通过硬件高效的最优控制学习推理
authors: "Peihao Wang, Shan Yang, Xijun Wang, Tesi Xiao, Xin Liu, Changlong Yu, Yu Lou, Pan Li, Zhangyang Wang, Ming Lin, René Vidal"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09221v1"
tags: ["query:tta"]
score: 8.0
evidence: 引入测试时控制层（TTC）在推理时进行规划
tldr: 本研究针对大语言模型缺乏原生推理与规划能力的问题，提出了测试时控制（TTC）层。该方法将推理建模为最优控制问题，在推理阶段通过潜在状态的有限时界LQR规划来实现目标导向的行动选择。为了保证效率，研究团队开发了基于辛表述的硬件高效LQR求解器并实现为融合CUDA算子。作为适配器集成到预训练模型后，TTC显著提升了数学推理性能，为超越传统测试时训练的推理机制提供了新路径。
selection_source: fresh_fetch
motivation: 现有的语言模型主要依赖关联记忆，缺乏像人类一样通过预测未来状态和选择目标导向行动来进行原生推理和规划的能力。
method: 提出了TTC层，将有限时界LQR规划嵌入神经架构，并利用硬件高效的融合CUDA内核实现并行化的最优控制计算。
result: "在MATH-500上性能提升高达27.8%，在AMC和AIME上的Pass@8指标提升了2至3倍，显著增强了模型的数学推理能力。"
conclusion: 将最优控制作为架构组件嵌入模型，是实现超越测试时训练、提升大模型推理能力的一种高效且可扩展的机制。
---

## 摘要
联想记忆长期以来一直是序列模型设计的基础。除了召回，人类还通过预测未来状态并选择目标导向的行动来进行推理，现代语言模型日益需要这种能力，但其原生架构并未对其进行编码。虽然先前的工作使用了强化学习或测试时训练，但规划在模型架构中仍然是外部化的。我们将推理建模为最优控制问题，并引入了测试时控制（TTC）层。该层在推理时对潜在状态执行有限时域 LQR 规划，在神经网络架构中表示价值函数，并将其作为嵌套目标，从而在预测前实现规划。为了确保可扩展性，我们推导了一种基于辛表述（symplectic formulation）的硬件高效 LQR 求解器，并将其实现为融合 CUDA 内核，从而以极低的开销实现并行执行。作为适配器集成到预训练大语言模型（LLM）中后，TTC 层在 MATH-500 上的数学推理性能提升了高达 27.8%，在 AMC 和 AIME 上的 Pass@8 提升了 2 到 3 倍，这表明将最优控制嵌入为架构组件，为超越测试时训练的推理提供了一种有效且可扩展的机制。

## Abstract
Associative memory has long underpinned the design of sequential models. Beyond recall, humans reason by projecting future states and selecting goal-directed actions, a capability that modern language models increasingly require but do not natively encode. While prior work uses reinforcement learning or test-time training, planning remains external to the model architecture. We formulate reasoning as optimal control and introduce the Test-Time Control (TTC) layer, which performs finite-horizon LQR planning over latent states at inference time, represents a value function within neural architectures, and leverages it as the nested objective to enable planning before prediction. To ensure scalability, we derive a hardware-efficient LQR solver based on a symplectic formulation and implement it as a fused CUDA kernel, enabling parallel execution with minimal overhead. Integrated as an adapter into pretrained LLMs, TTC layers improve mathematical reasoning performance by up to +27.8% on MATH-500 and 2-3x Pass@8 improvements on AMC and AIME, demonstrating that embedding optimal control as an architectural component provides an effective and scalable mechanism for reasoning beyond test-time training.

---

## 论文详细总结（自动生成）

# 论文总结：超越测试时训练——通过硬件高效的最优控制学习推理

## 1. 核心问题与整体含义
该论文针对现代大语言模型（LLM）在**推理（Reasoning）**和**规划（Planning）**能力上的原生缺陷提出了挑战。
*   **研究动机**：目前的序列模型（如 Transformer、SSM、线性 RNN）本质上是基于“联想记忆”的系统（类似人类的 System 1 认知），擅长模式匹配和信息检索，但缺乏目标导向的预见和多步规划能力（System 2 认知）。
*   **核心问题**：现有的强化学习（RL）或测试时训练（TTT）通常将规划视为外部过程。作者试图探索：能否将“规划”直接内化为模型架构的一个组件，使模型在生成下一个 Token 之前，在潜在空间内先进行“思考”和“控制优化”？

## 2. 方法论：测试时控制（TTC）层
论文提出了一种全新的架构组件——**测试时控制（Test-Time Control, TTC）层**，其核心思想是将推理过程建模为一个**最优控制问题**。

*   **核心思想**：在模型的前向传播中，TTC 层接收当前的上下文表示作为初始状态，并在内部解决一个有限时界的**线性二次型调节器（LQR）**问题。它通过预测未来的潜在轨迹来选择当前最优的“行动”（即下一个 Token 的表示）。
*   **关键技术细节**：
    *   **LQR 建模**：使用线性状态转移方程和二次代价函数（Cost Function）来模拟推理环境。代价函数反映了推理目标的达成程度。
    *   **硬件高效求解器**：传统的 LQR 求解依赖 Riccati 递归，涉及大量串行计算和矩阵求逆，难以在 GPU 上扩展。作者利用**辛表述（Symplectic Formulation）**推导了一种新型求解器，将串行递归转化为可并行的矩阵乘积。
    *   **融合 CUDA 内核**：为了最小化 I/O 开销，作者将整个 LQR 求解过程实现为融合的 CUDA Kernel，实现了比传统 KKT 求解器高出 10 倍以上的吞吐量。
    *   **上下文参数化**：LQR 的参数（动力学矩阵 $A, B$ 和代价矩阵 $Q, R$）不是固定的，而是根据当前上下文动态生成的，使模型能适应不同的推理任务。

## 3. 实验设计
论文在合成逻辑任务和真实世界的高难度数学任务上进行了验证：
*   **数据集/场景**：
    1.  **数独（Sudoku）**：用于评估逻辑约束传播和长程规划能力。
    2.  **数学推理**：包括 MATH-500、AMC（美国数学竞赛）、AIME 2024/2025（美国数学邀请赛）。
*   **Benchmark 与对比方法**：
    *   **基础架构对比**：Transformer、Mamba、Mamba2、GDN、Samba。
    *   **测试时训练对比**：MesaNet（基于局部最优测试时训练的方法）。
    *   **基座模型**：以 Llama-3-Instruct-7B 为基座，将 TTC 作为适配器（Adapter）插入。

## 4. 资源与算力
*   **硬件使用**：论文在效率基准测试中明确提到了使用 **NVIDIA H200 GPU**。
*   **训练细节**：模型在 OpenThoughts2-114K 数据集以及作者策展的 800K 推理数据上进行了微调。
*   **算力说明**：虽然论文详细展示了 TTC 层在 H200 上的吞吐量和内存占用随序列长度/规划时界的变化，但**未明确给出**训练整个 TTC-Net 模型所消耗的总 GPU 小时数或使用的集群规模。

## 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从合成任务（数独）到极具挑战性的数学竞赛题（AIME），实验设置较为全面。
*   **消融实验**：作者进行了多组消融实验，验证了时间异质性参数化、时界采样策略以及 TTC 层与 Attention 层的交替比例对性能的影响。
*   **客观性评价**：实验对比了多种最新的序列模型架构，并采用了 Pass@8 等标准指标。特别是在 AIME 这种基座模型几乎得分为零的极难任务上，TTC 展示了显著的性能突破，证明了其有效性。

## 6. 主要结论与发现
*   **性能飞跃**：TTC-Net 在 MATH-500 上比基座模型提升了 27.8%，在 AIME 上的 Pass@8 提升了 2-3 倍。
*   **测试时扩展性（Test-time Scaling）**：发现增加 TTC 层的规划步数（Horizon $T$）能单调提升推理准确率，这为 LLM 提供了一种除“增加模型参数”和“增加训练数据”之外的新性能提升维度。
*   **内化价值函数**：TTC 层成功在神经架构中内化了价值函数，实现了“预测前先规划”。

## 7. 优点
*   **理论与工程结合**：将经典控制理论（LQR）与现代深度学习架构融合，并针对 GPU 硬件进行了深度优化（辛表述求解器）。
*   **高效性**：通过 CUDA Kernel 融合，使得复杂的在线规划开销极低，能够作为轻量级适配器集成。
*   **原生支持 System 2**：不同于外部的搜索算法（如 MCTS），TTC 是在前向传播中自动完成规划的。

## 8. 不足与局限
*   **理论解释性**：虽然单个 TTC 层有明确的最优控制目标，但多层 TTC 在深度网络中如何协同工作、其联合优化的动力学过程仍缺乏深层的理论分析。
*   **线性假设限制**：LQR 假设状态转移是线性的，这可能限制了其对极其复杂的非线性推理环境的建模能力。
*   **任务覆盖面**：目前的显著提升主要集中在数学和逻辑推理，在通用创意写作或多模态任务上的表现尚未验证。

（完）
