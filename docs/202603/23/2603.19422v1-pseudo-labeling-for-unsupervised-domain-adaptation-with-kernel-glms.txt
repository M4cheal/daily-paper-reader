Title: Pseudo-Labeling for Unsupervised Domain Adaptation with Kernel GLMs

URL Source: https://arxiv.org/pdf/2603.19422v1

Published Time: Mon, 23 Mar 2026 00:08:22 GMT

Number of Pages: 55

Markdown Content:
# Pseudo-Labeling for Unsupervised Domain Adaptation with Kernel GLMs 

Nathan Weill ∗ Kaizheng Wang †

This version: March 2026 

Abstract 

We propose a principled framework for unsupervised domain adaptation under covariate shift in kernel Generalized Linear Models (GLMs), encompassing kernelized linear, logistic, and Poisson regression with ridge regularization. Our goal is to minimize prediction error in the target domain by leveraging labeled source data and unlabeled target data, despite differences in covariate distributions. We partition the labeled source data into two batches: one for training a family of candidate models, and the other for building an imputation model. This imputation model generates pseudo-labels for the target data, enabling robust model selection. We establish non-asymptotic excess-risk bounds that characterize adaptation performance through an “effective labeled sample size,” explicitly accounting for the unknown covariate shift. Experiments on synthetic and real datasets demonstrate consistent performance gains over source-only baselines. 

Keywords: Covariate shift, kernel GLMs, pseudo-labeling, unsupervised domain adaptation. 

# 1 Introduction 

The standard machine learning framework relies on the fundamental assumption that training and test datasets are drawn from the same underlying distribution (Vapnik, 1999). However, in realistic deployment scenarios, this assumption is frequently violated. A canonical instance of distribution shift is covariate shift (Shimodaira, 2000; Ben-David et al., 2010), where the marginal distribution of features varies between the source and target domains, while the conditional distribution of the output label remains stable. This phenomenon is pervasive in high-stakes applications, from personalized medicine to computer vision. It is well-documented that covariate shift can severely degrade predictive performance (Koh et al., 2021). In many applications, obtaining labels for the target population is prohibitively expensive, whereas collecting unlabeled covariates is straightforward. This motivates the study of unsupervised domain adaptation (UDA), where the learning algorithm must generalize to the target domain using labeled source data and only unlabeled target covariates. We investigate UDA for ridge-regularized Generalized Linear Models (GLMs) and their kernel extensions. In this regime, simply applying a model trained on the source is often suboptimal, even if the model is well-specified. Standard learning algorithms tune regularization hyperparameters to balance bias and variance on the source 

distribution, ignoring regions of the covariate space where the target density is high but source data is scarce. Consequently, achieving optimal target performance requires an adaptive strategy that leverages unlabeled target data to recalibrate the estimator.   

> ∗Department of IEOR, Columbia University. Email: nmw2142@columbia.edu
> †Department of IEOR and Data Science Institute, Columbia University. Email: kw2934@columbia.edu

1

> arXiv:2603.19422v1 [stat.ML] 19 Mar 2026

Contributions We address this challenge by developing a pseudo-labeling framework tailored for kernel GLMs. The approach enables model selection for the target domain using only unlabeled data. Our contributions are two-fold: 

• Methodology: We propose a procedure that partitions the labeled source data into two batches. The first batch is used to train a family of candidate models, while the second trains an imputation model to generate pseudo-labels for the target covariates. These pseudo-labels serve as a proxy for ground truth, allowing us to select the candidate model that minimizes the estimated target risk. We provide precise guidance for tuning the imputation model to optimize the final selection. 

• Theory: We derive non-asymptotic excess risk bounds for ridge-regularized kernel GLMs in the UDA setting. Our bounds quantify the information transfer via an “effective labeled sample size” that accounts for the discrepancy between source and target spectral properties. This theoretical result demonstrates that our method automatically adapts to the unknown covariate shift. 

Related Work The theoretical landscape of transfer learning largely focuses on settings where the learner has access to labeled target data or a known target covariate distribution (Ben-David et al., 2010; Kpotufe and Martinet, 2021). Such assumptions simplify adaptation by allowing for direct risk estimation or precise density ratio calculation. In contrast, we address the strictly unsupervised regime, where the target distribution is unknown and observed only through a finite set of unlabeled covariates. This necessitates inferring the structure of the shift and adapting the estimator solely from the geometry of the unlabeled data, without the guidance of target labels. The necessity of adaptation under covariate shift depends on the modeling regime. In the 

parametric setting, if the model is well-specified, standard Maximum Likelihood Estimation (MLE) on the source is asymptotically efficient for the target, rendering adaptation unnecessary (Ge et al., 2024). However, if the parametric model is mis-specified, the source MLE is biased for the target; the standard remedy is importance weighting (Shimodaira, 2000; Sugiyama et al., 2008) or its doubly robust variants utilizing pseudo-outcomes (Liu et al., 2023). While statistically consistent, these methods often suffer from high variance and the difficulty of estimating density ratios in high dimensions. In the nonparametric regime we consider, the situation differs. Even if the model is well-specified (e.g., the true function lies in an RKHS), regularization is required to ensure finite-sample generalization. Recent works have established theoretical guarantees for reweighting-based adaptation in kernel ridge regression (KRR) (Ma et al., 2023) and kernel methods with more general losses (Feng et al., 2023). However, these approaches remain dependent on the existence and accurate estimation of density ratios. An alternative that avoids density estimation is to adapt the regularization directly. Wang (2026) addressed this for KRR using pseudo-labels to estimate the target error for selecting the optimal regularization. Our work operates in this same nonparametric regime, extending the pseudo-labeling paradigm to the broader and technically more demanding class of kernel GLMs. The strategy of filling missing labels with model predictions has driven empirical success in deep learning (Lee, 2013; Sohn et al., 2020). Theoretical analysis of pseudo-labeling has mostly focused on classification under cluster assumptions or margin conditions (Liu et al., 2021; Wei et al., 2021; Cai et al., 2021), which do not translate to the continuous responses covered by our GLM framework. 

Notations The constants c1, c 2, C 1, C 2, · · · may differ from line to line. We use the symbol [n] as a shorthand for {1, 2, · · · , n } and | · | to denote the absolute value of a real number or cardinality of a set. For nonnegative sequences {an}∞ 

> n=1

and {bn}∞

> n=1

, we write an ≲ bn or an = O (bn) if there exists a positive constant C such that an ≤ Cb n. In addition, we write an ≍ bn if an ≲ bn

2and bn ≲ an; an = o (bn) if an = O (cnbn) for some cn → 0. Notations with tildes (e.g., eO ) hide logarithmic factors. For a matrix A, we use ∥A∥2 = sup ∥x∥2=1 ∥Ax ∥2 to denote its spectral norm. For a bounded linear operator A between two Hilbert spaces H1 and H2, we define its operator norm ∥A∥ = sup ∥u∥H1 =1 ∥Au ∥H2 . For a bounded, self-adjoint linear operator A mapping a Hilbert space H to itself, we write A ⪰ 0 if ⟨u, Au ⟩ ≥ 0 holds for all u ∈ H; in that case, we define 

∥u∥A = p⟨u, Au ⟩. For any u and v in a Hilbert space H, their tensor product u ⊗ v is a linear operator from H to itself that maps any w ∈ H to ⟨v, w⟩u. Define ∥X∥ψα = sup p≥1

p−1/α E1/p |X|p

for a random variable X and α ∈ { 1, 2}. If X is a random element in a separable Hilbert space H,then we let ∥X∥ψα = sup ∥u∥H=1 ∥⟨ u, X⟩∥ ψα , α ∈ { 1, 2}.

# 2 Problem Setup 

2.1 Generalized linear models under covariate shift 

Let {(xi, y i)}ni=1 and {(x0i, y 0i)}n0 

> i=1

denote samples drawn from the source and target distributions, respectively. Here, xi, x0i ∈ X are covariates in a compact metric space X , and yi, y 0i ∈ R are real-valued responses. In the Unsupervised Domain Adaptation (UDA) setting, the target labels 

{y0i}n0 

> i=1

are unobserved. Our goal is to learn a predictive model using the labeled source data 

{(xi, y i)}ni=1 and the unlabeled target covariates {x0i}n0 

> i=1

that achieves low error on the target population. We assume that both domains share a common Generalized Linear Model (GLM) (Nelder and Wedderburn, 1972) in a Reproducing Kernel Hilbert Space (RKHS) F. Specifically, conditioned on 

x, the response y is drawn from an exponential family distribution whose density function has the form: 

p(y|x) = h(y, τ ) exp 

 yf ∗(x) − a(f ∗(x)) 

d(τ )



, (2.1) where a : R → R is a known, strictly convex log-partition function; h and d are known functions; 

τ > 0 is a (possibly unknown) dispersion parameter; and f ∗ belongs to the class F. The conditional mean of the response is given by the canonical link: 

E[y | x] = a′(f ∗(x)) . (2.2) Up to an affine transform, the corresponding negative log-likelihood function is: 

ℓ(f (x), y ) := a(f (x)) − yf (x), ∀f ∈ F . (2.3) Classical examples include linear regression [a(u) = u2/2], logistic regression [a(u) = log (1 + eu)], and Poisson regression [a(u) = eu]. The RKHS F is generated by a continuous, symmetric, positive semi-definite kernel K : X × X → 

R. By the Moore-Aronszajn Theorem (Aronszajn, 1950), there exists a feature space (Hilbert space) 

H with inner product ⟨· , ·⟩ H and a mapping Φ : X → H such that K(x, x′) = ⟨Φ( x), Φ( x′)⟩H for all 

x, x′ ∈ X . The RKHS is defined as the class of linear functionals on the feature space: 

F = {fθ : X → R | fθ(x) = ⟨Φ( x), θ⟩H, θ ∈ H} . (2.4) Equipped with the norm ∥fθ∥F := ∥θ∥H, F is a Hilbert space isometric to H.Below are some frequently used kernels and associated RKHS’s (Wainwright, 2019). 

Example 2.1 (Linear and affine kernels) . Let X ⊂ Rd. The linear kernel K(z, w) = z⊤w and the affine kernel K(z, w) = 1 + z⊤w yield the RKHS F of all linear and affine functions respectively, of dimension D = d and D = d + 1 respectively. 

3Example 2.2 (Polynomial kernels) . Let X ⊂ Rd. The inhomogeneous polynomial kernel of degree 

m ≥ 2 is K(z, w) = (1 + z⊤w)m. It yields the RKHS F of all polynomials of degree m or less, of dimension D =  d+mm

.

The function spaces in Examples 2.1 and 2.2 are finite-dimensional. The following example is infinite-dimensional. 

Example 2.3 (First-order Sobolev kernel) . Let X = [0 , 1] and K(z, w) = min {z, w}. Then the first-order Sobolev space is 

F =



f : [0 , 1] → R | f (0) = 0 ,

Z 10

f ′(x) 2 dx < ∞



.

We adopt the following standard assumptions for the UDA setting: 

Assumption 2.1 (GLM in RKHS and Random Design) . The source dataset D = {(xi, y i)}ni=1 and target dataset D0 = {(x0i, y 0i)}n0 

> i=1

are independent, with samples drawn i.i.d. within each domain. 

• Covariate Shift: The covariate distributions for source and target are P and Q, respectively. We assume supp (Q) ⊆ supp (P). The second-moment operators Σ = Ex∼P [Φ( x) ⊗ Φ( x)] and 

Σ0 = Ex∼Q [Φ( x) ⊗ Φ( x)] are trace-class. 

• Well-Specified Model: There exists a true function f ∗ = fθ∗ ∈ F that generates the responses in both domains according to the distribution in (2.1) .

• Finite Noise Variance: Define noise variables εi = −yi + a′ (f ∗(xi)) and ε0i = −y0i +

a′ (f ∗(x0i)) . We assume that conditioned on the covariates, {εi}ni=1 and {ε0i}n0 

> i=1

have finite second moments. 

2.2 Ridge regularization for kernel GLMs 

For any predictive model f ∈ F , we evaluate its performance via the expected negative log-likelihood on the target population: 

R(f ) = E(x,y )∼Q [a(f (x)) − yf (x)] = Ex∼Q [a(f (x)) − f (x)a′(f ∗(x))] . (2.5) Under Assumption 2.1, R(f ) is minimized by f ∗. We define the excess risk as: 

R(f ) = R(f ) − R(f ∗) = Ex∼Q 

h

a(f (x)) − a(f ∗(x)) − a′(f ∗(x))( f (x) − f ∗(x)) 

i

. (2.6) This quantity, which is the expected Bregman divergence defined by a, measures the discrepancy between the predicted conditional distribution and the truth. A standard approach to estimating f ∗ is to minimize the regularized empirical risk on the source data: 

ˆfλ = argmin  

> f∈F

(

1

n

> n

X

> i=1

(a(f (xi)) − yif (xi)) + λ

2 ∥f ∥2

> F

)

, (2.7) where λ > 0 is a regularization hyperparameter balancing data fit and smoothness. By the Representer Theorem (Schölkopf et al., 2001), the solution lies in the span of the source data: 

ˆfλ(·) = Pni=1 αiK(xi, ·). The coefficient vector ˆα ∈ Rn is the solution to the convex optimization problem: 

min 

> α∈Rn

 1

n



1⊤a(α) − y⊤Kα 



+ λ

2 α⊤Kα 



, (2.8) 4where Kij = K(xi, xj ), y = ( y1, . . . , y n)⊤, and a(α) = ( a([ Kα ]1), . . . , a ([ Kα ]n)) ⊤.For source-only learning, λ is typically selected to minimize the source validation error. However, under covariate shift, an optimal penalty for the source may be suboptimal for the target, particularly if the target distribution is concentrated in directions where the source provides weak coverage. Ideally, if we had access to target labels {y0i}n0

> i=1

, we would select the parameter λ minimizing the empirical target risk: 

1

n0

> n0

X

> i=1



a( ˆfλ(x0i)) − y0i ˆfλ(x0i)



. (2.9) Due to missing labels, we require a strategy to estimate the target performance using only the unlabeled covariates. 

# 3 Methodology 

We propose an adaptive method for unsupervised domain adaptation with kernel ridge GLM (KRGLM). The core idea is to construct a computable proxy for the empirical risk (2.9). Note that the conditional mean response E(y0i|x0i) = a′(f ∗(x0i)) is given by the true model f ∗. We will build an auxiliary “imputation model” ˜f trained on part of the labeled source data and then use the estimated conditional mean ˜y0i = a′( ˜f (x0i)) to replace y0i in (2.9). The full procedure is detailed in Algorithm 1. 

Soft vs. Hard Labeling. For classification tasks such as logistic regression, our method utilizes “soft” pseudo-labels ˜y0i = 1 /(1+ e− ef (x0i)) ∈ (0 , 1) —representing the estimated conditional probability— rather than “hard” labels 1( ef (x0i) ≥ 0) ∈ { 0, 1}. This distinction is critical. While hard pseudo-labeling is common in semi-supervised learning to enforce low-entropy decision boundaries (Lee, 2013; Sohn et al., 2020; Liu et al., 2021; Wei et al., 2021; Cai et al., 2021), our objective is to minimize the negative log-likelihood to recover the true conditional distribution. Using hard labels would destroy the calibration information necessary for minimizing the log-likelihood, introducing severe bias into the risk proxy (3.1) by forcing candidate models to fit extreme probabilities (0 or 1) rather than the true underlying uncertainty. 

Implementation and Tuning Strategy. The procedure separates the role of model training (Step 2a) from validation (Step 2b & 4), ensuring that the data used to score the models ( D2) is independent of the data used to train them ( D1). The computational cost is dominated by solving the kernel GLM optimization, which is efficient for standard losses. Our theoretical analysis (presented in Section 4) provides specific guidance on parameter choices: 

• Split Ratio : We typically set n1 = ⌊n/ 2⌋, using half the source data for training candidates and half for imputation. This balanced split is standard in split-sample validation methods. 

• Candidate Grid ( Λ): The grid should be sufficiently dense to cover the bias-variance spectrum. We typically set Λ as a geometric sequence ranging from O(n−1) (low bias, high variance) to O(1) 

(high bias, low variance), with a cardinality of roughly O(log n).

• Imputation Penalty ( ˜λ): This is the critical hyperparameter. Unlike standard supervised learning, where ˜λ would be tuned to minimize source prediction error, here it must be chosen to optimize the model selection capability of the pseudo-labels. Our theory suggests setting ˜λ ≍ n−1

up to logarithmic factors. This rate typically corresponds to “undersmoothing” the imputation 5Algorithm 1 Pseudo-Labeling for Kernel GLM  

> 1:

Input : Source data {(xi, y i)}ni=1 , unlabeled target covariates {x0i}n0

> i=1

, training split size n1 ∈ [n],candidate penalty grid Λ ⊂ (0 , ∞), imputation penalty ˜λ > 0. 

> 2:

Step 1 (Data Splitting) . Randomly partition the source data into two disjoint subsets: D1 of size n1 (for candidate training) and D2 of size n2 = n − n1 (for imputation).  

> 3:

Step 2 (Training) . 

> 4:

(a) Candidate Models : For each λ ∈ Λ, train a candidate ˆfλ on D1 by solving the program 

min  

> f∈F

 1

n1

X

> (x,y )∈D 1

(a(f (x)) − yf (x)) + λ

2 ∥f ∥2

> F



. 

> 5:

(b) Imputation Model : Train the imputation model ˜f on D2 with penalty ˜λ by solving the program 

min  

> f∈F

 1

n2

X

> (x,y )∈D 2

(a(f (x)) − yf (x)) + ˜λ

2 ∥f ∥2

> F



. 

> 6:

Step 3 (Pseudo-Labeling) . Generate pseudo-labels for the target data using the conditional mean of the imputation model: 

˜y0i = a′( ˜f (x0i)) , for i = 1 , . . . , n 0. 

> 7:

Step 4 (Model Selection) . Select the candidate ˆλ ∈ Λ that minimizes the pseudo-target risk: 

1

n0

> n0

X

> i=1



a( ˆfλ(x0i)) − ˜y0i ˆfλ(x0i)



. (3.1)  

> 8:

Output : The selected model ˆf = ˆfˆλ.model—prioritizing low bias in the pseudo-labels over low variance—which is crucial for accurately ranking the candidate models on the target domain. 

# 4 Theoretical Guarantees on the Adaptivity 

This section develops theoretical guarantees for Algorithm 1. We show that the resulting estimator adapts both to the target distribution’s structure and to the unknown covariate shift. Our analysis introduces an effective sample size that quantifies how informative the labeled source data are for the target regression task. 

4.1 Assumptions 

Our analysis of kernel ridge GLM under covariate shift (Assumption 2.1) necessitates only mild regularity conditions, that one can verify are satisfied in most common scenarios. First, we need some assumptions on the kernel function K. Mercer’s theorem (see for instance Wainwright (2019)) guarantees the existence of an orthonormal eigen-expansion of the PSD kernel with respect to a measured function class when it is continuous and satisfies the Hilbert-Schmidt condition. 6Assumption 4.1 (Kernel regularity) . We assume Mercer’s theorem holds under both source and target distributions, impose a uniform boundedness assumptions on the eigenfunctions, and a polynomial decay of the eigenvalues: 

• For any x, z ∈ X , K(x, z) can be expanded as 

> ∞

X

> j=1

μj ϕj (x)ϕj (z) = 

> ∞

X

> j=1

μ0j ϕ0j (x)ϕ0j (z)

where (μj )∞ 

> j=1

and (μ0j )∞ 

> j=1

are sequences of positive eigenvalues 1, (ϕj )∞ 

> j=1

is an orthonormal sequence of eigenfunctions in L2(X ; P), and (ϕ0j )∞ 

> j=1

is an orthonormal sequence of eigenfunctions in L2(X ; Q). The convergence of the infinite series hold absolutely and uniformly. 

• ∃M1 < ∞, ∀j, ∥ϕj ∥∞ ≤ M1, ∥ϕ0j ∥∞ ≤ M1.

• ∃A, A ′ > 0, ∃α ≥ 1, ∀j ≥ 1, μ j ≤ Aj −2, μ 0j ≤ A′j−2α.

The polynomial upper-bound on the spectral decay is a smoothness condition for the kernel: a faster decay (larger α) corresponds to a smoother RKHS, as high-frequency eigendirections contribute less to the kernel. It is satisfied by the first-order Sobolev kernel on X = [0 , 1] (Example 2.3) with 

α = 1 under Q = U[0 , 1] . It is also trivially satisfied for any α by finite-rank kernels such as linear and polynomial kernels (Examples 2.1 and 2.2) which have at most D non-zero eigenvalues. The uniform boundedness of eigenfunctions holds for linear and polynomial kernels on compact domains, as well as for the first-order Sobolev kernel, whose eigenfunctions are sinusoidal. Under Assumption 4.1, the feature map induced by the kernel K can be written as 

Φ : x 7 → (√μ1ϕ1(x), √μ2ϕ2(x), . . . ) .

Remark 4.1. These assumptions on the kernel imply that the covariates are bounded almost surely: there exists a constant M2 < ∞ such that ∥Φ( x)∥ ≤ M2 holds almost surely for x from P and Q.We also assume bounded noise and signal. The noise is only assumed sub-exponential instead of sub-gaussian to include Poisson regression in our analysis. 

Assumption 4.2 (Sub-exponential noise) . Conditioned on X, the noise vector ε satisfies ∥ε∥ψ1 ≤

σ ≤ ∞ .

Assumption 4.3 (Bounded signal) . ∥f ∗∥F ≤ R < ∞.

Remark 4.2. Assumptions 4.1 and 4.3 imply that for any x ∈ supp( P), |f ∗(x)| ≤ C := RM 2.Finally, we assume some smoothness and convexity conditions for the log-partition function: 

Assumption 4.4 (Local strong convexity and boundedness) . We assume that the log-partition function a is strictly convex, smooth, and strongly convex in compact regions: for any ρ > 0, we have 

kρ := inf |z|≤ (ρ+1) C a′′ (z) > 0. Define k = k1. Moreover, a′′ is assumed to be upper bounded and Lips-chitz on compact regions: let Kρ := sup |z|≤ (ρ+1) C a′′ (z) < ∞, Lρ := sup |z|≤ (ρ+1) C a′′′ (z) < ∞, and define K = K1, L = L1. Then, ∀| z| ≤ 2C, k ≤ a′′ (z) ≤ K and ∀| z1|, |z2| ≤ 2C, |a′′ (z1) − a′′ (z2)| ≤ 

L |z1 − z2|.

One can easily check that Assumption 4.4 is satisfied for common GLMs. Table 1 provides simple bounds for the corresponding constants in the three canonical examples.                  

> 1we order them as μ1≥μ2≥ · · · >0and μ01 ≥μ02 ≥ · · · >0. Note that they are also the eigenvalues of the second moment operators Σand Σ0respectively.

7Table 1: Properties of the log-partition function a on [−2C, 2C].

Model a(z) k K L

Linear z2 

> 2

1 1 0

Logistic log(1 + ez ) e2C  

> (1+ e2C)2

1/4 0.1

Poisson ez e−2C e2C e2C

4.2 Main results 

Our main theorem shows that our method adapts to the unknown covariate shift. 

Theorem 4.1 (Excess risk) . Let Assumptions 2.1 and 4.1 to 4.4 hold. Define for μ2 = M 61 σ2

neff = sup 



t ≤ n tΣ0 ⪯ nΣ + μ2

k I



. (4.1) 

Choose any δ ∈ (0 , 1/e ]. Consider Algorithm 1 with n1 = n/ 2, ˜λ = μ2 log 7(n) log( n0/δ )/n , and 

Λ = 2j−1μ2/n : 1 ≤ j ≤ ⌈ log 2 n⌉ + 1 

Then there exists a function ζ polylogarithmic in (n, n 0, δ −1) multiplied by a polynomial in Kk , such that with probability at least 1 − δ:

R( ˆf ) − R(f ∗) ≤ ζ · inf 

> ρ> 0

ρR 2 + σ2

neff 

> ∞

X

> j=1

μ0j

kμ 0j + ρ

 + ζ · R2(1 + L2)μ2

 1

neff 

+ 1

n0



(4.2) The proof is deferred to Appendix B.3. 

Remark 4.3 (Interpretation of Effective Sample Size) . The quantity neff defined in (4.1) serves as a data-dependent measure of the covariate shift. It quantifies the extent to which the source covariance 

Σ “covers” the target covariance Σ0. Crucially, this alignment is not merely a check of input support, but is measured through the geometry of the kernel : the covariance inequality dictates how well the source data captures variance along the principal directions of the RKHS relative to the target distribution. 

• If the source dominates the target (i.e., Σ0 ⪯ BΣ), we have neff ≥ n/B , recovering standard fast rates. 

• If the target Σ0 has high energy in eigen-directions where the source Σ decays rapidly (a mismatch in kernel geometry), neff ≪ n, reflecting the difficulty of adaptation. For instance, consider the first-order Sobolev space (Example 2.3) with P = U[0 , 1] and Q = δ0.5. It is proved in Section B.2 of Wang (2026) that neff ≍ √n.The infimum over ρ in (4.2) captures the optimal bias-variance tradeoff for KRGLM on neff labeled target samples: the term ρR 2 is the regularization bias, which increases with ρ, while σ2

> neff

P∞ 

> j=1
> μ0j
> kμ 0j+ρ

is the variance, which decreases with ρ and reflects how well the target eigendirections are captured given neff effective samples. The infimum over ρ selects the regularization level that optimally 8balances these two competing terms, yielding the same rate as if one had direct access to neff 

labeled target samples. The second term in (4.2) reflects the extra error incurred by the imperfect pseudo-labels. The following corollary shows that this overhead is negligible compared to the first term in our non-parametric setting. 

Corollary 4.1 (Convergence Rate with Effective Sample Size) . Given the polynomial spectral decay assumption μ0j ≤ Aj −2α, and provided n and n0/ log n are sufficiently large, the excess risk satisfies with probability at least 1 − n−1:

R( ˆf ) − R(f ∗) ≲ n− 2α 

> 2α+1
> eff

+ n−10 . (4.3) The ≲ only hides polylogarithmic factors in n and n0, and constants depending on the GLM parameters. The proof is deferred to Appendix B.4. 

Remark 4.4 (Optimality and adaptivity) . The bound established in Corollary 4.1 is sharp. It is easily seen in the bounded density ratio regime dQ/dP ≤ B for some constant B ≥ 1, implying 

neff ≥ n/B . In this setting, we recover the rate (n/B )− 2α 

> 2α+1

, which matches the minimax lower bound for KRR with square loss derived in Theorem 2 of Ma et al. (2023). While their lower bound holds for estimators with full knowledge of Q and B, our Algorithm 1 achieves this rate without any prior information on the target distribution Q or the shift magnitude B, demonstrating its adaptivity. 

# 5 Proof Sketch 

Our main result hinges on two ingredients. Section 5.1 establishes an in-sample oracle inequality for model selection via pseudo-labels under the GLM loss, characterizing how the imperfection of the pseudo-labels propagates into the excess risk of the selected model. This result is generic and independent of the RKHS structure. Section 5.2 then shows how, in our kernel GLM setting, each term of this oracle inequality — the imputation error decomposition and the candidate excess risk bounds — can be controlled via a Taylor expansion of the regularized empirical loss’s gradient around f ∗, yielding the pointwise convergence rate of Theorem 5.2 as a key intermediate result. Section 5.3 explains how these two ingredients are combined to yield Theorem 4.1. 

5.1 Generic model selection analysis with GLM loss 

At the core of our analysis, we need to understand how the pseudo-labeler impacts the model selection. We consider the task of selecting an optimal candidate from a set of m deterministic models f1, . . . , f m to approximate an unknown regression function f ∗, whose conditional mean is characterized through the canonical link a′(f ∗(x)) . We work under a fixed design of unlabeled samples {xi}ni=1 : by treating the candidate models and design points as deterministic and the imputer ˜f as the sole source of randomness, we isolate the effect of pseudo-label errors from the finite-sample fluctuations of the target covariates, which are handled separately as described in Section 5.3. We decompose the pseudo-labels as 

˜f (xi) := f ∗(xi) + Bi + ˜ εi, (5.1) where ˜ε = ( ˜εi)i is a centered random vector and B = ( Bi)i is a deterministic term. They represent the random error and bias of the imputation model. 9Recall that the in-sample risk in a GLM framework is given by the negative log-likelihood: 

Rin (f ) = 1

n

> n

X

> i=1

(a(f (xi)) − a′(f ∗(xi)) f (xi)) , (5.2) and the in-sample excess risk is thus given by 

Rin (f ) = 1

n

> n

X

> i=1

h

a(f (xi)) − a(f ∗(xi)) − a′(f ∗(xi))[ f (xi) − f ∗(xi)] 

i

. (5.3) Since the true function f ∗ is unknown, we will use as in (3.1) a proxy for the in-sample excess risk, a pseudo excess risk for model selection purposes: 

bRin (f ) = 1

n

> n

X

> i=1

h

a(f (xi)) − a( ˜f (xi)) − a′( ˜f (xi))[ f (xi) − ˜f (xi)] 

i

. (5.4) The pseudo-labeling strategy is thus to choose 

ˆj = argmin 

> j∈[m]

ˆRin (fj ), (5.5) and we denote the selected model ˆf = fˆj .We will only impose some regularity conditions, that are verified by the covariate shift setting of the previous section, as shown in the appendix. 

Assumption 5.1. We make the following assumptions: 1. The image of covariates by the true model are bounded almost surely, i.e. there exists a constant 

C > 0 such that |f ∗(xi)| ≤ C, ∀i.2. a is twice differentiable, locally strongly convex and a′′ is locally upper bounded and Lipschitz, i.e. there exist finite constants k > 0, K > 0, L > 0 such that ∀x, y ∈ [−2C, 2C], K ≥ a′′ (x) ≥ k and 

|a′′ (x) − a′′ (y)| ≤ L|x − y|.3. With probability at least 1−δ/ 2, max i∈[n] | ˜f (xi)−f ∗(xi)| ≤ η, for a constant η < min {k/L, C, √kK/L }.4. The imputation random error ˜ε = ( ε1, ..., ε n)⊤ is a zero-mean sub-exponential random vector: 

∃˜σ > 0, ∥ ˜ε∥ψ1 ≤ ˜σ.

Now the objective is to bound the excess risk of the selected model ˆf , relative to the excess risk of the model selected by an oracle that knows the true function f ∗. We denote by fj∗ this oracle model, with 

j∗ = argmin 

> j∈[m]

Rin (fj ).

The following theorem is an oracle inequality for the in-sample excess risk of the selected model: 

Theorem 5.1 (Oracle inequality) . Let δ ∈ (0 , 1) . Then there exists a universal constant C1 > 0

such that under Assumption 5.1, the following holds with probability at least 1 − δ:

Rin ( ˆf ) ≤ inf 

> ϵ> 0



(1 + ϵ)Rin (fj∗ ) + C1Knk 



1 + 1

ϵ



ξ(˜ σ, B, η, δ )



, (5.6) 

with 

ξ(˜ σ, B, η, δ ) = K ˜σ2 log 2( mδ ) + K ∥B∥2 + L2nη 4

k .

10 The proof is deferred to Appendix E. It proceeds by applying the law of cosines for Bregman divergences, which decomposes the true excess risk of any candidate into its pseudo excess risk plus a cross term involving the imputation error ˜f − f ∗, controlled by the local curvature of a from Assumption 5.1. The minimality of the selected model’s pseudo risk then transfers to the true risk up to this cross term — a strategy that mirrors the square loss case, but where the Bregman structure of the GLM loss requires additional care to obtain a sharp bound. The additive overhead in the oracle inequality (5.6) corresponds to the error of the pseudo-labels. It comprises a bias term proportional to ∥B∥2/n , a variance term proportional to ˜σ2 log 2(m)/n , and a perturbation term proportional to L2η4, which arises from the nonlinearity of the GLM loss and is shown to be negligible compared to the other two terms in Section 5.3. Note that in the case of the square loss, L = 0 , so the perturbation term vanishes and the third item in Assumption 5.1 is no longer necessary. The key asymmetry between the bias and variance terms is that ˜σ is a scalar sub-exponential proxy for the noise of the imputer, whereas ∥B∥2 = Pni=1 B2 

> i

accumulates the squared pointwise biases over all n design points. Consequently, as long as log 2(m)/n is small, even a moderate pointwise bias of the imputation model can dominate the variance contribution, making bias the critical quantity to control. Together, that answers our bias-variance trade-off for the imputation model: we should prioritize an undersmoothed imputation model, i.e. with small bias, in order for the pseudo-labels to select a more faithful model. In the case of kernel GLM, the imputation model should thus be trained with a small regularization parameter. 

5.2 Analysis of the KRGLM estimator 

We now show how the oracle inequality of Theorem 5.1 can be applied in our kernel GLM setting by bounding each of its terms — the imputation error decomposition and the candidate excess risk bounds — via a Taylor expansion of the regularized empirical loss’s gradient. For any positive λ, the estimator ˆfλ of (2.7) is only defined implicitly as the minimizer of a nonlinear objective, unlike the closed-form solution available in kernel ridge regression. For ease of presentation, we derive the following expansion in the finite-dimensional setting with X ⊂ Rd and the linear kernel, writing X = ( x1, . . . , xn)⊤ ∈ Rn×d for the source design matrix, ε = ( ε1, . . . , ε n)⊤

for the noise vector, and fθ(x) = θ⊤x. The KRGLM minimization problem (2.7) becomes 

ˆθλ ∈ arg min 

> θ∈Rd

(

1

n

> n

X

> i=1



a(x⊤ 

> i

θ) − yix⊤ 

> i

θ



+ λ

2 ∥θ∥2

)

. (5.7) Setting the gradient of the associated loss to zero at ˆθλ and Taylor-expanding around θ∗ yields: 

ˆθλ − θ∗ = −H−1

 1

n X⊤ε + λθ∗



, (5.8) where the data-dependent Hessian is 

H = 1

n

> n

X

> i=1

Dii xix⊤ 

> i

+ λI, Dii =

Z 10

a′′ (x⊤ 

> i

(θ∗ + t( ˆθλ − θ∗))) dt. (5.9) To isolate the dependence on ˆθλ, we introduce the deterministic Hessian ¯H = 1

> n

Pni=1 a′′ (x⊤ 

> i

θ∗) xix⊤ 

> i

+

λI, obtained by evaluating a′′ at θ∗ rather than along the path. Splitting (5.8) accordingly gives the 11 decomposition: 

ˆθλ − θ∗ = −



λH−1θ∗

| {z }

> bias

+ 1

n ¯H−1X⊤ε

| {z }

> variance

+ 1

n (H−1 − ¯H−1)X⊤ε

| {z }

> perturbation



. (5.10) An equivalent decomposition holds in the general setting via the isometry between the RKHS F and the feature space H (2.4), with X interpreted as a bounded linear operator; see Appendix A.1. Recall that our Algorithm 1 trains a grid { ˆfλ}λ∈Λ of candidate models on the first half D1 of the source data and an imputation model ˜f = ˆf˜λ on the second half D2, which generates pseudo-labels ˜f (x0i) for each target covariate in D0. To bound the terms entering the oracle inequality of Theorem 5.1 applied to D0, we project decomposition (5.10) onto the target second-moment operator Σ0 for the candidate excess risk R( ˆfλ) − R(f ∗), and onto the target design points {x0i} for the imputation error { ˜f (x0i) − f ∗(x0i)}n0

> i=1

. The main difficulty is analyzing the perturbation term, since H depends on ˆθλ itself and is correlated with the noise. We resolve this in two steps. First, a localization argument (Lemma D.1.1) establishes, using only the global strict convexity of a, that ˆθλ

lies in a neighborhood of θ∗ with high probability. Once in this neighborhood, the local properties of a′′ from Assumption 4.4 — strong convexity, boundedness, and Lipschitz continuity — all become available. In particular, the Lipschitz continuity ensures that the relative operator error between H

and ¯H is controlled by the pointwise deviations | ˆfλ(xi) − f ∗(xi)|, reducing the perturbation analysis to a pointwise consistency problem. The following theorem provides the required bound. 

Theorem 5.2 (Pointwise convergence rate) . Consider a ridge-regularized kernel GLM estimator 

ˆfλ trained on n i.i.d. samples from the source distribution P via (2.7) . Let β ∈ (0 , 8) , and choose 

δ ∈ (0 , 1/e ]. Then, under Assumptions 2.1 and 4.1 to 4.4, there exist constants c, c ′, c 1, c 2 > 0, such that for any given x0 ∈ supp( P), when: 



λ ≤ c1  

> R4log β(n)

λ ≥ c2M 21 σ2 log( 8  

> δ)(log n)2+ β/ 2
> n

,

the following inequality holds with probability at least 1 − δ:

ˆfλ(x0) − f ∗(x0) ≤ cM 1 σ log( nδ ) λ−1/4

√n + λ1/4 ∥f ∗∥F

!

. (5.11) 

When setting λ to its lower bound, the result becomes 

ˆfλ(x0) − f ∗(x0) ≤ c′

q

M 31 σn −1/4 

log( nδ )

3

. (5.12) The proof is deferred to Appendix D. 

Bounding the oracle inequality terms via the pointwise rate. We can now carry out the program described above. Projecting (5.10) and applying Theorem 5.2 to control the perturbation yields: 1. Candidate excess risk bounds. Contracting (5.10) against the target second-moment operator 

Σ0 yields a bound on the excess risk R( ˆfλ) − R(f ∗) for each candidate. Informally, after applying union bounds, we obtain up to constant and log factors: 

min  

> λ∈Λ

{R( ˆfλ) − R(f ∗)} ≲ λR 2 ∥Sλ∥ + σ2

n1

Tr( Sλ), (5.13) 12 where Sλ = ( kΣ + λI)−1/2Σ0(kΣ + λI)−1/2. A formal statement can be found in Lemma B.2.1. Using the effective sample size to relate Σ to Σ0, one can easily verify that Tr (Sλ) ≤ 2nneff Tr (( kΣ0 +

λ nneff I)−1Σ0) and ∥Sλ∥ ≤ nkn eff , as shown in Appendix F.3. Thus taking n1 = n/ 2, it reduces to 

min  

> λ∈Λ

{R( ˆfλ) − R(f ∗)} ≲ nneff 

λR 2 + σ2

neff 

Tr(( kΣ0 + nneff 

λI)−1Σ0), (5.14) as proved in Appendix B.3. 2. Imputation error decomposition. Following the insight of Section 5.1, our Algorithm 1 trains the imputation model with a small bias with respect to our Λ grid, by choosing ˜λ ≍ n−1,which also corresponds to the optimal pointwise convergence rate as per Theorem 5.2. Applying (5.10) to ˜θ = ˆθ˜λ and projecting onto the target covariates {x0i}n0 

> i=1

yields a decomposition of 

{ ˜f (x0i) − f ∗(x0i)}n0 

> i=1

into the bias, variance, and perturbation terms entering ξ( ˜σ, B, η, δ ) in Theorem 5.1, and allows to bound them in Lemma B.1.1. Informally, for this choice of ˜λ, we find that the squared variance proxy and the square norm of the bias are of the same order: 

˜σ2 ≲ n0

n2

ˆS2,˜λ , ∥B∥2 ≲ n0

n2

ˆS2,˜λ , (5.15) where ˆS2,˜λ = ( k ˆΣ2 + ˜λI)−1/2 ˆΣ0(k ˆΣ2 + ˜λI)−1/2. To bound the perturbation term n0η4, we recall that η bounds max i∈[n0] ˜f (x0i) − f ∗(x0i) with high-probability. Substituting the rate η ≲ n−1/42

from Theorem 5.2 yields 

n0η4 ≲ n0

n2

. (5.16) 

Pointwise rate. We conclude this section by commenting on the rate obtained in Theorem 5.2. We obtain a O(n−1/4) pointwise convergence rate of the ridge-regularized kernel GLM estimator when 

λ ≍ n−1. This behavior is consistent with the sharp asymptotic theory for linear functionals of kernel ridge regression developed by Tuo and Zou (2024), who analyze point evaluation in Sobolev-equivalent RKHSs: when the input space X is a convex compact subset of Rd and F is equivalent to a Sobolev space of order m > d/ 2, they show in Theorem 7 that λ ≍ n−1 yields a rate of n−1/2+ d/ 4m for linear functionals, with matching upper and lower bounds (hence optimality in their framework). In particular, the first-order Sobolev (Brownian) kernel K(u, v ) = min {u, v } on [0 , 1] (d = m = 1 )satisfies the quadratic eigendecay μj ≲ j−2 and has uniformly bounded eigenfunctions, so both their results and ours predict the same O(n−1/4) pointwise rate. We also emphasize that this pointwise rate is slower than the L2(X ; P)-prediction rate, reflecting the additional difficulty of controlling a point evaluation functional compared to an integrated L2

loss. We show in Appendix B.5 how to recover the optimal prediction rate ˆfλ − f ∗ 

> L2

= O(n−1/3)

in our setting, obtained for λ ≍ n−2/3. This in turn matches the fast rate of O(n− 2rα +α 

> 2rα +α+1

) obtained by Marteau-Ferey et al. (2019) with capacity condition α = 2 (corresponding to our eigendecay 

α = 1 ) and source condition r = 0 (i.e. without any additional smoothness assumption on f ∗ beyond membership in the RKHS) in their Corollary 9. 

5.3 Putting things together 

We now combine the two previous ingredients. First, we can substitute the bounds on the imputation error decomposition from (5.15) and (5.16) with n2 = n/ 2 in the oracle inequality of Theorem 5.1. 13 We obtain informally and up to log factors: 

Rin ( ˆf ) − Rin (f ∗) ≲ min  

> λ∈Λ

{Rin ( ˆfλ) − Rin (f ∗)} + 1

n ˆS2,˜λ .

A formal statement can be found in Lemma B.2.2. By further bridging the empirical second-moment operator ˆS2,˜λ to its population counterparts Σ and Σ0 through neff , and relating the in-sample and out-of-sample excess risks via concentration inequalities, it implies: 

R( ˆf ) − R(f ∗) ≲ min  

> λ∈Λ

{R( ˆfλ) − R(f ∗)} + O

 1

neff 

+ 1

n0



.

A formal statement is presented in Corollary B.2. To obtain Theorem 4.1, it then suffices to substitute the candidate excess risk bounds from (5.14) into the first term. As seen in Corollary 4.1, the overhead due to pseudo-label errors, of order O(1 /n eff + 1 /n 0), is negligible compared to the resulting candidate excess risks of order O(n− 2α 

> 2α+1
> eff

), determined by the complexity of the target distribution. 

# 6 Numerical Experiments 

We conduct experiments on synthetic and real data to show the benefit of our approach over source-only baselines. The code for implementing our experiments is available at https://github. com/nathanweill/KRGLM/ . In particular, we implemented an ad hoc KRGLM solver that can be of independent interest; it is explained in details in Appendix G.1. 

Kernel logistic regression specifics. In the following experiments, we focus on kernel logistic regression, for which the log-partition function is a(u) = log (1 + eu), and its first derivative is the sigmoid function a′(u) = eu 

> 1+ eu

= σ(u) that maps real values to [0 , 1] . If the true underlying function is f ∗, then yi | xi ∼ Bernoulli (σ(f ∗(xi))) . We can thus denote the conditional mean as a probability 

p∗ 

> i

:= σ(f ∗(xi)) . Equivalently, f ∗(xi) = log ( p∗

> i
> 1−p∗
> i

) = logit (p∗ 

> i

). As such, for any estimator f of f ∗, if we denote for each sample pi = σ(f (xi)) , the in-sample version of the GLM risk (2.5) recovers the usual log-loss with "soft labels" or cross-entropy between the true distribution p∗ and the estimate p:

Rin (f ) = 1

n

> n

X

> i=1

a(f (xi)) − a′(f ∗(xi)) f (xi) = 1

n

> n

X

> i=1

[−(1 − p∗ 

> i

) log(1 − pi) − p∗ 

> i

log( pi)] .

6.1 Synthetic data 

For simulation experiments, our feature space X is the unit interval [0 , 1] , and the kernel K is the first-order Sobolev kernel K(u, v ) = min {u, v } from Example 2.3. Let f ∗(x) = 1 .5 cos (2 πx ), ν 0 = U[0 , 1/2] 

and ν1 = U[1 /2, 1] . For any even integer n ≥ 2, we follow the procedure below: 

• Let B = n0.4, P = BB+1 ν0 + 1 

> B+1

ν1 and Q = 1 

> B+1

ν0 + BB+1 ν1. Generate n i.i.d. source samples 

{(xi, y i)}ni=1 with xi ∼ P and yi | xi ∼ Bernoulli (σ(f ∗ (xi))) . Generate n0 = n i.i.d. unlabeled target samples D0 = {x0i}n0 

> i=1

from Q.

• Run Algorithm 1 with n1 = n/ 2, Λ = 2k/(10 n) : k = 0 , 1, · · · , ⌈log 2(10 n)⌉ , and eλ = 1 /(10 n) to get an estimate bf . Use the log-partition function associated to logistic regression a(u) = log (1+ eu).14 4000 8000 16000 32000 10 2  

> 6 × 10 3
> 2 × 10 2

Figure 1: Comparisons of three approaches on a log-log plot. x-axis: n. y-axis: excess risk. Red crosses: pseudo-labeling. Cyan triangles: the oracle method. Blue circles: the naive method. The 1.5 coefficient in front of the cos in f ∗ reflects the fact that the decision boundary for the logistic regression is crucial for training, so the source must contain enough point in the region around 0 to learn decent pseudo-labels. The quantity B controls the strength of the covariate shift. A larger sample size allows for a larger shift. We chose a relatively large covariate shift to better illustrate the relative performance of our method. Our method selects a candidate model that minimizes the risk estimate 

f 7 → 1

|D 0|

X

> x∈D 0



a (f (x)) − f (x)a′( ˜f (x)) 



based on pseudo-labels. We compare it with an oracle selection approach whose risk estimate 

f 7 → 1

|D 0|

X

> x∈D 0

 a (f (x)) − f (x)a′(f ∗(x)) 

is constructed from noiseless labels, and a naive approach that uses the empirical risk 

f 7 → 1

|D 2|

X

> (x,y )∈D 2

(a (f (x)) − yf (x)) 

on D2. The latter is the standard hold-out validation method that ignores the covariate shift. For every n ∈ { 4000 , 8000 , 16000 , 32000 }, we conduct 100 independent runs of the experiment. The results of our numerical experiments are illustrated in Fig. 1, where the axes represent the sample size n and the average estimated excess risk across 100 independent trials, respectively. The linear alignment of the data points on a log-log scale suggests a power-law decay of the excess risk, following an O (n−α) rate for some α > 0. To quantify these decay rates, we applied linear 15 regression to the log-transformed data. The estimated α ’s for the pseudo-labeling, oracle, and naive methods are 0.546(0 .047) , 0.523(0 .051) and 0.439(0 .043) , respectively, represented by the solid lines on the figure. The standard errors (reported in parentheses) were computed via a cluster bootstrap procedure (Cameron et al., 2008), which involved resampling the 100 trials associated with each n over 10,000 iterations. Our proposed method achieves an error exponent that does not differ significantly from the oracle benchmark. Furthermore, our approach significantly outperforms the naive method, which suffers from poor source coverage of the target domain. To visualize the estimator stability, we have included thin shaded regions representing the linear fits of 100 bootstrap replicates. We included in Appendix G.2 the results of the same experiment with a stronger covariate shift controlled by B = n0.45 .

6.2 Real-world data 

The Raisin dataset 2 (Çınar et al., 2020) contains 900 instances of two raisin varieties with 7 numerical features. The task is binary classification. To introduce covariate shift, we follow a usual sub-sampling technique on the first covariate (Quiñonero-Candela et al., 2008): after scaling the data, for each sample xi, look at its first component xi[0] and compute the rejection probability 

pi = min (1 , (xi[0] − c)2/l ), where c = min i(xi[0]) and l > 0 is a parameter to adjust the shift strength (the smaller it is, the larger the shift). Then sample i is assigned to the target out-of-distribution (OOD) set if a draw of the random variable Bernoulli (pi) returns 1, otherwise, it is assigned to the in-distribution (ID) source set. In the rest of this experiment, we selected a shift level l = 3 .To first realize the effect of this shift, we train a ridge-regularized logistic regression baseline only on source data, with a grid of ridge regularization parameters λ. We keep 20% of the ID data and all of the OOD data for evaluation. 

Figure 2: Baseline logistic regression performance on ID and OOD test data, when trained only on ID data, as a function of the ridge regularization parameter λ 

> 2available at https://archive.ics.uci.edu/dataset/850/raisin

16 Table 2: Risk mean estimate across seeds with confidence interval (CI) and standard error (SE).             

> Method Mean 95% CI SE Naive 0.502 [0.463, 0.541] 0.019 Pseudo-labeling 0.428 [0.412, 0.444] 0.008 Oracle 0.373 [0.361, 0.385] 0.006

Fig. 2 plots the results for four different metrics. We can observe that only a specific range of regularization (between 10 −3 and 10 −2) can achieve good performance on the target data, different from the optimal one for the source. The goal of our method is to find a near-optimal value without seeing the target labels used to plot these curves. We realize our pseudo-labeling experiment in the same fashion as for the synthetic data in the previous paragraph, by comparing it to a naive method that selects the best model on source validation data, and oracle method that uses the target labels for selection. We run it for 100 different random seeds for significance and report the results in Table 2. While the underlying task in this example is binary classification, our primary objective is the regression of the conditional probability function. Consequently, we evaluate performance using the excess risk (equivalently, the log-loss), as it is a strictly proper scoring rule that captures the full calibration of the predicted probabilities. For each seed, we first split the OOD data into training (for pseudo and oracle method) and test set for evaluation purposes. Given the limited sample size, we adopted a more robust model selection strategy: instead of a simple hold-out split, we employed repeated 2-fold cross-validation (see for instance Arlot and Celisse (2010); Kim (2009)) (6 repetitions) on the In-Distribution (ID) data. Candidate models were selected based on risk estimates averaged across all folds, and subsequently refitted on the full ID dataset using the chosen hyperparameters. While our theoretical analysis emphasizes the primacy of low bias for the imputation model, controlling variance proves critical as well in the small-sample regime to prevent performance degradation. Empirically, our pseudo-labeling approach consistently outperforms the naive baseline by effectively adapting to the underlying target distribution. More details on the method used, specific hyperparameter values, and additional experiment results are provided in Appendix G.3. 

# 7 Conclusion 

We presented a principled framework for unsupervised domain adaptation in kernel GLMs, utilizing a split-and-fit strategy to perform target-aware model selection via pseudo-labeling. Our theoretical contributions include non-asymptotic excess risk bounds that characterize adaptation through a novel “effective labeled sample size,” while our empirical results confirm consistent gains over source-only baselines. Several future directions are worth exploring. Extending our framework to broader function classes and alternative learning procedures beyond ridge regularization would significantly expand its applicability. Also, our current analysis focuses on a one-step imputation strategy. Developing theoretical insights for iterative refinement, where pseudo-labels are progressively updated in multiple rounds (akin to self-training heuristics), could bridge the gap between rigorous statistical bounds and the empirical success of iterative self-training methods. 17 Acknowledgment 

The research is supported by NSF grants DMS-2210907 and DMS-2515679, and a Data Science Institute seed grant SF-181 at Columbia University. 18 References 

Arlot, S. and Celisse, A. (2010). A survey of cross-validation procedures for model selection. 

Statistics Surveys 4 40–79. 

Aronszajn, N. (1950). Theory of reproducing kernels. Transactions of the American mathematical society 68 337–404. 

Ben-David, S. , Blitzer, J. , Crammer, K. , Kulesza, A. , Pereira, F. and Vaughan, J. W. 

(2010). A theory of learning from different domains. Machine Learning 79 151–175. 

Cai, T. , Gao, R. , Lee, J. and Lei, Q. (2021). A theory of label propagation for subpopulation shift. In International Conference on Machine Learning . PMLR. 

Cameron, A. C. , Gelbach, J. B. and Miller, D. L. (2008). Bootstrap-based improvements for inference with clustered errors. The review of economics and statistics 90 414–427. 

Charlier, B. , Feydy, J. , Glaunès, J. , Collin, F.-D. and Durif, G. (2021). Kernel operations on the GPU, with autodiff, without memory overflows. Journal of Machine Learning Research 22 

1–6. 

Çınar, İ. , Köklü, M. and Taşdemir, Ş. (2020). Raisin. UCI Machine Learning Repository. DOI: https://doi.org/10.24432/C5660T. 

Feng, X. , He, X. , Wang, C. , Wang, C. and Zhang, J. (2023). Towards a unified analysis of kernel-based methods under covariate shift. Advances in Neural Information Processing Systems 

36 73839–73851. 

Ge, J. , Tang, S. , Fan, J. , Ma, C. and Jin, C. (2024). Maximum likelihood estimation is all you need for well-specified covariate shift. In The Twelfth International Conference on Learning Representations .

Hestenes, M. R. and Stiefel, E. (1952). Methods of conjugate gradients for solving linear systems. Journal of Research of the National Bureau of Standards 49 409–436. 

Kim, J.-H. (2009). Estimating classification error rate: Repeated cross-validation, repeated hold-out and bootstrap. Computational Statistics & Data Analysis 53 3735–3745. 

Koh, P. W. , Sagawa, S. , Marklund, H. , Xie, S. M. , Zhang, M. , Balsubramani, A. , Hu, W. , Yasunaga, M. , Phillips, R. L. , Gao, I. , Lee, T. , David, E. , Stavness, I. , Guo, W. ,

Earnshaw, B. A. , Haque, I. S. , Beery, S. , Leskovec, J. , Kundaje, A. , Pierson, E. ,

Levine, S. , Finn, C. and Liang, P. (2021). WILDS: A benchmark of in-the-wild distribution shifts. In Proceedings of the 38th International Conference on Machine Learning , vol. 139 of 

Proceedings of Machine Learning Research . PMLR. 

Kpotufe, S. and Martinet, G. (2021). Marginal singularity and the benefits of labels in covariate-shift. The Annals of Statistics 49 3299–3323. 

Lee, D.-H. (2013). Pseudo-label: The simple and efficient semi-supervised learning method for deep neural networks. In ICML 2013 Workshop on Challenges in Representation Learning .

Liu, H. , Wang, J. and Long, M. (2021). Cycle self-training for domain adaptation. Advances in Neural Information Processing Systems 34 22968–22981. 19 Liu, M. , Zhang, Y. , Liao, K. P. and Cai, T. (2023). Augmented transfer regression learning with semi-non-parametric nuisance models. Journal of Machine Learning Research 24 1–50. 

Ma, C. , Pathak, R. and Wainwright, M. J. (2023). Optimally tackling covariate shift in rkhs-based nonparametric regression. The Annals of Statistics 51 738–761. 

Marteau-Ferey, U. , Ostrovskii, D. , Bach, F. and Rudi, A. (2019). Beyond least-squares: Fast rates for regularized empirical risk minimization through self-concordance. In Proceedings of the Thirty-Second Conference on Learning Theory , vol. 99 of Proceedings of Machine Learning Research . PMLR. 

McCullagh, P. and Nelder, J. A. (1989). Generalized Linear Models . 2nd ed. Chapman and Hall/CRC. 

Nelder, J. A. and Wedderburn, R. W. M. (1972). Generalized linear models. Journal of the Royal Statistical Society. Series A (General) 135 370–384. 

Quiñonero-Candela, J. , Sugiyama, M. , Schwaighofer, A. and Lawrence, N. D. (eds.) (2008). Dataset Shift in Machine Learning . The MIT Press. 

Schölkopf, B. , Herbrich, R. and Smola, A. J. (2001). A generalized representer theorem. In 

International conference on computational learning theory . Springer. 

Shimodaira, H. (2000). Improving predictive inference under covariate shift by weighting the log-likelihood function. Journal of Statistical Planning and Inference 90 227–244. 

Sohn, K. , Berthelot, D. , Li, C.-L. , Zhang, Z. , Carlini, N. , Cubuk, E. D. , Kurakin, A. , Zhang, H. and Raffel, C. (2020). Fixmatch: Simplifying semi-supervised learning with consistency and confidence. In Advances in Neural Information Processing Systems , vol. 33. Curran Associates, Inc. 

Sugiyama, M. , Suzuki, T. , Nakajima, S. , Kashima, H. , Von BÃŒnau, P. and Kawanabe, M. (2008). Direct importance estimation for covariate shift adaptation. Annals of the Institute of Statistical Mathematics 60 699–746. 

Tuo, R. and Zou, L. (2024). Asymptotic theory for linear functionals of kernel ridge regression. Tech. Rep. SS-2024-0256, Statistica Sinica (Preprint). 

Vapnik, V. (1999). The Nature of Statistical Learning Theory . Springer: New York. 

Wainwright, M. (2019). High-Dimensional Statistics: A Non-Asymptotic Viewpoint , vol. 48 of 

Cambridge Series in Statistical and Probabilistic Mathematics . Cambridge University Press. 

Wang, K. (2026). Pseudo-labeling for kernel ridge regression under covariate shift. The Annals of Statistics 54 252–276. 

Wei, C. , Shen, K. , Chen, Y. and Ma, T. (2021). Theoretical analysis of self-training with deep networks on unlabeled data. In International Conference on Learning Representations (ICLR) .20 Appendices 

A Preliminaries for the proofs 21 

A.1 Notations . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 21 A.2 Useful observations on the GLM excess risk . . . . . . . . . . . . . . . . . . . . . . . 22 

B Proof of Theorem 4.1 and corollaries 23 

B.1 Fixed-design analysis . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 23 B.2 Analysis of the selected candidate . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 24 B.3 Proof of the main Theorem 4.1 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 26 B.4 Proof of Corollary 4.1 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 27 B.5 Optimal L2(X ; P) prediction rate . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 28 

C Proofs of Appendix B 29 

C.1 Proof of Lemma B.1.1 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 29 C.2 Proof of Lemma B.1.2 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 31 C.3 Proof of Lemma B.2.1 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 32 C.4 Proof of Lemma B.2.2 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 32 C.5 Proof of Lemma B.2.3 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 33 C.6 Proof of Corollary B.1 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 33 

D Proofs of Section 5.2’s pointwise convergence rate 34 

D.1 Coarse-grained analysis of the estimator consistency . . . . . . . . . . . . . . . . . . 35 D.2 Proof of Theorem 5.2 . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 39 

E Proof of Section 5.1’s in-sample oracle inequality 45 F Useful lemmas 49 

F.1 Lemmas for the GLM estimator . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 49 F.2 Technical lemmas . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 49 F.3 Relating the source covariance operator to the target’s via the effective sample size . 51 

G Complement on the numerical experiments from Section 6 52 

G.1 Implementation details . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 52 G.2 Other synthetic data result . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 52 G.3 Details about the real data experiments . . . . . . . . . . . . . . . . . . . . . . . . . 52 

# A Preliminaries for the proofs 

A.1 Notations 

To facilitate the discussion and analysis, we introduce some notation. When X = Rd for some 

d < ∞ and K(z, w) = z⊤w, ϕ is the identity mapping. We can construct design matrices X =(x1, · · · , xn)⊤ ∈ Rn×d, X0 = (x01 , · · · , x0n0 )⊤ ∈ Rn0×d and the response vector y = (y1, · · · , y n)⊤ ∈

Rn. Define the index set T = {i ∈ [n] : ( xi, y i) ∈ D 1} of the data for training candidate models. In addition, let n1 = |T | and n2 = n − n1. Denote by X1 ∈ R|T |× d and X2 ∈ R(n−|T | )×d the 21 sub-matrices of X by selecting rows whose indices belong to T and [n]\T , respectively. Similarly, denote by y1 ∈ R|T | and y2 ∈ Rn−|T | the sub-vectors of y induced by those index sets. The design matrices can be generalized to operators when X and K are general. In that case, X

and X0 are bounded linear operators defined through 

X : H → Rn, θ 7 → (⟨Φ ( x1) , θ⟩ , · · · , ⟨Φ ( xn) , θ⟩)⊤ ,

X0 : H → Rn0 , θ 7 → (⟨Φ ( x01 ) , θ⟩ , · · · , ⟨Φ ( x0n0 ) , θ⟩)⊤ .

We can define X1 and X2 similarly. With slight abuse of notation, we use X⊤ to refer to the adjoint of X, i.e. X⊤ : Rn → H, u 7 → Pni=1 uiΦ (xi). Similarly, we can define X⊤ 

> 0

, X⊤ 

> 1

and X⊤ 

> 2

. Let 

bΣ0 = 1

> n0

Pn0 

> i=1

Φ (x0i) ⊗ Φ (x0i) , bΣ1 = 1

> n1

P 

> i∈T

Φ (xi) ⊗ Φ (xi) and bΣ2 = 1

> n2

P 

> i∈[n]\T

Φ (xi) ⊗ Φ (xi).We have bΣj = n−1 

> j

X⊤ 

> j

Xj for all j ∈ { 0, 1, 2}.We will also use the following notation for any λ > 0:

ˆfλ(·) = 

D

Φ( ·), ˆθλ

E

, f ∗(·) = ⟨Φ( ·), θ∗⟩ , ˜f (·) = 

D

Φ( ·), ˜θ

E

,

where ˜θ = ˆθ˜λ.

Also denote g(θ) := ( a′(⟨Φ( xi), θ⟩) − yi)⊤ 

> i

the vector of residues such that εi = gi(θ∗).

Remark A.1 (Convention) . We will adopt the convention that the minimum of a function f over an empty set is infinite: 

min  

> λ∈∅

f (λ) = + ∞

A.2 Useful observations on the GLM excess risk 

Note that, on the domain where assumption 4.4 holds, such that the second derivative of the log-partition function is bounded, k ≤ a′′ ≤ K, then, for the true excess risk, we have by Taylor-Lagrange: k

2 Ex∼Q |f (x) − f ∗(x)|2 ≤ R (f ) ≤ K

2 Ex∼Q |f (x) − f ∗(x)|2 , (A.1) and we recognize the usual mean squared estimation error under the target distribution Q.Likewise, for the finite-sample version, we get 

k

2n

> n

X

> i=1

(f (xi) − f ∗(xi)) 2 ≤ R in (f ) ≤ K

2n

> n

X

> i=1

(f (xi) − f ∗(xi)) 2 . (A.2) 

Remark A.2 (Excess risk bounds) . Based on (A.1) and (A.2) and the notations introduced, we have for any λ > 0: k

2 ˆθλ − θ∗ 2Σ0

≤ R ( ˆfλ) ≤ K

2 ˆθλ − θ∗ 2

> Σ0

,

and k

2 ˆθλ − θ∗ 2ˆΣ0

≤ R in ( ˆfλ) ≤ K

2 ˆθλ − θ∗ 2ˆΣ0

.

Remark A.3. The excess risk under the GLM loss is a type of Bregman divergence. The Bregman divergence of the strictly convex and continuously differentiable log-partition function a is defined as: 

Da(x, y ) := a(x) − a(y) − a′(y)( x − y), ∀x, y ∈ Ω,

22 where Ω is a closed convex set in R. Hence, we can rewrite the excess risk and in-sample excess risk respectively as: 

R(f ) = Ex∼Q [Da(f (x), f ∗(x))] ,

and 

Rin (f ) = 1

n

> n

X

> i=1

Da(f (xi), f ∗(xi)) .

Proof structure. The proof follows the same structure as the proof sketch presented in Section 5. The two building blocks are the in-sample oracle-inequality (Theorem 5.1) proved in Appendix D and the pointwise convergence rate of the KRGLM estimator (Theorem 5.2) proved in Appendix E. We start with Appendix B, that formalizes Section 5.3 to prove our main Theorem 4.1. To keep the logic of the proof clear, Appendix B states and combines a number of results that are proved in Appendix C. Throughout, we make use of technical lemmas presented in Appendix F. 

# B Proof of Theorem 4.1 and corollaries 

In this section, we will use the following notations for j ∈ { 1, 2}:

ˆCj,λ = ( k ˆΣj + λI)−1 ˆΣj , ˆSj,λ = ( k ˆΣj + λI)−1/2 ˆΣ0(k ˆΣj + λI)−1/2,

their population version 

Cλ = ( kΣ + λI)−1Σ , Sλ = ( kΣ + λI)−1/2Σ0(kΣ + λI)−1/2,

as well as the hybrid ˆUj,λ = ( k ˆΣj + λI)−1/2Σ0(k ˆΣj + λI)−1/2.

To be able to use the in-sample oracle inequality, we first need to conduct a fixed-design analysis by treating the covariates and the data split as deterministic. 

B.1 Fixed-design analysis 

Lemma B.1.1 (Fixed-design KRGLM) . Let Assumptions 2.1 and 4.1 to 4.4 hold except that all covariates are deterministic and the data split is fixed. Choose any λ satisfying Theorem 5.2 conditions: 

λ ≤ g(n1) 

> R4

= 1  

> R4log β(n1)

λ ≥

 M1σ√log(16 /δ ) log n1

> √n1

21 

> g(n1)1/2

= M 21 σ2 log( 16   

> δ)(log n1)2+ β/ 2
> n1

Choose any positive semi-definite trace-class linear operator Q : H → H and define ˆT1,λ = ( k ˆΣ1 +

λI)−1/2Q(k ˆΣ1 + λI)−1/2. Then, learning with the first half of the source dataset D1, there is a universal constant C such that for all δ ∈ (0 , 1/e ], with probability at least 1 − δ:

ˆθλ − θ∗ 2 

> Q

≤ 4



λR 2 ˆT1,λ + Cσ 2 log(1 /δ ) log 2(n1)

kn 1



L2R2M 41 ˆT1,λ + Tr( ˆT1,λ )



.

On the other hand, for ˜λ set to its lower bound 

M 21 σ2 log( 16 n0 

> δ

)(log n2)2+ β/ 2

n2

.

23 Then, learning on the second half of the source dataset D2, X0( ˜θ − θ∗), where ˆθ˜λ = ˜θ, can be decomposed into three terms X0( ˜θ − θ∗) = B1 + B2 + ˜ε, respectively bias, perturbation and variance, satisfying simultaneously, with probability at least 1 − δ/ 2, for all δ ∈ (0 , 1/e ]:

• ∥B1∥22 ≤ n0 ˜λR 2 ˆS2,˜λ ≤ CM 21 σ2R2 n0

> n2

ˆS2,˜λ log( n0 

> δ

)(log n2)6,

• ∥B2∥22 ≤ CM 41 σ2 L2n0

> k2n2

ˆS2,˜λ log 7(n2/δ ),

• ∥ ˜ε∥2 

> ψ1

≤ n0 

> n2
> σ2
> k

ˆS2,˜λ (this is deterministic), and ∥ ˜ε∥22 ≤ C n0 

> n2
> σ2
> k

Tr( ˆS2,˜λ) log 3(n2/δ ).The variance term is sub-exponential (conditionally on X2). C is universal constant. 

The proof is deferred to Appendix C.1. The next lemma allows us to convert the previous empirical error bounds to population guarantees, its proof is in Appendix C.2. 

Lemma B.1.2 (Concentration of second-moment operators) . For any λ satisfying Theorem 5.2 conditions when n1 = n2 = n/ 2:



λ ≤ c1g(n) 

> R4

= c1  

> R4log β(n)

λ ≥ c2

 M1σ√log(32 /δ ) log n

> √n

21 

> g(n)1/2

= c2M 21 σ2 log( 32   

> δ)(log n)2+ β/ 2
> n

, and λ0 = 16 M 22 log(28 n0/δ ) 

> n0

, we have simultaneously with probability at least 1 − δ:

ˆΣ0 ⪯ 32 Σ0 + λ0

2 I , k ˆΣj + λI ⪰ 12 (kΣ + λI), ∀j ∈ { 1, 2}.

Under this event, we have for j ∈ { 1, 2}:

Tr( ˆCj,λ ) ≤ 2M 21 Tr( Cλ),

and ˆUj,λ ≤ 2 ∥Sλ∥ , Tr( ˆUj,λ ) ≤ 2 Tr( Sλ),

and 

ˆSj,λ ≤ 3 ∥Sλ∥ + λ0

λ .

B.2 Analysis of the selected candidate 

From the two previous lemmas, we obtain an excess risk bound for the best candidate in { ˆfλ}λ∈Λ.The proof can be found in Appendix C.3. 

Lemma B.2.1 (Excess risk of the best candidate) . Let Assumptions 2.1 and 4.1 to 4.4 hold. Choose any δ ∈ (0 , 1/e ], and define for any λ > 0

E(λ, δ ) = K



λR 2 ∥Sλ∥ + σ2 log( m/δ ) log 2(n1)

kn 1

 Tr( Sλ) + L2R2M 41 ∥Sλ∥

.

Then there exists a universal constant C such that with probability at least 1 − δ,

min  

> λ∈Λ

{R( ˆfλ) − R(f ∗)} ≤ C min  

> λ∈Λ∩J(δ)

E(λ, δ ),

24 where 

J(δ) = 

"

M 21 σ2 log(1 /δ )(log n)2+ β/ 2

n , 1

R4 log β (n)

#

,

with the convention min ∅ = ∞.

The general oracle inequality from Theorem 5.1 combined with Lemma B.1.1 yields the following result for the in-sample risk, and the proof is in Appendix C.4. 

Lemma B.2.2 (Oracle inequality for in-sample risk) . Let Assumptions 2.1 and 4.1 to 4.4 hold, except that all covariates are deterministic and the data split is fixed. Choose any δ ∈ (0 , 1/e ], and let ˜λ = M 21 σ2 log(16 n0/δ )(log n2)2+ β/ 2 

> n2

, then define 

ˆξ(˜λ, δ ) = log 2( mδ ) (1 + L2)K2R2σ2M 61 log 7(n2/δ ) log( n0)

k2n2

 ˆS2,˜λ + 1 



.

There exists a universal constant C such that with probability at least 1 − δ,

q

Rin ( ˆf ) − Rin (f ∗) ≤ min 

> λ∈Λ

q

Rin ( ˆfλ) − Rin (f ∗) + C

q

ˆξ(˜λ, δ ).

From (Wang, 2026), we apply lemma D.5. The modified proof is in Appendix C.5. 

Lemma B.2.3 (Bridging in-sample and out-of-sample risks) . Let Assumptions 2.1 and 4.1 to 4.4 hold, except that the source covariates are deterministic and the data split is fixed. Choose any 

δ ∈ (0 , 1/e ] and assume 

Λ ⊆

"

M 21 σ2 log(8 /δ )(log n1)2+ β/ 2

n1

, 1

R4 log β (n1)

#

.

Then there exists a universal constant C such that with probability at least 1 − δ,

max 

> λ∈Λ

ˆθλ − θ∗

> Σ0

− ˆθλ − θ∗

> ˆΣ0

≤ CpM1σ

s

log 2(n0m/δ ) log( m/δ )

n0

.

With the same assumptions and definitions, the previous lemmas lead to: 

Corollary B.1. There exists a universal constant C such that with probability at least 1 − 2δ,

q

R( ˆf ) − R(f ∗) ≤ min 

> λ∈Λ

q

R( ˆfλ) − R(f ∗) + C

r Kk

q

ˆξ(˜λ, δ ) + 

s

M1σ log 2(n0m/δ ) log( m/δ )

n0

 .

The proof is in Appendix C.6. Further combining, we get: 

Corollary B.2 (Oracle inequality for pseudo-labeling) . Let Assumptions 2.1 and 4.1 to 4.4 hold. Assume n1 = n2 = n/ 2. When ˜λ ≍ M 21 σ2 log( n0/δ ) log( n)2+ β/ 2 

> n

and 

Λ ⊆

"

M 21 σ2 log(1 /δ )(log n)2+ β/ 2

n , 1

R4 log β (n)

#

,

with probability at least 1 − δ,

q

R( ˆf ) − R(f ∗) ≤ min 

> λ∈Λ

q

R( ˆfλ) − R(f ∗) + ζ ·

s

(1 + L2)K3R2M 61 σ2

k3 ( 1

neff 

+ 1

n0

),

where ζ is a polylogarithmic factor in n, n 0, m, δ −1.

25 It implies up to a polylog factor, 

R( ˆf ) − R(f ∗) ≲ min  

> λ∈Λ

{R( ˆfλ) − R(f ∗)} + (1 + L2)K3R2M 61 σ2

k3 ( 1

neff 

+ 1

n0

).

Plugging the bound on the second term from Lemma B.2.1 yields the main theorem. 

Proof. In this proof, we use ≲ to hide polylogarithmic factors in (n, n 0, m, δ −1). We relate the in-sample quantities from Corollary B.1 above to their population version via Lemma B.1.2. On the one hand, from Corollary B.1, we have 

ˆξ(˜λ, δ/ 4) = log 2( 4mδ ) (1 + L2)K2R2σ2M 61 log 7(n/δ ) log( n0)

k2n

 ˆS2,˜λ + 1 



.

On the other hand, the concentration bounds from Lemma B.1.2 give 

ˆS2,˜λ ≤ 3 S˜λ + λ0

˜λ ≲ nneff 

+ nn0

,

where λ0 = 16 M 22 log(56 n0/δ ) 

> n0

, and we used (F.3) since ˜λ > μ 2/n . Putting everything together, since by construction neff ≤ n, we obtain: 

ˆξ(λ, δ/ 4) ≲ (1 + L2)K2R2σ2M 61

k2

 1

neff 

+ 1

n0



.

Since the additional term M1σ log 2(n0m/δ ) log( m/δ ) 

> n0

is just another polylog divided by the target sample size, we deduce that with probability at least 1 − δ,

q

R( ˆf ) − R(f ∗) ≤ min 

> λ∈Λ

q

R( ˆfλ) − R(f ∗) + ζ ·

s

(1 + L2)K3R2M 61 σ2

k3 ( 1

neff 

+ 1

n0

).

B.3 Proof of the main Theorem 4.1 

Combining via union bound Corollary B.2 and Lemma B.2.1, we get that, for Λ = {λj }mj=1 defined in the theorem, the following holds with probability at least 1 − δ:

R( ˆf ) − R(f ∗) ≲ min 

> λ∈Λ∩J(δ)

{E (λ, δ )} + (1 + L2)K3R2M 61 σ2

k3 ( 1

neff 

+ 1

n0

),

and 

E(λ, δ ) ≤ Kk



λR 2r−1 + σ2 log 2(m/δ ) log 2(n)

n (2 r−1 Tr(( kΣ0 + λr −1I)−1Σ0) + L2R2M 41 r−1k−1)



≲ K log 2(n) log 2(m/δ )

k



λR 2 nneff 

+ σ2

neff 

Tr(( kΣ0 + λr −1I)−1Σ0)



,

where we used again that ∥Sλ∥ ≤ r−1k−1 and Tr (Sλ) ≤ 2r−1 Tr (( kΣ0 + λr −1I)−1Σ0), and that the last perturbation term is negligible compared to the trace term of order n1/2α0 

> eff

.26 The last step is to bridge the discrete grid with the continuous interval. We adapt the proof of theorem 4.1 (Wang, 2026), by replacing the decision set Λ by ˜Λ := Λ ∩ J(δ) = {λ1, ..., λ p}, with 

λ1 ≥ μ2 

> n

log(1 /δ ) log( n)2+ β/ 2 and λp ≤ 1  

> R4log( n)β

.Denote 

min  

> λ∈˜Λ

{nR 2λ| {z } 

> A(λ)

+ σ2 Tr 

h kΣ0 + r −1λI−1 Σ0

i| {z }

> B(λ)

}.

We will finish the proof by establishing the following connection between the finite grid and the infinite interval: 

min  

> λ∈˜Λ

{A(λ) + B(λ)} ≲ inf 

> λ> 0

{A(λ) + B(λ)} + R2μ2.

The functions A and B are increasing and decreasing, respectively. From the bound on λ1 we get A (λ1) ≍ R2μ2 and 

inf 

> 0<λ ≤λ1

{A(λ) + B(λ)} + R2μ2 ≥ B (λ1) + A (λ1) .

From λp ≍ μ2   

> R4log βn

we get 

inf 

> λ≥λp

{A(λ) + B(λ)} ≥ A (λp) ≍ nR 2μ2.

Meanwhile, 

B (λp) = σ2 Tr 

h kΣ0 + r −1λpI−1 Σ0

i

≲ λ−1/2α0 

> p

≍ log β/ 2α0 (n) ≲ A(λp).

Therefore, B (λp) ≲ A (λp) and 

inf 

> λ≥λp

{A(λ) + B(λ)} ≳ A (λp) + B (λp) .

Next, we bridge the grid ˜Λ and the interval [λ1, λ p]. For any λ ∈ [λj , λ j+1 ], we have A(λ) ≥ A (λj )

and kΣ0 + r −1λI ⪯ (λ/λ j )  kΣ0 + r −1λj I. Hence, 

A(λ) + B(λ) ≥ A (λj ) + λj

λ B (λj ) ≥ A (λj ) + B (λj )2 .

We get 

inf 

> λ1≤λ≤λp

{A(λ) + B(λ)} ≥ 12 min  

> λ∈˜Λ

{A(λ) + B(λ)}.

The claim and the theorem follow. Note that since m = ⌈log 2(n)⌉ + 1 , we make the pre-factor ζ a polylog in (n, n 0, δ −1) only. 

B.4 Proof of Corollary 4.1 

Proof. Throughout the proof, we use ≲ to hide polylogarithmic factors and constants independent of n and neff . From Theorem 4.1, the excess risk is bounded by: 

R( ˆf ) − R(f ∗) ≲ inf 

> ρ> 0

ρ + 1

neff 

> ∞

X

> j=1

μ0j

kμ 0j + ρ

| {z }

> A(ρ)

+ 1

n0

+ 1

neff 

. (B.1) 27 We analyze the term inside the sum. Observe that for any j:

μ0j

kμ 0j + ρ = 1

k

 kμ 0j

kμ 0j + ρ



≤ 1

k min 



1, kμ 0j

ρ



. (B.2) Substituting this bound and using the decay assumption μ0j ≤ cj −2α, we split the sum at an integer index J:

> ∞

X

> j=1

μ0j

kμ 0j + ρ ≤ 1

k

> J

X

> j=1

1 + 1

k

> ∞

X

> j=J+1

kcj −2α

ρ

= Jk + cρ

> ∞

X

> j=J+1

j−2α

≲ Jk + J1−2α

ρ .

Substituting this back into A(ρ):

A(ρ) ≲ ρ + 1

neff 

 Jk + J1−2α

ρ



. (B.3) We choose the regularization parameter ρ = n− 2α 

> 2α+1
> eff

and the cutoff index J = ⌈ρ− 12α ⌉ ≍ n 

> 12α+1
> eff

.Checking the balance of terms (ignoring k and constants for the rate): 

• ρ = n− 2α 

> 2α+1
> eff

.

• Jneff ≍ n  

> 12α+1 −1
> eff

= n− 2α 

> 2α+1
> eff

.

• J1−2α 

> ρn eff

≍ (n   

> 12α+1
> eff )1−2α
> n−2α
> 2α+1
> eff neff

= n− 2α 

> 2α+1
> eff

.Thus, A(ρ) ≲ n− 2α 

> 2α+1
> eff

, completing the proof. 

B.5 Optimal L2(X ; P) prediction rate 

We proved in Theorem 5.2 a pointwise convergence rate of O(n−1/4) for a generic ridge-regularized kernel GLM estimator, with polynomial eigendecay μj ≲ j−2. Our analysis also allows us to derive the optimal L2 prediction rate of this estimator. We will omit the details of the proof because it follows the exact same steps as our main proof. Below we present a proof sketch. ≲ hides constants and log factors. We can use part 1 of Lemma B.1.1 with Q = Σ, combined with the concentration results of lemma B.1.2 to obtain an equivalent form of Lemma B.2.1 for one distribution only. Further using the fact that ∥Cλ∥ ≲ 1 and Tr( Cλ) ≲ λ−1/2 (as per Remark F.1), we obtain 

ˆθλ − θ∗ 2 

> Σ

≲ λR 2 + σ2λ−1/2

n .

Minimizing on λ yields the announced λ∗ ≍ n−2/3 and ˆθλ∗ − θ∗ 2 

> Σ

≲ n−2/3.28 Since by definition 

ˆfλ − f ∗ 2 

> L2

= Ex∼P 

D

Φ( x), ˆθλ − θ∗E2

=

D ˆθλ − θ∗, Σ( ˆθλ − θ∗)

E

,

we directly get: 

ˆfλ∗ − f ∗ 

> L2

≲ n−1/3,

and the GLM excess risk 

R(fλ∗ ) ≲ n−2/3.

# C Proofs of Appendix B 

C.1 Proof of Lemma B.1.1 

The key decomposition to use for this lemma comes from the proof of Theorem 5.2 in Appendix D.2. With the same notations 3:

∀λ > 0, −( ˆθλ − θ∗) = λH−1θ∗ + ( H−1 − ¯H−1) 1

n X⊤ε + ¯H−1 1

n X⊤ε.

The first term is the bias term, the last term is a sub-exponential variance term, and the middle term is a perturbation that we regroup with the bias. For the first part of the lemma, let Q be any PSD trace-class operator and λ in the specified range, working on the first half of the dataset. Per Theorem 5.2 (and intermediate results obtained during its proof), with probability at least 1 − δ/ 2, the following inequality hold simultaneously: 

H1 ⪰ k ˆΣ1 + λI, (C.1) 

H−11 − ¯H−11 = ¯H−1/21 (∆1(I + ∆1)−1) ¯H−1/21 , (C.2) 

∥∆1∥ ≲ LM 1

k

σ log( n1/δ )λ−1/4

√n1

+ Rλ 1/4

!

, (C.3) 

1

√n1

¯H−1/21 X⊤ 

> 1

ε1

> 2

≤ σ2 log 2(n1) log(8 /δ ) Tr( ˆC1,λ ). (C.4) Then: 

ˆθλ − θ∗ 2 

> Q

≤ 4

"

λH−11 θ∗ 2 

> Q

+ (H−11 − ¯H−11 ) 1

n1

X⊤ 

> 1

ε1

> 2
> Q

+ ¯H−11

1

n1

X⊤ 

> 1

ε1

> 2
> Q

#

.

We study the three terms separately. 

• For the first term, use (C.1) 

λH−11 θ∗ 2 

> Q

≤ λ2R2 H−11 QH −11 ≤ λ2R2 Q1/2(k ˆΣ1 + λI)−2Q1/2 ≤ λR 2 ˆT1,λ .     

> 3In what follows, we denote by Hi,Di,∆ithe corresponding quantities defined in the proof of Theorem 5.2 when working with dataset Di.

29 • For the second term, use (C.2) and (C.4): 

(H−11 − ¯H−11 ) 1

n1

X⊤ 

> 1

ε1

> 2
> Q

= 1

n21

Q1/2 ¯H−1/21 (∆1(I + ∆1)−1) ¯H−1/21 X⊤ 

> 1

ε1

> 2

≤ 1

n1

Q1/2 ¯H−1/21

> 2

· (∆1(I + ∆1)−1) 2 · 1

√n1

¯H−1/21 X⊤ 

> 1

ε1

> 2

≲ 1

n1

ˆT1,λ ∥∆1∥2 σ2 log 2(n1) log(8 /δ ) Tr( ˆC1,λ )

≲ 1

n1

ˆT1,λ ∥∆1∥2 σ2 log 2(n1) log(8 /δ )M 21 Tr( Cλ),

where the last inequality uses Tr( ˆC1,λ ) ≤ 2M 21 Tr( Cλ).Recall that Tr( Cλ) ≲ λ−1/2 and from (C.3), we get: 

∥∆1∥2 Tr( Cλ) ≲ L2M 21

k2

 σ2 log 2(n1/δ )

n1λ + R2



≲ L2M 21

k2

1

M 21 log β/ 2(n1/δ ) + R2

!

≲ L2M 21 R2

k2 ,

so that 

(H−11 − ¯H−11 ) 1

n1

X⊤ 

> 1

ε1

> 2
> Q

≲ L2

k2n1

ˆT1,λ σ2 log 2(n1) log(8 /δ )R2M 41 .

• For the third term, use a sub-exponential quadratic form tail bound that holds with probability larger than 1 − δ/ 2:

¯H−11

1

n X⊤ 

> 1

ε1

> 2
> Q

= 1

n21

D

ε1, X1 ¯H−11 Q ¯H−11 X⊤ 

> 1

ε1

E

≤ σ2

n21

Tr( X1 ¯H−11 Q ¯H−11 X⊤ 

> 1

) log 2(n1) log(4 /δ )

≤ σ2

n1

Tr( ¯H−11 Q ¯H−11 ˆΣ1) log 2(n1) log(4 /δ )

≤ σ2

n1

Tr(( k ˆΣ1 + λI)−1Q(k ˆΣ1 + λI)−1 ˆΣ1) log 2(n1) log(4 /δ )

≤ σ2

kn 1

Tr(( k ˆΣ1 + λI)−1Q) log 2(n1) log(4 /δ )= σ2

kn 1

Tr( ˆT1,λ ) log 2(n1) log(4 /δ ).

The bound follows by union bound and redefining constant C.For the second part of the lemma, we work with the second half of the source dataset and ˜λ set to the lower bound of the range, so that Theorem 5.2 now guarantees a n−1/4 consistency rate (up to logarithmic factors), otherwise the three inequalities (C.1), (C.2) and (C.4) are still valid with probability at least 1 − δ/ 2 with X2, H2 ε2... and ∥∆2∥2 ≲ L2 

> k2

M 31 σn −1/22 log 6(n2/δ ). We use the same decomposition multiplied by the target design matrix X0:

X0( ˜θ − θ∗) = B1 + B2 + ˜ ε,

30 where 

∥B1∥22 = ˜λX0H−12 θ∗ 2

≤ ˜λ2R2 X0H−22 X⊤

> 0

≤ ˜λR 2 X0H−12 X⊤

> 0

≤ ˜λR 2 X0(k ˆΣ2 + ˜λI)−1X⊤

> 0

= n0 ˜λR 2 ˆS2,˜λ ,

∥B2∥22 = X0 ¯H−1/22 (∆2(I + ∆2)−1) ¯H−1/22

1

n2

X⊤ 

> 2

ε2

> 22

≤ 1

n2

X0 ¯H−1/22 

> 22

· 2 ∥∆2∥22 · 1

√n2

¯H−1/22 X⊤ 

> 2

ε2

> 22

≲ L2n0

k2n2

ˆS2,˜λ · M 31 σn −1/22 log 6(n2) · σ2 log 2(n2) log(8 /δ ) Tr( ˆC2,˜λ)

≲ L2n0

k2n2

ˆS2,˜λ · M 31 n−1/22 log 9(n2/δ ) · σ3M 21 ˜λ−1/2 since Tr( ˆC2,˜λ) ≤ 2M 21 Tr( Cλ) ≲ 2M 21 ˜λ−1/2

≲ L2n0

k2n2

ˆS2,˜λ · M 31 n−1/22 log 9(n2/δ ) · σ3M 21 · n1/22 M −11 σ−1 log −2(n2/δ ) log −1(n0/δ )

≲ L2n0

k2n2

ˆS2,˜λ log 7(n2/δ )σ2M 41 ,

and finally 

∥ ˜ε∥2 

> ψ1

= 1

n2

X0 ¯H−12 X⊤ 

> 2

ε2

> 2
> ψ1

≤ σ2 1

n2

X0 ¯H−12 X⊤

> 2
> 22

= σ2 X0 ¯H−12

1

n22

X⊤ 

> 2

X2 ¯H−12 X⊤

> 0
> 2

≤ σ2 X0 ¯H−12

1

n2k X⊤

> 0
> 2

because 1

n2

X⊤ 

> 2

X2 = ˆΣ2 ⪯ 1

k ¯H2

≤ σ2n0

kn 2

ˆS2,˜λ ,

with the associated tail bound for ∥ ˜ε∥22, obtained through the quadratic form tail bound of sub-exponential vectors result of Lemma F.2.1. 

C.2 Proof of Lemma B.1.2 

Apply Lemma F.2.2 with γ = 1 /2 and note that when n is large enough the new lower bound is larger than the one required on the lemma. 31 C.3 Proof of Lemma B.2.1 

Start by leveraging the observation made in (A.1): R( ˆfλ) − R(f ∗) ≤ K 

> 2

ˆθλ − θ∗ 2Σ0

. Then apply Lemma B.1.1 with Q = Σ0 and union bounds on all candidates to get error bounds with empirical second-moment operators. Further, apply Lemma B.1.2 to relate them to the population versions. The two first lemmas are only valid in the range of J(δ). Finally, use the fact that min λ∈Λ ≤ min λ∈Λ∩J .

C.4 Proof of Lemma B.2.2 

We want to apply the in-sample oracle inequality from Theorem 5.1 to our context. The samples of interest are the target covariates {x0i}n0

> i=1

, and the pseudo-labels are obtained via the imputation model ˜f trained on the second half of the source dataset D2. In our vector notation, the target pseudo-labels can be decomposed as in Lemma B.1.1: 

X0 ˜θ = X0θ∗ + ( B1 + B2) + ˜ ε.

We can verify that Assumption 5.1 needed to apply the oracle inequality is verified directly per Assumptions 4.1 to 4.4, with C = RM 2. Therefore the oracle inequality gives that 

q

Rin ( ˆf ) − Rin (f ∗) − min 

> λ∈Λ

q

Rin ( ˆfλ) − Rin (f ∗)

(this intermediate inequality is obtained directly in the proof of the theorem) is smaller up to a constant than √K

√n0k

r

K ˜σ2 log 2( mδ ) + K ∥B∥2 + L2n0η4

k , (C.5) with B = B1 + B2, ˜σ is an upper bound on ∥ ˜ε∥ψ1 , and η is an upper bound on X0( ˜θ − θ∗) ∞.We take the square and consider each term separately, bounding them using Lemma B.1.1 (for 

B1, B2, ∥ ˜ε∥ψ1 ) and Theorem 5.2 (for η). The variance term: 

Kn0k K ∥ ˜ε∥2 

> ψ1

log 2( mδ ) ≤ K2σ2

k2 ˆS2,˜λ

log 2(m/δ )

n2

.

The bias term: 

Kn0k K ∥B∥2 ≤ 2K2

n0k (∥B1∥2 + ∥B2∥2)

≲ K2

k (˜λR 2 ˆS2,˜λ + L2M 41 σ2

k2n2

ˆS2,˜λ log 7(n2/δ )) 

≲ (1 + L2)K2M 41 σ2R2

k2 ˆS2,˜λ

log 7(n2/δ ) log( n0)

n2

.

Finally, the perturbation term: 

Kn0kL2n0η4

k ≲ L2KM 61 σ2

k2

log 6(n2/δ )

n2

.

Combining the three terms together yields the desired result. 32 C.5 Proof of Lemma B.2.3 

We adapt the proof of lemma 6.5 in (Wang, 2026). The only difference is checking the assumptions of their lemma D.5. Use Theorem 5.2 that gives for any ϵ ∈ (0 , 1/2] 

P

D

Φ( x0), ˆθλ − θ∗E

≤ cM 1 σ log( n1

ϵ ) λ−1/4α

√n1

+ λ1/4 ∥θ∗∥

!! 

≥ 1 − ϵ. 

Plugging in the two bounds on λ, we obtain with probability larger than 1 − ϵ:

D

Φ( x0), ˆθλ − θ∗E

≲ pM1σ log( 1

ϵ )n−1/41 log 5(n1) + g(n1)1/4 ≤ CpM1σ log( 1

ϵ )

for n large enough. This exponential tail also yields finite moments such that 

E

 D

Φ( x0), ˆθλ − θ∗E 41/4

≤ CpM1σ. 

Thus the inner product satisfies the conditions of lemma 6.5 with r = C log (1 /ϵ )√M1σ and 

¯U = C√M1σ. The rest follows as in the original proof. 

C.6 Proof of Corollary B.1 

We recall from remark A.2 that there exist constants c1, c 2 ∈ [

q k 

> 2

,

q K 

> 2

] such that ∀λ

q

R( ˆfλ) − R(f ∗) = c1 ˆθλ − θ∗

> Σ0

, (C.6) and q

Rin ( ˆfλ) − Rin (f ∗) = c2 ˆθλ − θ∗

> ˆΣ0

. (C.7) Moreover, Lemma B.2.3 gives with high probability 

max 

> λ∈Λ

ˆθλ − θ∗

> Σ0

− ˆθλ − θ∗

> ˆΣ0

≤ x, (C.8) where we noted x = C√σM 1/41

q log 2(n0m/δ ) log( m/δ )

> n0

And Lemma B.2.2 gives an inequality of the form 

q

Rin ( ˆf ) − Rin (f ∗) ≤ min 

> λ∈Λ

q

Rin ( ˆfλ) − R in (f ∗) + b, (C.9) where b = C

q ˆξ(˜λ, δ ).Plugging (C.6) and (C.7) into (C.8) gives: 

max 

> λ∈Λ

1

c1

q

R( ˆfλ) − R(f ∗) − 1

c2

q

Rin ( ˆfλ) − Rin (f ∗) ≤ x. 

Thus, for all λ: q

Rin ( ˆfλ) − Rin (f ∗) ≤ c2x + c2

c1

q

R( ˆfλ) − R(f ∗),

33 in particular 

min 

> λ∈Λ

q

Rin ( ˆfλ) − Rin (f ∗) ≤ c2x + c2

c1

min 

> λ∈Λ

q

R( ˆfλ) − R(f ∗),

and q

R( ˆfλ) − R(f ∗) ≤ c1x + c1

c2

q

Rin ( ˆfλ) − Rin (f ∗),

in particular for λ = ˆλ

q

R( ˆf ) − R(f ∗) ≤ c1x + c1

c2

q

Rin ( ˆf ) − Rin (f ∗).

Now plugging (C.9) into the last line, we get: 

q

R( ˆf ) − R(f ∗) ≤ c1x + c1

c2



min 

> λ∈Λ

q

Rin ( ˆfλ) − Rin (f ∗) + b



≤ c1x + c1

c2

 

c2x + c2

c1

min 

> λ∈Λ

q

R( ˆfλ) − R(f ∗)



+ b



= 2 c1x + c1

c2

b + min 

> λ∈Λ

q

R( ˆfλ) − R(f ∗).

The bounds on c1 and c2 finish the proof. 

# D Proofs of Section 5.2’s pointwise convergence rate 

Recall our notations: k is strong convexity constant, M1 is a uniform bound on the eigenfunctions of the RKHS, M2 is a uniform bound on the covariates, σ is a bound on the sub-exponential norm of the noise vector, and R is a bound on the true parameter’s norm. Using the isometry between the RKHS and the feature space (2.4), an equivalent formulation of (2.7) is 

min 

> θ∈H

(

1

n

> n

X

> i=1

(a(⟨Φ( xi), θ⟩) − yi ⟨Φ( xi), θ⟩) + λ

2 ∥θ∥2

> H

)

, (D.1) In what follows, denote the regularized empirical GLM loss and its minimizer as: 

Lλ(θ) = 1

n

> n

X

> i=1

(a(⟨Φ( xi), θ⟩) − yi ⟨Φ( xi), θ⟩) + λ

2 ∥θ∥2 , ˆθλ ∈ argmin 

> θ∈H

Lλ(θ),

with gradient 

∇Lλ(θ) = 1

n

> n

X

> i=1

(a′(⟨Φ( xi), θ⟩) − yi)Φ( xi) + λθ , ∇Lλ(θ∗) = 1

n X⊤ε + λθ∗,

and Hessian 

∇2Lλ(θ) = 1

n

> n

X

> i=1

a′′ (⟨Φ( xi), θ⟩)(Φ( xi) ⊗ Φ( xi)) + λI.

We need the following definition in the rest of the proof: 34 Definition D.1 (Strong convexity region) . Define Ω( ρ) := {θ : ∥X(θ − θ∗)∥∞ ≤ ρC } the domain over which the local strong convexity implies that ∀θ ∈ Ω( ρ), ∀i ∈ [n], a ′′ (⟨Φ( xi), θ⟩) ≥ kρ. Denote also k := k1, the smallest strong convexity constant when ρ ≤ 1.

Remark D.1. It follows that ∀θ ∈ Ω( ρ), ∇2Lλ(θ) ⪰ kρ ˆΣ + λI.To stay more general in this section, we will assume that the kernel eigenvalues in the L2(P, X )

basis have a polynomial decay of exponent α ≥ 1: ∀j, μ j ≤ Aj −2α. Only at the end will we replace 

α by 1, as per our Assumption 4.1. 

GLM estimator properties To characterize the estimation error of the estimator, we let 

θ(t) = θ∗ + t( ˆθλ − θ∗) and write the Taylor expansion of ∇Lλ between ˆθλ and θ∗:

0 = ∇Lλ( ˆθλ) = ∇Lλ(θ∗) + 

Z 10

∇2Lλ(θ(t)) dt 



· ( ˆθλ − θ∗).

Then take the inverse to get an expression of: 

ˆθλ − θ∗ = −

 1

n X⊤DX + λI

−1  1

n X⊤ε + λθ∗



, (D.2) where the diagonal matrix D has components 

Dii =

Z 10

a′′ (⟨Φ( xi), θ(t)⟩) dt. 

However, to turn this non-linear equation into a usable expression, we need to know that θ(t) is in the region of strong convexity to lower bound D by kI. This motivates our first lemma which proves a crude consistency rate with high probability. This coarse-grained analysis will serve as a stepping stone to prove the main theorem. 

D.1 Coarse-grained analysis of the estimator consistency 

Lemma D.1.1. Let g(n) = 1log β (n) −→  

> n→∞

0, β > 0, and choose δ ∈ (0 , 1/7] . Then, under Assumptions 2.1 and 4.1 to 4.4, there exists constants c, c 0, c 1 > 0 and c2 ≥ k, such that when 

ρ = c0g(n)1/4 and: 



λ ≤ c1g(n) 

> R4

= c1  

> R4log β(n)

λ ≥ c2

 M 21 σ√log(4 /δ ) log n

> √n

21 

> g(n)1/2

= c2M 21 σ2 log( 4  

> δ)(log n)2+ β/ 2
> n

We have: 

P

 ˆθλ ∈ ˚Ω( ρ)



≥ 1 − δ, 

namely with probability at least 1 − δ,

X( ˆθλ − θ∗) ∞ ≤ cg (n)1/4

Proof. By contradiction, assume that ˆθλ /∈ Ω( ρ). We need show that ∀θ /∈ Ω( ρ), L λ(θ) > L λ(θ∗), for a well-chosen ρ, thus contradicting the optimality of ˆθλ. Inside Ω( ρ), we can use the local kρ-strong convexity. In this proof, we denote Sλ,ρ := kρ ˆΣ + λI.Let the ellipsoid Eρ = θ : ∥S1/2 

> λ,ρ

(θ − θ∗)∥ ≤ 2 ∥S−1/2 

> λ,ρ

∇Lλ(θ∗)∥ . We begin by proving the two following facts: 35 • Fact 1: ∀θ ∈ ˚Ω( ρ) \ Eρ, L λ(θ) > L λ(θ∗).

Proof. Let θ ∈ ˚Ω (ρ). Then we can apply Taylor Lagrange between θ and θ∗ with a θin =

θ∗ + t(θ − θ∗) ∈ ˚Ω (ρ) (for some t ∈ (0 , 1) ) on the line segment connecting the two vectors. We can then use the strong convexity of a to write: 

Lλ(θ) = Lλ(θ∗) + ⟨∇ Lλ(θ∗), θ − θ∗⟩ + 12 θ − θ∗, ∇2Lλ(θin )( θ − θ∗)) 

≥ Lλ(θ∗) + ⟨∇ Lλ(θ∗), θ − θ∗⟩ + 12 ⟨θ − θ∗, Sλ,ρ (θ − θ∗)) ⟩ ,

i.e. 

Lλ(θ) ≥ Lλ(θ∗) + 

D

S−1/2 

> λ,ρ

∇Lλ(θ∗), S1/2 

> λ,ρ

(θ − θ∗)

E

+ 12 S1/2 

> λ,ρ

(θ − θ∗) 2

.

Using Cauchy-Schwarz, we get: 

Lλ(θ) − Lλ(θ∗) ≥ S1/2 

> λ,ρ

(θ − θ∗)

 12 S1/2 

> λ,ρ

(θ − θ∗) − S−1/2 

> λ,ρ

∇Lλ(θ∗)



,

which yields the desired result. 

• Fact 2: If 

ρ > 2

C max 

> i

n

S−1/2 

> λ,ρ

Φ( xi) · S−1/2 

> λ,ρ

∇Lλ(θ∗)

o

, (D.3) then Eρ is a proper subset of Ω( ρ).

Proof. Let θ ∈ Eρ, i ∈ [n]. Then: 

|⟨ Φ( xi), θ − θ∗⟩| =

D

S−1/2 

> λ,ρ

Φ( xi), S1/2 

> λ,ρ

(θ − θ∗)

E

≤ S−1/2 

> λ,ρ

Φ( xi) · S1/2 

> λ,ρ

(θ − θ∗) .

By definition of Eρ, we get: 

|⟨ Φ( xi), θ − θ∗⟩| ≤ 2 S−1/2 

> λ,ρ

Φ( xi) · S−1/2 

> λ,ρ

∇Lλ(θ∗) .

Thus, if ρ > 2 

> C

max i

n

S−1/2 

> λ,ρ

Φ( xi) · S−1/2 

> λ,ρ

∇Lλ(θ∗)

o

, θ ∈ ˚Ω(ρ), and the result is proved. Together these two facts imply either that ˆθλ /∈ Ω( ρ) or that ˆθλ ∈ Eρ ⊂ ˚Ω (ρ) by optimality of 

ˆθλ. Let us prove that the first possibility is actually impossible. Let θ /∈ Ω( ρ) and construct the line parametrized by θ(t) = θ∗ + t(θ − θ∗), t ∈ [0 , 1] linking θ and 

θ∗. By continuity, it intersects Ω( ρ) and we define the intersection point as tb ∈ (0 , 1) , θ(tb) ∈ ∂Ω( ρ).Given that θ(tb) /∈ Eρ, we know from Fact 1 that Lλ(θ(tb)) ≥ Lλ(θ∗). Then by convexity of Lλ

(true everywhere), Lλ(θ) ≥ 1 

> tb

Lλ(θ(tb)) + (1 − 1 

> tb

)Lλ(θ∗) > L λ(θ∗).Finally, we have proved that with a well chosen ρ satisfying D.3, ˆθλ ∈ Eρ ⊂ ˚Ω( ρ).Let us now prove that the inequality D.3 is feasible and extract the adequate ρ. This is a nonlinear inequality in ρ. However, we know that kρ is decreasing as ρ increases (since ρ

expands the region of strong convexity), which makes the RHS of D.3 increasing in ρ. If we can assume that ρ is upper bounded, say ρ ≤ 1, and then can show the existence of ρ >  

> 2
> C

max i

n

S−1/2 

> λ, 1

Φ( xi) · S−1/2 

> λ, 1

∇Lλ(θ∗)

o

still smaller than 1, we are done. We are going to prove that it is possible by computing explicitly the RHS for ρ = 1 :36 Computation of the RHS of D.3 for ρ = 1 

RHS = 2

C max 

> i

n

S−1/2 

> λ, 1

Φ( xi) · S−1/2 

> λ, 1

∇Lλ(θ∗)

o

= 2

C max 

> i



(k ˆΣ + λI)) −1/2Φ( xi) · (k ˆΣ + λI)−1/2( 1

n X⊤ε + λθ∗)) 



≤ 2

C max 

> i



(k ˆΣ + λI)) −1/2Φ( xi) ·

 1

n (k ˆΣ + λI)−1/2X⊤ε + λ (k ˆΣ + λI)−1/2θ∗

 

,

(D.4) where k = k1.Define the event 

A := 

 12 (kΣ + λI) ⪯ k ˆΣ + λI ⪯ 32 (kΣ + λI)



.

By lemma F.2.2 applied with γ = 1 /2, λ = λ/k and δ = δ/ 2, which relates the sample and population covariance, since δ ∈ (0 , 1/7] and our choice of λ satisfies λ ≥ CM 22 log(2 n/δ ) 

> n

where 

C ≥ k > 0 for n large enough, we have 

P (A) ≥ 1 − δ

2 .

We consider the three norm terms of D.4 successively. We study the square norm to use inner products and then take the square-root. ≲ only hides universal multiplicative constants. 

• Under A,

D

Φ( xi), (k ˆΣ + λI)−1Φ( xi)

E

≤ 2 Φ( xi), (kΣ + λI)−1Φ( xi) . We make use of lemma F.1.1 to get: Φ( xi), (kΣ + λI)−1Φ( xi) ≲ M 21 λ−1/2α. Therefore, taking the square-root, we obtain 

(k ˆΣ + λI)−1/2Φ( xi) ≲ M1λ−1/4α.

• Under A, the bias term writes: λ (k ˆΣ + λI)−1/2θ∗ ≲ λ (kΣ + λI)−1/2θ∗ . Then, using 

(kΣ + λI)−1 ⪯ I/λ ,

(kΣ + λI)−1/2λθ∗ ≤ √λ ∥θ∗∥ ≲ λ1/2

for bounded θ∗.

• For the squared variance term 

D

ε, 1 

> n2

X(k ˆΣ + λI)−1X⊤ε

E

, conditioned on X, we use the quadratic form tail bound proved in lemma F.2.1 for the sub-exponential random vector ε and PSD matrix 

A = 1 

> n2

X(k ˆΣ + λI)−1X⊤, which bounds with high probability by the trace of the PSD operator. Since the δ defined earlier verifies δ ≤ 1/7 ≤ 4/e , we have: 

P

 

ε, 1

n2 X(k ˆΣ + λI)−1X⊤ε



≲ σ2 log 2(n) log(4 /δ ) Tr( 1

n2 X(k ˆΣ + λI)−1X⊤) X



≥ 1 − δ

2 ,

and thus integrating over the whole event: 

P

 

ε, 1

n2 X(k ˆΣ + λI)−1X⊤ε



≲ σ2 log 2(n) log(4 /δ ) Tr( 1

n2 X(k ˆΣ + λI)−1X⊤) A



≥ 1 − δ

2 .

37 Now, under A according to lemma F.2.2, 

Tr( 1

n2 X(k ˆΣ + λI)−1X⊤) = 1

n Tr(( k ˆΣ + λI)−1 ˆΣ) ≤ 2M 21

n Tr(( kΣ + λI)−1Σ).

Then applying again lemma F.1.1, we have that 

Tr(( kΣ + λI)−1Σ) = r(λ/k )/k ≲ λ−1/2α.

Therefore: 

P

 1

n (k ˆΣ + λI)−1/2X⊤ε ≲ σM 1

plog(4 /δ )n−1/2λ−1/4α log n A



≥ 1 − δ

2 .

All in all, since our analysis holds for any i, there exists c > 0, such that the event 

E := 



RHS ≤ cM 1λ−1/4α



λ1/2 ∥θ∗∥ + σM 1

plog(4 /δ )λ−1/4α log n

√n

 

has conditional probability 

P  E A ≥ 1 − δ

2 ,

and 

P (E) ≥ P  E A P (A) ≥



1 − δ

2

2

≥ 1 − δ. 

Crude consistency rate. Therefore, if we set 



σ2M 21 log( 4

δ )

α  log n

√n

2α 1

g(n)α/ 2 ≲ λ ≲ g(n)

R4 (D.5) for a slowly decaying g(n) → 0 (and still respecting the condition of event A 4), since 1/2−1/4α ≥ 1/4,we have with probability at least 1 − δ:

RHS ≲ g(n)1/4 → 0.

We can simplify inequality D.5 since it is still verified when setting α = 1 , and with g(n) =   

> 1log β(n)

, β > 0, so that it becomes 

M 21 σ2 log( 4 

> δ

)(log n)2+ β/ 2

n ≲ λ ≲ 1

R4 log β (n) . (D.6) Namely, if we set ρ = g(n)1/4, inequality D.3 is verified and ˆθλ ∈ Ω( ρ) so that the following consistency rate is established with probability at least 1 − δ:

X( ˆθλ − θ∗) ∞ ≲ g(n)1/4 = 1log β/ 4(n) .  

> 4For instance, we take 1/g (n)to be a polylog

38 D.2 Proof of Theorem 5.2 

Building on the previous lemma, we refine the analysis of the consistency rate in the theorem. 

Theorem 5.2 (Pointwise convergence rate) . Consider a ridge-regularized kernel GLM estimator 

ˆfλ trained on n i.i.d. samples from the source distribution P via (2.7) . Let β ∈ (0 , 8) , and choose 

δ ∈ (0 , 1/e ]. Then, under Assumptions 2.1 and 4.1 to 4.4, there exist constants c, c ′, c 1, c 2 > 0, such that for any given x0 ∈ supp( P), when: 



λ ≤ c1  

> R4log β(n)

λ ≥ c2M 21 σ2 log( 8  

> δ)(log n)2+ β/ 2
> n

,

the following inequality holds with probability at least 1 − δ:

ˆfλ(x0) − f ∗(x0) ≤ cM 1 σ log( nδ ) λ−1/4

√n + λ1/4 ∥f ∗∥F

!

. (5.11) 

When setting λ to its lower bound, the result becomes 

ˆfλ(x0) − f ∗(x0) ≤ c′

q

M 31 σn −1/4 

log( nδ )

3

. (5.12) 

Remark D.2. When union bounding on all the x0 of a target X0 of length n0, we get with 

g(n) := 1log β (n) that when 

λ ≍ M1σplog(8 n0/δ ) log n

√n

!2

1

g(n)1/2 ,

P



X0( ˆθλ − θ∗) ∞ ≲

q

M 31 σn −1/4(log( nδ )) 3



≥ 1 − δ. 

Proof. Recall (D.2): 

ˆθλ − θ∗ = −

 1

n X⊤DX + λI

−1  1

n X⊤ε + λθ∗



where the diagonal matrix D has components 

Dii =

Z 10

a′′ D 

Φ( xi), θ∗ + t( ˆθλ − θ∗)

E 

dt. 

We denote H := 1 

> n

X⊤DX + λI in what follows. Since D and H depend on ˆθλ, they are random and correlated with ε, so let us consider their deterministic (in fixed-design) versions ¯Dii := a′′ (⟨Φ( xi), θ∗⟩) and ¯H := 1 

> n

X⊤ ¯DX + λI. Let 

x0 ∈ supp (P ) and Φ0 := Φ( x0) the associated vector in the RKHS. We can now write a sort of bias-variance decomposition plus perturbation from D.2: 

D

Φ0, ˆθλ − θ∗E

≤ λ Φ0, H−1θ∗ +



Φ0, 1

n ¯H−1X⊤ε



+



Φ0, 1

n (H−1 − ¯H−1)X⊤ε



. (D.7) 39 We will show that the perturbation term is small. We let k = k1 be the constant of strong convexity of the link function a. This constant is the smallest allowed for the range of ρ considered, i.e. associated to the largest region of strong convexity. 

a is thus k-strongly convex on Ω( ρ), ∀ρ ≤ 1. As such, ¯D ⪰ kI and ¯H ⪰ k ˆΣ + λI, and if we let the event of lemma D.1.1 be: 

B := 

n

X( ˆθλ − θ∗) ∞ ≲ g(n)1/4o

,

which holds with probability larger than 1 − δ/ 2, when 



λ ≤ c1g(n)

> ∥θ∗∥4

λ ≥ c2

 M1σ√log(8 /δ ) log n

> √n

2α 

> 1
> g(n)α/ 2

,

and δ ∈ (0 , 2/7] , we also have a fortiori under B that D ⪰ kI and H ⪰ k ˆΣ + λI. Recall as well that under B, the events from lemma D.1.1 

A1 =

 12 (kΣ + λI) ⪯ k ˆΣ + λI ⪯ 32 (kΣ + λI)



,

and 

A2 =

 1

√n



k ˆΣ + λI

−1/2

X⊤ε ≲ M1σplog(8 /δ ) log( n)λ−1/4α



hold as well. We place ourselves under that event B in the rest of the proof , everything is conditional on X in particular. We study the three terms of D.7 separately. Begin by noting the following fact. 

Fact D.1 (Relative error) .

∀i ∈ [n], Dii − ¯Dii 

¯Dii 

≤ 12

for n large enough, the rank depending on the Lipschitz and strong convexity constant of the log-partition function a 5. It implies successively the following inequalities: 

− 12 ¯H ⪯ H − ¯H ⪯ 12 ¯H , ∥∆∥ ≤ 12 ,

where ∆ := ¯H−1/2H ¯H−1/2 − I. We also get that ∥∆∥ ≤ L 

> 2k

X( ˆθλ − θ∗) ∞.Proof. 

Dii − ¯Dii =

Z 10



a′′ D 

Φ( xi), θ∗ + t( ˆθλ − θ∗)

E 

− a′′ (⟨Φ( xi), θ∗⟩)



dt 

≤

Z 10

a′′ D 

Φ( xi), θ∗ + t( ˆθλ − θ∗)

E 

− a′′ (⟨Φ( xi), θ∗⟩) dt 

≤

Z 10

Lt 

D

Φ( xi), ˆθλ − θ∗E

dt by Lipschitz continuity of a′′ 

= L

2

D

Φ( xi), ˆθλ − θ∗E     

> 5E.g. for g(n) = 1 /log 4(n), with the logistic regression loss, nmust be larger than 28 . Compute other examples with explicit constants!

40 ≲ Lg (n)1/4 under event B.

Since we also have ¯Dii ≥ k, we deduce 

Dii − ¯Dii 

¯Dii 

≲ Lk g(n)1/4 ≤ 12

for n large enough. Since they are diagonal matrices, it implies that 

− 12 ¯D ⪯ D − ¯D ⪯ 12 ¯D,

and thus 

− 12 ¯H ⪯ H − ¯H ⪯ 12 ¯H,

and 

− 12 I ⪯ ∆ ⪯ 12 I,

hence ∥∆∥ ≤ 12 .Also note that if we denote by E = ¯D−1/2(D − ¯D) ¯D−1/2 the diagonal matrix of elements   

> Dii −¯Dii
> ¯Dii

, and define the matrix A = 1√n ¯D1/2X ¯H−1/2, then 

∆ = A⊤EA , and A⊤A = I − λ ¯H ⪯ I,

so that 

∥∆∥ ≤ ∥ E∥ ≤ L

2k X( ˆθλ − θ∗) ∞ ≲ g(n)1/4.

Note also that the strong convexity property allows to lower bound H, ¯H by k ˆΣ + λI, so that using successively lemmas F.2.2, F.1.1, and Lemma F.2.1 for the third point, we get the following fact: 

Fact D.2. The matrix A can be either H or ¯H

• A−1/2Φ0 ≲ M1λ−1/4α.

• 1√n A−1/2X⊤ ≲ 1.

• 1√n A−1/2X⊤ε ≲ M1σplog(8 /δ )λ−1/4α log n. 6

Proof. For the first item, write: 

A−1/2Φ0

> 2

= Φ0, A−1Φ0

≤

D

Φ0, (k ˆΣ + λI)−1Φ0

E

under B≤ 2 Φ0, (kΣ + λI)−1Φ0 via Lemma F.2.2     

> 6Thanks to the symmetric form, we can bound Hand ¯Himmediately, which makes the use of the subexponential tail bound valid, no need to wait for taking the trace

41 ≲ M 21 λ−1/2α via Lemma F.1.1 .

For the second item, we have: 

1

√n A−1/2X⊤

> 2

= 1

n XA −1X⊤

≤ 1

n X(k ˆΣ + λI)−1X⊤ under B

= (k ˆΣ + λI)−1 ˆΣ

≲ 1.

And for the third item: 

1

√n A−1/2X⊤ε ≤ 1

√n (k ˆΣ + λI)−1/2X⊤ε under B≤ M1σplog(8 /δ )λ−1/4α log n directly from event A2.

We also have the simple bound where A can be either H or ¯H:

A−1/2 ⪯ λ−1/2I.

Computation of the bias term of D.7 

λ Φ0, H−1θ∗ = λ

D

H−1/2Φ0, H−1/2θ∗E

≤ λ H−1/2Φ0 · H−1/2θ∗ (by Cauchy-Schwarz) 

≲ λ · M1λ−1/4α · λ−1/2 ∥θ∗∥ (by fact D.2) 

= M1λ(2 −1/α )/4 ∥θ∗∥≤ M1λ1/4 ∥θ∗∥ since α ≥ 1 and λ is small .

Computation of the variance term of D.7 Conditionally on X, ε is sub-exponential vector with ∥ε∥ψ1 ≤ σ. Hence, automatically for any vector a ∈ Rn, ⟨a, ε⟩ is a sub-exponential variable with ∥⟨ a, ε⟩∥ ψ1 ≤ ∥ a∥2 σ, such that the following tail bound holds ∀δ′ ∈ (0 , 1) with probability at least 1 − δ′:

|⟨ a, ε⟩| ≤ σ ∥a∥2 log(2 /δ ′).

Therefore, the variance term can be bound conditionally on the event B with probability at least 

1 − δ′, as: 



Φ0, 1

n ¯H−1X⊤ε



≤ σ log(2 /δ ′) 1

n X ¯H−1Φ0 2

≤ σ log(2 /δ ′) 1

√n ¯H−1/2Φ0 · 1

√n ¯H−1/2X⊤

≲ σ log(2 /δ ′) M1

√n λ−1/4α · 1 (by fact D.2) 

= σ log(2 /δ ′) M1

√n λ−1/4α.

42 Computation of the perturbation term of D.7 First note that (I + ∆)−1 = I − ∆(I + ∆)−1

and 

H−1 − ¯H−1 = ¯H−1/2

 ¯H−1/2H ¯H−1/2−1

− I



¯H−1/2

= ¯H−1/2 h

(I + ∆)−1 − I

i ¯H−1/2

= − ¯H−1/2 h

∆ (I + ∆)−1i ¯H−1/2,

and since ∥∆∥ ≤ 1/2,

(I + ∆)−1 ≤

> ∞

X

> n=0

∥∆∥n = 11 − ∥ ∆∥ ≤ 2

hence the perturbation term becomes: 



Φ0, 1

n (H−1 − ¯H−1)X⊤ε



=



Φ0, 1

n

h ¯H−1/2 h

∆ (I + ∆)−1i ¯H−1/2i

X⊤ε



≤ ¯H−1/2Φ0 · 2 ∥∆∥ · 1

√n

1

√n ¯H−1/2X⊤ε

≲ λ−1/4α · ∥ ∆∥ · 1

√n M1σplog(8 /δ )λ−1/4α log n

= log n

√nλ 1/α ∥∆∥ M1σplog(8 /δ ).

So far, we have proved that there exists a constant c > 0 such that for any x0 ∈ supp( P ):

P

D

Φ( x0), ˆθλ − θ∗E

≤ c log n

√nλ 1/α ∥∆∥ M1σ

r

log( 8

δ ) + σ log( 2

δ′ ) M1

√n λ−1/4α + M1λ1/4 ∥θ∗∥

!

B

!

≥ 1−δ′.

In particular, by union bound on all the xi and x0, we have simultaneously with probability larger than 1 − (n + 1) δ′ conditionally on B:

X( ˆθλ − θ∗) ∞ ≤ c log n

√nλ 1/α ∥∆∥ M1σ

r

log( 8

δ ) + σ log(2 /δ ′) M1

√n λ−1/4α + M1λ1/4 ∥θ∗∥

!

, (D.8) and 

D

Φ( x0), ˆθλ − θ∗E

≤ c log n

√nλ 1/α ∥∆∥ M1σ

r

log( 8

δ ) + σ log( 2

δ′ ) M1

√n λ−1/4α + M1λ1/4 ∥θ∗∥

!

.

(D.9) But by construction from Fact D.1, we also know that ∥∆∥ ≤ C1 X( ˆθλ − θ∗) ∞ where C1 > 0

is a constant depending on the Lipschitz and strong convexity constant. We can plug this expression in (D.8) and regroup the terms to get: 

X( ˆθλ − θ∗) ∞ 1 − cC 1 log n

√nλ 1/α M1σ

r

log( 8

δ )

!

≤ cM 1



σ log(2 /δ ′) 1

√n λ−1/4α + λ1/4 ∥θ∗∥



.

43 Thus, if cC 1 log n√nλ 1/α M1σ

q

log( 8 

> δ

) ≤ 12 ,

X( ˆθλ − θ∗) ∞ ≤ 2cM 1



σ log(2 /δ ′) 1

√n λ−1/4α + λ1/4 ∥θ∗∥



. (D.10) With λ ≳

 log n√n

2α 1 

> g(n)α/ 2

,

cC 1 log n

√nλ 1/α ≲ cC 1g(n)1/4 → 0

so for n large enough, we do have cC 1M1σ   

> q
> log( 8
> δ)
> √nλ 1/α

≤ 12 .Finally, we can plug back (D.10) into (D.9) with probability larger than 1 − (n + 1) δ′ conditionally on B:

D

Φ( x0), ˆθλ − θ∗E

≤ c log n

√nλ 1/α 2cC 1M 21 σ

r

log( 8

δ )



σ log(2 /δ ′) 1

√n λ−1/4α + λ1/4 ∥θ∗∥



+σ log(2 /δ ′) M1

√n λ−1/4α + M1λ1/4 ∥θ∗∥



,

i.e. there is a constant c′ > 0 such that, for n large enough and δ′ = δ 

> 2( n+1)

≤ δ 

> 6

∈ (0 , 1) :

P

 D

Φ( x0), ˆθλ − θ∗E

≤ c′M1



σ log( n/δ ) 1

√n λ−1/4α + λ1/4 ∥θ∗∥



B



≥ 1 − δ

2 , (D.11) and thus 

P

 D

Φ( x0), ˆθλ − θ∗E

≤ c′M1



σ log( n/δ ) 1

√n λ−1/4α + λ1/4 ∥θ∗∥

 

≥ (1 − δ

2 )2 ≥ 1 − δ. (D.12) If we set 

λ = c2

M1σplog(8 /δ ) log n

√n

!2α

1

g(n)α/ 2 = c2



M1σplog(8 /δ )

2α

(log( n)) α(2+ β/ 2) n−α,

the probabilistic bound in D.12 becomes: 

M1σ log( n/δ ) n−1/4

log( n)1/2+ β/ 8



M1σplog(8 /δ )

−1/2

+



M1σplog(8 /δ )

α/ 2 log( n)α(1 /2+ β/ 8) 

nα/ 4 M1 ∥θ∗∥ .

(D.13) With α = 1 and β ∈ (0 , 8) , it yields, with probability at least 1 − δ

D

Φ( x0), ˆθλ − θ∗E

≲

q

M 31 σn −1/4(log( nδ )) 3/2 ≲

q

M 31 σn −1/4(log( nδ )) 3.

(We only care about the n exponent, so we chose 3 as an upper bound to the true 3/2 for the logarithm exponent to lighten the expression.) 44 E Proof of Section 5.1’s in-sample oracle inequality 

Theorem 5.1 (Oracle inequality) . Let δ ∈ (0 , 1) . Then there exists a universal constant C1 > 0

such that under Assumption 5.1, the following holds with probability at least 1 − δ:

Rin ( ˆf ) ≤ inf 

> ϵ> 0



(1 + ϵ)Rin (fj∗ ) + C1Knk 



1 + 1

ϵ



ξ(˜ σ, B, η, δ )



, (5.6) 

with 

ξ(˜ σ, B, η, δ ) = K ˜σ2 log 2( mδ ) + K ∥B∥2 + L2nη 4

k .

Remark E.1. Under that event, we also have this intermediate inequality that holds with probability at least 1 − δ:

q

Rˆj ≤ pRj∗ + C2

√K

√nk 

√K ˜σ log( mδ ) + √K ∥B∥ +

√nη 2

√k



,

where C2 > 0.

Proof. We start by introducing some notations for the rest of the proof. For any function f ,let f = ( f (x1), · · · , f (xn)) be the evaluation vector over the samples. We also denote for all j,

Rj := Rin (fj ) and bRj := bRin (fj ).We use the definition of the selected index j∗ = argmin j∈[m] Rj . We first need to establish a decomposition result between R and ˆR:

Fact E.1. Let f be any candidate model. Then 

Rin (f ) = bRin (f ) − bRin (f ∗) + 1

n

> n

X

> i=1

(a′( ˜f (xi)) − a′(f ∗(xi)))( f (xi) − f ∗(xi)) .

Proof. We simply apply the law of cosines of Bregman divergence since 

Rin (f ) = 1

n

> n

X

> i=1

Da(f (xi), f ∗(xi)) , bRin (f ) = 1

n

> n

X

> i=1

Da(f (xi), ˜f (xi)) .

Applying fact E.1 to Rˆj , it follows that: 

Rˆj = bRˆj − bR(f ∗) + 1

n

> n

X

> i=1

(a′( ˜f (xi)) − a′(f ∗(xi)))( fˆj (xi) − f ∗(xi)) 

≤ bRj − bR(f ∗) + 1

n

> n

X

> i=1

(a′( ˜f (xi)) − a′(f ∗(xi)))( fˆj (xi) − f ∗(xi)) , ∀j ∈ [m]= Rj + 1

n

> n

X

> i=1

(a′( ˜f (xi)) − a′(f ∗(xi)))( fˆj (xi) − fj (xi)) ,

where the first inequality stems from the minimality of bRˆj , and the last equality is obtained by re-applying fact E.1 to Rj .45 By the Mean Value Theorem, we can write 

a′( ˜f (xi)) − a′(f ∗(xi)) = a′′ (qi)( ˜f (xi) − f ∗(xi)) 

for some qi between f ∗(xi) and ˜f (xi). Denote eD the positive diagonal matrix of entries a′′ (qi) that induces an inner product and a (weighted) norm so that the previous inequality becomes: 

Rˆj ≤ R j + 1

n

D

fˆj − fj , B + ˜ ε

E 

> eD

. (E.1) We cannot directly use a sub-exponential tail bound for the inner product because ˜ε and eD are correlated. Thus we introduce the intermediate weight matrix D with components Dii = a′′ (f ∗(xi)) 

and use the fact that the perturbation introduced is small thanks to the consistency rate assumed. In the remainder of this proof, we will use repeatedly the following inequalities linking the 

eD-weighted norm, the D-weighted norm, and the standard Euclidean norm: 

Fact E.2. For all z ∈ Rn:1. ∥z∥2 ≤ 1√k ∥z∥ eD , ∥z∥ eD ≤ √K ∥z∥2.2. ∥z∥D ≤ √2 ∥z∥ eD.Proof. Since, we assume that the ˜f − f ∗ 

> ∞

≤ η ≤ C, and ∥f ∗∥∞ ≤ C, we can leverage our assumptions on the local properties of the log-partition function a in [−2C, 2C]. Part 1 directly follows from the definition of the weighted norm and boundedness of a′′ . For part 2, we obtain a stronger bound by leveraging the Lipschitzness assumption on a′′ and the uniform consistency of the imputation model: 

∥z∥2 

> D

− ∥ z∥2 

> eD

≤ X

> i

z2 

> i

a′′ (qi) − a′′ (f ∗(xi)) ≤ Lη k

X

> i

kz 2 

> i

,

hence, via the strong convexity assumption, 

∥z∥2 

> D

− ∥ z∥2 

> eD

≤ Lη k

X

> i

a′′ (qi)z2 

> i

≤ Lη k ∥z∥2 

> eD

.

Therefore, since Lη k < 1 according to the assumptions, 

∥z∥2 

> D

≤ 2 ∥z∥2 

> eD

.

To make progress on inequality E.1, we decompose the inner product into three terms that we can tackle independently: 

Rˆj ≤ R j + 1

n

D

fˆj − fj , B

E 

> eD

+

D

fˆj − fj , ˜ε

E 

> D

+

D

fˆj − fj , ˜ε

E

> eD−D



.

• Cauchy-Schwarz for the first term yields: 

D

fˆj − fj , B

E 

> eD

≤ fˆj − fj eD ∥B∥ eD ≤ √K fˆj − fj eD ∥B∥2 .

46 • Cauchy-Schwarz for the third perturbation term gives: 

D

fˆj − fj , ˜ε

E  

> eD−¯D

≤ eD − ¯D ∥ ˜ε∥2 fˆj − fj 2 ≤ L

√k max 

> i∈[n]

˜f (xi) − f ∗(xi) · ∥ ˜ε∥2 · fˆj − fj eD ,

where we used eD − ¯D = max i |a′′ (qi) − a′′ (f ∗(xi)) | ≤ L max i ˜f (xi) − f ∗(xi) . We will see that the consistency rate of our estimator is good enough to make this term of the same order as the two others. We can improve the max i∈[n] ˜f (xi) − f ∗(xi) · ∥ ˜ε∥2 factor by noting that: By assumption, ∀i ∈ [n], |Bi + ˜ εi| ≤ η, hence | ˜εi| ≤ η + |Bi| and ∥ ˜ε∥22 ≤ 2( nη 2 + ∥B∥22).Therefore, 

max 

> i∈[n]

˜f (xi) − f ∗(xi) 2

· ∥ ˜ε∥22 ≤ 2( nη 4 + η2 ∥B∥22). (E.2) 

• For the second term, first bound its sub-exponential norm: 

D

fˆj − fj , ˜ε

E 

> Dψ1

= D(fˆj − fj ) 2

* D(fˆj − fj )

D(fˆj − fj ) 2

, ˜ε

+ 

> Dψ1

≤ √K fˆj − fj D

* D(fˆj − fj )

D(fˆj − fj ) 2

, ˜ε

+ 

> Dψ1

≤ ˜σ√K fˆj − fj D

≤ ˜σ√2K fˆj − fj eD .

Hence, the sub-exponential property and union bound gives that the event 

A =

n

∀j ∈ [m],

D

fˆj − fj , ˜ε

E 

> D

≤ √2K ˜σ log(4 m/δ ) fˆj − fj eD

o

holds with probability at least 1 − δ/ 2. We place ourselves under A.So we obtain with probability at least 1 − δ:

Rˆj ≤ R j + 2ξn fˆj − fj ˜D , (E.3) where ξ = ( √2K ˜σ log(4 m/δ ) + √K ∥B∥ + L max i∈[n]| ˜f (xi)−f ∗(xi)|·∥ ˜ε∥2 

> √k

)/2.Then using the properties of the energy norm and the boundedness of a′′ we get: 

1

√n fˆj − fj ˜D ≤

r Kk

1

√n fˆj − fj ¯D

≤

r Kk

1

√n



fˆj − f ∗ 

> ¯D

+ ∥fj − f ∗∥ ¯D



≤

r Kk

√K

√n



fˆj − f ∗ + ∥fj − f ∗∥



47 ≤ Kk

pRj +

q

Rˆj



,

where the last inequality follows from (A.2). Hence, with ˜ξ = Kk ξ/ √n, we get: 

Rˆj ≤ R j + 2 ˜ξ

pRj +

q

Rˆj



,

and we can now complete the square: 

• Rearranging: 

Rˆj − 2 ˜ξ

q

Rˆj ≤ R j + 2 ˜ξpRj ⇔ (

q

Rˆj − ˜ξ)2 − ˜ξ2 ≤ (pRj + ˜ξ)2 − ˜ξ2.

Thus, q

Rˆj ≤ pRj + 2 ˜ξ. 

Since this is valid for all j, an intermediate result is thus: 

q

Rˆj ≤ pRj∗ + 2 ˜ξ. 

• Squaring on both sides, we obtain: 

Rˆj ≤ R j∗ + 2 

q

4 ˜ξ2Rj∗ + 4 ˜ξ2.

Using the simple inequality 2ab ≤ a2 + b2 and the "Rob Peter to pay Paul" trick with any ϵ > 0,i.e. 2ab = 2 aϵ ϵb ≤ a2 

> ϵ2

+ ϵ2b2, we get: 

Rˆj ≤ (1 + ϵ)Rj∗ + 4 ˜ξ2(1 + 1

ϵ ).

Finally, to get the final formula of the theorem, we leverage (E.2) to bound ˜ξ2 as: 

˜ξ2 ≲ Kkn (K ˜σ log 2(m/δ ) + ∥B∥22 (K + 2 L2η2/k ) + L2nη 4

k ).

Using η ≤ √kK/L and taking the infimum on ϵ yields the final result. We deduce a bound on the expectation: 

Corollary E.1. Under the same assumptions as Theorem 5.1, we deduce: 

E

h

Rˆj

i

≤ inf 

> ϵ> 0



(1 + ϵ)Rj∗ + C1Knk 



1 + 1

ϵ

  

K ∥B∥2 + L2nη 4

k + K ˜σ2(1 + (log( m) + 1) 2)

 

.

Proof. Denoting W = [ Rˆj − (1 + ϵ)Rj∗ − C1Knk (1 + 1 

> ϵ

)( K ∥B∥2 + L2nη 4/k )] / C1Knk (1 + 1 

> ϵ

)K ˜σ2, we have from the theorem: 

P  W ≥ log 2(m/δ ) ≤ δ, ∀δ ∈ (0 , 1) .

Let t = log 2(m/δ ) such that: 

P (W ≥ t) ≤ me −√t, ∀t ≥ log 2(m).

Integrating to get the expectation yields: 

E [W ] = 

Z ∞

> 0

P (W > t ) dt ≤

Z log 2(m)0

1dt +

Z ∞

> log 2(m)

me −√tdt = (1 + log( m)) 2 + 1 .

Rearranging the terms and taking the infimum gives the desired result. 48 F Useful lemmas 

F.1 Lemmas for the GLM estimator 

Lemma F.1.1. Let the kernel regularity assumption 4.1 hold, with spectral eigendecay of the form 

μj ≤ Aj −2α, α ≥ 1, and let λ ∈ (0 , 1] . Then the effective dimension r(λ) := 

> ∞

X

> j=1

μj

μj + λ satisfies for a universal constant C > 0:

r(λ) ≤ Cλ −1/2α.

In particular, if the trace class operator Σ has eigenvalues μj , then Tr (( Σ + λI)−1Σ) = r(λ) ≲

λ−1/2α.Likewise, for a vector Φ( x) of the RKHS with basis decomposition Φ( x) = ( √μ1ϕ1(x), √μ2ϕ2(x)... ),where the basis functions are uniformly bounded by M1, the quadratic form Φ( x), (Σ + λI)−1Φ( x) ≤

M 21 r(λ) ≲ M 21 λ−1/2α.Proof. Split the sum before and after j = λ−1/2α and use the asymptotic tail of the series. Without loss of generality, we assume A = 1 (for A̸ = 1 , use λ = λ/A in the formula). 

> ∞

X

> j=1

μj

μj + λ = X

> j≤λ−1/2α

11 + λj 2α + X

> j>λ −1/2α

11 + λj 2α

≤ X

> j≤λ−1/2α

1 + X

> j>λ −1/2α

1

λj 2α

≤ λ−1/2α + 1

λ(2 α − 1) λ−(2 α−1) /2α

≤ 2α

2α − 1 λ−1/2α.

We find that C = 2αA 1/2α 

> 2α−1

in general. 

Remark F.1. By the eigendecay for the source distribution, applying Lemma F.1.1 with α = 1 

directly gives for λ ∈ (0 , 1) Tr( Cλ) ≲ λ−1/2.

F.2 Technical lemmas 

Lemma F.2.1. Let x be a centered sub-exponential vector with ∥xi∥ψ1 ≤ 1, ∀i ∈ [n], A ⪰ 0 a fixed PSD matrix in Rn×n. Then there exists a universal constant C > 0, such that ∀δ ∈ (0 , 2 

> e

], with probability at least 1 − δ,

x⊤Ax ≤ C Tr( A) log 2(n) log( 2

δ )

when n ≥   4

> δ

1/3.Proof. We will truncate the sub-exponential variables to get sub-gaussian variables and bound the gap between the quadratic forms. Then use lemma D.1 in (Wang, 2026). 

• First by definition of sub-exponential, 

∀i, P (|xi| > C log n) ≤ 2n−C

49 for any constant C > 0. Hence if we define the truncated version ¯x of components ¯xi := xi1(|xi| ≤ 

C log n), by union bound, x = ¯x with probability larger than 1 − 2n1−C . Denote this event A.Also, by definition of ¯x, we have immediately that ∥ ¯x∥ ≤ C√n log n almost surely. 

• ¯x is not centered anymore so let its mean be μ. Then ∀i, |μi| = E [xi1(|xi| ≤ C log n)] =

E [xi1(|xi| > C log n)] since E [xi] = 0 . So Cauchy-Schwarz yields: 

|μi| ≤ 

r

E

h

|xi|2i

P (|xi| > C log n) ≤ 2√2n−C/ 2 ≲ n−C/ 2,

where the first factor is bounded by a constant by the sub-exponential property, and the second obeys the tail bound mentioned before. Hence: 

∥μ∥ ≲ √nn −C/ 2 = n(1 −C)/2.

• Next define the centered version ˜x := ¯x − μ. It is centered and bounded almost surely: ∥ ˜x∥∞ ≤

2C log n hence it is sub-gaussian with ∥ ˜x∥ψ2 ≲ C log n. Thus its quadratic form satisfies a tail bound as proposed in lemma D.1 of (Wang, 2026) for a universal constant C′ > 0, ∀δ ∈ (0 , 2 

> e

],with probability at least 1 − δ 

> 2

:

˜x⊤A ˜x ≤ C′ ∥ ˜x∥2 

> ψ2

Tr( A) log(2 /δ ) ≲ log 2(n) Tr( A) log(2 /δ ).

Denote this event B.

• Finally, we can bound the difference ˜x⊤A ˜x − ¯x⊤A ¯x = μ⊤A(2 ¯ x − μ) ≤ ∥ A∥ (2 ∥μ∥ ∥ x∥ +

∥μ∥2) ≤ ∥ A∥ (n1−C + n1−C/ 2 log n).Putting everything together, under the event A ∩ B which holds with probability larger than 

1 − 2n1−C − δ 

> 2

by union bound, we get: x⊤Ax = ˜x⊤A ˜x + ( x⊤Ax − ¯x⊤A ¯x) + ( ¯x⊤A ¯x − ˜x⊤A ˜x) ≲

∥A∥ (n1−C + n1−C/ 2 log n) + log 2 n Tr( A) log(2 /δ ).Choosing C = 4 , since log(2 /δ ) ≥ 1 for δ ≤ 2/e , and ∥A∥ ≤ Tr( A), yields: 

x⊤Ax ≲ Tr( A) log( 2

δ )



log 2(n) + 1

n3 + log( n)

n



with probability larger than 1 − 2 

> n3

− δ 

> 2

. For n ≥   4

> δ

1/3, the probability is larger than 1 − δ as wanted. For δ = 0 .05 , this threshold is just n ≥ 5. Removing the negligible terms yields the desired result. 

Lemma F.2.2. Let Assumptions 2.1 and 4.1 hold, with M2 the bound on the covariates Φ( xi).Choose γ ∈ (0 , 1) , δ ∈ (0 , 1/e ], and λ ≥ 4M 22 log(14 n/δ ) 

> γ2n

. Then the following event holds with probability at least 1 − δ:

P



(1 − γ)( Σ + λI) ⪯ ˆΣ + λI ⪯ (1 + γ)( Σ + λI)



≥ 1 − δ. 

Under that event, the following inequalities are true: 1. For any vector a ∈ H,

D

a, ( ˆΣ + λI)−1a

E

≤ (1 − γ)−1 a, (Σ + λI)−1a .

50 2. Tr (( ˆΣ + λI)−1 ˆΣ) ≤ M 21 (1 − γ)−1 Tr (( Σ + λI)−1Σ), where M1 > 0 is the uniform bound on the eigenfunctions. Proof. The first part is just a repetition of corollary D.1 in (Wang, 2026). Then, the first inequality is a direct corollary from the main sandwich inequality, by definition of the PSD order. Finally, for the second inequality, observe that the sandwich inequality implies: 

Tr(( ˆΣ + λI)−1 ˆΣ) ≤ 11 − γ Tr(( Σ + λI)−1 ˆΣ).

Define ˜Φi := ( Σ + λI)−1/2Φ( xi) such that 

Tr(( Σ + λI)−1 ˆΣ) = Tr(( Σ + λI)−1/2 ˆΣ(Σ + λI)−1/2) = 1

n

> n

X

> i=1

˜Φi

> 2

.

Since by uniform boundedness of the eigenfunctions, 

˜Φi

> 2

=

> ∞

X

> j=1

μj

μj + λ |ϕj (xi)|2 ≤ M 21

> ∞

X

> j=1

μj

μj + λ = M 21 Tr(( Σ + λI)−1Σ),

we get: 

Tr(( Σ + λI)−1 ˆΣ) ≤ M 21 Tr(( Σ + λI)−1Σ),

and the result follows. 

F.3 Relating the source covariance operator to the target’s via the effective sample size 

Through the effective dimension defined as neff = sup 



t ≤ n|tΣ0 ⪯ nΣ + μ2 

> k

I



in equation (4.1), we can relate expressions depending on Σ to Σ0-only expressions. Take λ ≥ μ2 

> n

, and let r = neff  

> n

. On the one hand we have 

2( kΣ + λI) ⪰ (kΣ + μ2/n I) + λI ⪰ rk Σ0 + λI = r(kΣ0 + r−1λI),

hence 

(kΣ + λI)−1 ⪯ 2r−1(kΣ0 + r−1λI)−1, (F.1) and 

Tr( Sλ) ≤ 2r−1 Tr(( kΣ0 + λr −1I)−1Σ0). (F.2) On the other hand, we have simply: 

kΣ + λI ⪰ rk Σ0,

hence: 

∥Sλ∥ ≤ r−1k−1. (F.3) 51 G Complement on the numerical experiments from Section 6 

G.1 Implementation details 

We optimize the ridge-regularized kernel GLM (2.7) in function space f = Kα via Fisher scoring (McCullagh and Nelder, 1989), i.e. Newton’s method with the Hessian replaced by the expected Hessian (Fisher information). At each iteration, with current evaluations ηi = f (xi), mean μi =

E[yi | ηi], and variance Vi = Var [yi | ηi], we compute the standard GLM weights W = diag (( dμ i 

> dη i

)2/V i)

and pseudo-responses zi = ηi + ( yi − μi)/ dμ i 

> dη i

. The resulting iteratively reweighted least squares (IRLS) update requires solving (KW + nλ I)f = KW z . K ∈ Rn×n is the kernel matrix introduced in equation (2.8). To ensure stable convergence of our conjugate gradient (Hestenes and Stiefel, 1952) (CG) solver, we avoid extreme weight scalings by defining S = W 1/2 and substituting u = Sf to solve the equivalent symmetric positive definite system: 

(SKS + nλ I)u = SKW z 

We then recover f = S−1u.For large-scale problems, forming the dense n × n matrix K introduces an O(n2) memory bottleneck. We circumvent this by implementing the inner CG solver using KeOps (Charlier et al., 2021) for on-the-fly GPU kernel-vector products. This strategy reduces the memory footprint to 

O(nd ) without altering the exact IRLS update equations. Full algorithmic details and solvers are available at https://github.com/nathanweill/KRGLM/ .

G.2 Other synthetic data result 

Figure 3 shows the results for the same experiments as in Section 6.1, with covariate shift strength 

B = n0.45 . Estimates of the α coefficients are 0.434(0 .049) , 0.451(0 .053) and 0.360(0 .047) for the pseudo-labeling, oracle, and naive methods, respectively. 4000 8000 16000 32000 10 2  

> 2 × 10 2
> 3 × 10 2

Figure 3: B = n0.45 

G.3 Details about the real data experiments 

Let us explain in more details the experiments of Section 6.2. After sub-sampling, we have labeled in-distribution (ID) data D = {(xi, y i)}ni=1 and labeled out-of-distribution (OOD) data 52 Table 3: Risk mean estimate across seeds with confidence interval (CI) and standard error (SE).             

> Method Mean 95% CI SE Naive 0.444 [0.414, 0.474] 0.015 Pseudo-labeling 0.385 [0.373, 0.396] 0.006 Oracle 0.376 [0.363, 0.390] 0.007

D0 = {(x0,i , y 0,i )}n0 

> i=1

(OOD labels are used only for oracle benchmarking and final evaluation). We first split the OOD sample into a selection set and a test set, D0 = D0,sel ∪ D 0,test , and report all final metrics on D0,test .Recall that for a score function f : X → R, the empirical logistic risk on a finite set S with targets t ∈ [0 , 1] is 

RS (f ; t) := 1

|S|

X

> x∈S



a(f (x)) − t(x) f (x)



, a(u) = log(1 + eu),

where t(x) ∈ { 0, 1} yields standard log-loss and t(x) ∈ [0 , 1] yields the corresponding soft-label risk. As mentioned in Section 6.2, due to the small sample size, we use a slightly modified selection procedure compared to Algorithm 1 and Section 6.1, to deflate the variance of the final estimator. It is detailed in Algorithm 2. Each fitting in steps 2 and 5 is a ridge logistic regression task. 

Hyperparameters of the Raisin experiment In the specific case of the Raisin dataset, we chose λ1 = 10 −4 to λm = 10 2 for the candidates grid, and ˜λ = 10 −4 for the imputer. Then we ran Algorithm 2 over 100 random seeds. In Section 6.2, we reported the results for K = 2 , R = 6 .Table 3 provides the results for the same experiment, but using K = 5 , R = 2 instead. Finally for this 5-fold (2 repeats) cross-validation experiment, we plotted the risk curves used for model selection for each of the three methods, described in step 3’s equation (G.1), averaged over seeds in Fig. 4. The pseudo-labeling method constructs a risk curve consistent with the oracle, thus achieving good model selection for the target. In contrast, the naive risk curve stays flat. 53 Figure 4: Selection risk curves of the three methods. 54 Algorithm 2 Repeated K-Fold Cross-Validated Pseudo-Labeling (Practical Variant)  

> 1:

Input : Labeled ID data D = {(xi, y i)}ni=1 , labeled OOD data D0 = {(x0i, y 0i)}n0 

> i=1

split into 

D0,sel and D0,test . OOD labels are not used by the pseudo-labeling method; they serve only for the oracle benchmark and final evaluation. Candidate grid Λ = {λ1, . . . , λ m}, imputer penalty 

˜λ, number of folds K, number of repeats R. 

> 2:

Step 1 (Repeated K-fold partition). For each repeat r ∈ [R], draw a fresh stratified (i.e. preserving class proportions) K-fold partition of the ID indices: 

{1, . . . , n } =

> K

G

> k=1

F (r) 

> k

, F (r) 

> k

∩ F (r) 

> k′

= ∅.

Define the candidate and imputer index sets for each fold (r, k ):

I(r,k )cand := F (r) 

> k

, I(r,k )imp := {1, . . . , n } \ F (r) 

> k

. 

> 3:

Step 2 (Per-fold training and evaluation). For each fold (r, k ): 

> 4:

(a) Imputer. Fit ˜f (r,k ) on {(xi, y i)}i∈I(r,k )imp 

with penalty ˜λ. Compute soft pseudo-labels: 

p(r,k )(x) := σ( ˜f (r,k )(x)) , x ∈ D 0,sel , σ(u) = (1 + e−u)−1. 

> 5:

(b) Candidates. For each λj ∈ Λ, fit f (r,k ) 

> λj

on {(xi, y i)}i∈I(r,k )cand 

. 

> 6:

(c) Risk curves. For each λj , compute: 

bR(r,k )naive (λj ) := R{xi:i∈I(r,k )imp }

 f (r,k ) 

> λj

; y,

bR(r,k )pseudo (λj ) := RD0,sel 

 f (r,k ) 

> λj

; p(r,k ),

bR(r,k )oracle (λj ) := RD0,sel 

 f (r,k ) 

> λj

; y0

. 

> 7:

Step 3 (Aggregation). Average over all folds and repeats: 

bR⋆(λj ) := 1

KR 

> R

X

> r=1
> K

X

> k=1

bR(r,k ) 

> ⋆

(λj ), ⋆ ∈ { naive , pseudo , oracle }. (G.1)  

> 8:

Step 4 (Selection). Select regularization parameters independently: 

ˆλ⋆ ∈ arg min 

> λ∈Λ

bR⋆(λ), ⋆ ∈ { naive , pseudo , oracle }. 

> 9:

Step 5 (Refit). For each ⋆ ∈ { naive , pseudo , oracle }, refit a final model ˆf⋆ on all labeled ID data D. 

> 10:

Step 6 (Evaluation). Evaluate each final model on the held-out OOD test set: 

Rtest ( ˆf⋆) := RD0,test 

  ˆf⋆; y0

. 

> 11:

Output : Test risks Rtest ( ˆfnaive ), Rtest ( ˆfpseudo ), Rtest ( ˆforacle ).55