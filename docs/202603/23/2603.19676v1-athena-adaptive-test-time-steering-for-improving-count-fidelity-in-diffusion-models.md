---
title: "ATHENA: Adaptive Test-Time Steering for Improving Count Fidelity in Diffusion Models"
title_zh: ATHENA：用于提高扩散模型计数保真度的自适应测试时引导
authors: "Mohammad Shahab Sepehri, Asal Mehradfar, Berk Tinaz, Salman Avestimehr, Mahdi Soltanolkotabi"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19676v1"
tags: ["query:tta"]
score: 8.0
evidence: 扩散模型的自适应测试时引导框架
tldr: 针对文本生成图像扩散模型在处理明确物体数量指令时经常失效的问题，本文提出了ATHENA框架。这是一种模型无关的测试时自适应引导方法，无需重新训练或修改架构。它通过在去噪早期利用中间表示估计物体数量并应用计数感知噪声修正，从而在结构错误难以修改前引导生成轨迹。实验证明，ATHENA在多个扩散模型底座上显著提升了物体计数的准确性，尤其是在高数量目标下表现优异，且提供了灵活的计算与精度权衡方案。
selection_source: fresh_fetch
motivation: 现有的扩散模型在生成指定数量的物体时存在系统性失效，难以根据提示词精确控制物体的数值表现。
method: 提出一种在采样过程中利用中间表示估计数量并进行早期噪声修正的测试时自适应引导框架。
result: 在多个基准测试和复杂数据集上，ATHENA显著提高了物体计数的保真度，尤其是在高数量目标下表现出色。
conclusion: ATHENA提供了一种无需重训即可增强扩散模型数值控制能力的有效方案，在多种模型底座上展现了良好的通用性。
---

## 摘要
文本到图像扩散模型实现了极高的视觉保真度，但在提示词指定明确的对象数量时，却出人意料地在数值控制方面表现出系统性失效。为了解决这一局限性，我们引入了 ATHENA，这是一个与模型无关的测试时自适应引导框架，它在不修改模型架构或无需重新训练的情况下提高了对象计数保真度。ATHENA 利用采样过程中的中间表示来估计对象数量，并在去噪过程早期应用计数感知噪声修正，在结构性错误变得难以修正之前引导生成轨迹。我们提出了 ATHENA 的三个渐进式高级变体，通过额外的计算开销换取更高的数值准确度，范围涵盖从基于静态提示的引导到动态调整的计数感知控制。在既有基准测试和一个新的视觉及语义复杂的测试集上的实验表明，ATHENA 持续提高了计数保真度，特别是在目标计数较高的情况下，同时在多个扩散骨干网络中保持了良好的准确率与运行时间的权衡。

## Abstract
Text-to-image diffusion models achieve high visual fidelity but surprisingly exhibit systematic failures in numerical control when prompts specify explicit object counts. To address this limitation, we introduce ATHENA, a model-agnostic, test-time adaptive steering framework that improves object count fidelity without modifying model architectures or requiring retraining. ATHENA leverages intermediate representations during sampling to estimate object counts and applies count-aware noise corrections early in the denoising process, steering the generation trajectory before structural errors become difficult to revise. We present three progressively more advanced variants of ATHENA that trade additional computation for improved numerical accuracy, ranging from static prompt-based steering to dynamically adjusted count-aware control. Experiments on established benchmarks and a new visually and semantically complex dataset show that ATHENA consistently improves count fidelity, particularly at higher target counts, while maintaining favorable accuracy-runtime trade-offs across multiple diffusion backbones.

---

## 论文详细总结（自动生成）

以下是对论文《ATHENA: Adaptive Test-Time Steering for Improving Count Fidelity in Diffusion Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管当前的文本生成图像（T2I）扩散模型（如 SDXL, Flux）在视觉保真度上表现卓越，但在**数值控制（物体计数）**方面存在系统性失效。例如，要求生成“八头牛”时，模型经常出现漏掉、重复或物体融合的情况。
*   **核心挑战**：物体数量通常在去噪过程的早期阶段（结构形成期）就已确定，一旦早期轨迹偏离目标数量，后期微调很难修正。
*   **整体含义**：本文提出了 **ATHENA** 框架，旨在通过一种**模型无关（Model-agnostic）**、**无需重新训练**的测试时（Test-time）引导机制，在生成早期动态修正噪声轨迹，从而提高物体计数的准确性。

### 2. 方法论
ATHENA 的核心思想是在采样过程中引入“反馈-引导”循环，通过中间表示估计物体数量并应用噪声修正。

*   **核心技术细节**：
    1.  **中间计数估计**：在去噪的中间步骤 $t_{est}$，将当前的潜在表示解码为图像，利用预训练检测器（如 GroundingDINO）估计当前物体数量。
    2.  **提示词引导机制**：通过原始提示词 $p$ 和控制提示词 $\hat{p}$（如移除数量词或替换为错误数量词）的噪声预测差值来引导轨迹。公式表示为：$\tilde{\epsilon}_t = \epsilon_t + \gamma (\epsilon_t - \hat{\epsilon}_t)$，其中 $\gamma$ 是引导强度。
*   **三个渐进变体**：
    *   **ATHENA-Static**：使用固定的、不含数量词的控制提示词进行引导，不进行中间反馈。
    *   **ATHENA-Feedback**：根据中间步骤估计的数量，动态构建控制提示词并重启采样。
    *   **ATHENA-Adaptive（核心方法）**：在 Feedback 的基础上，根据引导后的效果自动调整引导强度 $\gamma$（若未达标则加倍，若过冲则减半），实现更精准的控制。

### 3. 实验设计
*   **扩散模型底座（Backbones）**：SDXL、Stable Diffusion 3.5 Large、FLUX.1-dev。
*   **数据集**：
    1.  **CoCoCount**：去重后的标准计数基准。
    2.  **CoCoCount-E**：扩展版，涵盖 2 到 10 个物体的平衡分布。
    3.  **ATHENA Dataset（新提出）**：包含 360 个更具挑战性的提示词，分为四个难度等级（硬物体类别、场景上下文、干扰物、空间关系）。
*   **对比方法（Baselines）**：原始未引导模型（Unsteered）、CountGen (Make It Count)、Counting Guidance。

### 4. 资源与算力
*   **硬件设备**：实验使用了 **NVIDIA RTX A6000 (48 GB)** 和 **NVIDIA A100 SXM4 (40 GB)** GPU。
*   **训练时长**：该方法为**推理时引导（Inference-only）**，无需任何模型训练或微调。
*   **推理开销**：ATHENA-Adaptive 需要最多两次额外的早期引导尝试。在 SDXL 上，其显存占用（11.9 GB）远低于 CountGen（47.5 GB），生成速度比 CountGen 快约 2.5 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流模型底座和 3 个不同难度的数据集上进行了详尽测试，目标计数范围从 2 覆盖到 10。
*   **充分性评价**：实验设计较为充分。作者不仅对比了准确率（Exact-match Accuracy），还分析了平均绝对误差（MAE）和均方根误差（RMSE），并提供了准确率与运行时间的权衡分析图。
*   **公平性**：所有超参数在 CoCoCount 上调优后固定，直接应用于其他数据集，证明了方法的泛化能力。

### 6. 主要结论与发现
*   **准确率显著提升**：ATHENA-Adaptive 在 SDXL 上将计数准确率提升了高达 **22.3%**，在所有模型和数据集上均优于原始模型。
*   **高数量控制力强**：在 5-10 个物体的高难度场景下，ATHENA 的提升尤为明显，准确率接近原始模型的两倍。
*   **高效性**：相比于需要额外训练或复杂优化的基线方法，ATHENA 在保持高准确率的同时，显存占用更低，推理速度更快。

### 7. 优点
*   **模型无关性**：不需要修改扩散模型的架构，也不需要访问模型内部的注意力图（Attention Maps），具有极强的通用性。
*   **无需重训**：完全在推理阶段完成，降低了部署门槛。
*   **自适应性**：Adaptive 变体能根据生成过程中的实际情况动态调整策略，比静态引导更鲁棒。
*   **视觉质量保持**：在修正数量的同时，较好地保留了图像的语义结构和视觉保真度，避免了背景扭曲。

### 8. 不足与局限
*   **依赖外部检测器**：引导的效果高度依赖于中间步骤所使用的物体检测器（如 GroundingDINO）的准确性。如果检测器在复杂场景下失效，引导也会失败。
*   **推理延迟**：虽然比部分基线快，但由于涉及多次前向传播和中间解码，其生成时间仍高于标准的单次扩散采样。
*   **极端数量限制**：实验显示，当目标数量超过 10 时，所有方法（包括 ATHENA）的性能都会出现明显下降，这反映了基础模型本身的表达极限。

（完）
