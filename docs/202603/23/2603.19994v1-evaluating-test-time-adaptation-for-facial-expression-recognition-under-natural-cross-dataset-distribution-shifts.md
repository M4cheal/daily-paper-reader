---
title: Evaluating Test-Time Adaptation For Facial Expression Recognition Under Natural Cross-Dataset Distribution Shifts
title_zh: 评估自然跨数据集分布偏移下的人脸表情识别测试时自适应方法
authors: "John Turnbull, Shivam Grover, Amin Jalali, Ali Etemad"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19994v1"
tags: ["query:tta"]
score: 10.0
evidence: 自然分布偏移下测试时间自适应方法的评估
tldr: "本研究针对面部表情识别（FER）在现实部署中面临的自然分布偏移问题，首次系统评估了测试时自适应（TTA）方法的有效性。通过在多个主流FER数据集上进行跨数据集实验，研究探讨了不同TTA策略（如熵最小化、原型调整和特征对齐）在应对由采集协议、标注标准和人口统计差异引起的真实偏移时的表现。结果表明，TTA能显著提升模型性能，最高增幅达11.34%，并揭示了方法选择与分布距离及偏移程度的内在联系。"
selection_source: fresh_fetch
motivation: 深度学习模型在面部表情识别中常因跨数据集的自然分布偏移而性能下降，需要在无源标签的情况下在推理阶段进行自适应。
method: 在多个主流FER数据集上开展跨数据集实验，对比评估了TENT、SAR、T3A和SHOT等多种测试时自适应方法在自然领域偏移下的表现。
result: "实验显示TTA最高可提升11.34%的准确率，其中TENT在干净分布下表现优异，T3A适合大分布距离，而SHOT在噪声分布下效果最佳。"
conclusion: TTA的有效性受分布距离和偏移严重程度的影响，针对不同类型的自然偏移选择合适的自适应策略是提升FER模型鲁棒性的关键。
---

## 摘要
深度学习模型在自然分布偏移下往往表现不佳，这是实际部署中面临的一个共同挑战。测试时自适应（TTA）通过在推理过程中调整模型而无需标记的源数据来解决这一问题。我们首次评估了自然领域偏移下用于人脸表情识别（FER）的 TTA 方法，并使用广泛使用的 FER 数据集进行了跨数据集实验。这超越了合成损坏，旨在研究由不同的采集协议、标注标准和人口统计特征引起的真实世界偏移。结果表明，TTA 在自然偏移下可将 FER 性能提升高达 11.34%。当目标分布较清晰时，诸如 TENT 和 SAR 等熵最小化方法表现最佳。相比之下，像 T3A 这样的原型调整方法在分布距离较大的场景下表现出色。最后，当目标分布比源分布噪声更多时，诸如 SHOT 等特征对齐方法带来的提升最大。我们的跨数据集分析表明，TTA 的有效性受分布距离和跨领域自然偏移严重程度的影响。

## Abstract
Deep learning models often struggle under natural distribution shifts, a common challenge in real-world deployments. Test-Time Adaptation (TTA) addresses this by adapting models during inference without labeled source data. We present the first evaluation of TTA methods for FER under natural domain shifts, performing cross-dataset experiments with widely used FER datasets. This moves beyond synthetic corruptions to examine real-world shifts caused by differing collection protocols, annotation standards, and demographics. Results show TTA can boost FER performance under natural shifts by up to 11.34\%. Entropy minimization methods such as TENT and SAR perform best when the target distribution is clean. In contrast, prototype adjustment methods like T3A excel under larger distributional distance scenarios. Finally, feature alignment methods such as SHOT deliver the largest gains when the target distribution is noisier than our source. Our cross-dataset analysis shows that TTA effectiveness is governed by the distributional distance and the severity of the natural shift across domains.

---

## 论文详细总结（自动生成）

### 评估自然跨数据集分布偏移下的人脸表情识别测试时自适应方法

#### 1. 核心问题与整体含义（研究动机和背景）
面部表情识别（FER）模型在实验室基准测试中表现优异，但在实际部署中常因“自然分布偏移”（Natural Distribution Shifts）导致性能大幅下降。这种偏移源于不同数据集之间在采集协议、标注标准、人口统计特征（如种族偏差）以及图像质量上的差异。
**测试时自适应（TTA）** 是一种在推理阶段无需访问源数据或目标标签即可调整模型的技术。然而，现有的 TTA 研究大多集中在合成损坏（如噪声、模糊）上，缺乏对 FER 领域真实自然偏移的评估。本论文旨在填补这一空白，系统评估 TTA 方法在跨数据集 FER 任务中的有效性。

#### 2. 论文提出的方法论
论文的核心思想是通过跨数据集实验模拟真实世界的分布偏移，并利用多种 TTA 策略进行修复。
*   **分布距离度量：** 引入 **最大均值差异（MMD）** 和 RBF 核函数来量化数据集间的特征空间距离，计算相似度得分 $S$。
*   **评估的 TTA 策略：**
    *   **熵最小化（Entropy Minimization）：** 如 **TENT**、**EATA**、**SAR**。通过最小化预测结果的熵来更新模型参数（如 LayerNorm 的缩放和偏移参数）。
    *   **特征对齐（Feature Alignment）：** 如 **SHOT**。冻结分类器，通过信息最大化和伪标签技术使目标特征与源分类器对齐。
    *   **原型调整（Prototype Adjustment）：** 如 **T3A**。不更新模型权重，而是根据高置信度样本动态调整类原型（质心）。
    *   **持续自适应（Continual Adaptation）：** 如 **NOTE**、**CoTTA**、**RoTTA**。利用教师-学生网络、内存回放等机制应对流式数据中的时间相关性。

#### 3. 实验设计
*   **数据集：** 使用了三个主流 FER 数据集：**AffectNet**（规模大、噪声多）、**RAF-DB**（标注较准、种族分布较窄）、**FERPlus**（标注一致性高）。
*   **场景设置：** 构建了 12 种跨数据集场景（如 AffectNet $\rightarrow$ RAF-DB，以及多源域组合场景）。
*   **Benchmark 与对比方法：**
    *   **基准（Baseline）：** 预训练的 ViT-L/16 模型（基于 FMAE-IAT 检查点）。
    *   **对比方法：** 涵盖了上述四类共 8 种最先进的 TTA 算法。
*   **评估指标：** Top-1 准确率、推理延迟（Latency）和内存占用（Memory）。

#### 4. 资源与算力
*   **硬件：** 使用了单张 **NVIDIA A100 40GB GPU**。
*   **训练细节：** 
    *   源模型训练：RAF-DB 运行 60 个 epoch，FERPlus 和 AffectNet 运行 30 个 epoch。
    *   TTA 过程：采用流式设置（Streaming setting），Batch Size 为 16，学习率为 $1 \times 10^{-3}$。
*   **效率测量：** 论文明确记录了各方法的推理耗时（ms）和显存占用（MB）。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 12 组跨数据集迁移实验，每组实验均运行 3 个随机种子并取平均值。
*   **充分性：** 实验不仅涵盖了单源到单目标的迁移，还包含了多源域组合（Multi-source）的复杂场景。
*   **客观性：** 通过 t-SNE 可视化和 MMD 相似度得分对实验结果进行了定性与定量的双重解释，证明了结果的可靠性。

#### 6. 主要结论与发现
*   **性能提升：** TTA 最高可将 FER 准确率提升 **11.34%**。
*   **方法适用性：**
    *   **熵最小化（TENT/SAR）：** 当目标域比源域更“干净”（标注噪声少）时效果最好。
    *   **特征对齐（SHOT）：** 在目标域噪声极大（如迁移到 AffectNet）时表现最强，但在伪标签不可靠时会起反作用。
    *   **原型调整（T3A）：** 在源域与目标域相似度较低（$S$ 值小）时最有效。
*   **持续自适应的局限：** CoTTA 等方法在自然偏移下表现不佳，容易因过度拟合噪声批次而产生误差累积。
*   **效率：** T3A 是最轻量高效的方法，而持续自适应方法（如 CoTTA）显存开销巨大。

#### 7. 优点
*   **现实意义强：** 首次针对 FER 领域的“自然偏移”而非“人工合成噪声”进行 TTA 评估，更符合工业界部署需求。
*   **可解释性：** 引入 MMD 相似度得分，成功建立了“数据集距离”与“特定 TTA 方法效果”之间的逻辑联系。
*   **全面性：** 综合考虑了准确率、延迟和内存，为实际应用提供了选型指南。

#### 8. 不足与局限
*   **数据集覆盖有限：** 仅使用了 3 个数据集，虽然具有代表性，但尚未涵盖所有可能的 FER 场景（如红外图像、侧脸等）。
*   **模型架构单一：** 实验主要基于 ViT 架构，未探讨 CNN 等其他架构在 TTA 下的鲁棒性差异。
*   **缺乏新算法：** 论文侧重于“评估”现有方法，并未提出针对 FER 自然偏移优化的新型 TTA 算法。

（完）
