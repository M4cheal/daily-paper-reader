---
title: "EVA: Aligning Video World Models with Executable Robot Actions via Inverse Dynamics Rewards"
title_zh: EVA：通过逆动力学奖励将视频世界模型与可执行机器人动作对齐
authors: "Ruixiang Wang, Qingming Liu, Yueci Deng, Guiliang Liu, Zhen Liu, Kui Jia"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17808v1"
tags: ["query:tta"]
score: 8.0
evidence: 机器人视频世界模型的推理时缓解技术
tldr: 针对视频世界模型在机器人任务中生成的视频虽视觉连贯但物理不可执行（可执行性差距）的问题，本文提出了EVA框架。该框架利用逆动力学模型（IDM）作为奖励模型，通过强化学习对视频生成模型进行后训练对齐。EVA通过评估生成视频所诱导的动作序列的平滑度及是否违反约束，显著减少了物理伪影，提升了机器人在仿真和真实环境中的任务成功率。
selection_source: fresh_fetch
motivation: 现有的视频世界模型生成的视频往往缺乏物理一致性，导致逆动力学模型解码出的机器人动作不稳定或不可执行。
method: 提出EVA框架，将逆动力学模型转化为奖励模型，通过强化学习优化视频生成模型，使其生成的视频更符合物理规律和机器人运动约束。
result: 在RoboTwin基准测试和真实双臂机器人实验中，EVA有效减少了生成视频中的物理伪影，并显著提高了下游任务的执行成功率。
conclusion: 通过将可执行性约束引入视频生成模型的对齐过程，可以有效弥合视觉生成与物理控制之间的差距，增强世界模型的实用性。
---

## 摘要
视频生成模型越来越多地被用作机器人学的世界模型。在这些模型中，系统根据当前观测和任务指令生成未来的视觉推演（rollout），并由逆动力学模型（IDM）将生成的帧转换为可执行的机器人动作。然而，目前的视频世界模型缺乏显式的可执行性约束。因此，视觉上连贯的推演仍可能违反刚体和运动学一致性，在被 IDM 解码时产生不稳定或不可行的控制指令。我们将这种视觉生成与物理可执行控制之间的不匹配称为“可执行性差距”（executability gap）。虽然在推理阶段可以使用拒绝采样等技术来缓解这一差距，但由于视频生成的成本很高，此类方法效率较低。在本文中，我们将可执行性差距作为训练信号，并引入了可执行视频对齐（EVA），这是一种用于对齐视频世界模型的强化学习后训练框架。EVA 在真实的机器人轨迹上训练逆动力学模型，并将其重新用作奖励模型，通过生成的视频所诱导的动作序列来评估视频，鼓励以速度、加速度和加加速度（jerk）衡量的平滑运动，同时惩罚违反具身约束的动作。重要的是，即使生成的视频包含严重的视觉伪影，该奖励仍然具有信息量，因为此类伪影通常会转化为不稳定或越界的动作。在 RoboTwin 基准测试和真实双臂机器人上的实验表明，EVA 减少了生成推演中特定于具身的伪影，并提高了下游任务的执行成功率。

## Abstract
Video generative models are increasingly used as world models for robotics, where a model generates a future visual rollout conditioned on the current observation and task instruction, and an inverse dynamics model (IDM) converts the generated frames into executable robot actions. However, current video world models lack explicit executability constraints. As a result, visually coherent rollouts may still violate rigid-body and kinematic consistency, producing unstable or infeasible control commands when decoded by an IDM. We refer to this mismatch between visual generation and physically executable control as the executability gap. While this gap can be mitigated at inference time using techniques such as rejection sampling, such approaches are inefficient due to the high cost of video generation. In this paper, we leverage the executability gap as a training signal and introduce Executable Video Alignment (EVA), a reinforcement-learning post-training framework for aligning video world models. EVA trains an inverse dynamics model on real robot trajectories and repurposes it as a reward model that evaluates generated videos through the action sequences they induce, encouraging smooth motions measured by velocity, acceleration, and jerk while penalizing actions that violate embodiment constraints. Importantly, the reward remains informative even when generated videos contain severe visual artifacts, since such artifacts typically translate into unstable or out-of-bound actions. Experiments on the RoboTwin benchmark and a real bimanual robot show that EVA reduces embodiment-specific artifacts in generated rollouts and improves downstream task execution success.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **EVA (Executable Video Alignment)** 的框架，旨在解决机器人视频世界模型中生成的视觉推演与实际物理可执行性之间的脱节问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题：** 论文识别并定义了**“可执行性差距”（Executability Gap）**。目前的机器人视觉规划通常采用“解耦架构”：先用视频生成模型规划未来画面，再用逆动力学模型（IDM）从画面中提取动作。然而，视频模型往往只追求视觉连贯，生成的画面可能包含手臂变形、关节突变或违反物理常识的运动，导致 IDM 解码出抖动、越界或不可行的控制指令。
*   **研究背景：** 尽管基础视频模型（如 Sora, Wan2.1）在视觉上很强大，但它们缺乏对机器人具身约束（如运动学限制、刚体一致性）的显式理解。

### 2. 方法论
*   **核心思想：** 将 IDM 从一个被动的“动作提取器”转变为一个主动的“奖励模型”。通过强化学习（RL）后训练，利用 IDM 反馈的动作质量来优化视频生成模型。
*   **关键技术细节：**
    *   **逆动力学模型 (IDM)：** 基于卷积骨干网络和**空间 Softmax（Spatial Softmax）**层，从视频帧中提取空间特征并预测关节动作。
    *   **可执行性奖励 (IDM-based Reward)：** 
        *   **平滑度惩罚：** 使用 Huber 损失函数惩罚动作序列中的加速度（Acceleration）和加加速度（Jerk），鼓励平滑运动。
        *   **具身约束惩罚：** 惩罚超出机器人物理极限的速度和加速度。
    *   **算法流程：** 采用 **GRPO (Group Relative Policy Optimization)** 算法。该算法通过对同一提示词生成的一组视频进行相对奖励评估，无需训练额外的价值函数（Value Function），适合高维视频生成任务。
    *   **训练阶段：** 先在具身数据上进行监督微调（SFT），再利用 IDM 奖励进行 GRPO 对齐。

### 3. 实验设计
*   **数据集与场景：**
    *   **仿真环境：** 使用 **RoboTwin 2.0** 基准测试，包含 21 个双臂操作任务（如开笔记本、按订书机、转开关等）。
    *   **真实世界：** 使用 Agilex CobotMagic 双臂机器人平台，涵盖 5 个训练任务和 5 个完全未见过的 **OOD（分布外）任务**。
*   **对比方法：**
    *   **模仿学习/VLA 基准：** ACT, Diffusion Policy (DP), RDT, $\pi_0$。
    *   **视频规划基准：** Vidar, GE-Act。
*   **评估指标：** 任务成功率、人类评估（运动学合理性、交互合理性、指令遵循度）。

### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长：**
    *   **仿真环境：** GRPO 训练约 136 个优化步，耗时约 **6 天**。
    *   **真实环境：** GRPO 训练约 46 个优化步，耗时约 **3 天**。
*   **模型参数：** 视频生成骨干网络基于 Wan2.1-14B，使用 LoRA（Rank 32）进行微调。

### 5. 实验数量与充分性
*   **实验规模：** 仿真实验覆盖了 21 个任务，每个任务 20 次测试；真实世界实验进行了 200 次试验（10 个任务 $\times$ 20 次）。
*   **消融实验：** 验证了 IDM 架构中空间 Softmax 的必要性，并分析了奖励分数与视觉质量、执行成功率之间的正相关性。
*   **公平性：** 所有对比方法均在相同的数据集划分下进行微调或测试，实验设计较为客观。

### 6. 主要结论与发现
*   **弥合差距：** EVA 显著减少了生成视频中的手臂变形和运动突变，将运动学合理性提升了约 21%。
*   **性能提升：** 在 RoboTwin 仿真中，EVA 的平均成功率（52.6%）优于所有基准方法（如 $\pi_0$ 为 45.7%）。
*   **泛化能力：** 在真实世界的 OOD 任务中，EVA 表现出极强的泛化性（成功率 60%），远超传统的模仿学习方法（如 $\pi_0$ 为 11%）。
*   **奖励有效性：** 即使视频有视觉伪影，IDM 也能通过异常动作捕捉到这些错误并给出低分，证明了动作空间奖励的鲁棒性。

### 7. 优点（亮点）
*   **创新的对齐目标：** 首次提出将“可执行性”作为视频世界模型的对齐准则，而非仅仅追求视觉真实度。
*   **物理反馈闭环：** 通过 IDM 将物理约束引入生成过程，解决了解耦架构中“规划器不顾执行器”的顽疾。
*   **零样本潜力：** 展示了利用 EVA 生成高质量合成数据来缓解机器人数据匮乏问题的潜力。

### 8. 不足与局限
*   **计算成本：** 扩散模型生成视频的速度较慢，目前难以实现高频的闭环反应式控制。
*   **物理建模简化：** 奖励函数主要关注运动学（平滑度、边界），尚未显式建模复杂的接触力学（如摩擦力、扭矩）。
*   **奖励黑客行为（Reward Hacking）：** 论文提到在训练后期，模型可能会为了高分而产生一些奇怪的伪影（如手臂静止不动），需要精细的早停策略。

（完）
