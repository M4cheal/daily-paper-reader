---
title: "Fast-dVLA: Accelerating Discrete Diffusion VLA to Real-Time Performance"
title_zh: Fast-dVLA：将离散扩散 VLA 加速至实时性能
authors: "Wenxuan Song, Jiayi Chen, Shuai Chen, Jingbo Wang, Pengxiang Ding, Han Zhao, Yikai Qin, Xinhu Zheng, Donglin Wang, Yan Wang, Haoang Li"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25661v1"
tags: ["query:tta"]
score: 8.0
evidence: VLA 模型的自适应成本与性能
tldr: 针对预训练VLA模型在标准微调中性能提升有限且计算开销大的挑战，本文提出Fast-dVLA。该方法通过在参数空间解耦辅助任务目标，提取增强通用能力的“能力向量”并将其与预训练参数融合，构建出能力增强的元模型。结合轻量级正交正则化损失，该方法在降低计算开销的同时，实现了与复杂辅助微调相当的性能，有效加速了离散扩散VLA模型的实时应用。
selection_source: fresh_fetch
motivation: 传统的辅助任务微调虽能提升VLA模型性能，但其高昂的计算开销阻碍了模型的快速适配与实时部署。
method: 通过在小规模任务上对比不同训练策略提取能力向量，并将其合并至预训练模型中，辅以正交正则化损失进行高效微调。
result: 在多种机器人任务上的实验表明，该方法在保持低计算成本的同时，达到了与复杂辅助微调方法相当的性能水平。
conclusion: 本研究证明了通过参数空间解耦与融合可以兼顾微调效率与模型性能，为加速VLA模型在机器人领域的应用提供了有效方案。
---

## 摘要
本文提出了一种新颖的方法，旨在解决预训练视觉-语言-动作（VLA）模型在标准监督微调（SFT）过程中往往无法有效提升性能并降低适配成本的挑战。一些带有辅助训练目标的高级微调方法可以提高性能并减少收敛步数，但由于辅助任务带来的额外损失函数计算，它们通常会产生显著的计算开销。为了在保持标准 SFT 简洁性的同时实现辅助训练的增强能力，我们在参数空间内解耦了辅助任务训练的两个目标，即增强通用能力和拟合特定任务的动作分布。为实现这一目标，我们只需使用两种不同的训练策略，使模型在小规模任务集上收敛。由此产生的模型参数之间的差异可以被解释为辅助任务提供的能力向量。随后，这些向量与预训练参数合并，形成一个能力增强的元模型。此外，当标准 SFT 辅以轻量级的正交正则化损失时，合并后的模型在计算开销降低的情况下，达到了与辅助微调基准相当的性能。实验结果表明，该方法在多种机器人任务中均非常有效。项目主页：https://chris1220313648.github.io/Fast-dVLA/

## Abstract
This paper proposes a novel approach to address the challenge that pretrained VLA models often fail to effectively improve performance and reduce adaptation costs during standard supervised finetuning (SFT). Some advanced finetuning methods with auxiliary training objectives can improve performance and reduce the number of convergence steps. However, they typically incur significant computational overhead due to the additional losses from auxiliary tasks. To simultaneously achieve the enhanced capabilities of auxiliary training with the simplicity of standard SFT, we decouple the two objectives of auxiliary task training within the parameter space, namely, enhancing general capabilities and fitting task-specific action distributions. To deliver this goal, we only need to train the model to converge on a small-scale task set using two distinct training strategies. The difference between the resulting model parameters can then be interpreted as capability vectors provided by auxiliary tasks. These vectors are then merged with pretrained parameters to form a capability-enhanced meta model. Moreover, when standard SFT is augmented with a lightweight orthogonal regularization loss, the merged model attains performance comparable to auxiliary finetuned baselines with reduced computational overhead. Experimental results demonstrate that this approach is highly effective across diverse robot tasks. Project page: https://chris1220313648.github.io/Fast-dVLA/

---

## 论文详细总结（自动生成）

以下是对论文《Fast-dVLA: Accelerating Discrete Diffusion VLA to Real-Time Performance》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：离散扩散视觉-语言-动作模型（dVLA）在多模态对齐和保留预训练知识方面表现优异，但其**推理速度极慢**，远低于机器人实时控制所需的 30Hz。
*   **核心瓶颈**：现有的 dVLA 采用全双向注意力机制（Bidirectional Attention），导致每一轮去噪迭代时 Key-Value (KV) 状态都会改变，无法像自回归（AR）模型那样利用 **KV Cache** 缓存技术。
*   **关键观察**：作者发现即使在双向注意力下，dVLA 仍表现出一种隐式的“从左到右”块状解码倾向。这启发了将自回归的顺序性与扩散模型的并行性结合的可能性。

### 2. 方法论
Fast-dVLA 的核心思想是通过**块状扩散（Block-wise Diffusion）**实现 KV Cache 复用与并行解码的平衡。
*   **块状注意力机制（Block-wise Attention）**：将动作序列划分为多个块（Block），块内采用双向注意力，块间采用因果（Causal）注意力。一旦前一个块解码完成，其 KV 状态即固定，后续块可直接复用缓存。
*   **扩散强迫（Diffusion Forcing）**：在训练中为不同块分配单调递增的噪声水平。前序块噪声低（更趋向于干净动作），后续块噪声高，允许模型在精炼前期动作的同时并行预测后期动作。
*   **非对称蒸馏（Asymmetric Distillation）**：为了降低训练成本，不从头训练，而是将预训练的双向 dVLA 作为教师模型，通过 KL 散度引导具有块状注意力的学生模型进行学习。
*   **流水线并行解码（Pipelined Parallel Decoding）**：设计了一种动态流水线，根据置信度阈值（$\tau_{add}, \tau_{act}$）决定何时引入新块。块分为“半激活”和“全激活”状态，实现了跨块的并行去噪。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：CALVIN（长程任务）、LIBERO（包含 Spatial, Object, Goal, Long 四个子集）、SimplerEnv（WidowX 机器人仿真）。
    *   **真实世界**：使用 AgileX 双臂机器人，执行传送带抓取（Conveyor Picking）、蔬菜分类（Vegetables Stowing）和蔬菜检索（Vegetables Retrieving）。
*   **对比基准（Baselines）**：
    *   **自回归 VLA**：OpenVLA, $\pi_0$-FAST。
    *   **流匹配（Flow-matching）VLA**：$\pi_0, \pi_0.5, GR00T-N1$。
    *   **离散扩散 VLA**：DD-VLA, Dream-VLA, UD-VLA（及其加速变体如 Block Diffusion）。

### 4. 资源与算力
*   **训练细节**：采用 LoRA（Rank=32）进行高效微调。
*   **训练时长**：非对称蒸馏非常高效，仅需 3k-4k 步即可收敛，约为标准全量微调预算的 1/8 到 1/10。
*   **算力设备**：文中未明确提及具体的 GPU 型号和数量（通常此类研究使用 A100 或 H100 集群），但强调了其方法在训练和推理上的低开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础动作预测到长程逻辑推理的多种任务。在 LIBERO 上进行了数百次 rollout，在真实世界中每个任务进行了 40 次测试。
*   **消融实验**：针对块大小（Block Size）、置信度阈值（$\tau_{conf}$）、激活阈值（$\tau_{add}, \tau_{act}$）以及不同的训练策略（从头训练 vs 蒸馏）进行了详细对比。
*   **客观性**：实验对比了当前最先进的各类架构，不仅关注成功率，还重点量化了推理速度（Tokens/s）和执行频率（Hz），评价维度全面。

### 6. 主要结论与发现
*   **速度突破**：Fast-dVLA 实现了 **2.8x 至 4.1x** 的推理加速，成功将离散扩散 VLA 提升至 **30Hz** 的实时控制水平。
*   **性能保持**：在大幅加速的同时，成功率几乎没有下降，甚至在某些长程任务（如 LIBERO-Long）中因块状注意力的时序约束而略有提升。
*   **训练高效**：通过非对称蒸馏，仅需极少的计算资源即可将现有的慢速 dVLA 转换为快速版本。

### 7. 优点与亮点
*   **架构创新**：巧妙地利用了 dVLA 隐式的自回归特性，通过块状注意力解决了扩散模型无法使用 KV Cache 的顽疾。
*   **实时性**：这是首个将离散扩散 VLA 推向真实机器人实时闭环控制的工作。
*   **通用性**：该方法具有即插即用的特性，可应用于 Dream-VLA、DD-VLA 和 UD-VLA 等多种不同的离散扩散架构。

### 8. 不足与局限
*   **依赖教师模型**：非对称蒸馏的效果高度依赖于预训练的双向 dVLA 教师模型的质量。
*   **超参数敏感性**：虽然消融实验证明了鲁棒性，但在不同硬件或任务下，流水线解码的阈值（$\tau_{add}, \tau_{act}$）可能仍需精细调优以平衡速度与精度。
*   **应用限制**：目前主要针对离散动作空间，对于需要极高连续控制精度的任务，离散化带来的量化误差仍是扩散类模型共有的潜在挑战。

（完）
