---
title: "Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning"
title_zh: 持久性机器人世界模型：通过强化学习稳定多步外推
authors: "Jai Bardhan, Patrik Drozdik, Josef Sivic, Vladimir Petrik"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25685v1"
tags: ["query:tta"]
score: 8.0
evidence: 通过强化学习后训练稳定机器人世界模型
tldr: 针对机器人世界模型在长程自回归预测中存在的误差累积和视觉质量退化问题，本文提出了一种基于强化学习（RL）的后训练方案。该方法通过在模型自身的预测序列上进行训练，并结合对比RL目标函数、多视图视觉保真度奖励以及多候选序列对比协议，显著提升了多步预测的稳定性。在DROID数据集上的实验表明，该方法在各项指标上均达到SOTA，并获得了极高的人类偏好率。
selection_source: fresh_fetch
motivation: 传统的机器人世界模型在长程自回归预测时会因误差累积导致视觉质量迅速崩溃。
method: 提出一种RL后训练方案，利用对比学习目标和多视图视觉保真度奖励，让模型在自身的预测序列上进行优化。
result: "在DROID数据集上刷新了SOTA，LPIPS降低了14%，SSIM提升了9.1%，且在人类盲测中获得80%的偏好率。"
conclusion: 通过RL微调自回归预测过程，可以有效解决世界模型的长期预测不稳定性，为复杂机器人任务模拟提供了更可靠的工具。
---

## 摘要
动作条件机器人世界模型根据机器人动作序列生成操作场景的未来视频帧，为模拟传统物理引擎难以建模的任务提供了一种极具前景的替代方案。然而，这些模型通常针对短期预测进行优化，在自回归部署时会失效：每个预测的视频片段都会作为下一个片段的上下文反馈，导致误差累积，视觉质量迅速下降。我们通过以下贡献解决了这一问题。首先，我们引入了一种强化学习 (RL) 后训练方案，该方案在世界模型自身的自回归外推（rollouts）上进行训练，而非基于真实历史数据。我们通过将近期用于扩散模型的对比强化学习目标适配到我们的设置中来实现这一点，并证明其收敛性保证可以完全延续。其次，我们设计了一种训练协议，从同一个外推状态生成并比较多个候选的可变长度未来，从而强化高保真度预测并抑制低保真度预测。第三，我们开发了高效的多视图视觉保真度奖励，结合了跨摄像机视图的互补感知指标，并在片段级别进行聚合，以获得密集且低方差的训练信号。第四，我们展示了我们的方法在 DROID 数据集的外推保真度上达到了新的先进水平（SOTA），在所有指标上均优于最强的基准模型（例如，外部摄像机的 LPIPS 降低了 14%，腕部摄像机的 SSIM 提高了 9.1%），在成对比较中胜率为 98%，并在盲测人类研究中获得了 80% 的偏好率。

## Abstract
Action-conditioned robot world models generate future video frames of the manipulated scene given a robot action sequence, offering a promising alternative for simulating tasks that are difficult to model with traditional physics engines. However, these models are optimized for short-term prediction and break down when deployed autoregressively: each predicted clip feeds back as context for the next, causing errors to compound and visual quality to rapidly degrade. We address this through the following contributions. First, we introduce a reinforcement learning (RL) post-training scheme that trains the world model on its own autoregressive rollouts rather than on ground-truth histories. We achieve this by adapting a recent contrastive RL objective for diffusion models to our setting and show that its convergence guarantees carry over exactly. Second, we design a training protocol that generates and compares multiple candidate variable-length futures from the same rollout state, reinforcing higher-fidelity predictions over lower-fidelity ones. Third, we develop efficient, multi-view visual fidelity rewards that combine complementary perceptual metrics across camera views and are aggregated at the clip level for dense, low-variance training signal. Fourth, we show that our approach establishes a new state-of-the-art for rollout fidelity on the DROID dataset, outperforming the strongest baseline on all metrics (e.g., LPIPS reduced by 14% on external cameras, SSIM improved by 9.1% on the wrist camera), winning 98% of paired comparisons, and achieving an 80% preference rate in a blind human study.

---

## 论文详细总结（自动生成）

以下是对论文《Persistent Robot World Models: Stabilizing Multi-Step Rollouts via Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：动作条件视频扩散世界模型（World Models, WM）在进行长程预测时存在**曝光偏差（Exposure Bias）**。
*   **背景与动机**：虽然现有的模型在生成短期视频片段上表现出色，但在自回归（Autoregressive）部署时，模型必须以自己生成的、带有微小误差的帧作为后续生成的上下文。这些误差会迅速累积，导致生成的物体在几秒钟内解体（如碗变成模糊的团块）、机器人末端执行器偏离轨迹以及场景一致性崩溃。
*   **研究目标**：通过强化学习（RL）后训练（Post-training），使模型在自身生成的“不完美”历史数据上进行学习，从而稳定长程外推（Rollouts）的质量。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将世界模型的优化从“单步教师迫导（Teacher-forcing）”转向“闭环自回归优化”。利用模型生成的多个候选序列进行对比，奖励高质量生成，惩罚低质量生成。
*   **关键技术细节**：
    *   **$x_0$ 预测参数化的对比 RL 目标**：作者适配了 DiffusionNFT 方法，将其从原始的“速度预测”模式改进为适用于机器人 WM 常用的 **$x_0$ 预测（直接预测清晰图像）** 模式。通过构建正向分支（向当前模型偏移）和负向分支（远离当前模型偏移），并根据奖励权重 $r$ 进行插值损失计算。
    *   **共享前缀分支协议（Shared-Prefix Branching）**：为了在没有固定 Prompt 的视频流中进行对比，模型先从真实初始状态自回归生成一段变长前缀（0-9 步），然后从该状态并行分支生成 $K=16$ 个候选片段。
    *   **多视图视觉奖励**：设计了一个综合奖励函数，结合了**LPIPS（感知相似度）**、**SSIM（结构相似度）**和**PSNR（峰值信噪比）**。奖励跨越三个摄像头视图（两个外部视图，一个腕部视图）进行平均，并进行组内归一化（Z-score），以消除不同外推深度导致的数值差异。
    *   **训练流程**：仅微调 LoRA 适配器和动作编码器，冻结主干网络，确保训练的稳定性和效率。

### 3. 实验设计
*   **数据集**：使用 **DROID** 数据集，这是一个大规模、多样化的机器人桌面操作数据集，包含三视角视频和机器人位姿数据。
*   **Benchmark**：14 步自回归外推（约 11 秒视频生成）。
*   **对比方法**：
    *   **WPE** (World Models as Environments)
    *   **IRASim** (Fine-grained World Model)
    *   **Ctrl-World** (本文的强力基准模型)
*   **评估指标**：除了标准的视觉指标（SSIM, PSNR, LPIPS），还引入了基于 **RoboEngine** 的物体中心和机器人中心掩码评估，以及人类盲测偏好研究。

### 4. 资源与算力
*   **硬件配置**：使用了 **8 张 NVIDIA H200 GPU**。
*   **训练时长**：训练持续了 **3 天**。
*   **训练参数**：训练了 8,000 步，Batch Size 为 64，Group Size $K=16$。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量评估（在 DROID 验证集上进行 1-to-1 成对比较）和定性分析。
*   **消融实验**：非常充分，涵盖了学习率、奖励函数组合（单指标 vs 综合指标）、前缀采样策略（固定 vs 随机 vs 课程学习）、Group Size 大小、预测步长（H=1 vs H=3）以及正则化项（KL 散度）的影响。
*   **客观性**：引入了人类盲测（8 位专家评审）和策略评估相关性实验（WM-to-Real Correlation），证明了模型改进不仅在于视觉好看，更在于对下游任务评估的准确性。

### 6. 主要结论与发现
*   **性能飞跃**：在 DROID 数据集上刷新了 SOTA。外部相机 LPIPS 降低 14%，腕部相机 SSIM 提升 9.1%。
*   **长程稳定性**：在 11 秒的长程外推中，新模型能保持物体的结构完整性，而基准模型在 3-5 秒内就会出现物体溶解。
*   **人类偏好**：在人类盲测中，本方法生成的视频获得了 **80% 的偏好率**。
*   **任务相关性**：掩码评估显示，改进主要集中在机器人和操作物体上，而非简单的背景重建。

### 7. 优点
*   **理论严谨**：对扩散模型 RL 目标的推导进行了 $x_0$ 适配，具有完备的数学证明。
*   **解决痛点**：直接针对世界模型最核心的“曝光偏差”和“长程崩溃”问题，具有极强的实用价值。
*   **评估全面**：不仅看像素指标，还看物体动力学一致性和对机器人策略评估的辅助作用。

### 8. 不足与局限
*   **计算开销**：后训练阶段需要为每个更新步骤采样 16 个候选序列，计算成本显著高于标准的监督微调。
*   **物理约束缺失**：目前的奖励函数主要基于视觉保真度，尚未显式引入物理定律（如重力、碰撞检测）或几何约束。
*   **应用范围**：目前主要在桌面操作场景（DROID）验证，对于更广阔的户外或高动态场景的泛化能力尚待观察。

（完）
