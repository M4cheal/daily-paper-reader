---
title: "CD-Buffer: Complementary Dual-Buffer Framework for Test-Time Adaptation in Adverse Weather Object Detection"
title_zh: CD-Buffer：恶劣天气目标检测中测试时自适应的互补双缓冲框架
authors: "Youngjun Song, Hyeongyu Kim, Dosik Hwang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26092v1"
tags: ["query:tta"]
score: 10.0
evidence: 用于目标检测中领域偏移实时自适应的测试时间自适应 (TTA)
tldr: 针对恶劣天气下目标检测的测试时自适应（TTA）问题，本文提出CD-Buffer框架。该框架发现“加法”特征细化和“减法”特征剔除在不同偏移程度下具有互补性。通过统一的差异度量指标，CD-Buffer实现了两种策略的自动平衡，能够根据特征层级的偏移严重程度动态调整，在多种天气条件下均达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有的TTA方法（加法或减法）仅在特定偏移范围内有效，无法在不同严重程度的恶劣天气下保持泛化性。
method: 提出一种互补双缓冲框架，利用统一的差异度量指标自动平衡特征剔除（减法）与特征细化（加法）策略。
result: 在KITTI、Cityscapes和ACDC等数据集上的实验表明，该方法在各种天气条件和偏移程度下均取得了领先的检测性能。
conclusion: CD-Buffer通过耦合加减法策略，实现了对异构偏移幅度的自适应处理，显著提升了目标检测在复杂环境下的鲁棒性。
---

## 摘要
测试时自适应（TTA）能够在无需离线重新训练的情况下，实现对领域偏移（domain shifts）的实时自适应。近期的 TTA 方法主要探索了通过引入轻量级模块进行特征细化的加法（additive）方法。最近，一种移除领域敏感通道的减法（subtractive）方法作为一种替代方向出现。我们观察到，这些范式表现出互补的有效性模式：减法方法通过移除受损特征在剧烈偏移下表现出色，而加法方法在需要细化的中度偏移下更为有效。然而，每种范式仅在有限的偏移严重程度范围内有效，无法泛化到各种不同的损坏水平。这引出了以下问题：我们能否根据测量的特征级领域偏移，自适应地平衡这两种策略？我们提出了 CD-Buffer，这是一种新颖的互补双缓冲框架，其中减法和加法机制在统一差异度量（discrepancy metric）的驱动下，以相反但协调的方向运行。我们的核心创新在于差异驱动的耦合：该框架通过统一的差异度量将移除和细化耦合在一起，根据特征级偏移的严重程度自动平衡这两种策略。这建立了自动的通道级平衡，无需手动调节即可针对异构偏移幅度进行差异化处理。在 KITTI、Cityscapes 和 ACDC 数据集上的广泛实验证明了其最先进的性能，在各种天气条件和严重程度水平下始终取得优异结果。

## Abstract
Test-Time Adaptation (TTA) enables real-time adaptation to domain shifts without off-line retraining. Recent TTA methods have predominantly explored additive approaches that introduce lightweight modules for feature refinement. Recently, a subtractive approach that removes domain-sensitive channels has emerged as an alternative direction. We observe that these paradigms exhibit complementary effectiveness patterns: subtractive methods excel under severe shifts by removing corrupted features, while additive methods are effective under moderate shifts requiring refinement. However, each paradigm operates effectively only within limited shift severity ranges, failing to generalize across diverse corruption levels. This leads to the following question: can we adaptively balance both strategies based on measured feature-level domain shift? We propose CD-Buffer, a novel complementary dual-buffer framework where subtractive and additive mechanisms operate in opposite yet coordinated directions driven by a unified discrepancy metric. Our key innovation lies in the discrepancy-driven coupling: Our framework couples removal and refinement through a unified discrepancy metric, automatically balancing both strategies based on feature-level shift severity. This establishes automatic channel-wise balancing that adapts differentiated treatment to heterogeneous shift magnitudes without manual tuning. Extensive experiments on KITTI, Cityscapes, and ACDC datasets demonstrate state-of-the-art performance, consistently achieving superior results across diverse weather conditions and severity levels.

---

## 论文详细总结（自动生成）

以下是对论文《CD-Buffer: Complementary Dual-Buffer Framework for Test-Time Adaptation in Adverse Weather Object Detection》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：目标检测模型在理想天气下表现优异，但在雨、雪、雾等恶劣天气下，由于领域偏移（Domain Shift），性能会大幅下降。
*   **核心问题**：现有的测试时自适应（TTA）方法主要分为两类：**加法（Additive）策略**（引入轻量级模块进行特征细化）和**减法（Subtractive）策略**（移除对领域敏感的受损通道）。
*   **研究动机**：作者发现这两类方法具有互补性——减法策略在剧烈偏移（严重受损特征）下效果好，而加法策略在中度偏移（需精细调整）下更优。目前尚无方法能根据偏移严重程度自动平衡这两种策略，导致在多变天气下的泛化性不足。

### 2. 论文提出的方法论
CD-Buffer 框架的核心思想是利用**统一的差异度量指标**来驱动“加减法”机制的协同工作。

*   **特征级领域差异评分（$D$）**：
    *   在测试时，计算当前目标图像（Image-level）和实例（Instance-level，通过 RoI Alignment 提取）特征与预先存储的源域统计数据之间的 $L1$ 距离。
    *   公式：$D = D_I + D_O$，其中 $D_I$ 为图像级差异，$D_O$ 为目标级差异。
*   **减法缓冲模块（Subtractive Buffer）**：
    *   引入可学习的掩码分数 $s$。通过**差异加权正则化**（Discrepancy-Weighted Regularization），对差异值 $D$ 较大的通道施加更强的惩罚，促使其掩码趋向于 0，从而抑制受损严重的特征通道。
*   **加法缓冲模块（Additive Buffer）**：
    *   由轻量级的 $1 \times 1$ 和 $3 \times 3$ 卷积组成残差路径。
    *   **关键技术：逆软掩码（Inverse Soft Masking）**。加法模块的补偿强度与减法模块的抑制程度成反比。即：被减法模块抑制越严重的通道，加法模块提供的补偿越强；而稳定的通道则保持较小的干预。
*   **优化策略**：
    *   结合特征对齐损失（$L_{align}$）和掩码正则化损失（$L_{mask}$）。
    *   采用层级梯度缩放，根据各层的平均差异度动态调整更新步长。

### 3. 实验设计
*   **数据集与场景**：
    *   **源域**：KITTI 和 Cityscapes（晴天数据）。
    *   **目标域（测试集）**：KITTI-Fog（3种能见度）、KITTI-Rain（3种降雨量）、Cityscapes-Foggy（2种严重程度）以及真实的 ACDC 数据集（雾、雪、雨、夜）。
*   **Benchmark 与对比方法**：
    *   基准指标：mAP@50。
    *   对比方法：包括 BufferTTA（加法代表）、PruningTTA（减法代表）、ActMAD（激活匹配）、WHW（加权适配器）等。
*   **实验设置**：涵盖了离散 TTA（针对特定严重程度）和连续 TTA（模拟天气逐渐变化的动态过程）。

### 4. 资源与算力
*   **硬件设备**：所有实验（包括源模型训练和 TTA）均在单张 **NVIDIA RTX 6000 Ada Generation GPU** 上完成。
*   **训练时长**：源模型在 KITTI 和 Cityscapes 上训练了 250 个 epoch。TTA 过程为在线实时更新，Batch Size 统一设为 16。
*   **推理速度**：完整版 CD-Buffer 约为 15.59 FPS，轻量版（CD-Buffer Light）可达 **35.86 FPS**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个主流数据集、10 余种不同的天气/严重程度组合上进行了详尽测试。
    *   进行了 10 轮循环的连续 TTA 实验，验证了长期运行的稳定性。
    *   **消融实验**：验证了 $L_{mask}$ 损失、加法/减法模块各自的贡献、层级梯度缩放的效果，以及超参数（正则化权重、抑制比例）的敏感性。
*   **客观性与公平性**：所有结果均为三次独立运行的平均值，且在对比实验中统一了 Batch Size 和学习率优化逻辑，实验设计较为严谨、客观。

### 6. 论文的主要结论与发现
*   **互补性验证**：实验证实了加法和减法策略在不同偏移强度下的性能交叉现象。
*   **性能领先**：CD-Buffer 在几乎所有恶劣天气场景下均达到了 SOTA 性能，尤其在严重雾天（KITTI Fog 50m）下，比现有最佳方法提升了约 5% 的 mAP。
*   **稳定性**：在连续 TTA 场景中，CD-Buffer 能够有效抑制误差累积，避免了类似 ActMAD 等方法在长期自适应中出现的性能崩溃。

### 7. 优点（亮点）
*   **自适应平衡**：首次提出通过统一指标动态耦合加减法策略，解决了单一 TTA 范式无法应对异构偏移的问题。
*   **通道级粒度**：相比于层级调整，通道级的处理能更精细地保留鲁棒特征并修复受损特征。
*   **无需手动调优**：通过差异驱动机制实现了自动化的通道选择和补偿强度分配。
*   **灵活性**：提供了 Full 和 Light 两个版本，兼顾了高精度和实时性需求。

### 8. 不足与局限
*   **计算开销**：Full 版本由于在每个残差块都插入了双缓冲模块，FPS 较低（15.59），在极高性能要求的嵌入式设备上可能受限。
*   **依赖源域统计**：需要预先计算并存储源域的特征统计数据，虽然开销不大，但增加了部署的预处理步骤。
*   **极端场景限制**：尽管在 ACDC 夜间场景有所提升，但绝对 mAP 仍然较低（8.92），说明对于光照极度匮乏等极端非线性偏移，仅靠特征层级的加减法调整仍有局限。

（完）
