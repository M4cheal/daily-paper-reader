---
title: Learning Clinical Representations Under Systematic Distribution Shift
title_zh: 系统性分布偏移下的临床表示学习
authors: "Yuanyun Zhang, Shi Li"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07348v1"
tags: ["query:tta"]
score: 8.0
evidence: 多模态临床预测的实践不变表示学习
tldr: 针对临床机器学习模型在不同医疗机构部署时面临的系统性分布偏移问题（如测量政策和工作流差异），本文提出了一种实践不变的表示学习框架。该方法通过结合监督学习、对抗性环境正则化和跨医院的不变风险惩罚，将生理信号与环境伪影解耦。实验表明，该方法在多项电子健康记录预测任务中显著提升了分布外泛化能力和模型校准度。
selection_source: fresh_fetch
motivation: 临床数据中生理信号与特定机构的医疗实践伪影相互纠缠，导致模型在跨机构部署时性能下降。
method: 提出一种联合优化框架，结合监督风险最小化、对抗性环境正则化以及跨医院的不变风险惩罚，以学习对环境不敏感的生理特征表示。
result: 在多项跨机构预测任务中，该方法比掩码预训练和标准监督基线提升了高达2至3个百分点的分布外AUROC，并改善了模型校准。
conclusion: 在医疗AI中，除了扩大模型规模，显式考虑系统性分布偏移并构建结构不变性对于提高模型的鲁棒性和可迁移性至关重要。
---

## 摘要
临床机器学习模型越来越多地采用大规模、多模态基础范式进行训练，然而部署环境往往与训练时的数据生成设置存在系统性差异。这种偏移源于异构的测量策略、记录习惯和机构工作流程，导致生理信号与特定实践伪影之间的表示纠缠。在这项工作中，我们提出了一种用于多模态临床预测的实践不变表示学习框架。我们将临床观察建模为源自潜在生理因素和环境依赖过程，并引入了一个目标函数，在优化预测性能的同时，抑制学习到的嵌入中的环境预测信息。具体而言，我们将监督风险最小化与对抗性环境正则化以及跨医院的不变风险惩罚相结合。在多项纵向电子健康档案（EHR）预测任务和跨机构评估中，与掩码预训练和标准监督基线相比，我们的方法将分布外（OOD）AUROC 提升了多达 2 到 3 个百分点，同时保持了分布内性能并改善了校准度。这些结果表明，在表示学习过程中明确考虑系统性分布偏移可以产生更稳健、更具迁移性的临床模型，强调了在医疗人工智能中，结构不变性与架构规模同等重要。

## Abstract
Clinical machine learning models are increasingly trained using large scale, multimodal foundation paradigms, yet deployment environments often differ systematically from the data generating settings used during training. Such shifts arise from heterogeneous measurement policies, documentation practices, and institutional workflows, leading to representation entanglement between physiologic signal and practice specific artifacts. In this work, we propose a practice invariant representation learning framework for multimodal clinical prediction. We model clinical observations as arising from latent physiologic factors and environment dependent processes, and introduce an objective that jointly optimizes predictive performance while suppressing environment predictive information in the learned embedding. Concretely, we combine supervised risk minimization with adversarial environment regularization and invariant risk penalties across hospitals. Across multiple longitudinal EHR prediction tasks and cross institution evaluations, our method improves out of distribution AUROC by up to 2 to 3 points relative to masked pretraining and standard supervised baselines, while maintaining in distribution performance and improving calibration. These results demonstrate that explicitly accounting for systematic distribution shift during representation learning yields more robust and transferable clinical models, highlighting the importance of structural invariance alongside architectural scale in healthcare AI.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Clinical Representations Under Systematic Distribution Shift》（系统性分布偏移下的临床表示学习）的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
临床机器学习模型在实际部署时，经常面临**系统性分布偏移**。这种偏移并非随机，而是源于不同医疗机构之间异构的测量政策（如检查频率）、记录习惯（如诊断编码偏好）和临床工作流。
*   **核心矛盾**：现有的临床基础模型（Foundation Models）多采用大规模预训练（如掩码自编码），这会导致模型将“生理信号”（Physiologic Signal）与“实践伪影”（Practice Artifacts）纠缠在一起。
*   **研究目标**：将临床数据分解为**实践相关**和**生理相关**两个部分，学习一种对环境（医院、时间段等）具有不变性、仅保留生理特征的鲁棒表示。

### 2. 论文提出的方法论
论文提出了一种**实践不变表示学习框架**，其核心思想是通过结构化约束实现生理与环境信息的解耦。
*   **潜在变量建模**：假设观测数据 $x$ 由潜在生理状态 $z$ 和环境上下文 $c$ 共同决定，而临床结果 $y$ 仅取决于 $z$。
*   **核心技术细节**：
    1.  **多模态编码器**：使用 Transformer 处理 EHR 事件序列，结合 CNN/ViT 处理影像，通过交叉注意力机制融合。
    2.  **对抗性环境正则化（Adversarial Regularization）**：引入一个环境分类器，通过对抗训练（Minimax 目标）迫使编码器丢弃能预测“医院身份”的信息，实现 $h(x) \perp e | y$。
    3.  **不变风险惩罚（Invariant Risk Penalty）**：借鉴 IRM（不变风险最小化）思想，惩罚不同环境下最优线性预测器之间的差异，确保模型在各医院间的预测逻辑一致。
*   **最终目标函数**：联合优化监督预测损失、对抗环境损失和不变风险惩罚项。

### 3. 实验设计
*   **数据集与场景**：使用来自 4 个不同医院系统的纵向 EHR 数据。
*   **预测任务**：院内死亡率、30天内再入院、48小时内急性恶化。
*   **评估方式**：采用“留一医院法”（Leave-one-hospital-out），即在 3 个医院上训练，在第 4 个完全未见的医院上测试分布外（OOD）泛化能力。
*   **对比基线（Benchmarks）**：
    1.  标准监督学习（无不变性约束）。
    2.  掩码预训练（Masked Pretraining, 如 CEHR-BERT）后微调。
    3.  对比预训练（Contrastive Pretraining）。

### 4. 资源与算力
*   **模型规模**：所有对比模型均保持一致的架构和参数量，约为 **40M（4000万）参数**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及训练总时长。这在学术论文中较为常见，但通常此类规模的模型在单张或少量 A100/V100 GPU 上即可完成训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 项核心临床任务，并在 4 个独立环境间进行了交叉验证。
*   **消融实验**：专门针对“对抗训练”和“不变风险惩罚”进行了拆解实验，证明了两者结合的互补性。
*   **环境泄露分析**：通过尝试从冻结的表示中重新预测医院身份，量化了环境信息的过滤程度。
*   **评价**：实验设计较为**充分且客观**。它不仅关注了常见的 AUROC/AUPRC 指标，还重点评估了临床部署中至关重要的**校准度（ECE）**，并直接对比了当前最流行的基础模型预训练范式。

### 6. 论文的主要结论与发现
*   **泛化性能提升**：在 OOD 评估中，该方法比掩码预训练和标准监督基线提升了 **2-3 个 AUROC 点**。
*   **校准度改善**：OOD 环境下的校准误差（ECE）降低了约 **29%**，意味着模型给出的概率预测更具临床参考价值。
*   **规模非万能**：实验发现，单纯增加预训练数据量或模型规模（如掩码预训练）并不能自动获得对系统性偏移的鲁棒性，显式的结构不变性约束至关重要。
*   **信息解耦成功**：环境预测准确率从 70%+ 降至 39.7%，证明成功抑制了实践相关的干扰信号。

### 7. 优点
*   **结构化视角**：不同于盲目追求“大模型”的思路，本文从临床数据生成的本质（生理 vs. 流程）出发，具有很强的医学解释性。
*   **鲁棒性与校准并重**：在提升预测准确性的同时，显著改善了模型在陌生环境下的校准度，这对医疗 AI 的安全部署意义重大。
*   **即插即用**：该框架可以作为现有临床基础模型的一个正则化模块，具有较好的通用性。

### 8. 不足与局限
*   **环境定义较粗**：目前将“医院”作为单一环境变量，但实际中同一医院内部不同科室、不同年份也存在实践偏移。
*   **任务覆盖面**：主要集中在二分类预测任务，尚未验证在生存分析、连续风险评估或生成式任务中的表现。
*   **因果建模深度**：虽然引入了潜在变量假设，但本质上仍是基于关联的对抗学习，缺乏更深层次的因果发现机制来保证不变性。

（完）
