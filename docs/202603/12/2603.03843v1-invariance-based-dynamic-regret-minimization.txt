Title: Invariance-Based Dynamic Regret Minimization

URL Source: https://arxiv.org/pdf/2603.03843v1

Published Time: Thu, 05 Mar 2026 01:43:58 GMT

Number of Pages: 32

Markdown Content:
# Invariance-based dynamic regret minimization 

Margherita Lazzaretto 1∗

, Jonas Peters 2 and Niklas Pfister 31University of Copenhagen, 2ETH Zürich, 3Lakera Zürich 

Abstract 

We consider stochastic non-stationary linear bandits where the linear parameter connecting contexts to the reward changes over time. Existing algorithms in this setting localize the policy by gradually discarding or down-weighting past data, effectively shrinking the time horizon over which learning can occur. However, in many settings historical data may still carry partial information about the reward model. We propose to leverage such data while adapting to changes, by assuming the reward model decomposes into stationary and non-stationary components. Based on this assumption, we introduce ISD-linUCB, an algorithm that uses past data to learn invariances in the reward model and subsequently exploits them to improve online performance. We show both theoretically and empirically that leveraging invariance reduces the problem dimensionality, yielding significant regret improvements in fast-changing environments when sufficient historical data is available. 

# 1 Introduction 

A stochastic contextual bandit models an online decision-making process in which an agent, over 

T ∈ N rounds, sequentially selects actions based on contextual information (see, e.g., Lattimore and Szepesvári, 2020). The agent’s objective is to learn a decision policy that selects actions in a way that maximizes a cumulative reward, by balancing exploration of new actions and exploitation of acquired knowledge. Bandit algorithms provide the simplest framework for decision problems under uncertainty, where the actions of the agent do not affect the environment, often serving as a starting point for more complex models such as reinforcement learning. The design of stochastic contextual bandit algorithms relies on assumptions that specify the class of possible reward distributions over which the agent learns. We consider the case of linear time-varying reward functions, where the expected reward is assumed to depend linearly on some p-dimensional context-action feature. Regret is the main performance measure of bandit algorithms. It corresponds to the difference between the cumulative reward of an optimal (oracle) sequence of actions and the action selected by the bandit algorithm. The analysis of linear bandit algorithms involves studying finite sample upper and lower bounds for the regret in terms of the time horizon T and the dimensionality 

p. The standard setup assumes that the environment in which the agent acts is stationary, and is widely studied in the literature (see, for example, Lattimore and Szepesvári, 2020, a detailed review of the bandit literature is provided in Appendix A.). Dani et al. (2008) show a lower bound for the regret of the stationary stochastic linear bandit problem of Ω( p√T ). Different algorithms have been proposed in the literature achieving an upper bound that match this lower bound up to logarithmic factors, e.g., based on strategies using upper confidence bound (UCB) (Auer, 2002;  

> ∗E-mail: mala@math.ku.dk

1 

> arXiv:2603.03843v1 [stat.ML] 4 Mar 2026 invariant reward prediction
> residual reward prediction
> +UCB
> ISD-linUCB
> context
> offline data
> online data
> (optional)
> reward
> action

Figure 1: ISD-linUCB exploits historical data to improve reward predictions used by the UCB policy. Dani et al., 2008; Abbasi-Yadkori et al., 2011). Recent works relax the stationarity assumption. In such settings, the evaluation of non-stationary bandit algorithms additionally takes into account a variation budget BT , measuring how much the environment changes through the T rounds. When the underlying reward function changes through time, learning and updating a reliable policy can be challenging since the algorithm needs to continually explore the context-action space to detect and adapt to changes. Cheung et al. (2019) deal with non-stationarity by using a sliding window regularized least squares estimator, Russac et al. (2019) weight, instead, past observations by a discounting factor; Zhao et al. (2020) propose to periodically restart a standard linear bandit algorithm, where the restarting interval depends on BT . All these algorithms achieve the same regret upper bound in terms of p, T and BT given by ˜O(p 78 T 34 B 14 

> T

). The approach of Zhao et al. (2020) implies in particular that the excess regret compared to the stationary case arises from assuming a fixed linear parameter, and incurring an additional loss that depends on its variation between restarts. With the overall goal of adapting more rapidly to environment changes, we investigate whether parts of the non-stationary reward function remain invariant through all rounds, so that the data collected further in the past can be exploited rather than being discounted or fully discarded. Invariant information allows, for example, to learn policies that select worst-case optimal actions (e.g., Saengkyongam et al., 2023). Since purely invariant policies can be sub-optimal at specific time points, we are interested in learning an invariant policy that can be updated online. To do so, we rely on the invariant subspace decomposition (ISD) framework proposed by Lazzaretto et al. (2025) for regression in linear non-stationary settings. ISD splits the learning of the time-varying parameter into two lower-dimensional components, one of which is time-invariant of dimension 

pinv < p : this allows us to use all available data for the invariant component estimation and thus to reduce prediction error. A schematic representation of the proposed algorithm is shown in Figure 1. Our contributions are as follows. (i) We propose a practical novel linear contextual bandit algorithm (ISD-linUCB) that reduces online adaptation to a lower-dimensional residual subspace by exploiting the ISD framework to estimate the invariant component from historical data. (ii) We establish regret bounds scaling with the residual dimension (p − pinv ) rather than p,yielding significant improvements in rapidly changing environments when sufficient historical data is available. 21.1 Notation 

For all n ∈ N, we define [n] := {1, . . . , n } and [−n] := {− n, . . . , −1}. For all linear subspaces 

S ⊆ Rp, we denote by ΠS ∈ Rp×p the orthogonal projection matrix onto S and by S⊥ the orthogonal complement of S in Rp. For all x ∈ Rp and for all positive semi-definite matrices 

A ∈ Rp×p, we define the norm ∥x∥A := √x⊤Ax . We further denote by λmin (A) and λmax (A)

the minimum and maximum eigenvalues of A, respectively. For all p ∈ N, we denote by Ip the 

p-dimensional identity matrix. We write ˜O(·) for asymptotic bounds up to polylogarithmic factors, i.e., bn = ˜O(an) if there exist constants c > 0 and k ≥ 0 such that bn ≤ ca n log (n)k for sufficiently large n.

# 2 Problem setting 

In a linear stochastic contextual bandit, an agent sequentially observes context Xt ∈ X ⊆ Rp for rounds t ∈ [T ], where each Xt is drawn independently from previous contexts and has distribution 

PXt . Given Xt, the agent selects an action at ∈ A = [ K] and receives a noisy reward Rat 

> t

. The reward for all a ∈ A is assumed to satisfy 

Rat = φ(Xt, a )⊤γ0,t + ϵt (1) where φ : X ×A → Rp is a known context-action feature map, ϵt is a conditionally zero mean σ-sub-Gaussian noise variable with respect to the σ-algebra Ft = σ(( φ(Xτ , a τ ), R aτ 

> τ

)τ ∈[t−1] , φ (Xt, a t)) ,and γ0,t ∈ Rp is an unknown linear parameter. As in the standard linear bandit setup (e.g., Lattimore and Szepesvári, 2020), we assume that the context-action features and the linear parameter are bounded, and define L := max t∈N sup a∈A ∥φ(Xt, a )∥2 and M := max t∈N ∥γ0,t ∥2.Linear bandit algorithms sequentially estimate the linear parameter γ0,t and use it to select the optimal action given the observed context Xt. The quality of the algorithm can then be assessed by considering the dynamic regret defined by 

Reg T := E

" TX

> t=1

(Ra∗ 

> t
> t

− Rat 

> t

)

#

(2) where a∗ 

> t

:= arg max a∈A φ(Xt, a )⊤γ0,t is the unknown optimal action. We call the regret at time 

t, reg t := E(Ra∗ 

> t
> t

− Rat 

> t

), instantaneous regret .We consider a non-stationary setup in which both the parameter γ0,t and the context distri-bution PXt may vary across rounds. To be able to effectively estimate γ0,t in the non-stationary setting, we make two assumptions: (1) We assume γ0,t can be decomposed into a varying and an invariant part and (2) we assume that the varying part changes slowly so it can be seen as approximately constant within short time-intervals. To formalize (1), we introduce Assumption 1 below, which is based on the invariant subspace decomposition framework proposed by Lazzaretto et al. (2025) adapted to the linear bandit setup. 

Assumption 1 (Invariant subspace decomposition (ISD)) . There exists a non-degenerate invariant subspace decomposition of Rp for the time-varying linear bandit model (1) , that is, a partition of 

Rp into two linear subspaces (Sinv , Sres ) such that dim (Sinv ) := pinv < p and dim (Sres ) := pres =

p − pinv , satisfying i) Sinv = ( Sres )⊥;ii) ∀t ∈ N, ∀x ∈ X , ∀a ∈ A : Cov(Π Sinv 

φ(x, a ), ΠSres 

φ(x, a )) = 0 ;

3iii) ∀t ∈ N, ∃βinv ∈ S inv , ∃δres  

> t

∈ S res : γ0,t = βinv + δres  

> t

;iv) ∀ partitions ( ˜Sinv , ˜Sres ) of Rp satisfying (i)—(iii): dim( ˜Sinv ) ≤ dim( Sinv ).

The spaces Sinv and Sres are called invariant and residual subspace , and βinv and δres  

> t

are called invariant and residual component of γ0,t , respectively. Lazzaretto et al. (2025) show that both the invariant and the residual component can be expressed as the least squares solution to the regression problem in the corresponding subspace. Using Assumption 1, we can now formalize the bandit setting. We decompose the problem into an offline phase followed by a shorter online phase. 

Setting 1. We have access to T0 ∈ N offline observations (φ(Xt, a t), a t, R at 

> t

)t∈[−T0], collected by a bandit agent interacting with a changing environment satisfying model (1) . The online time horizon T ∈ N is such that, for all t ∈ [T ], the parameter γ0,t in model (1) is constant and Assumption 1 holds. 

The constant γ0,t assumption simplifies the analysis and is motivated by settings of other non-stationary bandit algorithms, where T may represent a single epoch in restarting algorithms (Zhao et al., 2020) or a sliding window (Russac et al., 2019). In practice, our proposed algorithm sequentially moves previously seen observations to the offline data. This framing also facilitates comparison with stationary linear bandit algorithms. While for stationary bandits the time horizon T tends to dominate the bounds, for non-stationary bandits the p term becomes more dominant because time horizons are short due to the shifting distributions. By leveraging the offline data, we are able to reduce the upper bound from ˜O(p√T ) to ˜O(pres √T ) where pres is the dimension of the residual subspace (in Appendix D we show that under Assumption 1 the lower bound for the regret of a linear bandit algorithm is 

Ω( pres √T )). We begin by recalling some concepts for stationary linear bandits relevant for our work in Section 2.1. We then introduce our method (Section 3) and analyze it in two steps: first assuming the decomposition (Sinv , Sres ) is known (Section 4.1.2), then extending the analysis to the case where it is estimated from data (Section 4.2). 

2.1 Regret analysis for stationary linear bandits 

In a linear contextual bandit algorithm, the exploration is normally based on the uncertainty in the linear parameter estimation. More specifically, let ˆγt denote an estimate of γ0,t obtained as the solution to ridge regression with regularization parameter λ > 0, using observations (Xτ , R aτ 

> τ

)t−1 

> τ=1

.The instantaneous regret is usually upper bounded with high probability by a term proportional to the product of the estimation error on the linear parameter, ∥ˆγt −γ0,t ∥ˆΣt−1 , and the context-action features norm, ∥φ(Xt, a t)∥ˆΣ−1

> t−1

, normalized by ˆΣt−1 and its inverse, respectively, where for all 

t ∈ [T ] we define the regularized sample covariance matrix 

ˆΣt := λI p +

> t

X 

> τ=1

φ(Xτ , a τ )φ(Xτ , a τ )⊤. (3) This is the case, for example, both for the upper confidence bound based algorithm by Abbasi-Yadkori et al. (2011) (LinUCB) and for the Thompson sampling based algorithm by Agrawal and Goyal (2013) (LinTS). Using the explicit expression for the linear parameter estimator and the triangle inequality, it holds that 

∥ˆγt − γ0,t ∥ˆΣt−1 ≤∥ 

> t−1

X 

> τ=1

φ(Xτ , a τ )⊤ϵτ ∥ˆΣ−1

> t−1

+ √λ∥γ0,t ∥2. (4) 4A result that plays a key role in bounding the linear parameter estimation error is given by the following lemma by Abbasi-Yadkori et al. (2011). 

Lemma 1 (Abbasi-Yadkori et al. (2011), Theorem 1) . Let {Ft}∞ 

> t=0

be a filtration. Let {ϵt}∞

> t=1

be a real-valued stochastic process such that ϵt is Ft-measurable and sub-Gaussian conditionally on Ft−1 with parameter σ > 0. Let {φ(Xt, a t)}∞ 

> t=1

be an Rp-valued stochastic process such that 

φ(Xt, a t) is Ft−1-measurable. Let λ ∈ R be a strictly positive constant. Then, for all η ∈ (0 , 1) , it holds with probability at least 1 − η that, for all t ∈ N,

∥

> t

X 

> τ=1

φ(Xτ , a τ )ϵτ ∥2ˆΣ−1

> t

≤ 2σ2 log 1

> η

r

> det( ˆΣt)det( λI p)

!

.

Moreover, if, for all t ∈ [T ], ∥φ(Xt, a t)∥2 ≤ L, then 

log 

 det( ˆΣt)det( λI p)



≤ p log 



1 + tL 2

> λp



.

Lemma 1 implies that ∥ˆγt − γ0,t ∥ˆΣt−1 is ˜O(√p), where the dependence on p is determined by the dimensionality of the matrix ˆΣt. The sum over the time horizon of the squared norm of the context-action features, PTt=1 ∥φ(Xt, a t)∥2ˆΣ−1

> t−1

, is also shown to be ˜O(p), depending again on the context-action features dimensionality. This term appears under square root when bounding the cumulative regret, leading to an additional ˜O(√p) term in the final bound. Our goal is to show that, in Setting 1, we can reduce the regret to depend on pres rather than p, up to some term that becomes negligible for large enough T0. In the following, we focus on the LinUCB analysis, but a similar reasoning could apply for other linear bandit algorithms based on least squares estimation. 

# 3 ISD-linUCB algorithm 

Assuming Setting 1, we propose an algorithm that is split into an offline phase for the estimation of (Sinv , Sres ) and βinv , followed by an online bandit phase adapting to the non-stationarity. The offline phase relies on the T0 historical observations from Setting 1. To estimate (Sinv , Sres ),Lazzaretto et al. (2025) estimate an orthonormal matrix U that jointly block diagonalizes the covariance matrices ( Var (φ(Xt, a t))) t∈[−T0]. U can be partitioned into two submatrices 

U inv ∈ Rp×pinv 

and U res ∈ Rp×pres 

whose columns form a basis for Sinv and Sres , respectively. Then, the orthogonal projection matrices onto Sinv and Sres are ΠSinv 

= U inv (U inv )⊤ and 

ΠSres 

= U res (U res )⊤. Under Assumption 1, for all t ∈ [T ] we can rewrite (1) as 

Rat 

> t

= φ(Xt, a t)⊤ΠSinv 

βinv + φ(Xt, a t)⊤ΠSres 

δres  

> t

+ ϵt, (5) where Cov (Π Sinv 

φ(Xt, a t), ΠSres 

φ(Xt, a t)) = 0 . The subspace decomposition therefore allows to estimate βinv and δres  

> t

separately. For any index set T ⊆ [−T0] ∪ [T ], we define the sample covariance matrix ˆΣT := X 

> τ∈T

φ(Xτ , a τ )φ(Xτ , a τ )⊤.

We make the following assumption to ensure that ˆΣ[−T0], is strictly positive definite. 

Assumption 2. The policy used to collect the T0 observations is such that ∃λ0 > 0 such that 

λmin ( 1

> T0

ˆΣ[−T0]) ≥ λ0 almost surely. 

5This means in particular that the policy used in the collection of the offline data has explored the context-action feature space sufficiently well in all directions. This is also required to be able to estimate the invariant subspace decomposition in the first place (see Lazzaretto et al., 2025). We denote an estimate of U by ˆU = [ ˆU inv , ˆU res ]. Under Assumption 2, we can estimate the invariant component βinv as the OLS solution in the invariant subspace, using the estimated ˆU inv 

and the offline observations, that is, 

ˆβinv := ˆU inv ( ˜Σinv [−T0])−1 X

> t∈[−T0]

( ˆU inv )⊤φ(Xt, a t)Rat 

> t

, (6) where ˜Σinv [−T0] := ( ˆU inv )⊤ ˆΣ[−T0] ˆU inv . We define a confidence set around ˆβinv by 

ˆCβ := {β ∈ ˆSinv | ∥ ˆβinv − β∥2ˆΣ[−T0]

≤ ˆρinv  

> T0

(η, L, M )}, (7) where ˆρinv  

> T0

(η, L, M ) is defined in (25) in Appendix C.3 such that the set contains ˆΠSinv 

βinv with probability at least 1 − η. As new observations become available, the estimate ˆβinv and the confidence set in in (7) may be recomputed online. At round t ∈ [T ] of the online ISD-linUCB algorithm, we estimate the residual component as 

ˆδres  

> t

:= ˆU res ( ˜Σres 

> t−1

)−1

> t−1

X 

> τ=1

( ˆU res )⊤φτ (Raτ 

> τ

− φ⊤ 

> τ

ˆβinv )

where φτ := φ(Xτ , a τ ) and for all t ∈ [T ], ˜Σres  

> t

:= ( ˆU res )⊤ ˆΣt ˆU res and ˆΣt is defined as in (3) . We then define a confidence set around ˆδres  

> t

by 

ˆCδt := {δ ∈ ˆSres | ∥ ˆδres  

> t

− δ∥2ˆΣt−1

≤ ˆρres  

> t

(η, L, M )},

where ˆρres  

> t

(η, L, M ) is defined in (30) in Appendix C.3 such that the set contains ˆΠSres 

δres  

> t

with probability at least 1 − η, and choose an action based on ˆCβ ⊕ ˆCδt . Assumption 1 ensures that there is no bias introduced due to omitting the context-action features in the residual subspace when estimating βinv (and vice versa for δres  

> t

) and therefore that ˆCβ and ˆCδt have the desired coverage. The full ISD-linUCB algorithm for a single online step, with and without updating the invariant component, is provided in Algorithm 1. Since Setting 1 assumes γ0,t is fixed in [T ], ISD-linUCB acts in Sres as a standard LinUCB algorithm. When this assumption fails, Algorithm 1 can be modified to act in Sres as existing non-stationary algorithms (e.g., using a sliding window), to improve their performance in terms of dimensionality. 

# 4 Regret analysis 

To motivate our approach, in Section 4.1 we first analyze the regret of a simplified algorithm having oracle knowledge of the subspace decomposition (Sinv , Sres ). We then discuss the complete regret analysis in Section 4.3. 

4.1 Motivation: oracle ISD-linUCB 

We study Algorithm 1 assuming the matrix U = [ U inv , U res ] is known. For simplicity, we keep the notation introduced in the previous section to denote the same quantities with known U . We 6Algorithm 1 ISD-linUCB (iteration at time t)

Input: (φ(Xτ , a τ ), a τ , R aτ 

> τ

)τ ∈[t−1] ∪[−T0], Xt

Parameters: A, λ, η, L, M , recompute 

Xt ← [φ(X1, a 1), . . . , φ (Xt−1, a t−1)] ⊤

Rt ← [Ra1 

> 1

, . . . , R at−1 

> t−1

]⊤

if recompute or t = 1 then 

T ← [−T0] ∪ [t − 1] 

X ← [φ(X−T0 , a −T0 ), . . . , φ (Xt−1, a t−1)] ⊤

compute ˆU res and ˆU inv using joint block diagonalization 

Xinv ← X ˆU inv 

ˆβinv ← ˆU inv  (Xinv )⊤Xinv −1(Xinv )⊤R

ˆCβ ← { β ∈ ˆSinv | ∥ ˆβinv − β∥2ˆΣT

≤ ˆρinv 

> |T |

(η, L, M )}

else 

load ˆβinv , ˆU res and ˆCβ from previous iteration 

Rres  

> t

← Rt − Xt ˆβinv 

Xres  

> t

← Xt ˆU res 

ˆδres  

> t

← ˆU res  λI pres + ( Xres  

> t

)⊤Xres 

> t

−1(Xres  

> t

)⊤Rres 

> t

ˆCδt ← { δ ∈ ˆSres | ∥ ˆδres  

> t

− δ∥2ˆΣt−1

≤ ˆρres  

> t

(η, L, M )}

at ← arg max a∈A max γ∈ ˆCβ ⊕ ˆCδt

φ(Xt, a )⊤γ

return at

define ( ¯βt, ¯δt) := arg max β∈ ˆCβ ,δ ∈ ˆCδ φ(Xt, a t)⊤(β +δ) the parameters at which the upper confidence bound for the chosen action at time t is achieved. Using the decomposition of the reward from (5) and the standard LinUCB analysis (see, for example, Lattimore and Szepesvári, 2020, Section 19.3), we obtain that at time t ∈ [T ], with high probability, the instantaneous regret is upper bounded by 

reg t ≤ φ(Xt, a t)⊤U inv (U inv )⊤( ¯βt − βinv )

| {z }

> reg inv
> t

+ φ(Xt, a t)⊤U res (U res )⊤(¯δt − δres  

> t

)

| {z }

> reg res
> t

. (8) Intuitively, for larger T0 the confidence set ˆCβ shrinks and ¯βt − βinv gets close to zero. Therefore, for T0 ≫ T , reg inv  

> t

becomes negligible with respect to reg res  

> t

. Removing the uncertainty on the invariant component implies that, in the regret analysis, the dependence on the dimension p of the bandit parameter is reduced to the dimension pres of the residual parameter. We first study the regret assuming to also have oracle knowledge of the invariant component βinv (Section 4.1.1). Then, we include in the regret analysis the estimation of the invariant component (Section 4.1.2). 

4.1.1 Regret analysis for oracle (Sinv , Sres ), β inv 

If we know βinv , then ˆCβ = {βinv } and reg inv  

> t

= 0 . Therefore, Algorithm 1 only performs the exploration in the residual subspace. As shown in Theorem 1, its regret only depends on the uncertainty on ˆδres  

> t

, which lies on a pres -dimensional space. 7Theorem 1. Consider Setting 1, assume (Sinv , Sres ) and βinv are known and consider the oracle version of Algorithm 1 that uses βinv , U inv and U res instead of ˆβinv , ˆU inv and ˆU res . Then, the regret of this oracle algorithm over a time horizon T with η = 1 /T is ˜O(pres √T ).

The proof of Theorem 1 uses that, under oracle knowledge, reg t = reg res  

> t

, where the dimension of (U res )⊤φ(Xt, a t) and of (U res )⊤( ¯δt − δres  

> t

) is pres . It then follows similar steps as the regret analysis for the standard LinUCB algorithm introduced in Section 2.1. A detailed proof is provided in Appendix C.1. 

4.1.2 Estimating the invariant component from offline data 

When also considering the uncertainty on the invariant component estimated using T0 observations, we can upper bound the regret of Algorithm 1 as follows. 

Theorem 2. Consider Setting 1, assume Assumption 2 holds and that (Sinv , Sres ) is known, and consider the oracle version of Algorithm 1 that uses U inv and U res instead of ˆU inv and 

ˆU res . Then, the regret of this oracle algorithm over a time horizon T with η = 1/T is 

˜O

√T



pres +

q pinv  

> λ0

+ 1

> λ0

q TT0

 

.

Theorem 2 implies that, whenever T0 is sufficiently larger than T , we have an advantage in using offline data to estimate invariant information in the regret function over only relying on online data. Indeed, the larger T0 is, the closer we get to the oracle bound shown in Section 4.1.1 of ˜O(pres √T ).

Proof sketch of Theorem 2. To bound reg inv  

> t

we choose ˆρinv  

> T0

(η, L, M ) such that ˆCβ contains βinv 

with probability at least 1 − η. By Assumption 2, we have that ˆΣ[−T0] ⪰ λ0T0Ip (with ⪰ denoting the Loewner order between two matrices) which implies that ˜Σinv [−T0] ⪰ λ0T0Ipinv . It follows that, for all β ∈ ˆCβ ,

∥ ˆβinv − β∥2ˆΣ[−T0]

=∥( ˜Σinv [−T0]) 12 (U inv )⊤( ˆβinv − β)∥22

≥∥ pλ0T0(U inv )⊤( ˆβinv − β)∥22

=λ0T0∥(U inv )⊤( ˆβinv − β)∥22. (9) Finally, this implies that 

∥(U inv )⊤( ˆβinv − β)∥22 ≤ 1

λ0T0

ˆρinv  

> T0

(η, L, M ).

Therefore, with probability at least 1 − η,

reg inv  

> t

≤ 2∥(U inv )⊤φ(Xt, a t)∥2

s

ˆρinv  

> T0

(η, L, M )

λ0T0

.

By Lemma 1 and Assumption 2, we can define 

q

ˆρinv  

> T0

(η, L, M ) to be ˜O(ppinv +

q 1 

> λ0

). The analysis of reg res  

> t

is similar to Theorem 1, with the addition of a term due to the introduction of ˆβinv 

which is ˜O(

q

T ˆρinv  

> T0

(η, L, M )/(λ0T0)). This implies, summing over t ∈ [T ], that the cumulative regret is ˜O

√T



pres +

q pinv Tλ0T0 + 1

> λ0

q TT0

 

. For the full proof, see Appendix C.2. 84.2 Accounting for errors in the subspace decomposition 

When estimating (Sinv , Sres ) using the available T0 observations, we need to consider two sources of errors. Lazzaretto et al. (2025) propose to estimate the subspaces via joint block diagonalization, in our case of the sample covariance matrices of the context-action features through time. The procedure first finds an irreducible joint decomposition of the features into orthogonal lower dimensional subspaces such that the projections of the features onto the subspaces are pairwise uncorrelated. Then, it groups together subspaces in which the linear relationship between the reward and the features is invariant, defining Sinv , and the remaining ones in which the linear relationship is time-varying, defining Sres .The first source of error can occur when some directions in Rp are wrongly identified as invariant or time-varying. Labeling directions as time-varying when they are invariant is less problematic, as it leads to a suboptimal but not incorrect algorithm: in this case, pinv is underestimated, preventing the decomposition from achieving its maximum benefit. Overestimating pinv , i.e., assuming invariance of time-varying directions, implies instead that in such directions the algorithm uses the pooled estimated parameter from the T0 historical observations to predict the reward, while the true parameter may have changed. Then, intuitively, ISD-linUCB suffers an additional loss scaling with how different the true parameter is in [T ] compared to its average in [−T0] in the wrongly labeled subspace. The second source of error is the estimation error due to finite sample approximation. To estimate the matrix U we first estimate (Var (φ(Xt, a t))) t∈[−T0]; in practice, it is sufficient to split 

[−T0] into m windows and compute ˆΣMi , defined as in (3) with Mi := {− im T0, . . . , − i−1 

> m

T0 − 1},

i ∈ { 1, . . . , m }. Then, ˆU is an estimated joint block diagonalizer for ( ˆΣMi )i∈{ 1,...,m }. The matrix 

ˆU then enters in the estimation of both βinv and δres  

> t

. Let ˆγISD = ˆβinv + ˆδres  

> t

be an estimator for 

γ0,t obtained using the ISD framework. For all t ∈ [T ], we can express the estimation error as 

ˆγISD  

> t

− γ0,t = ˆβinv − βinv + ˆδres  

> t

− δres 

> t

= ( ˆβinv − ˆΠSinv 

βinv ) + ( ˆΠSinv 

− ΠSinv 

)βinv 

+ ( ˆδres  

> t

− ˆΠSres 

δres  

> t

) + ( ˆΠSres 

− ΠSres 

)δres  

> t

. (10) The first and third term correspond to the estimation error for the invariant and residual component on the estimated subspaces. The second and fourth term only depend on the subspaces estimation error. When bounding the regret of Algorithm 1, we assume that the subspace decomposition is correctly estimated and only take into account the finite sample error in estimating (Sinv , Sres ),starting from the decomposition in (10). 

4.3 Complete regret analysis 

To analyze the terms in (10) we need to quantify the subspace estimation error. A common way to do this is to consider the distance between the true and the estimated subspace, which can be described through the notion of principal angles. Let U S ∈ Rp×k be the submatrix of U

whose columns span the subspace S of Rp, and let ˆU S be its estimate obtained through ˆU , whose columns span ˆS. The principal angles between S and ˆS are characterized as the inverse cosine of the nonzero singular values of the matrix ( ˆU S )⊤U S (intuitively, the cosine of the angle between two vectors is given by their normalized inner product). Let ΘS denote the diagonal matrix with the principal angles between S and ˆS on the diagonal. Then, we measure the distance between S

and ˆS by ∥ sin ΘS ∥op , i.e., the largest principal angle between S and ˆS (see for example Theorem 4.5 by Stewart and Sun, 1990), which also equals ∥ ˆΠS − ΠS ∥op (Stewart and Sun, 1990, Corollary 4.6). We denote by ∆Π such distance for the invariant and residual subspaces (this must be 9the same for both subspaces, see Stewart and Sun (1990, Preliminaries—Corollary 5.4)), and introduce the following assumption for the subspace decomposition error. 

Assumption 3. For all η ∈ (0 , 1) , it holds with probability at least 1 − η that ∆Π is 

O(plog( p/η )/T 0).

Assumption 3 can be justified using the Davis-Kahan theorem (see, for example, Yu et al., 2015) and by concentration results for sample covariance matrices (see, for example, Vershynin, 2018, Sections 5.4 and 5.6). In more detail, the Davis-Kahan theorem provides an upper bound for ∥ sin ΘS ∥op in terms of estimation error on a matrix M ∈ Rp×p such that the columns of U are eigenvectors for M . In our case, U jointly block diagonalizes (Var( φ(Xt, a t))) t∈[−T0], and for all 

t ∈ [−T0] the span of the columns of U inv and U res coincide with the union of subsets of eigenspaces of Var (φ(Xt, a t)) . We estimate ˆU through the matrices ( ˆΣMi )i=1 ,...,m , each computed using T0/m 

observations. As Var (φ(Xt, a t)) is not constant with t, we cannot use exact concentration results; with Assumption 3, we assume that ∆Π scales as the finite-sample error for estimating a fixed 

p-dimensional covariance matrix from T0 observations (we verify it empirically in Appendix B.1). The sum of the second and fourth term in (10) equals ∆Π γ0,t . Under Assumption 3, this quantity is, with probability at least 1 − η, O(plog( p/η )/T 0). Moreover, under Assumption 3, we can show the following high probability bound for the estimation error on ˆβinv in the estimated invariant subspace. 

Lemma 2. Consider Setting 1 and assume Assumptions 2 and 3 hold. Then, for all η ∈ (0 , 1) it holds with probability at least 1 − η that ∥ ˆβinv − ˆΠSinv 

βinv ∥ˆΣ[−T0] is 

O

q 

pinv log( 1  

> pinv λ0

) + log( 1 

> η

)



+ O

q 

pinv log( pη )



+ O

q 1 

> λ0

log( pη )



.

Proof sketch. We can decompose the error into three terms as follows 

∥ ˆβinv − ˆΠSinv 

βinv ∥ˆΣ[−T0] ≤∥ ( ˆU inv )⊤ ˆΣ[−T0](Π Sinv 

− ˆΠSinv 

)βinv ∥( ˜Σinv [−T0])−1

+∥( ˆU inv )⊤−1X

> t=−T0

φ(Xt, a t)φ(Xt, a t)⊤δres  

> t

∥( ˜Σinv [−T0])−1

+∥( ˆU inv )⊤−1X

> t=−T0

φ(Xt, a t)ϵt∥( ˜Σinv [−T0])−1 .

The last term can be analyzed as in the oracle case (Theorem 2). The first two terms are introduced due to the misalignment between the true and the estimated subspaces. Under Assumption 3, for all η ∈ (0 , 1) , with probability at least 1 − η, the first is O(

q

pinv log( pη )) and the second is O(

q 1 

> λ0

log( pη )) . For the full proof, see Appendix C.3. We can further obtain an upper bound for ∥ ˆβinv − ˆΠSinv 

βinv ∥2 by multiplying all the factors in Lemma 2 by 1√λ0T0

(see (9)). For the residual component, we can similarly bound ∥ˆδres  

> t

− ˆΠSres 

δres  

> t

∥ˆΣt−1 using Assumption 3 as follows. 10 Lemma 3. Consider Setting 1 and assume Assumptions 2 and 3 hold. Then, for all t ∈ [T ] and for all η ∈ (0 , 1) it holds with probability at least 1 − η that ∥ˆδres  

> t

− ˆΠSres 

δres  

> t

∥ˆΣt−1 is 

O

q 

pres log( tpres ) + log( 1 

> η

)



+ O

√pres t

q log( p/η ) 

> T0

+ ∥ ˆβinv − βinv ∥2

 

.

The first term is the same that appears in the case of oracle subspaces. The second term is instead due to the error in the estimation of the subspaces. 

Proof sketch. We can upper bound ∥ˆδres  

> t

− ˆΠSres 

δres  

> t

∥ˆΣt−1 by the sum of the following components 

∥ˆδres  

> t

− ˆΠSres 

δres  

> t

∥ˆΣt−1 ≤∥ 

> t−1

X 

> τ=1

( ˆU res )⊤φτ φ⊤ 

> τ

(Π Sres 

− ˆΠSres 

)δres  

> t

∥( ˜Σres 

> t−1)−1

+∥

> t−1

X 

> τ=1

( ˆU res )⊤φτ φ⊤ 

> τ

(βinv − ˆβinv )∥( ˜Σres 

> t−1)−1

+∥

> t−1

X 

> τ=1

( ˆU res )⊤φτ ϵτ ∥( ˜Σres  

> t−1)−1

+ √λ∥δres  

> t

∥2

where φτ := φ(Xτ , a τ ). The analysis of the last two terms is the same as in Theorem 1 and results in such terms being, for all η ∈ (0 , 1) , with probability at least 1 − η, O

q 

log( 1 

> η

) + pres log( tpres )



.The first two terms are introduced due to the subspace estimation error. Under Assumption 3, the first term is, for all η ∈ (0 , 1) , with probability at least 1 − η, O

q pres tT0 log( pη )



and the second is O

√pres t∥ ˆβinv − βinv ∥2



. For the full proof, see Appendix C.3. Lemma 2 and Lemma 3 imply the following regret bound for the ISD-linUCB algorithm. 

Theorem 3. Consider Setting 1 and assume Assumptions 2 and 3 hold. If ˆCβ is defined to contain 

ˆΠSinv 

βinv with probability at least 1 − 4/T 0 and ˆCδt is defined to contain ˆΠSres 

δres  

> t

with probability at least 1 − ( 2 

> T

+ 4 

> T0

), then, with probability at least 1 − ( 2 

> T

+ 9 

> T0

) the regret of ISD-linUCB is 

˜O

√T



pres + pres 

q Tλ0T0

p pinv +

q

max {λ0, 1 

> λ0

}

 

.

Theorem 3 shows in particular that, for T0 sufficiently larger than T (e.g., T0 = Ω( T 1+ ε) for some ϵ > 0), the regret bound for ISD-linUCB is dominated by pres √T .

# 5 Simulation experiments 

To support our theoretical results, we present several simulation experiments. We first apply the ISD-linUCB algorithm with oracle subspace knowledge in Section 5.1, and show that the regret indeed scales as pres √T , supporting the result that if T0 grows faster than T , the regret indeed scales as pres √T . We then include the estimation of the subspace decomposition in the algorithm (Section 5.2), and show that for increasing T0 its performance gets closer to the one of ISD-linUCB with oracle knowledge on subspaces. In all simulations, we provide a comparison with the standard LinUCB algorithm since, in Setting 1 we assume γ0,t to be fixed in [T] (in this setting, the performance of other non-stationary algorithms is comparable to the one of LinUCB, see Appendix B.2). 11 0 25 50 75 100         

> t
> 0
> 20
> 40
> 60
> ∑Tt=1  reg t
> linUCB ISD-linUCB(oracle Sinv ,Sres ): pres = 2
> pres = 4
> pres = 6
> pres = 8

Figure 2: Regret of ISD-linUCB with oracle (Sinv , Sres ) over T = 100 rounds for pres ∈ { 2, 4, 6, 8}.For each pres the experiment is repeated 20 times. The left plot shows the average performance and the standard deviation over the 20 repetitions, the right plot shows the distribution of the regret over the 20 repetitions. 

5.1 Oracle subspace decomposition 

We consider a setting with T0 = 2000 , T = 100 and |A| = 5 and with known U inv , U res . The matrix 

U is sampled as a random orthonormal matrix, and we generate the covariance matrix of context-action features as U ˜VtU ⊤, where ˜Vt is a block diagonal matrix with two blocks of dimensions 

pinv , p res . The entries of (U inv )⊤βinv are sampled uniformly in (0 .5, 1.5) . The same is done for the initial values of (U res )⊤δres  

> t

, to which for t ∈ [−T0] we add −1.5( t/T 0) sin 2(0 .25 it/T 0 + i), with 

i ∈ { 1, . . . , p res } being the entry index. The data in [−T0], used to estimate βinv , is the output of a bandit algorithm using a policy that chooses the actions uniformly at random. For t ∈ [T ], the entries of (U res )⊤δres  

> t

are sampled uniformly in (0 .5, 1.5) . We set λ = 0 .1 and we use η = 1 /T .We then run two experiments (in both, ˆβinv is not updated online). First, for a fixed dimension p = 10 of the context-action features we consider values of 

pres ∈ { 2, 4, 6, 8}. Figure 2 shows that the regret of ISD-linUCB (with oracle subspace information) grows sublinearly in T (left) and (approximately) linearly in pres (right), empirically supporting the result obtained in Theorems 2. To further support these results, we run a second experiment where we consider context-action features of dimension p varying between 3 and 10 , while keeping the dimension of Sres fixed to pres = 2 . The results are shown in Figure 3, which compares the cumulative regret to the one of the standard LinUCB algorithm. While the latter increases linearly with p, the regret of ISD-linUCB remains approximately constant as pres is fixed. 

5.2 Estimated subspace decomposition 

We consider the same data-generating process as in Section 5.1, but now include the subspace estimation using the T0 observations. We set T = 500 and consider T0 ∈ { 1000 , 3500 , 8000 }.Moreover, we fix the dimensions of the subspaces to pinv = 7 and pres = 3 . Figure 4 shows the cumulative regret for increasing T0 for the standard LinUCB algorithm (unaffected by T0) and for the algorithms studied in Sections 4.1.1, 4.1.2 and 4.2, supporting the presented theoretical results. Indeed, for increasing T0, the regret of the ISD-linUCB algorithm estimating (Sinv , Sres )

and βinv using the T0 observations gets closer to the one of an algorithm with oracle knowledge. Moreover, the regret of the algorithm using the estimated ISD is lower than the standard LinUCB one for all considered values of T0.12 3 4 5 6 7 8 9 10     

> p
> 0
> 20
> 40
> 60
> ∑Tt=1  reg t
> linUCB ISD-linUCB (oracle (Sinv ,Sres ),pres = 2 )

Figure 3: Cumulative regret of standard LinUCB and ISD-linUCB with oracle (Sinv , Sres ) for 

T = 100 and increasing values of context-action feature dimension p. For ISD-linUCB, the invariant component βinv is estimated using T0 = 2000 observations. pinv varies from 3 to 10 ,while the pres is fixed to 2. For each p the experiment is repeated 20 times. 1000 3500 8000 

T0     

> 0
> 1
> 2
> 3
> ∑Tt=1  reg t, T  = 500  linUCB ISD-linUCB (oracle Sinv ,Sres )ISD-linUCB (oracle Sinv ,Sres ,βinv )ISD-linUCB

Figure 4: Cumulative regret for T0 ∈ { 1000 , 3500 , 8000 } (20 repetitions) for ISD-linUCB, in comparison with the same algorithm having oracle information and with the standard linUCB alogorithm (unaffected by T0). For increasing T0, the regret of ISD-linUCB gets closer to the one of the oracle version. 

# 6 Conclusions 

Exploiting invariances in non-stationary environments allows linear contextual bandit algorithms to adapt more efficiently to changes in the reward model. We propose ISD-linUCB, a novel algorithm that learns such invariances from offline bandit data by decomposing the context-action feature space into two orthogonal subspaces: in one of the two subspaces, which has dimension 

pinv , the relationship between reward and context-action features remains stationary. When sufficient offline data are available, the regret of ISD-linUCB scales with (p − pinv ) rather than p,leading to a substantial performance gain in environments that are subject to rapid changes. 

Acknowledgments 

NP and ML are supported by a research grant (0069071) from Novo Nordisk Fonden. 13 References 

Y. Abbasi-Yadkori, D. Pál, and C. Szepesvári. Improved algorithms for linear stochastic bandits. 

Advances in Neural Information Processing Systems , 24, 2011. S. Agrawal and N. Goyal. Thompson sampling for contextual bandits with linear payoffs. In 

International Conference on Machine Learning , pages 127–135. PMLR, 2013. P. Auer. Using confidence bounds for exploitation-exploration trade-offs. Journal of Machine Learning Research , 3(Nov):397–422, 2002. S. Bilaj, S. Dhouib, and S. Maghsudi. Meta learning in bandits within shared affine subspaces. In 

27th International Conference on Artificial Intelligence and Statistics , pages 523–531. PMLR, 2024. W. C. Cheung, D. Simchi-Levi, and R. Zhu. Learning to optimize under non-stationarity. In 22nd International Conference on Artificial Intelligence and Statistics , pages 1079–1087. PMLR, 2019. W. Chu, L. Li, L. Reyzin, and R. Schapire. Contextual bandits with linear payoff functions. In 

14th International Conference on Artificial Intelligence and Statistics , pages 208–214, 2011. V. Dani, T. P. Hayes, and S. M. Kakade. Stochastic linear optimization under bandit feedback. In 21st Annual Conference on Learning Theory , pages 355–366, 2008. C. Kausik, K. Tan, and A. Tewari. Leveraging offline data in linear latent bandits. arXiv preprint arXiv:2405.17324 , 2024. T. Lattimore and C. Szepesvári. Bandit algorithms . Cambridge University Press, 2020. M. Lazzaretto, J. Peters, and N. Pfister. Invariant subspace decomposition. Journal of Machine Learning Research , 26(95):1–56, 2025. L. Li, W. Chu, J. Langford, and R. E. Schapire. A contextual-bandit approach to personalized news article recommendation. In Proceedings of the 19th international conference on World wide web , pages 661–670, 2010. M. Papini, A. Tirinzoni, M. Restelli, A. Lazaric, and M. Pirotta. Leveraging good representations in linear contextual bandits. In 38th International Conference on Machine Learning , pages 8371–8380. PMLR, 2021. Y. Qin, T. Menara, S. Oymak, S. Ching, and F. Pasqualetti. Non-stationary representation learning in sequential linear bandits. IEEE Open Journal of Control Systems , 1:41–56, 2022. Y. Russac, C. Vernade, and O. Cappé. Weighted linear bandits for non-stationary environments. 

Advances in Neural Information Processing Systems , 32, 2019. S. Saengkyongam, N. Thams, J. Peters, and N. Pfister. Invariant policy learning: A causal perspective. IEEE Transactions on Pattern Analysis and Machine Intelligence , 45(7):8606–8620, 2023. G. W. Stewart and J.-g. Sun. Matrix perturbation theory . Computer science and scientific com-puting. Academic Press, 1990. URL https://cir.nii.ac.jp/crid/1971149384759715084 .14 A. L. Trella, W. Dempsey, F. Doshi-Velez, and S. A. Murphy. Non-stationary latent auto-regressive bandits. arXiv preprint arXiv:2402.03110 , 2024. J. A. Tropp. User-friendly tail bounds for sums of random matrices. Foundations of computational mathematics , 12(4):389–434, 2012. R. Vershynin. High-dimensional probability: An introduction with applications in data science ,volume 47. Cambridge University Press, 2018. Y. Yu, T. Wang, and R. J. Samworth. A useful variant of the Davis–Kahan theorem for statisticians. Biometrika , 102(2):315–323, 2015. P. Zhao, L. Zhang, Y. Jiang, and Z.-H. Zhou. A simple approach for non-stationary linear bandits. In 23rd International Conference on Artificial Intelligence and Statistics , pages 746–755. PMLR, 2020. 15 A Further related works 

Linear contextual bandits The stochastic linear bandit problem is extensively studied in the literature. It is commonly assumed (see, for example, Lattimore and Szepesvári, 2020) that there exists an unknown linear parameter of dimension p that is shared among all context-action features: this allows in particular to deal with arbitrarily large action spaces, and to obtain regret bounds that do not depend on the cardinality K of the action space. Two standard algorithms proposed in the literature to solve this problem are LinUCB (linear upper confidence bound), and LinTS (linear Thompson sampling). In UCB-based algorithms, at all rounds the agent chooses the action maximizing an upper confidence bound on the estimated regret. Li et al. (2010) consider finite action spaces and build such confidence bound directly on the estimated regret for each possible action. Chu et al. (2011) show a regret bound for this version of LinUCB of ˜O(√pKT ).Abbasi-Yadkori et al. (2011) propose a more general UCB algorithm that allows for infinitely large action spaces, by constructing a confidence set for the estimated linear parameter rather than for the reward, and show a regret bound of ˜O(p√T ). Linear bandits based on Thompson sampling (Agrawal and Goyal, 2013) at all rounds sample from the rewards posterior distribution for all possible arms, and then choose the action maximizing the sampled reward. LinTS is shown to achieve a regret of ˜O(p 32

√T ).In these algorithms, no assumptions (besides boundedness of the context-action features and of the bandit parameter) are made on the nature of the observed contexts. Papini et al. (2021) review different diversity conditions on the context-action features space that allow to improve the regret bounds, leading to either logarithmic or constant regret in T . One example is when the variance of the optimal context-action features is a positive definite matrix, meaning that all directions in Rp are potentially optimal for some context. 

Linear latent contextual bandits To deal with heterogeneity in the reward function, Kausik et al. (2024) consider a latent linear contextual bandits framework, where a latent linear parameter 

θ ∈ Rpl , with pl < p , is shared across all heterogeneous observations. They assume the reward is of the form Rat 

> t

= φ(Xt, a t)⊤γ0 + ϵt, with γ0 = U θ and U ∈ Rp×pl is a unitary matrix. They propose to learn the matrix U from offline data, and use this to obtain a regret bound that depends on the dimension pl of the latent parameter rather than the context dimension p.Bilaj et al. (2024) assume that the agent sequentially interacts with different tasks such that, for each task, the parameter γ0 is independently drawn from the same distribution. They assume there exists an orthogonal projection matrix P with rank pl such that Eγ0 [∥(I −P )( γ0 −E[γ0]) ∥2] ≪

Eγ0 [∥P (γ0 − E[γ0]) ∥2] ≤ Eγ0 [∥γ0 − E[γ0]∥2], namely the variance of the distribution of γ0 is very low along p − pl orthogonal directions. They show a regret bound that depends on pl rather than 

p.Qin et al. (2022) consider a multi-task sequential linear bandit model where the agent plays a sequence of S bandit tasks (drawn from m different environments) for N rounds each. They model the reward as Rat 

> t

= φ(Xt, a t)⊤γs(t) + ϵt and assume that there exists a shared latent parameter θ ∈ Rpl such that for all s ∈ [S] there exists a matrix Us ∈ Rp×pl such that γs = Usθ.They propose an algorithm that sequentially learns the latent representation and achieves a regret of ˜O(pp l

√mSN + plS√N ).Trella et al. (2024) consider a non-stationary reward model governed by an underlying latent variable evolving as an AR process of order ℓ. They show a regret bound of ˜O(ℓ√pT ).

Non-stationary linear bandits Cheung et al. (2019) and Russac et al. (2019) consider the linear bandit problem under non-stationarity of the environment, and in particular allow for the linear bandit parameter to change through time, so that the reward model is of the 16 form Rat = φ(Xt, a )⊤γ0,t + ϵt. They define the variation budget BT as a constant such that 

PT −1 

> t=1

∥γ0,t +1 − γ0,t ∥2 ≤ BT . Cheung et al. (2019) propose an algorithm based on a sliding window regularized least squares estimator, Russac et al. (2019) propose instead to weight past observations by a discounting factor. Both achieve a regret bound of ˜O(p 78 T 34 B 14 

> T

). As shown by Zhao et al. (2020), this same regret is achieved by periodically restarting the standard LinUCB algorithm, where the optimal number of rounds to be played before each restart depends on BT .This means that the excess regret compared to the stationary case is due to applying the standard LinUCB algorithm assuming that the linear parameter is fixed, and incurring an additional loss that depends on how much the parameter is changing in the time period between two restarts. Cheung et al. (2019) also show a lower bound for this setting of Ω( p 23 T 23 B 13 

> T

).

# B Additional experiments 

B.1 Projection error 

The key role of Assumption 3 is to ensure that the projection error on Sinv and Sres decreases with the sample size T0 used to estimate the matrix U (and thus the projection matrices onto the subspaces). Within the same experiment described in Section 5.2, we evaluate such error, i.e., ∥ ˆΠSinv 

− ΠSinv 

∥op , and show (Figure 5) that it indeed decreases approximately as √T0, as assumed in Assumption 3. 1000 3500 8000   

> T0
> 0.1
> 0.2
> 0.3
> ‖Πinv  − ˆΠinv ‖op
> 1000 3500 8000
> T0
> 5
> 10
> 15
> √T0‖Πinv  − ˆΠinv ‖op

Figure 5: Projection error for T0 ∈ { 1000 , 3500 , 8000 }. The plot on the left shows the same values multiplied by √T0, confirming our assumption. 

B.2 Other non-stationary algorithms 

Within the same setting of the second experiment described in Section 5.1, we add an additional comparison with two non-stationary algorithms: one using a sliding window (SW-linUCB) by Cheung et al. (2019) and one using a discounting factor (D-linUCB) by Russac et al. (2019). In this setting, the linear parameter γ0,t is fixed in the time horizon [T ], so the dimension of the window for SW-linUCB is set to T and the discounting factor for D-linUCB is set to 0.999 , and Figure 6 shows that these two algorithms indeed have a comparable performance to LinUCB. In particular, we keep γ0,t fixed because our focus is on the reduction of the dimensionality (for the non-stationary part of the problem). When this assumption fails, we could still integrate the ISD framework within existing non-stationary algorithms to improve their performance in terms of dimensionality, as we do in the presented experiments with LinUCB. 17 3 4 5 6 7 8 9 10 

p    

> 0
> 10
> 20
> 30
> 40
> 50
> ∑Tt=1  reg t
> linUCB ISD-linUCB (oracle (Sinv ,Sres ),pres = 2 )SW-linUCB D-linUCB

Figure 6: Cumulative regret of standard LinUCB, SW-linUCB, D-linUCB and ISD-linUCB with oracle (Sinv , Sres ) for T = 100 and increasing values of context-action feature dimension p. For ISD-linUCB, the invariant component βinv is estimated using T0 = 2000 observations. pinv varies from 3 to 10 , while the pres is fixed to 2.The same happens for the simulation experiments presented in Section 5.2, as shown in Figure 7. 1000 3500 8000 

T0     

> 0
> 10
> 20
> 30
> 40
> 50
> 60
> 70
> ∑Tt=1  reg t
> linUCB ISD-linUCB (oracle Sinv ,Sres )ISD-linUCB (oracle Sinv ,Sres ,βinv )ISD-linUCB SW-linUCB D-linUCB

Figure 7: Cumulative regret for T0 ∈ { 1000 , 3500 , 8000 } for ISD-linUCB, in comparison with the same algorithm having oracle information and with the standard linUCB alogorithm, with SW-linUCB and D-linUCB (unaffected by T0). For each T0 the experiment is repeated 20 times. 

# C Regret analysis: proofs 

C.1 Oracle Sinv , Sres , β inv 

Proof of Theorem 1. We start by proving that the estimated confidence set ˆCδt around ˆδres  

> t

contains 

δres  

> t

with high probability. Let ˜Σres  

> t

and ˆΣt be defined as in Section 3 and (3) , respectively, and let ˜δres  

> t

:=  λI pres +(Xres )⊤ 

> t

Xres 

> t

−1(Xres  

> t

)⊤Rres  

> t

. For all t ∈ [T ], we have that 

∥ˆδres  

> t

− δres  

> t

∥ˆΣt−1 = ∥˜δres  

> t

− (U res )⊤δres  

> t

∥˜Σres 

> t−1

18 = ∥( ˜Σres 

> t−1

)−1

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )( φ(Xτ , a τ )⊤δres  

> τ

+ ϵτ ) − (U res )⊤δres  

> t

∥˜Σres 

> t−1

= ∥( ˜Σres 

> t−1

)−1

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )ϵτ − λ( ˜Σres 

> t−1

)−1(U res )⊤δres  

> t

∥˜Σres 

> t−1

= ∥

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )ϵτ − λ(U res )⊤δres  

> t

∥( ˜Σres 

> t−1)−1

≤ ∥ 

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )ϵτ ∥( ˜Σres  

> t−1)−1

+ ∥λ(U res )⊤δres  

> t

∥( ˜Σres 

> t−1)−1

≤ ∥ 

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )ϵτ ∥( ˜Σres  

> t−1)−1

+ √λ∥δres  

> t

∥2 (11) From Lemma 1, we obtain that, for all η ∈ (0 , 1) , with probability at least 1 − η,

∥ˆδres  

> t

− δres  

> t

∥ˆΣt−1 ≤ σ

vuut2 log 

 1

η



+ log det( ˜Σres 

> t−1

)det( λI pres )

!

+ √λ∥δres  

> t

∥2

Assuming that L and M where chosen sufficiently large in Algorithm 1 such that they indeed bound the context-action features and the linear parameter respectively, we get that, with probability at least 1 − η,

∥ˆδres  

> t

− δres  

> t

∥ˆΣt−1 ≤ σ

s

2 log 

 1

η



+ pres log 



1 + tL 2

λp res 



+ √λM =: pˆρres  

> t

(η, L, M ).

Assumption 1 allows us to estimate γ0,t by separately estimating βinv and δres  

> t

on the invariant and residual subspace respectively. This implies that the full regret consists of the sum of regret on the invariant and residual subspace, as follows 

reg t = ( φ(Xt, a ∗ 

> t

) − φ(Xt, a t)) ⊤γ0,t 

≤ φ(Xt, a t)⊤(¯ γt − γ0,t )= φ(Xt, a t)⊤( ¯βt − βinv )

| {z }

> reg inv
> t

+ φ(Xt, a t)⊤(¯δt − δres  

> t

)

| {z }

> reg res
> t

= reg res  

> t

, (12) where ¯γt = arg max γ∈ ˆCβ ⊕ ˆCδt

φ(Xt, a t)⊤γ, ( ¯βt, ¯δt) := arg max β∈ ˆCβ ,δ ∈ ˆCδt

φ(Xt, a t)⊤(β + δ) and we have that by construction ¯γt = ¯βt + ¯δt. The first inequality holds because, by definition, ¯γt

maximizes the upper confidence bound at time t. The last equality follows from the assumption that we have oracle knowledge of βinv , therefore ˆCβ = {βinv } and reg inv  

> t

= 0 and we only need to consider the regret on the residual subspace. Then, for all t ∈ [T ], the residual instantaneous regret is such that 

reg res  

> t

= φ(Xt, a t)⊤(¯δt − δres  

> t

)

≤ ∥ (U res )⊤φ(Xt, a t)∥( ˜Σres  

> t−1)−1

∥(U res )⊤(¯δt − δres  

> t

)∥˜Σres 

> t−1

≤ 2∥(U res )⊤φ(Xt, a t)∥( ˜Σres 

> t−1)−1

pˆρres  

> t

(η, L, M ), (13) 19 The first inequality follows from Cauchy-Schwarz. The last inequality is obtained by adding and subtracting ˆδres  

> t

inside the second norm and using that ¯δt ∈ ˆCδt by definition and that, with probability at least 1 − η, δres  

> t

∈ ˆCδt . Moreover, we have that for all t ∈ [T ], reg t ≤ 2LM and for 

η = 1 /T , with T > 2, that ˆρres  

> t

(η, L, M ) ≥ 1. Together with (13), it implies that 

reg res  

> t

≤ min {2LM, 2pˆρres  

> t

(η, L, M )∥(U res )⊤φ(Xt, a t)∥( ˜Σres  

> t−1)−1

}≤ 2 max {LM, 1}pˆρres  

> t

(η, L, M ) min {1, ∥(U res )⊤φ(Xt, a t)∥( ˜Σres  

> t−1)−1

}≤ Cres pˆρres  

> t

(η, L, M ) min {1, ∥(U res )⊤φ(Xt, a t)∥( ˜Σres  

> t−1)−1

}

where Cres := 2 max {LM, 1}. Using that, for all x ≥ 0, min {1, x } ≤ 2 log (1 + x), we can upper bound the cumulative full regret as 

Reg T = X 

> t∈[T]

reg res  

> T

≤

s

T X 

> t∈[T]

(reg res  

> t

)2

≤ Cres 

s

T ρ res  

> T

(η, L, M ) X 

> t∈[T]

log(1 + ∥(U res )⊤φ(Xt, a t)∥2( ˜Σres  

> t−1)−1

). (14) As a final step, we use the following Lemma by Lattimore and Szepesvári (2020), with notation adapted to our problem. 

Lemma 4 (Lattimore and Szepesvári (2020), Lemma 19.4) . Consider the same assumptions of Theorem 1. Let λ > 0 and L < ∞. Then, 

X 

> t∈[T]

log(1 + ∥(U res )⊤φ(Xt, a t)∥2( ˜Σres  

> t−1)−1

) ≤ pres log 



1 + T L 2

λp res 



.

Lemma 4 implies that 

Reg T ≤ Cres 

s

T p res log 



1 + T L 2

λp res 



σ

s

2 log 

 1

η



+ pres log 



1 + T L 2

λp res 



+ √λM 

!

.

Finally, choosing η = 1 /T implies that Reg T is ˜O(pres √T ).

C.2 Oracle subspaces 

Proof of Theorem 2. We start by showing that, under oracle knowledge of the subspaces, the radius 

q

ˆρinv  

> T0

(η, L, M ) for the confidence set ˆCβ introduced in (7) can be defined to be ˜O(ppinv +

q 1 

> λ0

). To do so, we consider the estimation error on ˆβinv defined in (6) , where ˆU inv is replaced by U inv .

∥ ˆβinv − β∥ˆΣ[−T0] (15) 

= ∥U inv ( ˜Σinv [−T0])−1

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)Rat 

> t

− U inv (U inv )⊤βinv ∥ˆΣ[−T0]

= ∥( ˜Σinv [−T0])−1

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)Rat 

> t

− (U inv )⊤βinv ∥˜Σinv [−T0]

20 = ∥( ˜Σinv [−T0])−1

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)( φ(Xt, a t)⊤(βinv + δres  

> t

) + ϵt) − (U inv )⊤βinv ∥˜Σinv [−T0]

= ∥( ˜Σinv [−T0])−1

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)( φ(Xt, a t)⊤U res (U res )⊤δres  

> t

+ ϵt)∥˜Σinv [−T0]

≤ ∥ ( ˜Σinv [−T0])−1

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥˜Σinv [−T0]

+ ∥( ˜Σinv [−T0])−1

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)ϵt∥˜Σinv [−T0]

= ∥

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥( ˜Σinv [−T0])−1 (16) 

+ ∥( ˜Xinv  

> T0

)⊤ϵT0 ∥( ˜Σinv [−T0])−1 (17) where ˜Σinv [−T0] := ( U inv )⊤ ˆΣ[−T0]U inv , ˜Xinv  

> T0

:= [( U inv )⊤φ(X−T0 , a −T0 ), . . . , (U inv )⊤φ(X−1, a −1)] ⊤ ∈

RT0×pinv 

and ϵT0 := [ ϵ−T0 , . . . , ϵ −1]⊤ ∈ RT0 . For the term in (16), it holds that 

∥

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥( ˜Σinv [−T0])−1

≤∥ ( ˜Σinv [−T0])− 12 ∥op ∥

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥2

≤

r 1

λ0T0

∥

> −1

X

> t=−T0

αt∥2 (18) where αt := ( U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∈ Rpinv 

. We bound ∥ P−1 

> t=−T0

αt∥2 using the matrix Hoeffding inequality (see, for example, Tropp, 2012, Theorem 1.3, which we report below). 

Lemma 5 (Matrix Hoeffding inequality) . Let {Mk}k∈[K] a finite sequence of independent, random, self-adjoint matrices of dimension p and let {Ak}k∈[K] be a sequence of fixed self-adjoint matrices of dimension p. Assume that, for all k ∈ [K], E[Mk] = 0 and M2 

> k

⪯ Ak almost surely. Then, for all ξ > 0, it holds that 

P

λmax 

 X

> k∈[K]

Mk

 ≥ ξ

 ≤ p exp 



− ξ2

8σ2



where σ2 := X

> k∈[K]

A2 

> k

.

By definition of ISD, it holds that, for all t ∈ [−T0], E[αt] = 0 . Using Cauchy-Schwarz, we also have that ∥αt∥2 ≤ L2M . Moreover, we can construct the self-adjoint dilation (see Tropp, 2012, Section 2.6) for αt as 

S (αt) := 

 0 αt

α⊤ 

> t

0



∈ R(pinv +1) ×(pinv +1) ,

21 which is such that ∥αt∥2 = ∥S (αt)∥op = λmax (S (αt)) and 

S (αt)2 := 

αtα⊤ 

> t

00 α⊤ 

> t

αt



.

Hoeffding inequality can be now applied to the sequence of self-adjoint matrices (S (αt)) t∈[−T0].Let σ2 

> α

:= ∥ P 

> t∈[−T0]

S (αt)2∥op ≤ P 

> t∈[−T0]

∥S (αt)∥2op ≤ T0(L2M )2. Then, for all ξ ≥ 0 it holds by Hoeffding’s inequality that 

P

λmax 

 X

> t∈[−T0]

S (αt)

 ≥ ξ

 ≤ (pinv + 1) e− ξ28σ2 

> α

.

Using the definition of S (αt) and defining η := ( pinv + 1) e− ξ28σ2 

> α

, the above is equivalent to 

P

∥ X

> t∈[−T0]

αt∥2 ≤ 2L2M

s

2T0 log 

 pinv + 1 

η

 ≥ 1 − η. 

Therefore, with probability at least 1 − η,

q 1 

> λ0T0

∥ P−1 

> t=−T0

αt∥2 is O

r  

> 1
> λ0

log 

 pinv 

> η



.We now need to bound the term ∥( ˜Xinv  

> T0

)⊤ϵT0 ∥( ˜Σinv [−T0])−1 in (17) . By Assumption 2, we have that ˆΣ[−T0] ⪰ λ0T0Ip, which implies that ˜Σinv [−T0] ⪰ λ0T0Ipinv and therefore ˜Σinv [−T0] ⪰ 

> 12

(λ0T0Ipinv + ˜Σinv [−T0]). This implies that, 

∥( ˜Xinv  

> T0

)⊤ϵT0 ∥( ˜Σinv [−T0])−1 ≤ √2∥( ˜Xinv  

> T0

)⊤ϵT0 ∥(λ0T0Ipinv + ˜Σinv  

> T0)−1

Lemma 1 implies that, for all η ∈ (0 , 1) , with probability at least 1 − η,

∥( ˜Xinv  

> T0

)⊤ϵT0 ∥(λ0T0Ipinv + ˜Σinv [−T0])−1 ≤ σ

vuutlog 

 1

η



+ 12 log det( ˜Σinv [−T0])det( λ0T0Ipinv )

!

By the arithmetic mean—geometric mean inequality, it holds that 

det( ˜Σinv [−T0]) ≤

 1

pinv trace( ˜Σinv [−T0])

pinv 

≤

 T0L2

pinv 

pinv 

where the last inequality follows from trace ( ˜Σinv [−T0]) = trace (( ˜Xinv  

> T0

)⊤ ˜Xinv  

> T0

) ≤ T0L2. Moreover, 

det( λ0T0Ipinv ) = ( λ0T0)pinv 

. Therefore, with probability at least 1 − η,

∥( ˜Xinv  

> T0

)⊤ϵT0 ∥(λ0T0Ipinv + ˜Σinv [−T0])−1 ≤ σ

s

log 

 1

η



+ pinv 

2 log 

 T0L2

pinv λ0T0



(19) and, with probability at least 1 − 2η,

∥ ˆβinv − β∥ˆΣ[−T0]

≤σ

s

2 log 

 1

η



+ pinv log 

 L2

pinv λ0



+ 2 L2M

s

2

λ0

log 

 pinv + 1 

η



=: ˆ ρinv  

> T0

(η, L, M ) (20) 22 Using Assumption 2, we further obtain that, with probability at least 1 − 2η,

∥ ˆβinv − β∥2 ≤

s

ˆρinv  

> T0

(η, L, M )

λ0T0

(21) Consider now Algorithm 1. For all a ∈ A , let UCB β,δ t (a) := max β∈ ˆCβ ,δ ∈ ˆCδt

φ(Xt, a )⊤(β +δ) and let 

( ¯βt, ¯δt) := arg max β∈ ˆCβ ,δ ∈ ˆCδt

φ(Xt, a t)⊤(β + δ) . Then, as in (12) , for all t ∈ [T ], the instantaneous regret is such that reg t ≤ reg inv  

> t

+ reg res  

> t

. We upper bound the reg inv  

> t

as follows 

reg inv  

> t

:= (( U inv )⊤φ(Xt, a t)) ⊤(U inv )⊤( ¯βt − βinv )

≤ ∥ (U inv )⊤φ(Xt, a t)∥2∥(U inv )⊤( ¯βt − βinv )∥2

≤ 2L

√λ0T0

σ

s

2 log 

 1

η



+ pinv log 

 L2

pinv λ0



+ 2 L2M

s

2

λ0

log 

 pinv + 1 

η

!

where the first inequality holds with probability 1 − 2η and follows from the definition of ˆCβ and from Cauchy-Schwarz inequality. For reg res  

> t

, the analysis from Theorem 1 remains valid, with an additional term to be considered due to using the estimated invariant component instead of the oracle one. In particular, following the same steps as in (11), we have that, for all t ∈ [T ],

∥ˆδres  

> t

− δres  

> t

∥ˆΣt−1 ≤ ∥ 

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )ϵτ ∥( ˜Σres  

> t−1)−1

+ √λ∥δres  

> t

∥2

+ ∥

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(βinv − ˆβinv )∥( ˜Σres  

> t−1)−1

.

We analyzed the first two terms in Theorem 1. For the last term, we have that 

∥

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(βinv − ˆβinv )∥( ˜Σres 

> t−1)−1

≤∥ ( ˜Σres 

> t−1

)− 12 ∥op ∥

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤U inv ∥op ∥(U inv )⊤(βinv − ˆβinv )∥2.

Using matrix Hoeffding inequality (see Lemma 5) and following the same steps used to bound (16) that, for all η ∈ (0 , 1) , with probability at least 1 − η,

∥

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤U inv ∥op ≤ 2L2

s

2( t − 1) log 

 p + 1 

η



.

Therefore, with probability at least 1 − 3η,

∥

> t−1

X 

> τ=1

(U res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(βinv − ˆβinv )∥( ˜Σres 

> t−1)−1

≤ 1

√λ 2L2

s

2( t − 1) log 

 p + 1 

η

s

ˆρinv  

> T0

(η, L, M )

λ0T0

.

23 The right-hand side of the inequality can be added to pˆρres  

> t

(η, L, M ) obtained in the proof of Theorem 1, so that, with probability at least 1 − 4η

X 

> t∈[T]

reg res  

> t

≤ Cres 

s

T p res log 



1 + T L 2

λp res 



×

σ

s

2 log 

 1

η



+ pres log 



1 + T L 2

λp res 



+ √λM 2L2

s

2T ˆρinv  

> T0

(η, L, M )

λλ 0T0

log 

 p + 1 

η

 .

Finally, this implies that the cumulative regret can be upper bounded, for all η ∈ (0 , 15 ) with probability at least 1 − 5η as 

Reg T = X 

> t∈[T]

reg t = X 

> t∈[T]

reg inv + X 

> t∈[T]

reg res 

≤ 2LT 

√λ0T0

σ

s

2 log 

 1

η



+ pinv log 

 L2

pinv λ0



+ 2 L2M

s

2

λ0

log 

 pinv + 1 

η

!

+ Cres 

s

T p res log 



1 + T L 2

λp res 



×

σ

s

2 log 

 1

η



+ pres log 



1 + T L 2

λp res 



+ √λM + 2 L2

s

2T ˆρinv  

> T0

(η, L, M )

λλ 0T0

log 

 p + 1 

η



Choosing η′ = 1 /T , where η′ := 5 η, implies that Reg T is, with probability at least 1 − η′

˜O

√T



pres +

q pinv Tλ0T0 + 1

> λ0

q TT0

 

.

C.3 Accounting for subspace decomposition errors 

Proof of Lemma 2. We want to bound the ˆΣ[−T0]-norm of the estimation error for the invari-ant component in the estimated invariant subspace ˆSinv , i.e., ∥ ˆβinv − ˆΠSinv 

βinv ∥ˆΣ[−T0] . Let 

˜Σinv [−T0] := ( ˆU inv )⊤ ˆΣ[−T0] ˆU inv , XT0 := [ φ(X−T0 , a −T0 ), . . . , φ (X−1, a −1)] ⊤ ∈ RT0×p, RT0 :=[Ra−T0 

> −T0

, . . . , R a−1 

> −1

]⊤ ∈ RT0 and ϵT0 := [ ϵ−T0 , . . . , ϵ −1]⊤ ∈ RT0 . Then, we have that 

∥ ˆβinv − ˆΠSinv 

βinv ∥ˆΣ[−T0] = ∥ ˆU inv (( ˜Σinv [−T0])−1

> −1

X

> t=−T0

( ˆU inv )⊤φ(Xt, a t)Rat 

> t

− ( ˆU inv )⊤βinv )∥ˆΣ[−T0]

≤ ∥ ( ˜Σinv [−T0])−1( ˆU inv )⊤X⊤ 

> T0

XT0 βinv − ( ˆU inv )⊤βinv ∥˜Σinv [−T0]

+ ∥( ˜Σinv [−T0])−1

> −1

X

> t=−T0

( ˆU inv )⊤φ(Xt, a t)φ(Xt, a t)⊤δres  

> t

∥˜Σinv [−T0]

+ ∥( ˜Σinv [−T0])−1( ˆU inv )⊤X⊤ 

> T0

ϵT0 ∥˜Σinv [−T0]

= ∥( ˆU inv )⊤X⊤ 

> T0

XT0 (U inv (U inv )⊤ − ˆU inv ( ˆU inv )⊤)βinv ∥( ˜Σinv [−T0])−1 (22) 

+ ∥

> −1

X

> t=−T0

( ˆU inv )⊤φ(Xt, a t)φ(Xt, a t)⊤δres  

> t

∥( ˜Σinv [−T0])−1 (23) 24 + ∥( ˆU inv )⊤X⊤ 

> T0

ϵT0 ∥( ˜Σinv [−T0])−1 (24) The term in (24) can be upper bounded exactly as in Section C.2 (see Equation (19) ), since the presence of ˆU inv in place of U inv does not influence its analysis. The terms in (22) and (23) appear because of the misalignment between the true and the estimated invariant subspace: the former represents how much of the invariant parameter we are not able to estimate due to the difference between ˆSinv and Sinv , the latter quantifies how much of the true residual parameter enters in the estimation of the invariant component due to the intersection between ˆSinv and Sres .For (22), we have that 

∥( ˆU inv )⊤X⊤ 

> T0

XT0 (Π Sinv 

− ˆΠSinv 

)βinv ∥( ˜Σinv [−T0])−1

=∥( ˜Σinv [−T0])− 12 ( ˆU inv )⊤X⊤ 

> T0

XT0 (Π Sinv 

− ˆΠSinv 

)βinv ∥2

≤

s X

> t∈[−T0]

∥( ˜Σinv [−T0])− 12 ( ˆU inv )⊤φ(Xt, a t)∥22

s X

> t∈[−T0]

(φ(Xt, a t)⊤(Π Sinv − ˆΠSinv )βinv )2

≤ppinv pT0L2M 2(∆Π) 2 =: ˆ ρinv 1

The first inequality uses Cauchy-Schwarz inequality. The second inequality follows from the fact that 

X

> t∈[−T0]

∥( ˜Σinv [−T0])− 12 ( ˆU inv )⊤φ(Xt, a t)∥22

= X

> t∈[−T0]

φ(Xt, a t)⊤ ˆU inv ( ˜Σinv [−T0])−1( ˆU inv )⊤φ(Xt, a t)= trace 

 X

> t∈[−T0]

( ˆU inv )⊤φ(Xt, a t)φ(Xt, a t)⊤ ˆU inv ( ˜Σinv [−T0])−1



= trace  Ipinv 



= pinv ,

and by singling out the norm of the individual factors under the second square root. Finally, under Assumption 3 we obtain that, for all η ∈ (0 , 1) , it holds with probability at least 1 − η that 

∥( ˆU inv )⊤X⊤ 

> T0

XT0 (Π Sinv 

− ˆΠSinv 

)βinv ∥( ˜Σinv [−T0])−1 is O(

q

pinv log( pη )) .We now need to upper bound the term in (23), that is 

∥

> −1

X

> t=−T0

( ˆU inv )⊤φ(Xt, a t)φ(Xt, a t)⊤δres  

> t

∥( ˜Σinv [−T0])−1

=∥

> −1

X

> t=−T0

( ˆU inv )⊤(U inv (U inv )⊤ + U res (U res )⊤)φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥( ˜Σinv [−T0])−1

≤∥ 

> −1

X

> t=−T0

( ˆU inv )⊤U inv (U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥( ˜Σinv [−T0])−1

+∥

> −1

X

> t=−T0

( ˆU inv )⊤U res (U res )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥( ˜Σinv [−T0])−1

25 For the second term, it holds that 

∥

> −1

X

> t=−T0

( ˆU inv )⊤U res (U res )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥( ˜Σinv [−T0])−1

≤∥ ( ˜Σinv [−T0])− 12 ∥op ∥( ˆU inv )⊤U res ∥op T0L2M

≤

r 1

λ0T0

∆Π T0L2M

=

r T0

λ0

∆Π L2M =: ˆ ρinv 2 .

Under Assumption 3, for all η ∈ (0 , 1) , this quantity is, with probability at least 1 − η,

O

q log( p/η )

> λ0



. For the first term, we obtain 

∥

> −1

X

> t=−T0

( ˆU inv )⊤U inv (U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥( ˜Σinv [−T0])−1

≤∥ ( ˜Σinv [−T0])− 12 ∥op ∥( ˆU inv )⊤U inv ∥op ∥

> −1

X

> t=−T0

(U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∥2

≤

r 1

λ0T0

∥

> −1

X

> t=−T0

αt∥2

where αt := ( U inv )⊤φ(Xt, a t)φ(Xt, a t)⊤U res (U res )⊤δres  

> t

∈ Rpinv 

and in the last inequality we have used that ∥( ˆU inv )⊤U inv ∥op ≤ 1 since both matrices have orthonormal columns. This is the same quantity that appears in (18) in the oracle subspaces case (together with (24) , this forms the full oracle bound given in (20) , which we denote here by ˆρinv 3 ). We have shown (18) to be, for all 

η ∈ (0 , 1) , with probability at least 1 − η, O

r  

> 1
> λ0

log 

 pinv 

> η



. For the same choice of η, this term is dominated by O

r  

> 1
> λ0

log 

 pη



. Hence, the expression in (23) is, with probability at least 1 − 2η, O

r  

> 1
> λ0

log 

 pinv 

> η



.Let 

ˆρinv  

> T0

(η, L, M ) := 

> 3

X

> i=1

ˆρinv  

> i

= σ

s

2 log 

 1

η



+ pinv log 

 L2

pinv λ0



+ 2 L2M

s

2

λ0

log 

 pinv + 1 

η



(25) 

+ ppinv T0∆Π LM +

r T0

λ0

∆Π L2M. 

By the union bound we have that, with probability at least 1 − 4η,

∥ ˆβinv − ˆΠSinv 

βinv ∥ˆΣ[−T0] ≤

> 3

X

> i=1

ˆρinv  

> i

=: ˆ ρinv  

> T0

(η, L, M )

26 is 

O

s

log 

 1

η



+ pinv log 

 1

pinv λ0

 !

+ O

s

pinv log 

 pη

!

+ O

s

1

λ0

log 

 pη

!

.

This concludes the proof of the lemma. 

Proof of Lemma 3. We want to bound the estimation error for the residual component in the residual subspace, that is, ∥ˆδres  

> t

− ˆΠSres 

δres  

> t

∥ˆΣt−1 . Recall that we have defined ˜Σres  

> t

:= ( ˆU res )⊤ ˆΣt ˆU res .We start by stating explicitly the expression for ˆδres  

> t

, obtaining that 

∥ˆδres  

> t

− ˆΠSres 

δres  

> t

∥ˆΣt−1

=∥ ˆU res ( ˜Σres 

> t−1

)−1 X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )( Raτ 

> τ

− φ(Xτ , a τ )⊤ ˆβinv ) − ˆU res ( ˆU res )⊤δres  

> t

∥ˆΣt−1

=∥( ˜Σres 

> t−1

)−1 X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )( Raτ 

> τ

− φ(Xτ , a τ )⊤ ˆβinv ) − ( ˆU res )⊤δres  

> t

∥˜Σres 

> t−1

≤∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(U res (U res )⊤ − ˆU res ( ˆU res )⊤)δres  

> t

− λ( ˆU res )⊤δres  

> t

∥( ˜Σres 

> t−1)−1

+∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(βinv − ˆβinv )∥( ˜Σres 

> t−1)−1

+∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )ϵτ ∥( ˜Σres 

> t−1)−1

≤∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(U res (U res )⊤ − ˆU res ( ˆU res )⊤)δres  

> t

∥( ˜Σres  

> t−1)−1

(26) 

+∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(βinv − ˆβinv )∥( ˜Σres  

> t−1)−1

(27) 

+∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )ϵτ ∥( ˜Σres  

> t−1)−1

+ √λ∥δres  

> t

∥2 (28) The analysis of the terms in (28) follows the same steps as the one for (11) in the oracle case. In particular, from Theorem 1 we have that for all η ∈ (0 , 1) , with probability at least 1 − η,

∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )ϵτ ∥( ˜Σres  

> t−1)−1

+ √λ∥δres  

> t

∥2

≤σ

s

2 log 

 1

η



+ pres log 



1 + tL 2

λp res 



+ √λM =: ˆ ρres 1 .

For the term in (26), we have that 

∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(Π Sres 

− ˆΠSres 

)δres  

> t

∥( ˜Σres 

> t−1)−1

≤

s X 

> τ∈[t−1]

∥( ˜Σres 

> t−1

)− 12 ( ˆU res )⊤φ(Xτ , a τ )∥22

s X 

> τ∈[t−1]

(φ(Xτ , a τ )⊤(Π Sres − ˆΠSres )δres  

> t

)2

≤√pres 

r

t max   

> τ∈[t−1]

∥φ(Xτ , a τ )∥22∥δres  

> t

∥22∥ΠSres − ˆΠSres ∥2op 

27 ≤LM ∆Π √pres t =: ˆ ρres 2 . (29) The first inequality follows from Cauchy-Schwarz inequality. The second inequality uses the fact that 

X 

> τ∈[t−1]

∥( ˜Σres 

> t−1

)− 12 ( ˆU res )⊤φ(Xτ , a τ )∥22

= X 

> τ∈[t−1]

φ(Xτ , a τ )⊤ ˆU res ( ˜Σres 

> t−1

)−1( ˆU res )⊤φ(Xτ , a τ )= trace 

 X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤ ˆU res ( ˜Σres 

> t−1

)−1



≤ trace 

 X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤ ˆU res + λI pres 

 ( ˜Σres 

> t−1

)−1



= trace( Ipres )=pres .

Under Assumption 3, for all η ∈ (0 , 1) , with probability at least 1 − η, (29) is O

q pres t log( p/η )

> T0



.To bound the term in (27) we use the result in Lemma 2 on the estimation error for the invariant component, since 

∥ X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(βinv − ˆβinv )∥( ˜Σres 

> t−1)−1

=∥( ˜Σres 

> t−1

)− 12

X 

> τ∈[t−1]

( ˆU res )⊤φ(Xτ , a τ )φ(Xτ , a τ )⊤(βinv − ˆβinv )∥2

≤

s X 

> τ∈[t−1]

∥( ˜Σres 

> t−1

)− 12 ( ˆU res )⊤φ(Xτ , a τ )∥22

s X 

> τ∈[t−1]

(φ(Xτ , a τ )⊤(βinv − ˆβinv )) 2

≤√pres 

r

t max   

> τ∈[t−1]

∥φ(Xτ , a τ )∥22∥βinv − ˆβinv ∥22

≤L∥βinv − ˆβinv ∥2

√pres t =: ˆ ρres 3 .

Taking the union bound, we have that, with probability at least 1 − 2η,

∥ˆδres  

> t

− ˆΠSres 

δres  

> t

∥ˆΣt−1 ≤

> 3

X

> i=1

ˆρres  

> i

=: ˆ ρres  

> t

(η, L, M ). (30) To conclude the proof of the lemma it is sufficient to replace η with η′ := η/ 2 in all above terms. 

Proof of Theorem 3. We start by considering a decomposition of the instantaneous regret similar to the one in (8) . Now, the true subspace decomposition is unknown, so ˆCβ and ˆCδt are confidence sets containing, with probability at least 1 − η, ˆΠSinv 

βinv and ˆΠSres 

δres  

> t

, respectively. As before, we denote by ( ¯βt, ¯δt) = arg max β∈ ˆCβ ,δ ∈ ˆCδt

φ(Xt, a t)⊤(β + δ), and by ¯γt = ¯βt + ¯δt the parameter at which the upper confidence bound for the predicted reward at time t is achieved. Moreover, 28 recall that ˜Σres  

> t

= ( ˆU res )⊤ ˆΣt ˆU res and ˜Σinv [−T0] = ( ˆU inv )⊤ ˆΣ[−T0] ˆU inv . Then, at time t ∈ [T ], reg t is such that 

reg t := ( φ(Xt, a ∗ 

> t

) − φ(Xt, a t)) ⊤γ0,t 

≤ φ(Xt, a t)⊤(¯ γt − γ0,t )= φ(Xt, a t)⊤( ¯βt − βinv ) + φ(Xt, a t)⊤(¯δt − δres  

> t

)= φ(Xt, a t)⊤( ¯βt − ˆΠSinv 

βinv + ˆΠSinv 

βinv − βinv )+ φ(Xt, a t)⊤(¯δt − ˆΠSres 

δres  

> t

+ ˆΠSres 

δres  

> t

− δres  

> t

)

≤ ∥ ( ˆU inv )⊤φ(Xt, a t)∥2∥( ˆU inv )⊤ ¯βt − ( ˆU inv )⊤βinv ∥2

+ ∥( ˆU res )⊤φ(Xt, a t)∥(( ˆU res )⊤ ˆΣt−1 ˆU res )−1 ∥( ˆU res )⊤ ¯δt − ( ˆU res )⊤δres  

> t

∥( ˆU res )⊤ ˆΣt−1 ˆU res 

+ ∥φ(Xt, a t)∥2∥γ0,t ∥2∆Π 

≤ ∥ ( ˆU inv )⊤φ(Xt, a t)∥2 1√λ0T0

∥( ˆU inv )⊤ ¯βt − ( ˆU inv )⊤βinv ∥˜Σinv [−T0]

+ ∥( ˆU res )⊤φ(Xt, a t)∥( ˜Σres  

> t−1)−1

∥( ˆU res )⊤ ¯δt − ( ˆU res )⊤δres  

> t

∥˜Σres 

> t−1

+ ∥φ(Xt, a t)∥2∥γ0,t ∥2∆Π 

≤ 2∥( ˆU inv )⊤φ(Xt, a t)∥2

r   

> ˆρinv
> T0(η,L,M )
> λ0T0

+ 2 ∥( ˆU res )⊤φ(Xt, a t)∥( ˜Σres 

> t−1)−1

pˆρres  

> t

(η, L, M )+ ∥φ(Xt, a t)∥2∥γ0,t ∥2∆Π .

Lemma 2 implies that we can define ˆCβ to contain ˆΠSinv 

βinv with probability at least 1 − η so that 

q

ˆρinv  

> T0

(η, L, M ) is 

O

r

pinv log 

 1 

> pinv λ0



+ log( 1 

> η

)

!

+ O

q 

pinv log( pη )



+ O

q 1 

> λ0

log( pη )



.

Lemma 3 implies instead that we can define ˆCδt to contain ˆΠSres 

δres  

> t

so that pˆρres  

> t

(η, L, M ) is, for all η ∈ (0 , 12 ) with probability at least 1 − 2ηO

q 

pres log( tpres ) + log( 1 

> η

)



+ O

q pres tT0

q

log( pη ) + 

r  

> ˆρinv
> T0(η,L,M )
> λ0

!! 

. (31) Moreover, by Assumption 3, we have that, for all η ∈ (0 , 1) , with probability at least 1 − η,

∥φ(Xt, a t)∥2∆Π ∥γ0,t ∥2 ≤ LM ∆Π 

is O(plog( p/η )/T 0). Taking now the sum over the time horizon T we obtain that 

Reg T =

> T

X

> t=1

reg t

≤

> T

X

> t=1

2∥( ˆU inv )⊤φ(Xt, a t)∥2

r   

> ˆρinv
> T0(η,L,M )
> λ0T0

+ ∥φ(Xt, a t)∥2∥γ0,t ∥2∆Π 

!

+

> T

X

> t=1

2∥( ˆU res )⊤φ(Xt, a t)∥( ˜Σres 

> t−1)−1

pρres 

> t

29 We consider these two sums separately. For the first, we obtain that 

> T

X

> t=1

2∥( ˆU inv )⊤φ(Xt, a t)∥2

r   

> ˆρinv
> T0(η,L,M )
> λ0T0

+ ∥φ(Xt, a t)∥2∥γ0,t ∥2∆Π 

!

≤T 2L

r   

> ˆρinv
> T0(η,L,M )
> λ0T0

+ LM ∆Π 

!

.

From the results above, we obtain that this term is, for all η ∈ (0 , 15 ), with probability at least 

1 − 5η,

O

 T√T0λ0

q

pinv + log( 1 

> η

)



+ O

 T√T0λ0

q

pinv log( pη )



+ O

 T√T0λ0

q

max {λ0, 1 

> λ0

} log( pη )



.

For the remaining part of the cumulative regret, we have that 

> T

X

> t=1

2∥( ˆU res )⊤φ(Xt, a t)∥( ˜Σres 

> t−1)−1

pˆρres  

> t

(η, L, M ) ≤ 2

vuutT ˆρres  

> T

(η, L, M )

> T

X

> t=1

∥( ˆU res )⊤φ(Xt, a t)∥2( ˜Σres 

> t−1)−1

As in the oracle case (see the proof of Lemma 3), we can use (14) and Lemma 4 and obtain 

> T

X

> t=1

2∥( ˆU res )⊤φ(Xt, a t)∥( ˜Σres 

> t−1)−1

pˆρres  

> t

(η, L, M ) ≤ Cres 

r

ˆρres  

> T

(η, L, M )T p res log 



1 + T L 2

> λp res



.

Using (31), we have that the above term is, for all η ∈ (0 , 12 ), with probability at least 1 − 2η,

O pres 

r

T log 



1 + T L 2

> λp res

  

log( Tpres ) + log( 1 

> η

)

!

+O pres T√T0

r

log 



1 + T L 2

> λp res

 q

log( pη ) + 

r  

> ˆρinv
> T0(η,L,M )
> λ0

!! 

.

As a result, up to logarithmic terms, we obtain that the cumulative regret for the ISD-linUCB algorithm is, for all η ∈ (0 , 111 ), with probability at least 1 − 11 η

˜O

√T



pres + pres 

q Tλ0T0

p pinv +

q

max {λ0, 1 

> λ0

}

 

.

# D Lower bound 

D.1 Lower bound for LinUCB 

Lattimore and Szepesvári (2020) show the lower bound Ω( p√T ) for the LinUCB algorithm. In the linear model, they consider Gaussian noise with unit variance. Then, for a context-action space X × A and a linear parameter γ ∈ Rp, the regret of a policy is 

Reg T (X × A , γ ) = T max  

> φ∈X ×A

φ⊤γ − Eγ

" TX

> t=1

Rat

> t

#

30 where the expectation is taken with respect to the measure on the rewards induced by the interaction of the policy with the bandit parametrized by γ. The idea is to find a worst case instance of the bandit problem, which is characterized by the context-action space and by the parameter γ, with cumulative regret at least of the order of p√T . Lattimore and Szepesvári (2020) show the result for two different choices of the context-action space. The first is the hypercube, where X ×A = [ −1, 1] p. The second choice is the unit sphere, namely X ×A = {x ∈ Rp | ∥ x∥2 ≤ 1}.In the first case, the proof is simpler because the value of the context-action feature in one direction is not constrained by the values in the remaining directions: we focus on this proof for now. More in detail, the proof for X × A = [ −1, 1] p shows that there exists a parameter γ ∈ Γ :=

n

± 1√T

op

such that Reg T (X × A , γ ) is Ω( p√T ). The proof starts by considering the relative entropy (or KL-divergence) between the probability measures Pγ , Pγ′ on the rewards, induced by the interaction of a fixed policy, i.e., a fixed sequence of context-action features {φ(Xt, a t)}Tt=1 , with two bandits parametrized by γ, γ ′ ∈ Γ respectively (with noise ϵt ∼ N (0 , 1) ), that is, 

D(Pγ , Pγ′ ) = 12

> T

X

> t=1

Eγ [( φ(Xt, a t)⊤(γ − γ′)) 2].

Denoting with the subscript i the i-th component of a vector, they then define for i ∈ [p] and 

γ ∈ Γ

qγi := Pγ

> T

X

> t=1

1{sign( φi(Xt, a t)̸ = sign( γi)} ≥ T

2

!

,

that is, the probability that the number of times that the sign of the i-th component of the selected context-action feature is not optimal exceeds half of the total learning horizon. For a fixed i ∈ [p] and γ ∈ Γ, they further fix γ′ as the parameter that equals γ in all its components except for the i-th, which is set to γ′ 

> i

= −γi. Then, it holds by a property of the relative entropy (Bretangolle-Huber inequality) that 

qγi + qγ′ 

> i

≥ 12 exp ( −D(Pγ , Pγ′ )) = 12 exp − 12

> T

X

> t=1

Eγ [( φ(Xt, a t)⊤(γ − γ′)) 2]

!

≥ 12 exp( −2) ,

where the last inequality follows from the definition of the spaces A × X and Γ. Averaging over all possible parameters γ ∈ Γ, this implies that 

1

|Γ|

X

> γ∈Γ

X

> i∈[p]

qγi = 1

|Γ|

X

> i∈[p]

X

> γ∈Γ

qγi ≥ 1

|Γ|

X

> i∈[p]

|Γ|

212 exp ( −2) = p

4 exp( −2) .

Finally, this implies that there exists at least one parameter γ ∈ Γ such that P 

> i∈[p]

qγi ≥ p 

> 4

exp (−2) .For this choice of γ, the regret is 

Reg T (X × A , γ ) = Eγ

" TX

> t=1
> p

X

> i=1

(sign( γi) − φi(Xt, a t)) γi

#

≥ 1

√T

> p

X

> i=1

Eγ

" TX

> t=1

1{sign( φi(Xt, a t)) ̸ = sign( γi)}

#

≥√T

2

> p

X

> i=1

Pγ

> T

X

> t=1

1{sign( φi(Xt, a t)) ̸ = sign( γi)} ≥ T

2

!

31 =

√T

2

> p

X

> i=1

qγi

≥ exp −28 p√T . 

The first equality follows from the fact that max φ∈[−1,1] p φ⊤θ = sign (θ)⊤θ. The first inequality follows from the definition of the space Γ and a case-based analysis on the value of the summation terms. The second inequality uses Markov’s inequality. The last equality follows from the choice of γ. This concludes the proof for the lower bound. 

D.2 Lower bound for ISD with oracle subspaces and oracle invariant component 

Consider now the case in which we have oracle access to the invariant subspace decomposition 

(Sinv , Sres ) and the invariant parameter βinv . We can modify the proof for the standard linear case above by considering a parameter space where the parameters only differ in their residual components, that is, their projection onto Sres , while the invariant component remains fixed. Under this additional constraint, the effective dimension of Γ is reduced from p to pres , leading to the lower bound Ω( pres √T ).More in detail, we consider again the context-action space X × A = [ −1, 1] p. We now define the parameter space as Γ :=



γ ∈ Rp | γ = βinv + δres , (U res )⊤δres ∈

n

± 1√T

opres 

. Then, we have that for two parameters γ, γ ′ ∈ Γ,

D(Pγ , Pγ′ ) = 12

> T

X

> t=1

Eγ [( φ(Xt, a t)⊤(δres − δ′ res )) 2].

For all t ∈ [T ] let ˜φres (Xt, a t) := ( U res )⊤φ(Xt, a t) and ˜δres := ( U res )⊤δres . As above, we can define for i ∈ [pres ] and γ ∈ Γ

qγi := Pγ

> T

X

> t=1

1{sign( ˜ φres  

> i

(Xt, a t)) ̸ = sign( ˜δres  

> i

)} ≥ T

2

!

.

For a fixed i ∈ [pres ] and γ ∈ Γ, define γ′ ∈ Γ as the parameter obtained from γ by setting 

˜δ′ res  

> i

= −˜δres  

> i

(and otherwise equal to γ). Then, 

qγi + qγ′ 

> i

≥ 12 exp − 12

> T

X

> t=1

Eγ [( φ(Xt, a t)⊤(δres − δ′ res )) 2]

!

≥ 12 exp ( −2) 

which again follows from the definition of the spaces X × A and Γ. Taking the average over all possible parameters γ ∈ Γ, we obtain 

1

|Γ|

X

> γ∈Γ

X

> i∈[pres ]

qγi ≥ 1

|Γ|

X

> i∈[pres ]

|Γ|

212 exp ( −2) = pres exp ( −2) 4 ,

meaning that there exists γ ∈ Γ such that P 

> i∈[pres ]

qγi ≥ pres exp ( −2) 4 . The last part of the proof follows exactly the same steps as the one in the standard case, leading this time to a regret that is Ω( pres √T ).32