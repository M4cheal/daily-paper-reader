---
title: Tiny Inference-Time Scaling with Latent Verifiers
title_zh: 基于潜空间验证器的极小推理时缩放
authors: "Davide Bucciarelli, Evelyn Turri, Lorenzo Baraldi, Marcella Cornia, Lorenzo Baraldi, Rita Cucchiara"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22492v2"
tags: ["query:tta"]
score: 8.0
evidence: 多模态模型的推理时缩放
tldr: 本研究针对扩散模型推理阶段验证器计算开销大的问题，提出了VHS（Verifier on Hidden States）。传统方法需将候选图解码至像素空间再由多模态大模型验证，而VHS直接作用于Diffusion Transformer的中间隐藏层特征。该方法在极低推理预算下，显著降低了计算延迟、FLOPs和显存占用，同时在GenEval基准上取得了性能提升，实现了高效的推理侧扩展。
selection_source: fresh_fetch
motivation: 现有的多模态大模型验证器需要频繁进行像素空间编解码，导致推理侧扩展的计算成本过高且过程冗余。
method: 提出VHS验证器，直接利用Diffusion Transformer单步生成器的中间隐藏表示进行评分，无需解码至像素空间。
result: "相比标准MLLM验证器，VHS在相同预算下将总耗时缩短63.3%，计算量减少51%，显存占用降低14.5%，且GenEval得分提升2.7%。"
conclusion: 在潜空间直接进行验证是实现高效推理侧扩展的有效途径，能在大幅降低资源消耗的同时提升生成质量。
---

## 摘要
推理时缩放已成为在测试阶段通过使用验证器对候选输出进行评分和筛选来提升生成模型性能的一种有效方法。一种常见的选择是采用多模态大语言模型（MLLM）作为验证器，这虽然能提升性能，但会引入巨大的推理时间开销。事实上，扩散流水线在自动编码器的潜空间中运行以减少计算量，然而 MLLM 验证器仍需将候选样本解码到像素空间，并将其重新编码到视觉嵌入空间，从而导致了冗余且昂贵的操作。在这项工作中，我们提出了“隐藏状态验证器”（VHS），这是一种直接在扩散 Transformer（DiT）单步生成器的中间隐藏表示上运行的验证器。VHS 在不解码到像素空间的情况下分析生成器特征，从而降低了每个候选样本的验证成本，同时在性能上优于或等同于基于 MLLM 的竞争方案。我们表明，在每个提示词仅有少量候选样本的极小推理预算下，VHS 实现了更高效的推理时缩放。与标准 MLLM 验证器相比，它将生成与验证的总时间缩短了 63.3%，计算量（FLOPs）减少了 51%，显存（VRAM）占用降低了 14.5%，并在相同的推理时间预算下，在 GenEval 基准测试上实现了 +2.7% 的提升。

## Abstract
Inference-time scaling has emerged as an effective way to improve generative models at test time by using a verifier to score and select candidate outputs. A common choice is to employ Multimodal Large Language Models (MLLMs) as verifiers, which can improve performance but introduce substantial inference-time cost. Indeed, diffusion pipelines operate in an autoencoder latent space to reduce computation, yet MLLM verifiers still require decoding candidates to pixel space and re-encoding them into the visual embedding space, leading to redundant and costly operations. In this work, we propose Verifier on Hidden States (VHS), a verifier that operates directly on intermediate hidden representations of Diffusion Transformer (DiT) single-step generators. VHS analyzes generator features without decoding to pixel space, thereby reducing the per-candidate verification cost while improving or matching the performance of MLLM-based competitors. We show that, under tiny inference budgets with only a small number of candidates per prompt, VHS enables more efficient inference-time scaling reducing joint generation-and-verification time by 63.3%, compute FLOPs by 51% and VRAM usage by 14.5% with respect to a standard MLLM verifier, achieving a +2.7% improvement on GenEval at the same inference-time budget.

---

## 论文详细总结（自动生成）

以下是对论文《Tiny Inference-Time Scaling with Latent Verifiers》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：推理时缩放（Inference-time scaling，如 Best-of-N 采样）能显著提升生成模型的质量，但现有的验证器（通常是多模态大模型 MLLM）计算开销巨大。
*   **核心矛盾**：当前的扩散模型（Diffusion Models）多在潜空间（Latent Space）运行，而 MLLM 验证器需要先将潜变量解码为像素图像，再通过视觉编码器（如 CLIP）重新编码。这种“解码-再编码”的过程在单步生成器（Single-step generators）中产生了严重的计算冗余和延迟。
*   **研究目标**：开发一种极轻量级的验证器，直接利用生成器的内部特征进行评分，从而在极小的计算预算下实现高效的推理侧扩展。

### 2. 方法论
*   **核心思想**：提出 **VHS（Verifier on Hidden States）**。它直接将 Diffusion Transformer (DiT) 的中间隐藏层表示（Hidden States）与大语言模型（LLM）的嵌入空间对齐，跳过像素解码和外部视觉编码步骤。
*   **关键技术细节**：
    *   **特征提取**：从 DiT 的第 $\ell^*$ 层提取特征 $h_{\ell^*}$，并进行归一化处理。
    *   **多模态连接器**：使用轻量级的两层 MLP（GELU 激活）将 DiT 特征投影到 LLM 的输入空间。
    *   **训练阶段一（对齐）**：利用 LLaVA 风格的数据集，通过生成图像并重新标注（使用 Gemma-3-4B）来训练连接器，使 LLM 理解 DiT 的潜空间特征。
    *   **训练阶段二（验证器微调）**：在特定任务（如 GenEval）上进行微调。使用加权交叉熵损失（Weighted Cross-Entropy）解决正负样本不平衡问题。
    *   **评分机制**：通过 LLM 输出“Yes/No”标记的概率值来构建连续评分，用于 Best-of-N 筛选。

### 3. 实验设计
*   **数据集与基准**：主要使用 **GenEval** 基准（涵盖单对象、双对象、计数、颜色、位置、属性绑定六大维度）。
*   **生成器模型**：主要基于 **SANA-Sprint**（单步 DiT 模型），并在 **PixArt-α-DMD** 上验证了泛化性。
*   **对比方法**：
    *   **MLLM w/ CLIP**：标准验证器（解码后用 CLIP 编码）。
    *   **MLLM w/ AE**：直接使用自动编码器（AE）的潜变量 $z_0$ 作为输入。
    *   **Multi-step Baselines**：对比了增加扩散步数（如 SANA-1.5）的性能。

### 4. 资源与算力
*   **硬件环境**：使用 NVIDIA **A100** GPU。
*   **训练开销**：
    *   **对齐阶段**：16 张 A100，训练 1 个 epoch，Batch Size 为 512。
    *   **微调阶段**：8 张 A100，训练 10 个 epoch，Batch Size 为 64。
*   **推理效率**：在单张 A100 上，VHS 相比标准验证器减少了 51% 的 FLOPs 和 14.5% 的显存占用。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的详尽测试，包括不同时间预算（550ms, 1100ms, 1650ms）下的性能对比。
*   **消融实验**：
    *   测试了 DiT 不同层（h1 到 h19）的影响，发现中间层（如 h7）语义最强。
    *   对比了不同损失函数（XE, Weighted XE, Focal Loss）。
    *   对比了不同 LLM 主干（Qwen2.5-0.5B vs 1.5B）。
*   **客观性**：实验采用了“等效时间预算”原则，即在相同的墙钟时间（Wall-clock time）内对比不同方法的表现，这比单纯对比样本数更符合实际应用场景。

### 6. 主要结论与发现
*   **效率飞跃**：VHS 将生成加验证的总时间缩短了 **63.3%**。
*   **性能提升**：在相同的时间预算下，由于 VHS 验证速度极快，可以筛选更多的候选样本（例如在 1100ms 内，标准方法只能做 Best-of-4，而 VHS 能做 Best-of-9），从而在 GenEval 上取得 **+2.7%** 的准确率提升。
*   **层级特性**：DiT 的中间层比靠近输出的层具有更好的语义对齐能力，而靠近输出的层更多包含感知/重建信息。

### 7. 优点
*   **极高效率**：彻底消除了推理侧扩展中最耗时的编解码瓶颈。
*   **端到端优化**：通过截断 DiT 的后续层（在验证阶段），进一步节省了生成开销。
*   **实用性强**：特别适合商业化图像生成服务中“低延迟、少样本”的部署需求。

### 8. 不足与局限
*   **架构耦合**：验证器与特定的生成器架构（如 DiT）强绑定，如果更换生成器主干，通常需要重新训练连接器和微调。
*   **泛化风险**：虽然在 PixArt 上做了验证，但对于非 Transformer 架构（如 U-Net）的适用性尚未探讨。
*   **训练依赖**：需要高质量的合成数据和强大的教师模型（如 Gemma-3）进行标注，训练流程相对复杂。

（完）
