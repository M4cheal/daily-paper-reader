---
title: Adapting Point Cloud Analysis via Multimodal Bayesian Distribution Learning
title_zh: 通过多模态贝叶斯分布学习适配点云分析
authors: "Xingyu Zhu, Liang Yi, Shuo Wang, Wenbo Zhu, Yonglinag Wu, Beier Zhu, Hanwang Zhang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22070v2"
tags: ["query:tta"]
score: 10.0
evidence: 多模态3D视觉语言模型的测试时间自适应
tldr: 针对多模态3D视觉语言模型在领域偏移下性能下降的问题，本文提出BayesMM框架。该方法通过贝叶斯分布学习，将文本先验和流式视觉特征建模为高斯分布，并利用贝叶斯模型平均进行自动融合。BayesMM无需训练即可实现持续的在线测试时自适应，有效解决了传统缓存机制信息丢失和融合不稳定的问题，在多个点云基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有的测试时自适应方法依赖有限的缓存机制，导致历史信息丢失且多模态融合策略过于主观，难以应对动态的领域偏移。
method: 提出BayesMM框架，将文本和视觉特征建模为高斯分布，并通过贝叶斯模型平均动态调整两者的贡献以实现统一预测。
result: "在多个点云基准测试中，BayesMM在处理分布偏移时表现出极强的鲁棒性，平均性能提升超过4%。"
conclusion: 该研究证明了贝叶斯分布学习在多模态3D点云分析自适应中的有效性，为无需训练的在线模型优化提供了新思路。
---

## 摘要
多模态3D视觉-语言模型在多种3D任务中表现出强大的泛化能力，但在领域偏移下其性能仍会显著下降。这激发了近期对测试时适配（TTA）的研究，该技术使模型能够利用测试时数据进行在线适配。在现有的TTA方法中，基于缓存的机制被广泛采用，用于在在线预测优化中利用先前观察到的样本。然而，它们仅存储有限的历史信息，导致随着测试流的演进，信息逐渐丢失。此外，它们的预测逻辑值（logits）采用启发式融合，使得适配过程不稳定。为了解决这些局限性，我们提出了 BayesMM，一种用于测试时点云分析的多模态贝叶斯分布学习框架。BayesMM 将每个类别的文本先验和流式视觉特征建模为高斯分布：文本参数源自语义提示，而视觉参数则随到达的样本进行在线更新。这两种模态通过贝叶斯模型平均进行融合，该方法根据后验证据自动调整它们的贡献，从而产生一个无需训练即可持续适配不断演进的测试时数据的统一预测。在多个点云基准测试上的广泛实验表明，BayesMM 在分布偏移下保持了鲁棒性，平均性能提升超过 4%。

## Abstract
Multimodal 3D vision-language models show strong generalization across diverse 3D tasks, but their performance still degrades notably under domain shifts. This has motivated recent studies on test-time adaptation (TTA), which enables models to adapt online using test-time data. Among existing TTA methods, cache-based mechanisms are widely adopted for leveraging previously observed samples in online prediction refinement. However, they store only limited historical information, leading to progressive information loss as the test stream evolves. In addition, their prediction logits are fused heuristically, making adaptation unstable. To address these limitations, we propose BayesMM, a Multimodal Bayesian Distribution Learning framework for test-time point cloud analysis. BayesMM models textual priors and streaming visual features of each class as Gaussian distributions: textual parameters are derived from semantic prompts, while visual parameters are updated online with arriving samples. The two modalities are fused via Bayesian model averaging, which automatically adjusts their contributions based on posterior evidence, yielding a unified prediction that adapts continually to evolving test-time data without training. Extensive experiments on multiple point cloud benchmarks demonstrate that BayesMM maintains robustness under distributional shifts, yielding over 4% average improvement.

---

## 论文详细总结（自动生成）

### 论文总结：通过多模态贝叶斯分布学习适配点云分析 (BayesMM)

#### 1. 核心问题与整体含义（研究动机和背景）
大型多模态 3D 视觉-语言模型（如 ULIP, Uni3D）虽然在零样本学习上表现出色，但在面对现实世界中的**领域偏移（Domain Shift）**（如传感器噪声、几何形变、环境变化）时，性能会显著下降。
现有的**测试时自适应（TTA）**方法主要依赖“缓存机制”（Cache-based），即存储高置信度的测试样本特征。然而，这类方法存在两个核心缺陷：
*   **信息丢失**：缓存容量有限，随着测试流的推进，旧的分布信息会被覆盖，导致“灾难性遗忘”。
*   **融合不稳定**：通常使用启发式的超参数来手动调节文本特征与缓存特征的融合比例，缺乏理论支撑且在不同场景下难以泛化。

#### 2. 方法论：核心思想与关键技术
论文提出了 **BayesMM** 框架，将 TTA 问题转化为一个**动态多模态贝叶斯分布学习**过程。其核心思想是不再存储离散样本，而是维护类别的概率分布。
*   **多模态高斯建模**：假设每个类别的特征在文本和几何空间中均服从高斯分布。
*   **文本分布学习（先验）**：利用大语言模型（LLM）为每个类别生成多个同义描述（Paraphrases），计算其嵌入向量的均值和协方差，从而捕捉语义的多样性，形成稳健的文本先验。
*   **几何分布学习（在线更新）**：当测试点云流式到达时，利用贝叶斯规则递归地更新每个类别的几何分布参数（均值 $\mu$ 和协方差 $\Sigma$）。这种方式能够整合长期统计信息，避免了缓存溢出导致的信息丢失。
*   **贝叶斯模型平均（融合）**：通过贝叶斯模型平均（Bayesian Model Averaging）自动计算文本和几何模态的后验证据（Evidence），并以此作为权重进行融合。这实现了**自动化的模态加权**，无需手动调节融合系数。

#### 3. 实验设计
*   **数据集与场景**：
    *   **鲁棒性评估**：ModelNet-C 和 ScanObjectNN-C（包含 7 种原子腐蚀类型，如噪声、旋转、缩放等）。
    *   **泛化性评估**：ModelNet40, ScanObjectNN (Hardest split), Objaverse-LVIS, OmniObject3D。
    *   **跨域评估**：Sim-to-Real（从模拟数据迁移到真实世界数据）。
*   **Benchmark 与对比方法**：
    *   **基座模型**：ULIP, ULIP-2, OpenShape, Uni3D。
    *   **对比方法**：原始 Zero-shot 模型、Point-Cache（Global 和 Hierarchical 变体）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练/推理时长**：该方法是 **Training-free（无需训练）** 的，因此没有训练耗时。
*   **推理效率**：在 Batch size 为 1 的设置下，BayesMM 保持了原始模型 97% 以上的推理速度（吞吐量仅轻微下降），证明了其在实时系统中的可行性。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **多维度对比**：在 4 个主流基座模型上验证了 4 种不同类型的基准数据集。
*   **消融实验**：分别验证了文本分布建模、几何分布建模以及贝叶斯权重的贡献。
*   **敏感性分析**：测试了超参数 $\alpha^2$ 和 $\beta^2$ 对结果的影响，证明了模型对参数变化不敏感。
*   **资源分析**：对比了内存占用和推理延迟，并利用 t-SNE 进行了特征分布的可视化。
实验设计客观、公平，涵盖了从合成到真实、从简单到复杂的多种 3D 场景，论证非常充分。

#### 6. 主要结论与发现
*   **性能提升显著**：BayesMM 在多个基准测试中平均提升了 **4% 以上** 的准确率，在某些严重腐蚀的场景下提升更明显。
*   **稳定性更强**：相比缓存方法，BayesMM 的 KL 散度和 MMD 指标更低，说明学习到的分布与真实分布更一致。
*   **内存友好**：由于采用了协方差共享结构，内存占用随类别增加的增长速度远低于缓存类方法，适合大规模类别识别（如 O-LVIS 的 1156 类）。
*   **模态偏好**：实验发现文本模态在分布偏移下通常比几何模态更稳定，贝叶斯权重能自动识别并赋予文本模态更高的权重。

#### 7. 优点（亮点）
*   **数学严谨性**：将启发式的 Logits 融合提升到了概率推理层面，解决了 TTA 中权重难调的问题。
*   **无需训练**：即插即用，不需要访问源域数据，也不需要反向传播更新网络参数。
*   **长效记忆**：通过递归更新分布参数，克服了固定大小缓存带来的信息丢失问题。

#### 8. 不足与局限
*   **高斯假设限制**：模型假设特征分布为单模态高斯分布，对于某些分布极其复杂的类别（多峰分布），建模可能不够精确。
*   **超参数依赖**：虽然对 $\alpha, \beta$ 不敏感，但这些先验方差仍需预设。
*   **任务覆盖**：目前主要集中在分类任务，尚未在 3D 目标检测或分割等更复杂的下游任务中验证其有效性。

（完）
