---
title: Dual-Teacher Distillation with Subnetwork Rectification for Black-Box Domain Adaptation
title_zh: 面向黑盒领域自适应的带子网络纠正的双教师蒸馏
authors: "Zhe Zhang, Jing Li, Wanli Xue, Xu Cheng, Jianhua Zhang, Qinghua Hu, Shengyong Chen"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22908v2"
tags: ["query:tta"]
score: 8.0
evidence: 利用视觉语言模型进行黑盒领域自适应
tldr: 针对黑盒领域自适应中源数据和模型不可访问的挑战，本文提出DDSR模型。该模型通过双教师蒸馏机制，结合黑盒源模型和视觉语言模型（ViL）的互补知识生成可靠伪标签。引入子网络校正策略缓解噪声监督导致的过拟合，并通过迭代优化标签与提示词，结合原型自训练提升性能，在多个基准测试中超越了现有方法。
selection_source: fresh_fetch
motivation: 现有的黑盒领域自适应方法在利用视觉语言模型时存在噪声干扰和语义先验利用不足的问题。
method: 提出双教师蒸馏模型DDSR，结合源模型与ViL生成伪标签，并利用子网络正则化和原型自训练进行优化。
result: 在多个基准数据集上的实验表明，该方法在性能上一致优于现有的黑盒及部分白盒领域自适应方法。
conclusion: DDSR通过有效整合黑盒模型知识与视觉语言模型语义，为解决极度受限环境下的领域自适应问题提供了高效方案。
---

## 摘要
在假设源数据和源模型均不可访问的情况下，黑盒领域自适应代表了一种极具实用性但也极具挑战性的设定，因为可迁移的信息仅限于黑盒源模型的预测结果，且只能通过目标样本进行查询。现有方法尝试通过伪标签细化或利用外部视觉语言模型（ViLs）来提取可迁移知识，但它们往往受到噪声监督的影响，或对 ViLs 提供的语义先验利用不足，最终阻碍了自适应性能。为了克服这些局限性，我们提出了一种带子网络纠正的双教师蒸馏（DDSR）模型，该模型共同利用了嵌入在黑盒源模型中的特定知识和 ViL 的通用语义信息。DDSR 自适应地整合它们的互补预测，为目标域生成可靠的伪标签，并引入了一种子网络驱动的正则化策略，以减轻由噪声监督引起的过拟合。此外，细化后的目标预测迭代地增强了伪标签和 ViL 提示词，从而实现了更准确且语义一致的自适应。最后，通过基于类原型的自训练进一步优化目标模型。在多个基准数据集上的广泛实验验证了我们方法的有效性，证明了其相对于现有最先进方法（包括那些使用源数据或模型的方法）的持续改进。

## Abstract
Assuming that neither source data nor the source model is accessible, black box domain adaptation represents a highly practical yet extremely challenging setting, as transferable information is restricted to the predictions of the black box source model, which can only be queried using target samples. Existing approaches attempt to extract transferable knowledge through pseudo label refinement or by leveraging external vision language models (ViLs), but they often suffer from noisy supervision or insufficient utilization of the semantic priors provided by ViLs, which ultimately hinder adaptation performance. To overcome these limitations, we propose a dual teacher distillation with subnetwork rectification (DDSR) model that jointly exploits the specific knowledge embedded in black box source models and the general semantic information of a ViL. DDSR adaptively integrates their complementary predictions to generate reliable pseudo labels for the target domain and introduces a subnetwork driven regularization strategy to mitigate overfitting caused by noisy supervision. Furthermore, the refined target predictions iteratively enhance both the pseudo labels and ViL prompts, enabling more accurate and semantically consistent adaptation. Finally, the target model is further optimized through self training with classwise prototypes. Extensive experiments on multiple benchmark datasets validate the effectiveness of our approach, demonstrating consistent improvements over state of the art methods, including those using source data or models.

---

## 论文详细总结（自动生成）

### 论文总结：面向黑盒领域自适应的带子网络纠正的双教师蒸馏 (DDSR)

#### 1. 核心问题与研究背景
*   **核心问题**：论文探讨了**黑盒领域自适应 (Black-Box Domain Adaptation, BBDA)**。在这种设定下，源域的数据和模型参数/架构均不可访问，开发者只能通过查询接口获取源模型对目标样本的预测结果。
*   **研究动机**：现有的 BBDA 方法主要依赖伪标签细化，但往往面临严重的预测噪声。虽然引入视觉语言模型（如 CLIP）可以提供语义先验，但如何有效整合黑盒模型的特定知识与 CLIP 的通用语义，并防止模型对噪声标签过拟合，仍是亟待解决的挑战。

#### 2. 方法论：DDSR 框架
DDSR 采用两阶段训练策略，核心思想是利用“双教师”引导和“子网络”正则化。

*   **第一阶段：双教师蒸馏与子网络纠正**
    *   **双教师蒸馏**：同时使用黑盒源模型和预训练的 CLIP 作为教师。
    *   **自适应预测融合**：根据预测熵（不确定性）和目标域样本量 ($n_t$) 动态调整两个教师的权重。当样本量充足时更信任 CLIP；样本量较小时则侧重源模型知识。
    *   **子网络纠正 (Subnetwork Rectification)**：从目标网络中提取部分参数（比例 $\gamma=0.84$）构建子网络。通过最小化主网络与子网络输出的 JS 散度，并增大梯度差异，强制模型学习互补特征，从而抑制对噪声伪标签的过拟合。
    *   **在线细化**：利用指数移动平均 (EMA) 更新伪标签，并同步微调 CLIP 的可学习提示词 (Prompts)，使其更贴合目标域。
*   **第二阶段：原型自训练**
    *   提取目标特征并计算类中心（原型），根据样本到原型的余弦距离重新分配标签，通过交叉熵损失进行最后的精调，增强特征的判别性。

#### 3. 实验设计
*   **数据集**：使用了三个主流领域自适应基准：
    *   **Office-31**：包含 3 个域，31 类，规模较小但领域偏移明显。
    *   **Office-Home**：包含 4 个域，65 类，跨域差异大。
    *   **VisDA-17**：大规模合成到真实的转换任务，包含 12 类，样本量超 20 万。
*   **对比方法**：
    *   **UDA (有源数据)**：如 MCD, FixBi, DAPL。
    *   **SFDA (有源模型)**：如 SHOT, AaD, TPDS。
    *   **BBDA (黑盒)**：如 DINE, SEAL, RAIN, AEM, BBC 等。
*   **评价指标**：目标域分类准确率 (Accuracy)。

#### 4. 资源与算力
*   **硬件**：论文明确提到实验在**单张 Nvidia RTX GPU** 上完成（具体型号未标明，但属于消费级或常规工作站级别）。
*   **软件/框架**：基于 PyTorch 实现。
*   **训练时长**：VisDA-17 训练 15+10 个 Epoch；Office 系列训练 25+10 个 Epoch。由于采用了 ResNet-50/101 作为骨干网络且仅需单卡，该方法具有较高的计算效率。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型到大型数据集的数十个迁移任务（如 Office-Home 包含 12 个子任务）。
*   **消融实验**：详细验证了双教师蒸馏 ($L_{dt}$)、子网络纠正 ($L_{sr}$)、原型自训练 ($L_{self}$) 等每个组件的贡献。
*   **参数敏感性**：对子网络比例 $\gamma$、融合阈值 $\tilde{n}_t$ 以及损失权重进行了详尽的敏感性分析。
*   **可视化**：通过 t-SNE 展现了特征聚类的演变过程。
*   **评价**：实验设计全面且客观，通过与 UDA 和 SFDA 方法的跨类别对比，证明了其在更严苛约束下的优越性。

#### 6. 主要结论与发现
*   **性能领先**：DDSR 在所有 BBDA 基准上均达到了 SOTA 性能，甚至在某些任务上超过了拥有源模型访问权限的 SFDA 方法。
*   **互补性**：实验证明 CLIP 的通用语义与源模型的任务特定知识具有极强的互补性，自适应融合比简单平均更有效。
*   **正则化威力**：子网络纠正策略能显著提升模型在噪声环境下的鲁棒性，防止模型陷入错误的伪标签陷阱。

#### 7. 优点与亮点
*   **创新的融合机制**：提出了一种基于样本规模和熵的“反直觉”融合策略，并给出了合理的经验解释。
*   **高效的正则化**：子网络纠正不需要额外的辅助网络（如判别器），仅通过内部参数共享实现，计算开销低。
*   **隐私保护**：严格遵循黑盒设定，不泄露源域任何结构或数据信息，符合现代 AI 安全趋势。

#### 8. 不足与局限
*   **类别偏移处理**：论文承认目前尚未显式处理源域与目标域之间可能存在的**类别不匹配**（如开集或部分域自适应问题）。
*   **超参数依赖**：虽然敏感性分析显示模型较稳健，但融合阈值 $\tilde{n}_t$ 和子网络比例 $\gamma$ 仍需根据经验预设，在完全未知的领域可能需要更多调试。
*   **黑盒查询成本**：虽然不访问模型，但仍需对目标域所有样本进行一次或多次黑盒查询，在 API 调用受限或计费昂贵的场景下仍有成本压力。

（完）
