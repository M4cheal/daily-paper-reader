---
title: Stability-Preserving Online Adaptation of Neural Closed-loop Maps
authors: "Danilo Saccani, Luca Furieri, Giancarlo Ferrari-Trecate"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22469v1"
tags: ["query:tta"]
score: 8.0
evidence: 神经网络控制器的稳定性保持在线自适应机制
tldr: 针对现代控制任务中在线适应与闭环稳定性难以兼得的问题，本文提出了一种针对非线性神经网络控制器的稳定性保持更新机制。研究将控制器建模为具有有界 lp 增益的因果算子，并推导出基于增益的在线更新条件。通过时间调度和状态触发两种方案，确保在多次更新后系统仍保持 lp 稳定性。该方法实现了稳定性与最优性的解耦，在处理时变目标和扰动时，相比静态和朴素在线基准表现出更优且稳定的性能。
selection_source: fresh_fetch
motivation: 现有的非线性系统神经网络控制器缺乏在运行中更新的原则性方法，且直接切换策略可能导致闭环系统失稳。
method: 将控制器建模为有界 lp 增益的算子，并提出时间调度和状态触发两种基于增益条件的在线更新方案。
result: 实验表明，该方法在处理时变目标和扰动的非线性系统中，在保证稳定性的前提下，性能显著优于静态和朴素在线更新基准。
conclusion: 本文成功实现了神经网络控制器的在线安全更新，证明了稳定性可与控制器最优性解耦，为复杂控制任务提供了可靠的自适应框架。
---

## Abstract
The growing complexity of modern control tasks calls for controllers that can react online as objectives and disturbances change, while preserving closed-loop stability. Recent approaches for improving the performance of nonlinear systems while preserving closed-loop stability rely on time-invariant recurrent neural-network controllers, but offer no principled way to update the controller during operation. Most importantly, switching from one stabilizing policy to another can itself destabilize the closed-loop. We address this problem by introducing a stability-preserving update mechanism for nonlinear, neural-network-based controllers. Each controller is modeled as a causal operator with bounded $\ell_p$-gain, and we derive gain-based conditions under which the controller may be updated online. These conditions yield two practical update schemes, time-scheduled and state-triggered, that guarantee the closed-loop remains $\ell_p$-stable after any number of updates. Our analysis further shows that stability is decoupled from controller optimality, allowing approximate or early-stopped controller synthesis. We demonstrate the approach on nonlinear systems with time-varying objectives and disturbances, and show consistent performance improvements over static and naive online baselines while guaranteeing stability.

---

## 论文详细总结（自动生成）

### 论文总结：Stability-Preserving Online Adaptation of Neural Closed-loop Maps

#### 1. 核心问题与整体含义（研究动机和背景）
本文针对现代控制任务中**在线自适应与闭环稳定性**难以兼得的问题展开研究。虽然神经网络（NN）能参数化高性能的反馈策略，但其稳定性证明极具挑战。现有的稳定非线性控制器（如基于系统级合成 SLS 或内部模型控制 IMC 的方法）通常是时不变的，即在离线训练后权重固定，无法应对运行时的目标变化或环境扰动。更关键的是，**切换系统理论**指出，即使两个控制器各自都能使系统稳定，在它们之间直接切换也可能导致闭环系统失稳。因此，论文的核心动机是提出一种有理论保证的、允许在运行中安全更新神经网络控制器的机制。

#### 2. 方法论：核心思想与关键技术
论文提出了一种基于**增益预算触发（Gain-budgeted Triggering）**的更新机制，核心思想是将控制器建模为具有有界 $\ell_p$ 增益的因果算子。

*   **核心架构**：采用神经网络 IMC 架构，通过一个 $\mathcal{L}_p$ 稳定算子 $M$ 来参数化所有稳定控制器。
*   **稳定性更新条件**：推导出了一个基于增益的判定准则。当在时间 $t_i$ 更新控制器时，需满足：
    $$\gamma(F) \cdot (\gamma(M^{(i)}) + 1) |x_{t_i}| \leq r(i)$$
    其中 $\gamma(F)$ 是受控对象的增益，$|x_{t_i}|$ 是当前状态范数，$r(i)$ 是预设的衰减预算序列。
*   **两种实现方案**：
    *   **时间调度方案（Algorithm 1）**：在固定时间间隔尝试更新。如果当前状态足够小，满足增益约束，则应用新控制器；否则保留旧控制器。
    *   **状态触发方案（Algorithm 2）**：预设控制器增益上限，当系统状态进入由增益条件确定的“安全区域”时，自动触发控制器更新。
*   **解耦特性**：该方法实现了**稳定性与最优性的解耦**。只要满足增益边界条件，即使在线优化未达到全局最优（如提前停止迭代），闭环稳定性依然得到保证。

#### 3. 实验设计
论文在两个非线性机器人仿真场景中验证了方法：
*   **场景一：大山问题（Mountains Problem）**：两个代理在狭窄山谷中协作导航，需避障并到达目标点。对比了离线训练的静态控制器。
*   **场景二：动态障碍物追踪（Dynamic Obstacles）**：代理在存在随机移动障碍物的环境中追踪圆形轨迹。
*   **Benchmark（基准方法）**：
    1.  **离线控制器**：基于先前研究 [10] 的静态神经网络控制器。
    2.  **收缩时界开环（RHO）规划器**：一种类似于 NMPC 的基准，在每个步长求解有限时界优化问题。
    3.  **朴素在线基准**：在扰动环境下对比性能提升。

#### 4. 资源与算力
*   **软件框架**：使用 PyTorch 实现，采用 Adam 优化器进行在线梯度下降。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号、数量或具体的训练时长。但提到在线更新采用了“预算化”的非精确求解（如每次更新仅进行 100 个 epoch 的梯度下降），以适应实时性要求。

#### 5. 实验数量与充分性
*   **实验规模**：每个场景均进行了 **50 次独立运行**，并以箱线图（Boxplots）形式展示了统计结果。
*   **充分性**：实验涵盖了名义场景（Nominal）和存在突发脉冲扰动的扰动场景（Perturbed）。对比了静态控制器和 RHO 规划器，证明了该方法在降低总成本和减小性能波动方面的优势。实验设计较为客观，考虑了随机扰动对结果的影响。

#### 6. 主要结论与发现
*   **性能提升**：在“大山问题”中，相比离线静态控制器，该方法在名义和扰动场景下分别降低了 **35.1% 和 40.3%** 的平均成本。
*   **鲁棒性**：在动态障碍物场景中，该方法比 RHO 规划器表现出更低的成本和更小的方差，证明了在线自适应闭环映射的可靠性。
*   **安全性**：实验证实了即使在频繁更新控制器的情况下，系统依然能严格保持 $\ell_p$ 稳定性，未出现失稳现象。

#### 7. 优点
*   **理论完备性**：解决了非线性神经网络控制器在线切换的稳定性难题，提供了严谨的数学证明。
*   **计算灵活性**：稳定性不依赖于优化的收敛程度，允许在算力受限时使用近似解或提前停止优化。
*   **自适应能力**：能够实时响应环境变化（如移动障碍物）和未见过的扰动。

#### 8. 不足与局限
*   **模型依赖**：稳定性保证依赖于对受控对象增益 $\gamma(F)$ 的准确估计，对于极其复杂或完全未知的系统，获取该上界可能存在困难或过于保守。
*   **状态观测要求**：触发机制依赖于对系统状态 $x_t$ 的实时精确测量，传感器噪声可能影响触发的准确性。
*   **计算开销**：虽然支持近似求解，但在线运行反向传播（BPTT）仍比纯推理型控制器消耗更多算力。

（完）
