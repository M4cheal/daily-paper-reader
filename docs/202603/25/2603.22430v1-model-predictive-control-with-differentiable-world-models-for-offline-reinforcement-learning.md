---
title: Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning
title_zh: 基于可微世界模型的离线强化学习模型预测控制
authors: "Rohan Deb, Stephen J. Wright, Arindam Banerjee"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22430v1"
tags: ["query:tta"]
score: 9.0
evidence: 强化学习中世界模型的推理时自适应框架
tldr: 本文针对离线强化学习在推理阶段策略固定的局限性，提出了一种结合可微世界模型（DWM）的模型预测控制（MPC）框架。该框架利用预训练策略和学习到的动力学模型，在推理时通过想象轨迹的端到端梯度计算来实时优化策略参数。在D4RL基准测试（MuJoCo和AntMaze）上的实验表明，这种推理时自适应机制能显著提升现有离线RL算法的性能。
selection_source: fresh_fetch
motivation: 现有的离线强化学习方法在推理时直接应用预训练策略，缺乏根据实时环境反馈进行在线优化和调整的能力。
method: 构建了一个可微世界模型流水线，通过MPC在推理阶段利用想象的轨迹进行端到端梯度下降，从而动态微调策略参数。
result: 在D4RL连续控制基准测试中，该方法在MuJoCo和AntMaze任务上均表现出优于强力离线RL基准的性能。
conclusion: 通过可微世界模型在推理时利用环境信息优化策略，是增强离线强化学习效果的一种高效且一致的手段。
---

## 摘要
离线强化学习（Offline RL）旨在从固定的离线数据集中学习最优策略，而无需与环境进行进一步交互。此类方法通常训练一个离线策略（或价值函数），并在推理阶段直接应用而不再进行改进。我们引入了一种受模型预测控制（MPC）启发的推理时自适应框架，该框架利用预训练策略以及学习到的状态转移和奖励的世界模型。虽然现有的世界模型和扩散规划方法在训练期间利用学习到的动力学生成想象轨迹，或在推理时采样候选规划，但它们并未利用推理时信息来实时优化策略参数。相比之下，我们设计了一个可微世界模型（DWM）流水线，能够通过想象回滚（imagined rollouts）进行端到端梯度计算，从而在推理时基于 MPC 进行策略优化。我们在 D4RL 连续控制基准测试（MuJoCo 运动任务和 AntMaze）上评估了我们的算法，结果表明，利用推理时信息来优化策略参数相比强大的离线强化学习基准模型取得了持续的性能提升。

## Abstract
Offline Reinforcement Learning (RL) aims to learn optimal policies from fixed offline datasets, without further interactions with the environment. Such methods train an offline policy (or value function), and apply it at inference time without further refinement. We introduce an inference time adaptation framework inspired by model predictive control (MPC) that utilizes a pretrained policy along with a learned world model of state transitions and rewards. While existing world model and diffusion-planning methods use learned dynamics to generate imagined trajectories during training, or to sample candidate plans at inference time, they do not use inference-time information to optimize the policy parameters on the fly. In contrast, our design is a Differentiable World Model (DWM) pipeline that enables endto-end gradient computation through imagined rollouts for policy optimization at inference time based on MPC. We evaluate our algorithm on D4RL continuous-control benchmarks (MuJoCo locomotion tasks and AntMaze), and show that exploiting inference-time information to optimize the policy parameters yields consistent gains over strong offline RL baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Model Predictive Control with Differentiable World Models for Offline Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的离线强化学习（Offline RL）方法在训练完成后，推理阶段通常直接应用固定的策略网络，无法根据推理时遇到的具体状态进行实时调整。
*   **研究背景**：在医疗、自动驾驶、机器人等领域，在线探索受限，只能依赖离线数据。虽然现有方法通过行为正则化或保守价值估计来缓解分布偏移，但它们忽略了推理时的计算潜力。
*   **整体含义**：本文提出了一种**推理时自适应（Inference-time Adaptation）**框架。其核心逻辑是：既然状态转移和奖励函数是局部的、与策略无关的，那么可以学习一个“世界模型”，并在推理时利用模型预测控制（MPC）的思想，通过在“想象”的轨迹中进行梯度优化，实时微调策略参数。

### 2. 方法论：DWM-MPC 框架
*   **核心思想**：构建一个**可微世界模型（DWM）**流水线，将扩散模型（Diffusion Model）作为状态转移采样器，结合奖励模型和终端价值函数，形成一个可微的计算图。
*   **关键技术细节**：
    *   **可微扩散采样器 ($f_\theta$)**：利用条件扩散模型建模 $P(s'|s, a)$。通过固定扩散过程中的高斯噪声，将反向去噪过程转化为一个确定的、可微的函数。
    *   **奖励与价值模型**：训练一个回归模型 $r_\xi$ 预测即时奖励，并利用预训练的离线 RL 算法（如 ReBRAC）的 Critic 函数 $Q_\phi$ 作为有限步回滚的终端价值估计。
    *   **推理时优化**：在每个真实环境步 $t$，从当前状态 $s_t$ 出发，利用当前策略 $\pi_\psi$ 和世界模型生成 $M$ 条长度为 $H$ 的想象轨迹。
    *   **梯度递归（Theorem 4.1）**：推导了通过扩散模型回滚进行反向传播的链式法则。利用想象轨迹的累积奖励和终端价值作为目标函数，对策略参数 $\psi$ 进行 $E$ 步梯度上升更新。
*   **算法流程**：观察状态 -> 想象回滚 -> 计算梯度 -> 更新策略参数 -> 执行动作 -> 进入下一状态。

### 3. 实验设计
*   **数据集**：使用了标准的 **D4RL** 连续控制基准测试。
    *   **MuJoCo 运动任务**：包含 HalfCheetah, Hopper, Walker2d 的 18 个子集（Random, Medium, Expert 等）。
    *   **AntMaze 任务**：包含 6 个更具挑战性的迷宫任务（UMaze, Medium, Large）。
*   **Benchmark 与对比方法**：
    *   **强力基准**：TD3+BC, IQL, CQL, SAC-RND, 以及作为预训练基础的 ReBRAC。
    *   **生成式/模型化方法**：Decision Transformer (DT), Trajectory Transformer (TT), MOPO, Diffuser 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及具体的训练/推理耗时。
*   **潜在开销**：由于该方法在推理的每一步都需要进行多次扩散模型的反向去噪和反向传播计算，其推理延迟（Inference Latency）显著高于传统的单次前向传播策略，这在实时性要求极高的场景下可能是一个挑战。

### 5. 实验数量与充分性
*   **实验规模**：共在 24 个不同的 D4RL 数据集上进行了测试，每个任务运行了 10 个不同的随机种子。
*   **充分性**：实验覆盖了从简单运动到复杂长程规划（AntMaze）的多种场景。
*   **客观性**：作者不仅对比了传统的离线 RL 算法，还专门对比了同样基于扩散模型的规划算法（如 Diffuser），证明了“推理时参数优化”比单纯的“推理时采样规划”更有效。

### 6. 主要结论与发现
*   **性能提升**：MPCwDWM 在 18 个 MuJoCo 任务中的 12 个上取得了改进，平均分（85.33）优于所有对比基准。
*   **复杂任务表现优异**：在 AntMaze 任务中提升显著（平均分从 77.62 升至 85.07），尤其在 Large 级别任务中，通过推理时优化获得了大幅度增益。
*   **自适应有效性**：实验证明，即使初始策略（ReBRAC）已经很强，推理时的梯度微调仍能进一步挖掘性能潜力。

### 7. 优点与亮点
*   **端到端可微性**：成功将复杂的扩散模型集成进端到端的梯度优化流程中，解决了扩散模型采样过程的求导问题。
*   **推理时智能**：借鉴了大语言模型（LLM）推理时计算（Test-time Compute）的思想，为强化学习提供了类似的性能扩展能力。
*   **通用性**：该框架可以插拔式地应用在任何预训练的离线策略和价值函数之上。

### 8. 不足与局限
*   **推理延迟**：由于涉及多步扩散采样和反向传播，推理速度较慢，论文未给出具体的延迟数据。
*   **模型依赖性**：性能高度依赖于世界模型（动力学和奖励）的准确性。如果世界模型在 OOD（分布外）区域预测错误，梯度优化可能会误导策略进入错误方向。
*   **算力细节缺失**：缺乏对训练和推理资源的量化描述，不便于评估其实际部署的成本。
*   **视野限制**：MPC 的视野 $H$ 是有限的，虽然有终端价值函数补偿，但对于极长程的信用分配仍可能存在偏差。

（完）
