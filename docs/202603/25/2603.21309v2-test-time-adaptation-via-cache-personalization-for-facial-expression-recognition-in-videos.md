---
title: Test-Time Adaptation via Cache Personalization for Facial Expression Recognition in Videos
title_zh: 视频面部表情识别中基于缓存个性化的测试时自适应
authors: "Masoumeh Sharafi, Muhammad Osama Zeeshan, Soufiane Belharbi, Alessandro Lameiras Koerich, Marco Pedersoli, Eric Granger"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21309v2"
tags: ["query:tta"]
score: 10.0
evidence: 针对视觉语言模型（VLM）提出了一种无梯度的测试时间自适应方法。
tldr: 针对视频面部表情识别中跨受试者分布偏移的问题，本文提出了一种名为 TTA-CaP 的测试时自适应方法。该方法基于视觉语言模型，通过引入包含源域原型、正向目标样本和负向目标样本的三重缓存机制，实现了无需梯度优化的低成本个性化适配。通过三门控机制控制缓存更新，有效缓解了伪标签噪声，在多个数据集上实现了优于现有 SOTA 方法的性能，且计算开销极低。
selection_source: fresh_fetch
motivation: 现有的测试时自适应方法通常依赖高开销的参数优化，难以应对视频表情识别中复杂的受试者差异和实时性需求。
method: 提出 TTA-CaP 方法，利用三重协调缓存（源、正向、负向）和三门控机制，在无需梯度更新的情况下实现视频级预测的个性化。
result: 在 BioVid、StressID 和 BAH 三个挑战性数据集上的实验表明，该方法在处理受试者和环境偏移方面优于现有 SOTA 技术。
conclusion: TTA-CaP 为视频面部表情识别提供了一种高效、稳健的个性化方案，特别适用于计算资源受限的实际部署场景。
---

## 摘要
视频面部表情识别（FER）需要模型个性化，以捕捉不同受试者之间的显著差异。视觉语言模型（VLM）通过图文对齐在下游任务中表现出强大的迁移能力，但在受试者间的分布偏移下，其性能仍可能下降。使用测试时自适应（TTA）方法进行模型个性化可以缓解这一挑战。然而，大多数最先进的 TTA 方法依赖于无监督参数优化，这带来了在许多实际应用中难以承受的计算开销。本文提出了基于缓存个性化的 TTA（TTA-CaP），这是一种基于缓存的 TTA 方法，能够为视频 FER 实现低成本（无梯度）的 VLM 个性化。先前的基于缓存的 TTA 方法仅依赖于存储测试样本的动态内存，这可能会因噪声伪标签而导致误差累积和漂移。TTA-CaP 利用了三个协调的缓存：存储源域原型的个性化源缓存、积累可靠的受试者特定样本的正向目标缓存，以及将低置信度案例存储为负样本以减少噪声伪标签影响的负向目标缓存。缓存的更新和替换由基于时间稳定性、置信度以及与个性化缓存一致性的三门控机制控制。最后，TTA-CaP 通过嵌入融合来优化预测，产生支持时间稳定的视频级预测的精细化表示。我们在 BioVid、StressID 和 BAH 三个具有挑战性的视频 FER 数据集上进行的实验表明，TTA-CaP 在受试者特定和环境偏移下能够优于最先进的 TTA 方法，同时保持较低的计算和内存开销，适用于实际部署。

## Abstract
Facial expression recognition (FER) in videos requires model personalization to capture the considerable variations across subjects. Vision-language models (VLMs) offer strong transfer to downstream tasks through image-text alignment, but their performance can still degrade under inter-subject distribution shifts. Personalizing models using test-time adaptation (TTA) methods can mitigate this challenge. However, most state-of-the-art TTA methods rely on unsupervised parameter optimization, introducing computational overhead that is impractical in many real-world applications. This paper introduces TTA through Cache Personalization (TTA-CaP), a cache-based TTA method that enables cost-effective (gradient-free) personalization of VLMs for video FER. Prior cache-based TTA methods rely solely on dynamic memories that store test samples, which can accumulate errors and drift due to noisy pseudo-labels. TTA-CaP leverages three coordinated caches: a personalized source cache that stores source-domain prototypes, a positive target cache that accumulates reliable subject-specific samples, and a negative target cache that stores low-confidence cases as negative samples to reduce the impact of noisy pseudo-labels. Cache updates and replacement are controlled by a tri-gate mechanism based on temporal stability, confidence, and consistency with the personalized cache. Finally, TTA-CaP refines predictions through fusion of embeddings, yielding refined representations that support temporally stable video-level predictions. Our experiments on three challenging video FER datasets, BioVid, StressID, and BAH, indicate that TTA-CaP can outperform state-of-the-art TTA methods under subject-specific and environmental shifts, while maintaining low computational and memory overhead for real-world deployment.

---

## 论文详细总结（自动生成）

以下是对论文《Test-Time Adaptation via Cache Personalization for Facial Expression Recognition in Videos》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：视频面部表情识别（FER）在实际应用中面临严重的**受试者差异（Inter-subject variations）**和**环境偏移**问题。虽然视觉语言模型（VLM，如 CLIP）具有强大的泛化能力，但在部署阶段（测试时）面对特定受试者的独特表情风格时，性能会显著下降。
*   **研究动机**：
    *   现有的测试时自适应（TTA）方法多依赖梯度优化（如 Prompt-tuning），计算开销大，难以在实时或资源受限设备上部署。
    *   现有的基于缓存（Cache-based）的无梯度 TTA 方法通常只依赖测试样本的动态内存，容易受到视频中噪声伪标签（如表情转换期的模糊）的影响，导致误差累积和模型漂移。

### 2. 方法论
TTA-CaP 提出了一种**无梯度、基于三重缓存协调**的个性化自适应框架：
*   **核心思想**：结合固定的“源域先验”和动态的“目标域经验”，通过嵌入空间（Embedding space）的融合来修正预测，而非更新模型参数。
*   **三重缓存机制**：
    1.  **个性化源缓存（Personalized Source Cache）**：离线从源域受试者中提取类原型。测试时，根据目标受试者的特征统计量，选取最相似的 $m$ 个源受试者原型作为稳定的语义锚点。
    2.  **正向目标缓存（Positive Target Cache）**：在线存储高置信度的目标受试者样本及其伪标签。
    3.  **负向目标缓存（Negative Target Cache）**：存储低置信度或模糊的样本作为负面参考，用以抵消噪声影响。
*   **三门控更新机制（Tri-gate Mechanism）**：
    *   **时间门控**：利用滑动窗口内的多数投票确保预测的短期稳定性。
    *   **熵门控**：通过预测分布的熵值衡量置信度，区分可靠样本、模糊样本和不可用样本。
    *   **原型门控**：检查当前样本预测是否与个性化源缓存中的原型一致，确保语义不偏离。
*   **嵌入级融合（Embedding-level Fusion）**：将当前帧特征与从三个缓存中检索到的特征进行加权融合（$z_{fuse} = z_{target} + z_{source} + z_{pos} - z_{neg}$），利用融合后的特征进行最终的相似度分类。

### 3. 实验设计
*   **数据集**：
    *   **BioVid**：热疼痛估计数据集（受控环境）。
    *   **StressID**：多模态压力识别数据集。
    *   **BAH**：行为矛盾/犹豫识别数据集（大规模、非受控环境）。
*   **Benchmark 与对比方法**：
    *   **零样本/微调基准**：CLIP-ViT-B/32、EmoCLIP、X-CLIP、Exp-CLIP。
    *   **SOTA TTA 方法**：包括基于 Prompt-tuning 的 TPT、PromptAlign，以及基于缓存的 TDA、DPE、ReTA、T3AL。
*   **评价指标**：加权平均召回率（WAR）和 F1 分数。

### 4. 资源与算力
*   **硬件设备**：单张 **NVIDIA A100 GPU (48 GB)**。
*   **效率表现**：
    *   **推理延迟**：每批次（Batch size=16）耗时约 **110ms**，远低于 TPT（771ms）和 PromptAlign（900ms）。
    *   **内存占用**：约 **904MB**，是所有对比 TTA 方法中最低的（其他方法多在 1700MB-3100MB 之间）。
    *   **结论**：该方法在提升精度的同时，显著降低了计算和内存开销。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同性质的数据集上进行了测试，每个数据集选取了 10 个目标受试者进行逐一评估（Subject-wise analysis）。
*   **消融实验**：
    *   验证了三门控机制中每个门控的贡献。
    *   对比了静态缓存与动态缓存的互补性。
    *   研究了源受试者选择数量 $m$ 对性能的影响。
    *   对比了嵌入级融合与 Logit 级融合的效果。
*   **充分性评价**：实验设计非常全面，涵盖了从宏观性能到微观受试者差异的对比，且包含了计算复杂度的量化分析，具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：TTA-CaP 在所有数据集上的 WAR 和 F1 分数均优于现有的 TTA 方法（例如在 BioVid 上 WAR 达到 81.0%）。
*   **稳定性**：引入负向缓存和原型门控能有效抑制视频中的伪标签噪声，防止模型在长视频序列中发生漂移。
*   **个性化价值**：通过相似源受试者的原型锚定，模型能够更快、更准地适应新受试者的表情特征。

### 7. 优点（亮点）
*   **低成本部署**：无需反向传播（Gradient-free），极大地降低了测试时的计算负担，适合移动端应用。
*   **鲁棒性强**：创新的负向缓存机制和三门控策略，专门针对视频数据的时序噪声进行了优化。
*   **即插即用**：作为一种外挂式缓存机制，不需要重新训练 CLIP 等大型骨干网络。

### 8. 不足与局限
*   **离线依赖**：构建个性化源缓存仍需要访问源域数据进行离线聚类，虽然测试时不需要源数据，但增加了前期准备工作的复杂度。
*   **超参数敏感性**：三门控机制涉及多个阈值（如熵阈值、时间窗口大小），在不同领域的数据集上可能需要精细调优。
*   **极端环境挑战**：在 BAH 这种非受控、高噪声的视频数据集中，虽然优于基准，但绝对准确率（F1 约 41%）仍有较大提升空间。

（完）
