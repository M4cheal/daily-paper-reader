Title: Stability-Preserving Online Adaptation of Neural Closed-loop Maps

URL Source: https://arxiv.org/pdf/2603.22469v1

Published Time: Wed, 25 Mar 2026 00:24:27 GMT

Number of Pages: 9

Markdown Content:
# Stability-Preserving Online Adaptation of Neural Closed-loop Maps 

Danilo Saccani, Luca Furieri, Giancarlo Ferrari-Trecate 

Abstract — The growing complexity of modern control tasks calls for controllers that can react online as objectives and disturbances change, while preserving closed-loop stability. Recent approaches for improving the performance of nonlinear systems while preserving closed-loop stability rely on time-invariant recurrent neural-network controllers, but offer no principled way to update the controller during operation. Most importantly, switching from one stabilizing policy to another can itself destabilize the closed-loop. We address this problem by introducing a stability-preserving update mechanism for nonlinear, neural-network-based controllers. Each controller is modeled as a causal operator with bounded ℓp-gain, and we derive gain-based conditions under which the controller may be updated online. These conditions yield two practical update schemes, time-scheduled and state-triggered, that guarantee the closed-loop remains ℓp-stable after any number of updates. Our analysis further shows that stability is decoupled from controller optimality, allowing approximate or early-stopped controller synthesis. We demonstrate the approach on nonlinear systems with time-varying objectives and disturbances, and show consistent performance improvements over static and naive online baselines while guaranteeing stability. 

I. I NTRODUCTION 

Modern control systems operate in increasingly dynamic settings, enabled by advances in sensing, onboard computa-tion, and model-based design. While closed-loop stability is a core requirement, alone it is not enough, as applications also demand good tracking, efficiency, and constraint handling under time-varying operating conditions. Nonlinear Optimal Control (NOC) provides a natural framework to improve performance by minimizing a task-dependent cost [1]. How-ever, enforcing stability while optimizing a general nonlinear cost is difficult, especially for complex and time-varying dynamics [2], [3]. Classical approaches such as dynamic programming and the maximum principle [1] face significant computational limitations, and existing stability guarantees typically require restrictive assumptions on the cost, which limits applicability in practice. Nonlinear Model Predictive Control (NMPC) offers an approximate solution to the NOC problem by implementing an implicit policy obtained from a finite-horizon problem solved at each time step in receding-horizon fashion [2].                  

> This work was supported as a part of NCCR Automation, a National Centre of Competence in Research, funded by the Swiss National Science Foundation (grant number 51NF40_225155), the Swiss National Science Foundation Ambizione (grant number PZ00P2_208951) and the NECON project (grant number 200021-219431). D. Saccani and G. Ferrari-Trecate are with the Institute of Mechanical Engineering, Ecole Polytechnique Fédérale de Lausanne (EPFL), CH-1015 Lausanne, Switzerland. (email: {danilo.saccani, giancarlo.ferraritrecate}@epfl.ch). L. Furieri is with the Department of Engineering Science, University of Oxford, United Kingdom (luca.furieri@eng.ox.ac.uk );

While NMPC provides a structured way to handle constraints and nonlinearities, it typically imposes strict conditions on the cost to guarantee stability, which makes it difficult to apply in scenarios with rapidly changing objectives [4]. Neural networks (NNs) can parametrize rich families of high-performance feedback policies [5], but certify-ing closed-loop stability remains a nontrivial task. Indeed, generic NN architectures do not lend themselves to the utilization of Lyapunov or small-gain conditions [6]. Recent works utilize System Level Synthesis (SLS) [7], [8], Internal Model Control (IMC) [9], [10], and Youla parametrizations [11]–[13] to improve nonlinear closed-loop performance while guaranteeing ℓp-stability. The key idea is to search over a space of stable Lp operators used to parametrize the controller. In practice, these operators are implemented as time-invariant neural parametrizations whose stability is certified for any choice of weights. The main limitation is that the controller is fixed after training: the weights are not updated online, so significant offline training is required to cover changing operating conditions. A key obstacle to online adaptation is that replacing one stabilizing policy with another may destabilize the transient closed loop, even when both policies are stabilizing in isolation, as known from switched-systems theory [14]–[16]. In this work, building on the neural IMC architecture of [10], we propose gain-budgeted triggering mechanisms, in both time-scheduled and state-triggered form, that certify when a newly optimized controller may safely replace the current one. The main contributions of this work are: (i) a gain-budgeted update condition, derived from finite-gain and small-gain arguments, that guarantees the closed-loop re-mains ℓp-stable under repeated online controller updates; (ii) two implementable triggering mechanisms, one time-based and one state-based, that enforce this condition while trading off computational effort against adaptivity; and (iii) evidence on two nonlinear benchmarks of consistent improvements over offline baselines and receding-horizon open-loop con-trollers. NOTATION 

We denote by N the set of non-negative integers and N≥a

the integers {a, a + 1 , . . . }. The set of all sequences v =(v0, v 1, v 2, . . . ), where vt ∈ Rn for all t ∈ N, is denoted as ℓn. Moreover, v belongs to ℓnp ⊂ ℓn with p ∈ [1 , ∞] if 

∥v∥p = ( P∞ 

> t=0

|vt|p) 1 

> p

< ∞, where |·| denotes an arbitrarily chosen vector norm. We say that v ∈ ℓn 

> ∞

if sup t ∥vt∥ <

∞. We refer to v0: T to denote the truncation of v with t

ranging from 0 to T . An operator A : ℓn → ℓm is said to be 

> arXiv:2603.22469v1 [eess.SY] 23 Mar 2026

F(x, u)

x

−

+

Controller 

u

F(x, u)

M(·)

w

x+

+

ˆw

System 

Fig. 1: IMC architecture parametrizing all stability-preserving controllers via a free operator M ∈ L p.

causal if A(x) = ( A0(x0), A 1(x0:1 ), . . . , A t(x0: t), . . . ). If 

At(x0: t) = At(0 , x 0: t−1), then A is said to be strictly causal. An operator A : ℓn → ℓm is ℓp-stable if it is causal and 

A(a) ∈ ℓmp for all a ∈ ℓnp . Equivalently, we write A ∈ L p.We say that an Lp operator A : w 7 → u has a finite Lp-gain γp(A) > 0 if ∥u∥p ≤ γp(A)∥w∥p, for all w ∈ ℓnp .When p is clear from the context, we simply write the gain as γ(A) > 0 and the norm as ∥v∥.II. P RELIMINARIES 

Let us consider the following discrete-time, time-varying nonlinear system: 

xt = ft−1(xt−1, u t−1) + wt, t = 1 , 2, . . . , (1) where xt ∈ Rn is the state vector, ut ∈ Rm is the control input, and wt ∈ Rn represents an unknown process noise embedding the initial value of the system state by setting 

w0 = x0. Rewriting system (1) in operator form, we obtain: 

x = F(x, u) + w, (2) where F : ℓn × ℓm → ℓn is a strictly causal operator defined as F(x, u) = (0 , f 0(x0, u 0), . . . , f t−1(xt−1, u t−1), . . . ). We model the system as the input-to-state operator 

F : ( u, w) 7 → x ∈ L p, (3) and assume F is causal and admits a finite Lp-gain γ(F) <

∞. This is satisfied when the plant is stable, or when ft

represents the dynamics of a system in closed-loop with a stabilizing controller K′1. Additionally, we assume that w ∈

ℓp and that the process noise wt follows a distribution D. To regulate system (1), we consider a nonlinear, time-varying state-feedback controller of the form: 

u = K(x) = ( K0(x0), K 1(x0:1 ), . . . , K t(x0: t), . . . ) . (4) where K : ℓn → ℓm is a causal operator to be designed. Due to causality, each disturbance sequence w ∈ ℓn induces unique trajectories in the closed-loop system (1)–(4) (see e.g. [10]). For a given system F and controller K, we denote by Φx[F, K] and Φu[F, K] the induced closed-loop operators mapping w 7 → x and w 7 → u, respectively. Thus, 

x = Φx[F, K]( w), u = Φu[F, K]( w), ∀w ∈ ℓn.

Our objective is to design a policy K(x) addressing the following problem: 

> 1The latter case is the one considered in the experiments in Section V.

Problem 1: Design K by solving the infinite-horizon Nonlinear Optimal Control (NOC) problem: 

min  

> K(·)

Ew [l(x, u)] (5a) s.t. xt = ft−1(xt−1, u t−1) + wt, w0 = x0, (5b) 

ut = Kt(x0: t), ∀t = 0 , 1, . . . , (5c) 

(Φx[F, K], Φu[F, K]) ∈ L p , (5d) where l : ℓn × ℓm → R is any piecewise differentiable loss satisfying l(x, u) ≥ 0. This loss can be defined through a (possibly time-varying) stage cost, e.g., l(x, u) = P∞ 

> t=0

ct(xt, u t), and is meant to quantify infinite-horizon closed-loop performance. □

The expectation Ew[·] captures the impact of disturbances 

w on the loss, while (5d) imposes a hard ℓp-stability con-straint on the closed-loop system. It has been shown in [8], [10] that the NOC problem admits an equivalent formulation by exploiting an IMC control architecture based on an operator M : ℓn → ℓm

to be designed. Specifically, all and only stability-preserving controllers, i.e., controllers verifying (5d), are parametrized by M ∈ Lp. The control architecture proposed in [8], [10] is shown in Figure 1 and incorporates a model of the system dynamics to estimate the disturbance w. The following theorem summarizes these results: 

Theorem 1: (adapted from Theorem 1 in [10]) Assume that the operator F is ℓp-stable, and consider the evolution of (2) where u is defined as 

u = M(x − F(x, u)) , (6) for a causal operator M : ℓn → ℓm. Let K be the operator for which u = K(x) is equivalent to (6). The following two statements hold true. (i) If M ∈ L p, then the closed-loop system is ℓp-stable. (ii) If there is a causal policy C such that Φx[F, C],

Φu[F, C] ∈ L p, then 

M = Φu[F, C],

gives K = C.Theorem 1 implies that exploring the space of operators 

M ∈ L p is sufficient to identify all and only stability-preserving policies, i.e. all policies satisfying (5d).This sug-gests that solving Problem 1 is equivalent to replacing u =

K(x) in (5c) with (6) and min K(·) in (5a) with min M∈L p .However, solving Problem 1 through numerical methods requires some simplifications. First, searching within the infinite-dimensional space Lp is infeasible. This suggests considering subsets O ⊂ L p of operators M(θ) depending on finitely many parameters θ ∈ Rd. Various methods for defining different sets O are provided in [17], [18]. With the structures proposed in these papers, the operators M can be modelled using time-invariant nonlinear dynamical systems embedding NNs. The formulations in [17], [19], [20] also allow imposing an explicit upper bound on the operator’s 

ℓ2-gain, a feature we will exploit in Section III for control design. We highlight that the resulting operators can be written as M(θ, w) = ( M0(θ, w 0), . . . , Mt(θ, w 0: t), . . . ),where Mt is obtained by unrolling a time-invariant dynami-cal system. In combination with this parametrization, to make the NOC problem (5) tractable, [10] proposes the following finite-dimensional optimization problem: 

min 

> θ∈Rd

1

S

> S

X

> s=1

˜l(xs 

> 0: T

, u s 

> 0: T

) (7a) s.t. xst = ft−1(xst−1, u st−1) + wst , ws 

> 0

= xs

> 0

,ust = Mt(θ, w s

> 0: t

), ∀t = 0 , . . . , T. (7b) In (7), compared to (5a) the expectation is replaced with an empirical average over S samples and the horizon is trun-cated at T . Crucially, stability is decoupled from optimality ;indeed, from part (i) of Theorem 1, closed-loop ℓp stability is guaranteed even when (7) provides an approximated local minimizer. Although effective, this approach suffers from key limi-tations. First, no matter how large T is chosen, it may still be insufficient for time-varying costs. Second, the existing operator parametrizations leverage time-invariant dynamics, associated to a single value of the parameters, significantly limiting the ability of the closed-loop behavior to adapt to changing costs and environmental conditions. To address these points, in this paper, we investigate the following question: under which conditions can we safely replace the current controller with a newly updated controller M(i) ∈ O, i = 1 , 2, . . . without compromising stability? Answering this question enables performing online optimization to continuously update control policies. III. S TABILITY -P RESERVING CONTROLLER UPDATES VIA TRIGGERING CONDITIONS 

We propose to design a new class of time-varying con-trollers obtained by concatenating, over time, operators 

M(θ) ∈ O ⊆ L p, where each M(θ) is time-invariant and parametrized by a finite-dimensional parameter vector 

θ ∈ Rd. Our goal is to derive conditions under which the resulting switched closed loop remains ℓp-stable. We denote with ti ∈ N, i = 1, 2, . . . the (possibly infinitely many) discrete time instants at which the operator 

M(θ) is updated. Given an update schedule {ti}i≥0 and corresponding operators {M(i)}i≥0, we define the resulting time-varying controller as u = ˜M(x, w), where for any 

t ∈ [ti, t i+1 ) the composite operator ˜M is defined as 

( ˜M(x, w)) t := M(i) 

> t−ti

(θ(i), z(i)0: t−ti ),

where z(i) := (xti , w ti+1 , w ti+2 , . . . ) and the operator 

M(i) is (re)initialized with a zero internal state at ti. This generalizes the w0 = x0 convention: the first input is the state at ti, followed by disturbances. We call ˜M the concatenation 

of the M(i)’s. At each update step, a new control law, to be used over the interval ti : ti+1 − 1, is obtained by solving the optimization problem: 

min 

> θ(i)∈Rd

1

S

> S

X

> s=1

˜l(xsti:ti+H , u sti:ti+H ) (8) s.t. xsti = xti , zsti = xti ,xsti+k = fti+k−1(xsti+k−1, u sti+k−1) + wsti+k,zsti+k = wsti+k, k = 1 , . . . , H, s = 1 , . . . , S, usti+k = M(i) 

> k

(θ(i), z sti:ti+k), k = 0 , . . . , H, s = 1 , . . . , S, 

where the cost ˜l is the truncation to horizon H ∈ N of the infinite-horizon cost l(x, u). Although solving problem (8) at any given time ti yields an operator M(i)(θ(i)) ∈ O ⊆ L p,which would guarantee ℓp-stability if applied from ti onward, repeatedly solving (8) and varying the controller over time results in a switched closed loop . It is well known that switching between individually stabilizing controllers can it-self destabilize the closed loop [21]–[23], which necessitates a principled update mechanism. To make online updating safe, we introduce a structured update mechanism. The idea is to allow a new operator M(i) to take effect only when a prescribed triggering condition is satisfied. As desired, this will ensure that the closed loop induced by the concatenated controller ˜M remains ℓp-stable, even though the controller is updated over time through the sub-operators M(i)(θ) ∈ O.We consider triggering instants ti ∈ N defined by 

t0 = 0 , ti+1 = ti + μ(xti ), i ∈ N≥0, (9) where μ : Rn → N≥1 is a triggering function to be designed for ensuring closed-loop stability. Our main result is as follows. 

Theorem 2: (Stable update of the controller) For a given 

¯γ, let M(i) ∈ O be the control operators updated at the triggering instants ti given by (9), and verifying γ(M(i)) ≤

¯γ, ∀i ≥ 0. If the Lp gains γ(M(i)) satisfy 

γ(F) · (γ(M(i)) + 1) ϵ(i) ≤ r(i), ∀i ≥ 1, (10) for some non-negative scalar sequence r = {r(i)}∞ 

> i=1

∈ ℓp

and thresholds ϵ(i) > 0, and if the controller update is triggered by 

|xti | ≤ ϵ(i), (11) then, for every disturbance sequence w ∈ ℓp, the closed-loop trajectories generated by the concatenated controller ˜M

satisfy x ∈ ℓp and u ∈ ℓp. In particular, the resulting time-varying closed loop is ℓp-stable. The proof is reported in Appendix A. Theorem 2 highlights a trade-off between the update condition, which bounds the state at switching times, and the intensity of the control action, governed by the gain of 

M(i). Indeed, (10) yields 

ϵ(i) ≤ r(i)

γp(F) ( γp(M(i)) + 1) ,

and, therefore, the looser the update triggers (larger ϵ(i)), the gentler the controllers (smaller γp(M(i))). Conversely, larger controller gains amplify both state and input on each inter-update window. Note that (11) is an admissibility con-dition: an update at time ti is only allowed if the condition is met. We do not require ti to be the first time this condition becomes true; any ti satisfying (11) is permissible. In the next section, we show how to select {r(i)}i≥1, {ϵ(i)}i≥1, and the triggering mechanism (9) so as to manage this trade-off. IV. I MPLEMENTATION 

In this section, we derive practical update rules from Theorem 2. An update is admissible if the state at switching is sufficiently small and the gain of the new operator is compatible with that state through (10)–(11). The key design quantities are the thresholds ϵ(i) and the sequence r =

{r(i)}: larger thresholds make updates easier to trigger but force smaller gains, whereas smaller thresholds allow more aggressive controllers but delay the update. We consider a time-scheduled scheme and a state-triggered scheme. Both require an upper bound ˆγ(F) on the gain of the stable or pre-stabilized plant; conservatism only makes updates more cautious. In the ℓ2 case, such a bound can be obtained from a storage inequality under the convention 

w0 = x0. Since stability is enforced by the gain condition rather than by optimality, problem (8) may be solved only approximately, for instance using shorter horizons, fewer samples, or budgeted warm-started gradient steps. A practical discussion on choosing r can be found in Appendix B. 

A. Algorithm 1: Predefined Update Times 

We first consider a time-scheduled update mechanism, where μ in (9) is assumed to be constant, in which the designer selects (i) an update period topt ∈ N≥1 and (ii) a nonnegative budget sequence {r(i)}i≥1 ∈ ℓp. Updates are only attempted every topt steps: 

t0 = 0 , ti+1 = ti + topt , i ∈ N≥1.

At each scheduled time ti, we (a) measure the current state and set ϵ(i) := |xti |, and (b) check if an update is admissible under Theorem 2. Rearranging (10) yields an upper bound on the allowable gain of the new controller: 

0 < γ p(M(i)) ≤ r(i)

γp(F) ϵ(i) − 1. (12) We also require γp(M(i)) ≤ ¯γ, where ¯γ is the uniform gain bound assumed in Theorem 2. If at ti there is γp(M(i))

satisfying (12), we solve (8) (with that gain bound enforced) to obtain new parameters θ(i) for M(i), and we apply this controller on [ti, . . . , t i+1 −1] . If it is infeasible (e.g., because |xti | is too large or r(i) is too small), we keep the previous controller for the next interval, M(i) = M(i−1) ,which preserves ℓp-stability. Whenever there is γp(M(i))

satisfying (12), Theorem 2 guarantees that we can update the controller again and the resulting time-varying closed-loop remains ℓp-stable. In summary, topt ∈ N≥1 is freely chosen (with no minimum dwell-time constraint) and determines how often we attempt an update and r(i) sets how aggressive the i-th update is allowed to be: for a given state norm |xti |, larger 

r(i) allows larger controller gain and thus potentially faster transients. 

B. Algorithm 2: State-Dependent Triggering 

The second scheme removes the fixed update period. Instead, the designer specifies (i) an admissible gain level 

γp(M) that upper-bounds the Lp-gain of any controller to be used, and (ii) a nonnegative sequence {r(i)}i≥1 ∈ ℓp.Given γp(M) and r(i), we define the admissible state threshold for the i-th update as 

ϵ(i) = r(i)

γp(F) γp(M) + 1  ,

which is exactly the feasibility condition of Theorem 2. We then set ti to be the first time after ti−1 such that 

|xti | ≤ ϵ(i).

At that instant ti, we solve (8) to obtain a new controller 

M(i) with γp(M(i)) ≤ γp(M), and keep it active until the next trigger time ti+1 . This “self-triggered” rule inverts Algorithm 1. Rather than updating on a fixed schedule and then checking if the state is small enough, we wait until the state is small enough to admit an update of the operator 

M. This can reduce unnecessary recomputations when the state is still large.The only design degrees of freedom are therefore the gain bound γp(M) and the budget sequence 

{r(i)}, which together determine when updates occur. Again, enforcing the gain cap γp(M(i)) ≤ γp(M) and trigger condition implies ℓp-stability by Theorem 2. V. N UMERICAL EXAMPLE 

In this section, we demonstrate the application of the pro-posed approach to robotic tasks. First, we apply our solution to the mountains problem from [10], [24] to highlight the ad-vantages of our method. Then, we present a tracking problem in a time-varying environment with dynamic obstacles. We consider a point-mass system of mass m ∈ R+ subject to nonlinear friction forces: 

qt+1 

vt+1 



=

 qt + Tsvt

vt + Tsm−1 Ft + G(vt)

+ wt, (13) where qt ∈ R2 and vt ∈ R2 represent the position and velocity of the system, respectively, while Ft ∈ R2 is the control input. The sampling time is denoted as Ts = 0 .05 

seconds. The term wt ∈ R4 represents process noise. The nonlinear damping term is defined as: G(vt) = −b1vt +

b2 tanh( vt), where 0 < b2 < b1. To parameterize the operators M(i) ∈ O, we employed a Recurrent Equilibrium Network (REN) [17] with prescribed gain. Problem (8) is solved using backpropagation through time to compute gradients (see [10]), and the Adam optimizer to update the parameters via stochastic gradient descent. The approach is implemented using PyTorch, and the code to reproduce the examples is available at: https://github.com/ DecodEPFL/Online_neurSLS.git .Fig. 2: Qualitative closed-loop behavior of Algorithm 1 in the two case studies. (Left) Mountains problem at τ = 0 .8 s . Green: obstacles. Colored lines: predicted trajectories over [τ, τ +1 .25] ; black: executed trajectory over [0 , τ ); gray dashed: predicted continuation after τ . Colored disks show agent positions and radii. (Right) Dynamic-obstacles problem at τ1 = 9 .3 s . Gray: executed trajectory; red dash-dot: reference; blue: predicted trajectory over [τ1, τ 1 + 0 .5] ; green: obstacle positions at τ1.

A. Mountains problem 

The mountains scenario depicted in Figure 2 features two agents aiming to collaboratively navigate through a narrow valley. Each agent j ∈ { 1, 2} evolves according to (13) and has a target position ¯qj ∈ R2 that must be reached with zero velocity (i.e., ¯vj = 0 2). We stack both agents’ states, so the overall state is xt = [ qt, 1, v t, 1, q t, 2, v t, 2]⊤ ∈ R8 and 

¯x stacks the target positions and zero velocities. We study the regulation problem in error coordinates with respect to the desired equilibrium ¯qj , ¯vj . We apply a baseline linear feedback K′ = diag( k1, k 2) with gains k1, k 2 > 0, around the equilibrium, and we set Ft,j = K′(¯ qj −qt,j )+ ut,j , where 

ut,j is an auxiliary input. We treat ut = [ ut, 1, u t, 2]⊤ as the control signal to be optimized. Under this pre-stabilization, the closed-loop error dynamics can be written in the form of (1), with state et = xt − ¯x ∈ R8, input ut = [ ut, 1, u t, 2]⊤ ∈

R4, and disturbance wt ∈ R8. The associated input-to-state operator Fe(u, w) 7 → e has finite ℓ2-gain. An upper bound on this gain is obtained from a discrete-time bounded-real LMI [25]; see Appendix C.1. In this example, we set p = 2 ,and we model the disturbance sequence w as Gaussian noise with exponential decay. The loss function is defined as: 

˜l(e0: T , u 0: T ) = 

> T

X

> t=0

 ltraj (et, u t) + lca (et) + lobs (et),

where ltraj (e, u ) = [ e⊤ u⊤]Q[e⊤ u⊤]⊤ with Q ⪰ 0 pe-nalizing deviation from the targets and control effort. The terms lca (e) and lobs (e) are barrier functions that prevent collisions between agents and obstacles (see [10] for their definitions). We compare with the approach proposed in [10], where the controller is parametrized by an operator M ∈ L 2

obtained offline over a horizon T = 100 , matching the total simulation time used for our online controller. Figure 2 (left) illustrates closed-loop trajectories obtained using Algorithm 1, which attempts an update to a new operator M(i) at fixed intervals of topt = 2 time steps. Problem (8) is solved online to update the operator with a horizon of H = 25 and 

S = 3 disturbance realizations. Two scenarios are simulated: the nominal scenario replicating the example in [10], and a perturbed scenario involving abrupt impulse disturbances. Specifically, the local disturbance wt,j is replaced by: 

wt,j + 0 .3δ(t − 1) + 0 .3δ(t − 8) , j = 1 , 2, (14) where δ is the Kronecker delta. Over 50 runs, our method reduces the average cost by 35.1% (nominal) and 40.3% (perturbed) versus [10], indicating improved adaptation to abrupt, previously unseen disturbance events; boxplots are reported in Appendix C. 

B. Dynamic obstacles 

In the second scenario (Figure 2), an agent tracks acircular path while avoiding dynamic obstacles. We track a reference trajectory xref ,t by applying a baseline stabilizing feedback K′ together with a feedforward term uref ,t . We define the tracking error et = xt − xref ,t ∈ R4 and write the total input as Ft = uref ,t − K′et + ut, where ut is an auxiliary control signal that we optimize online. Under this pre-stabilization, the closed-loop error dynamics for et can be written as a nonlinear system et = fe(et−1, u t−1) + wt,which matches the general form of (1) with state et, input 

ut, and disturbance wt. As in the previous example, the gain of the associated input-to-state operator Fe(u, w) 7 → e is computed upper-bounding the worst-case realization of the nonlinearity. Two dynamic obstacles move on the y-axis following a perturbed sinusoidal trajectory: 

yobs  

> t,j

= A sin((2 πψ j + ηj )t + ϕj ) + yobs  

> 0,j

, j = 1 , 2. (15) where ψj is the nominal frequency, ηj ∼ N (0 , 0.01) is a random frequency perturbation, and ϕj is a random phase. The finite-horizon objective ˜l in (8) is now time-varying because the obstacles move. We define 

˜l(e0: T , u 0: T ) = 

> T

X

> t=0

 ltraj (et, u t) + lobs (et, t ).

where ltraj is a positive semidefinite quadratic penalty on tracking error and control effort, and lobs (et, t ) penalizes collisions with the moving obstacle. The system is simulated for 300 time steps under a disturbance sequence generated at each step with bounded amplitude and interpreted as an element of ℓ2 by zero extension beyond the simulation horizon. In other words, the disturbance is persistent over the considered horizon, but has finite energy on the full infinite sequence. Accordingly, in this experiment we set p = 2 ,consistently with the certified ℓ2-gain of the REN controller class. We compare against (i) the offline controller of [10] and (ii) a receding-horizon open-loop (RHO) planner that solves at time t the problem 

min 

> u0: H−1
> S

X

> s=0

˜l(e0: H , u 0: H )

s.t. esk+1 = fe(esk, u k) + wsk, e s 

> 0

= et, ∀s, ws ∼ D ,

and applies the first input ut = u∗

> 0

, advances the state, and re-solves at t+1 . For a fair comparison, all approaches use the same horizon ( H = 10 ) and cost weights. Algo-rithm 1 attempts updates M every topt = 1 time step, using a budgeted, inexact solution (100 epochs of gradient descent warm-started from the previous parameters). This is admissible under Theorem 2 because stability is guaranteed even without reaching local optimality. The right panel in Figure 2 shows the closed-loop trajectory (in grey) and the predicted trajectories (in blue). Persistent disturbances make perfect tracking unachievable, and apparent overlaps between moving obstacles and past trajectory segments are only visualization artifacts. Over 50 runs, the proposed approach achieves lower cost than both RHO and the offline controller from [10], while also exhibiting smaller variability; the corresponding boxplots are reported in Appendix C. Overall, the results indicate that online adaptation with closed-loop prediction improves both performance and reliability relative to the two baselines. VI. C ONCLUSIONS 

We introduced a framework for iteratively updating neural-network-based controllers while maintaining closed-loop sta-bility via gain-based update conditions. This opens several research directions. First, robustness margins against model-plant mismatch could be embedded directly into the update logic, preserving stability under imperfect models. Second, online optimization could be used to update both the con-trol operator and the system model used by the controller. Finally, connecting these ideas to reinforcement learning by enforcing our gain-based admissibility condition during policy improvement may offer stability guarantees during learning, not only after convergence. REFERENCES [1] D. P. Bertsekas, Dynamic Programming and Optimal Control . Athena Scientific, 1995. [2] J. B. Rawlings, D. Q. Mayne, and M. M. Diehl, Model Predictive Control: Theory, Computation, and Design , 2nd ed. Nob Hill Publishing, 2017. [3] D. Saccani, L. Fagiano, M. N. Zeilinger, and A. Carron, “Model pre-dictive control for multi-agent systems under limited communication and time-varying network topology,” in 2023 62nd IEEE Conference on Decision and Control (CDC) . IEEE, 2023, pp. 3764–3769. [4] L. Grüne and J. Pannek, Nonlinear model predictive control . Springer, 2017. [5] G. Cybenko, “Approximation by superpositions of a sigmoidal func-tion,” Mathematics of control, signals and systems , 1989. [6] E. D. Sontag, “Neural networks for control,” in Essays on Control: Perspectives in the Theory and its Applications . Springer, 1993. [7] D. Ho, “A system level approach to discrete-time nonlinear systems,” in 2020 American Control Conference (ACC) . IEEE, 2020. [8] L. Furieri, C. L. Galimberti, and G. Ferrari-Trecate, “Neural system level synthesis: Learning over all stabilizing policies for nonlinear systems,” in 2022 IEEE 61st Conference on Decision and Control (CDC) . IEEE, 2022, pp. 2765–2770. [9] D. Saccani, L. Massai, L. Furieri, and G. Ferrari-Trecate, “Optimal distributed control with stability guarantees by training a network of neural closed-loop maps,” in 2024 IEEE 63rd Conference on Decision and Control (CDC) . IEEE, 2024, pp. 3776–3781. [10] L. Furieri, C. L. Galimberti, and G. Ferrari-Trecate, “Learning to boost the performance of stable nonlinear systems,” IEEE Open Journal of Control Systems , 2024. [11] R. Wang and I. R. Manchester, “Youla-ren: Learning nonlinear feed-back policies with robust stability guarantees,” in 2022 American Control Conference (ACC) . IEEE, 2022, pp. 2116–2123. [12] N. H. Barbara, R. Wang, and I. R. Manchester, “Learning over contracting and lipschitz closed-loops for partially-observed nonlinear systems,” in 2023 62nd IEEE Conference on Decision and Control (CDC) . IEEE, 2023, pp. 1028–1033. [13] C. L. Galimberti, L. Furieri, and G. Ferrari-Trecate, “Parametrizations of all stable closed-loop responses: From theory to neural network control design,” Annual Reviews in Control , vol. 60, p. 101012, 2025. [14] D. Liberzon, Switching in systems and control . Springer, vol. 190. [15] A. S. Morse, “Control using logic-based switching,” Trends in control: A European perspective , pp. 69–113, 1997. [16] C. De Persis, R. De Santis, and A. S. Morse, “Switched nonlinear systems with state-dependent dwell-time,” Systems & Control Letters ,vol. 50, no. 4, pp. 291–302, 2003. [17] M. Revay, R. Wang, and I. R. Manchester, “Recurrent equilibrium networks: Flexible dynamic models with guaranteed stability and robustness,” IEEE Transactions on Automatic Control , 2023. [18] F. Bonassi, C. Andersson, P. Mattsson, and T. B. Schön, “Structured state-space models are deep wiener models,” IFAC-PapersOnLine ,vol. 58, no. 15, pp. 247–252, 2024. [19] L. Massai and G. Ferrari-Trecate, “Free parametrization of l2-bounded state space models,” arXiv preprint arXiv:2503.23818 , 2025. [20] M. Zakwan and G. Ferrari-Trecate, “Neural port-hamiltonian models for nonlinear distributed control: An unconstrained parametrization approach,” arXiv preprint arXiv:2411.10096 , 2024. [21] D. Liberzon and A. S. Morse, “Basic problems in stability and design of switched systems,” IEEE control systems magazine , 1999. [22] H. Lin and P. J. Antsaklis, “Stability and stabilizability of switched linear systems: a survey of recent results,” IEEE Transactions on Automatic control , vol. 54, no. 2, pp. 308–322, 2009. [23] W. P. Heemels, K. H. Johansson, and P. Tabuada, “An introduction to event-triggered and self-triggered control,” in 2012 IEEE 51st Conference on Decision and Control (CDC) . IEEE, 2012. [24] D. Onken, L. Nurbekyan, X. Li, S. W. Fung, S. Osher, and L. Ruthotto, “A neural network approach applied to multi-agent optimal control,” in 2021 European Control Conference (ECC) . IEEE, 2021. [25] C. E. de Souza and L. Xie, “On the discrete-time bounded real lemma with application in the characterization of static state feedback h ∞

controllers,” Systems & Control Letters , vol. 18, no. 1, 1992. [26] Z.-P. Jiang and Y. Wang, “Input-to-state stability for discrete-time nonlinear systems,” Automatica , vol. 37, no. 6, pp. 857–869, 2001. APPENDIX 

A. Proof of Theorem 2 and Input to State stability relation 

We first establish an upper bound for the state and input norms when applying M(0) on the first interval [t0, t 1 −

1] , where t0 = 0 . From the definition of F in (2) and the controller ˜M, the controller input is z(0) = ( x0, w 1, . . . ).From (1), one has w0 = x0, so z(0) = w. Let ¯x0 = x0: t1−1

and ¯w0 = w0: t1−1.

∥u0: t1−1∥ ≤ γ(M(0) )∥ ¯w0∥ (16) 

∥¯x0∥ ≤ γ(F)( ∥u0: t1−1∥ + ∥ ¯w0∥) (17) Substituting (16) into (17) yields: 

∥¯x0∥ ≤ γ(F)( γ(M(0) ) + 1) 

| {z }

> C0

∥ ¯w0∥. (18) Now consider the evolution on a subsequent window 

i ≥ 1 on the interval [ti, t i+1 − 1] . Let ¯xi =

xti:ti+1 −1, ¯ui = uti:ti+1 −1, ¯wi = wti:ti+1 −1, and ¯zi =(xti , w ti+1 , . . . , w ti+1 −1). We obtain 

∥¯xi∥p ≤ 2γp(F)|xti | + γp(F)( ∥¯ui∥p + ∥ ¯wi∥p) (19) The controller’s output ¯ui is bounded by its gain with zero initial condition: 

∥¯ui∥p ≤ γp(M(i)) ∥¯zi∥p. (20) The input ¯zi is bounded using the triangle inequality: 

∥¯zi∥p ≤ | xti | + ∥wti+1: ti+1 −1∥p ≤ | xti | + ∥ ¯wi∥p (21) Substitute (20) and (21) into (19) and grouping terms by |xti |

and ∥ ¯wi∥p yields: 

∥¯xi∥p ≤ γp(F)( γp(M(i)) + 1) |xti |

+ γp(F)( γp(M(i)) + 1) ∥ ¯wi∥p

Now, apply the triggering condition (11), |xti | ≤ ϵ(i), and the gain condition (10), γp(F)( γp(M(i)) + 1) ϵ(i) ≤ r(i):

∥¯xi∥p ≤ r(i) + C∥ ¯wi∥p, (22) where C = γp(F)(¯ γ + 1) (using ¯γ as an upper bound). We now analyze the total norm of x = (¯ x0, ¯x1, . . . ).

Case: 1 ≤ p < ∞. Summing the p-th power of (22) over all windows i:

∥x∥pp = ∥¯x0∥pp +

> ∞

X

> i=1

∥¯xi∥pp (23) 

≤ (C0∥ ¯w0∥p)p +

> ∞

X

> i=1



r(i) + C∥ ¯wi∥p

p

Let ˜w = ( ∥ ¯w1∥p, ∥ ¯w2∥p, . . . ). The sum in (23) is the p-norm of a sum of sequences in ℓp:

∥x∥pp ≤ (C0∥ ¯w0∥p)p + ( ∥r + C ˜w∥p)p

≤ (C0∥ ¯w0∥p)p + ( ∥r∥p + C∥ ˜w∥p)p                 

> 2The system Fon this window has a non-zero initial state xtiand external inputs ¯uiand ¯wi, and since as established by the w0=x0
> convention, the unforced response to an initial state xtiis equivalent to the response to a disturbance impulse w′= ( xti,0, . . . ).

Using (a + b)p ≤ 2p−1(ap + bp), which holds for 

p ∈ N≥1 on the second term: ∥x∥pp ≤ (C0∥ ¯w0∥p)p +2p−1(∥r∥pp + Cp∥ ˜w∥pp). The windows ¯wi are disjoint. Let 

w′ = ( ∥ ¯w0∥, ˜w). Then ∥w′∥pp = P∞ 

> i=0

∥ ¯wi∥pp = ∥w∥pp. The total norm is bounded by: 

∥x∥pp ≤ 2p−1  (C0∥ ¯w0∥p)p + ∥r∥pp + Cp∥ ˜w∥pp



≤ 2p−1  max( C0, C )p∥w∥pp + ∥r∥pp



Since r, w ∈ ℓp, the right-hand side is finite, so we conclude that ∥x∥p < ∞. The same arguments applied to (20) show 

∥u∥p < ∞. Thus, the system is ℓp-stable. 

Case: p = ∞. The bound (22) (which also holds for p =

∞) becomes: ∥¯xi∥∞ ≤ r(i) + C∥ ¯wi∥∞.

Taking the supremum over all i ∈ N≥1:

sup 

> i∈N≥1

∥¯xi∥∞ ≤ sup 

> i∈N≥1

r(i) + sup 

> i∈N≥1

(C∥ ¯wi∥∞)

The total state norm is ∥x∥∞ =max( ∥¯x0∥∞, sup i∈N≥1 ∥¯xi∥∞). From (18), ∥¯x0∥∞ ≤

C0∥ ¯w0∥∞, and hence 

∥x∥∞ ≤ max( C0∥ ¯w0∥∞, ∥r∥∞ + C sup 

> i∈N≥1

∥ ¯wi∥∞) (24) Since sup i∈N≥0 ∥ ¯wi∥∞ ≤ ∥ w∥∞, and r, w ∈ ℓ∞, the right hand side of (24) is finite. Thus, the system is ℓ∞-stable. Therefore, for any p ∈ [1 , ∞] and any disturbance sequence 

w ∈ ℓp, the corresponding closed-loop trajectories satisfy 

x ∈ ℓp and u ∈ ℓp. Hence, the closed loop induced by the concatenated controller ˜M is ℓp-stable. ■

Next, we discuss how imposing additional conditions on 

r can yield an input-to-state type bound for the resulting closed loop, on top of ℓp-stability. 

Corollary 1 (ISS under time-scheduled persistent updates): 

Consider Algorithm 1 with p = ∞ and fixed update times 

ti = i t opt , i ∈ N, and assume that the conditions of Theorem 2 hold at every scheduled update. Suppose that 

r(i) = d(i) γ(F) γ(M(0) ) + 1 |x0| + g∥w∥∞, i ≥ 1,

for some positive, strictly decreasing scalar sequence 

{d(i)}i≥0 such that d(0) ≥ 1 and lim i→∞ d(i) = 0 , and for some constant g > 0. Then the resulting closed loop is Input-to-State Stable (ISS) [26]; that is, there exist functions 

β ∈ KL and α ∈ K ∞ such that, for any initial condition x0

and any bounded disturbance sequence w ∈ ℓ∞, the state trajectory satisfies 

|xt| ≤ β(|x0|, t ) + α(∥w∥∞), ∀t ∈ N.

Proof: Let 

D := γ(F) γ(M(0) ) + 1 , G := γ(F)(¯ γ + 1) .

From the proof of Theorem 2, if t ∈ [ti, t i+1 ) with i ≥ 1,then 

|xt| ≤ r(i) + G∥w∥∞ = D d (i)|x0| + ( g + G)∥w∥∞.

For t ∈ [t0, t 1), (18) gives 

|xt| ≤ D|x0| + G∥w∥∞ ≤ D d (0) |x0| + ( g + G)∥w∥∞,since d(0) ≥ 1 and g > 0.Now define a continuous, nonincreasing function ¯d :[0 , ∞) → (0 , ∞) as follows: set ¯d(t) = d(0) for t ∈ [0 , t 1],and for each i ≥ 1 let ¯d be linear on [ti, t i+1 ] with 

¯d(ti) = d(i−1) , ¯d(ti+1 ) = d(i).

Since {d(i)} is strictly decreasing and ti → ∞ , the function 

¯d is continuous, nonincreasing, and satisfies ¯d(t) → 0 as t →∞. Moreover, for every t ∈ [ti, t i+1 ) one has ¯d(t) ≥ d(i).Therefore, for all t ≥ 0,

|xt| ≤ D ¯d(t) |x0| + ( g + G)∥w∥∞.

Define 

β(s, t ) := D ¯d(t) s, α(s) := ( g + G)s. 

Then α ∈ K ∞. Also, for each fixed t ≥ 0, β(·, t ) ∈ K , and for each fixed s ≥ 0, β(s, ·) is continuous, nonincreasing, and converges to 0 as t → ∞ . Hence β ∈ KL , and the claimed ISS estimate follows. 

B. Implementation details 1) Design of the sequence r: The sequence r = {r(i)}i≥1

regulates how aggressive updates are allowed to be over time. The design of r can leverage a priori information obtained through offline closed-loop simulations. Let ˜x0: T be a nominal state trajectory generated under a representative disturbance and a baseline controller M(0) . We define a time-indexed budget profile 

0 ≤ rt = ρ ∥˜xt∥, t = 0 , . . . , T, 

with ρ > 0 a scaling factor. To obtain an infinite-horizon sequence compatible with Theorem 2, the profile is extended for t > T by a nonnegative tail {rt}t>T chosen so that 

{rt}t≥0 ∈ ℓp. For instance, when 1 ≤ p < ∞, one may append any summable tail, e.g. rt = rT η t−T , t > T, for some η ∈ (0 , 1) , while for p = ∞ any bounded continuation is admissible (and a decaying continuation can be used if one also wants the update budget to vanish asymptotically). Online, at each actual update time ti, we set r(i) := rti .In this way, large nominal deviations ∥˜xt∥ early in the system evolution yield larger r(i), allowing updates even for relatively large states (or higher controller gains), while smaller ∥˜xt∥ later in the system evolution yield smaller r(i),enforcing more conservative updates near the origin. This provides a simple, simulation-driven procedure to generate a sequence r consistent with Theorem 2. 

2) Controller Updates through optimization: The method for updating the controller depends significantly on the chosen update strategy and the available computational re-sources. Indeed, solving problem (8) can be computationally demanding. A practical way to reduce computational burden is to solve a simplified instance of (8) (shorter horizon H,fewer disturbance samples). Equivalently, one may warm-start from the previously found parameters θ and take a budgeted number of gradient steps during the sampling interval; the resulting inexact iterate can be deployed without risking destabilization. Indeed, the proposed method ensures 

Fig. 3: Mountains problem - Total cost (log scale) over 50 runs: nominal scenario (center) and perturbed scenario with impulse disturbances (14) (right). 

Fig. 4: Dynamic obstacles problem - Total cost (log scale) over 50 runs for the offline controller in [10], a receding-horizon open-loop (RHO) planner, and the proposed ap-proach. 

stability without requiring optimality and without requiring specific assumptions on the cost function, except that it must be differentiable (see Problem 1). 

C. Numerical Example 

Figures 3 and 4 report the boxplots of the total closed-loop cost for the mountains and dynamic-obstacles scenarios, respectively. 

1) Computation of the upper bound of the ℓ2-gain: For the pre-stabilized error dynamics, we consider the least favorable slope of the friction term 

G(v) = −b1v + b2 tanh( v), σwc := −(b1 − b2),

which corresponds to the minimum-damping case since 

−b1 ≤ ∂G ∂v ≤ − (b1 − b2).

Using this worst-case slope, we obtain a linear error model of the form 

et+1 = Awc et + Buut + wt+1 .

Let ¯B := [ Bu I ] and ξt := [ u⊤ 

> t

w⊤ 

> t+1

]⊤. We compute an upper bound on the ℓ2-gain of Fe : ( u, w) 7 → e by finding P ≻ 0, η > 0, and ρ > 0 such that 

A⊤

> wc

P A wc − P + ηI A⊤

> wc

P ¯B

¯B⊤P A wc ¯B⊤P ¯B − ρI 



⪯ 0. (25) Then, with V (e) = e⊤P e , (25) implies 

V (et+1 ) − V (et) ≤ − η∥et∥2 + ρ ∥ut∥2 + ∥wt+1 ∥2.

Summing over time and using the convention w0 = e0 yields 

η∥e∥22 ≤ λmax (P )∥w0∥2+ρ∥u∥22+ρ∥w1: ∞∥22 ≤ c ∥w∥22+∥u∥22

,

where c := max {λmax (P ), ρ }. Therefore, 

∥e∥2 ≤ ˆγ(Fe) ∥w∥2+∥u∥2

, ˆγ(Fe) := max 



1,

r cη



.