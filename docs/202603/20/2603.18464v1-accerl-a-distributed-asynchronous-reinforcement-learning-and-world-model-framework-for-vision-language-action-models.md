---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种用于视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v1"
tags: ["query:tta"]
score: 8.0
evidence: 用于VLA模型的世界模型框架
tldr: AcceRL是一个针对大规模视觉-语言-动作（VLA）模型设计的分布式异步强化学习框架。它通过物理隔离训练、推理和采样过程，消除了同步障碍，并首次将可训练的世界模型集成到异步流水线中以生成虚拟经验。该框架在LIBERO基准测试中达到了SOTA水平，显著提升了硬件利用率、吞吐量和样本效率，为复杂控制任务提供了高效的训练方案。
selection_source: fresh_fetch
motivation: 旨在解决大规模VLA模型在强化学习过程中面临的计算效率低下、同步延迟以及数据获取困难等核心挑战。
method: 提出了一种完全异步且解耦的框架，通过物理隔离核心环节并集成即插即用的世界模型来生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现了吞吐量的超线性扩展，并展现出极高的样本效率和训练稳定性。
conclusion: AcceRL为大规模VLA模型的高效强化学习训练提供了一个高性能、可扩展且具备世界模型增强能力的系统级解决方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展，并具有极高的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks.

---

## 论文详细总结（自动生成）

### AcceRL：面向视觉-语言-动作（VLA）模型的分布式异步强化学习与世界模型框架论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：大规模视觉-语言-动作（VLA）模型（如 7B 参数级别）在具身智能领域展现了强大潜力，但其强化学习（RL）训练面临严重的效率瓶颈。
*   **核心问题**：
    *   **同步障碍**：传统 RL 框架采用“收集-更新”的同步模式，导致高性能 GPU 在等待缓慢的物理模拟器时处于闲置状态（长尾效应）。
    *   **数据饥渴**：物理模拟器频率低且 VLA 推理延迟高，导致获取在线交互数据的成本极高。
    *   **策略滞后**：在异步架构中，收集数据的旧策略与当前学习策略之间的差异（Policy Lag）会导致训练不稳定。
*   **整体含义**：AcceRL 旨在通过系统级的异步解耦和算法级的世界模型增强，实现大规模 VLA 模型的高效、稳定在线微调。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：实现训练（Training）、推理（Inference）与采样（Rollout）在物理上的完全隔离与异步流转，并引入“在想象中学习”的世界模型机制。
*   **关键技术细节**：
    *   **双层异步架构**：
        *   **宏观异步**：训练器与采样工人解耦，通过分布式重放池交换数据，训练器利用 ZeRO-2 优化显存。
        *   **微观异步**：推理即服务（Inference-as-a-Service），将环境模拟（CPU）与模型推理（GPU）分离，通过动态窗口机制实现高效批处理。
    *   **世界模型（WM）集成**：集成基于扩散模型（DIAMOND）的观测模型和奖励模型，生成高保真虚拟经验，实现 200 倍的样本效率提升。
    *   **策略滞后修正**：
        *   **价值重计算**：在梯度更新前使用最新评论家网络重新评估状态价值。
        *   **全局优势归一化**：跨节点聚合统计量以消除局部偏差。
        *   **GIPO 算法**：采用高斯重要性采样策略优化，用平滑的信任权重代替 PPO 的硬截断，增强异步稳定性。
    *   **VLA 专用优化**：词表瘦身（仅保留动作 Token）、动作感知注意力池化（Value Head 设计）以及 Token 级策略优化。

#### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 具身智能基准测试，涵盖四个子集：LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **对比方法**：
    *   **监督学习基准**：OpenVLA-OFT。
    *   **SOTA RL 框架**：SimpleVLA-RL、RLinf-VLA。
*   **评估指标**：任务成功率、系统吞吐量（SPS）、硬件利用率、样本效率。

#### 4. 资源与算力
*   **硬件型号**：实验中明确提到了使用 **NVIDIA H200 GPU** 进行扩展性测试。
*   **算力规模**：
    *   **扩展性实验**：测试了从 1 到 7 个 Trainer GPU 的扩展效果，以及多达 64 个采样工人的吞吐量。
    *   **训练时长**：文中未明确给出总训练小时数，但展示了在 400 次更新或 10,000 步环境交互内即可实现快速收敛。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的四个大类任务上进行了全面的性能对比。
    *   进行了系统吞吐量的扩展性实验（验证超线性扩展）。
    *   **消融实验**：针对“价值重计算”、“GIPO 算法”以及“世界模型增强”分别做了三组深入的对比实验。
*   **充分性评价**：实验设计较为充分，不仅涵盖了最终的性能指标，还从系统工程（吞吐量、显存）和算法鲁棒性（策略滞后处理）多个维度验证了框架的有效性，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：AcceRL 在 LIBERO 所有类别中均达到 SOTA，尤其在长程任务（LIBERO-Long）中成功率达 99.1%，远超监督学习（90.7%）。
*   **超线性扩展**：得益于 ZeRO 优化和异步架构，系统吞吐量随 GPU 数量增加呈现超线性增长，GPU 利用率保持在 94% 以上。
*   **效率飞跃**：引入世界模型后，在线样本效率提升了 **20,000% (200倍)**，在极少量的真实环境交互下即可完成复杂任务微调。
*   **算法稳定性**：GIPO 算法相比标准 PPO 在异步环境下收敛速度提升约 7.5 倍。

#### 7. 优点
*   **系统架构先进**：彻底解决了具身智能训练中常见的“GPU 等待模拟器”的空泡问题。
*   **即插即用**：世界模型模块化设计，可根据需求灵活开启，且支持高保真像素级生成。
*   **工程优化深入**：针对 VLA 模型的词表裁剪和 Token 级优化非常具有实操价值，显著降低了显存压力。

#### 8. 不足与局限
*   **模型规模限制**：论文提到目前尚未支持超大规模语言模型（如全量参数对齐）的后训练，未来需扩展至更大规模的架构。
*   **世界模型开销**：虽然减少了环境交互，但训练和运行基于扩散模型的世界模型本身需要额外的计算资源和显存。
*   **应用场景**：实验主要集中在 MuJoCo 模拟环境，尚未展示在真实物理机器人硬件上的部署表现及跨域泛化能力。

（完）
