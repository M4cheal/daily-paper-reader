---
title: Adaptive Decoding via Test-Time Policy Learning for Self-Improving Generation
title_zh: 通过测试时策略学习实现自我改进生成的自适应解码
authors: "Asmita Bhardwaj, Yuya Jeremy Ong, Eelaaf Zahid, Basel Shbita"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18428v1"
tags: ["query:tta"]
score: 8.0
evidence: 测试时策略学习以调整采样参数
tldr: 针对大语言模型解码策略静态且缺乏任务适应性的问题，本文提出一种基于强化学习的自适应解码采样器。该方法在不改变模型权重的前提下，通过学习轻量级策略在推理阶段动态调整采样参数。实验表明，该方法在多项摘要任务中显著优于贪婪搜索等基准，为实现领域感知和用户可控的生成提供了一种高效的测试时优化方案。
selection_source: fresh_fetch
motivation: 现有的静态解码策略（如贪婪搜索或固定参数采样）难以适应不同任务对生成风格和结构的灵活需求。
method: 提出一种基于强化学习的轻量级策略，将解码视为序列决策过程，在推理时动态调整采样参数而无需重新训练大模型。
result: "在BookSum和WikiHow等摘要数据集上，该方法相比基准策略分别实现了高达88%和79%的相对性能提升。"
conclusion: 强化学习是实现测试时解码自适应的有效机制，能够在保持模型权重不变的情况下显著提升生成的领域相关性和质量。
---

## 摘要
解码策略在很大程度上决定了大语言模型（LLM）输出的质量，然而广泛使用的启发式方法（如贪婪解码或固定温度/top-p 解码）是静态的，且通常与任务无关，导致在需要风格或结构灵活性的领域中生成质量欠佳或不一致。我们引入了一种基于强化学习的解码采样器，将解码视为序列决策过程，并在保持 LLM 权重冻结的情况下，学习一种轻量级策略以在测试时调整采样参数。我们使用 Granite-3.3-2B 和 Qwen-2.5-0.5B 在包括 BookSum、arXiv 和 WikiHow 在内的摘要数据集上进行了评估。我们的策略采样器始终优于贪婪和静态基准，实现了高达 +88%（BookSum, Granite）和 +79%（WikiHow, Qwen）的相对增益。奖励消融实验表明，仅包含重叠度的目标函数表现不如复合奖励，而结构化塑造项（长度、覆盖度、重复率、完整性）能够实现稳定且持续的改进。这些发现强调了强化学习作为解码中测试时自适应的一种实用机制，能够在不重新训练大模型的情况下实现领域感知和用户可控的生成。

## Abstract
Decoding strategies largely determine the quality of Large Language Model (LLM) outputs, yet widely used heuristics such as greedy or fixed temperature/top-p decoding are static and often task-agnostic, leading to suboptimal or inconsistent generation quality across domains that demand stylistic or structural flexibility. We introduce a reinforcement learning-based decoder sampler that treats decoding as sequential decision-making and learns a lightweight policy to adjust sampling parameters at test-time while keeping LLM weights frozen. We evaluated summarization datasets including BookSum, arXiv, and WikiHow using Granite-3.3-2B and Qwen-2.5-0.5B. Our policy sampler consistently outperforms greedy and static baselines, achieving relative gains of up to +88% (BookSum, Granite) and +79% (WikiHow, Qwen). Reward ablations show that overlap-only objectives underperform compared to composite rewards, while structured shaping terms (length, coverage, repetition, completeness) enable stable and sustained improvements. These findings highlight reinforcement learning as a practical mechanism for test-time adaptation in decoding, enabling domain-aware and user-controllable generation without retraining large models.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Decoding via Test-Time Policy Learning for Self-Improving Generation》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）的生成质量高度依赖解码策略。然而，现有的解码方法（如贪婪搜索、固定温度采样或 Top-p 采样）通常是静态且任务无关的，难以适应不同领域（如科学论文摘要与小说摘要）对风格、长度和结构的多样化需求。
*   **核心挑战**：改进解码行为通常需要对 LLM 进行昂贵的微调或重新训练。
*   **研究目标**：提出一种在**不改变 LLM 权重**的前提下，通过强化学习（RL）在推理阶段（测试时）动态调整解码参数的轻量级方法，实现“自我改进”式生成。

### 2. 方法论
*   **核心思想**：将解码过程建模为**马尔可夫决策过程（MDP）**。引入一个轻量级的策略网络（Agent），在每个解码步骤观察 LLM 的内部状态，并动态决定采样参数（如温度 $T$ 和 $p$ 值）。
*   **关键技术细节**：
    *   **状态空间 (State)**：连接了均值池化的最后隐藏层状态、Top-50 Logits、当前前缀长度以及下一步 token 分布的归一化熵。
    *   **动作空间 (Action)**：二维连续空间，通过 Sigmoid 映射到 $T \in [0.2, 1.2]$ 和 $p \in [0.8, 1.0]$。
    *   **奖励函数 (Reward)**：采用复合奖励函数，包含 ROUGE-L F1 分数（重叠度）、长度惩罚、覆盖度奖励（针对源文本重要词汇）、重复惩罚和完整性惩罚（标点符号检查）。
    *   **算法流程**：使用**近端策略优化（PPO）**算法训练策略网络。策略网络是一个简单的 2 层 MLP（隐藏层维度 256）。在生成过程中，LLM 保持冻结，仅更新 MLP 的参数。
*   **公式**：优化目标为标准的 PPO 剪切损失函数 $L_{clip}(\theta)$，结合了价值函数损失和熵正则项，以确保训练稳定性和探索性。

### 3. 实验设计
*   **数据集**：涵盖三种不同风格的摘要任务：
    1.  **BookSum**：长篇叙事章节摘要。
    2.  **arXiv**：科学论文摘要。
    3.  **WikiHow**：指导性说明文摘要。
*   **评估模型**：Granite-3.3-2B-Base 和 Qwen-2.5-0.5B。
*   **基准方法 (Benchmarks)**：
    1.  **Greedy Decoding**：每一步选择概率最大的 token。
    2.  **Static Sampling**：固定温度 $T=0.3$ 的采样。
*   **对比维度**：最终奖励得分、相对基准的提升百分比、以及 PPO 训练前后的性能变化（Early vs. Late）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **资源消耗线索**：作者强调这是一种“低资源 RL 设置”，每个数据集仅使用 100 个提示词（Prompts）进行训练。由于策略网络仅为极小的 MLP，且 LLM 权重冻结，其计算开销相对于 LLM 本身的推理过程几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了 2 个不同架构的模型和 3 个不同领域的典型数据集。
    *   进行了详细的**奖励函数消融实验**（共 6 组变体，如仅 ROUGE、无覆盖奖励、软重复惩罚等）。
*   **充分性评价**：实验设计较为合理，通过跨模型和跨领域的验证证明了方法的通用性。消融实验深入探讨了奖励设计对 RL 训练稳定性的影响，具有较高的参考价值。但由于训练样本量较小（100 个提示词），其在大规模生产环境下的泛化能力仍有待进一步验证。

### 6. 主要结论与发现
*   **显著提升**：RL 采样器在所有任务上均优于静态基准。在 BookSum 任务上，相比贪婪搜索，Granite 模型实现了 **+88%** 的相对增益；在 WikiHow 上，Qwen 模型提升了 **+79%**。
*   **奖励设计至关重要**：仅使用 ROUGE（文本重叠度）作为奖励效果极差，甚至不如基准。必须引入长度、重复率等结构化塑造项（Shaping terms）才能实现稳定增长。
*   **小模型受益更大**：实验发现 0.5B 的小模型在自适应解码下表现出更强的相对改进，说明该方法是增强受限模型能力的有效手段。
*   **测试时自适应**：策略网络能够根据生成的上下文实时调整参数，证明了在不修改模型权重的情况下实现行为优化的可行性。

### 7. 优点
*   **轻量且高效**：无需重新训练 LLM，仅需训练一个极小的 MLP，适合快速部署。
*   **解耦性**：将“生成能力”（LLM）与“控制能力”（采样策略）解耦，允许针对不同用户需求定制不同的解码策略。
*   **动态性**：相比传统的固定参数，能够处理生成过程中的不确定性和风格漂移。

### 8. 不足与局限
*   **任务局限性**：实验仅集中在摘要任务，尚未在对话、代码生成或逻辑推理等更复杂的任务上验证。
*   **奖励依赖性**：RL 的效果高度依赖于人工设计的复合奖励函数，这在某些难以量化评价指标的任务中可能难以实施。
*   **模型规模限制**：测试的模型规模较小（最高 2B），在大参数量模型（如 70B+）上的表现和必要性尚不明确。
*   **分布外风险**：论文未深入探讨学习到的策略在面对完全不同分布的输入时的鲁棒性。

（完）
