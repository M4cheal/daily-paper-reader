---
title: Predictive Spectral Calibration for Source-Free Test-Time Regression
title_zh: 面向无源测试时回归的预测谱校准
authors: "Nguyen Viet Tuan Kiet, Huynh Thanh Trung, Pham Huy Hieu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09338v1"
tags: ["query:tta"]
score: 10.0
evidence: 无源测试时回归与光谱校准
tldr: 针对图像回归任务中测试时自适应（TTA）研究不足的问题，本文提出了一种名为预测谱校准（PSC）的无源框架。该方法将子空间对齐扩展到块谱匹配，通过在源预测支撑集内对齐目标特征，并校准正交补空间中的残余谱松弛，实现了对预训练回归器的有效优化。实验证明，PSC在多个基准测试中表现优异，尤其在处理严重分布偏移时具有显著优势。
selection_source: fresh_fetch
motivation: 现有的测试时自适应方法多针对分类任务，难以直接应用于具有连续目标的图像回归任务。
method: 提出预测谱校准（PSC）框架，通过块谱匹配同时对齐源预测支撑集内的特征并校准正交补空间。
result: 在多个图像回归基准测试中，PSC 相比强基准方法取得了持续的性能提升，特别是在极端分布偏移下表现更佳。
conclusion: PSC 是一种简单、模型无关且高效的无源回归自适应方案，为解决回归任务中的分布偏移问题提供了新思路。
---

## 摘要
图像回归的测试时自适应（TTA）所获得的关注远少于其分类对应任务。为分类设计的算法通常依赖于特定于分类的目标和决策边界，这使得它们难以直接迁移到连续的回归目标。最近的进展通过子空间对齐重新审视了回归 TTA，表明简单的源引导对齐既实用又有效。在此基础上，我们提出了预测谱校准（PSC），这是一个将子空间对齐扩展到块谱匹配的无源框架。PSC 不仅依赖于固定的支撑子空间，还联合对齐源预测支撑内的目标特征，并校准正交补空间中的残差谱松弛。PSC 易于实现、与模型无关，且与现成的预训练回归器兼容。在多个图像回归基准上的实验表明，该方法相比强基线模型有一致的提升，在严重的分布偏移下增益尤为明显。

## Abstract
Test-time adaptation (TTA) for image regression has received far less attention than its classification counterpart. Methods designed for classification often depend on classification-specific objectives and decision boundaries, making them difficult to transfer directly to continuous regression targets. Recent progress revisits regression TTA through subspace alignment, showing that simple source-guided alignment can be both practical and effective. Building on this line of work, we propose Predictive Spectral Calibration (PSC), a source-free framework that extends subspace alignment to block spectral matching. Instead of relying on a fixed support subspace alone, PSC jointly aligns target features within the source predictive support and calibrates residual spectral slack in the orthogonal complement. PSC remains simple to implement, model-agnostic, and compatible with off-the-shelf pretrained regressors. Experiments on multiple image regression benchmarks show consistent improvements over strong baselines, with particularly clear gains under severe distribution shifts.

---

## 论文详细总结（自动生成）

### 论文总结：面向无源测试时回归的预测谱校准 (PSC)

#### 1. 核心问题与研究动机
*   **核心问题**：现有的测试时自适应（TTA）方法大多针对分类任务（如熵最小化、伪标签），由于回归任务的目标是连续的，这些方法难以直接迁移。
*   **研究背景**：在实际部署中，模型常面临光照、天气或传感器变化导致的分布偏移。虽然最近的“显著子空间对齐”（SSA）方法在回归 TTA 上取得了进展，但它仅关注源域特征的主要子空间，忽略了正交补空间（残余空间）中的特征演变，这在严重偏移下会导致性能下降。
*   **研究目标**：提出一种无源（Source-Free）且模型无关的回归 TTA 框架，通过更全面的谱对齐来增强鲁棒性。

#### 2. 方法论：预测谱校准 (PSC)
PSC 的核心思想是将特征空间分解为**预测支撑集（Support）**和**残余补空间（Residual）**，并进行块谱匹配。
*   **块谱源模型**：利用源域特征的协方差矩阵进行特征值分解，提取前 $K$ 个主成分作为支撑子空间，其余部分视为各向同性的残余空间。
*   **支撑空间谱匹配**：引入了一个包含 $K^2$ 个探测向量的“探测库”（Probe Bank），不仅对齐坐标轴方向的方差，还通过探测向量对齐非对角线的协方差结构。
*   **残余空间谱匹配**：通过控制残余空间的特征质量（Spectral Slack），防止目标域特征在偏移下“泄漏”到与预测无关的维度中。
*   **优化目标**：最小化目标域与源域在两个空间中的对称 KL 散度（SKL）。在测试时，仅更新特征提取器的归一化层参数（如 BatchNorm 的 affine 参数），保持回归头固定。
*   **理论保证**：论文证明了 PSC 的优化目标能够直接收敛于预测均值漂移（Predictive Mean Drift）的上界，从而在理论上保证了鲁棒性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **跨域迁移**：从 SVHN（源域）到 MNIST（目标域），测试外观差异巨大的场景。
    *   **鲁棒性测试**：使用 UTKFace 数据集，涵盖 13 种常见的图像损坏（如高斯噪声、模糊、亮度变化、压缩等）。
*   **基准方法 (Benchmarks)**：
    *   **Source-only**：无自适应的预训练模型。
    *   **领域自适应 (DA)**：DANN, RSD, FR。
    *   **自监督训练 (SS)**：TTT, AM。
    *   **特征对齐 (FA)**：BNA, SSA（最强基线）。
    *   **Oracle**：使用目标域标签进行监督训练的上界。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：提到 PSC 易于实现且与现成的预训练模型兼容，通常只需更新归一化层的参数，暗示其计算开销较低，符合测试时自适应的实时性要求。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 UTKFace 上进行了 13 种不同损坏类型的详尽测试。
    *   进行了跨域（SVHN $\to$ MNIST）的对比实验。
    *   针对超参数 $\lambda$（平衡支撑空间与残余空间的权重）进行了消融分析。
*   **充分性评价**：实验设计较为充分，涵盖了从低级噪声到高级语义偏移的多种情况。对比方法包含了近年来主流的 DA 和 TTA 算法，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升**：PSC 在所有基准测试中均优于 SSA 和其他 TTA 方法。
*   **残余空间的重要性**：实验发现，在处理图像损坏（如噪声）时，加入残余空间校准（$\lambda=1$）效果更好；而在处理剧烈的语义跨域迁移时，侧重支撑空间对齐（$\lambda=0$）更为灵活。
*   **鲁棒性**：PSC 在极端分布偏移（如严重噪声）下的增益比轻微偏移时更显著。

#### 7. 优点
*   **理论严谨**：提供了关于识别性和预测漂移控制的数学证明。
*   **全面性**：相比 SSA，PSC 通过探测库和残余空间建模，实现了更精细的特征分布对齐。
*   **实用性**：无源（不需要原始训练数据）、模型无关、计算简便，适合实际部署。

#### 8. 不足与局限
*   **超参数敏感性**：$\lambda$ 的选择依赖于偏移的类型（是语义偏移还是环境噪声），在完全未知的测试环境下，如何自动选择最优 $\lambda$ 仍是一个挑战。
*   **回归头限制**：目前的理论和实验主要基于线性回归头，对于具有复杂非线性输出层的模型，其适用性有待进一步验证。
*   **数据集范围**：虽然涵盖了年龄估计和数字识别，但尚未在更大规模的 3D 姿态估计或深度估计任务上进行验证。

（完）
