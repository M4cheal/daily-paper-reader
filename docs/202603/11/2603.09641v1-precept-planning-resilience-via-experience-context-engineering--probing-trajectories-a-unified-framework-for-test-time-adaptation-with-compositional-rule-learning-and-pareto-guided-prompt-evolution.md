---
title: "PRECEPT: Planning Resilience via Experience, Context Engineering & Probing Trajectories A Unified Framework for Test-Time Adaptation with Compositional Rule Learning and Pareto-Guided Prompt Evolution"
title_zh: PRECEPT：通过经验、上下文工程与探测轨迹实现规划韧性——一个结合组合式规则学习与帕累托引导提示词演化的统一测试时自适应框架
authors: Arash Shahmansoori
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09641v1"
tags: ["query:tta"]
score: 9.0
evidence: LLM智能体的统一测试时自适应框架
tldr: 针对LLM智能体在处理多条件规则检索、规则组合及应对陈旧知识时的局限性，本文提出PRECEPT框架。该框架结合了确定性精确匹配检索、基于贝叶斯可靠性的冲突感知记忆以及Pareto引导的提示词演化算法（COMPASS）。实验表明，PRECEPT在组合泛化、持续学习和对抗鲁棒性方面表现优异，显著提升了智能体在复杂动态环境下的规划韧性。
selection_source: fresh_fetch
motivation: 解决LLM智能体在规则检索退化、规则组合不可靠以及缺乏对陈旧或对抗性知识检测机制的问题。
method: 采用确定性精确匹配检索、冲突感知记忆管理及COMPASS提示词演化 outer loop 构建统一的测试时自适应框架。
result: "在首试成功率上领先Full Reflexion 41.1个百分点，组合泛化提升33.3个百分点，并实现100%的漂移恢复与更少的执行步骤。"
conclusion: PRECEPT通过结构化规则学习与动态提示词演化，显著增强了LLM智能体在复杂任务中的规划能力与环境适应性。
---

## 摘要
以自然语言存储知识的 LLM 智能体在条件数量增加时面临严重的检索性能下降，通常难以可靠地组合已学习的规则，且往往缺乏检测陈旧或对抗性知识的显式机制。我们提出了 PRECEPT，这是一个用于测试时自适应的统一框架，包含三个紧密耦合的组件：(1) 基于结构化条件键的确定性精确匹配规则检索；(2) 具有贝叶斯来源可靠性和基于阈值的规则失效机制的冲突感知记忆；(3) COMPASS，一个帕累托引导的提示词演化外环。精确检索消除了确定性路径上的部分匹配解释错误（根据设计为 0%，而定理 B.6 的独立模型在 N=10 时为 94.4%），并通过语义层级结构支持组合式堆叠；冲突感知记忆解决了静态与动态知识之间的不一致，并支持漂移自适应；COMPASS 通过相同的端到端执行流水线评估提示词。实验结果（9-10 个随机种子）显示：PRECEPT 相比 Full Reflexion 取得了 +41.1pp 的首试优势 (d>1.9)，+33.3pp 的组合泛化提升 (d=1.55)，在双向物流组合上达到 100% 的 P1 (d=2.64)，+40-55pp 的持续学习增益，在对抗性静态知识下表现出强大的最终鲁棒性（在对抗性 SK 激活时物流任务达到 100%；集成任务部分恢复），+55.0pp 的漂移恢复 (d=0.95, p=0.031)，且步骤减少了 61%。核心对比具有统计学显著性，通常 p < 0.001。

## Abstract
LLM agents that store knowledge as natural language suffer steep retrieval degradation as condition count grows, often struggle to compose learned rules reliably, and typically lack explicit mechanisms to detect stale or adversarial knowledge. We introduce PRECEPT, a unified framework for test-time adaptation with three tightly coupled components: (1) deterministic exact-match rule retrieval over structured condition keys, (2) conflict-aware memory with Bayesian source reliability and threshold-based rule invalidation, and (3) COMPASS, a Pareto-guided prompt-evolution outer loop. Exact retrieval eliminates partial-match interpretation errors on the deterministic path (0% by construction, vs 94.4% under Theorem~B.6's independence model at N=10) and supports compositional stacking through a semantic tier hierarchy; conflict-aware memory resolves static--dynamic disagreements and supports drift adaptation; COMPASS evaluates prompts through the same end-to-end execution pipeline.   Results (9--10 seeds): PRECEPT achieves a +41.1pp first-try advantage over Full Reflexion (d>1.9), +33.3pp compositional generalization (d=1.55), 100% $P_1$ on 2-way logistics compositions (d=2.64), +40--55pp continuous learning gains, strong eventual robustness under adversarial static knowledge (100% logistics with adversarial SK active; partial recovery on integration), +55.0pp drift recovery (d=0.95, p=0.031), and 61% fewer steps. Core comparisons are statistically significant, often at p<0.001.

---

## 论文详细总结（自动生成）

### PRECEPT 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对当前大语言模型（LLM）智能体在复杂规划任务中的四大核心局限性：
*   **组合爆炸（L1）：** $N$ 个原子条件对应 $2^N$ 种组合，传统方法难以泛化。
*   **解释退化（L2）：** 随着条件增加，基于自然语言的检索准确率呈指数级下降。
*   **漂移盲目（L3）：** 无法有效识别和处理环境变化导致的陈旧规则或对抗性知识。
*   **样本低效（L4）：** 强化学习（RL）需要大量样本，而部署环境要求极低样本学习。

**PRECEPT** 旨在构建一个统一的测试时自适应（Test-Time Adaptation, TTA）框架，通过结构化规则学习和确定性检索，提升智能体在动态、复杂环境下的规划韧性。

#### 2. 方法论：核心思想与关键技术
PRECEPT 由三个紧密耦合的组件构成：
*   **确定性精确匹配检索（Deterministic Retrieval）：** 弃用模糊的语义检索，采用基于结构化条件键的 $O(1)$ 哈希查找。通过**语义层级结构（Semantic Tier Hierarchy）**（安全 > 合规 > 偏好）实现原子规则的组合堆叠，支持从 $N$ 个规则生成 $2^N-1$ 种组合覆盖。
*   **冲突感知记忆与进化记忆（Evo-Memory）：**
    *   **Type I 冲突（静态 vs 动态）：** 使用贝叶斯来源可靠性模型（Beta 分布）和汤普森采样（Thompson Sampling）来解决预设知识与实时经验的不一致。
    *   **Type II 冲突（环境漂移）：** 引入基于阈值的规则失效机制（如连续 2 次失败则删除规则）和置信度衰减。
*   **COMPASS 提示词演化外环：**
    *   **双频率控制：** 高频监控（每步执行）与低频演化（事件触发）。
    *   **帕累托引导（Pareto-Guided）：** 在成功率和步骤效率之间进行多目标优化。
    *   **多样性维护：** 借鉴 MAP-Elites 算法确保提示词策略的多样性，防止收敛到单一失效策略。
*   **RefineInterceptor：** 确定性剪枝机制，确保智能体在单次任务中绝不重复已失败的尝试（$P(\text{repeat\_fail}) = 0$）。

#### 3. 实验设计
*   **实验场景：** 选择了三个具有挑战性的隐藏约束满足问题域：
    1.  **物流（Logistics）：** 港口关闭、海关延迟等组合条件。
    2.  **预订（Booking）：** 复杂的航班/酒店预订失败处理。
    3.  **集成（Integration）：** API 编排中的 OAuth 失败和命名陷阱（最难，涉及 LLM 先验偏差）。
*   **基准方法（Benchmarks）：** 增强版的 **Full Reflexion**（带向量库和结构化提示）和 **ExpeL**（带经验积累机制）。
*   **评估指标：** 首试成功率（$P_1$）、最终成功率（$P_t$）、平均执行步骤。

#### 4. 资源与算力
*   **模型使用：** 实验统一使用 **GPT-4o-mini** 作为推理引擎，**text-embedding-3-small** 用于向量嵌入。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号、数量或总训练时长。由于该框架侧重于测试时自适应和上下文学习（In-context Learning），其核心开销在于 API 调用和本地轻量级贝叶斯计算，而非大规模模型训练。

#### 5. 实验数量与充分性
*   **实验规模：** 进行了 9 组核心实验，涵盖主领域对比、组合泛化、训练规模消融（$\beta$ 效应）、持续学习、规则持久性、对抗性知识消融及漂移自适应。
*   **充分性：** 每组实验运行 9-10 个独立随机种子，提供了 95% 置信区间和效应量（Cohen's $d$）。
*   **客观性：** 实验设计包含了“黑天鹅”错误代码和对抗性静态知识，专门测试系统在极端情况下的表现，统计显著性分析（$p < 0.001$）增强了结论的可信度。

#### 6. 主要结论与发现
*   **性能优势：** PRECEPT 在首试成功率上比 Full Reflexion 高出 **41.1pp**，步骤减少 **61%**。
*   **组合泛化：** 仅通过原子规则学习，在复杂组合任务中表现出显著优势（+33.3pp）。
*   **漂移恢复：** 在环境发生剧变（Drift）时，PRECEPT 的恢复速度远超基准方法，物流任务中实现 **55.0pp** 的恢复增益。
*   **对抗鲁棒性：** 即使在被注入错误静态知识的情况下，通过贝叶斯冲突解决机制，最终仍能达到 100% 的成功率。

#### 7. 优点（亮点）
*   **理论完备性：** 将进化计算中的“数字红皇后（Digital Red Queen）”理论引入 LLM 智能体，提供了确定性剪枝和循环失败消除的数学证明。
*   **架构创新：** 成功将确定性的符号逻辑（哈希检索、层级堆叠）与概率性的神经推理（LLM）结合。
*   **实用性：** 解决了 LLM 容易被自身先验知识误导（如 Integration 任务中的命名陷阱）的问题。

#### 8. 不足与局限
*   **基准局限：** 实验主要在合成的、受控的基准域上进行，尚未在真实世界的大规模非结构化生产环境中验证。
*   **表示假设：** 假设环境条件可以被离散化和规范化（Token 化），对于连续、高维的特征空间，可能需要额外的预处理（如分箱或编码）。
*   **规模扩展：** 虽然检索是 $O(1)$，但全覆盖训练仍随条件键数量线性增长，在极大规模（$E \gg 1000$）条件下的表现有待观察。

（完）
