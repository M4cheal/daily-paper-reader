---
title: "TALON: Test-time Adaptive Learning for On-the-Fly Category Discovery"
title_zh: TALON：针对即时类别发现的测试时自适应学习
authors: "Yanan Wu, Yuhan Yan, Tailai Chen, Zhixiang Chi, ZiZhang Wu, Yi Jin, Yang Wang, Zhenbo Li"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08075v1"
tags: ["query:tta"]
score: 9.0
evidence: 针对无标签在线流数据的测试时自适应学习
tldr: 针对在线类别发现（OCD）中固定特征提取器导致的知识更新受限及类别爆炸问题，本文提出TALON框架。该框架通过测试时自适应技术，结合语义感知原型更新和稳定编码器更新，使模型能从在线流数据中持续学习。此外，引入离线边际感知逻辑校准以优化嵌入空间。实验证明，TALON在提升新类别准确率和抑制类别碎片化方面显著优于现有哈希方法。
selection_source: fresh_fetch
motivation: 现有OCD方法因冻结特征提取器和特征量化导致信息丢失，且无法从新数据中学习，容易引发严重的类别爆炸问题。
method: 提出一种结合语义感知原型动态更新、稳定测试时编码器更新以及离线边际感知逻辑校准的自适应学习框架。
result: 在标准OCD基准测试中，该方法显著提升了新类别的识别准确率，并有效缓解了类别碎片化现象。
conclusion: TALON通过测试时自适应学习机制，成功实现了在动态数据流中对新旧类别的持续发现与高效识别。
---

## 摘要
即时类别发现（OCD）旨在利用仅在有标签数据上训练的模型，在识别已知类别的同时，从无标签在线流中发现新类别。现有方法通常冻结离线训练的特征提取器，并采用基于哈希的框架将特征量化为二进制代码作为类别原型。然而，使用固定的知识库来发现新类别是违反直觉的，因为这完全忽略了输入数据的学习潜力。此外，特征量化会导致信息损失、降低表示能力并放大类内方差，往往引发“类别爆炸”问题，即单个类别被分割成多个伪类别。为了克服这些局限性，我们提出了一种通过发现实现学习的测试时自适应框架。该框架包含两种互补策略：语义感知原型更新和稳定的测试时编码器更新。前者动态优化类别原型以增强分类效果，而后者将新信息直接整合到参数空间中。这些组件共同使模型能够利用新遇到的样本不断扩展其知识库。此外，我们在离线阶段引入了边际感知逻辑校准，以扩大类间边际并提高类内紧凑性，从而为未来的类别发现预留嵌入空间。在标准 OCD 基准上的实验表明，我们的方法显著优于现有的基于哈希的最先进方法，在新类别准确率方面取得了显著提升，并有效缓解了类别爆炸问题。代码已在 https://github.com/ynanwu/TALON 公开。

## Abstract
On-the-fly category discovery (OCD) aims to recognize known categories while simultaneously discovering novel ones from an unlabeled online stream, using a model trained only on labeled data. Existing approaches freeze the feature extractor trained offline and employ a hash-based framework that quantizes features into binary codes as class prototypes. However, discovering novel categories with a fixed knowledge base is counterintuitive, as the learning potential of incoming data is entirely neglected. In addition, feature quantization introduces information loss, diminishes representational expressiveness, and amplifies intra-class variance. It often results in category explosion, where a single class is fragmented into multiple pseudo-classes. To overcome these limitations, we propose a test-time adaptation framework that enables learning through discovery. It incorporates two complementary strategies: a semantic-aware prototype update and a stable test-time encoder update. The former dynamically refines class prototypes to enhance classification, whereas the latter integrates new information directly into the parameter space. Together, these components allow the model to continuously expand its knowledge base with newly encountered samples. Furthermore, we introduce a margin-aware logit calibration in the offline stage to enlarge inter-class margins and improve intra-class compactness, thereby reserving embedding space for future class discovery. Experiments on standard OCD benchmarks demonstrate that our method substantially outperforms existing hash-based state-of-the-art approaches, yielding notable improvements in novel-class accuracy and effectively mitigating category explosion. The code is publicly available at \textcolor{blue}{https://github.com/ynanwu/TALON}.

---

## 论文详细总结（自动生成）

# TALON：针对即时类别发现的测试时自适应学习论文总结

### 1. 核心问题与整体含义
论文关注的是**即时类别发现（On-the-Fly Category Discovery, OCD）**任务。在现实场景中，模型需要在处理未知的在线数据流时，既能识别已知的类别，又能动态地发现并区分新的类别。

**研究动机：**
*   **现有方法的局限：** 现有的 OCD 方法（如 SMILE, PHE）通常在离线训练后冻结特征提取器，并使用哈希技术将特征量化为二进制代码。
*   **两大痛点：** 
    1.  **静态推理：** 冻结模型使其无法从新遇到的数据中吸收知识，难以应对语义偏移。
    2.  **类别爆炸：** 哈希量化导致严重的信息丢失和类内方差放大，使得模型倾向于将同一个真实类别拆分为多个“伪类别”。

### 2. 方法论
TALON 提出了一个**测试时自适应（Test-Time Adaptation, TTA）**框架，核心思想是“在发现中学习”。

**关键技术细节：**
*   **离线阶段：边际感知逻辑校准（Margin-aware Logit Calibration, MLC）**
    *   在训练已知类时，在交叉熵损失中引入角度边际（Angular Margin）。
    *   **作用：** 压缩已知类的特征空间，增强类内紧凑性和类间分离度，从而为未来新类别的嵌入预留出“空白”空间。
*   **在线阶段：双重自适应机制**
    1.  **语义感知原型更新（TTA-P）：** 当新样本被分配给某个原型时，利用置信度控制的指数移动平均（EMA）动态调整该原型。更新步长由样本数量和分配置信度共同决定，确保更新的稳定性。
    2.  **稳定编码器更新（TTA-M）：** 定期收集一小批样本，通过最小化**熵损失**（提高预测确定性）、**对齐损失**（使特征向原型靠拢）和**分离损失**（防止不同类原型塌陷）来微调编码器参数。
*   **哈希自由（Hash-free）架构：** 舍弃了启发式的哈希编码，直接在连续的特征空间进行操作，保留了高维特征的表达力。

### 3. 实验设计
*   **数据集：** 涵盖了 7 个标准基准，包括粗粒度（CIFAR10, CIFAR100, ImageNet-100）和细粒度（CUB-200, Stanford Cars, Oxford Pets, Food-101）场景。
*   **Benchmark 与指标：** 采用 Greedy-Hungarian 和 Strict-Hungarian 协议，评估已知类（Old）、新类（New）及全类（All）的准确率。
*   **对比方法：** 与 SLC, MLDG, RankStat, WTA, SMILE, PHE, DiffGRE 等多种 SOTA 方法进行了全面对比。

### 4. 资源与算力
*   **硬件环境：** 使用单张 **NVIDIA RTX 3090 GPU (24GB)**。
*   **训练细节：** 离线训练 100 个 epoch，使用 AdamW 优化器。
*   **效率表现：** 论文明确给出了训练时间对比。TALON 的训练效率显著高于 SMILE 和 PHE（例如在 Food-101 数据集上，TALON 训练耗时约 12157s，而 SMILE 需 27000s），这得益于其更简洁的特征学习策略。

### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **多维度对比：** 在 7 个数据集上验证了性能，且针对 DINO 和 CLIP 两种不同的预训练 Backbone 均做了测试。
*   **消融实验：** 逐一验证了 MLC、原型更新（TTA-P）和编码器更新（TTA-M）的贡献。
*   **鲁棒性分析：** 详细分析了超参数（如边际 $m$、相似度阈值 $\tau$、Batch Size 等）对结果的影响。
*   **类别估计分析：** 专门对比了模型估计的类别数量，证明了 TALON 能有效抑制“类别爆炸”现象，估计的类别数远比哈希方法准确。

### 6. 主要结论与发现
*   **性能领先：** TALON 在所有数据集上均显著优于现有的基于哈希的方法，尤其是在新类别（New Class）的识别准确率上提升巨大。
*   **自适应的重要性：** 实验证明，在测试阶段动态更新模型参数和原型，对于处理在线流数据中的语义偏移至关重要。
*   **哈希并非必要：** 直接在连续特征空间操作不仅能提升准确率，还能极大地增强类别发现的稳定性。

### 7. 优点
*   **创新性：** 首次将 TTA 引入 OCD 任务，打破了以往 OCD 研究中模型必须冻结的思维定式。
*   **解决痛点：** 通过 MLC 和 TTA 的结合，系统性地解决了类别爆炸和知识更新停滞的问题。
*   **高效性：** 相比于复杂的生成式方法（如 DiffGRE）或双视图对比学习方法，TALON 的训练和推理逻辑更直接且高效。

### 8. 不足与局限
*   **计算开销：** 虽然训练高效，但在推理阶段引入了参数更新（TTA），这会比纯静态推理消耗更多的计算资源和时间。
*   **依赖 Backbone：** 性能在很大程度上依赖于预训练模型（如 CLIP 或 DINOv2）的初始质量。
*   **极端流场景：** 对于类别分布极度不平衡或类别数量极大的极端长尾在线流，原型的维护和更新策略可能仍面临挑战。

（完）
