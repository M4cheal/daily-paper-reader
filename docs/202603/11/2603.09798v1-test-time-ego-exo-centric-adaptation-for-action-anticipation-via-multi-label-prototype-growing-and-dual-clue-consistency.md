---
title: Test-time Ego-Exo-centric Adaptation for Action Anticipation via Multi-Label Prototype Growing and Dual-Clue Consistency
title_zh: 基于多标签原型增长与双重线索一致性的动作预测测试时第一人称与第三人称视角自适应
authors: "Zhaofeng Shi, Heqian Qiu, Lanxiao Wang, Qingbo Wu, Fanman Meng, Lili Pan, Hongliang Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09798v1"
tags: ["query:tta"]
score: 10.0
evidence: 跨视图动作预测的在线测试时自适应
tldr: 本研究针对人机协作中第一人称（Ego）与第三人称（Exo）视角转换的动作预测问题，首次提出了测试时视角自适应任务（TE2A3）。为解决多动作候选及视角间巨大的时空差异，提出了双线索增强原型增长网络（DCPGN）。该方法通过多标签原型增长模块积累知识，并利用轻量级叙述者生成的文本线索与视觉线索构建双线索一致性，实现了高效的在线模型调整。在多个基准数据集上表现优异，显著提升了跨视角动作预测的准确性。
selection_source: fresh_fetch
motivation: 现有的视角自适应方法过度依赖目标视图训练数据，且难以应对测试时多动作候选及显著的视角间时空差异。
method: 提出了DCPGN网络，结合了用于多标签知识积累的原型增长模块（ML-PGM）和利用文本与视觉线索构建一致性的双线索一致性模块（DCCM）。
result: 在EgoMe-anti和EgoExoLearn基准测试中，该方法显著优于现有的最先进测试时自适应方法。
conclusion: 本研究通过引入双线索一致性和多标签原型管理，成功解决了测试时跨视角动作预测的挑战，为视角自适应领域提供了新思路。
---

## 摘要
第一人称（Ego）与第三人称（Exo）视角之间的高效自适应对于人机协作等应用至关重要。然而，大多数现有的 Ego-Exo 自适应方法的成功在很大程度上依赖于目标视角数据的训练，从而增加了计算和数据收集成本。在本文中，我们首次探索了动作预测的测试时 Ego-Exo 自适应（TE²A³）任务，旨在测试阶段在线调整在源视角上训练的模型，以预测目标视角的动作。由于存在多个候选动作以及显著的时空跨视角差异，现有的测试时自适应（TTA）方法难以应对这一任务。因此，我们提出了一种新型的双重线索增强原型增长网络（DCPGN），它通过积累多标签知识并整合跨模态线索，实现有效的测试时 Ego-Exo 自适应和动作预测。具体而言，我们提出了一个多标签原型增长模块（ML-PGM），通过多标签分配和基于置信度的重加权来平衡多个正类别，并利用熵优先级队列策略更新类别内存库。随后，双重线索一致性模块（DCCM）引入了一个轻量级叙述器来生成指示动作进展的文本线索，这些线索与包含各种物体的视觉线索互补。此外，我们约束推断出的文本和视觉逻辑值（logits）以构建双重线索一致性，从而在时间和空间上桥接 Ego 和 Exo 视角。在新提出的 EgoMe-anti 和现有的 EgoExoLearn 基准测试上进行的大量实验证明了我们方法的有效性，其性能大幅超过了相关的最先进方法。代码可在 https://github.com/ZhaofengSHI/DCPGN 获取。

## Abstract
Efficient adaptation between Egocentric (Ego) and Exocentric (Exo) views is crucial for applications such as human-robot cooperation. However, the success of most existing Ego-Exo adaptation methods relies heavily on target-view data for training, thereby increasing computational and data collection costs. In this paper, we make the first exploration of a Test-time Ego-Exo Adaptation for Action Anticipation (TE$^{2}$A$^{3}$) task, which aims to adjust the source-view-trained model online during test time to anticipate target-view actions. It is challenging for existing Test-Time Adaptation (TTA) methods to address this task due to the multi-action candidates and significant temporal-spatial inter-view gap. Hence, we propose a novel Dual-Clue enhanced Prototype Growing Network (DCPGN), which accumulates multi-label knowledge and integrates cross-modality clues for effective test-time Ego-Exo adaptation and action anticipation. Specifically, we propose a Multi-Label Prototype Growing Module (ML-PGM) to balance multiple positive classes via multi-label assignment and confidence-based reweighting for class-wise memory banks, which are updated by an entropy priority queue strategy. Then, the Dual-Clue Consistency Module (DCCM) introduces a lightweight narrator to generate textual clues indicating action progressions, which complement the visual clues containing various objects. Moreover, we constrain the inferred textual and visual logits to construct dual-clue consistency for temporally and spatially bridging Ego and Exo views. Extensive experiments on the newly proposed EgoMe-anti and the existing EgoExoLearn benchmarks show the effectiveness of our method, which outperforms related state-of-the-art methods by a large margin. Code is available at \href{https://github.com/ZhaofengSHI/DCPGN}{https://github.com/ZhaofengSHI/DCPGN}.

---

## 论文详细总结（自动生成）

### 论文总结：基于多标签原型增长与双重线索一致性的动作预测测试时视角自适应

#### 1. 核心问题与整体含义（研究动机和背景）
本文首次提出了**动作预测的测试时第一人称与第三人称视角自适应（TE²A³）**任务。
*   **研究背景**：在人机协作和具身智能中，模型需要能够从第三人称（Exo，观察者）视角理解动作并切换到第一人称（Ego，执行者）视角进行预测。
*   **核心挑战**：
    1.  **视角差异巨大**：Ego 和 Exo 视角在空间布局（物体外观）和时间进度（动作异步）上存在显著鸿沟。
    2.  **多动作候选**：现实场景中一个事件往往包含多个原子动作，而传统测试时自适应（TTA）方法多针对单标签分类，容易产生预测偏差。
    3.  **在线自适应需求**：现有方法多依赖目标视角的离线训练或无监督域自适应（UDA），成本高且难以实时部署。

#### 2. 方法论：核心思想与技术细节
论文提出了一种**双重线索增强原型增长网络（DCPGN）**，主要包含两个核心模块：
*   **多标签原型增长模块 (ML-PGM)**：
    *   **多标签分配**：针对目标视角数据，分配 Top-K 伪标签而非单标签，以平衡多个潜在的正类别。
    *   **熵优先级队列更新**：建立类别内存库（Memory Banks），利用预测熵值作为可靠性指标，通过优先级队列动态更新最可靠的特征表示。
    *   **置信度重加权原型**：在计算类别原型时，根据置信度对内存库中的特征进行加权，减少错误伪标签的干扰。
*   **双重线索一致性模块 (DCCM)**：
    *   **跨模态线索挖掘**：引入一个轻量级叙述器（Narrator）生成描述动作进度的**文本线索**（补充时间信息），并结合当前帧的**视觉线索**（提供空间物体信息）。
    *   **双重线索一致性约束**：利用预训练的 CLIP 模型，将文本和视觉线索分别映射到动作类别空间，并通过 **KL 散度** 约束两者的逻辑值（logits）分布一致，从而在时空维度上桥接视角鸿沟。

#### 3. 实验设计
*   **数据集与场景**：
    *   **EgoMe-anti**：基于 EgoMe 数据集构建的新基准，包含 7902 对 Exo 观察者和 Ego 追随者的视频，涵盖 41 个真实生活场景。
    *   **EgoExoLearn**：现有的跨视角异步动作预测基准，包含约 120 小时的日常及专业操作视频。
*   **Benchmark 设置**：分为 **Exo2Ego**（从第三人称迁移到第一人称）和 **Ego2Exo** 两种设置。
*   **对比方法**：对比了 7 种最先进的 TTA 方法，包括 Tent、TPT、VITTA、TDA、ZERO、TCA 和针对多标签的 ML-TTA。
*   **评价指标**：类别平均 Top-5 召回率（Recall@5）和 Top-1 召回率。

#### 4. 资源与算力
*   **模型骨干**：使用预训练的 **CLIP (ViT-L/14)** 作为视觉编码器。
*   **优化器**：使用 SGD 优化器进行在线更新。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量及总训练/推理时长。但提到轻量级叙述器和特征提取的 FLOPs（分别为 0.03G 和 4.06G），强调了其在测试时的计算开销是可接受的。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了详尽的对比实验。
*   **消融实验**：针对双重线索一致性损失（$L_C$）、视觉线索（VC）、文本线索（TC）、置信度重加权（Conf）和多标签分配（ML）等组件进行了逐一验证。
*   **深入分析**：包括超参数 $K$（伪标签数量）和 $N$（内存库容量）的敏感性分析、模型复杂度分析、t-SNE 特征可视化以及定性预测结果可视化。
*   **客观性**：实验设置遵循了 TTA 的标准协议（在线流式处理，不接触目标域标签），对比方法均采用官方代码重现，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：DCPGN 在所有基准测试中均显著优于现有 SOTA。例如在 EgoExoLearn 的 Exo2Ego 任务中，名词预测比 ML-TTA 提升了 **9.91%**。
*   **多标签的重要性**：在动作预测任务中，Top-K 伪标签分配比单标签分配能更有效地缓解模型对高置信度类别的过度偏好。
*   **跨模态互补性**：视觉线索擅长捕捉空间物体，而文本线索能更好地指示动作的时间进度，两者的结合对跨视角自适应至关重要。

#### 7. 优点
*   **任务创新**：首次定义并探索了 TE²A³ 这一具有高度实用价值的视角自适应任务。
*   **方案全面**：同时考虑了多标签不平衡问题和跨视角的时空鸿沟，设计了针对性的原型增长和双线索一致性机制。
*   **即插即用**：作为 TTA 方法，不需要目标域的离线训练，具有较强的实际应用潜力。

#### 8. 不足与局限
*   **叙述器依赖**：虽然叙述器是轻量级的，但它仍需要在其他带有文本标注的数据集上进行预训练，这引入了一定的额外数据依赖。
*   **超参数敏感性**：超参数 $K$（伪标签数）在不同数据集上表现不同（EgoMe-anti 为 3，EgoExoLearn 为 5），可能需要根据场景先验进行微调。
*   **实时性挑战**：虽然计算量可控，但在极低延迟要求的嵌入式设备上，同时运行视觉编码器、叙述器和 CLIP 文本/视觉分支可能仍存在压力。

（完）
