---
title: "Med-Evo: Test-time Self-evolution for Medical Multimodal Large Language Models"
title_zh: Med-Evo：面向医疗多模态大语言模型的测试时自我进化
authors: "Dunyuan Xu, Xikai Yang, Juzheng Miao, Yaoqian Li, Jinpeng Li, Pheng-Ann Heng"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07443v1"
tags: ["query:tta"]
score: 10.0
evidence: 利用未标记测试数据进行医学多模态大语言模型的测试时自我进化
tldr: 针对医疗多模态大模型（MLLM）对标注数据依赖性强且获取成本高的问题，本文提出了Med-Evo框架。这是首个利用无标签测试数据进行自我进化的医疗MLLM框架，通过特征驱动伪标签（FPL）和硬软结合奖励（HSR）机制，在无需额外标注的情况下实现模型性能的持续提升。实验表明，该方法在多个医疗VQA基准上显著优于现有SOTA方法，展现了强大的自我进化能力。
selection_source: fresh_fetch
motivation: 医疗领域标注数据稀缺且敏感，现有微调方法难以利用无标签测试数据来进一步增强模型性能。
method: 提出Med-Evo框架，利用特征驱动伪标签筛选可靠信号，并结合硬匹配与语义相似度构建分层奖励机制。
result: "在多个医疗VQA任务中取得显著提升，例如在SLAKE数据集上将Qwen2.5-VL的准确率提高了10.43%。"
conclusion: Med-Evo证明了医疗MLLM可以通过无标签数据的自我进化有效提升性能，为缓解医疗数据标注压力提供了新路径。
---

## 摘要
医疗多模态大语言模型（MLLMs）在多种医疗保健任务中展现出了卓越的能力。然而，现有的后训练策略（如监督微调和强化学习）严重依赖大量的标注数据，而忽视了利用无标注测试数据进行模型增强的潜力。这种局限性在医疗领域尤为显著，因为受限于严格的数据敏感性和标注复杂性，获取大规模标注医疗数据十分困难。此外，利用测试数据在从无标注样本中生成可靠的监督信号以及维持稳定的自我进化方面也存在挑战。为了应对这些挑战，我们提出了 Med-Evo，这是首个针对医疗 MLLMs 的自我进化框架，它利用无标签强化学习在无需额外标注数据的情况下提升模型性能。我们的框架引入了两项关键创新：1) 特征驱动伪标注（FPL），通过从所有异构候选响应中识别语义质心，在每次 rollout 中选择伪标签；2) 硬-软奖励（HSR），结合精确匹配、词元级评估和语义相似度，提供分层奖励。在三个医疗 VQA 基准测试和两个基础 MLLMs 上的实验表明，我们的方法相较于 SOTA 方法具有明显优势，在使用 Qwen2.5-VL 的 SLAKE 数据集上，准确率和召回率分别显著提升了 10.43% 和 4.68%，证明了该方法的有效性。

## Abstract
Medical Multimodal Large Language Models (MLLMs) have demonstrated remarkable capabilities across diverse healthcare tasks. However, current post-training strategies, such as supervised fine-tuning and reinforcement learning, heavily depend on substantial annotated data while overlooking the potential of unlabeled test data for model enhancement. This limitation becomes particularly pronounced in medical domains, where acquiring extensive labeled medical data is difficult due to the strict data sensitivity and annotation complexity. Moreover, leveraging test data poses challenges in generating reliable supervision signals from unlabeled samples and maintaining stable self-evolution. To address these limitations, we propose Med-Evo, the first self-evolution framework for medical MLLMs that utilizes label-free reinforcement learning to promote model performance without requiring additional labeled data. Our framework introduces two key innovations: $1)$ Feature-driven Pseudo Labeling (FPL) that identifies semantic centroids from all heterogeneous candidate responses to select pseudo labels in each rollout, and $2)$ Hard-Soft Reward (HSR) that combines exact match with token-level assessment and semantic similarity to provide hierarchical reward. Experiments on three medical VQA benchmarks and two base MLLMs show clear advantages of our approach over SOTA methods, with significant improvements of 10.43\% accuracy and 4.68\% recall on the SLAKE dataset using Qwen2.5-VL, showing the effectiveness of our method.

---

## 论文详细总结（自动生成）

这是一份关于论文《Med-Evo: Test-time Self-evolution for Medical Multimodal Large Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的医疗多模态大语言模型（MLLM）高度依赖大规模标注数据进行微调或强化学习。然而，医疗数据具有极高的**敏感性**和**标注复杂性**，导致高质量标注数据稀缺。
*   **研究动机**：
    *   **忽视测试数据**：现有方法通常只在训练阶段优化，忽略了在推理（测试）阶段利用无标签数据进行自我提升的潜力。
    *   **伪标签不可靠**：传统的测试时训练（TTT）多采用“多数投票法”生成伪标签，但在复杂的医疗推理中，模型输出往往具有高度异构性，难以形成简单多数。
    *   **奖励机制单一**：现有的二元奖励（对/错）无法捕捉医疗回答中的语义相似性和部分正确性，导致学习信号微弱。

### 2. 方法论：Med-Evo 框架
Med-Evo 是首个针对医疗 MLLM 的测试时自我进化框架，其核心流程包含四个阶段：
*   **特征驱动伪标注 (FPL)**：
    *   **Rollout 生成**：对每个测试样本进行 $N$ 次随机采样，生成多个候选回答。
    *   **语义质心定位**：利用语义编码器（如 RoBERTa）将所有回答转化为高维向量，计算这些向量的平均值作为“语义质心”。
    *   **伪标签选择**：选择距离质心最近的候选回答作为该样本的伪标签，相比多数投票法，这能更好地处理表达多样但语义集中的情况。
*   **硬-软奖励机制 (HSR)**：
    *   **硬奖励 (Hard)**：精确匹配奖励（Binary Reward），确保词汇层面的准确性。
    *   **软奖励 (Soft)**：结合了 **Jaccard 相似度**（词元重叠度）和**编码器派生的语义相似度**（向量空间距离），为语义相近但表达不同的回答提供细粒度反馈。
*   **基于 GRPO 的自我进化**：
    *   采用**组相对策略优化 (GRPO)** 算法，通过计算组内相对优势（Advantage）来更新模型参数。
    *   引入 KL 散度约束，防止模型在进化过程中偏离原始分布，确保训练的稳定性。

### 3. 实验设计
*   **数据集**：使用了三个主流医疗 VQA 基准：**SLAKE**（语义标注知识增强数据集）、**VQA-Rad**（放射学图像问答）和 **VQA-Med**（医学图像问答）。
*   **基础模型**：验证了通用模型 **Qwen2.5-VL-3B-Instruct** 和医疗专用模型 **MedVLM-R1**。
*   **对比方法 (Baselines)**：
    *   **EN-INF**：推理时熵最小化方法。
    *   **TTRV**：基于频率和熵的视觉语言模型测试时强化学习。
    *   **TTRL**：基于多数投票和强化学习的测试时训练方法。
    *   **Base Model**：直接推理，不进行任何测试时适配。

### 4. 资源与算力
*   **硬件环境**：实验在 **4 台 NVIDIA RTX A6000 GPU** 上运行。
*   **超参数**：学习率设为 $5 \times 10^{-7}$，Batch Size 为 4。
*   **采样规模**：每次 Rollout 生成 32 个响应用于伪标签选择，其中前 16 个用于 GRPO 优化。
*   **训练时长**：文中未明确给出总训练时长，但提到在测试时进行迭代优化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个数据集、2 种不同性质的基础模型，并进行了详尽的消融实验。
*   **充分性**：
    *   **消融实验**：分别验证了 FPL 和 HSR 的增益，证明了各组件的必要性。
    *   **命中率分析**：对比了 FPL 与多数投票法的伪标签准确率（Hit Rate），结果显示 FPL 在不同采样数下均表现更优。
    *   **进化过程可视化**：展示了奖励分数与模型性能（准确率、召回率）随进化步骤同步上升的趋势。
*   **客观性**：使用了 Accuracy（闭口题）、Recall 和 ROUGE-1（开口题）等多种指标，评价维度全面。

### 6. 主要结论与发现
*   **性能显著提升**：在 SLAKE 数据集上，Med-Evo 将 Qwen2.5-VL 的准确率提升了 **10.43%**，召回率提升了 **4.68%**。
*   **跨模型通用性**：无论是通用大模型还是经过医疗微调的模型，Med-Evo 都能在无标签测试数据上实现进一步的性能飞跃。
*   **自我进化闭环**：实验证明模型可以通过自身生成的反馈信号实现“左脚踩右脚”式的性能爬升，有效缓解了医疗领域对人工标注的依赖。

### 7. 优点与亮点
*   **首创性**：首次将测试时自我进化（Test-time Self-evolution）引入医疗 MLLM 领域。
*   **语义驱动**：摒弃了机械的字符串匹配，利用语义向量空间（FPL）和分层奖励（HSR）处理医疗术语的多样性。
*   **标签无关**：完全不需要额外的地面真值（Ground Truth），极具临床实用价值，尤其适用于隐私敏感的医疗场景。

### 8. 不足与局限
*   **计算开销**：测试时需要进行多次 Rollout 采样和参数更新，这会显著增加推理延迟，可能不适用于对实时性要求极高的临床场景。
*   **语义编码器依赖**：FPL 和 HSR 的效果高度依赖于预训练语义编码器（如 RoBERTa）的质量，若编码器无法准确捕捉特定医学领域的深层语义，可能引入偏差。
*   **任务局限**：目前主要在 VQA 任务上验证，对于更复杂的医疗报告生成或长文本推理任务的效果尚待观察。

（完）
