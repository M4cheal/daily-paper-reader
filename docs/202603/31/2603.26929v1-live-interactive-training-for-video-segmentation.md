---
title: Live Interactive Training for Video Segmentation
title_zh: 视频分割的实时交互式训练
authors: "Xinyu Yang, Haozheng Yu, Yihong Sun, Bharath Hariharan, Jennifer J. Sun"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26929v1"
tags: ["query:tta"]
score: 9.0
evidence: 推理时通过LoRA从人类修正中进行在线学习
tldr: "针对交互式视频分割中用户反馈利用率低、导致重复劳动的问题，本文提出了一种名为LIT（Live Interactive Training）的新型框架。该框架允许模型在推理过程中通过在线学习人类的修正反馈来实时优化。具体实现LIT-LoRA通过在用户提供修正时快速更新轻量级LoRA模块，显著减少了后续帧的错误。实验表明，该方法在多个基准测试中减少了18-34%的修正次数，且每步训练开销极低，展示了实时自适应在降低人工成本方面的巨大潜力。"
selection_source: fresh_fetch
motivation: 现有的交互式分割模型仅将用户修正用于即时修复，而无法从中学习，导致在处理复杂场景时需要大量重复的人工干预。
method: 提出了LIT框架及其具体实现LIT-LoRA，通过在推理阶段实时更新轻量级的LoRA模块，使模型能够从用户的在线反馈中持续学习。
result: "在挑战性的视频分割基准上，该方法将总修正次数减少了18-34%，且每次修正仅增加约0.5秒的极小训练开销。"
conclusion: 实时在线自适应技术能有效提升交互式视觉工具的效率，显著减轻复杂视觉任务中的冗余人工负担。
---

## 摘要
交互式视频分割在处理挑战性场景（如遮挡、物体分离、伪装等）时，通常需要大量的用户干预以保证鲁棒性。然而，即使是像 SAM2 这样最先进的模型，也仅将修正用于即时修复，而无法从这些反馈中学习，导致用户工作效率低下且重复。为解决这一问题，我们提出了实时交互式训练（Live Interactive Training, LIT），这是一种针对基于提示（prompt-based）的视觉系统的新型框架，模型可以在推理阶段从人类修正中进行在线学习。我们的主要实例化方案 LIT-LoRA 通过实时持续更新轻量级 LoRA 模块来实现这一目标。当用户提供修正时，该模块会根据反馈进行快速训练，从而使视觉系统能够提升在同一视频后续帧上的表现。利用 LIT 的核心原理，我们的 LIT-LoRA 实现方案在挑战性的视频分割基准测试中，平均减少了 18-34% 的总修正次数，且每次修正的训练开销仅为约 0.5 秒，几乎可以忽略不计。我们通过将其成功适配到其他分割模型，并扩展到基于 CLIP 的细粒度图像分类任务，进一步证明了该方法的通用性。我们的工作突显了实时自适应在变革交互式工具以及显著减少复杂视觉任务中冗余人力投入方面的巨大潜力。项目地址：https://youngxinyu1802.github.io/projects/LIT/。

## Abstract
Interactive video segmentation often requires many user interventions for robust performance in challenging scenarios (e.g., occlusions, object separations, camouflage, etc.). Yet, even state-of-the-art models like SAM2 use corrections only for immediate fixes without learning from this feedback, leading to inefficient, repetitive user effort. To address this, we introduce Live Interactive Training (LIT), a novel framework for prompt-based visual systems where models also learn online from human corrections at inference time. Our primary instantiation, LIT-LoRA, implements this by continually updating a lightweight LoRA module on-the-fly. When a user provides a correction, this module is rapidly trained on that feedback, allowing the vision system to improve performance on subsequent frames of the same video. Leveraging the core principles of LIT, our LIT-LoRA implementation achieves an average 18-34% reduction in total corrections on challenging video segmentation benchmarks, with a negligible training overhead of ~0.5s per correction. We further demonstrate its generality by successfully adapting it to other segmentation models and extending it to CLIP-based fine-grained image classification. Our work highlights the promise of live adaptation to transform interactive tools and significantly reduce redundant human effort in complex visual tasks. Project: https://youngxinyu1802.github.io/projects/LIT/.

---

## 论文详细总结（自动生成）

### 论文总结：视频分割的实时交互式训练 (LIT)

#### 1. 核心问题与整体含义（研究动机和背景）
交互式视频分割（VOS）旨在通过用户提供的提示（如点击、框选）来精确分割视频目标。尽管现有最先进模型（如 SAM2）表现强劲，但它们存在一个关键局限：**模型仅将用户修正作为即时修复的输入，而不会从中“学习”**。这意味着在处理遮挡、物体分离或伪装等复杂场景时，模型可能会在同一视频的后续帧中重复犯同样的错误，迫使人类进行冗余的重复修正。

本文的研究动机是打破这种“修正-遗忘-再修正”的低效循环，提出了一种让模型在推理阶段能够根据用户反馈进行实时在线自适应的框架。

#### 2. 方法论
论文提出了 **LIT (Live Interactive Training)** 框架，其核心思想是将推理过程转变为一个“在线学习”过程。

*   **核心流程**：
    1.  **错误触发**：当模型预测结果不达标时，用户提供修正信号（如掩码或点击）。
    2.  **实时更新**：将该修正信号作为监督信号，立即训练一个轻量级的适配器模块。
    3.  **预测应用**：更新后的适配器被用于后续帧的预测，从而泛化并纠正类似的潜在错误。
*   **关键技术细节 (LIT-LoRA)**：
    *   **参数高效微调 (PEFT)**：为了保证实时性，模型主干（Backbone）保持冻结，仅在掩码解码器（Mask Decoder）中插入极小的 **LoRA** 模块（约 35k 参数，仅占总参数的 0.01%）。
    *   **损失函数**：结合了 Focal Loss 和 Dice Loss（比例为 20:1），平衡像素级精度和区域一致性。
    *   **更新机制**：采用小学习率（1e-4）进行 40 个 epoch 的快速迭代，通常在 0.5 秒内完成。
    *   **验证逻辑**：后续帧先由 LoRA 适配器处理，若结果满足质量阈值则接受，否则再次触发修正并增量更新 LoRA。

#### 3. 实验设计
*   **数据集**：
    *   **VOST**：侧重于物体破碎和剧烈变形。
    *   **LVOSv2**：长视频基准，包含大量遮挡和相似干扰物。
    *   **MOSEv2**：复杂真实场景，包含物体消失和拥挤。
    *   **SA-V**：SAM2 官方的大规模多样化数据集。
*   **Benchmark 与对比方法**：
    *   以 **SAM2** 为主要基准。
    *   对比了 **DAM4SAM** 和 **SAMURAI** 等基于 SAM2 的改进变体。
    *   在图像分类任务中对比了 **CLIP**。
*   **评估指标**：修正次数减少率、总标注时间、J&F 分数（区域相似度与轮廓准确度）。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA RTX Ada 6000 GPU**。
*   **开销**：
    *   **训练时长**：每次修正的在线训练耗时约为 **0.5 ± 0.2 秒**。
    *   **参数量**：LoRA 模块仅包含 **35K** 个可训练参数。
    *   **对比**：相对于人类标注一个掩码所需的约 80 秒，0.5 秒的计算开销几乎可以忽略不计。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **跨数据集验证**：在 4 个主流 VOS 数据集上进行了测试，结果一致。
*   **消融实验**：探讨了训练轮数（Epochs）、适配器插入位置（解码器 vs 存储库）、不同微调策略（全量微调 vs LoRA）的影响。
*   **通用性测试**：不仅适用于视频分割，还成功扩展到了基于 CLIP 的图像分类任务。
*   **人类用户研究**：招募了 6 名志愿者进行真实交互测试，验证了模拟实验与实际使用场景的一致性。
*   **公平性**：采用了交互式分割的标准模拟协议（Synthetic User Corrections），确保了实验的可重复性。

#### 6. 主要结论与发现
*   **显著减负**：LIT-LoRA 在挑战性视频中减少了 **18-34%** 的用户修正次数，节省了约 **22%** 的总标注时间。
*   **性能提升**：在相同的修正预算下，LIT-LoRA 显著提高了视频分割的整体精度（J&F 分数）。
*   **实时性强**：证明了在交互循环中嵌入轻量级训练是完全可行的，不会造成明显的系统延迟。
*   **模型无关性**：该框架可以作为“插件”应用于多种不同的视觉基础模型。

#### 7. 优点
*   **闭环学习**：首次在交互式视频分割中实现了真正的推理时在线学习，而不仅仅是缓存反馈。
*   **极低开销**：通过 LoRA 实现了性能与速度的极佳平衡。
*   **即插即用**：无需重新训练庞大的基础模型，易于集成到现有系统中。
*   **泛化性**：实验证明该框架在视频分割和图像分类任务中均有效。

#### 8. 不足与局限
*   **依赖人工监控**：系统目前仍需要人类实时监控以触发错误修正。虽然论文探索了使用模型预测的 IoU 作为自动触发信号，但发现其可靠性不足。
*   **模拟偏差**：大部分实验基于模拟修正协议，虽然有小规模用户研究支撑，但在极其复杂或非理性的真实用户行为下，其表现仍需进一步验证。
*   **基础模型依赖**：该方法的有效性部分取决于基础模型（如 SAM2）本身已具备的强大特征表达能力。
*   **场景局限**：对于物体发生极端突变或完全消失后重现的极端情况，LoRA 的泛化能力仍有提升空间。

（完）
