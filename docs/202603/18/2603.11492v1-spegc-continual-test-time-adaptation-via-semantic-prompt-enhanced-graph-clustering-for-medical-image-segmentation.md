---
title: "SPEGC: Continual Test-Time Adaptation via Semantic-Prompt-Enhanced Graph Clustering for Medical Image Segmentation"
title_zh: SPEGC：通过语义提示增强图聚类实现医学图像分割的持续测试时自适应
authors: "Xiaogang Du, Jiawei Zhang, Tongfei Liu, Tao Lei, Yingbo Wang"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11492v1"
tags: ["query:tta"]
score: 10.0
evidence: 针对不断变化领域的持续测试时间自适应
tldr: 针对医疗图像分割中持续测试时自适应（CTTA）面临的域偏移和误差累积问题，本文提出了一种基于语义提示增强图聚类（SPEGC）的新方法。该方法通过解耦的共性与异性提示池增强局部特征，并利用基于最优传输的微分图聚类求解器提取高阶结构表示，从而引导模型在不断变化的未标记领域中进行稳健自适应，有效缓解了性能退化并提升了分割精度。
selection_source: fresh_fetch
motivation: 现有的CTTA方法因依赖不可靠的监督信号而容易陷入误差累积的恶性循环，导致在临床部署中性能大幅下降。
method: 提出SPEGC框架，利用语义提示增强机制注入全局上下文，并结合基于最优传输的可微分图聚类求解器来指导模型的一致性学习。
result: 实验结果表明，SPEGC在两个主流医疗图像分割基准数据集上均达到了最先进的性能水平。
conclusion: SPEGC通过结合语义增强与结构化聚类，有效提升了预训练模型在动态变化环境下的鲁棒性和自适应能力。
---

## 摘要
在医学图像分割任务中，训练数据与测试数据采集差异导致的领域间隙（domain gap）严重阻碍了预训练模型在临床实践中的部署。持续测试时自适应（CTTA）旨在使预训练模型能够适应不断变化的无标签领域，为解决这一问题提供了一种有效的方法。然而，现有的 CTTA 方法通常依赖于不可靠的监督信号，从而引发错误累积的自我强化循环，最终导致灾难性的性能下降。为了克服这些挑战，我们提出了一种通过语义提示增强图聚类（SPEGC）实现的医学图像分割持续测试时自适应方法。首先，我们设计了一种语义提示特征增强机制，利用解耦的共性与异质性提示池将全局上下文信息注入局部特征，从而减轻其在领域偏移下对噪声干扰的敏感性。其次，基于这些增强特征，我们设计了一个可微图聚类求解器。该求解器将全局边稀疏化重新表述为一个最优传输问题，使其能够以端到端的方式将原始相似度矩阵提炼为精细的高阶结构化表示。最后，这种鲁棒的结构化表示被用于指导模型自适应，确保预测在聚类层面保持一致，并动态调整决策边界。大量实验表明，SPEGC 在两个医学图像分割基准数据集上优于其他最先进的 CTTA 方法。源代码可在 https://github.com/Jwei-Z/SPEGC-for-MIS 获取。

## Abstract
In medical image segmentation tasks, the domain gap caused by the difference in data collection between training and testing data seriously hinders the deployment of pre-trained models in clinical practice. Continual Test-Time Adaptation (CTTA) aims to enable pre-trained models to adapt to continuously changing unlabeled domains, providing an effective approach to solving this problem. However, existing CTTA methods often rely on unreliable supervisory signals, igniting a self-reinforcing cycle of error accumulation that culminates in catastrophic performance degradation. To overcome these challenges, we propose a CTTA via Semantic-Prompt-Enhanced Graph Clustering (SPEGC) for medical image segmentation. First, we design a semantic prompt feature enhancement mechanism that utilizes decoupled commonality and heterogeneity prompt pools to inject global contextual information into local features, alleviating their susceptibility to noise interference under domain shift. Second, based on these enhanced features, we design a differentiable graph clustering solver. This solver reframes global edge sparsification as an optimal transport problem, allowing it to distill a raw similarity matrix into a refined and high-order structural representation in an end-to-end manner. Finally, this robust structural representation is used to guide model adaptation, ensuring predictions are consistent at a cluster-level and dynamically adjusting decision boundaries. Extensive experiments demonstrate that SPEGC outperforms other state-of-the-art CTTA methods on two medical image segmentation benchmarks. The source code is available at https://github.com/Jwei-Z/SPEGC-for-MIS.

---

## 论文详细总结（自动生成）

以下是对论文《SPEGC: Continual Test-Time Adaptation via Semantic-Prompt-Enhanced Graph Clustering for Medical Image Segmentation》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：医学图像分割模型在临床部署时面临严重的**领域偏移（Domain Shift）**问题（如不同扫描仪、采集协议导致的差异）。
*   **核心挑战**：现有的持续测试时自适应（CTTA）方法通常依赖不可靠的伪标签或熵最小化，容易陷入“错误累积”的恶性循环，并导致“灾难性遗忘”。
*   **整体含义**：本文提出了一种名为 **SPEGC** 的框架，通过引入高阶结构化信息（图聚类）和语义增强机制，在无监督、流式数据的情况下，实现稳健的模型自适应。

### 2. 方法论
SPEGC 框架主要由两个核心模块组成：
*   **语义提示特征增强（SPFE）**：
    *   **不确定性采样**：利用 MC Dropout 估计预测的不确定性，筛选出低不确定性的稳定节点构建图。
    *   **解耦提示池**：设计了“共性提示池（Commonality）”和“异质性提示池（Heterogeneity）”。通过注意力机制从池中检索全局上下文信息，注入局部特征中，增强特征对噪声和风格变化的鲁棒性。
*   **可微图聚类求解器（DGCS）**：
    *   **最优传输（OT）建模**：将复杂的图划分问题转化为一个连续可微的最优传输问题。
    *   **Sinkhorn 算法**：利用 Sinkhorn 迭代将原始噪声相似度矩阵提炼为精细的高阶结构化表示（$S^*$），从而捕捉数据的内在语义聚类结构。
*   **优化目标**：
    *   **图一致性损失（$L_G$）**：确保结构相似的节点具有一致的语义预测。
    *   **聚类损失（$L_C$）**：显式约束共性提示池，使其学习跨领域的共享特征，缓解灾难性遗忘。

### 3. 实验设计
*   **数据集**：
    1.  **视网膜眼底图像分割**：包含 RIM-ONE, REFUGE, ORIGA 等 5 个不同来源的领域。
    2.  **息肉分割**：包含 BKAI, CVC-ClinicDB, ETIS, Kvasir 等 4 个领域。
    3.  **3D 医学图像**：在 M&MS 心脏 MRI 数据集上进行了扩展验证。
*   **Benchmark 与对比方法**：
    *   对比了 6 种最先进的方法：SAR (ICLR'23), DomainAdaptor (ICCV'23), NC-TTT (CVPR'24), VPTTA (CVPR'24), GraTa (AAAI'25), TTDG (CVPR'25)。
*   **评价指标**：Dice 系数 (DSC), 增强对齐度 ($E_{max}^\phi$), 结构相似度 ($S_\alpha$), 平均对称表面距离 (ASSD)。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 3090 GPU**。
*   **软件**：基于 PyTorch 框架实现。
*   **算力细节**：文中提到特征池大小为 3 时，FLOPs 为 5.8G，推理时间约为 0.521s/img。虽然图构建增加了计算开销，但在单卡上仍可运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了单源到多目标的跨领域测试。
    *   **长期 CTTA 实验**：进行了 5 轮连续不重置参数的自适应实验，验证了抗遗忘能力。
    *   **消融实验**：分别验证了图聚类、不确定性采样、共性/异质性提示池的有效性。
    *   **超参数分析**：对聚类数 $Z$、提示词数 $M$、特征池大小等进行了详细讨论。
*   **充分性评价**：实验设计非常充分，涵盖了 2D/3D 任务、长期稳定性验证及混合领域偏移场景，对比方法均为近两年的顶级会议成果，结果具有高度客观性。

### 6. 主要结论与发现
*   **性能领先**：SPEGC 在视网膜和息肉分割任务上均达到了 SOTA 性能，平均 DSC 提升显著。
*   **结构化引导的优越性**：相比于基于熵的方法，利用数据的内在图结构作为监督信号更加稳定，能有效抑制错误累积。
*   **解耦提示的作用**：共性提示池成功保留了跨领域的通用语义，而异质性提示池捕捉了领域特定风格，两者结合平衡了模型的“塑性”与“稳定性”。

### 7. 优点
*   **创新性强**：首次将可微图聚类与最优传输引入 CTTA 任务，提供了一种全新的结构化自适应视角。
*   **鲁棒性高**：通过不确定性采样和语义提示增强，解决了测试时数据噪声大的痛点。
*   **抗遗忘能力出色**：在长期持续学习场景下，性能下降远低于同类方法。

### 8. 不足与局限
*   **计算复杂度**：由于需要构建图并进行 Sinkhorn 迭代，其计算开销（FLOPs 和内存占用）高于轻量级的提示学习方法（如 VPTTA）。
*   **超参数依赖**：聚类类别数 $Z$ 和提示池大小 $M$ 需要根据具体任务预设，对不同任务的泛化可能需要微调。
*   **实时性挑战**：虽然在 3090 上可运行，但在对实时性要求极高的临床实时手术视频分析中，可能仍存在延迟压力。

（完）
