---
title: Learning Adaptive LLM Decoding
title_zh: 学习自适应大语言模型解码
authors: "Chloe H. Su, Zhe Ye, Samuel Tenka, Aidan Yang, Soonho Kong, Udaya Ghai"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09065v1"
tags: ["query:tta"]
score: 8.0
evidence: 在推理时选择自适应解码策略
tldr: 针对大语言模型解码时固定采样参数无法适应不同任务难度的问题，本文提出了一种学习自适应解码策略的方法。通过引入轻量级解码适配器并利用强化学习进行训练，该方法能在推理时根据可用计算资源动态选择采样策略。研究涵盖了序列级和标记级两种粒度，在数学和编程基准测试中显著提升了准确率与计算预算之间的权衡效率。
selection_source: fresh_fetch
motivation: 传统的固定采样超参数无法应对不同提示词和解码步骤中任务难度及不确定性的巨大差异。
method: 引入轻量级适配器，通过强化学习在序列级和标记级动态调整采样策略，以优化推理时的资源分配。
result: "在MATH和CodeContests基准上，该方法显著优化了准确率与预算的权衡，标记级适配器在固定预算下将Pass@1准确率提升了10.2%。"
conclusion: 实验证明，通过序列级和标记级的自适应解码，可以在不微调原模型的情况下有效提升大语言模型在受限资源下的推理性能。
---

## 摘要
大语言模型（LLM）的解码通常依赖于固定的采样超参数（如 temperature、top-p），尽管不同提示词和单个解码步骤之间的任务难度和不确定性存在显著差异。我们提出学习自适应解码策略，在推理时根据可用的计算资源动态选择采样策略。我们没有微调语言模型本身，而是引入了通过强化学习和可验证终端奖励（如数学和编程任务的正确性）训练的轻量级解码适配器。在序列层面，我们将解码建模为一个上下文老虎机（contextual bandit）问题：策略根据提示词嵌入和并行采样预算，为每个提示词选择一种解码策略（如 greedy、top-k、min-p）。在 Token 层面，我们将解码建模为一个部分可观测马尔可夫决策过程（POMDP），策略根据模型内部特征和剩余 Token 预算，在每个 Token 步骤选择采样动作。在 MATH 和 CodeContests 基准测试上的实验表明，学习到的适配器改善了准确率与预算之间的权衡：在 MATH 数据集上，在固定 Token 预算下，Token 级适配器相比最佳静态基准将 Pass@1 准确率提高了多达 10.2%，而序列级适配器在固定并行采样下获得了 2-3% 的提升。消融分析支持了序列级和 Token 级自适应的贡献。

## Abstract
Decoding from large language models (LLMs) typically relies on fixed sampling hyperparameters (e.g., temperature, top-p), despite substantial variation in task difficulty and uncertainty across prompts and individual decoding steps. We propose to learn adaptive decoding policies that dynamically select sampling strategies at inference time, conditioned on available compute resources. Rather than fine-tuning the language model itself, we introduce lightweight decoding adapters trained with reinforcement learning and verifiable terminal rewards (e.g. correctness on math and coding tasks). At the sequence level, we frame decoding as a contextual bandit problem: a policy selects a decoding strategy (e.g. greedy, top-k, min-p) for each prompt, conditioned on the prompt embedding and a parallel sampling budget. At the token level, we model decoding as a partially observable Markov decision process (POMDP), where a policy selects sampling actions at each token step based on internal model features and the remaining token budget. Experiments on the MATH and CodeContests benchmarks show that the learned adapters improve the accuracy-budget tradeoff: on MATH, the token-level adapter improves Pass@1 accuracy by up to 10.2% over the best static baseline under a fixed token budget, while the sequence-level adapter yields 2-3% gains under fixed parallel sampling. Ablation analyses support the contribution of both sequence- and token-level adaptation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Adaptive LLM Decoding》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在推理时通常使用固定的采样超参数（如温度 $T$、top-p 等）。然而，不同的提示词（Prompt）难度不一，且同一序列中不同 Token 的不确定性也随步骤剧烈波动。固定参数会导致计算资源的浪费或推理质量的下降。
*   **核心问题**：如何在不微调原始 LLM 参数的前提下，根据当前的**计算预算**（Token 数量或并行采样数）和**模型内部状态**，动态地为每一步或每个序列选择最优的解码策略？

### 2. 方法论
论文提出了**学习型解码适配器（Learned Decoding Adapters）**，通过强化学习（RL）训练轻量级 MLP 模块来控制解码过程。
*   **序列级适配器（Sequence-Level）**：
    *   **建模**：上下文老虎机（Contextual Bandit）。
    *   **流程**：输入 Prompt 的嵌入向量 $e$ 和并行采样预算 $B$，适配器 $\pi_\theta$ 为该 Prompt 选择一个固定的解码配置（如特定的 $T$、top-k、min-p 组合）。
    *   **目标函数**：使用 REINFORCE 算法，最大化终端奖励（如 Pass@k）。
*   **Token 级适配器（Token-Level）**：
    *   **建模**：部分可观测马尔可夫决策过程（POMDP）。
    *   **流程**：在每个生成步骤 $t$，输入当前隐藏状态 $e_t$ 和剩余 Token 预算 $b_t$，适配器选择该步的采样动作（实验中主要为温度调节）。
    *   **关键技术**：为了稳定训练，论文应用了“奖励稀疏性过滤”和“高置信度 Token 掩码”（即当模型对下一个 Token 极度确定时，不更新适配器）。
*   **动作空间构建**：利用数据驱动的贪心选择程序（基于子模函数最大化），从 180 种超参数组合中筛选出 4-6 种最具代表性的互补策略作为适配器的动作空间。

### 3. 实验设计
*   **数据集**：
    *   **MATH**：竞赛级数学问题。
    *   **CodeContests**：算法编程竞赛题。
    *   **AIME 2025**：用于评估零样本泛化能力的极难数学题。
*   **基准模型**：Qwen3-4B（主要）、Qwen3-8B、Qwen2.5-Math-1.5B。
*   **对比方法**：
    *   **Best Static**：在验证集上表现最好的单一固定解码策略。
    *   **Mixed Static**：在动作空间内进行均匀随机采样的非学习型策略。
*   **评估指标**：Pass@1、Pass@8 准确率。

### 4. 资源与算力
*   **框架支持**：使用了 **vLLM** 进行快速推理生成，**Ray Train** 和分布式数据并行（DDP）进行适配器训练。
*   **具体算力说明**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）数量以及具体的训练总时长。但由于适配器仅为轻量级 MLP 且冻结了基础 LLM，其训练开销远小于全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 种不同规模的模型，2 个主要领域（数学、代码），以及跨领域的混合训练实验。
*   **消融实验**：
    *   验证了“预算感知”（Budget-conditioning）的必要性。
    *   对比了“仅基于熵的启发式”与“全特征学习”的效果，证明了学习上下文特征的优越性。
*   **充分性评价**：实验设计较为全面，不仅验证了性能提升，还通过 AIME'25 验证了适配器在未见过的更难任务上的泛化能力。对比基准（Best Static）选取客观，实验结果具有说服力。

### 6. 主要结论与发现
*   **性能显著提升**：在 MATH 任务上，Token 级适配器将 Pass@1 准确率提升了 **10.2%**；序列级适配器在 CodeContests 上也有显著增益。
*   **预算感知的重要性**：显式地将剩余预算作为输入，能让适配器在预算充足时倾向于探索（高熵采样），在预算不足时倾向于保守（贪心解码）。
*   **Token 级控制更优**：相比于为整个序列设定单一参数，逐 Token 的动态调整能更有效地捕捉“分叉 Token”（即决定解题走向的关键决策点）。

### 7. 优点
*   **非侵入性**：无需修改原始 LLM 的权重，适合闭源模型或已部署模型的插件式增强。
*   **端到端学习**：避开了人工设计复杂的启发式规则（如手动设定熵阈值），直接从最终的正确性奖励中学习。
*   **计算高效**：适配器参数量极小，推理时的额外延迟几乎可以忽略不计。

### 8. 不足与局限
*   **奖励依赖**：该方法高度依赖于**可验证的终端奖励**（如数学答案对错、代码通过测试），对于开放式生成（如文学创作、对话）等缺乏客观评价标准的任务，应用难度较大。
*   **训练稳定性**：Token 级的强化学习训练容易出现高方差，论文中提到的过滤和掩码技巧是必需的，这增加了实现复杂度。
*   **动作空间离散化**：目前仅在有限的离散动作空间内选择，未来若能实现连续参数空间的平滑控制，效果可能更好。

（完）
