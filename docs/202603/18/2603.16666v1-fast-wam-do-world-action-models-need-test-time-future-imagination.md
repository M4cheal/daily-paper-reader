---
title: "Fast-WAM: Do World Action Models Need Test-time Future Imagination?"
title_zh: Fast-WAM：世界动作模型是否需要测试时的未来想象？
authors: "Tianyuan Yuan, Zibin Dong, Yicheng Liu, Hang Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16666v1"
tags: ["query:tta"]
score: 9.0
evidence: 世界动作模型中的测试时间未来想象
tldr: Fast-WAM 探讨了世界动作模型（WAM）在推理时是否需要显式的未来想象。研究发现，虽然现有 WAM 依赖耗时的视频去噪生成未来图像，但其核心优势其实源于训练阶段的视频建模。Fast-WAM 保留了视频协同训练，但在推理时跳过预测步骤，在 LIBERO 和 RoboTwin 等基准测试中表现优异，且速度提升了 4 倍以上，实现了实时控制。
selection_source: fresh_fetch
motivation: 现有 WAM 采用“先想象再执行”模式导致推理延迟高，研究旨在探究推理时的显式未来生成是否为性能所必需。
method: 提出 Fast-WAM 架构，在训练时保留视频协同训练以增强表示学习，但在推理时直接输出动作而跳过视频生成。
result: Fast-WAM 在仿真和真实任务中达到了 SOTA 水平，延迟仅为 190ms，比传统 WAM 快 4 倍以上。
conclusion: 视频预测在 WAM 中的主要价值在于训练期间提升世界表示能力，而非推理时的显式观测生成。
---

## 摘要
世界动作模型（World Action Models, WAMs）已成为具身控制中视觉-语言-动作（Vision-Language-Action, VLA）模型的一种极具前景的替代方案，因为它们显式地建模了视觉观测在动作作用下如何演化。大多数现有的 WAM 遵循“先想象后执行”的范式，由于迭代视频去噪导致了显著的测试时延迟，但目前尚不清楚显式的未来想象对于强大的动作性能是否真的必要。在本文中，我们探讨了 WAM 是否在测试时需要显式的未来想象，或者其优势是否主要源于训练期间的视频建模。我们通过提出 Fast-WAM 来解耦训练期间视频建模的作用与推理期间显式未来生成的作用。Fast-WAM 是一种在训练时保留视频协同训练但在测试时跳过未来预测的 WAM 架构。我们进一步实例化了多个 Fast-WAM 变体，以便对这两个因素进行受控比较。在这些变体中，我们发现 Fast-WAM 与“先想象后执行”变体相比仍具有竞争力，而移除视频协同训练会导致更大幅度的性能下降。实验表明，Fast-WAM 在模拟基准测试（LIBERO 和 RoboTwin）以及现实世界任务中均取得了与最先进方法相当的结果，且无需具身预训练。它的运行延迟为 190 毫秒，实现了实时运行，比现有的“先想象后执行”型 WAM 快 4 倍以上。这些结果表明，WAM 中视频预测的主要价值可能在于改进训练期间的世界表示，而非在测试时生成未来观测。项目页面：https://yuantianyuan01.github.io/FastWAM/

## Abstract
World Action Models (WAMs) have emerged as a promising alternative to Vision-Language-Action (VLA) models for embodied control because they explicitly model how visual observations may evolve under action. Most existing WAMs follow an imagine-then-execute paradigm, incurring substantial test-time latency from iterative video denoising, yet it remains unclear whether explicit future imagination is actually necessary for strong action performance. In this paper, we ask whether WAMs need explicit future imagination at test time, or whether their benefit comes primarily from video modeling during training. We disentangle the role of video modeling during training from explicit future generation during inference by proposing \textbf{Fast-WAM}, a WAM architecture that retains video co-training during training but skips future prediction at test time. We further instantiate several Fast-WAM variants to enable a controlled comparison of these two factors. Across these variants, we find that Fast-WAM remains competitive with imagine-then-execute variants, while removing video co-training causes a much larger performance drop. Empirically, Fast-WAM achieves competitive results with state-of-the-art methods both on simulation benchmarks (LIBERO and RoboTwin) and real-world tasks, without embodied pretraining. It runs in real time with 190ms latency, over 4$\times$ faster than existing imagine-then-execute WAMs. These results suggest that the main value of video prediction in WAMs may lie in improving world representations during training rather than generating future observations at test time. Project page: https://yuantianyuan01.github.io/FastWAM/

---

## 论文详细总结（自动生成）

以下是对论文《Fast-WAM: Do World Action Models Need Test-time Future Imagination?》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：世界动作模型（WAM）通过显式建模视觉观测在动作作用下的演化，成为具身控制领域的重要方向。
*   **痛点**：现有 WAM 普遍采用“先想象后执行”范式，依赖迭代视频去噪生成未来图像，导致推理延迟极高，难以满足实时控制需求。
*   **核心问题**：WAM 的性能提升究竟源于**推理时的显式未来想象**，还是源于**训练时的视频建模任务**（即视频协同训练带来的更强表示能力）？
*   **研究意义**：如果推理时的想象并非必需，则可以大幅提升 WAM 的运行效率，使其在保持高性能的同时实现实时部署。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **Fast-WAM** 架构，将训练时的视频预测目标与推理时的显式生成过程解耦。在训练时利用视频建模学习物理先验，在推理时仅作为单次前向传递的编码器。
*   **关键技术细节**：
    *   **架构设计**：基于混合 Transformer（MoT）架构，结合了预训练的视频扩散 Transformer（Wan2.2-5B）和专门的动作专家 DiT（1B），总参数量约 6B。
    *   **结构化注意力掩码**：设计了特殊的掩码机制。训练时，动作 token 只能访问当前帧和语言信息，不能访问未来视频 token，确保动作预测不依赖未来信息泄露；视频 token 则进行双向建模。
    *   **训练目标**：采用联合流匹配（Flow Matching）目标函数，同时优化动作生成损失（$L_{act}$）和视频建模损失（$L_{vid}$）。
    *   **推理流程**：推理时完全跳过未来视频的去噪步骤。视频 DiT 仅对当前观测进行单次编码，提取潜在的世界表示，随后由动作专家直接生成动作块。

### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，共 40 个任务。
    *   **RoboTwin 2.0**：具有挑战性的双臂协同操作基准，包含 50 多个任务，涉及大量场景随机化。
    *   **真实世界任务**：长程毛巾折叠任务（Towel Folding），涉及可变形物体操作。
*   **对比方法**：
    *   **VLA 基准**：OpenVLA, $\pi_0$, $\pi_{0.5}$。
    *   **WAM 基准**：Motus, LingBot-VA。
    *   **受控变体**：Fast-WAM-Joint（联合去噪）、Fast-WAM-IDM（先视频后动作）、Fast-WAM w/o video co-train（无视频协同训练）。

### 4. 资源与算力
*   **算力设备**：推理延迟在单张 **NVIDIA RTX 5090D V2 (32GB)** GPU 上测量。
*   **训练时长**：LIBERO 训练 20k 步；RoboTwin 和真实世界任务训练 30k 步。
*   **模型规模**：视频分支 5B，动作专家 1B，总计 6B 参数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流仿真基准（数千次测试）和 60 小时的真实世界演示数据。
*   **充分性**：研究设计了严谨的消融实验（三种变体对比），通过控制变量法隔离了“训练目标”和“推理想象”两个因素。
*   **客观性**：在不使用具身预训练（Embodied Pretraining）的情况下，与使用了预训练的 SOTA 模型进行对比，证明了其数据效率。

### 6. 主要结论与发现
*   **性能保持**：Fast-WAM 在推理时不生成未来图像，其成功率仍与显式想象的 WAM 变体相当。
*   **关键因素**：**视频协同训练（Video Co-training）是性能提升的主因**。移除该目标会导致性能大幅下降（如在 RoboTwin 上下降约 8%），而移除推理时的想象步骤对性能影响微乎其微。
*   **效率飞跃**：Fast-WAM 的推理延迟仅为 **190ms**，比传统的“先想象后执行”模型快 **4 倍以上**。
*   **数据效率**：即使没有大规模具身数据预训练，Fast-WAM 也能在复杂任务中达到或超过现有 SOTA 模型的水平。

### 7. 优点
*   **高效性**：解决了 WAM 难以实时运行的顽疾，使其具备了实际部署的潜力。
*   **理论洞察**：通过解耦实验，澄清了 WAM 领域关于“未来想象”必要性的长期争论。
*   **通用性**：在刚性物体操作（RoboTwin）和柔性物体操作（毛巾折叠）中均表现出色。

### 8. 不足与局限
*   **预训练依赖**：虽然没有具身预训练，但高度依赖强大的互联网规模视频生成模型（Wan2.2）作为骨干。
*   **长程预测限制**：实验主要集中在单次动作块生成，尚未深入探讨在极长程、需要多步闭环规划的任务中，显式想象是否会展现出更大的优势。
*   **算力门槛**：尽管推理变快，但 6B 参数的模型对于边缘计算设备仍有一定压力。

（完）
