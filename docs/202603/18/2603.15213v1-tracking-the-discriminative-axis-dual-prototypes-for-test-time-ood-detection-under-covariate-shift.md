---
title: "Tracking the Discriminative Axis: Dual Prototypes for Test-Time OOD Detection Under Covariate Shift"
title_zh: 追踪判别轴：协变量偏移下测试时 OOD 检测的双原型方法
authors: "Wooseok Lee, Jin Mo Yang, Saewoong Bahk, Hyung-Sin Kim"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15213v1"
tags: ["query:tta"]
score: 9.0
evidence: 流式协变量偏移下的测试时间离群检测
tldr: 针对现实场景中协变量偏移导致的分布外（OOD）检测失效问题，本文提出了一种名为DART的在线测试时检测方法。该方法基于ID与OOD样本在特征空间中仍保持可分性的观察，通过动态跟踪双原型（ID和OOD）来恢复漂移的判别轴，并结合多层融合与翻转校正增强鲁棒性，在多种极端的协变量偏移基准测试中显著提升了检测性能。
selection_source: fresh_fetch
motivation: 现有的OOD检测方法通常假设分布平稳，但在现实中ID和OOD样本常受动态协变量偏移影响，导致传统方法性能大幅下降。
method: 提出DART方法，通过在线跟踪ID和OOD双原型来动态定位特征空间中的判别轴，并利用多层特征融合和翻转校正机制提高检测稳定性。
result: "在ImageNet-C等挑战性基准上，相比现有基准方法，AUROC提升了15.32个百分点，FPR@95TPR降低了49.15个百分点。"
conclusion: 实验证明，在动态变化的环境中，通过测试时跟踪判别轴是实现可靠OOD检测的一种极具潜力且有效的途径。
---

## 摘要
为了深度学习系统的可靠部署，分布外（OOD）检测是必不可少的。在现实世界中，测试时输入通常以分布内（ID）和 OOD 样本的流式混合形式出现，且处于不断演变的协变量偏移（covariate shift）之下。此时，OOD 样本受领域约束并被环境所限制，且 ID 和 OOD 样本共同受到相同协变量因素的影响。现有方法通常假设 ID 分布是平稳的，但在这种设置下，该假设失效，导致性能严重下降。我们通过实验发现，即使在协变量偏移下，协变量偏移后的 ID（csID）和 OOD（csOOD）样本在特征空间中沿判别轴仍然是可分的。基于这一观察，我们提出了 DART，一种测试时在线 OOD 检测方法。该方法动态追踪双原型（一个用于 ID，另一个用于 OOD）以恢复漂移的判别轴，并辅以多层融合和翻转校正以增强鲁棒性。在广泛的挑战性基准测试上进行的实验（所有数据集均经受了 5 级严重程度的 15 种常见损坏类型）表明，与现有基准方法相比，我们的方法显著提高了性能，在 ImageNet-C 与 Textures-C 的对比中，AUROC 提升了 15.32 个百分点（pp），FPR@95TPR 降低了 49.15 个百分点。这些结果突显了测试时判别轴追踪在动态变化环境中进行可靠 OOD 检测的潜力。

## Abstract
For reliable deployment of deep-learning systems, out-of-distribution (OOD) detection is indispensable. In the real world, where test-time inputs often arrive as streaming mixtures of in-distribution (ID) and OOD samples under evolving covariate shifts, OOD samples are domain-constrained and bounded by the environment, and both ID and OOD are jointly affected by the same covariate factors. Existing methods typically assume a stationary ID distribution, but this assumption breaks down in such settings, leading to severe performance degradation. We empirically discover that, even under covariate shift, covariate-shifted ID (csID) and OOD (csOOD) samples remain separable along a discriminative axis in feature space. Building on this observation, we propose DART, a test-time, online OOD detection method that dynamically tracks dual prototypes -- one for ID and the other for OOD -- to recover the drifting discriminative axis, augmented with multi-layer fusion and flip correction for robustness. Extensive experiments on a wide range of challenging benchmarks, where all datasets are subjected to 15 common corruption types at severity level 5, demonstrate that our method significantly improves performance, yielding 15.32 percentage points (pp) AUROC gain and 49.15 pp FPR@95TPR reduction on ImageNet-C vs. Textures-C compared to established baselines. These results highlight the potential of the test-time discriminative axis tracking for dependable OOD detection in dynamically changing environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tracking the Discriminative Axis: Dual Prototypes for Test-Time OOD Detection Under Covariate Shift》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的分布外（OOD）检测方法通常假设分布内（ID）数据是静态平稳的。但在现实部署中，系统常面临**协变量偏移（Covariate Shift）**（如天气变化、传感器噪声），这会导致 ID 和 OOD 分布在特征空间中同时发生漂移。
*   **研究动机**：现有方法在协变量偏移下性能会剧烈下降（AUROC 甚至跌至 0.5 左右），因为它们依赖的决策边界在特征空间几何结构改变时失效。
*   **核心发现**：作者观察到，尽管分布在漂移，但 csID（偏移后的 ID）和 csOOD（偏移后的 OOD）在特征空间中仍然沿着一个特定的“判别轴”保持近似线性可分。

### 2. 论文提出的方法论：DART
DART（Discriminative Axis Real-time Tracker）是一种**在线测试时（Online Test-time）**检测方法，其核心思想是动态追踪特征空间中的判别方向。
*   **双原型追踪（Dual Prototypes）**：在每个特征层为 ID 和 OOD 各维护一个原型（均值向量）。
*   **相对距离得分（RDS）**：对于新样本，计算其到 ID 原型和 OOD 原型的欧氏距离，通过公式 $RDS = 1 - \frac{d_{ID}}{d_{ID} + d_{OOD}}$ 衡量其相对位置。该得分具有尺度不变性。
*   **在线更新流程**：
    1.  **伪标签分配**：利用 **Otsu 算法**（最大类间方差法）根据当前 Batch 的得分分布自动确定阈值，将样本分为伪 ID 和伪 OOD。
    2.  **异常过滤**：使用 Tukey's Outlier Filtering 剔除远离原型的噪声样本。
    3.  **平滑更新**：使用指数移动平均（EMA）更新双原型，使判别轴平滑适应分布漂移。
*   **关键技术细节**：
    *   **多层得分融合**：由于不同类型的偏移（如模糊 vs 噪声）对神经网络不同层的影响不同，DART 融合了从低级到高级多个特征层的 RDS 得分。
    *   **翻转校正（Flip Correction）**：通过与基准得分（如 MSP）对比，检测并纠正由于初始化错误导致的 ID/OOD 原型位置反转。

### 3. 实验设计
*   **数据集与场景**：
    *   **ID 数据集**：CIFAR-100, ImageNet-1K。
    *   **协变量偏移**：使用 ImageNet-C 和 CIFAR-100-C 中的 15 种常见损坏（如高斯噪声、雾、模糊等），严重程度设为最高级（Level 5）。
    *   **OOD 数据集**：SVHN, Places, LSUN, iNaturalist, Textures 等。
*   **Benchmark 与对比方法**：
    *   对比了 18 种方法，包括基础方法（MSP, Energy）、后置处理方法（ReAct, ASH, ViM, KNN）以及测试时自适应方法（RTL, OODD）。
    *   涵盖了需要训练集统计量（如 Mahalanobis, ViM）和不需要统计量的方法。
*   **模型架构**：WideResNet-40-2, RegNetY-16GF, ResNet-50, 以及 Transformer 架构（ViT-Tiny, Swin-T）。

### 4. 资源与算力
*   **硬件**：使用了 NVIDIA RTX 3090 和 RTX 4090 GPU。
*   **算力开销**：论文未明确给出总训练时长，但强调 DART 是**后置处理（Post-hoc）**且**无需反向传播**的方法。实验显示其推理速度极快，处理 20,000 个样本仅需约 1.89 秒（在 RegNetY 上），远快于需要回归拟合或 KNN 搜索的方法。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 15 种损坏类型 × 5 种 OOD 数据集的大规模组合测试。
    *   进行了**跨领域评估**：包括混合 OOD 来源、连续变化的 OOD 类别、混合协变量偏移以及连续演变的偏移场景。
    *   **消融实验**：验证了多层融合、翻转校正、EMA 系数、Batch 大小及 ID/OOD 比例的影响。
*   **充分性评价**：实验设计非常全面且贴近现实（流式数据、动态偏移），对比基准涵盖了该领域最新的 SOTA 方法，结果具有高度的说服力和客观性。

### 6. 主要结论与发现
*   **性能飞跃**：在 ImageNet-C 挑战中，DART 相比次优方法，AUROC 提升了 **15.32 pp**，FPR@95 降低了 **49.15 pp**。
*   **架构通用性**：无论是在 CNN 还是 Transformer 架构上，DART 均表现出显著的优越性。
*   **无需先验**：DART 在完全不接触训练集统计信息的情况下，性能甚至超过了许多利用了训练集先验的方法（因为先验在偏移下会失效）。
*   **干净数据表现**：即使在没有协变量偏移的干净数据集上，DART 依然保持了 SOTA 级别的性能。

### 7. 优点（亮点）
*   **现实针对性强**：直接解决了 OOD 检测在动态环境（协变量偏移）下的痛点。
*   **算法简洁高效**：基于原型追踪，无需复杂的优化或权重更新，适合端侧部署。
*   **鲁棒性设计**：通过多层融合解决了不同偏移对特征层影响不一的问题；翻转校正机制增强了在线系统的稳定性。
*   **理论支撑**：附录中提供了关于判别轴有效性的理论分析（基于 BN 缩放因子和 Fisher 准则）。

### 8. 不足与局限
*   **Batch 依赖性**：作为一种基于 Batch 的在线方法，如果测试时的 Batch Size 过小（如极端的单样本推理），原型的估计将变得不可靠。
*   **类别极度不平衡风险**：虽然实验证明在 ID/OOD 比例 0.2 到 0.8 之间表现稳定，但在 OOD 样本极度稀缺（如比例 < 0.01）的情况下，OOD 原型的初始化和更新可能面临挑战。
*   **初始化依赖**：初始阶段依赖 MSP 进行伪标签分配，若初始 Batch 的偏移极其严重导致 MSP 完全失效，系统可能需要更长时间恢复。

（完）
