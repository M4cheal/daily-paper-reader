---
title: "Beyond Test-Time Training: Learning to Reason via Hardware-Efficient Optimal Control"
title_zh: 超越测试时训练：通过硬件高效的最优控制学习推理
authors: "Peihao Wang, Shan Yang, Xijun Wang, Tesi Xiao, Xin Liu, Changlong Yu, Yu Lou, Pan Li, Zhangyang Wang, Ming Lin, René Vidal"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09221v1"
tags: ["query:tta"]
score: 8.0
evidence: 通过推理阶段规划进行推理的测试时间控制层
tldr: 本研究针对大语言模型缺乏原生推理和规划能力的问题，提出将推理建模为最优控制过程。通过引入测试时控制（TTC）层，在推理阶段对潜在状态进行有限时域LQR规划。为保证效率，开发了基于辛公式的硬件高效LQR求解器并实现为融合CUDA算子。作为适配器集成到预训练模型后，在MATH-500等数学竞赛数据集上表现显著提升，证明了将最优控制嵌入架构是增强模型推理能力的有效途径。
selection_source: fresh_fetch
motivation: 现有的语言模型主要依赖关联记忆，缺乏像人类一样通过预测未来状态和目标导向行动来进行原生推理和规划的能力。
method: 提出了测试时控制（TTC）层，通过在推理时对潜在状态执行有限时域LQR规划，并利用硬件高效的融合CUDA内核实现并行计算。
result: "在MATH-500上性能提升高达27.8%，在AMC和AIME竞赛题目上的Pass@8指标也实现了2-3倍的增长。"
conclusion: 将最优控制作为架构组件嵌入模型，为超越传统测试时训练、实现可扩展的推理机制提供了有效方案。
---

## 摘要
关联记忆长期以来一直是序列模型设计的基础。除了召回，人类还通过预测未来状态和选择目标导向的行动来进行推理，这种能力是现代语言模型日益需要但尚未原生编码的。虽然之前的工作使用了强化学习或测试时训练，但规划仍然处于模型架构之外。我们将推理建模为最优控制问题，并引入了测试时控制（TTC）层。该层在推理时对潜在状态执行有限时界 LQR 规划，在神经架构中表示价值函数，并将其作为嵌套目标，从而在预测前实现规划。为了确保可扩展性，我们推导了一种基于辛公式（symplectic formulation）的硬件高效 LQR 求解器，并将其实现为融合 CUDA 内核，从而以极低的开销实现并行执行。作为适配器集成到预训练大语言模型（LLM）中，TTC 层在 MATH-500 上的数学推理性能提升高达 +27.8%，在 AMC 和 AIME 上的 Pass@8 提升了 2-3 倍，这表明将最优控制嵌入为架构组件，为超越测试时训练的推理提供了一种有效且可扩展的机制。

## Abstract
Associative memory has long underpinned the design of sequential models. Beyond recall, humans reason by projecting future states and selecting goal-directed actions, a capability that modern language models increasingly require but do not natively encode. While prior work uses reinforcement learning or test-time training, planning remains external to the model architecture. We formulate reasoning as optimal control and introduce the Test-Time Control (TTC) layer, which performs finite-horizon LQR planning over latent states at inference time, represents a value function within neural architectures, and leverages it as the nested objective to enable planning before prediction. To ensure scalability, we derive a hardware-efficient LQR solver based on a symplectic formulation and implement it as a fused CUDA kernel, enabling parallel execution with minimal overhead. Integrated as an adapter into pretrained LLMs, TTC layers improve mathematical reasoning performance by up to +27.8% on MATH-500 and 2-3x Pass@8 improvements on AMC and AIME, demonstrating that embedding optimal control as an architectural component provides an effective and scalable mechanism for reasoning beyond test-time training.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Test-Time Training: Learning to Reason via Hardware-Efficient Optimal Control》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的序列模型（如 Transformer、SSM）主要基于“关联记忆”机制，类似于人类的“系统 1”思维（快速、自动的模式匹配）。然而，复杂的推理任务需要“系统 2”思维（深思熟虑、多步规划）。
*   **核心问题**：现有的推理增强方法（如强化学习 RL 或测试时训练 TTT）通常将规划视为外部过程，而非模型架构的原生能力。论文旨在将“规划”直接内化为模型的一个层，使模型在预测下一个 token 之前先在潜在空间进行“思考”。

### 2. 方法论
*   **核心思想**：将推理建模为内部表示上的**最优控制问题**。引入**测试时控制（Test-Time Control, TTC）层**，该层通过解决一个有限时界的线性二次调节器（LQR）问题来选择最优动作。
*   **关键技术细节**：
    *   **LQR 建模**：在 TTC 层内部，模型根据当前上下文生成状态转移矩阵 $A_t$、输入矩阵 $B_t$ 以及代价矩阵 $Q_t, R_t$。通过最小化未来时界的累积代价，求解第一步的最优控制动作作为输出。
    *   **硬件高效求解器**：传统的 Riccati 方程求解涉及串行矩阵求逆，效率低下。论文推导了一种基于**辛公式（Symplectic Formulation）**的求解器，将串行计算转化为可并行的矩阵乘积，并实现为**融合 CUDA 内核**。
    *   **可微性设计**：利用 KKT 条件构建对偶 LQR 系统，使得 TTC 层可以通过反向传播进行端到端训练。
    *   **混合架构（TTC-Net）**：将 TTC 层作为适配器（Adapter）插入预训练 LLM（如 Llama-3）中，与注意力层交替工作。

### 3. 实验设计
*   **场景与数据集**：
    *   **数独（Sudoku）**：用于评估逻辑约束传播和长程规划能力。
    *   **数学推理**：使用 MATH-500、AMC、AIME 2024/2025 等高难度竞赛数据集。
*   **对比方法**：
    *   **基础架构**：Transformer、Mamba、Mamba2、GDN、Samba。
    *   **测试时存储/训练方法**：RetNet、MesaNet 等。
*   **Benchmark**：主要衡量准确率（Accuracy）、Pass@8（采样 8 次的通过率）以及推理时计算量扩展（Test-time scaling）的效果。

### 4. 资源与算力
*   **硬件平台**：效率基准测试在 **NVIDIA H200 GPU** 上完成。
*   **训练细节**：
    *   使用 Llama-3-Instruct-7B 作为基座模型。
    *   微调数据集包括 OpenThoughts2-114K 以及 800K 经过筛选的推理样本。
    *   训练参数：Batch size 96，学习率 2e-5，使用 AdamW 优化器。
*   **效率提升**：论文明确指出其辛求解器比传统的 Riccati 或基于 KKT 的求解器快 **10 倍以上**，且内存占用随规划时界保持常数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成逻辑任务（数独）到真实世界复杂数学问题的多维度评估。
*   **消融实验**：非常充分。研究了时间异构性（参数随步骤变化）、时界采样策略（泊松对数正态分布）、以及 TTC 层与注意力层的交替比例（如 8:1）。
*   **客观性**：通过在相同微调数据下对比多种主流架构（如 Mamba, GDN），确保了对比的公平性。

### 6. 主要结论与发现
*   **性能飞跃**：TTC-Net 在 MATH-500 上比纯微调基座模型提升了 **27.8%** 的准确率；在极难的 AIME 竞赛题上，Pass@8 实现了 **2-3 倍** 的增长。
*   **测试时扩展性**：TTC 层支持在推理时通过增加规划时界（Horizon $T$）来提升性能，展现了类似于人类“想得越久，算得越准”的特性。
*   **架构优势**：证明了将最优控制作为架构组件比单纯的外部 RL 或记忆增强更能触及推理能力的上限。

### 7. 优点
*   **理论与工程结合**：将经典控制理论（LQR）与现代深度学习硬件优化（CUDA 融合、辛几何）完美结合。
*   **原生规划能力**：不同于 CoT（思维链）在外部生成文本，TTC 在隐藏层内部进行连续空间的规划，效率更高。
*   **即插即用**：作为适配器集成，无需改变预训练模型的权重初始化，具有良好的兼容性。

### 8. 不足与局限
*   **理论黑盒**：虽然单个 TTC 层可解释，但多个 TTC 层在深度网络中如何协同交互的理论分析尚不完善。
*   **线性假设**：内部规划基于线性状态转移，对于极度非线性的环境动态可能建模能力有限。
*   **模型规模覆盖**：实验主要集中在 7B 规模的模型上，尚未验证在超大规模模型（如 70B+）或混合专家模型（MoE）上的表现。
*   **训练开销**：尽管推理高效，但训练时需要求解对偶 LQR 系统，计算负担高于普通 Transformer 层。

（完）
