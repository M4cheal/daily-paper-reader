---
title: Test-Time Attention Purification for Backdoored Large Vision Language Models
title_zh: 针对受后门攻击的大视觉语言模型的测试时注意力净化
authors: "Zhifang Zhang, Bojun Yang, Shuo He, Weitong Chen, Wei Emma Zhang, Olaf Maennel, Lei Feng, Miao Xu"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12989v1"
tags: ["query:tta"]
score: 9.0
evidence: 针对大型视觉语言模型的测试时间注意力净化
tldr: 针对大视觉语言模型（LVLM）易受微调阶段后门攻击且现有防御成本高的问题，本文揭示了后门触发器通过“注意力窃取”机制干扰预测的原理。据此提出 CleanSight，一种无需训练、即插即用的测试时防御方法。它通过监测跨模态注意力比例检测中毒样本，并剪枝可疑的高注意力视觉 Token，在保持模型性能的同时有效中和后门激活。
selection_source: fresh_fetch
motivation: 现有的后门防御通常依赖昂贵的重训练且会降低模型性能，亟需一种高效的测试时防御方案。
method: 提出 CleanSight，通过分析跨模态融合层的视觉-文本注意力比例来检测中毒输入，并选择性剪枝高注意力的视觉 Token。
result: 实验证明 CleanSight 在多种数据集和攻击类型上均优于像素级净化防御，且能很好地保持模型在干净样本上的效用。
conclusion: 本文通过揭示后门攻击的注意力窃取机制，为 LVLM 提供了一种无需重训练的高效、通用的测试时防御新范式。
---

## 摘要
尽管大视觉语言模型（LVLMs）具有强大的多模态性能，但在微调过程中容易受到后门攻击。攻击者通过在训练数据中插入嵌入触发器的样本，植入可在测试时被恶意激活的行为。现有的防御方法通常依赖于使用干净数据重新训练受后门攻击的参数（例如适配器或 LoRA 模块），这在计算上非常昂贵，且往往会降低模型性能。在这项工作中，我们对 LVLMs 中的后门行为提供了一种新的机理理解：触发器并非通过低级视觉模式影响预测，而是通过异常的跨模态注意力重新分配，即带有触发器的视觉标记（tokens）会从文本上下文中窃取注意力——我们将这一现象称为“注意力窃取”（attention stealing）。受此启发，我们提出了 CleanSight，这是一种无需训练、即插即用的防御方案，纯粹在测试时运行。CleanSight (i) 根据选定跨模态融合层中的相对视觉-文本注意力比例来检测中毒输入，并且 (ii) 通过选择性地剪枝可疑的高注意力视觉标记来净化输入，从而中和后门激活。大量实验表明，CleanSight 在多种数据集和后门攻击类型上显著优于现有的基于像素的净化防御，同时保留了模型在干净样本和中毒样本上的效用。

## Abstract
Despite the strong multimodal performance, large vision-language models (LVLMs) are vulnerable during fine-tuning to backdoor attacks, where adversaries insert trigger-embedded samples into the training data to implant behaviors that can be maliciously activated at test time. Existing defenses typically rely on retraining backdoored parameters (e.g., adapters or LoRA modules) with clean data, which is computationally expensive and often degrades model performance. In this work, we provide a new mechanistic understanding of backdoor behaviors in LVLMs: the trigger does not influence prediction through low-level visual patterns, but through abnormal cross-modal attention redistribution, where trigger-bearing visual tokens steal attention away from the textual context - a phenomenon we term attention stealing. Motivated by this, we propose CleanSight, a training-free, plug-and-play defense that operates purely at test time. CleanSight (i) detects poisoned inputs based on the relative visual-text attention ratio in selected cross-modal fusion layers, and (ii) purifies the input by selectively pruning the suspicious high-attention visual tokens to neutralize the backdoor activation. Extensive experiments show that CleanSight significantly outperforms existing pixel-based purification defenses across diverse datasets and backdoor attack types, while preserving the model's utility on both clean and poisoned samples.

---

## 论文详细总结（自动生成）

### 针对受后门攻击的大视觉语言模型的测试时注意力净化 (CleanSight) 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大视觉语言模型（LVLM）在下游任务微调（如使用适配器或 LoRA）时，极易受到后门攻击。攻击者通过注入带有触发器的中毒样本，使模型在测试时遇到特定触发器即输出恶意结果。
*   **研究动机**：现有的防御手段主要依赖于使用干净数据重新训练（Retraining），这不仅计算成本高昂，且往往会破坏模型在下游任务上的性能。
*   **核心发现**：作者发现 LVLM 的后门机制并非源于低级像素模式，而是一种**“注意力窃取”（Attention Stealing）**现象——即触发器区域的视觉 Token 会在跨模态融合层中异常地夺取本应分配给文本上下文的注意力，从而主导模型输出。

#### 2. 论文提出的方法论：CleanSight
CleanSight 是一种**无需训练、即插即用**的测试时防御框架，包含两个核心步骤：
*   **核心思想**：通过干预非参数化的数据流（注意力分配）而非修改模型参数来消除后门。
*   **检测阶段（Detection）**：
    *   在选定的中间跨模态融合层中，计算每个注意力头（Head）的**视觉-文本注意力比例** $S_{\ell,h}$。
    *   将这些比例拼接成特征向量 $s$，并利用少量干净样本建立参考分布（均值 $\mu$ 和标准差 $\sigma$）。
    *   计算测试样本的**白化 $\ell_2$ 距离** $d(\hat{s})$，若超过阈值 $\gamma$ 则判定为中毒输入。
*   **净化阶段（Purification）**：
    *   **选择性剪枝**：识别出注意力值超过阈值 $\tau$ 的异常视觉 Token，并生成全局掩码 $\Omega$。
    *   **注意力抑制**：在后续所有层和解码步骤中，通过在注意力 Logits 中加入极大的负偏置，强制使这些“小偷”Token 的注意力权重归零，从而阻断后门激活。

#### 3. 实验设计
*   **数据集/场景**：
    *   **视觉问答 (VQA)**：VQAv2, OKVQA。
    *   **图像描述 (Captioning)**：MSCOCO, Flickr8k。
*   **受害模型 (Victim Models)**：LLaVA-1.5 (7B/13B), InstructBLIP (7B), Qwen2-VL (7B), Qwen3-VL (2B/4B/8B/32B)。
*   **后门攻击类型 (6种)**：BadNet (补丁), Blended (全局混合), ISSBA (不可见), WaNet (扭曲), 以及针对 LVLM 的 TrojVLM 和 VLOOD。
*   **对比方法 (Baselines)**：高斯模糊 (Blur)、空间变换 (ST defense)、BDMAE、SampDetox、ZIP。
*   **评价指标**：攻击成功率 (ASR)、干净样本效用 (CU, 如 V-score/CIDEr)、中毒样本效用 (PU)。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到微调 LLaVA-1.5-7B 适配器时使用了 3000 个样本（其中 1500 个中毒），这暗示了实验在常规科研级 GPU 集群（如 A100 或 H100）上是可行的。由于 CleanSight 本身是测试时防御，其推理开销极低，不需要额外的训练算力。

#### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了从 2B 到 32B 不同参数规模的模型，跨越了多个任务领域（VQA 和 Captioning），并针对 6 种主流攻击进行了详尽测试。
*   **消融实验**：作者对检测模块、剪枝模块、检测层选择、阈值敏感度以及干净验证集大小（$|D_{val}|$）进行了深入的消融研究。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最新的像素级净化方法，还考虑了**自适应攻击者（Adaptive Attackers）**的情况，验证了防御的鲁棒性。

#### 6. 主要结论与发现
*   **高效性**：CleanSight 能将几乎所有攻击的 ASR 降至接近 0%，显著优于基于像素变换的传统方法。
*   **性能保持**：在防御中毒样本的同时，CleanSight 对干净样本的性能几乎没有负面影响（CU 保持稳定）。
*   **机理验证**：通过 Logit Lens 分析证实，后门触发器主要在跨模态融合后的后期层改变预测轨迹，而 CleanSight 能成功纠正这一轨迹。
*   **通用性**：该方法在不同架构（LLaVA, Qwen, InstructBLIP）和不同规模的模型上均表现出一致的有效性。

#### 7. 优点
*   **理论新颖**：首次提出了“注意力窃取”这一机械论解释，为理解多模态后门提供了新视角。
*   **零成本部署**：无需重新训练模型，无需访问训练数据，适合“微调即服务”（FTaaS）等黑盒场景。
*   **精准打击**：相比于模糊或裁剪整张图像，CleanSight 只针对异常 Token 进行剪枝，最大限度保留了图像的语义信息。

#### 8. 不足与局限
*   **层依赖性**：防御效果依赖于正确识别“跨模态融合层”，对于结构迥异的新型模型，可能需要重新分析融合发生的层位置。
*   **自适应风险**：虽然测试了简单的自适应攻击，但如果攻击者能设计出完全不依赖注意力尖峰的后门（尽管目前看来很难），该方法可能会失效。
*   **超参数敏感性**：虽然实验显示阈值 $\tau$ 和 $\gamma$ 有一定的鲁棒区间，但在极端分布偏移下，可能仍需微调这些参数以平衡误报率和漏报率。

（完）
