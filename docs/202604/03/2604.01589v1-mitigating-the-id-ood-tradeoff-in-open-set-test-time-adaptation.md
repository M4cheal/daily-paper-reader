---
title: Mitigating the ID-OOD Tradeoff in Open-Set Test-Time Adaptation
title_zh: 缓解开集测试时自适应中的 ID-OOD 权衡
authors: "Wenjie Zhao, Jia Li, Xin Dong, Yapeng Tian, Yu Xiang, Yunhui Guo"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01589v1"
tags: ["query:tta"]
score: 10.0
evidence: 针对ID-OOD权衡的开集测试时间自适应
tldr: 针对开放集测试时自适应（OSTTA）中熵最小化（提升ID性能）与熵最大化（增强OOD检测）之间的内在冲突，本文提出了ROSETTA方法。该方法通过引入角度损失调节特征范数，并结合特征范数损失抑制离群样本的逻辑值响应，有效缓解了ID分类与OOD检测之间的性能权衡。实验证明，ROSETTA在多个图像分类及语义分割基准数据集上均显著提升了模型在分布偏移下的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的熵优化策略在处理受分布偏移影响的ID和OOD样本时，存在分类精度与检测能力难以兼顾的内在冲突。
method: 提出ROSETTA框架，利用角度损失调节特征范数大小，并配合特征范数损失来抑制csOOD样本的逻辑值。
result: 在CIFAR-C、ImageNet-C及Cityscapes等多个数据集上，该方法在保持高ID分类准确率的同时显著增强了OOD检测性能。
conclusion: ROSETTA通过优化特征空间分布成功解决了OSTTA中的ID-OOD权衡问题，为开放环境下的模型自适应提供了稳健的解决方案。
---

## 摘要
开集测试时自适应（OSTTA）旨在解决将模型迁移到新环境的挑战，在这些环境中，受分布偏移影响的分布内（ID）样本与分布外（OOD）样本共存。在这种设置下，协变量偏移（例如降雪等天气条件的变化）会改变 ID 样本，从而降低模型的可靠性。因此，模型不仅必须正确分类协变量偏移的 ID（csID）样本，还必须有效地拒绝协变量偏移的 OOD（csOOD）样本。熵最小化是测试时自适应中在分布偏移下维持 ID 性能的常用策略，而熵最大化则被广泛应用于增强 OOD 检测。若干研究试图结合这些目标来应对 OSTTA 的挑战。然而，熵最小化与最大化之间的内在冲突不可避免地导致了 csID 分类与 csOOD 检测之间的权衡。在本文中，我们首先分析了 OSTTA 中熵最大化的局限性，然后引入了一种角损失（angular loss）来调节特征范数大小，并结合特征范数损失（feature-norm loss）来抑制 csOOD 的 logits，从而提高 OOD 检测性能。这些目标构成了 ROSETTA，一种鲁棒的开集测试时自适应方法。我们的方法在 CIFAR-10-C、CIFAR-100-C、Tiny-ImageNet-C 和 ImageNet-C 上实现了强大的 OOD 检测能力，同时保持了较高的 ID 分类性能。此外，在 Cityscapes 上的实验验证了该方法在真实场景语义分割中的有效性，而在 HAC 数据集上的结果证明了其在不同开集 TTA 设置下的适用性。

## Abstract
Open-set test-time adaptation (OSTTA) addresses the challenge of adapting models to new environments where out-of-distribution (OOD) samples coexist with in-distribution (ID) samples affected by distribution shifts. In such settings, covariate shift-for example, changes in weather conditions such as snow-can alter ID samples, reducing model reliability. Consequently, models must not only correctly classify covariate-shifted ID (csID) samples but also effectively reject covariate-shifted OOD (csOOD) samples. Entropy minimization is a common strategy in test-time adaptation to maintain ID performance under distribution shifts, while entropy maximization is widely applied to enhance OOD detection. Several studies have sought to combine these objectives to tackle the challenges of OSTTA. However, the intrinsic conflict between entropy minimization and maximization inevitably leads to a trade-off between csID classification and csOOD detection. In this paper, we first analyze the limitations of entropy maximization in OSTTA and then introduce an angular loss to regulate feature norm magnitudes, along with a feature-norm loss to suppress csOOD logits, thereby improving OOD detection. These objectives form ROSETTA, a $\underline{r}$obust $\underline{o}$pen-$\underline{se}$t $\underline{t}$est-$\underline{t}$ime $\underline{a}$daptation. Our method achieves strong OOD detection while maintaining high ID classification performance on CIFAR-10-C, CIFAR-100-C, Tiny-ImageNet-C and ImageNet-C. Furthermore, experiments on the Cityscapes validate the method's effectiveness in real-world semantic segmentation, and results on the HAC dataset demonstrate its applicability across different open-set TTA setups.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mitigating the ID-OOD Tradeoff in Open-Set Test-Time Adaptation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**开集测试时自适应（Open-Set Test-Time Adaptation, OSTTA）**。在现实场景中，模型在推理阶段不仅会遇到受环境干扰（如雨、雪、雾）的分布内样本（csID），还会遇到同样受干扰的分布外样本（csOOD）。
*   **研究动机**：现有的 OSTTA 方法通常结合“熵最小化”（提升 ID 分类置信度）和“熵最大化”（增强 OOD 检测能力）。然而，作者发现这两者存在**内在冲突**：熵最小化会无差别地增加所有样本的特征范数，导致 ID 和 OOD 难以区分；而 OOD 检测器的不完美会导致错误的优化方向，从而在提升检测能力时损害分类精度，反之亦然。
*   **核心目标**：解耦 ID 分类适配与 OOD 检测的目标，缓解两者之间的性能权衡（Trade-off）。

### 2. 方法论：ROSETTA 框架
作者提出了 **ROSETTA** 框架，其核心思想是通过特征空间的几何约束（角度与范数）来代替冲突的熵优化。

*   **核心思想**：
    1.  **角度对齐代替范数增长**：通过增强特征与类原型之间的方向一致性来提升 ID 置信度，而非盲目增大特征向量的长度。
    2.  **特征范数抑制**：利用 OOD 样本通常具有较小特征激活的特性，显式抑制疑似 OOD 样本的范数。
*   **关键技术细节**：
    *   **Angular Loss ($L_{ang}$)**：维护一个类原型的运行均值（Running Mean），计算 csID 样本特征与对应原型之间的余弦相似度，通过最小化该损失使特征在角度上向原型靠拢。
    *   **Feature Norm Loss ($L_{norm}$)**：对识别为 csOOD 的样本施加 $L_1$ 范数惩罚，压低其 Logits 响应，从而在特征强度上拉开 ID 与 OOD 的差距。
    *   **整体优化目标**：$L_{ROSETTA} = L_{csID} + \gamma_1 L_{ang} + \gamma_2 L_{norm}$。其中 $L_{csID}$ 包含基础的熵最小化和多样性正则项。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：CIFAR-10-C、CIFAR-100-C、Tiny-ImageNet-C、ImageNet-C（均包含 15 种腐蚀类型）。
    *   **语义分割**：Cityscapes 迁移至 ACDC（包含雾、夜、雨、雪四种恶劣天气）。
    *   **多模态动作识别**：HAC 数据集（涉及人、动物、卡通三种域的迁移）。
*   **Benchmark 与对比方法**：
    *   对比了 TENT、EATA、CoTTA、OSTTA 等基础 TTA 方法。
    *   重点对比了当前的 SOTA 开集方法：**UniEnt** 和 **UniEnt+**。
*   **评价指标**：分类准确率（Acc）、AUROC、FPR95、OSCR（综合衡量分类与检测）以及 H-score。

### 4. 资源与算力
*   **硬件环境**：使用了 4 台 NVIDIA RTX A5000 显卡，配备 512 GB RAM。
*   **时间开销**：单次实验通常在 4 小时内完成。
*   **计算效率**：文中提到 ROSETTA 相比 AEO 方法仅增加了极小的计算延迟（每批次从 2.29s 增至 2.52s）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主流分类基准、1 个分割基准和 1 个多模态基准上进行了测试，涵盖了从小型（CIFAR）到大型（ImageNet）数据集。
*   **消融实验**：详细验证了 $L_{ang}$ 和 $L_{norm}$ 各自的贡献，并展示了它们如何改变特征分布。
*   **鲁棒性分析**：针对 ID/OOD 比例失衡、完全没有 OOD 样本、以及未知类别数量变化等极端情况进行了补充实验。
*   **客观性**：实验采用了统一的预训练模型（如 RobustBench 提供的模型）和相同的超参数设置，对比过程较为公平。

### 6. 主要结论与发现
*   **解耦有效性**：ROSETTA 成功解决了 ID 分类和 OOD 检测的冲突。在增加 OOD 损失权重时，ROSETTA 能在提升检测性能的同时保持 ID 准确率稳定，而基线方法（UniEnt）的 ID 准确率会大幅下降。
*   **性能领先**：在所有测试的分类基准上，ROSETTA 在 OSCR 指标上均优于 UniEnt+。
*   **通用性**：该方法不仅适用于分类，在语义分割和多模态任务中也表现出良好的适配性。

### 7. 优点（亮点）
*   **理论洞察深**：通过观察特征范数在熵优化过程中的变化，指出了现有方法失效的根本原因（特征范数膨胀）。
*   **即插即用**：ROSETTA 作为一个损失函数项，可以轻松集成到 TENT、EATA 等现有的测试时自适应框架中。
*   **几何直观**：利用角度和范数这两个几何维度来区分样本，比单纯的概率值（熵）更具鲁棒性。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文做了敏感性分析，但 $\gamma_1$ 和 $\gamma_2$ 在不同规模的数据集（如 CIFAR vs ImageNet）上仍需要调整，缺乏一种完全自动化的参数设置机制。
*   **初始检测器依赖**：尽管 ROSETTA 缓解了冲突，但其性能仍部分取决于初始阶段对 ID/OOD 的粗略划分（如基于能量或熵的过滤）。
*   **特征范数异常**：作者在附录中提到，在某些大型模型（如 ResNet50）适配初期，csID 的范数竟然可能小于 csOOD，这与直觉相反，该现象的深层原因尚待研究。

（完）
