---
title: Test-Time Adaptation for Height Completion via Self-Supervised ViT Features and Monocular Foundation Models
title_zh: 基于自监督 ViT 特征与单目基础模型的测试时自适应高度补全
authors: "Osher Rafaeli, Tal Svoray, Ariel Nahlieli"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02009v1"
tags: ["query:tta"]
score: 10.0
evidence: 利用自监督特征进行高度补全的测试时间自适应
tldr: 本研究提出Prior2DSM，一种无需训练的测试时自适应框架，用于解决数字表面模型（DSM）的缺失补全问题。该方法结合DINOv3的自监督ViT特征与单目深度基础模型，通过LoRA和轻量级MLP在测试阶段学习空间变化的缩放与偏移参数，将局部度量先验传播至全局。该框架在保持结构保真度的同时显著提升了补全精度，为地理空间数据的动态更新提供了高效方案。
selection_source: fresh_fetch
motivation: 传统DSM补全方法依赖空间插值或特定领域的监督学习，在处理复杂地物缺失时效果不佳且泛化能力受限。
method: 利用单目深度模型生成相对深度，并通过DINOv3特征引导的测试时自适应（TTA）技术，将不完整的度量高度先验转化为全局精确高度。
result: "实验显示该方法显著优于插值和主流深度估计模型，在RMSE指标上比传统线性拟合方法降低了高达46%。"
conclusion: Prior2DSM证明了结合基础模型与测试时自适应技术，可以在无监督训练的情况下实现高精度的度量级DSM补全与更新。
---

## 摘要
精确的数字地表模型 (DSM) 对于城市监测、环境分析、基础设施管理和变化检测等许多地理空间应用至关重要。然而，由于采集限制、重建伪影或建筑环境的变化，大规模 DSM 经常包含不完整或过时的区域。传统的补全方法主要依赖于空间插值，这些方法假设空间连续性，因此在物体缺失时会失效。最近的基于学习的方法提高了重建质量，但通常需要对特定传感器的数据集进行监督训练，限制了它们在不同领域和传感条件下的泛化能力。我们提出了 Prior2DSM，这是一个无需训练的度量级 DSM 补全框架，它通过利用基础模型完全在测试时运行。与以往需要特定任务训练的高度补全方法不同，该方法结合了来自 DINOv3 的自监督视觉 Transformer (ViT) 特征与单目深度基础模型，通过语义特征空间对应关系从不完整的高度先验中传播度量信息。测试时自适应 (TTA) 是使用参数高效的低秩自适应 (LoRA) 结合轻量级多层感知器 (MLP) 执行的，该感知器预测空间变化的缩放和偏移参数，以将相对深度估计转换为度量高度。实验表明，该方法相比于基于插值的方法、基于先验的缩放高度方法以及最先进的单目深度估计模型具有持续的改进。Prior2DSM 在保持结构保真度的同时降低了重建误差，与 MDE 的线性拟合相比，RMSE 降低了高达 46%，并进一步实现了 DSM 更新和耦合的 RGB-DSM 生成。

## Abstract
Accurate digital surface models (DSMs) are essential for many geospatial applications, including urban monitoring, environmental analyses, infrastructure management, and change detection. However, large-scale DSMs frequently contain incomplete or outdated regions due to acquisition limitations, reconstruction artifacts, or changes in the built environment. Traditional height completion approaches primarily rely on spatial interpolation or which assume spatial continuity and therefore fail when objects are missing. Recent learning-based approaches improve reconstruction quality but typically require supervised training on sensor-specific datasets, limiting their generalization across domains and sensing conditions. We propose Prior2DSM, a training-free framework for metric DSM completion that operates entirely at test time by leveraging foundation models. Unlike previous height completion approaches that require task-specific training, the proposed method combines self-supervised Vision Transformer (ViT) features from DINOv3 with monocular depth foundation models to propagate metric information from incomplete height priors through semantic feature-space correspondence. Test-time adaptation (TTA) is performed using parameter-efficient low-rank adaptation (LoRA) together with a lightweight multilayer perceptron (MLP), which predicts spatially varying scale and shift parameters to convert relative depth estimates into metric heights. Experiments demonstrate consistent improvements over interpolation based methods, prior-based rescaling height approaches, and state-of-the-art monocular depth estimation models. Prior2DSM reduces reconstruction error while preserving structural fidelity, achieving up to a 46% reduction in RMSE compared to linear fitting of MDE, and further enables DSM updating and coupled RGB-DSM generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Test-Time Adaptation for Height Completion via Self-Supervised ViT Features and Monocular Foundation Models》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在高分辨率遥感图像中，利用不完整或过时的数字表面模型（DSM）先验，实现精确的度量级高度补全？
*   **研究背景**：
    *   **传统方法局限**：空间插值（如克里金法）假设地表连续，在建筑物等物体完全缺失时失效。
    *   **学习方法局限**：现有的监督学习模型通常针对特定传感器或数据集训练，泛化能力差，且难以处理单目高度估计中的尺度歧义（Scale Ambiguity）。
    *   **动机**：利用视觉基础模型（Foundation Models）强大的泛化能力，通过测试时自适应（TTA）技术，在无需针对性训练的情况下，将稀疏的度量先验传播到全局。

### 2. 核心方法论
论文提出了 **Prior2DSM** 框架，其核心思想是将高度补全视为一种“特征引导的校正过程”：
*   **多模态输入**：输入单幅 RGB 图像、不完整的度量高度先验（Prior DSM）以及变化掩码（Change Mask）。
*   **相对几何提取**：利用单目深度估计（MDE）基础模型（如 Depth Anything V2）生成初始的相对高度图 $H_{rel}$。
*   **语义特征提取**：使用自监督 ViT 模型（DINOv3）提取密集的语义特征。为了解决 ViT 补丁导致的“块状”效应，采用了**步长重叠累加策略（Strided Overlap）**，将采样密度提升了 16 倍。
*   **测试时自适应（TTA）**：
    *   **LoRA 适配**：在 DINOv3 的注意力层注入低秩自适应（LoRA）参数，仅优化少量参数以适应当前场景。
    *   **轻量级 MLP**：预测空间变化的缩放（Scale, $s$）和偏移（Shift, $b$）参数。
    *   **度量对齐公式**：最终高度预测为 $\hat{z} = s \cdot r + b$，其中 $r$ 是相对深度。在测试阶段，利用已知的度量先验点作为监督信号，通过反向传播优化 LoRA 和 MLP 参数。

### 3. 实验设计
*   **数据集**：
    *   **Denver NAIP**：航空影像（30cm 分辨率），地形复杂，含高层建筑。
    *   **Fresno WorldView-3 (WV3)**：卫星影像（60cm 分辨率），以住宅区为主。
*   **实验场景**：设置了 25%、50%、75% 三种不同程度的高度先验缺失率，通过 LULC 数据随机移除建筑物或植被。
*   **对比基准（Benchmarks）**：
    *   **传统插值**：双线性插值。
    *   **对齐策略**：全局仿射变换、局部加权线性回归（LWLR）、空间 kNN。
    *   **SOTA 模型**：RS3DAda（单目高度模型）、Marigold-DC（基于扩散的深度补全）、PriorDA（基于先验的深度对齐）。

### 4. 资源与算力
*   **硬件设备**：实验使用了 **NVIDIA RTX 4000 GPU**。
*   **时间成本**：在 100 个优化步骤下，处理每平方公里的 DSM 更新大约需要 **8.2 分钟**。
*   **效率说明**：虽然 TTA 增加了推理时的计算开销，但对于局部区域（如单个街区）的更新，仅需数秒即可完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同来源（航空与卫星）、不同城市形态的代表性数据集。
*   **消融实验**：
    1.  **MDE 的必要性**：对比了直接从 MLP 预测高度与通过 MLP 缩放 MDE 结果的效果。
    2.  **LoRA 的作用**：验证了冻结 DINOv3 与开启 LoRA 适配的性能差异。
    3.  **骨干网络敏感性**：测试了不同 MDE 模型（DAv2, Depth Pro, MoGe-2）作为底座的表现。
*   **充分性评价**：实验设计较为全面，不仅有定量指标（MAE, RMSE, SSIM），还包含了真实世界的 DSM 历史数据更新实验和合成数据的 RGB-DSM 协同编辑实验，客观地展示了模型在不同缺失率下的鲁棒性。

### 6. 主要结论与发现
*   **精度提升**：Prior2DSM 在所有缺失水平下均优于对比方法。在 Denver 数据集上，相比 MDE 的线性拟合，**RMSE 降低了高达 46%**。
*   **结构保真度**：由于引入了 DINOv3 的语义特征，补全后的 DSM 在建筑物边缘和结构细节上（SSIM 指标）显著优于基于空间邻域的插值方法。
*   **模型通用性**：该框架是模型无关的（Model-agnostic），可以无缝集成最新的单目深度基础模型（如 MoGe-2 表现最佳）。
*   **语义传播优势**：实验证明，在特征空间进行高度信息传播比在空间域（kNN）更有效，尤其是在大面积数据缺失的情况下。

### 7. 优点与亮点
*   **无需训练（Training-free）**：完全在测试时运行，避免了对大规模标注遥感数据集的依赖，解决了传感器偏差问题。
*   **语义与几何耦合**：巧妙地利用自监督 ViT 特征来引导几何缩放，使得空间上不连续但语义相似的物体（如两栋相同的建筑）可以共享度量校正参数。
*   **高分辨率输出**：通过步长重叠策略克服了 ViT 的分辨率限制，生成了细节丰富的度量级 DSM。

### 8. 不足与局限
*   **计算延迟**：TTA 过程涉及迭代优化，相比于直接推理的深度学习模型，其实时性较差，不适合超大规模区域的即时处理。
*   **误差传播**：最终结果高度依赖于底层 MDE 模型生成的相对几何质量。如果 MDE 模型在某些地形（如极度阴影区）出现结构性错误，TTA 难以完全修复。
*   **先验依赖**：虽然能处理高缺失率，但仍需要一定比例的准确度量点作为锚点；在完全没有任何先验的区域，模型退化为普通的单目估计。

（完）
