---
title: "Pref-CTRL: Preference Driven LLM Alignment using Representation Editing"
title_zh: Pref-CTRL：使用表示编辑的偏好驱动大语言模型对齐
authors: "Imranul Ashrafi, Inigo Jauregi Unanue, Massimo Piccardi"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23543v1"
tags: ["query:tta"]
score: 8.0
evidence: 通过表示编辑进行测试时间对齐
tldr: 本研究针对大语言模型对齐问题，提出了一种名为Pref-CTRL的推理阶段对齐框架。该方法改进了现有的表征编辑技术，通过引入基于偏好数据的多目标价值函数，在推理时对模型内部状态进行轻量级干预。实验证明，Pref-CTRL在多个基准测试中优于RE-Control，并展现出更强的跨领域泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.23543v1/fig-001.webp\", \"caption\": \"Figure 1: Example responses from our proposed method and the baseline model given a harmful prompt.\", \"page\": 1, \"index\": 1, \"width\": 458, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2604.23543v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of Pref-CTRL: During value function training, preferred, rejected, and LLM-generated hidden states are extracted from a frozen LLM using preference data, unlike RLHF which fine-tunes the model. The value function estimates a reward for each hidden state, which are then used to train the objective loss functions: LRegression, LMargin, and LRegularizer.\", \"page\": 2, \"index\": 2, \"width\": 942, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.23543v1/fig-003.webp\", \"caption\": \"Table 1: Main results across datasets. Bold indicates best scores and Italicized rows indicate training-time methods (for distant comparison only). For the Win Rate & Avg. Reward, † indicates statistical significance with p < 0.05 – pairwise bootstrap test (Dror et al., 2018). Pref-CTRL shows consistent improvements over RE-Control, with similar diversity and coherence.\", \"page\": 3, \"index\": 3, \"width\": 952, \"height\": 407}, {\"url\": \"assets/figures/arxiv/2604.23543v1/fig-004.webp\", \"caption\": \"Figure 4: Prompt template for Win Rate evaluation.\", \"page\": 9, \"index\": 4, \"width\": 950, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2604.23543v1/fig-005.webp\", \"caption\": \"Figure 3: Effect of different step sizes (α) and numbers of steps (k) during inference on our best-performing model (Margin + Regularizer). The analysis was conducted on the HH-RLHF dataset using the Hermes3 base model.\", \"page\": 5, \"index\": 5, \"width\": 950, \"height\": 373}]"
motivation: 现有的推理阶段对齐方法忽略了对齐任务通常基于人类偏好数据这一核心特征。
method: 提出Pref-CTRL框架，利用基于偏好数据训练的多目标价值函数，通过梯度编辑引导模型生成。
result: 在两个基准数据集上超越了RE-Control，并在域外数据集上表现出更优的泛化性能。
conclusion: Pref-CTRL证明了结合偏好学习的表征编辑是实现大语言模型高效、精准对齐的有效途径。
---

## 摘要
测试时对齐方法通过在推理时对大语言模型（LLMs）的内部表示进行轻量级干预来引导其输出，为微调提供了一种极具前景的替代方案。最近，一种显著且有效的方法 RE-Control (Kong et al., 2024) 提出利用在大语言模型隐藏状态上训练的外部价值函数，通过基于梯度的编辑来引导生成。尽管该方法有效，但它忽略了对齐任务的一个关键特征，即对齐任务通常被表述为从候选回复之间的人类偏好中学习。为了解决这一问题，本文提出了一种新颖的基于偏好的训练框架 Pref-CTRL，该框架使用多目标价值函数来更好地反映偏好数据的结构。我们的方法在两个基准数据集上优于 RE-Control，并在域外数据集上表现出更强的泛化能力。我们的源代码可在 https://github.com/UTS-nlPUG/pref-ctrl 获取。

## Abstract
Test-time alignment methods offer a promising alternative to fine-tuning by steering the outputs of large language models (LLMs) at inference time with lightweight interventions on their internal representations. Recently, a prominent and effective approach, RE-Control (Kong et al., 2024), has proposed leveraging an external value function trained over the LLM's hidden states to guide generation via gradient-based editing. While effective, this method overlooks a key characteristic of alignment tasks, i.e. that they are typically formulated as learning from human preferences between candidate responses. To address this, in this paper we propose a novel preference-based training framework, Pref-CTRL, that uses a multi-objective value function to better reflect the structure of preference data. Our approach has outperformed RE-Control on two benchmark datasets and showed greater generalization on out-of-domain datasets. Our source code is available at https://github.com/UTS-nlPUG/pref-ctrl.

---

## 论文详细总结（自动生成）

这是一份关于论文《Pref-CTRL: Preference Driven LLM Alignment using Representation Editing》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）的对齐（如 RLHF）通常依赖于计算成本高昂的微调过程。为了提高效率，**测试时对齐（Test-time Alignment）**技术应运而生，它通过在推理阶段干预模型的内部表示（隐藏状态）来引导生成。
*   **研究动机**：现有的代表性方法 RE-Control 虽然有效，但其价值函数仅基于单一的奖励分数进行训练，忽略了对齐任务中核心的**偏好结构**（即人类在两个候选回复中更倾向于哪一个）。
*   **核心问题**：如何利用成对的偏好数据（Preferred vs. Rejected）来增强推理阶段的表示编辑效果，使模型输出更符合人类预期。

### 2. 方法论：Pref-CTRL 框架
Pref-CTRL 在 RE-Control 的基础上，将对齐视为一个受控的动力系统，通过一个多目标的价值函数来引导隐藏状态的演化。
*   **核心思想**：在训练价值函数 $V_\phi$ 时，不仅要求其预测奖励值，还要求其能够显式区分“偏好回复”和“拒绝回复”的内部表示。
*   **关键技术细节（损失函数）**：
    1.  **回归损失 ($L_{Regression}$)**：确保价值函数能准确估计状态的预期奖励。
    2.  **边际损失 ($L_{Margin}$)**：通过最大化偏好回复和拒绝回复末端状态之间的分值差距，强制价值函数捕捉相对排名。
    3.  **正则化损失 ($L_{Regularizer}$)**：鼓励模型生成的当前状态与偏好回复的状态保持接近，防止在梯度上升过程中出现“奖励作弊”（Reward Hacking）或生成不连贯的文本。
*   **推理流程**：在生成每个 Token 时，利用价值函数的梯度对隐藏状态进行迭代更新（梯度上升），从而实时修正生成方向。

### 3. 实验设计
*   **数据集**：
    *   **核心对齐任务**：Stanford SHP（Reddit 评论偏好）和 Anthropic HH-RLHF（有用性与无害性）。
    *   **域外泛化测试**：PKU-SafeRLHF（安全对齐）和 Nectar（多模型排序数据集）。
*   **基座模型**：Vicuna-7B 和 Hermes-3-Llama-3.1-8B。
*   **对比方法（Benchmark）**：
    *   **RE-Control**：最直接的推理阶段表示编辑基准。
    *   **DPO (Direct Preference Optimization)**：强力的训练阶段微调方法（作为性能上限参考）。
    *   **Best-of-N** 和 **CAST**：其他测试时对齐策略。
*   **评估指标**：LLM-as-a-judge 胜率（使用 Llama-3.3-70B、DeepSeek-R1 和 GPT-5.1 作为裁判）、平均奖励分、多样性及连贯性。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **模型规模**：价值函数本身是一个极其轻量的 3 层 MLP（多层感知机），输入维度与 LLM 隐藏层一致（如 4096），这表明其训练和推理的计算开销远低于全量微调。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了 2 个基座模型、2 个训练数据集、2 个泛化测试集，并使用了 3 种不同的顶级 LLM 作为裁判，实验组数较为丰富。
*   **消融实验**：专门对比了“仅边际损失”与“边际+正则化损失”的效果，验证了正则化在防止过度优化方面的必要性。
*   **客观性**：通过统计显著性检验（p < 0.05）和超参数敏感性分析（步长 $\alpha$ 和步数 $k$），证明了性能提升并非偶然，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能超越**：Pref-CTRL 在所有基准测试中均一致优于 RE-Control，显著提升了胜率和平均奖励分。
*   **逼近微调效果**：作为一种无需更新模型权重的测试时方法，Pref-CTRL 的表现非常接近经过 LoRA 微调的 DPO 模型。
*   **强泛化性**：在未见过的安全数据集（PKU-SafeRLHF）上，Pref-CTRL 表现出比 RE-Control 更好的零样本迁移能力。
*   **质量保持**：在提升对齐性能的同时，模型生成的文本多样性和连贯性未受明显损害。

### 7. 优点
*   **结构化利用偏好**：首次将偏好对的对比逻辑引入表示编辑的价值函数训练中。
*   **轻量且灵活**：无需微调庞大的 LLM，仅需训练一个极小的 MLP 即可实现强大的对齐控制。
*   **鲁棒性**：引入正则化项有效缓解了推理阶段梯度优化常见的“奖励作弊”问题。

### 8. 不足与局限
*   **超参数依赖**：推理时的梯度上升步长和步数需要针对不同任务进行微调，缺乏自适应机制。
*   **任务局限**：目前的实验主要集中在单轮对话（Single-turn），在长文本或多轮对话中的表现尚待验证。
*   **外部依赖**：高度依赖外部奖励模型（Reward Model）的质量，如果奖励模型本身存在偏见，Pref-CTRL 可能会放大这些偏差。

（完）
