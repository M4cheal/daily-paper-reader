---
title: "MemCoT: Test-Time Scaling through Memory-Driven Chain-of-Thought"
title_zh: MemCoT：通过记忆驱动思维链实现的测试时扩展
authors: "Haodong Lei, Junming Liu, Yirong Chen, Ding Wang, Hongsong Wang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08216v1"
tags: ["query:tta"]
score: 8.0
evidence: 长上下文推理的测试时内存扩展
tldr: 针对大语言模型在处理海量碎片化长上下文时存在的幻觉和遗忘问题，本文提出MemCoT框架。该框架将长文本推理转化为迭代的状态化信息搜索，通过多视图长期记忆感知模块实现证据定位与上下文重构，并结合双短期记忆系统动态引导查询分解。MemCoT在多个长文本基准测试中达到SOTA水平，显著提升了模型的长程因果推理能力。
selection_source: fresh_fetch
motivation: 现有的静态单步检索机制在处理长上下文时会导致严重的语义稀释和上下文碎片化，难以支持复杂的因果推理。
method: 提出一种基于记忆驱动的思维链框架，利用多视图长期记忆进行证据定位与扩展，并结合双短期记忆系统记录搜索决策以指导迭代推理。
result: 实验表明，MemCoT使多种开源和闭源模型在LoCoMo和LongMemEval-S等长文本基准测试中均取得了当前最先进的性能。
conclusion: MemCoT通过将推理过程重定义为动态、状态化的记忆搜索，有效解决了长上下文推理中的核心瓶颈，实现了测试时性能的显著扩展。
---

## 摘要
大语言模型（LLMs）在处理海量、碎片化的长上下文进行因果推理时，仍面临严重的幻觉和灾难性遗忘问题。现有的记忆机制通常将检索视为静态、单步的被动匹配过程，导致严重的语义稀释和上下文碎片化。为了克服这些根本性瓶颈，我们提出了 MemCoT，这是一种测试时记忆扩展框架，它通过将长上下文推理转化为迭代的、有状态的信息搜索，重新定义了推理过程。MemCoT 引入了一个多视图长期记忆感知模块，支持“放大”（Zoom-In）证据定位和“缩小”（Zoom-Out）上下文扩展，使模型能够首先识别相关证据的位置，随后重建推理所需的周边因果结构。此外，MemCoT 采用了一个由语义状态记忆和情节轨迹记忆组成的任务条件双重短期记忆系统。该短期记忆记录了历史搜索决策，并在迭代过程中动态引导查询分解与剪枝。实证评估表明，MemCoT 达到了当前最先进的性能。在 MemCoT 的赋能下，多款开源和闭源模型在 LoCoMo 基准测试和 LongMemEval-S 基准测试中均取得了 SOTA 性能。

## Abstract
Large Language Models (LLMs) still suffer from severe hallucinations and catastrophic forgetting during causal reasoning over massive, fragmented long contexts. Existing memory mechanisms typically treat retrieval as a static, single-step passive matching process, leading to severe semantic dilution and contextual fragmentation. To overcome these fundamental bottlenecks, we propose MemCoT, a test-time memory scaling framework that redefines the reasoning process by transforming long-context reasoning into an iterative, stateful information search. MemCoT introduces a multi-view long-term memory perception module that enables Zoom-In evidence localization and Zoom-Out contextual expansion, allowing the model to first identify where relevant evidence resides and then reconstruct the surrounding causal structure necessary for reasoning. In addition, MemCoT employs a task-conditioned dual short-term memory system composed of semantic state memory and episodic trajectory memory. This short-term memory records historical search decisions and dynamically guides query decomposition and pruning across iterations. Empirical evaluations demonstrate that MemCoT establishes a state-of-the-art performance. Empowered by MemCoT, several open- and closed-source models achieve SOTA performance on the LoCoMo benchmark and LongMemEval-S benchmark.

---

## 论文详细总结（自动生成）

这篇论文提出了 **MemCoT**，一种旨在通过“记忆驱动的思维链”（Memory-Driven Chain-of-Thought）实现大语言模型（LLM）在测试时（Test-Time）性能扩展的框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管 LLM 的上下文窗口已扩展至百万级，但在处理长程、碎片化的信息时，仍面临严重的**幻觉**和**灾难性遗忘**。
*   **研究动机**：作者认为长上下文推理的失败并非容量不足，而是信息搜索与组织方式的问题。现有的记忆机制（如 RAG）多采用“被动匹配”的单步检索模式，这在处理复杂的多跳推理时会导致语义稀释（噪声多）或上下文碎片化（丢失背景）。
*   **整体含义**：MemCoT 将记忆检索从“静态匹配”转变为“动态、有状态的迭代搜索”，通过增加测试时的计算量（推理步骤）来换取更高的推理准确性。

### 2. 方法论
MemCoT 的核心是一个**迭代的记忆-推理循环**，主要包含以下关键技术：
*   **多视图长期记忆感知（Multi-view Perception）**：
    *   **Zoom-In（放大）**：进行精细化的焦点检索，定位关键实体。
    *   **Zoom-Out（缩小）**：基于定位点扩展检索相邻的上下文窗口，重建因果结构。
    *   **视觉接地（Visual Grounding）**：针对多模态记忆，通过 OCR 和空间位置关联提取图像中的宏观结构信息。
*   **双短期记忆系统（Dual Short-term Memory）**：
    *   **语义状态记忆**：存储当前已获取的有效事实。
    *   **情节轨迹记忆**：记录历史搜索决策和失败的查询路径，防止重复搜索。
*   **任务条件演化流程**：
    1.  **感知**：从长期记忆中提取多视图信息。
    2.  **演化**：更新短期记忆状态。
    3.  **评判（Judge Agent）**：判断当前信息是否足以回答问题。若不足，则识别“知识盲点”。
    4.  **修正（Query Refinement）**：对查询进行**分解**（拆为原子问题）或**剪枝**（删除已解决部分），进入下一轮循环。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **LoCoMo**：评估极长程对话记忆，包含单跳、多跳、开放域和时间推理任务。
    *   **LongMemEval-S**：评估长交互记忆，涵盖用户偏好、知识更新等 7 个维度。
*   **对比方法**：包括传统的 RAG、Mem0、MemoryOS、HippoRAG、CompassMem、A-Mem、Mnemis 等多种先进的记忆增强系统。
*   **基础模型**：测试了闭源模型 GPT-4o-mini 以及开源模型 Qwen2.5-14B/7B。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：由于 MemCoT 是一个**无需训练（Training-free）**、即插即用的框架，其核心开销在于推理时的 API 调用或本地模型推理。
*   **成本分析**：文中提供了 Token 消耗分析，指出 MemCoT 的 Token 消耗远低于全上下文处理（Full Context），但在迭代过程中会比单步 RAG 消耗更多 Token（例如在 LoCoMo 上平均迭代约 1.87 - 2.03 次）。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流长文本基准上进行了全面测试，涵盖了从 7B 到 GPT-4 级别的多种模型。
*   **消融实验**：针对 Zoom-Out 模块、视觉模块进行了消融分析，验证了各组件对缓解上下文碎片化的作用。
*   **参数敏感性分析**：对邻近窗口大小（W）、最大迭代次数（J）、Top-K 检索量等关键超参数进行了详细讨论。
*   **充分性评价**：实验设计较为客观、公平，通过对比不同规模的模型证明了该框架的普适性，尤其是能显著提升 7B 小模型的长文本处理能力。

### 6. 主要结论与发现
*   **SOTA 性能**：MemCoT 在 LoCoMo 上取得了 58.03% 的 F1 分数，显著超过所有基线。
*   **测试时扩展有效性**：通过增加推理迭代次数（Test-time compute），模型能够自主纠正检索偏差，解决复杂的多跳逻辑。
*   **小模型逆袭**：配备 MemCoT 的 Qwen2.5-7B 在某些指标上甚至超过了使用全上下文的 GPT-4o-mini，证明了高效记忆管理比单纯增加参数或上下文长度更重要。

### 7. 优点
*   **范式创新**：将记忆检索从“辅助工具”提升为“推理引擎”，提出了记忆驱动的思维链。
*   **多视图感知**：Zoom-In/Out 设计有效解决了 RAG 中常见的“断章取义”问题。
*   **即插即用**：无需对模型进行微调，兼容性强，适合实际 Agent 场景部署。
*   **数学统一性**：论文为单跳和多跳推理提供了统一的概率建模框架。

### 8. 不足与局限
*   **推理延迟**：由于采用迭代循环机制，获取最终答案的时间（Latency）会随迭代次数增加，不适合对实时性要求极高的场景。
*   **依赖评判质量**：系统的效果高度依赖于“Judge Agent”判断知识盲点和修正查询的能力，如果基础模型逻辑能力太弱，可能会陷入无效循环。
*   **视觉实验深度**：虽然提出了视觉接地模块，但在多模态实验部分的广度和深度略逊于文本部分。

（完）
