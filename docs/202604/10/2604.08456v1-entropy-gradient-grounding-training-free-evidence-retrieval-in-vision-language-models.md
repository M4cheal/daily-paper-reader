---
title: "Entropy-Gradient Grounding: Training-Free Evidence Retrieval in Vision-Language Models"
title_zh: 熵梯度定位：视觉语言模型中无需训练的证据检索
authors: "Marcel Gröpl, Jaewoo Jung, Seungryong Kim, Marc Pollefeys, Sunghwan Hong"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08456v1"
tags: ["query:tta"]
score: 9.0
evidence: 视觉语言模型中无需训练的测试时证据检索
tldr: 针对预训练视觉语言模型在处理微小细节和多区域线索时的不足，本文提出了一种无需训练的证据检索方法。该方法通过将模型预测分布的熵回传至视觉标记，生成熵梯度相关性图，从而精确定位关键视觉区域。结合多区域提取和迭代缩放策略，该方法在多个基准测试中显著提升了模型在细节敏感任务上的表现和可解释性。
selection_source: fresh_fetch
motivation: 现有视觉语言模型在处理依赖微小细节或多区域组合信息的复杂查询时，难以准确识别和利用关键视觉证据。
method: 利用模型预测的熵作为监督信号，通过反向传播生成梯度相关性图，并结合迭代缩放机制实现精准的视觉证据检索。
result: 在七个基准测试和四种模型架构上的实验证明，该方法显著提升了模型在细节敏感任务中的准确率，并提供了更具解释性的定位结果。
conclusion: 熵梯度定位提供了一种无需额外训练的通用方案，有效解决了VLM在复杂场景下的视觉定位与证据获取难题。
---

## 摘要
尽管取得了快速进展，但当答案依赖于微小的视觉细节或需要结合分散在多个区域的线索（如在文档和组合式查询中）时，预训练视觉语言模型仍然面临困难。我们将定位（grounding）框架化为测试时的证据检索来解决这一问题：给定一个查询，模型应主动识别下一步该看哪里以消除歧义。为此，我们提出了一种无需训练、模型内在的定位方法，利用不确定性作为监督。具体而言，我们计算模型下一个 token 分布的熵，并将其反向传播到视觉 token 嵌入中，从而获得熵梯度相关性图，而无需辅助检测器或注意力图启发式方法。随后，我们提取并排序多个连贯区域以支持多证据查询，并引入了一种带有空间熵停止规则的迭代缩放与重新定位程序，以避免过度细化。在四种 VLM 架构上的七个基准测试实验表明，该方法相比现有方法取得了持续的改进，在细节关键和高分辨率设置下提升最为显著，同时还能产生更具解释性的证据定位。

## Abstract
Despite rapid progress, pretrained vision-language models still struggle when answers depend on tiny visual details or on combining clues spread across multiple regions, as in documents and compositional queries. We address this by framing grounding as test-time evidence retrieval: given a query, the model should actively identify where to look next to resolve ambiguity. To this end, we propose a training-free, model-intrinsic grounding method that uses uncertainty as supervision. Specifically, we compute the entropy of the model's next-token distribution and backpropagate it to the visual token embeddings to obtain an entropy-gradient relevance map, without auxiliary detectors or attention-map heuristics. We then extract and rank multiple coherent regions to support multi-evidence queries, and introduce an iterative zoom-and-reground procedure with a spatial-entropy stopping rule to avoid over-refinement. Experiments on seven benchmarks across four VLM architectures demonstrate consistent improvements over existing methods, with the largest gains on detail-critical and high-resolution settings, while also producing more interpretable evidence localizations.

---

## 论文详细总结（自动生成）

以下是对论文《Entropy-Gradient Grounding: Training-Free Evidence Retrieval in Vision-Language Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
预训练视觉语言模型（VLM）虽然在通用任务上表现出色，但在处理**微小视觉细节**（如文档中的小字）或**分散在多个区域的线索**（如复杂的组合查询）时经常失败。作者认为，这些失败并非源于生成能力的不足，而是由于**选择性感知（Selective Perception）**的缺陷——模型未能识别出关键的视觉证据。现有的无需训练的定位方法通常依赖于注意力图（Attention Maps），但这往往需要复杂的启发式调优，且容易被单一显著区域主导，难以处理多区域证据。

### 2. 方法论
本文提出了一种名为**熵梯度定位（Entropy-Gradient Grounding）**的框架，将定位视为测试时的证据检索过程：
*   **核心思想**：利用模型预测下一个 Token 分布的**香农熵（Shannon Entropy）**作为不确定性的指标。如果模型对某个查询感到不确定，其熵值会较高。
*   **关键技术细节**：
    1.  **熵梯度图生成**：在推理的第一步，计算预测分布的熵，并将其反向传播至视觉嵌入（Visual Embeddings）。梯度的大小反映了该视觉区域对模型预测不确定性的影响程度。
    2.  **多区域选择与排序**：对梯度图进行高斯平滑，利用“拐点法”（Elbow Method）自动确定二值化阈值，提取连贯的连通分量（ROI），并根据梯度强度进行排序。
    3.  **迭代细化（Iterative Refinement）**：对选定的 ROI 进行裁剪并重新输入模型，进行多次“缩放-重新定位”循环。
    4.  **空间熵停止准则**：引入空间熵（Spatial Entropy）来衡量激活区域的集中程度。当空间熵不再下降时停止迭代，以防止过度缩放丢失上下文。

### 3. 实验设计
*   **数据集/场景**：涵盖了 7 个基准测试，分为两类：
    *   **细粒度理解**：TextVQA（场景文本）、V*（高分辨率视觉搜索）、DocVQA（文档问答）、InfoQA（信息图表）、POPE（物体幻觉探测）。
    *   **通用/现实世界理解**：GQA（组合推理）、RWQA（现实世界空间推理）。
*   **模型架构**：在 LLaVA-1.5、LLaVA-1.6、InternVL-3.5 和 Qwen2.5-VL 四种主流架构上进行了验证。
*   **对比方法**：
    *   **无需训练方法**：Vanilla VLM（基准）、ViCrop（基于注意力的裁剪）、ZoomEye。
    *   **需要训练方法**：TEVA、SEAL（作为性能参考上限）。

### 4. 资源与算力
*   论文在对比实验中提到，在 **A100 GPU (80GB)** 上运行 ZoomEye 等基准方法时曾遇到内存不足（OOM）问题，而本文方法相对更轻量。
*   **推理开销**：单次迭代的额外耗时约为 1-2 秒，带停止准则的完整迭代过程平均耗时约 3 秒（以 LLaVA-1.6 为例）。
*   **训练时长**：由于是 **Training-Free（无需训练）** 方法，因此训练时长为零。

### 5. 实验数量与充分性
*   **实验规模**：在 4 种架构和 7 个数据集上进行了详尽的定量评估。
*   **消融实验**：非常充分，涵盖了停止准则对比（空间熵 vs 置信度）、损失函数选择（熵 vs 最大概率）、ROI 数量的影响、梯度计算的层级分析、以及不同 Token 位置的影响。
*   **客观性**：通过在不同家族的模型上测试，证明了该方法的通用性；同时对比了训练驱动的 SOTA 方法，显示了其在无需训练前提下的竞争力。

### 6. 主要结论与发现
*   **性能提升**：在所有模型上均实现了性能增长，尤其在 V*（提升高达 15-20%）和 DocVQA 等细节敏感任务上效果显著。
*   **通用性**：该方法不破坏模型的通用问答能力（GQA/RWQA 表现平稳），同时显著增强了细粒度感知。
*   **定位质量**：定量评估显示，其定位的 IoU（交并比）远高于基于注意力的 ViCrop 方法。
*   **模型缩放效应**：越强大的基础模型（如 Qwen2.5-VL）生成的熵梯度图越精确，表明该方法能受益于模型能力的提升。

### 7. 优点
*   **无需训练**：直接利用预训练模型的内在信号，无需昂贵的微调或辅助检测器。
*   **模型无关**：适用于各种基于 Transformer 的 VLM 架构。
*   **多区域支持**：能够同时捕捉和处理图像中多个不连续的关键证据区域。
*   **可解释性**：生成的梯度图直观地展示了模型“为什么看这里”，增强了黑盒模型的透明度。

### 8. 不足与局限
*   **推理延迟**：由于涉及反向传播和多次前向推理，其推理速度比原始模型慢 2-6 倍。
*   **依赖基座模型**：如果基座模型本身对某些视觉特征完全没有表征能力，熵梯度也无法找回这些丢失的信息。
*   **推理与感知的脱节**：在某些失败案例中，即使模型精准定位并裁剪了细节区域，其语言组件仍可能做出错误的逻辑推理。

（完）
