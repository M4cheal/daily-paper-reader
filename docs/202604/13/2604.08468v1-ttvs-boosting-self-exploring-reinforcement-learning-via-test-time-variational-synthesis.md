---
title: "TTVS: Boosting Self-Exploring Reinforcement Learning via Test-time Variational Synthesis"
title_zh: TTVS：通过测试时变分合成增强自探索强化学习
authors: "Sikai Bai, Haoxi Li, Jie Zhang, Yongjiang Liu, Song Guo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08468v1"
tags: ["query:tta"]
score: 10.0
evidence: 通过无标签测试查询动态增强实现大推理模型的测试时间自适应
tldr: 本文提出TTVS框架，旨在解决大推理模型在缺乏标注数据领域难以进行强化学习的挑战。TTVS通过测试时变分合成技术，将无标注的静态测试查询动态转化为多样化的语义等价变体，并结合混合探索机制引导模型自我进化。实验表明，该方法在八种架构上均表现优异，仅利用无标注数据便超越了现有的测试时自适应方法及部分高性能的有监督强化学习技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08468v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison of TTVS with state-of-the-art methods on various model architectures. For RL Post-trained Models w / Labeled Data (·), (·) indicates the amount of labeled data used during post-training.\", \"page\": 6, \"index\": 1, \"width\": 814, \"height\": 902}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-002.webp\", \"caption\": \"Figure 4: Computational cost during test-time training on Qwen3-4B\", \"page\": 8, \"index\": 2, \"width\": 462, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-003.webp\", \"caption\": \"Figure 3: Comparative performance analysis across the five difficulty levels of MATH-500.\", \"page\": 8, \"index\": 3, \"width\": 732, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-004.webp\", \"caption\": \"Figure 1: The schematic illustration of the Test-time Variational Synthesis (TTVS). In Stage 1: Online Variational Synthesis. Pseudo labels are generated from test data to synthesize diversified samples, which are then online filtered for quality and diversity. In Stage 2: Test-time Hybrid Exploration. A policy model is optimized on this augmented data through a hybrid intra- and cross-group exploration strategy, culminating in diversified rewards that guide the model’s optimization.\", \"page\": 4, \"index\": 4, \"width\": 826, \"height\": 588}]"
motivation: 传统的强化学习依赖昂贵的标注奖励，且现有的测试时自适应方法因受限于静态查询集而容易产生过拟合。
method: 引入在线变分合成模块生成动态查询流，并配合测试时混合探索模块在合成变体间平衡准确性与一致性。
result: TTVS在八个模型架构上均取得领先，且在无标注情况下优于使用大规模高质量标注数据的有监督强化学习方法。
conclusion: TTVS通过动态增强测试流实现了高效的测试时自我进化，为提升大模型在未知领域的推理能力提供了新路径。
---

## 摘要
尽管由可验证奖励强化学习（RLVR）驱动的大型推理模型（LRMs）取得了显著进展，但在专业或新颖领域，这种监督信号极其昂贵或难以获取，这使得该范式受到根本性限制，并为测试时自适应带来了关键挑战。虽然现有的测试时方法提供了一种潜在的解决方案，但它们受限于从静态查询集中学习，存在过拟合文本模式的风险。为填补这一空白，我们引入了测试时变分合成（TTVS），这是一个新颖的框架，通过从无标签测试查询中动态增强训练流，使 LRMs 能够实现自我进化。TTVS 包含两个协同模块：（1）在线变分合成，它将静态测试查询转换为多样化且语义等价的变体动态流，迫使模型学习底层的问题逻辑而非表层模式；（2）测试时混合探索，它在合成变体之间平衡了准确性驱动的利用（exploitation）与一致性驱动的探索（exploration）。大量实验表明，TTVS 在八种模型架构上均表现出优异的性能。值得注意的是，仅使用无标签的测试时数据，TTVS 不仅超越了其他测试时自适应方法，还优于在海量高质量标注数据上训练的最先进的有监督强化学习技术。

## Abstract
Despite significant advances in Large Reasoning Models (LRMs) driven by reinforcement learning with verifiable rewards (RLVR), this paradigm is fundamentally limited in specialized or novel domains where such supervision is prohibitively expensive or unavailable, posing a key challenge for test-time adaptation. While existing test-time methods offer a potential solution, they are constrained by learning from static query sets, risking overfitting to textual patterns. To address this gap, we introduce Test-Time Variational Synthesis (TTVS), a novel framework that enables LRMs to self-evolve by dynamically augmenting the training stream from unlabeled test queries. TTVS comprises two synergistic modules: (1) Online Variational Synthesis, which transforms static test queries into a dynamic stream of diverse, semantically-equivalent variations, enforcing the model to learn underlying problem logic rather than superficial patterns; (2) Test-time Hybrid Exploration, which balances accuracy-driven exploitation with consistency-driven exploration across synthetic variants. Extensive experiments show TTVS yields superior performance across eight model architectures. Notably, using only unlabeled test-time data, TTVS not only surpasses other test-time adaptation methods but also outperforms state-of-the-art supervised RL-based techniques trained on vast, high-quality labeled data.

---

## 论文详细总结（自动生成）

### 论文总结：TTVS (Test-time Variational Synthesis)

#### 1. 核心问题与整体含义（研究动机和背景）
大型推理模型（LRMs）如 OpenAI-o1 和 DeepSeek-R1 的成功高度依赖于**可验证奖励的强化学习（RLVR）**。然而，这种范式在医疗诊断、航空航天等专业领域面临巨大挑战，因为这些领域缺乏大规模的高质量标注数据和自动验证工具。
现有的**测试时自适应（Test-time Adaptation, TTA）**方法虽然尝试在推理阶段利用无标签数据进行自我改进，但通常受限于**静态查询集**，导致模型容易过拟合特定的文本模式，而非掌握底层逻辑。本文提出的 **TTVS** 旨在通过动态合成多样化的查询变体，让模型在测试阶段实现更鲁棒的自我进化。

#### 2. 方法论：核心思想与技术细节
TTVS 框架由两个协同模块组成，旨在将静态测试任务转化为动态的、逻辑驱动的训练流：

*   **在线变分合成 (Online Variational Synthesis)：**
    *   **伪标签生成：** 对原始查询进行多次采样（Rollouts），通过多数投票（Majority Voting）确定共识答案作为伪标签。
    *   **变分增强：** 利用模型自身的能力，将原始查询改写为多个语义等价但表述不同的变体。
    *   **在线过滤：** 引入“难度脚手架”，仅保留模型准确率在特定区间（既不太难也不太易）的查询，并限制 Token 长度以确保合成质量。
*   **测试时混合探索 (Test-time Hybrid Exploration)：**
    *   **组内探索 (Intra-Group Exploration, IGE)：** 针对每个独立查询（原始或合成）进行优化，旨在提高特定问题下的准确性（Exploitation）。
    *   **跨组探索 (Cross-Group Exploration, CGE)：** 在语义等价的查询簇之间进行联合投票和优化，强制模型在不同表述下保持逻辑一致性（Consistency）。
*   **优化算法：** 默认采用 **GRPO (Group Relative Policy Optimization)**，通过组内相对奖励计算优势函数，无需显式的 Critic 模型，计算效率高。

#### 3. 实验设计
*   **数据集：** 涵盖了数学推理和科学问答的核心基准，包括 **MATH-500**、**AIME-2024**、**AMC-2023** 和 **GPQA**。此外，还在 **DeepMath-103K**（防污染测试）、**MedMCQA**（医疗领域）和 **OpenBookQA**（常识推理）上进行了扩展验证。
*   **对比基准 (Benchmarks)：**
    *   **有监督 RL 模型：** 如 DeepSeek-R1-Distill (1.5B/7B)、SimpleRL-Zero-7B、OpenReasoner-Zero-7B（这些模型使用了数千至数十万条标注数据）。
    *   **测试时 RL 方法：** **TTRL**（仅在原始无标签数据上进行 RL）。
*   **模型架构：** 验证了 8 种不同规模的模型，包括 Qwen3 (1.7B/4B/8B)、Qwen2.5 (1.5B/3B/7B) 和 LLaMA (3.2-3B/3.1-8B)。

#### 4. 资源与算力
*   **硬件设备：** 实验使用了 **8 台 NVIDIA GeForce H800 80GB GPU**。
*   **训练细节：** 
    *   优化器：AdamW，学习率峰值 $5 \times 10^{-7}$。
    *   采样：每步采样 32 个响应（Temperature=0.6）。
    *   训练时长：根据数据集大小设置了 10 到 40 个 Episode 不等。
*   **成本分析：** 文中特别指出，TTVS 的显存占用与 TTRL 相当，且由于生成的答案更简洁，推理效率甚至略有提升。

#### 5. 实验数量与充分性
论文进行了极其详尽的实验验证：
*   **跨模型验证：** 在 3 个模型家族、8 种参数规模上展现了普适性。
*   **消融实验：** 验证了 IGE 和 CGE 模块的各自贡献、不同 RL 算法（GRPO, OPO, DAPO）的兼容性、过滤阈值的影响、以及采样数量（Rollouts）的敏感性。
*   **难度分析：** 针对 MATH-500 的不同难度等级（L1-L5）进行了细分对比。
*   **鲁棒性测试：** 针对 Base 模型（无指令遵循能力）提出了预热策略，并验证了其在非数学任务（常识推理）上的潜力。
*   **评价：** 实验设计非常全面、客观，通过多维度对比证明了方法在无标签场景下的优越性。

#### 6. 主要结论与发现
*   **性能飞跃：** TTVS 在所有基准上均显著优于 TTRL。例如在 Qwen3-4B 上，平均分提升了约 29.8%。
*   **超越有监督：** 令人惊讶的是，仅使用无标签测试数据的 TTVS，其表现超过了在数十万条标注数据上训练的 SOTA 有监督 RL 模型（如 DeepSeek-R1-Distill-7B）。
*   **逻辑一致性：** 变分合成迫使模型关注问题的逻辑本质而非文本表面，这在处理高难度问题（MATH L5）时优势尤为明显。

#### 7. 优点
*   **数据高效：** 彻底摆脱了对外部专家模型或人工标注的依赖，实现了真正的“自进化”。
*   **泛化性强：** 变分合成机制有效缓解了测试时的过拟合问题。
*   **即插即用：** 框架不依赖特定的 RL 算法，可灵活集成到现有的 RL 流程中。
*   **推理友好：** 训练后的模型在推理时不需要额外的计算开销（如多次采样或改写）。

#### 8. 不足与局限
*   **模型规模限制：** 受限于算力，实验未在超大规模模型（如 32B 或 70B 以上）上进行验证。
*   **模态局限：** 目前仅限于纯文本任务，尚未探索在多模态（视觉-语言）推理中的应用。
*   **初始能力依赖：** 对于完全不具备改写能力或推理基础极差的 Base 模型，仍需要少量的合成数据进行初步预热。
*   **非客观任务挑战：** 在缺乏明确客观标准（如创意写作）的领域，伪标签和过滤机制的有效性尚待验证。

（完）
