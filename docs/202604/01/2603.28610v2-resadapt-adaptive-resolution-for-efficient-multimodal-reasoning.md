---
title: "ResAdapt: Adaptive Resolution for Efficient Multimodal Reasoning"
authors: "Huanxuan Liao, Zhongtao Jiang, Yupu Hao, Yuqiao Tan, Shizhu He, Ben Wang, Jun Zhao, Kun Xu, Kang Liu"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28610v2"
tags: ["query:tta"]
score: 8.0
evidence: 多模态大语言模型的输入端自适应框架
tldr: ResAdapt利用上下文老虎机算法为高效多模态大模型推理自适应分配视觉分辨率。
selection_source: fresh_fetch
motivation: 多模态大语言模型的输入端自适应框架。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Multimodal Large Language Models (MLLMs) achieve stronger visual understanding by scaling input fidelity, yet the resulting visual token growth makes jointly sustaining high spatial resolution and long temporal context prohibitive. We argue that the bottleneck lies not in how post-encoding representations are compressed but in the volume of pixels the encoder receives, and address it with ResAdapt, an Input-side adaptation framework that learns how much visual budget each frame should receive before encoding. ResAdapt couples a lightweight Allocator with an unchanged MLLM backbone, so the backbone retains its native visual-token interface while receiving an operator-transformed input. We formulate allocation as a contextual bandit and train the Allocator with Cost-Aware Policy Optimization (CAPO), which converts sparse rollout feedback into a stable accuracy-cost learning signal. Across budget-controlled video QA, temporal grounding, and image reasoning tasks, ResAdapt improves low-budget operating points and often lies on or near the efficiency-accuracy frontier, with the clearest gains on reasoning-intensive benchmarks under aggressive compression. Notably, ResAdapt supports up to 16x more frames at the same visual budget while delivering over 15% performance gain. Code is available at https://github.com/Xnhyacinth/ResAdapt.

---

## 论文详细总结（自动生成）

以下是对论文《ResAdapt: Adaptive Resolution for Efficient Multimodal Reasoning》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：多模态大语言模型（MLLM）在处理视频推理时面临“空间分辨率”与“时间覆盖范围”的权衡。现有的效率优化方法（如Token剪枝或合并）通常发生在编码器之后，此时编码器已经支付了全额计算成本；而基于智能体的迭代检索方法则带来了极高的推理延迟。
*   **核心问题**：如何在编码之前（输入端）动态分配每一帧的视觉预算，从而在不损失关键细节的前提下，大幅降低计算开销并扩展处理帧数。

### 2. 论文提出的方法论
*   **核心思想**：提出 **ResAdapt** 框架，在视觉编码前引入一个轻量级的**分配器（Allocator）**，根据查询（Query）内容动态预测每帧的分辨率缩放比例。
*   **关键技术细节**：
    *   **分配器架构**：基于轻量级视觉编码器和浅层 Transformer，将原始视频特征与查询文本融合，输出每帧的缩放因子。
    *   **视觉算子（Operator）**：采用双线性插值（Bilinear Resizing）作为算子，根据分配器的预测调整输入像素，保持与标准推理引擎（如 vLLM, SGLang）的兼容性。
    *   **CAPO（成本感知策略优化）**：针对分配过程的不可导性，将其建模为上下文老虎机（Contextual Bandit）问题。引入**动态成本基准（Dynamic Pivot）**和**非对称奖励塑造（Asymmetric Reward Shaping）**，防止模型为了节省成本而导致性能崩溃。
    *   **时间相似性正则化（$L_{sim}$）**：惩罚相邻相似帧同时分配高预算的行为，强制模型在冗余帧之间进行差异化选择。

### 3. 实验设计
*   **数据集/场景**：
    *   **视频问答（Video QA）**：VideoMME, LongVideoBench, MMVU, MLVU, VideoMMMU, LVBench。
    *   **时间定位（Temporal Grounding）**：Charades-STA, ActivityNet, NExT-GQA。
    *   **图像推理**：MathVista, MMMU, OCRBench 等（用于测试迁移能力）。
*   **对比方法**：
    *   **启发式方法**：随机丢帧（Random Drop）、固定缩放（FixedScale）。
    *   **模型端压缩**：ToMe, VisionZip, FlashVid。
    *   **推理增强**：VideoAuto-R1（基于思维链推理的方法）。
*   **基座模型**：Qwen2.5-VL-7B 和 Qwen3-VL-8B。

### 4. 资源与算力
*   **算力配置**：使用了 **32 台 NVIDIA H100 GPU** 进行训练。
*   **软件框架**：采用了 VeRL 强化学习框架、DeepSpeed 优化器以及 vLLM 推理引擎。
*   **训练细节**：在约 9.34 万个样本上进行了一个 epoch 的训练，分配器学习率为 $2 \times 10^{-5}$，基座模型学习率为 $1 \times 10^{-6}$。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了 6 个视频 QA 评测集和 3 个定位评测集，并进行了跨模型（Qwen2.5 到 Qwen3）的零样本迁移实验。
*   **消融实验**：针对奖励设计（CAPO vs 直接成本惩罚）、算子泛化性（Resize 迁移到 Frame Selection）、时间正则化项的作用以及不同分布函数（Beta vs Normal）进行了详尽的对比。
*   **评价**：实验设计非常充分且客观，通过帕累托前沿（Pareto Frontier）曲线直观展示了在相同 Token 预算下的性能优势。

### 6. 主要结论与发现
*   **极致压缩**：ResAdapt 在消除超过 90% 视觉 Token 的情况下，依然能匹配或超过全分辨率基准模型的性能。
*   **时间扩展**：在同等计算预算下，ResAdapt 可处理 **16 倍**更多的帧数，在长视频推理任务上获得超过 15% 的相对增益。
*   **主动感知**：模型学到了“主动感知”能力，能自动将像素预算集中在包含关键信息（如文字覆盖、场景切换、关键动作）的帧上。
*   **硬件友好**：由于在输入端调整，无需修改模型内部算子，可直接享受 FlashAttention 等硬件加速红利。

### 7. 优点（亮点）
*   **干预点前移**：打破了“先编码再压缩”的常规思路，从源头解决视觉冗余问题。
*   **端到端学习**：分配策略完全由任务奖励驱动，无需人工标注显著性区域。
*   **CAPO 算法**：有效解决了强化学习中精度与成本权衡时的策略坍缩问题。
*   **零样本迁移**：训练好的分配器可以直接应用于不同的 MLLM 基座，具有很强的通用性。

### 8. 不足与局限
*   **固定开销**：分配器本身存在一定的计算开销，在处理极短视频（如少于 32 帧）时，这种前置开销可能会抵消部分后端节省的成本。
*   **开环决策**：分配是一次性的，模型在推理过程中如果发现分辨率不足，无法动态回溯并重新请求高分辨率输入。
*   **图像任务表现平平**：虽然在视频任务上表现优异，但在纯图像任务（如 OCR）上，由于缺乏时间冗余，其性能提升并不明显。

（完）
