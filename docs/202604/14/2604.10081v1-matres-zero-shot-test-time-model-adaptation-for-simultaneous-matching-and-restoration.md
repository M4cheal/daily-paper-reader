---
title: "MatRes: Zero-Shot Test-Time Model Adaptation for Simultaneous Matching and Restoration"
title_zh: MatRes：用于同时匹配与修复的零样本测试时模型自适应
authors: "Kanggeon Lee, Soochahn Lee, Kyoung Mu Lee"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10081v1"
tags: ["query:tta"]
score: 10.0
evidence: 零样本测试时间自适应框架，共同提高恢复质量和对应估计
tldr: 针对现实中图像退化与视角变化并存导致匹配与修复相互干扰的问题，本文提出MatRes框架。这是一种零样本测试时自适应方法，仅需一对低/高质量图像，通过在对应位置强制执行条件相似性，在无需离线训练的情况下同时优化修复质量与几何对齐，显著提升了协同处理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10081v1/fig-001.webp\", \"caption\": \"Table 2: Restoration score comparison across different tasks.\", \"page\": 7, \"index\": 1, \"width\": 519, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2604.10081v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative Restoration Results. Restoration performance is compared with and without the matching networkMθ across three tasks. For super-resolution (SR), we evaluate EDSR [33], ESRGAN [45], SwinIR [32], and HAT [46]; for denoising, we evaluate DnCNN [47], N2V [35], N2N [34], and Restormer [31]; and for deblurring, we evaluate DeblurGAN [36], SRN [37], Restormer [31], and NAFNet [48]. The green box indicates the zoomed-in region.\", \"page\": 7, \"index\": 2, \"width\": 1040, \"height\": 783}, {\"url\": \"assets/figures/arxiv/2604.10081v1/fig-003.webp\", \"caption\": \"Figure 2: Method Overview. Given an input pair (ILQ, IHQ), the pretrained generative prior model Mθ and the pretrained restoration network Rϕ remain frozen during adaptation (gradient flows through both models but their parameters are not updated). The zero-initialized adapter Hψ is the only trainable module, refined iteratively using losses computed on the IEQ and IHQ image pairs. At each adaptation step,Mθ extracts generative priors and estimates the transformation TLQ→HQ, the ILQ image are warped and injected into Rϕ, and the adapter Hψ learns to align and modulate features for improved restoration.\", \"page\": 3, \"index\": 3, \"width\": 517, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2604.10081v1/fig-004.webp\", \"caption\": \"Table 1: Experimental Results for Super-Resolution, Denoising, and Deblurring Tasks\", \"page\": 6, \"index\": 4, \"width\": 1054, \"height\": 1227}, {\"url\": \"assets/figures/arxiv/2604.10081v1/fig-005.webp\", \"caption\": \"Figure 4: Qualitative Matching Results. Geometric transform estimation results for five matching networks (LoftUp [26], ART [27], DIFT [28], RoMa [29], Mast3R [30]) across SR, denoising, and deblurring tasks, is evaluated with and without the restoration network Rϕ. The green box denotes the ground-truth transform, while the red box represents the estimated transform.\", \"page\": 8, \"index\": 5, \"width\": 509, \"height\": 496}, {\"url\": \"assets/figures/arxiv/2604.10081v1/fig-006.webp\", \"caption\": \"Table 3: Matching score comparison across different tasks.\", \"page\": 8, \"index\": 6, \"width\": 519, \"height\": 288}]"
motivation: 现实图像对常同时存在严重退化和大幅视角变化，导致独立的图像修复与几何匹配任务相互干扰。
method: 提出一种零样本测试时自适应框架，通过冻结预训练模型并仅更新轻量级模块，在对应位置强制执行条件相似性。
result: 实验表明，MatRes在修复质量和几何对齐精度上均显著优于单独使用修复或匹配模型。
conclusion: MatRes为处理多视角、变质量图像对提供了一种实用且广泛适用的解决方案，有效解决了匹配与修复间的干扰问题。
---

## 摘要
现实世界的图像对通常同时存在严重的退化和剧烈的视角变化，当独立处理时，图像修复与几何匹配往往会成为相互干扰的任务。在本文中，我们提出了 MatRes，这是一个零样本测试时自适应框架，仅需一对低质量和高质量图像，即可同时提升修复质量和对应关系估计。通过在对应位置施加条件相似性约束，MatRes 仅更新轻量级模块，并保持所有预训练组件冻结，无需离线训练或额外监督。在多种组合下的广泛实验表明，与单独使用修复或匹配模型相比，MatRes 在修复和几何对齐方面均取得了显著提升。MatRes 为现实场景提供了一种实用且普适的解决方案，能够有效应对用户拍摄同一场景时视角和质量各异的情况，解决了匹配与修复之间常被忽视的相互干扰问题。

## Abstract
Real-world image pairs often exhibit both severe degradations and large viewpoint changes, making image restoration and geometric matching mutually interfering tasks when treated independently. In this work, we propose MatRes, a zero-shot test-time adaptation framework that jointly improves restoration quality and correspondence estimation using only a single low-quality and high-quality image pair. By enforcing conditional similarity at corresponding locations, MatRes updates only lightweight modules while keeping all pretrained components frozen, requiring no offline training or additional supervision. Extensive experiments across diverse combinations show that MatRes yields significant gains in both restoration and geometric alignment compared to using either restoration or matching models alone. MatRes offers a practical and widely applicable solution for real-world scenarios where users commonly capture multiple images of a scene with varying viewpoints and quality, effectively addressing the often-overlooked mutual interference between matching and restoration.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MatRes** 的框架，旨在解决现实世界图像处理中一个被长期忽视的挑战：当图像对同时存在**严重退化**（如噪声、模糊、低分辨率）和**大幅度视角变化**时，如何同时实现高质量的图像修复与精确的几何匹配。

以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在实际拍摄中，用户常会从不同角度拍摄同一场景，其中某些图像可能因环境因素退化。传统的图像修复（Restoration）通常针对单图或对齐良好的图像对，而几何匹配（Matching）在图像严重退化时往往失效。
*   **研究动机**：修复与匹配在独立处理时会相互干扰——不准确的对齐导致参考修复失败，而严重的图像退化导致匹配点难以提取。论文旨在利用“高质量参考图”这一现实假设，通过两者相互引导，在无需离线训练的情况下实现协同优化。

### 2. 方法论（Methodology）
MatRes 的核心是一个**零样本测试时自适应（Zero-Shot Test-Time Adaptation）**框架。
*   **核心思想**：冻结预训练的匹配网络 $M_\theta$ 和修复网络 $R_\phi$，仅通过一个轻量级的、零初始化的 **LoRA 适配器 $H_\psi$** 在推理阶段针对特定图像对进行迭代优化。
*   **关键流程**：
    1.  **生成先验提取**：从预训练匹配模型（如扩散模型、视觉 Transformer）中提取具有语义信息的特征作为生成先验。
    2.  **特征对齐与注入**：利用匹配网络估计的变换矩阵将低质量（LQ）图像及其特征变换到高质量（HQ）图像视角，并通过适配器 $H_\psi$ 将这些先验注入修复网络的编码器中。
    3.  **互引导优化**：
        *   **修复引导匹配**：修复后的图像特征更清晰，有助于提高匹配精度。
        *   **匹配引导修复**：精确的对齐允许修复网络从 HQ 参考图中获取更多细节。
*   **损失函数**：
    *   **离散对角损失（Off-diagonal Loss）**：惩罚特征成本体积（Cost Volume）中的非对角响应，促使特征匹配更尖锐、唯一。
    *   **像素匹配损失（Pixel-matching Loss）**：计算增强图像与 HQ 参考图在重叠区域的均方误差（MSE）。

### 3. 实验设计
*   **任务覆盖**：超分辨率（SR）、去噪（Denoise）、去模糊（Deblur）。
*   **数据集**：
    *   **SR**: RealSR, DIV2K, Set5, Set14, Urban100, BSD100。
    *   **去噪**: Set5, Set14, Urban100。
    *   **去模糊**: GoPro, HIDE, RealBlur-J/R, CelebA。
*   **对比基准（Baselines）**：
    *   **匹配模型**：RoMa, ART, DIFT, LoftUp, Mast3R 等。
    *   **修复模型**：EDSR, ESRGAN, SwinIR, HAT, Restormer, NAFNet 等。
    *   **SOTA 对比**：与专门的修复方法（如 AutoLUT, MAXIM）和匹配方法（如 MINIMA, MatchAnything）进行对比。

### 4. 资源与算力
*   **硬件使用**：论文明确提到使用了 **NVIDIA A100 GPU** 进行测试时优化。
*   **优化细节**：使用 AdamW 优化器，学习率为 0.001，每 10 次迭代减半。
*   **时长**：虽然未给出每个图像对的具体秒数，但提到优化会持续到扩散损失（LD）不再下降为止（$T_{adapt}$ 步）。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。论文进行了跨任务、跨模型的大规模组合实验（5 种匹配器 $\times$ 4 种修复器），涵盖了从经典的 CNN 到最新的 Transformer 和扩散模型。
*   **客观性**：通过 PSNR、SSIM 衡量修复质量，通过 mAUC 衡量匹配精度。实验不仅对比了“有无互引导”的消融结果，还与各领域的最新 SOTA 进行了横向对比，证明了其通用性。

### 6. 主要结论与发现
*   **互惠效应显著**：引入匹配引导后，修复性能（PSNR/SSIM）大幅提升；反之，修复后的图像也显著提高了匹配模型的 mAUC。
*   **零样本泛化强**：该方法无需任何离线训练数据，直接在测试阶段对预训练模型进行微调，能够灵活适应未见过的退化类型和视角变化。
*   **超越专用模型**：在存在视角差异的复杂场景下，MatRes 的表现优于许多专门为单项任务设计的 SOTA 模型。

### 7. 优点与亮点
*   **实用性极高**：利用“一张好图、一张坏图”的常见摄影场景，解决了实际应用中的痛点。
*   **即插即用**：兼容几乎所有现有的基于网格特征的匹配和修复架构，无需修改原始模型权重。
*   **无需标注**：完全依赖自监督的特征一致性和像素一致性进行优化，摆脱了对 GT 变换场和清洁图像的依赖。

### 8. 不足与局限
*   **推理延迟**：由于需要在测试时进行迭代优化（Test-time Optimization），其处理速度必然慢于直接的前向推理，可能不适用于实时视频处理。
*   **依赖初始匹配**：如果初始视角差异极端巨大或图像完全全黑/全白，导致初始匹配完全失效，优化过程可能会陷入局部最优。
*   **算力要求**：虽然适配器轻量，但优化过程涉及通过大型预训练模型（如扩散模型）进行梯度回传，对显存和计算能力有一定要求。

（完）
