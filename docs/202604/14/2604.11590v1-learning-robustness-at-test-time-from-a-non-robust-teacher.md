---
title: Learning Robustness at Test-Time from a Non-Robust Teacher
title_zh: 在测试时从非鲁棒教师模型中学习鲁棒性
authors: "Stefano Bianchettin, Giulio Rossolini, Giorgio Buttazzo"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11590v1"
tags: ["query:tta"]
score: 10.0
evidence: 无监督测试时间自适应中的对抗鲁棒性
tldr: 本文探讨了在无标签目标数据稀缺的测试阶段，如何提升非鲁棒预训练模型的对抗鲁棒性。研究发现传统的蒸馏方法在教师模型非鲁棒时表现不稳定，为此提出一种无标签框架，利用教师预测作为语义锚点来指导干净和对抗目标的优化。该方法在CIFAR-10和ImageNet上显著提升了优化稳定性，并在鲁棒性与准确率之间取得了更好的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11590v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed Teacher-guided Robust Adaptation (TgRA) framework, detailed in Section 5: the student model\", \"page\": 2, \"index\": 1, \"width\": 979, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2604.11590v1/fig-002.webp\", \"caption\": \"Table 2\", \"page\": 7, \"index\": 2, \"width\": 1029, \"height\": 605}, {\"url\": \"assets/figures/arxiv/2604.11590v1/fig-003.webp\", \"caption\": \"Figure 2: Clean and robust test accuracy under PGD-20 attacks during test-time finetuning on CIFAR-10 (𝜀 = 8∕255) and\", \"page\": 7, \"index\": 3, \"width\": 970, \"height\": 490}, {\"url\": \"assets/figures/arxiv/2604.11590v1/fig-004.webp\", \"caption\": \"Figure 4: Examples of the target-domain corruptions for\", \"page\": 12, \"index\": 4, \"width\": 450, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2604.11590v1/fig-005.webp\", \"caption\": \"Figure 3: Clean and robust accuracy (Top-1) on ImageNet\", \"page\": 12, \"index\": 5, \"width\": 412, \"height\": 400}]"
motivation: 旨在解决预训练模型在测试时自适应过程中，如何从非鲁棒的教师模型中有效获取对抗鲁棒性的难题。
method: 提出一种以非鲁棒教师预测为语义锚点的无标签框架，通过理论分析改进了传统的自一致性正则化。
result: 在多个数据集上的实验证明，该方法在提升鲁棒性的同时，具有更高的优化稳定性和更低的超参数敏感度。
conclusion: 证明了通过语义锚定策略，可以在测试阶段从非鲁棒模型中成功诱导出更强的对抗防御能力。
---

## 摘要
如今，预训练模型越来越多地被用作通用骨干网络，并在测试时被调整以适应目标数据稀缺且无标签的下游环境。虽然这种范式已被证明能有效提高目标域的干净准确率（clean accuracy），但对抗鲁棒性（adversarial robustness）受到的关注却少得多，尤其是当原始预训练模型并非专门为鲁棒性而设计时。这提出了一个实际问题：预训练的非鲁棒模型能否在测试时进行调整，以提高其在目标分布上的对抗鲁棒性？为了回答这个问题，本研究探讨了在仅有少量无标签目标样本的无监督测试时设置下，对抗训练策略与自适应方案结合时的表现。研究首先分析了经典的对抗训练公式如何扩展到这一场景，结果表明，直接基于蒸馏的自适应方法仍然不稳定，且对超参数调节高度敏感，特别是当教师模型本身不具备鲁棒性时。为了解决这些局限性，本研究提出了一个无标签框架，该框架在自适应过程中利用非鲁棒教师模型的预测作为干净目标和对抗目标的语义锚点（semantic anchor）。我们进一步提供了理论见解，证明我们的公式比经典对抗训练中常用的基于自一致性（self-consistency）的正则化提供了更稳定的替代方案。实验在 CIFAR-10 和 ImageNet 上针对诱导的光度变换评估了所提方法。结果支持了理论见解，表明在部署后的测试时设置下，与现有基准方法相比，所提方法实现了更好的优化稳定性、更低的参数选择敏感性以及更优的鲁棒性与准确率权衡。

## Abstract
Nowadays, pretrained models are increasingly used as general-purpose backbones and adapted at test-time to downstream environments where target data are scarce and unlabeled. While this paradigm has proven effective for improving clean accuracy on the target domain, adversarial robustness has received far less attention, especially when the original pretrained model is not explicitly designed to be robust. This raises a practical question: \emph{can a pretrained, non-robust model be adapted at test-time to improve adversarial robustness on a target distribution?} To face this question, this work studies how adversarial training strategies behave when integrated into adaptation schemes for the unsupervised test-time setting, where only a small set of unlabeled target samples is available. It first analyzes how classical adversarial training formulations can be extended to this scenario, showing that straightforward distillation-based adaptations remain unstable and highly sensitive to hyperparameter tuning, particularly when the teacher itself is non-robust.   To address these limitations, the work proposes a label-free framework that uses the predictions of a non-robust teacher model as a semantic anchor for both the clean and adversarial objectives during adaptation. We further provide theoretical insights showing that our formulation yields a more stable alternative to the self-consistency-based regularization commonly used in classical adversarial training. Experiments evaluate the proposed approach on CIFAR-10 and ImageNet under induced photometric transformations. The results support the theoretical insights by showing that the proposed approach achieves improved optimization stability, lower sensitivity to parameter choices, and a better robustness-accuracy trade-off than existing baselines in this post-deployment test-time setting.

---

## 论文详细总结（自动生成）

这篇论文《Learning Robustness at Test-Time from a Non-Robust Teacher》探讨了在模型部署后的测试阶段，如何利用无标签数据提升非鲁棒预训练模型的对抗鲁棒性。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在测试时间自适应（Test-Time Adaptation, TTA）场景下，当只有少量无标签的目标域数据，且原始预训练模型和教师模型都**不具备鲁棒性**时，能否提升模型的对抗鲁棒性？
*   **研究背景**：现有的对抗训练（AT）通常依赖大量标注数据，而现有的鲁棒蒸馏方法通常假设存在一个预先训练好的“鲁棒教师”。但在实际部署中，这些条件往往无法满足。此外，分布偏移（Distribution Shift）会显著降低非鲁棒模型的安全性。

### 2. 方法论：TgRA 框架
*   **核心思想**：提出**教师引导的鲁棒自适应（Teacher-guided Robust Adaptation, TgRA）**。该方法不再依赖学生模型的自一致性（Self-consistency），而是将非鲁棒教师模型的预测作为“语义锚点”，同时指导干净样本的准确率优化和对抗样本的鲁棒性优化。
*   **关键技术细节**：
    *   **目标函数**：优化目标包含两部分：一是干净样本的蒸馏损失 $KL(g(x) || f(x))$，二是对抗样本的蒸馏损失 $\beta \cdot \max KL(g(x) || f(\hat{x}))$。其中 $g$ 是教师，$f$ 是学生。
    *   **语义锚定**：与 TRADES 等方法不同，TgRA 在生成对抗样本和计算损失时，参考对象是教师的预测输出，而非学生自身的预测。
    *   **理论支持**：论文通过梯度分解证明，传统的自一致性正则化存在“移动目标”效应（学生模型更新会导致参考分布变化），而 TgRA 通过锚定教师输出，消除了参考端的梯度波动，使优化过程更稳定。

### 3. 实验设计
*   **数据集与场景**：
    *   **CIFAR-10**：引入高斯噪声、模糊和色彩抖动等光度变换，设置不同严重程度（Severity 0, 1, 2）。
    *   **ImageNet-Val**：同样引入分布偏移，评估大规模数据集上的表现。
*   **模型架构**：WideResNet-34 (CIFAR-10), ResNet-50 和 ViT-B/16 (ImageNet)。
*   **对比方法（Baselines）**：
    *   **无监督基准**：TRADES-U（TRADES 的无监督变体）。
    *   **有监督基准**：PGD、TRADES、MART、DKL（用于评估无监督方法与有监督方法之间的差距）。
*   **评估指标**：干净准确率（Clean Acc）、对抗鲁棒性（PGD-20, PGN, Square Attack）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提到在 CIFAR-10 上训练 30 个 epoch，使用 SGD 优化器，初始学习率为 $10^{-3}$。在 ImageNet 上同样训练 30 个 epoch。代码已在 GitHub 开源。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种主流数据集、三种不同的网络架构（包括 CNN 和 Transformer）、多种攻击算法以及不同的数据稀缺程度（如 50/50 拆分实验）。
*   **充分性**：实验设计较为全面，不仅对比了同类无监督方法，还跨界对比了有监督方法。通过对超参数 $\beta$ 的敏感性分析和训练动力学（Training Dynamics）的可视化，验证了方法的稳定性。实验结果具有统计意义，对比公平。

### 6. 主要结论与发现
*   **稳定性提升**：TgRA 显著解决了无监督对抗训练中的优化不稳定问题，避免了训练早期的准确率崩溃。
*   **参数鲁棒性**：TgRA 对权衡参数 $\beta$ 的敏感度远低于 TRADES-U，在较大范围的 $\beta$ 取值下均能保持稳定。
*   **性能优越**：在分布偏移较大的情况下，TgRA 的鲁棒性-准确率权衡优于现有基准，甚至在数据稀缺时能超越某些有监督的微调方法。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还通过数学推导解释了为什么“锚定教师”比“自一致性”更稳定。
*   **实用性强**：不需要标签，不需要原始源域数据，也不需要鲁棒教师，非常符合实际部署环境。
*   **架构通用性**：在 CNN 和 Transformer 上均表现良好，证明了方法的普适性。

### 8. 不足与局限
*   **偏移类型有限**：实验主要集中在光度变换（噪声、模糊等），未探讨更复杂的语义偏移或几何偏移。
*   **教师依赖性**：虽然教师不需要鲁棒，但仍需要教师在目标域上具有一定的干净准确率（文中通过 BN 自适应提升教师性能），如果教师在目标域完全失效，该方法可能受限。
*   **实时性挑战**：虽然是测试时自适应，但每个 batch 需要进行多步对抗攻击（PGD），计算开销较大，可能难以满足极高实时性的场景。

（完）
