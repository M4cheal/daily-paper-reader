---
title: "Retrieve-then-Adapt: Retrieval-Augmented Test-Time Adaptation for Sequential Recommendation"
title_zh: 先检索后自适应：用于序列推荐的检索增强测试时自适应
authors: "Xing Tang, Jingyang Bin, Ziqiang Cui, Xiaokun Zhang, Fuyuan Lyu, Jingyan Jiang, Dugang Liu, Chen Ma, Xiuqiang He"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05379v1"
tags: ["query:tta"]
score: 9.0
evidence: 针对序列推荐的检索增强测试时间自适应
tldr: 针对序列推荐模型在推理阶段难以适应用户实时偏好变化的问题，本文提出了一种名为 ReAd 的检索增强测试时自适应框架。该框架通过从协作记忆库中检索相似项，利用轻量级模块生成包含协作信号的增强嵌入，并以此修正初始预测。实验证明，ReAd 在五个基准数据集上均优于现有方法，实现了高效且有效的模型自适应。
selection_source: fresh_fetch
motivation: 现有的序列推荐模型在推理时难以应对分布偏移，且现有的自适应方法存在计算开销大或依赖随机增强等局限。
method: 提出 ReAd 框架，通过检索协作相似项并利用轻量级学习模块将其转化为增强嵌入，进而优化初始预测结果。
result: 在五个公开基准数据集上的广泛实验表明，ReAd 显著提升了推荐性能，且优于现有的测试时训练和增强方法。
conclusion: ReAd 证明了结合检索机制与轻量级自适应模块是解决序列推荐中实时偏好偏移问题的有效途径。
---

## 摘要
序列推荐（SR）任务旨在根据用户的历史交互序列预测下一个项目。SR 模型通常在历史数据上进行训练，由于分布差异和参数化约束带来的挑战，在推理过程中往往难以适应实时的偏好偏移。解决这一问题的现有方法包括测试时训练、测试时增强和检索增强微调。然而，这些方法要么引入了显著的计算开销，要么依赖于随机增强策略，或者需要精心设计的两阶段训练范式。在本文中，我们认为有效的测试时自适应的关键在于同时实现有效的增强和高效的自适应。为此，我们提出了“先检索后自适应”（ReAd），这是一个通过检索到的用户偏好信号，将已部署的 SR 模型动态调整到测试分布的新颖框架。具体而言，给定一个训练好的 SR 模型，ReAd 首先从构建的协同记忆数据库中为测试用户检索协同相似的项目。随后，一个轻量级的检索学习模块将这些项目整合到一个信息丰富的增强嵌入中，该嵌入捕捉了协同信号和预测细化线索。最后，通过融合机制结合该嵌入来细化初始的 SR 预测。在五个基准数据集上的广泛实验表明，ReAd 一贯优于现有的 SR 方法。

## Abstract
The sequential recommendation (SR) task aims to predict the next item based on users' historical interaction sequences. Typically trained on historical data, SR models often struggle to adapt to real-time preference shifts during inference due to challenges posed by distributional divergence and parameterized constraints. Existing approaches to address this issue include test-time training, test-time augmentation, and retrieval-augmented fine-tuning. However, these methods either introduce significant computational overhead, rely on random augmentation strategies, or require a carefully designed two-stage training paradigm. In this paper, we argue that the key to effective test-time adaptation lies in achieving both effective augmentation and efficient adaptation. To this end, we propose Retrieve-then-Adapt (ReAd), a novel framework that dynamically adapts a deployed SR model to the test distribution through retrieved user preference signals. Specifically, given a trained SR model, ReAd first retrieves collaboratively similar items for a test user from a constructed collaborative memory database. A lightweight retrieval learning module then integrates these items into an informative augmentation embedding that captures both collaborative signals and prediction-refinement cues. Finally, the initial SR prediction is refined via a fusion mechanism that incorporates this embedding. Extensive experiments across five benchmark datasets demonstrate that ReAd consistently outperforms existing SR methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ReAd (Retrieve-then-Adapt)** 的框架，旨在解决序列推荐（SR）模型在推理阶段难以应对用户偏好偏移（Preference Shift）的问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的序列推荐模型在训练完成后参数即被冻结，但在实际应用中，用户的兴趣会随时间、环境或流行趋势发生动态偏移（即测试时分布偏移）。
*   **研究动机**：
    *   **静态参数限制**：固定参数无法捕捉推理时的实时偏好变化。
    *   **现有方法缺陷**：测试时训练（TTT）计算开销过大；测试时增强（TTA）依赖随机算子，缺乏鲁棒性；检索增强微调需要复杂的两阶段预训练。
*   **整体含义**：ReAd 旨在通过一种**轻量级、模型无关**的检索增强机制，在不重新训练主模型的前提下，利用历史协作信号动态修正推理结果。

### 2. 方法论：核心思想与技术细节
ReAd 框架分为**线下准备**和**线上自适应**两个阶段：
*   **线下：构建协作记忆库 (Collaborative Memory)**
    *   使用训练好的编码器将训练集序列转化为表示向量，并与其对应的下一个项目（Next Item）嵌入组成键值对 `<h, e>` 存入数据库。
*   **线上：检索与自适应 (Retrieve-then-Adapt)**
    1.  **检索 (Retrieve)**：对于测试序列，计算其表示向量，从记忆库中检索 Top-K 个最相似的协作项目。
    2.  **检索学习模块 (Retrieval Learning Module)**：采用**交叉注意力机制 (Cross-Attention)**，以当前序列表示为 Query，检索到的项目嵌入为 Key 和 Value，融合生成“增强嵌入” ($e_{aug}$)。
    3.  **对齐损失 (Alignment Loss)**：引入 KL 散度，将注意力权重与项目实际的预测效用进行对齐，确保模型关注真正有帮助的项目而非仅仅是相似的项目。
    4.  **动态融合 (Dynamic Fusion)**：基于**信息熵 (Entropy)** 评估初始预测和增强预测的不确定性，动态计算融合权重 $\alpha$，生成最终排名。

### 3. 实验设计
*   **数据集**：使用了 5 个公开基准数据集，包括 Amazon 的四个子集（Office, Beauty, Sports, Home）以及电影数据集 ML-1M。
*   **Benchmark 与对比方法**：
    *   **经典/架构模型**：GRU4Rec, SASRec, BERT4Rec。
    *   **对比学习模型**：CL4SRec, DuoRec, CoSeRec, ICLRec, MCLRec, ICSRec。
    *   **测试时自适应模型**：RaSeRec (检索增强), TTA (数据增强)。
*   **评估指标**：Hit Ratio (HR@K) 和 NDCG@K (K=5, 10, 20)。

### 4. 资源与算力
*   **硬件环境**：论文明确提到实验是在 **NVIDIA GeForce RTX 4090D** GPU 上进行的。
*   **算力开销**：
    *   **训练**：检索学习模块非常轻量（仅三个投影矩阵），训练开销极小。
    *   **推理**：虽然引入了检索步骤，但通过 FAISS 库加速和并行化处理，推理延迟保持在可接受范围内（论文给出了详细的推理时间对比图）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同领域、不同稀疏度的数据集上进行了测试，对比了 12 种基线模型。
*   **充分性**：
    *   **通用性验证**：将 ReAd 应用于 SASRec、DuoRec、BERT4Rec 和 GRU4Rec 等多种不同架构，证明了其模型无关性。
    *   **消融实验**：分别验证了推荐损失、注意力机制、动态融合权重和对齐损失的有效性。
    *   **参数敏感性**：分析了检索数量 $K$、损失系数 $\lambda$ 和熵计算比例 $\rho$ 的影响。
*   **客观性**：采用全排名评估（无负采样偏差），并进行了 10 次运行取平均值，通过了显著性检验（p < 0.01）。

### 6. 主要结论与发现
*   **性能领先**：ReAd 在所有数据集上均显著优于现有 SOTA 模型，尤其在稀疏的 Amazon 数据集上提升更明显。
*   **有效应对偏移**：案例研究显示，当用户兴趣从“剧情片”转向“惊悚片”时，ReAd 能通过检索相似的转换模式准确修正预测。
*   **不确定性引导有效**：基于熵的动态融合能识别模型何时“迷茫”，并适时引入检索信号进行补偿。

### 7. 优点（亮点）
*   **模型无关 (Model-Agnostic)**：可以无缝集成到任何现有的序列推荐模型中，无需修改原模型架构。
*   **高效性**：相比于需要梯度更新的 TTT 方法，ReAd 仅通过前向计算和轻量级融合实现自适应，适合实时系统。
*   **协作信号显式化**：将隐藏在参数中的协作信号转化为显式的记忆库检索，增强了模型的可解释性和适应力。

### 8. 不足与局限
*   **记忆库规模**：对于超大规模工业级数据，协作记忆库的存储和检索压力会增大，虽然有 FAISS 加速，但仍存在扩展性挑战。
*   **冷启动限制**：如果测试序列在记忆库中完全没有相似的模式（极度冷启动），检索增强的效果可能会打折扣。
*   **离线依赖**：记忆库的构建仍依赖于训练集，如果测试环境发生了完全脱离历史数据的极端分布偏移，该方法可能失效。

（完）
