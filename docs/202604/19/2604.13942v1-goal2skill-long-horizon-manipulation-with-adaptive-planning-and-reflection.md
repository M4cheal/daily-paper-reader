---
title: "Goal2Skill: Long-Horizon Manipulation with Adaptive Planning and Reflection"
title_zh: Goal2Skill：具有自适应规划与反思的长程操作
authors: "Zhen Liu, Xinyu Ning, Zhe Hu, Xinxin Xie, Weize Li, Zhipeng Tang, Chongyu Wang, Zejun Yang, Hanlin Wang, Yitong Liu, Zhongzhu Pu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13942v1"
tags: ["query:tta"]
score: 9.0
evidence: VLA系统中长程操作的自适应规划与反思
tldr: 针对现有VLA系统在长程、依赖记忆的任务中表现脆弱的问题，本文提出Goal2Skill框架。该框架采用双系统架构，将基于VLM的高层语义推理（负责任务分解、验证与纠错）与基于VLA的底层运动执行（负责扩散式动作生成）相结合。通过构建规划与执行的闭环，实现了具备记忆感知和自适应重规划能力的具身操纵，在复杂任务中显著提升了成功率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.13942v1/fig-001.webp\", \"caption\": \"Figure 1 A long-horizon manipulation example requiring memory and reflective replanning. The robot moves a Coke can to a scanner, the first scan fails because the barcode is not visible, and execution feedback triggers a corrective retry. The example highlights the need for persistent task context, intermediate verification, and adaptive recovery beyond reactive action generation.\", \"page\": 2, \"index\": 1, \"width\": 790, \"height\": 408}, {\"url\": \"assets/figures/arxiv/2604.13942v1/fig-002.webp\", \"caption\": \"Figure 3 Memory-guided closed-loop recovery mechanism of the high-level planner. Green indicates standard sub-task execution under the current plan; yellow denotes a retry of the same sub-task following local diagnosis and adjustment; red signifies dynamic replanning when repeated failures or timeouts invalidate the current plan. This loop is grounded in structured memory, which comprises episodic history, working memory, and the error register.\", \"page\": 7, \"index\": 2, \"width\": 446, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2604.13942v1/fig-003.webp\", \"caption\": \"Figure 4 Qualitative results of our framework on five representative RMBench tasks. From top to bottom, the task sequences correspond to Observe and Pick Up, Rearrange Blocks, Battery Try, Blocks Ranking Try, and Press Button. Each row shows key frames from one rollout and illustrates that the method can complete representative memory-dependent manipulation tasks with different sequential interaction requirements.\", \"page\": 10, \"index\": 3, \"width\": 473, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2604.13942v1/fig-004.webp\", \"caption\": \"Figure 2 Overview of the proposed dual-system framework. The high-level planner manages task planning, memory maintenance, and verification-driven reflection, while the low-level executor performs sub-task execution through masked perception and a skill library. This interaction establishes a closed-loop hierarchy for long-horizon embodied manipulation.\", \"page\": 5, \"index\": 4, \"width\": 936, \"height\": 487}]"
motivation: 现有的端到端VLA策略在处理具有部分可观测性和多阶段依赖的长程任务时，缺乏持久记忆和自适应纠错能力。
method: 提出一种双系统框架，结合了负责结构化记忆与任务分解的高层VLM规划器和负责几何感知动作生成的底层VLA执行器。
result: "在RMBench基准测试中，该框架将平均成功率从最强基线的9.8%大幅提升至32.4%。"
conclusion: 实验证明，结构化记忆和闭环恢复机制是实现稳健长程具身操纵的关键。
---

## 摘要
最近的视觉-语言-动作（VLA）系统在具身操作中展现了强大的能力。然而，大多数现有的 VLA 策略依赖于有限的观测窗口和端到端的动作预测，这使得它们在具有部分可观测性、遮挡和多阶段依赖的长程、依赖记忆的任务中表现脆弱。此类任务不仅需要精确的视觉运动控制，还需要持久记忆、自适应任务分解以及从执行失败中显式恢复的能力。为了解决这些局限性，我们提出了一种用于长程具身操作的双系统框架。我们的框架将高层语义推理与底层运动执行显式分离。高层规划器实现为基于视觉语言模型（VLM）的智能体模块，负责维护结构化任务记忆，并执行目标分解、结果验证和错误驱动的修正。底层执行器实例化为基于 VLA 的视觉运动控制器，通过以保持几何特征的过滤观测为条件的扩散动作生成来执行每个子任务。这两个系统共同构成了规划与执行之间的闭环，实现了记忆感知推理、自适应重新规划和鲁棒的在线恢复。在代表性 RMBench 任务上的实验表明，所提出的框架显著优于代表性基线，平均成功率达到 32.4%，而最强基线仅为 9.8%。消融研究进一步证实了结构化记忆和闭环恢复对于长程操作的重要性。

## Abstract
Recent vision-language-action (VLA) systems have demonstrated strong capabilities in embodied manipulation. However, most existing VLA policies rely on limited observation windows and end-to-end action prediction, which makes them brittle in long-horizon, memory-dependent tasks with partial observability, occlusions, and multi-stage dependencies. Such tasks require not only precise visuomotor control, but also persistent memory, adaptive task decomposition, and explicit recovery from execution failures. To address these limitations, we propose a dual-system framework for long-horizon embodied manipulation.   Our framework explicitly separates high-level semantic reasoning from low-level motor execution. A high-level planner, implemented as a VLM-based agentic module, maintains structured task memory and performs goal decomposition, outcome verification, and error-driven correction. A low-level executor, instantiated as a VLA-based visuomotor controller, carries out each sub-task through diffusion-based action generation conditioned on geometry-preserving filtered observations. Together, the two systems form a closed loop between planning and execution, enabling memory-aware reasoning, adaptive replanning, and robust online recovery. Experiments on representative RMBench tasks show that the proposed framework substantially outperforms representative baselines, achieving a 32.4% average success rate compared with 9.8% for the strongest baseline. Ablation studies further confirm the importance of structured memory and closed-loop recovery for long-horizon manipulation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Goal2Skill: Long-Horizon Manipulation with Adaptive Planning and Reflection》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型虽然在短程指令遵循上表现出色，但在**长程（Long-Horizon）具身操作**任务中面临严峻挑战。这些任务通常具有部分可观测性、遮挡和复杂的多阶段依赖关系。
*   **研究动机**：端到端的 VLA 策略通常缺乏持久记忆和显式推理，导致在执行过程中一旦发生偏差或环境变化，系统难以自我纠正，产生错误累积。
*   **核心目标**：构建一个能够像人类一样进行“高层规划-底层执行-反馈反思”的闭环系统，提升机器人在复杂、长程任务中的鲁棒性和成功率。

### 2. 方法论：核心思想与关键技术
论文提出了 **Goal2Skill** 双系统框架，模拟生物学中“大脑（规划）”与“小脑（执行）”的分工：

*   **高层规划器（High-Level Planner）**：
    *   **核心思想**：基于视觉语言模型（VLM）作为智能体，负责语义推理。
    *   **任务分解**：将全局目标 $G$ 分解为一系列结构化的子任务 $\tau_k$，每个子任务包含指令、预设条件、后置条件、空间约束和技能索引。
    *   **结构化记忆管理**：维护三种记忆状态：**情节历史**（记录过去的操作）、**工作记忆**（当前世界状态的自然语言总结）和**错误寄存器**（记录失败原因）。
    *   **验证与反思机制**：在子任务结束后，通过 VLM 验证后置条件。若失败，则触发“反思引擎”诊断原因，决定是重试、调整参数还是重新规划。

*   **底层执行器（Low-Level Executor）**：
    *   **核心思想**：基于 VLA 的视觉运动控制器，负责精细动作。
    *   **几何感知过滤**：利用规划器提供的空间约束，通过掩码过滤掉环境中的干扰物（Distractors），使模型专注于任务相关区域。
    *   **扩散式技能库**：采用基于扩散（Diffusion-based）的策略生成连续动作序列，确保动作的平滑性和多峰分布处理能力。

### 3. 实验设计
*   **数据集与基准**：使用了 **RMBench** 机器人操作基准测试。
*   **实验场景**：选择了 5 个具有代表性的任务，分为 **M(1)**（低记忆需求：观察并抓取、重排积木）和 **M(n)**（高记忆需求：电池测试、积木排序测试、按按钮）。
*   **对比方法**：
    *   非预训练策略：**DP** (Diffusion Policy), **ACT**。
    *   预训练 VLA 策略：**Pi0.5**, **X-VLA**。
*   **评估指标**：任务成功率（Success Rate），每个任务进行 100 次 Rollout 评估。

### 4. 资源与算力
*   **训练细节**：论文提到针对每个任务使用了 50 个专家演示进行训练，优化步骤为 30k 步。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。通常此类 VLA 模型的微调需要高性能计算资源（如 A100 或 H100 集群）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个任务上进行了 100 次重复实验，提供了统计学上的参考价值。
*   **消融实验**：设计了针对“结构化记忆”和“恢复机制”的专项消融实验，分别验证了情节历史、工作记忆、验证器和反思引擎对成功率的贡献。
*   **充分性评价**：实验设计较为客观，通过对比 M(1) 和 M(n) 任务，清晰地展示了该框架在处理复杂依赖任务时的优势。但对比的任务种类（5 种）相对有限，若能覆盖更多样化的物理交互场景会更具说服力。

### 6. 主要结论与发现
*   **性能飞跃**：Goal2Skill 的平均成功率达到 **32.4%**，远超最强基线（9.8%）。
*   **记忆的重要性**：在记忆密集型任务（M(n)）中，该方法的优势扩大（38.7% vs 9.0%），证明了显式记忆管理是长程任务的关键。
*   **闭环恢复的价值**：引入验证和反思机制后，系统在面对执行失败时表现出更强的韧性，能够通过重试或修正参数完成任务。

### 7. 优点与亮点
*   **解耦设计**：成功将高层语义规划与底层几何控制分离，降低了端到端学习的难度。
*   **主动记忆**：将记忆从被动存储提升为主动推理的基质，支持诊断和纠错。
*   **鲁棒性**：通过几何过滤（Masked Filtering）有效解决了复杂环境中的干扰物问题。
*   **闭环逻辑**：构建了“规划-执行-验证-反思”的完整闭环，更接近人类的操作逻辑。

### 8. 不足与局限
*   **绝对成功率仍有提升空间**：虽然远超基线，但 32.4% 的成功率距离实际工业应用仍有较大差距。
*   **推理延迟**：由于高层规划依赖 VLM 的多次调用（规划、验证、反思），系统的实时响应速度可能受限。
*   **泛化性验证**：实验主要集中在 RMBench 的特定任务上，对于跨场景、跨机器人的泛化能力探讨较少。
*   **依赖专家数据**：底层技能库仍依赖于高质量的专家演示数据。

（完）
