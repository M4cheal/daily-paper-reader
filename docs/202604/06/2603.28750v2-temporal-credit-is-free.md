---
title: Temporal Credit Is Free
title_zh: 时间信用是免费的
authors: Aur Shalev Merin
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28750v2"
tags: ["query:tta"]
score: 7.0
evidence: 针对流式机器学习基准进行在线自适应
tldr: 本研究挑战了循环神经网络在线学习需要复杂梯度传播的传统观点，提出仅利用即时导数（Immediate Derivatives）结合RMSprop梯度归一化即可实现高效的时间信用分配。通过引入一种预测归一化需求的架构规则，该方法在多种架构和基准测试中表现优异，在内存消耗降低1000倍的同时，性能达到甚至超过了全量实时循环学习（RTRL）算法。
selection_source: fresh_fetch
motivation: 旨在解决循环神经网络在线学习中实时循环学习（RTRL）算法计算复杂度高和内存开销巨大的瓶颈。
method: 采用即时导数代替复杂的Jacobian传播，并根据架构规则使用RMSprop对非线性状态更新的梯度进行尺度归一化。
result: 在十种架构和多个基准测试中，该方法在n=1024规模下比RTRL节省了1000倍内存，且学习效果相当或更好。
conclusion: 时间信用分配可以低成本实现，即时导数配合适当的梯度归一化足以支持高效的在线循环网络训练。
---

## 摘要
循环网络在进行在线自适应时无需雅可比传播。隐藏状态在前向传递中已携带了时间信用；若能停止使用陈旧的迹记忆对其造成干扰，并对各参数组的梯度尺度进行归一化，则即时导数已然足够。一条架构规则可预测何时需要归一化：当梯度必须通过没有输出旁路的非线性状态更新时，需要 $eta_2$ 归一化，反之则不需要。在十种架构、真实灵长类神经数据以及流式机器学习基准测试中，结合 RMSprop 的即时导数表现达到或超过了完整的实时循环学习（RTRL），在内存占用减少 1000 倍的情况下，可扩展至 n = 1024。

## Abstract
Recurrent networks do not need Jacobian propagation to adapt online. The hidden state already carries temporal credit through the forward pass; immediate derivatives suffice if you stop corrupting them with stale trace memory and normalize gradient scales across parameter groups. An architectural rule predicts when normalization is needed: \b{eta}2 is required when gradients must pass through a nonlinear state update with no output bypass, and unnecessary otherwise. Across ten architectures, real primate neural data, and streaming ML benchmarks, immediate derivatives with RMSprop match or exceed full RTRL, scaling to n = 1024 at 1000x less memory.