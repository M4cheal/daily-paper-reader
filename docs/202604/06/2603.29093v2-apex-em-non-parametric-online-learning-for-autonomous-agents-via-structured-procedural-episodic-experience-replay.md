---
title: "APEX-EM: Non-Parametric Online Learning for Autonomous Agents via Structured Procedural-Episodic Experience Replay"
title_zh: APEX-EM：通过结构化程序-情境经验回放实现自主智能体的非参数化在线学习
authors: "Pratyay Banerjee, Masud Moshtaghi, Ankit Chadha"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29093v2"
tags: ["query:tta"]
score: 8.0
evidence: 自主智能体无需修改权重的非参数在线学习框架
tldr: APEX-EM是一个针对LLM自主智能体的非参数化在线学习框架，旨在解决智能体无法持久化存储和复用程序化经验的问题。它通过构建包含执行轨迹、错误分析和质量评分的结构化经验库，并结合PRGII工作流与混合检索机制（语义、结构及DAG遍历），实现了跨领域经验迁移。实验表明，该方法在代码生成和复杂查询等任务上显著提升了性能，超越了现有基准。
selection_source: fresh_fetch
motivation: 现有的LLM智能体缺乏持久的程序化记忆，导致在处理结构相似的任务时仍需从零开始推导方案。
method: 提出APEX-EM框架，通过结构化经验表示、PRGII工作流以及结合语义与结构特征的混合检索机制，实现经验的积累与复用。
result: "在KGQAGen-10k和BigCodeBench等基准测试中，APEX-EM分别实现了48.3%和29.4%的成功率提升，显著优于无记忆基准和现有方法。"
conclusion: 结构化的程序化经验回放能有效提升智能体的在线学习能力，且不同任务对反馈信号和检索维度的依赖程度存在差异。
---

## 摘要
基于大语言模型（LLM）的自主智能体缺乏持久的程序性记忆：即使之前已经解决过结构相同的任务，它们仍会从头开始推导解决方案。我们提出了 APEX-EM，这是一种非参数化在线学习框架，它在不修改模型权重的情况下积累、检索并重用结构化程序计划。APEX-EM 引入了：(1) 一种结构化经验表示，编码了每次执行的完整程序-情境追踪——包括规划步骤、中间产物、带有错误分析的迭代历史以及质量评分；(2) 一种“规划-检索-生成-迭代-摄取”（PRGII）工作流，并配备提供多维奖励信号的任务验证器；(3) 一种双结果经验记忆，采用结合了语义搜索、结构特征匹配和计划有向无环图（DAG）遍历的混合检索方式——实现了在词汇无重叠但操作结构相似的任务之间的跨领域迁移。成功的经验作为正向上下文示例，而失败的经验则作为带有结构化错误标注的负向示例。我们使用 Claude Sonnet 4.5 和 Opus 4.5 在 BigCodeBench、KGQAGen-10k 和 Humanity's Last Exam (HLE) 上进行了评估。在 KGQAGen-10k 上，APEX-EM 达到了 89.6% 的准确率，而无记忆基线为 41.3%（提升了 48.3 个百分点），超过了真值检索（oracle-retrieval）的上限（84.9%）。在 BigCodeBench 上，其成功率从 53.9% 的基线提升至 83.3%（提升了 29.4 个百分点），在骨干网络冻结的同等条件下，超过了 MemRL 11.0 个百分点的增益（注：我们在分析中对骨干网络的差异进行了控制）。在 HLE 上，实体图检索从 25.2% 提升至 48.0%（提升了 22.8 个百分点）。消融实验表明，各组件的价值取决于具体任务：丰富的评判反馈对代码生成的影响微乎其微，但对结构化查询至关重要（提升了 10.3 个百分点），而二值信号迭代可以部分弥补较弱的反馈。

## Abstract
LLM-based autonomous agents lack persistent procedural memory: they re-derive solutions from scratch even when structurally identical tasks have been solved before. We present APEX-EM, a non-parametric online learning framework that accumulates, retrieves, and reuses structured procedural plans without modifying model weights. APEX-EM introduces: (1) a structured experience representation encoding the full procedural-episodic trace of each execution -- planning steps, artifacts, iteration history with error analysis, and quality scores; (2) a Plan-Retrieve-Generate-Iterate-Ingest (PRGII) workflow with Task Verifiers providing multi-dimensional reward signals; and (3) a dual-outcome Experience Memory with hybrid retrieval combining semantic search, structural signature matching, and plan DAG traversal -- enabling cross-domain transfer between tasks sharing no lexical overlap but analogous operational structure. Successful experiences serve as positive in-context examples; failures as negative examples with structured error annotations.   We evaluate on BigCodeBench, KGQAGen-10k, and Humanity's Last Exam using Claude Sonnet 4.5 and Opus 4.5. On KGQAGen-10k, APEX-EM achieves 89.6% accuracy versus 41.3% without memory (+48.3pp), surpassing the oracle-retrieval upper bound (84.9%). On BigCodeBench, it reaches 83.3% SR from a 53.9% baseline (+29.4pp), exceeding MemRL's +11.0pp gain under comparable frozen-backbone conditions (noting backbone differences controlled for in our analysis). On HLE, entity graph retrieval reaches 48.0% from 25.2% (+22.8pp). Ablations show component value is task-dependent: rich judge feedback is negligible for code generation but critical for structured queries (+10.3pp), while binary-signal iteration partially compensates for weaker feedback.

---

## 论文详细总结（自动生成）

以下是对论文《APEX-EM: Non-Parametric Online Learning for Autonomous Agents via Structured Procedural-Episodic Experience Replay》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的 LLM 智能体在程序层面是“无状态”的。即使面对结构完全相同的任务，它们每次都会从头开始推导方案，无法积累和复用过去的执行经验。
*   **研究动机**：
    *   现有的记忆机制（如 MemRL）多存储非结构化的文本摘要或标量 Q 值，丢失了执行过程中的因果链和程序结构。
    *   语义检索依赖词汇重叠，导致在描述不同但逻辑相似的跨领域任务间（如“对比球员数据”与“对比公司营收”）无法有效迁移知识。
    *   缺乏对失败经验的系统性利用（负向约束）。

### 2. 方法论：APEX-EM 框架
APEX-EM 是一种无需修改模型权重的非参数化在线学习框架，其核心由以下三部分组成：

*   **程序化知识图谱 (PKG)**：
    *   将经验存储为图节点，包含实体、子任务、操作（Operation）等。
    *   **结构化经验表示**：每个经验包含六层：目标反射（RG）、程序反射（RP）、证据反射（RE）、执行轨迹（RT）、错误注册表（Rerr）和补丁反射（Rpatch）。
*   **PRGII 工作流**：
    1.  **Plan（规划）**：分解任务，提取“结构特征签名”（如 `[实体解析 -> 时间过滤 -> 聚合]`）。
    2.  **Retrieve（检索）**：混合检索机制，结合语义搜索、**结构签名匹配（基于最长公共子序列 LCS）**和图遍历。
    3.  **Generate（生成）**：利用检索到的成功案例（正样本）和失败案例（带错误标注的负样本）进行上下文学习。
    4.  **Iterate（迭代）**：通过验证器进行自我修正。
    5.  **Ingest（摄取）**：质量评估后将新经验存入 PKG，实现闭环学习。
*   **双结果记忆（Dual-outcome Memory）**：
    *   成功经验提供可复用的程序模板；失败经验通过错误注册表提供避坑指南（Guardrails）。

### 3. 实验设计
*   **数据集与场景**：
    *   **BigCodeBench (BCB)**：复杂代码生成，需调用多个库。
    *   **KGQAGen-10k**：基于 Wikidata 的多步结构化查询生成（SPARQL）。
    *   **Humanity’s Last Exam (HLE)**：跨学科专家级知识推理。
*   **基准 (Benchmark)**：以 MemRL 为主要对比对象，同时涵盖 RAG、Self-RAG、Reflexion、Mem0 和 MemP。
*   **骨干模型**：主要使用 Claude Sonnet 4.5 和 Claude Opus 4.5，对比组包含 GPT-4o 和 Gemini-3-pro。

### 4. 资源与算力
*   **算力说明**：论文未明确给出具体的 GPU 型号和总算力消耗。
*   **实验细节**：提到使用了并行处理（$p=3$ 或 $5$），实验运行 10 个 Epoch。由于使用的是 API 模型（Claude, GPT-4o），主要的资源消耗体现在 API 调用次数和 Token 数上。

### 5. 实验数量与充分性
*   **实验规模**：在三个迥异的领域（代码、知识图谱、通用推理）进行了大规模测试，KGQA 样本量达 2000+，BCB 涵盖 1140 个任务。
*   **消融实验**：设计了 A0-A5 等多组消融实验，分别验证了记忆、评判反馈、迭代、检索策略（语义 vs. 图）的贡献。
*   **充分性评价**：实验设计非常充分且客观。不仅对比了最终成功率（SR），还分析了累积成功率（CSR）、跨任务迁移能力以及迭代效率的提升（随着经验增加，解决问题所需的迭代次数显著下降）。

### 6. 主要结论与发现
*   **性能飞跃**：在 KGQA 上，APEX-EM 将准确率从 41.3% 提升至 89.6%，超过了拥有真值子图的 Oracle 检索上限。
*   **跨领域迁移**：结构签名匹配允许智能体在词汇零重叠的情况下复用逻辑模式，解决了传统 RAG 在低语义相关性下的失效问题。
*   **任务依赖性**：发现高质量的评判反馈对结构化查询（KGQA）至关重要（+10.3pp），但对代码生成（BCB）提升有限，因为代码运行结果本身提供了强信号。
*   **效率增益**：随着记忆库的充实，智能体在首轮尝试中解决问题的比例大幅增加，推理成本随之降低。

### 7. 优点
*   **结构化深度**：相比于扁平的文本记忆，PKG 捕获了程序逻辑的本质。
*   **双向学习**：系统地利用失败经验作为负约束，这在现有智能体记忆研究中较为少见。
*   **模型无关性**：作为非参数化方法，它可以即插即用地提升任何冻结权重的 LLM 性能。
*   **可解释性**：通过图谱和反射层，可以清晰地追踪智能体为何复用某段逻辑。

### 8. 不足与局限
*   **模型差异**：虽然在增益百分点上超过了 MemRL，但由于使用了不同的骨干网络（Claude 4.5 vs GPT-4o），绝对数值的直接对比存在一定偏差风险。
*   **评判器依赖**：系统高度依赖“评判模型（Judge LLM）”的质量，若评判器给出错误反馈，可能导致错误经验的摄入。
*   **分类体系**：结构签名的分类法目前仍带有一定的人工设计成分，自动化发现签名的能力有待加强。
*   **收敛性**：作为在线学习系统，缺乏像强化学习那样的形式化收敛证明。

（完）
