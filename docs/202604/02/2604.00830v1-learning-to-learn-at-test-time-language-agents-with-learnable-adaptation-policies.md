---
title: "Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies"
title_zh: 学习在测试时学习：具有可学习自适应策略的语言智能体
authors: "Zhanzhi Lou, Hui Chen, Yibo Li, Qian Wang, Bryan Hooi"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00830v1"
tags: ["query:tta"]
score: 9.0
evidence: 测试时间学习的可学习自适应策略
tldr: 本研究针对语言智能体在推理阶段的测试时学习（TTL）问题，指出传统方法依赖手工设计的固定适配策略。为此，作者提出了Meta-TTL框架，通过双层优化将适配策略的发现过程自动化。该框架利用进化搜索在多样化任务上优化策略，使其能根据历史经验更有效地修正错误。实验证明，Meta-TTL在Jericho和WebArena-Lite基准测试中显著优于手工基准，展现了强大的泛化能力。
selection_source: fresh_fetch
motivation: 现有的测试时学习方法依赖于人工设计的固定适配策略，无法针对特定任务环境进行最优调整。
method: 提出Meta-TTL框架，通过双层优化和进化搜索，在多样化任务分布中自动学习并优化智能体的适配策略。
result: 在Jericho和WebArena-Lite基准测试中，Meta-TTL在分布内和分布外设置下均显著优于手工设计的基准方法。
conclusion: 学习到的适配策略能够编码可迁移的策略，有效提升语言智能体在推理阶段的自我修正和泛化能力。
---

## 摘要
测试时学习（TTL）使语言智能体能够在推理阶段通过与环境的重复交互来迭代优化其性能。TTL 的核心是一个自适应策略，它根据先前回合的经验更新执行者策略（actor policy），从而改善未来的行为。现有方法依赖于固定的、手工设计的自适应策略，而不是针对下游改进对其进行优化。我们认为，最优的自适应策略应该从任务环境中学习，而不是基于人类直觉进行手工设计。为此，我们引入了 Meta-TTL，这是一个将有效自适应策略的发现表述为双层优化问题的框架。在该框架内，内循环执行标准的 TTL 过程，衡量候选自适应策略在连续回合中帮助智能体纠正错误的有效性。在智能体性能的指导下，外循环在多样化的训练任务分布上采用进化搜索，以迭代地优化自适应策略。我们在 Jericho 和 WebArena-Lite 上评估了 Meta-TTL，涵盖了分布内（ID）和分布外（OOD）设置，并使用了多种元智能体骨干网络。两个基准测试的结果表明，Meta-TTL 一致优于手工设计的基准方法，这表明优化后的自适应策略编码了可迁移的策略，能够泛化到训练任务分布之外。

## Abstract
Test-Time Learning (TTL) enables language agents to iteratively refine their performance through repeated interactions with the environment at inference time. At the core of TTL is an adaptation policy that updates the actor policy based on experience from previous episodes, thereby improving future behavior. Existing methods rely on fixed, hand-crafted adaptation policies rather than optimizing them for downstream improvement. We argue that optimal adaptation policies should be learned from task environments, not hand-engineered based on human intuition. To achieve this, we introduce Meta-TTL, a framework that formulates the discovery of effective adaptation policies as a bi-level optimization problem. Within this framework, the inner loop executes the standard TTL process, measuring how effectively a candidate adaptation policy helps an agent correct errors across sequential episodes. Guided by the agent's performance, the outer loop employs evolutionary search over a diverse distribution of training tasks to iteratively refine the adaptation policy. We evaluate Meta-TTL on Jericho and WebArena-Lite across both in-distribution (ID) and out-of-distribution (OOD) settings, using multiple meta-agent backbones. Results on both benchmarks show that Meta-TTL consistently outperforms hand-crafted baselines, suggesting that the optimized adaptation policy encodes transferable strategies that generalize beyond the training task distribution.

---

## 论文详细总结（自动生成）

以下是对论文《Learning to Learn-at-Test-Time: Language Agents with Learnable Adaptation Policies》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的语言智能体（LLM Agents）在面对新环境时，往往无法像人类一样通过“失败-诊断-调整-尝试”的循环进行有效学习。虽然现有的“测试时学习”（Test-Time Learning, TTL）方法（如 Reflexion）允许智能体在推理阶段进行自我修正，但其**自适应策略（Adaptation Policy）**通常是固定且由人工设计的。
*   **核心问题**：人工设计的策略往往基于直觉，无法针对特定任务环境达到最优。论文提出，智能体“如何根据经验进行自我更新”这一能力本身应该是可学习的，而非预设的。
*   **整体含义**：通过将 TTL 建模为一个元学习（Meta-Learning）问题，使智能体能够从任务分布中自动发现并优化其自适应规则，从而在测试阶段实现更强的自我进化能力。

### 2. 方法论
论文提出了 **META-TTL** 框架，其核心是一个**双层优化（Bi-level Optimization）**结构：
*   **内循环（Inner Loop - TTL 过程）**：
    *   **执行者（Actor）**：一个冻结参数的 LLM，根据当前的系统提示词（System Prompt）在环境中执行任务。
    *   **元智能体（Meta-Agent）**：观察执行者的轨迹历史，根据**元提示词（Meta-Prompt）$\phi$** 生成更新后的执行者提示词。
    *   **目标**：在 $K$ 个连续回合中，通过不断重写执行者的提示词来提升任务表现。
*   **外循环（Outer Loop - 元训练过程）**：
    *   **进化搜索**：在训练任务分布上，利用 LLM 作为优化器（Proposer）对元提示词 $\phi$ 进行迭代演化。
    *   **评估指标**：使用**加权学习曲线下面积（W-AUC）**，该指标赋予后期回合更高的权重，以奖励持续的性能提升。
    *   **专家池（Expert Pool）**：维护每个任务的最优元提示词，通过局部验证和全局验证确保候选策略的有效性和泛化性。
*   **关键技术**：最终产出的 $\phi^*$ 是一个高度优化的自然语言指令，它指导元智能体如何进行信用分配（Credit Assignment）、提取持久事实以及管理探索策略。

### 3. 实验设计
*   **场景与数据集**：
    *   **Jericho**：交互式小说游戏（文本决策），包含 3 个分布内（ID）游戏用于训练，3 个分布外（OOD）游戏用于测试。
    *   **WebArena-Lite**：网页导航任务，涵盖 GitLab、地图、购物等 5 个领域，同样划分为 ID 和 OOD 集合。
*   **基准方法（Baselines）**：
    *   **Static**：无自适应的静态智能体。
    *   **Reflexion**：经典的手工设计自我反思方法。
    *   **Memory Agent**：带有进化内存机制的智能体。
    *   **Naive Meta-Agent**：使用未经过元训练优化提示词的元智能体架构。
*   **模型骨干**：实验采用了 Gemini 3 Flash、GLM-5 和 GPT-5（注：论文标注时间为 2026 年，此处为该背景下的前沿模型）作为元智能体骨干。

### 4. 资源与算力
*   **训练时长**：论文提到 GPT-5 在 Jericho ID 游戏上的元训练过程经历了 26 次迭代，耗时约 **27 小时**。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号和数量。由于该方法主要在提示词空间（Prompt Space）进行梯度无关的搜索，其算力消耗主要集中在 LLM 的 API 调用或推理开销上，而非传统的权重梯度更新。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个截然不同的领域（文本游戏与网页操作），并在多种模型骨干上进行了验证。
*   **消融与对比**：对比了从静态到最先进的手工 TTL 方法，并详细分析了 ID 和 OOD 的表现。
*   **充分性评价**：实验设计较为充分。通过引入 OOD 测试，验证了学习到的自适应策略并非简单的“过拟合”训练任务，而是掌握了通用的学习方法论。此外，通过 W-AUC 曲线分析了学习的稳定性，证明了方法的客观性。

### 6. 主要结论与发现
*   **性能飞跃**：META-TTL 在 Jericho ID 任务上的平均得分从 50.4 提升至 110.8（约 120% 提升），在 WebArena-Lite 上也有显著增长。
*   **强泛化性**：优化后的元提示词在未见过的 OOD 任务上依然表现优异，证明了“学习如何学习”的策略具有可迁移性。
*   **涌现策略**：元训练自动发现了诸如“强制结构化输出”、“显式信用分配协议”、“受控的探索管理”以及“条件化事实库”等高级自适应策略。

### 7. 优点
*   **原理性创新**：将 TTL 从“手工设计”转向“自动优化”，填补了智能体自我进化领域的一个重要空白。
*   **可解释性**：最终学习到的是自然语言形式的元提示词，人类可以直观地理解智能体学到了哪些自适应技巧。
*   **轻量化部署**：测试时无需更新模型权重，仅通过提示词重写即可实现性能提升，具有很强的工程实用性。

### 8. 不足与局限
*   **奖励粒度依赖**：在奖励信号密集的任务（如 Jericho）中效果极佳，但在奖励稀疏或二元化的任务（如 WebArena）中，进化搜索的效率和提升幅度相对较小。
*   **搜索成本**：外循环的进化搜索需要大量的环境交互和 LLM 调用，元训练阶段的成本较高。
*   **任务覆盖**：虽然验证了两个领域，但对于更复杂的长程规划或多模态实时交互任务的适用性仍有待进一步探索。

（完）
