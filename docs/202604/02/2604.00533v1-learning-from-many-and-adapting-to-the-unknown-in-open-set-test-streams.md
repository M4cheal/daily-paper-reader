---
title: Learning from Many and Adapting to the Unknown in Open-set Test Streams
title_zh: 在开放集测试流中从多源学习并适应未知
authors: "Xiao Zhang, Juntao Lyu, Tianyu Hu, Qianchuan Zhao, Huimin Ma"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00533v1"
tags: ["query:tta"]
score: 10.0
evidence: 针对开集测试流的参数高效大语言模型自适应方法
tldr: 针对大语言模型在动态分布和新任务下的脆弱性，本文提出了一种受果蝇记忆更新机制启发的参数高效测试时自适应方法 SyCo。该方法通过 Rac1 和 MAPK 路径在低秩适配器上进行更新，结合结构化 TTA 目标，在保留源知识的同时实现快速特化。研究还引入了多源开集自适应（MOA）基准，实验证明 SyCo 在处理未知任务和数据偏移方面显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的测试时自适应方法往往忽略了源知识的保留以及自适应信号的可靠性，导致模型在处理非平稳测试流时表现不佳。
method: 提出 SyCo 方法，利用 Rac1 路径限制梯度更新以保护源知识，并结合 MAPK 路径的层级控制器抑制噪声更新，实现低秩适配器的稳健调整。
result: "在 18 个 NLP 数据集及 MOA 设置下，SyCo 在未知任务自适应和未知数据偏移上分别达到了 78.31% 和 85.37% 的准确率，显著超越强基线。"
conclusion: SyCo 通过模拟生物信号传导机制，为 LLM 在复杂、动态的真实部署环境中提供了一种高效且可靠的持续学习与自适应方案。
---

## 摘要
大语言模型（LLMs）通过可重用的表示和灵活的推理在不同任务间进行泛化，但在面对不断演化的任务和持续的分布偏移时，在实际部署中仍然表现脆弱。一种常见的方法是测试时自适应（TTA），现有方法通常在全参数空间上使用人工设计的无监督目标来更新模型，且大多忽略了对共享源知识的保留以及自适应信号的可靠性。借鉴果蝇（Drosophila）记忆更新的分子信号级联机制，我们提出了突触巩固（Synapse Consolidation, SyCo），这是一种参数高效的 LLM 自适应方法。该方法在由问题理解、过程理解和源域护栏驱动的结构化 TTA 目标指导下，通过 Rac1 和 MAPK 通路更新低秩适配器。Rac1 将塑性限制在对源知识较不重要的尾部梯度子空间中，从而在保留源表示的同时实现快速专业化。MAPK 使用分层控制器来抑制噪声更新，并在非平稳流中巩固有效的自适应。为了模拟具有多个源和不断涌现任务的真实部署场景，我们引入了多源开放集自适应（MOA）设置。在该设置中，模型在多个有标签的源任务上进行训练，然后在混合了已见和未见任务（且在标签和意图空间中存在部分重叠）的开放、非平稳无标签测试流上进行自适应。在 18 个 NLP 数据集和 MOA 设置下，SyCo 一贯优于强基准模型，在未见任务自适应上达到了 78.31%，在未见数据偏移上达到了 85.37%。

## Abstract
Large Language Models (LLMs) generalize across tasks via reusable representations and flexible reasoning, yet remain brittle in real deployment under evolving tasks and continual distribution shift. A common approach is Test-Time Adaptation (TTA), existing ones of which updates models with hand-designed unsupervised objectives over the full parameter space and mostly overlook preserving shared source knowledge and the reliability of adaptation signals. Drawing on molecular signaling cascades of memory updating in Drosophila, we propose Synapse Consolidation (SyCo), a parameter-efficient LLM adaptation method that updates low-rank adapters through Rac1 and MAPK pathways under the guidance of a structured TTA objective driven by problem understanding, process understanding, and source-domain guardrail. Rac1 confines plasticity to a tail-gradient subspace that is less critical for source knowledge, enabling rapid specialization while preserving source representations. MAPK uses a tiered controller to suppress noisy updates and consolidate useful adaptations under non-stationary streams. To model real deployments with multiple sources and continually emerging tasks, we introduce Multi-source Open-set Adaptation (MOA) setting, where a model is trained on multiple labeled source tasks and then adapts on open, non-stationary unlabeled test streams that mix seen and unseen tasks with partial overlap in label and intent space. Across 18 NLP datasets and the MOA setting, SyCo consistently outperforms strong baselines, achieving 78.31\% on unseen-task adaptation and 85.37\% on unseen-data shifts.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning from Many and Adapting to the Unknown in Open-set Test Streams》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
论文关注的是大语言模型（LLMs）在实际部署中面临的**鲁棒性与适应性**挑战。
*   **研究动机**：尽管 LLM 具有强大的泛化能力，但在面对现实世界中不断演化的任务（新任务涌现）和持续的分布偏移（数据漂移）时表现脆弱。
*   **现有挑战**：传统的测试时自适应（TTA）方法通常在全参数空间上使用简单的无监督目标进行更新，这会导致两个严重问题：1. **破坏源知识**（灾难性遗忘）；2. **对噪声信号敏感**（不可靠的伪监督导致模型崩溃）。
*   **核心目标**：在保留多源任务共享知识的同时，实现对未知、非平稳测试流的高效、稳健自适应。

### 2. 方法论：SyCo 框架
受果蝇记忆更新的分子信号级联机制启发，作者提出了**SyCo（Synapse Consolidation，突触巩固）**方法，包含两个核心生物模拟通路：

*   **Rac1 通路（子空间重构）**：
    *   **核心思想**：将塑性限制在对源知识不重要的“尾部梯度子空间”。
    *   **技术细节**：利用 SVD 风格的低秩适配器（LoRA）分解，通过掩码（Mask）保护具有大奇异值的核心方向（代表源知识），仅允许在奇异值较小的“塑料槽”（Plastic Slots）中进行梯度更新。
*   **MAPK 通路（可靠性感知调节）**：
    *   **核心思想**：作为层级控制器，根据信号的可靠性动态调节学习率。
    *   **技术细节**：监控三个指标：熵（Entropy）、似然（Likelihood）和一致性（Consistency）。通过定义的逻辑规则（如公式 8-9）触发“部分更新”或“完全更新”，从而抑制瞬时噪声并巩固长期有益的自适应。
*   **结构化 TTA 目标 ($L_{SC^3}$)**：
    *   由三部分组成：**问题级信号**（确保语义扰动下的一致性）、**过程级信号**（校准推理路径与伪标签）、**源域护栏**（利用少量高质量源数据锚定模型，防止长期漂移）。

### 3. 实验设计
*   **新设置：MOA（Multi-source Open-set Adaptation）**：模型先在多个有标签源任务上训练，然后在包含已见/未见任务混合的无标签流中进行自适应。
*   **数据集**：涵盖 18 个 NLP 数据集（如 AFQMC, TNews, OCNLI, ChnSent, FinRE 等），涉及情感分析、自然语言推理、阅读理解等多种任务。
*   **Benchmark 与对比方法**：
    *   **TTA 类**：TENT, ClusT3, FOA, POEM, T2ARD。
    *   **MTL（多任务学习）类**：SPoT, ATTEMPT, MPT, TA-LoRA。
    *   **混合类**：将上述 MTL 骨干与 TTA 策略结合作为强基准。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件型号（如 GPU 种类）、数量及具体的训练/推理总时长。
*   **参数效率**：由于采用了基于 LoRA 的参数高效微调（PEFT）架构，且 Rac1 通路仅更新低秩矩阵的对角系数或部分子空间，其计算开销理论上远低于全参数微调。

### 5. 实验数量与充分性
*   **实验规模**：作者在 18 个数据集上进行了大规模测试，分为“未见任务（Cross-task）”和“未见数据（Distributional shift）”两个协议。
*   **消融实验**：非常充分。分别针对 Rac1 通路（掩码空间、比例）、MAPK 通路（激活阶段、平滑窗口）、损失函数组件（$L_{prob}, L_{proc}, L_{guard}$）以及不同规模的骨干网络（Qwen, LLaMA, Mistral 等）进行了详细对比。
*   **客观性**：通过引入 MOA 这一更接近现实的复杂设置，并对比了多种“MTL+TTA”的组合变体，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：SyCo 在未见任务自适应上达到 **78.31%**，在未见数据偏移上达到 **85.37%**，显著优于所有单一 TTA 或 MTL 方法。
*   **子空间的重要性**：实验证明，仅更新尾部梯度空间（Rac1）能有效防止模型在长程自适应中崩溃。
*   **可靠性控制**：MAPK 通路的分层调节能有效过滤伪标签中的噪声，使模型在非平稳流中保持稳定。
*   **骨干网络无关性**：SyCo 的增益在不同架构（Qwen, LLaMA 等）和不同参数规模（1.8B 到 8B）下均具有一致性。

### 7. 优点（亮点）
*   **生物启发式创新**：将复杂的分子生物学机制（Rac1/MAPK）成功转化为深度学习的优化策略，具有较强的跨学科新颖性。
*   **解决核心痛点**：针对 TTA 中常见的“灾难性遗忘”和“噪声累积”给出了结构化的解决方案。
*   **现实意义**：提出的 MOA 设置比传统的单源 TTA 更符合 LLM 作为通用助手的实际部署场景。

### 8. 不足与局限
*   **超参数敏感性**：系统引入了较多超参数（如掩码比例 $\alpha$、学习率增益 $\gamma$、损失权重 $\lambda$ 等），虽然文中给出了推荐值，但在极端不同的任务流中可能需要精细调优。
*   **任务范围限制**：实验主要集中在 NLP 文本分类和推理任务上，尚未在长文本生成、多模态或代码生成等更复杂的生成式任务中验证。
*   **实时性开销**：虽然是参数高效的，但 MAPK 通路需要计算熵、似然和多次生成的一致性，这在极高并发的实时部署中可能会带来额外的推理延迟。

（完）
