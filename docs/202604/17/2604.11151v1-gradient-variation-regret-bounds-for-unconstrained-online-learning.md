---
title: Gradient-Variation Regret Bounds for Unconstrained Online Learning
title_zh: 无约束在线学习的梯度变化遗憾界
authors: "Yuheng Zhao, Andrew Jacobsen, Nicolò Cesa-Bianchi, Peng Zhao"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11151v1"
tags: ["query:tta"]
score: 6.0
evidence: 具有遗憾保证的无约束在线学习的无参数算法
tldr: 本文研究无约束在线学习，提出了一类无需预知参数的自适应算法。该算法针对L-平滑凸损失函数，实现了与梯度变化量相关的遗憾界，且无需比较向量范数、Lipschitz常数或平滑度等先验信息。算法每轮更新均有闭式解，计算高效。此外，研究还扩展到了动态遗憾界及随机扩展对抗（SEA）模型，在理论保障上显著优于现有最优结果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11151v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of parameter-free gradient-variation (static) regret; we also include the best-known parameter-free optimistic/problem-independent regret. gt denotes the gradient feedback ∇ft(wt). “Efficient” denotes that the algorithm has a per-round computational cost independent of T , and it is meaningful only when the leading term has a linear-polylogarithmic dependence on ∥u∥.\", \"page\": 3, \"index\": 1, \"width\": 931, \"height\": 331}]"
motivation: 旨在开发无需先验知识且能根据梯度变化自适应调整的无约束在线学习算法，以获得更紧凑的遗憾界。
method: 提出了一种全自适应的参数无关算法，利用梯度变化信息进行动态更新，且每轮计算均有闭式表达式。
result: 在平滑凸损失下实现了与梯度变化平方根成比例的遗憾界，且在SEA模型下取得了目前最优的理论性能。
conclusion: 该研究为无约束在线学习提供了高效且自适应的算法框架，显著提升了在静态和动态环境下的理论界限。
---

## 摘要
我们为无约束在线学习开发了无参数算法，其遗憾保证随梯度变化 $V_T(u) = \sum_{t=2}^T \|\nabla f_t(u)-\nabla f_{t-1}(u)\|^2$ 缩放。对于 $L$-光滑凸损失，我们提供了全自适应算法，在不需要预先知道比较器范数 $\|u\|$、Lipschitz 常数 $G$ 或光滑度 $L$ 的情况下，实现了 $\widetilde{O}(\|u\|\sqrt{V_T(u)} + L\|u\|^2+G^4)$ 阶的遗憾。每轮的更新可以通过闭式表达式高效计算。我们的结果扩展到了动态遗憾，并直接应用于随机扩展对抗（SEA）模型，显著改进了之前的已知最佳结果 [Wang et al., 2025]。

## Abstract
We develop parameter-free algorithms for unconstrained online learning with regret guarantees that scale with the gradient variation $V_T(u) = \sum_{t=2}^T \|\nabla f_t(u)-\nabla f_{t-1}(u)\|^2$. For $L$-smooth convex loss, we provide fully-adaptive algorithms achieving regret of order $\widetilde{O}(\|u\|\sqrt{V_T(u)} + L\|u\|^2+G^4)$ without requiring prior knowledge of comparator norm $\|u\|$, Lipschitz constant $G$, or smoothness $L$. The update in each round can be computed efficiently via a closed-form expression. Our results extend to dynamic regret and find immediate implications to the stochastically-extended adversarial (SEA) model, which significantly improves upon the previous best-known result [Wang et al., 2025].