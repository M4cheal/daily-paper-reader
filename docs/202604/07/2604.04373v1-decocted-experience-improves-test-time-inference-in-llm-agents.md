---
title: Decocted Experience Improves Test-Time Inference in LLM Agents
title_zh: 萃取经验提升大语言模型智能体的测试时推理
authors: "Maohao Shen, Kaiwen Zha, Zexue He, Zhang-Wei Hong, Siru Ouyang, J. Jon Ryu, Prasanna Sattigeri, Suhas Diggavi, Gregory Wornell"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04373v1"
tags: ["query:tta"]
score: 9.0
evidence: 通过测试时间缩放和上下文构建改进大语言模型
tldr: 本研究探讨了在不更新模型参数的情况下，如何通过优化上下文来提升大语言模型智能体的推理性能。针对传统推理时缩放成本高且效率低的问题，本文提出了“提炼经验”的概念。该方法通过从历史经验中提取核心要点、有序组织并检索关键信息，构建高质量的上下文来引导模型推理。实验在数学推理、网页浏览和软件工程等任务中验证了该方法的有效性，证明了经验积累与上下文构建是提升智能体性能的关键轴。
selection_source: fresh_fetch
motivation: 传统的推理时计算缩放（如增加采样或搜索）在处理复杂任务时成本高昂且容易产生无效探索。
method: 提出“提炼经验”机制，通过从历史经验中提取精华、系统化组织并检索相关信息来构建高效的推理上下文。
result: 在数学推理、网页导航和软件工程等多个领域验证了该方法能显著提升智能体的任务成功率和推理效率。
conclusion: 提炼后的经验是构建有效上下文的核心，为大语言模型在推理阶段的性能缩放提供了除计算量之外的新维度。
---

## 摘要
人们对在不更新模型参数的情况下改进大语言模型（LLMs）的兴趣日益浓厚。一个成熟的方向是测试时扩展（test-time scaling），即通过增加推理时计算（如更长的推理、采样或搜索）来提高性能。然而，对于复杂的推理和智能体任务，盲目扩展测试时计算会大幅增加成本，并可能导致在次优探索上浪费预算。在本文中，我们将“上下文”视为改进 LLM 性能的互补扩展轴，并系统地研究了如何构建更好的输入，以通过“经验”引导推理。我们表明，有效的上下文构建关键取决于“萃取经验”（decocted experience）。我们对经验增强型智能体进行了详细分析，研究了如何从经验中推导上下文、性能如何随经验积累而扩展、优质上下文的特征，以及哪些数据结构最能支持上下文构建。我们将“萃取经验”确定为有效上下文构建的关键机制：从经验中提取精华，连贯地组织经验，并检索显著信息以构建有效的上下文。我们在推理和智能体任务（包括数学推理、网页浏览和软件工程）中验证了我们的发现。

## Abstract
There is growing interest in improving LLMs without updating model parameters. One well-established direction is test-time scaling, where increased inference-time computation (e.g., longer reasoning, sampling, or search) is used to improve performance. However, for complex reasoning and agentic tasks, naively scaling test-time compute can substantially increase cost and still lead to wasted budget on suboptimal exploration. In this paper, we explore \emph{context} as a complementary scaling axis for improving LLM performance, and systematically study how to construct better inputs that guide reasoning through \emph{experience}. We show that effective context construction critically depends on \emph{decocted experience}. We present a detailed analysis of experience-augmented agents, studying how to derive context from experience, how performance scales with accumulated experience, what characterizes good context, and which data structures best support context construction. We identify \emph{decocted experience} as a key mechanism for effective context construction: extracting essence from experience, organizing it coherently, and retrieving salient information to build effective context. We validate our findings across reasoning and agentic tasks, including math reasoning, web browsing, and software engineering.

---

## 论文详细总结（自动生成）

以下是对论文《Decocted Experience Improves Test-Time Inference in LLM Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：目前提升大语言模型（LLM）性能的主流方向是“测试时计算缩放”（如增加采样次数或搜索深度），但在复杂智能体任务中，这种方式成本高昂且容易陷入无效探索。
*   **核心问题**：论文提出“上下文”是除计算量之外的另一个重要缩放轴。研究重点在于如何从智能体过去积累的原始经验中，构建出既能引导推理又能保持简洁的高质量上下文。
*   **整体含义**：通过“萃取经验”（Decocted Experience）——即对原始轨迹进行蒸馏、组织和检索，可以在不更新模型参数的情况下，显著提升智能体在数学推理、网页导航和软件工程等任务中的效率和成功率。

### 2. 方法论
论文提出了一个经验增强型智能体的框架，核心步骤包括：
*   **经验蒸馏（Lesson Distillation）**：将原始的长轨迹（包含环境观察和噪声）压缩为“教训/经验”（Lessons）。智能体自我反思成功和失败的尝试，提取出可复用的策略、工作流和避坑指南。
*   **存储整合（Memory Consolidation）**：针对经验随时间无限增长的问题，采用基于聚类（k-means）的整合机制。通过保留类簇中心代表，去除冗余信息，在检索相关性与多样性之间寻找平衡点。
*   **理论支撑（信息增益）**：提出命题 4.1，证明上下文对输出轨迹的信息增益越高，推理所需的预期长度（Token 数或步骤）就越短，从而提升效率。
*   **分层概念树（Hierarchical Concept Tree）**：
    1.  **概念提取**：利用 LLM 将经验抽象为结构化的概念描述（主题、模式、技术）。
    2.  **分层聚类**：构建多层树状结构组织经验。
    3.  **检索与重排序**：测试时先定位叶子节点，再由 LLM 对候选经验进行重排序，以确保上下文兼具相关性和多样性。

### 3. 实验设计
*   **任务与数据集**：
    *   **数学推理**：使用 DAPO-Math 构建经验池，在 AMC、AIME、HMMT 和 BeyondAIME 等竞赛级数据集上评估。
    *   **网页浏览**：使用 WebShop 环境，模拟在线购物决策。
    *   **软件工程**：使用 SWE-bench，要求智能体修复真实的 GitHub Issue。
*   **基准对比（Baselines）**：
    *   **Vanilla**：无上下文的原始模型。
    *   **Top-K Experience**：直接检索原始成功轨迹作为上下文。
    *   **Top-K Lessons**：检索蒸馏后的教训作为上下文。
    *   **Concept Tree**：使用论文提出的树状结构进行检索。
*   **评估指标**：有效性（平均准确率 `avg_m`）和效率（推理 Token 数或交互步数 `T`）。

### 4. 资源与算力
*   **模型使用**：主要实验基于 **Seed-OSS-36B-Instruct**，并在 **GPT-OSS-20B** 上进行了泛化性验证。嵌入模型使用 **Qwen3-Embedding-4B**。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。由于该方法侧重于测试时推理（Inference-time），主要开销在于离线的经验蒸馏、聚类构建以及在线的多次采样评估。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从数千（WebShop/SWE）到上万（Math）规模的经验条目。
*   **充分性**：实验设计较为全面，包含了：
    *   **缩放分析**：探讨了性能随输入上下文长度（K 值）和存储规模（N 值）的变化趋势。
    *   **消融实验**：对比了原始轨迹与蒸馏教训的效果差异。
    *   **理论验证**：通过蒙特卡洛估计验证了条件熵与输出长度的线性相关性。
    *   **多样性分析**：量化了相关性与多样性的权衡（λ 参数）。
*   **客观性**：在三个完全不同的领域（数学、网页、代码）均观察到一致趋势，并使用了两种不同的开源模型基座，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **蒸馏优于原始**：在智能体任务中，蒸馏后的“教训”比原始轨迹更有效，因为它过滤了环境噪声。
*   **存储并非越大越好**：存储整合存在“甜点区”（Sweet Spot），适度压缩冗余经验比保留全部原始经验性能更好。
*   **相关性与多样性的权衡**：有效的上下文不仅要与当前问题相关，内部也需要保持多样性，以覆盖不同的解题策略。
*   **结构化存储的优势**：分层概念树能比扁平化的向量检索提供更具启发性的上下文。

### 7. 优点
*   **新颖的视角**：将上下文视为一种可扩展的资源，并给出了系统化的构建方法。
*   **理论与实践结合**：不仅有工程上的改进（概念树），还通过信息论解释了为什么好的上下文能提高推理效率。
*   **通用性强**：方法不依赖于特定模型架构，且在多个复杂任务领域均表现出色。

### 8. 不足与局限
*   **离线依赖**：经验的蒸馏和树的构建需要预先完成，对于完全陌生的新任务，初期缺乏经验积累。
*   **计算开销**：虽然减少了推理步骤，但检索过程中的 LLM 重排序（Re-ranking）和概念提取本身也会消耗一定的计算资源。
*   **自我经验局限**：实验主要基于智能体的“自我经验”，尚未深入探讨跨智能体经验共享或人类专家经验的融合效果。

（完）
