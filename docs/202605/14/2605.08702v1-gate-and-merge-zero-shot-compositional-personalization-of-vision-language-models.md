---
title: "Gate-and-Merge: Zero-shot Compositional Personalization of Vision Language Models"
title_zh: Gate-and-Merge：视觉语言模型的零样本组合个性化
authors: "Guodong Ding, Angela Yao"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08702v1"
tags: ["query:tta"]
score: 8.0
evidence: 视觉语言模型在测试时的零样本组合个性化
tldr: 本研究针对视觉语言模型（VLM）的组合个性化问题，提出了Gate-and-Merge零样本框架。该方法将每个用户定义的概念独立学习为轻量级LoRA适配器，在推理时通过权重空间合并实现概念组合。核心创新在于引入门控机制以抑制无关激活，并结合一致性筛选来稳定合并过程，从而在无需共同训练的情况下有效解决概念间的干扰。实验证明，该方法在单概念及多概念组合任务中均表现优异，提升了模型的组合识别能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08702v1/fig-001.webp\", \"caption\": \"Figure 1. Personalized VLMs can learn each concept in isolation, but without a mechanism for compositionality, they may struggle to interpret scenes containing multiple user-defined concepts.\", \"page\": 1, \"index\": 1, \"width\": 516, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2605.08702v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative comparison of personalized captioning capabilities across different models. From top to bottom, each row shows examples containing a single concept, two concepts, and three concepts, respectively. It shows our approach can be effectively extend to compositional scenarios. Red indicates the failure in generating the personalized concept.\", \"page\": 7, \"index\": 2, \"width\": 1036, \"height\": 666}, {\"url\": \"assets/figures/arxiv/2605.08702v1/fig-003.webp\", \"caption\": \"Figure 2. The proposed Gate-and-Merge framework. (a) During personalization, each concept is independently learned as a LoRA module associated with a unique concept token, enabling scalable and disentangled representation learning. (b) At inference, the gating function identifies concept relevance from text and image cues, while the merging mechanism fuses the aligned LoRA updates to achieve zero-shot compositional reasoning.\", \"page\": 4, \"index\": 3, \"width\": 1030, \"height\": 421}]"
motivation: 解决视觉语言模型在处理多个自定义概念组合时，因缺乏共同训练数据而导致的干扰和识别困难问题。
method: 采用独立训练的LoRA适配器表示各概念，并在推理阶段通过门控机制和一致性筛选在权重空间进行零样本合并。
result: 在单概念和多概念组合的个性化任务中，该方法均显著提升了模型的识别与描述性能。
conclusion: Gate-and-Merge为视觉语言模型的组合个性化提供了一种无需协同训练的高效、解耦且稳定的解决方案。
---

## 摘要
本文解决了视觉语言模型（VLMs）的组合个性化问题。在这一问题中，多个用户定义的概念必须在测试时被共同识别或描述。我们引入了 Gate-and-Merge，这是一个无需共现训练即可实现组合个性化的零样本框架。在个性化过程中，每个概念都作为轻量级的 LoRA 适配器独立学习，并与一个概念标记（concept token）配对。基础模型保持不变，概念之间保持解耦。在推理时，我们通过在权重空间直接合并特定概念的 LoRA 更新来实现组合。为了抑制无关激活并防止干扰，我们采用了一种门控机制来估计文本和视觉线索，并仅选择对预测有贡献的模块。我们通过仅结合最有意义且相互一致的更新来进一步稳定组合，从而有助于保留每个概念的身份。我们的定量和定性分析表明，在单概念和组合设置下的多个个性化任务中，性能均有一致的提升。

## Abstract
This paper tackles compositional personalization of vision-language models (VLMs). In this problem, multiple user-defined concepts must be recognized or described jointly at test time. We introduce Gate-and-Merge, a zero-shot framework that enables compositional personalization without the need for co-occurrence training. During personalization, each concept is learned independently as a lightweight LoRA adapter, paired with a concept token. The base model remains unchanged and concepts are kept disentangled. At inference, we enable composition by merging concept-specific LoRA updates directly in weight space. To suppress irrelevant activations and prevent interference, a gating mechanism is employed to estimate textual and visual cues and select only the modules that contribute to the prediction. We further stabilize composition by combining only the most meaningful and mutually consistent updates, helping preserve each concept's identity. Our quantitative and qualitative analyses show consistent gains in performance across multiple personalization tasks in both single-concept and compositional settings.

---

## 论文详细总结（自动生成）

以下是对论文《Gate-and-Merge: Zero-shot Compositional Personalization of Vision Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何让视觉语言模型（VLM）在**无需共同训练数据**的情况下，在推理阶段同时识别和描述多个用户定义的个性化概念（如特定的宠物、人脸或私有物品）。
*   **研究背景**：
    *   现有的个性化方法（如参数微调或提示学习）通常针对单一概念设计，在处理多个概念组合时会出现“概念缠绕”或干扰。
    *   检索增强方法（Retrieval-based）虽然灵活，但依赖外部数据库，存在隐私风险且未真正将知识内化到模型中。
    *   **研究动机**：提出一种模块化、可扩展且隐私友好的框架，实现“零样本组合个性化”，即独立学习每个概念，推理时动态融合。

### 2. 方法论
该框架分为**单概念学习**和**多概念组合**两个阶段：
*   **核心思想**：将每个概念建模为一个独立的模块，包含一个**专属概念标记（Concept Token）**和一组**轻量级 LoRA 适配器**。
*   **关键技术细节**：
    1.  **单概念学习**：为每个概念 $c$ 引入新 Token $\langle c \rangle$，并仅在语言模型分支添加 LoRA 参数 $\delta_c$。训练时冻结视觉编码器和基础模型，确保特征空间的稳定性。
    2.  **多概念组合（Gate-and-Merge）**：
        *   **门控机制（Gate）**：结合文本线索（查询中是否提到 Token）和视觉线索（图像特征与概念原型的余弦相似度），动态筛选当前输入中相关的概念模块。
        *   **合并机制（Merge）**：在权重空间合并选中的 LoRA 更新。为了防止干扰，采用了 **DARE**（随机丢弃冗余参数并缩放）和 **TIES**（解决符号冲突，仅合并方向一致的更新）策略。
*   **流程**：独立训练各概念 LoRA $\rightarrow$ 推理时通过门控激活相关 LoRA $\rightarrow$ 稀疏化并符号一致化合并权重 $\rightarrow$ 应用于基础模型进行推理。

### 3. 实验设计
*   **数据集与场景**：
    *   构建了一个包含 **118 个不同概念**（名人、物体、动漫角色等）的基准测试。
    *   每个概念包含 10 张正样本，并从 CC12M 中检索约 100 张视觉相似的图像作为**硬负样本**。
    *   测试场景分为：单概念（SS）、单查询多概念背景（SM）、多概念共同识别（MM）。
*   **对比方法（Baselines）**：
    *   **MyVLM**：基于参数的个性化方法。
    *   **Yo’LLaVA**：基于提示学习（Prompt Learning）的个性化助手。
    *   **RAP-MLLM**：检索增强的个性化方法。
    *   **GPT-4o + Prompt**：作为强力基准。

### 4. 资源与算力
*   **模型基础**：采用 LLaVA 作为基础 VLM。
*   **训练配置**：使用 AdamW 优化器，概念 Token 学习率 1e-3，LoRA 学习率 1e-4，训练 15 个 Epoch。LoRA Rank 设为 4。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。但由于采用了轻量级 LoRA 和冻结主干网络的策略，其算力需求相对较低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 118 个概念，测试集包含 36 组双概念、10 组三概念及 2 组四概念组合。
*   **实验维度**：进行了视觉识别（准确率、精确率、召回率、F1）、文本/视觉问答（QA）以及图像描述（Captioning）等多维度评估。
*   **消融实验**：针对稀疏比例 $p$ 和视觉门控阈值 $\tau$ 进行了详细的消融研究。
*   **评价**：实验设计较为全面，涵盖了从简单到复杂的多种组合场景，对比了不同技术路线的代表性方法，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：在多概念识别（MM）任务中，该方法准确率接近 70%，远超 Yo’LLaVA（29.3%）。
*   **抑制干扰**：通过 DARE 和 TIES 策略，有效地解决了独立训练的 LoRA 在合并时的语义干扰问题。
*   **内化优势**：相比检索增强方法，内化概念的模型在处理细粒度问题（如“某概念的头发颜色”）时表现更好。
*   **发现**：高稀疏率（0.7-0.8）有助于提升组合识别性能，证明了减少参数重叠的重要性。

### 7. 优点
*   **模块化与可扩展性**：每个概念独立训练，可以像插件一样随时添加或删除，无需重新训练整个系统。
*   **零样本组合**：成功实现了在没有“概念共现”数据的情况下的多概念推理。
*   **鲁棒性**：引入硬负样本训练和门控机制，显著降低了模型在个性化任务中常见的“过度肯定（Yes-bias）”倾向。

### 8. 不足与局限
*   **描述任务挑战**：在多概念图像描述（Captioning）任务中，虽然优于基准，但性能较单概念场景仍有明显下降，说明复杂的组合生成仍是难题。
*   **概念解耦假设**：该方法假设概念之间是可分离的，对于视觉上高度相似或存在强烈物理交互的概念，简单的权重合并可能不足以捕捉复杂的关联。
*   **超参数敏感性**：视觉门控阈值 $\tau$ 对描述任务较敏感，可能需要针对不同应用场景进行微调。

（完）
