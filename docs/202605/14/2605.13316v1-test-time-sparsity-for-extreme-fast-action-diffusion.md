---
title: Test-time Sparsity for Extreme Fast Action Diffusion
title_zh: 面向极速动作扩散的测试时稀疏化
authors: "Kangye Ji, Yuan Meng, Jianbo Zhou, Ye Li, Chen Tang, Zhi Wang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13316v1"
tags: ["query:tta"]
score: 9.0
evidence: 测试时间稀疏性以加速开放环境中的动作扩散
tldr: "动作扩散模型在生成高质量动作方面表现出色，但迭代去噪导致计算开销巨大。本文提出测试时稀疏化（Test-time Sparsity）方法，通过动态预测并剪枝残差计算来加速推理。针对编码开销和剪枝误差，设计了高度并行的推理流水线和全向特征重用策略，在保持性能无损的前提下，实现了92%的计算量减少和5倍的生成加速，推理频率达到47.5 Hz。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13316v1/fig-001.webp\", \"caption\": \"Figure 1. Inference Latency Breakdown. Test-time sparsity removes 5% of the decoder computations by omnidirectionally reusing historical features (Section 3.3), and reduces the delay of conditional encoding and pruning to within one millisecond through a highly parallelized inference pipeline (Section 3.2).\", \"page\": 1, \"index\": 1, \"width\": 496, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-002.webp\", \"caption\": \"Figure 9. Similarity between features from different rollouts on Lift, Square, Tool, and Can tasks.\", \"page\": 11, \"index\": 2, \"width\": 489, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-003.webp\", \"caption\": \"Figure 10. Success rates on the Kitchen task with different numbers of training trajectories.\", \"page\": 11, \"index\": 3, \"width\": 483, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-004.webp\", \"caption\": \"Table 3. Benchmark on multi-stage task (Kitchen). In the Kitchen task, px is the frequency of interacting with x or more objects, including an openable microwave, four turnable oven burners, an oven light switch, and a freely movable kettle. Our method achieves a lossless 5.90×\", \"page\": 7, \"index\": 4, \"width\": 1036, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-005.webp\", \"caption\": \"Figure 2. The overview of Test-time Sparsity. The paradigm comprises four key components: 1) Parallelized Inference Pipeline that reduces non-decoder latency to milliseconds; 2) Omnidirectional Reusing which achieves extreme sparsity by adaptively reusing features cached from the current forward, previous denoising timesteps, and earlier rollout iterations; 3) 3D Lattice Modeling for efficiently organizing large-scale historical features; and 4) Trajectory-level Training that supervises the learning of Omnidirectional Reusing strategy.\", \"page\": 3, \"index\": 5, \"width\": 1032, \"height\": 591}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-006.webp\", \"caption\": \"Figure 15. Visualization of M on Transport.\", \"page\": 13, \"index\": 6, \"width\": 512, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-007.webp\", \"caption\": \"Figure 14. Visualization of M on Tool.\", \"page\": 13, \"index\": 7, \"width\": 512, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-008.webp\", \"caption\": \"Figure 13. Visualization of M on Square.\", \"page\": 13, \"index\": 8, \"width\": 506, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-009.webp\", \"caption\": \"Figure 16. Visualization of M on Kitchen.\", \"page\": 13, \"index\": 9, \"width\": 512, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-010.webp\", \"caption\": \"Figure 8. Visualization of M during the robot lifting a small box. Each cell at coordinate (k, b) denotes Mb,k. The proportions of the colored regions within each cell indicate the confidence scores pcb,k, pFb,k, pTb,k, and pRb,k.\", \"page\": 8, \"index\": 10, \"width\": 512, \"height\": 650}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-011.webp\", \"caption\": \"Figure 12. Visualization of M on Can.\", \"page\": 12, \"index\": 11, \"width\": 512, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2605.13316v1/fig-012.webp\", \"caption\": \"Figure 11. Success rates on the Transport task with different numbers of training trajectories.\", \"page\": 12, \"index\": 12, \"width\": 483, \"height\": 260}]"
motivation: 动作扩散模型因迭代去噪特性导致计算成本高昂，且现有加速技术难以适应开放环境下的动态策略需求。
method: 提出测试时稀疏化范式，结合高度并行的推理流水线与全向特征重用策略，通过异步预测剪枝和跨时间步特征复用来降低冗余计算。
result: "实验表明该方法在保持性能无损的情况下，减少了92%的浮点运算量，并将动作生成速度提升了5倍，达到47.5 Hz的推理频率。"
conclusion: 本研究证明了通过动态稀疏化和高效并行流水线设计，可以显著提升动作扩散模型的实时推理性能，使其更适用于复杂动态环境。
---

## 摘要
动作扩散在生成高保真动作方面表现出色，但由于其迭代去噪的特性，导致了沉重的计算开销。尽管现有技术通过重用缓存特征在加速扩散 Transformer 方面展现出潜力，但它们难以适应开放环境中由多样化感知和多轮展开迭代产生的策略动态。我们提出了测试时稀疏化来应对这一挑战，旨在通过在测试时动态预测每次模型前向传播中可剪枝的残差计算，来加速动作扩散。然而，该范式仍存在两个瓶颈：1）重复的条件编码和剪枝抵消了大部分潜在的速度提升；2）在激进的稀疏度下，从先前去噪时间步缓存的特征无法抑制巨大的剪枝误差。为了解决第一个瓶颈，我们设计了一个高度并行的推理流水线，将非解码器延迟降低到毫秒级。具体而言，我们首先设计了一个轻量级剪枝器，它与扩散 Transformer 共享编码器。然后，我们通过并行处理所有去噪时间步，将编码和剪枝从自回归去噪循环中解耦，并通过异步方式使剪枝器与解码器前向推理重叠。为了克服第二个瓶颈，我们引入了一种全向重用策略，通过选择性地重用从当前前向传播、先前去噪时间步以及早期展开迭代中缓存的特征，实现了 95% 的稀疏度。为了学习展开层级的重用策略，我们采样了一些动作轨迹，以逐步监督稀疏化的扩散过程。大量实验表明，我们的方法减少了 92% 的 FLOPs，并将动作生成速度提升了 5 倍，在 47.5 Hz 的推理频率下实现了无损性能。

## Abstract
Action diffusion excels at high-fidelity action generation but incurs heavy computational costs owing to its iterative denoising nature. Despite current technologies showing promise in accelerating diffusion transformers by reusing the cached features, they struggle to adapt to policy dynamics arising from diverse perceptions and multi-round rollout iterations in open environments. We propose test-time sparsity to tackle this challenge, which aims to accelerate action diffusion by dynamically predicting prunable residual computations for each model forward at test time. However, two bottlenecks remain in this paradigm: 1) repetitive conditional encoding and pruning offset most potential speed gains, and 2) the features cached from previous denoising timesteps cannot constrain large pruning errors under aggressive sparsity. To address the first bottleneck, we design a highly parallelized inference pipeline that minimizes the non-decoder delay to milliseconds. Specifically, we first design a lightweight pruner that shares the encoder with the diffusion transformer. Then, we decouple the encoding and pruning from the autoregressive denoising loop by processing all denoising timesteps in parallel, and overlap the pruner with the decoder forward inference through asynchronism. To overcome the second bottleneck, we introduce an omnidirectional reusing strategy, which achieves 95% sparsity by selectively reusing features cached from the current forward, previous denoising timesteps, and earlier rollout iterations. To learn the rollout-level reusing strategies, we sample a few action trajectories to supervise the sparsified diffusion step by step. Extensive experiments demonstrate that our method reduces FLOPs by 92% and accelerates action generation by 5x, achieving lossless performance with an inference frequency of 47.5 Hz. Our code is available at https://github.com/ky-ji/Test-time-Sparsity.

---

## 论文详细总结（自动生成）

### 论文总结：面向极速动作扩散的测试时稀疏化 (Test-time Sparsity)

#### 1. 核心问题与整体含义
动作扩散模型（Action Diffusion）在机器人高保真动作生成中表现优异，但其**迭代去噪**的本质导致计算开销巨大，推理频率通常仅为 5-6Hz，远低于实时控制所需的 30Hz。现有的加速方法（如特征重用或蒸馏）往往采用静态调度，难以适应开放环境中多变的感知输入和多轮交互动态。本文旨在通过**测试时稀疏化（Test-time Sparsity）**，在推理阶段动态预测并剪枝冗余计算，从而在不损失性能的前提下实现极速推理。

#### 2. 方法论
论文提出了一个名为 **Test-time Sparsity** 的新范式，包含以下核心技术：

*   **高度并行的推理流水线 (Parallelized Inference Pipeline)：**
    *   **解耦与并行化：** 将剪枝器（Pruner）与去噪循环解耦。在推理开始前，一次性并行处理所有 $K$ 个去噪时间步的条件编码和剪枝预测，将 $K$ 步序列循环转化为单次大批次（Batch）操作。
    *   **轻量级剪枝器设计：** 剪枝器与扩散 Transformer 共享条件编码器，仅增加一个极轻量的 Transformer 解码器块。
    *   **异步执行：** 通过多线程异步技术，使剪枝器的运行时间与解码器的首步推理重叠，将非解码器延迟降低至 1ms 以内。
*   **全向特征重用策略 (Omnidirectional Reusing Strategy)：**
    *   **3D 格点建模：** 将历史特征空间建模为由“块索引 (b)”、“去噪步 (k)”和“展开迭代 (r)”组成的 3D 格点。
    *   **多维度重用：** 剪枝器不仅决定是否剪枝，还决定从哪个方向重用特征（当前前向传播、前一去噪步、或前一轮动作展开）。这种“全向”重用能有效补偿高稀疏度（如 95%）带来的误差。
*   **轨迹级训练 (Trajectory-level Training)：**
    *   使用 **Straight-Through Estimator (STE)** 解决离散剪枝掩码的梯度回传问题。
    *   通过采样动作轨迹进行步进式监督，使模型学习到跨越整个动作展开过程的重用策略。

#### 3. 实验设计
*   **数据集与场景：**
    *   **Diffusion Policy：** 在 Robomimic 的 6 个任务（Lift, Can, Square, Transport, Tool Hang, Kitchen）上测试，涵盖熟练人类（PH）和混合人类（MH）数据。
    *   **VLA 模型：** 在 ManiSkill 模拟环境的 4 个任务（PushCube, PickCube, StackCube, Insert）上测试 SOTA 模型 RDT-1B。
*   **Benchmark 与对比方法：**
    *   **Dense：** 原始全量计算模型。
    *   **对比方法：** 包括 EfficientVLA（固定间隔重用）、BAC（块级自适应缓存）和 L2C（学习缓存，原用于图像生成）。
*   **采样器兼容性：** 测试了 DDPM（100步）、DDIM（40步）和 DPM-Solver（50步）。

#### 4. 资源与算力
*   **硬件环境：** 实验主要在 **NVIDIA Tesla A40 (48G)** GPU 和 Intel Xeon Gold 6230 CPU 上进行。
*   **推理性能测试：** 在 **NVIDIA RTX 4090** 上达到了 47.5 Hz 的推理频率。
*   **训练细节：** 剪枝器训练 20 个 epoch，学习率为 1e-4。Diffusion Policy 的批大小为 16，RDT-1B 为 1。

#### 5. 实验数量与充分性
论文进行了非常充分的实验验证：
*   覆盖了从小型 Diffusion Policy 到 10 亿参数规模的 RDT-1B 模型。
*   包含了多阶段复杂任务（Kitchen）和长时程任务。
*   **消融实验：** 针对“全向重用”的三个维度分别做了对比，证明了单一维度重用在高稀疏度下会失效。
*   **数据效率实验：** 证明了仅需 2 条轨迹数据即可训练出性能良好的剪枝策略。
实验设计客观、公平，对比了多种最新的加速基准。

#### 6. 主要结论与发现
*   **极高稀疏度：** 动作扩散模型存在巨大的计算冗余，在 **95% 稀疏度**下仍能保持无损的成功率。
*   **显著加速：** 实现了 **5 倍** 的端到端墙钟时间加速，FLOPs 减少了 **92%**。
*   **实时性：** 将推理频率提升至 47.5 Hz，跨越了机器人实时控制（30 Hz）的门槛。
*   **重用价值：** 发现“展开迭代（Rollout）”维度的特征相似性极高，是实现超高稀疏度的关键。

#### 7. 优点
*   **系统级优化：** 不仅关注算法稀疏性，还通过并行流水线解决了实际推理中的非计算开销瓶颈。
*   **动态适应：** 相比静态缓存，测试时剪枝能根据环境感知动态调整计算分配。
*   **通用性强：** 方案可无缝迁移至不同架构（Transformer-based）和不同规模（从 DP 到 VLA）的模型。

#### 8. 不足与局限
*   **视觉/语言编码器瓶颈：** 在 RDT-1B 等大模型中，加速效果受限于沉重的视觉和语言编码器（这些部分未被稀疏化），导致端到端加速比低于纯动作生成部分。
*   **训练依赖：** 虽然数据效率高，但仍需针对特定任务进行微调训练，并非完全的“即插即用（Training-free）”。
*   **应用限制：** 主要针对 Transformer 架构，对于传统的 U-Net 架构扩散模型，其并行化和块剪枝逻辑可能需要重新设计。

（完）
