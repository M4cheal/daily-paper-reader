---
title: Instance-Adaptive Online Multicalibration
title_zh: 实例自适应在线多校准
authors: "Zhiming Huang, Jamie Morgenstern, Aaron Roth, Claire Jie Zhang"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09273v1"
tags: ["query:tta"]
score: 7.5
evidence: 针对流式序列的实例自适应在线多校准
tldr: 本研究探讨了超越最坏情况的在线多校准问题，提出了一种高效的实例自适应算法。该算法通过动态细化预测值的二叉树网格，在良性序列与最坏情况序列之间进行插值。研究证明，该方法在最坏情况下能达到最优的O(T^2/3)误差率，同时在随机或分段平稳等简单场景下能自动提速至O(sqrt(T))级别，实现了对预测均值过程复杂度的紧致依赖。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09273v1/fig-001.webp\", \"caption\": \"Figure 1: A typical state of the dynamic-bin data structure. The learner predicts using the midpoints of the current leaves, and an interval is refined only after the total mass assigned to it reaches the threshold L/w2 I .\", \"page\": 5, \"index\": 1, \"width\": 814, \"height\": 259}]"
motivation: 旨在解决传统在线多校准算法仅关注最坏情况性能，而无法在更简单的实际数据序列中实现更优误差率的问题。
method: 提出一种通过自适应细化预测值二叉树网格来动态调整预测精度的单算法框架。
result: 算法在最坏情况下保持最优速率，并在随机和分段平稳设置下分别实现了更优的误差界限。
conclusion: 该研究建立了一个与阈值复杂度相关的紧致误差界限，实现了在线多校准在不同复杂度实例下的自适应优化。
---

## 摘要
我们研究了超越最坏情况的在线多校准。我们提出了一种单一且高效的算法，该算法通过自适应地细化预测值的二分网格，在良性序列和最坏情况序列之间进行动态插值。其误差由细化树中的叶节点数量控制。我们的分析恢复了在线多校准已知的 $\widetilde O(T^{2/3})$ 最坏情况最优率，同时能自动适应更简单的实例：在边际随机设定下，它获得了 $\widetilde O(\sqrt T)$ 的速率；对于具有 $J$ 个分段的分段平稳均值，其速率为 $\widetilde O(\sqrt{JT})$。更一般地，该速率取决于可预测均值过程相对于群体族的阈值复杂度度量。我们证明了这种依赖关系在对数因子范围内是紧致的。

## Abstract
We study online multicalibration beyond the worst-case. We give a single, efficient algorithm which dynamically interpolates between benign and worst-case sequences by adaptively refining a dyadic grid of prediction values. Its error is controlled by the number of leaves in the refinement tree. Our analysis recovers the known $\widetilde O(T^{2/3})$ worst-case-optimal rate for online multicalibration, while simultaneously automatically adapting to easier instances: in the marginal stochastic setting it obtains a rate of $\widetilde O(\sqrt T)$, and for piecewise-stationary means with $J$ segments its rate is $\widetilde O(\sqrt{JT})$. More generally, the rate depends on a threshold-complexity measure of the predictable mean process relative to the group family. We show that this dependence is tight up to logarithmic factors.