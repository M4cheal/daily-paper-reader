---
title: Linearizing Vision Transformer with Test-Time Training
title_zh: 通过测试时训练实现视觉 Transformer 的线性化
authors: "Yining Li, Dongchen Han, Zeyu Liu, Hanyi Wang, Yulin Wang, Gao Huang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02772v1"
tags: ["query:tta"]
score: 9.0
evidence: 用于架构对齐的测试时训练 (TTT)
tldr: 本研究旨在解决线性注意力机制难以直接继承预训练Softmax Transformer权重的问题。作者提出利用测试时训练（TTT）架构，通过其两层动态结构实现与Softmax注意力的架构对齐。通过引入键实例归一化和轻量级局部增强模块，成功将Stable Diffusion 3.5线性化为SD3.5-T5。该方法仅需极短的微调时间即可在保持图像生成质量的同时，显著提升高分辨率下的推理速度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02772v1/fig-001.webp\", \"caption\": \"Figure 1. Left: 2K image generated by SD3.5-T5; right: 1K images generated by SD3.5-T5.\", \"page\": 2, \"index\": 1, \"width\": 1017, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2605.02772v1/fig-002.webp\", \"caption\": \"Table 8. Hyperparameters for image classification on ImageNet-1K.\", \"page\": 13, \"index\": 2, \"width\": 413, \"height\": 375}, {\"url\": \"assets/figures/arxiv/2605.02772v1/fig-003.webp\", \"caption\": \"Table 6. Comparison on class-conditional ImageNet 256×256. The reported epochs for DiT5 denote additional fine-tuning after inheriting the pretrained DiT checkpoint. †: Two-stage training: 14 blocks initialized with 8K samples (200 epochs each, ≈17.5 ImageNet-equivalent epochs) + fine-tuning on 10% data for 50K steps (≈10 epochs).\", \"page\": 8, \"index\": 3, \"width\": 492, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2605.02772v1/fig-004.webp\", \"caption\": \"Figure 3. Top: Softmax absorbs key shifts while TTT does not. Our method recenters the keys when inheriting pretrained weights. Bottom: Distribution of key shift ratio across 5K images: pretrained ViT exhibits ratio ≈ 0.5, indicating substantial key bias, while random initialization yields ratio ≈ 0.07.\", \"page\": 5, \"index\": 4, \"width\": 442, \"height\": 362}]"
motivation: 线性注意力模型从头训练成本高昂，且与预训练Softmax模型之间存在显著的表示差异，导致权重难以直接迁移。
method: 采用TTT架构实现架构对齐，并结合键实例归一化与局部增强模块来对齐表示特性。
result: 在SD3.5上仅微调1小时，SD3.5-T5在1K和2K分辨率下分别实现了1.32倍和1.47倍的推理加速，且质量与原模型相当。
conclusion: 该方法证明了通过TTT架构和对齐策略，可以高效地将预训练Transformer转换为线性复杂度模型，实现性能与效率的双赢。
---

## 摘要
虽然线性复杂度的注意力机制为克服 Softmax 注意力的二次方瓶颈提供了一种有前景的替代方案，但从头开始训练此类模型的成本仍然高得令人望而却步。继承预训练 Transformer 的权重提供了一条极具吸引力的捷径，然而 Softmax 与线性注意力之间根本的表示差异阻碍了有效的权重迁移。在这项工作中，我们从架构对齐和表示对齐两个角度解决了这一转换挑战。我们将测试时训练（TTT）识别为一种线性复杂度架构，其双层动态公式在结构上与 Softmax 注意力对齐，从而能够直接继承预训练的注意力权重。为了进一步对齐表示特性（包括关键的平移不变性和局部性），我们引入了键实例归一化和轻量级局部性增强模块。我们通过对 Stable Diffusion 3.5 进行线性化验证了我们的方法，并推出了 SD3.5-T$^5$（Transformer To Test Time Training）。仅需在 4 台 H20 GPU 上进行 1 小时的微调，SD3.5-T$^5$ 即可达到与微调后的 Softmax 模型相当的文本生成图像质量，同时在 1K 和 2K 分辨率下分别将推理速度提升了 1.32 倍和 1.47 倍。

## Abstract
While linear-complexity attention mechanisms offer a promising alternative to Softmax attention for overcoming the quadratic bottleneck, training such models from scratch remains prohibitively expensive. Inheriting weights from pretrained Transformers provides an appealing shortcut, yet the fundamental representational gap between Softmax and linear attention prevents effective weight transfer. In this work, we address this conversion challenge from two perspectives: architectural alignment and representational alignment. We identify Test-Time Training (TTT) as a linear-complexity architecture whose two-layer dynamic formulation is structurally aligned with Softmax attention, enabling direct inheritance of pretrained attention weights. To further align representational properties, including key shift-invariance and locality, we introduce key instance normalization and a lightweight locality enhancement module. We validate our approach by linearizing Stable Diffusion 3.5 and introduce SD3.5-T$^5$ (Transformer To Test Time Training). With only 1 hour of fine-tuning on 4$\times$H20 GPUs, SD3.5-T$^5$ achieves comparable text-to-image quality to the fine-tuned Softmax model, while accelerating inference by 1.32$\times$ and 1.47$\times$ at 1K and 2K resolutions.

---

## 论文详细总结（自动生成）

这篇论文《Linearizing Vision Transformer with Test-Time Training》由清华大学黄高教授团队完成，提出了一种将预训练的 Softmax Transformer 高效转换为线性复杂度模型的新方法。以下是对该论文的深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 Transformer 依赖 Softmax 注意力机制，其计算复杂度随序列长度 $N$ 呈二次方增长（$O(N^2)$），严重限制了其在高分辨率图像生成和长序列任务中的应用。
*   **研究动机**：虽然线性注意力（Linear Attention）能将复杂度降至 $O(N)$，但从头训练大型线性模型成本极高。现有的“线性化”方法（将预训练好的 Softmax 模型转换为线性模型）通常面临**表示空间不匹配**的问题，导致权重迁移效果差，需要大量的蒸馏或长时间微调。
*   **整体含义**：本文旨在寻找一种在结构和表示上都与 Softmax 注意力高度兼容的线性架构，从而实现“极速”权重继承与转换。

### 2. 方法论：核心思想与关键技术
作者提出了 **T5 (Transformer To Test-Time Training)** 框架，核心思想是从**架构对齐**和**表示对齐**两个维度进行优化：

*   **架构对齐（TTT 层的引入）**：
    *   作者发现，Softmax 注意力从查询（Query）的角度看，本质上是一个**带有非线性激活的两层动态 MLP**。
    *   **测试时训练（TTT）** 架构通过在推理过程中动态更新内部模型参数来处理序列。作者选用两层 MLP 作为 TTT 的内部模型，使其在数学结构上与 Softmax 注意力高度对齐，从而能够直接继承预训练的 $Q, K, V$ 权重。
*   **表示对齐（解决训练不稳定性）**：
    *   **键实例归一化（Key Instance Normalization）**：Softmax 具有平移不变性（对 Key 整体偏移不敏感），但 TTT 不具备。作者发现预训练模型的 Key 存在显著偏置，直接迁移会导致梯度爆炸。通过对 Key 进行 InstanceNorm，消除了偏置，实现了训练稳定。
    *   **局部性增强（Locality Enhancement）**：Softmax 注意力天然具有局部偏置，而 TTT 偏向全局建模。作者在 $Q$ 和 $K$ 上引入了轻量级的**深度可分离卷积（DWC）**，以补偿局部特征提取能力的缺失。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像分类**：ImageNet-1K。
    *   **类条件图像生成**：ImageNet 256×256（基于 DiT-XL/2）。
    *   **文本转图像（T2I）**：基于 Stable Diffusion 3.5 Medium。
*   **Benchmark 与对比方法**：
    *   **基准**：原始的 Softmax Transformer 模型（DeiT, DiT, SD3.5）。
    *   **对比方法**：Linear Attention、LiT、CLEAR、Hedgehog、Diffusion Grafting 等主流线性化或高效注意力方案。

### 4. 资源与算力
论文明确提到了算力消耗，展示了该方法极高的效率：
*   **SD3.5-T5 转换**：在 **4 × NVIDIA H20 GPU** 上仅需 **1 小时** 微调（约 3000 步）。
*   **DiT-XL/2 转换**：微调步数仅为原始训练步数的 **0.57%**（约 8 个 Epoch）。
*   **推理加速**：在 1K 分辨率下提速 1.32 倍，在 2K 分辨率下提速 1.47 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础分类到前沿的大规模生成模型，实验设计较为全面。
*   **消融实验**：作者针对“全权重继承 vs 部分继承”、“归一化策略（LayerNorm vs InstanceNorm）”、“局部性模块设计”、“学习率缩放比例”等关键变量进行了详细的对比实验。
*   **客观性**：通过 FID、Inception Score、GenEval 和 DPG-Bench 等多个自动化指标进行定量评估，并提供了高分辨率（2K）的可视化结果，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **TTT 是理想的替代者**：TTT 架构由于其非线性的内部模型，比传统的线性核函数（Linear Kernels）更能拟合 Softmax 的表示空间。
*   **权重继承是可行的**：通过正确的对齐策略，线性模型可以完全继承预训练的注意力权重，而不仅仅是 MLP 权重。
*   **效率与质量并存**：SD3.5-T5 在大幅降低计算量（FLOPs）和延迟的同时，保持了与原模型相当甚至略优的生成质量（如 GenEval 分数从 0.66 提升至 0.69）。

### 7. 优点（亮点）
*   **极简转换**：无需复杂的知识蒸馏（Distillation）或多阶段训练，流程非常直接。
*   **极速收敛**：相比于其他需要数天训练的方法，该方法在 1 小时内即可完成模型线性化。
*   **高分辨率友好**：线性复杂度的特性使其在处理 2K 及以上分辨率图像时优势巨大，解决了显存溢出和推理缓慢的问题。

### 8. 不足与局限
*   **架构修改**：虽然是线性化，但仍需引入 DWC 和 InstanceNorm 等微小架构改动，并非完全“零额外参数”。
*   **替换比例**：在 SD3.5 的实验中，作者只替换了约 50% 的 Block（13 个双向注意力和最后 5 个联合注意力层），尚未实现 100% 的全线性化转换。
*   **任务覆盖**：目前主要集中在视觉任务（分类与生成），尚未在自然语言处理（NLP）的大语言模型（LLM）上验证该对齐策略的有效性。

（完）
