---
title: Online Self-Calibration Against Hallucination in Vision-Language Models
title_zh: 针对视觉语言模型幻觉的在线自我校准
authors: "Minghui Chen, Chenxu Yang, Hengjie Zhu, Dayan Wu, Zheng Lin, Qingyi Si"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00323v1"
tags: ["query:tta"]
score: 8.0
evidence: 视觉语言模型的在线自校准
tldr: 针对大型视觉语言模型（LVLM）中的幻觉问题，本文指出传统离线对齐方法存在监督与感知不匹配的缺陷，导致模型倾向于猜测而非观察。为此提出了OSCAR框架，利用模型自身的“生成-判别差距”，通过蒙特卡洛树搜索和双粒度奖励机制在线构建高质量偏好数据，并结合直接偏好优化（DPO）进行迭代校准，在降低幻觉的同时提升了通用多模态能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00323v1/fig-001.webp\", \"caption\": \"Figure 1: Supervision-Perception Mismatch. Offline supervision from a stronger teacher model forces the student to describe visual details it cannot reliably perceive, resulting in hallucinations.\", \"page\": 1, \"index\": 1, \"width\": 498, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2605.00323v1/fig-002.webp\", \"caption\": \"Table 1: Comparison with state-of-the-art methods on hallucination benchmarks. We evaluate on both generative tasks and discriminative tasks. The best results are shown in bold and the second best results are underlined. ↓ indicates lower is better, ↑ indicates higher is better.\", \"page\": 6, \"index\": 2, \"width\": 1022, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2605.00323v1/fig-003.webp\", \"caption\": \"Figure 5: Qualitative comparison between LLaVA-1.5 and OSCAR. Hallucinated content is highlighted in red, while correct descriptions are shown in green. OSCAR generates fewer hallucinations.\", \"page\": 7, \"index\": 3, \"width\": 502, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2605.00323v1/fig-004.webp\", \"caption\": \"Figure 2: Performance comparison before and after supervised finetuning with stronger teacher-distilled data.\", \"page\": 3, \"index\": 4, \"width\": 508, \"height\": 463}, {\"url\": \"assets/figures/arxiv/2605.00323v1/fig-005.webp\", \"caption\": \"Figure 4: Overview of Online Self-CAlibRation (OSCAR). Left: Monte Carlo Tree Search explores the generation space through selection, expansion, evaluation, and backpropagation. Middle: Preference pairs are extracted from the MCTS tree to refine the model via Direct Preference Optimization. Right: The Dual-Granularity Reward Mechanism combines a process reward that verifies each sentence, and a gated outcome reward that evaluates response quality only when the trajectory is hallucination-free.\", \"page\": 5, \"index\": 5, \"width\": 1081, \"height\": 479}]"
motivation: 传统的离线偏好对齐依赖外部模型监督，导致模型因感知能力不足而产生“猜测”式幻觉。
method: 提出OSCAR框架，利用蒙特卡洛树搜索和双粒度奖励机制在线生成偏好数据，并通过DPO进行迭代优化。
result: 在多个幻觉评估基准上达到了最先进的性能，并增强了模型的通用多模态能力。
conclusion: 通过利用模型内部的判别能力进行自我校准，可以有效解决LVLM的幻觉问题并提升整体表现。
---

## 摘要
大规模视觉语言模型（LVLMs）经常受到幻觉问题的困扰，生成的描述中包含输入图像中并不存在的视觉细节。最近的偏好对齐方法通常依赖于从 GPT 等更强大模型中蒸馏出的监督信号。然而，这种离线范式引入了“监督-感知不匹配”问题：学生模型被迫对齐超出其感知能力的细粒度细节，从而学会了猜测而非观察。为了获得用于在线学习的可靠自我监督，我们发现了 LVLMs 内部存在的“生成-判别差距”，即模型在判别式验证上的准确率高于开放式生成。利用这一能力，我们提出了在线自我校准（OSCAR）框架，该框架结合了蒙特卡洛树搜索（Monte Carlo Tree Search）与双粒度奖励机制（Dual-Granularity Reward Mechanism）来构建偏好数据，并通过直接偏好优化（Direct Preference Optimization）迭代地改进模型。大量实验表明，OSCAR 在幻觉基准测试上达到了最先进的性能，同时提升了通用的多模态能力。

## Abstract
Large Vision-Language Models (LVLMs) often suffer from hallucinations, generating descriptions that include visual details absent from the input image. Recent preference alignment methods typically rely on supervision distilled from stronger models such as GPT. However, this offline paradigm introduces a Supervision-Perception Mismatch: the student model is forced to align with fine-grained details beyond its perceptual capacity, learning to guess rather than to see. To obtain reliable self-supervision for online learning, we identify a Generative-Discriminative Gap within LVLMs, where models exhibit higher accuracy on discriminative verification than open-ended generation. Leveraging this capability, we propose \textbf{O}nline \textbf{S}elf-\textbf{CA}lib\textbf{R}ation (OSCAR), a framework that integrates Monte Carlo Tree Search with a Dual-Granularity Reward Mechanism to construct preference data and iteratively refines the model via Direct Preference Optimization. Extensive experiments demonstrate that OSCAR achieves state-of-the-art performance on hallucination benchmarks while improving general multimodal capabilities.

---

## 论文详细总结（自动生成）

这是一份关于论文《Online Self-Calibration Against Hallucination in Vision-Language Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型视觉语言模型（LVLM）在生成图像描述时经常出现“幻觉”（即描述不存在的物体或错误的属性）。
*   **研究动机**：
    *   **监督-感知不匹配（Supervision-Perception Mismatch）**：现有的离线对齐方法（如 DPO）通常使用 GPT-4V 等强模型的输出作为金标准。作者发现，强行让弱模型去学习强模型能看到但弱模型感知不到的细粒度细节，会导致弱模型学会“猜测”而非“观察”，反而加剧幻觉。
    *   **生成-判别差距（Generative-Discriminative Gap）**：作者观察到，LVLM 在开放式生成时容易出错，但在被明确询问“图中是否有某物”时（判别式任务），其准确率显著更高。这表明模型内部存在尚未被充分利用的验证能力。

### 2. 方法论：OSCAR 框架
OSCAR（Online Self-CAlibRation）的核心思想是利用模型自身的判别能力，通过在线搜索构建高质量偏好数据进行自我校准。
*   **核心思想**：将生成过程视为一种搜索任务，利用蒙特卡洛树搜索（MCTS）探索多种生成路径，并利用模型自身的判别能力作为奖励信号。
*   **关键技术细节**：
    *   **MCTS 引导生成**：在句子级别进行搜索。每个节点代表一个部分响应，通过选择、扩展、评估和回传四个阶段寻找最优路径。
    *   **双粒度奖励机制（Dual-Granularity Reward）**：
        1.  **过程奖励（Node-Level）**：利用模型自身的判别能力，对每个生成的句子进行验证（询问模型该句是否包含幻觉），以负面响应的概率作为即时奖励。
        2.  **门控结果奖励（Trajectory-Level）**：对完整路径进行评估。首先通过物体提取与真值对比进行“忠实度检查”（门控），若通过则由模型对逻辑、流畅度评分；若未通过则奖励归零。
    *   **迭代偏好学习**：从 MCTS 树中提取“全局路径对比”和“兄弟节点对比”两种粒度的偏好对（Chosen vs Rejected），使用 DPO 算法更新模型。模型更新后，在下一轮迭代中生成更高质量的数据。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练数据**：从 LLaVA-150k 中采样图像和提示词。
    *   **评估基准**：
        *   **生成式任务**：Object-HalBench（评估 CHAIR 指标）、AMBER（评估幻觉分数 Hal 和认知偏差 Cog）、MM-VET（评估通用多模态能力）。
        *   **判别式任务**：POPE、AMBER-Discrimination。
*   **对比方法**：
    *   **基础模型**：LLaVA-1.5 (7B/13B)。
    *   **开源模型**：InstructBLIP, MiniGPT-4, mPLUG-Owl2。
    *   **对齐方法**：STIC, POVID, SIMA, 以及基于 Beam Search 的 Self-Rewarding 基线。

### 4. 资源与算力
*   **算力细节**：论文中**未明确提及**具体的 GPU 型号、数量及总训练时长。
*   **训练配置**：提到了使用 LoRA 进行微调（rank=128），学习率为 $1 \times 10^{-5}$，迭代训练了 3 个轮次（Iterations）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个不同规模的基座模型（7B 和 13B）上进行了验证。
    *   每轮迭代构建了约 120k 个偏好对。
    *   涵盖了 5 个主流幻觉和多模态评估基准。
*   **充分性与客观性**：实验设计较为充分。除了主实验外，还进行了**消融实验**（验证过程奖励、结果奖励和 MCTS 的各自贡献）和**在线学习分析**（对比了不同数据源对性能的影响），客观地证明了“离线蒸馏强模型数据”在某些情况下确实会导致性能下降。

### 6. 主要结论与发现
*   **打破离线迷信**：简单地使用更强模型的蒸馏数据进行 SFT 可能会增加幻觉率，因为学生模型无法真正“看到”老师描述的细节。
*   **自我校准有效**：通过 MCTS 结合模型自身的判别能力，可以有效提取出符合模型感知边界的高质量偏好数据。
*   **迭代增益**：OSCAR 支持迭代进化，随着模型能力的提升，其生成的偏好数据质量也随之提高，形成良性循环。
*   **性能领先**：在 LLaVA-1.5-13B 上，OSCAR 将 CHAIRs 指标降低了 87.9%，同时在 MM-VET 上提升了通用能力，证明了该方法在减少幻觉的同时没有牺牲表达能力。

### 7. 优点与亮点
*   **视角独特**：敏锐捕捉到了“监督-感知不匹配”这一被忽视的问题，为 LVLM 对齐提供了新思路。
*   **机制协同**：将 MCTS 的长远规划能力与模型自身的判别优势结合，解决了局部优化导致的幻觉级联问题。
*   **双粒度偏好**：不仅对比完整路径，还对比兄弟节点，极大地提高了搜索树中信息的利用率。

### 8. 不足与局限
*   **计算开销**：MCTS 搜索过程涉及大量的采样和多次判别式调用，在线构建偏好数据的推理成本显著高于传统的离线方法。
*   **依赖真值**：在结果奖励的门控机制中，仍需要图像的物体真值（Ground-truth objects）来进行忠实度检查，这限制了其在完全无标注数据上的扩展性。
*   **算力信息缺失**：未披露训练资源消耗，难以评估该方法的训练效率和落地成本。

（完）
