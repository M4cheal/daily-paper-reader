Title: Why Zeroth-Order Adaptation May Forget Less: A Randomized Shaping Theory

URL Source: https://arxiv.org/pdf/2605.10658v1

Published Time: Tue, 12 May 2026 02:45:44 GMT

Number of Pages: 26

Markdown Content:
# Why Zeroth-Order Adaptation May Forget Less: A Randomized Shaping Theory 

Yao Shu Jian Mu Zhongxiang Dai 

## Abstract 

Continual learning requires new-task adaptation without damaging previously acquired capabilities. Recent forward-pass and zeroth-order (ZO) results show that low-query adaptation may retain better than first-order (FO) descent, but the usual view of ZO as noisy FO estimation does not explain why. We give a local randomized gradient-shaping analysis: finite differences expose a raw shape that is mean-aligned with FO, while the norm-matched comparator fixes the expected squared adaptation norm. Under this controlled comparison, forgetting is controlled by how the adaptation shape exposes retention curvature. For norm-matched ZO, the expected shaped retention curvature obeys an exact identity that preserves the isotropic retention floor while contracting only the anisotropic component. Projecting this identity onto the incoming gradient yields the observable FO–ZO quadratic forgetting gap: ZO improves mean forgetting precisely when the FO direction has above-average retention curvature, by a query-dependent fraction of that curvature excess. A practical finite-query accounting separates the mean mech-anism from one-batch sampling and smoothing perturbations. This mechanism also yields an algorithmic transfer: RISE ( Retention-Aware Isotropic Shaping for Exact-Gradient Adaptation ) applies the calibrated ZO shape to exact FO gradients inside parameter blocks. The resulting stability–plasticity tradeoff is explicit: randomized shaping may reduce the retention exposure paid by FO, exact gradients remove finite-smoothing bias from finite-difference ZO, and blockwise sampling supplies many local shaping directions after one gradient computation. The blockwise anal-ysis separates mean-step damage from centered random exposure, showing how block-diagonal curvature, cross-block coupling, and local shaping scores indicate where this exact-gradient transfer is most likely to be visible. 

## 1 Introduction 

Continual learning asks a model to absorb new tasks without erasing the capabilities learned from previous ones. This tension is acute when large models are repeatedly adapted across domains, users, and deployment conditions: even an accurate new-task gradient can move parameters through locally fragile historical-loss directions. Classical methods address this risk with explicit retention channels: distillation in Learning without Forgetting [ 16 ], Fisher-weighted regularization in EWC [13 ], exemplar replay in iCaRL [ 22 ], and gradient constraints in GEM and A-GEM [ 2 , 18 ]. Recent forward-pass and zeroth-order (ZO) continual-learning work changes the baseline: ZeroFlow [ 6 ] and forward-only adaptation [ 3] show that forward passes can mitigate forgetting, while ZO-FC reports a ZO/FO stability–plasticity tradeoff in adapter/head training [ 30 ]. MeZO establishes forward-pass language-model fine-tuning [ 19 ], and the ZO-LLM benchmark broadens the large-model evidence for memory-efficient ZO adaptation [ 32 ]. These results make the empirical phenomenon visible, but they do not identify the scale-controlled local mechanism. This paper studies that mechanism after the incoming signal and adaptation scale are matched; Appendix A gives the detailed positioning. Viewing ZO only as noisy FO estimation misses the object seen by forgetting. Estimation accuracy concerns the incoming gradient; forgetting is governed by the induced displacement and the retention  

> Preprint. Correspondence to Yao Shu <yaoshu@hkust-gz.edu.cn> .
> arXiv:2605.10658v1 [cs.LG] 11 May 2026

curvature it exposes. We therefore compare FO and ZO through randomized gradient shaping : a random operator acts on the incoming gradient before local adaptation. Sec. 3 separates the practical finite-difference estimator into raw mean alignment, smoothing error, norm matching, and the mean shrinkage induced by matching adaptation scale. With these confounders separated, Sec. 4 shows that calibrated ZO preserves the isotropic part of retention curvature while contracting the anisotropic part. Projecting this contraction onto the incoming gradient gives a sign-sensitive gap: ZO reduces mean quadratic forgetting exactly when the FO direction crosses above-average retention curvature, and it can hurt in the opposite regime. The theory therefore describes a query-controlled move toward isotropic exposure, not a uniform ZO-dominates-FO result. RISE ( Retention-Aware Isotropic Shaping for Exact-Gradient Adaptation ) transfers this mechanism to exact-gradient adaptation, as developed in Sec. 5. It computes the FO gradient by backpropagation, removing finite-difference smoothing bias; applies the calibrated norm-matched ZO shape, importing the retention-curvature mixing that may lower FO forgetting in favorable curvature-gap regimes; and applies the shape inside parameter blocks, turning a small number of random directions per block into many local shaping samples after one gradient computation. These pieces define a stability–plasticity tradeoff: RISE keeps the exact-gradient plasticity signal missing from raw finite-difference ZO, while norm matching still shrinks the mean direction relative to plain FO. The same controls shape the experiments in Sec. 6: retention gains from exact-gradient shape transfer must be separated from smaller steps, mean scaling, generic covariance noise, and matched-budget optimization effects. This paper makes four contributions. First, it formulates local forgetting as shaped adaptation: after the incoming direction is fixed, FO and ZO can be compared by the quadratic forgetting damage of their induced displacements under matched expected squared adaptation norm. Second, it calibrates finite-difference ZO for this comparison by separating raw mean alignment, finite-smoothing residuals, raw norm inflation, norm-matched scaling, and the resulting mean shrinkage. Third, it proves the main mechanism: norm-matched ZO preserves the isotropic retention floor, contracts anisotropic retention curvature, and yields an exact FO–ZO quadratic-forgetting gap governed by the directional retention-curvature excess. Fourth, it transfers the calibrated shape to exact gradients through RISE, and evaluates the resulting stability–plasticity tradeoff with controlled mechanism tests and end-to-end continual-learning benchmarks. The first step is therefore to make precise what historical loss sees during a local adaptation. 

## 2 Local Forgetting as Shaped Adaptation 

The mechanism above needs a comparison object that is independent of how an incoming direction was estimated. Historical loss observes the displacement caused by adaptation, not whether that displacement came from backpropagation or finite differences. We therefore fix one adaptation stage and compare FO and ZO at the level where forgetting is measured: the local adaptation displacement. This local view removes trajectory confounders such as data order, optimizer state, higher-order drift, and learning-rate schedules. The displacement may be one optimizer step or a short net adaptation, as long as it stays in the Taylor neighborhood of the historical loss. At that level, forgetting reduces to a quadratic damage functional, and FO–ZO comparison becomes a question about the retention curvature exposed by each induced displacement. 

2.1 From Historical Loss to Quadratic Damage 

Fix an adaptation stage and suppress its index. Let θ ∈ Rd with d ≥ 2 be the parameter before adapting to the incoming loss f , and let L be the historical loss over past data or tasks. For a local adaptation displacement ∆ ∈ Rd, write θ+ = θ + ∆ and define induced forgetting as 

F(∆) ≜ L(θ+) − L (θ) = L(θ + ∆) − L (θ). (1) This definition does not specify the optimizer. It keeps only what the historical loss observes: the displacement and the retention curvature exposed by that displacement. In the approximately consolidated regime, assume ∥∇L (θ)∥ ≤ ε for small ε ≥ 0. When the quadratic object is the true Taylor term, write HL ≜ ∇2L(θ) and assume the historical-loss Hessian is locally Lipschitz in the 2Taylor neighborhood. Then 

F(∆) = ⟨∇L (θ), ∆⟩ + 12 ∆⊤HL∆ + O(∥∆∥3)= 12 ∆⊤HL∆ + O(ε ∥∆∥ + ∥∆∥3).

(2) Thus, up to lower-order terms, the true Taylor contribution is governed by the historical-loss Hessian along the adaptation direction. The main theory writes H for the retention curvature used in the quadratic analysis: it may be HL in the true-Hessian reading, or a fixed symmetric retention proxy such as a Fisher, Gauss–Newton, or empirical sensitivity matrix. In the proxy reading, the next display is a surrogate damage functional rather than the Taylor equality in (2) . The resulting comparison object is 

Q(∆) ≜ 12 ∆⊤H∆ . (3) Introducing Q does not turn continual learning into a retention-only objective. Standard methods also separate stability from plasticity by protecting past knowledge with regularizers such as EWC [ 13 ], distillation as in Learning without Forgetting [ 16 ], replay as in iCaRL [ 22 ], or gradient constraints as in GEM and A-GEM [ 2, 18 ], while another term drives the incoming task. The present analysis is narrower: once the incoming-loss direction is fixed, the local question is whether the resulting adaptation shape lowers retention cost. This scope avoids the trivial zero-displacement solution of minimizing Q(∆) over arbitrary displacements. Plasticity enters as a control: the setup fixes the incoming direction, and Section 3 separates the raw ZO first-order signal from the norm-matched squared adaptation scale. 

2.2 Adaptation Shape as the Comparison Object 

Once forgetting is reduced to Q(∆), the object to compare is the displacement placed inside this quadratic form, not the procedure that produced the direction. Let g ≜ ∇f (θ)̸ = 0 be the incoming-loss gradient. A shape operator P transforms this gradient before it becomes a local adaptation. With learning rate η > 0,

∆ = −ηPg, QP ≜ Q(∆) = η2

2 g⊤P⊤HP g. (4) Here QP is the forgetting damage induced by P, and P⊤HP is the retention curvature seen by the shaped incoming gradient. We call this operator-level transformation shaping , and call it randomized shaping when P is random. At this stage P = I gives the FO reference; ZO enters in Section 3 as a calibrated randomized shaping distribution. The local comparison also removes scale as an explanation. Because Q is quadratic, a method could reduce forgetting merely by producing a smaller displacement. We therefore match expected squared adaptation norm, but do not solve an unrestricted minimization over arbitrary curvature-aware conditioners. If P could be chosen with full knowledge of H, the best rule would steer Pg toward low-curvature eigenspaces, which is not the ZO family calibrated in Sec. 3. Instead, for a specified curvature-agnostic family C of shaping rules, the theory compares 

n

E [QP] : P ∈ C , E

h

∥Pg∥2i

= ∥g∥2o

. (5) For this paper, C contains the FO reference and the ZO shaping rules calibrated in Section 3. The expectation is over shaping randomness and is vacuous for deterministic FO. The norm constraint prevents smaller expected squared adaptation from being mistaken for a retention mechanism. ZO therefore needs a rule with two properties: a raw form that aligns the incoming-loss signal and a normalized form that fits the norm-matched comparison. Sec. 3 provides that calibration. 

Assumption 2.1 (Local comparison) . The local comparison in (5) relies on three conditions: (i) 

g̸ = 0; (ii) the stage is in the consolidated local regime, so the first-order and higher-order terms in (2) are lower-order at the local displacement scale considered; and (iii) H is a fixed symmetric retention curvature or proxy used in Q. The Gaussian moment identities in Section 4 require only this symmetry. Whenever Q is interpreted as nonnegative forgetting damage, and in the curvature-agnostic benchmark and finite-query deviation bound, we additionally assume H ⪰ 0 and tr( H) > 0.3This assumption turns forgetting into a displacement-level comparison, but (5) still needs a ZO rule that supplies both the incoming signal and the matched scale. Finite differences provide that rule: their raw shape carries the leading first-order signal, and their normalized shape fits the norm-matched comparison. 

## 3 Calibrating the ZO Shape 

The shaped-adaptation comparison in (5) uses a common local form −ηPg, but practical ZO arrives through finite differences rather than an explicit shape operator. The two-point estimator admits the needed separation: a raw ZO shape carrying the leading first-order signal, a finite-smoothing residual that remains a controlled perturbation, and a norm-matched shape whose expected squared adaptation norm is comparable with FO. 

3.1 From Finite Differences to Raw ZO Shape 

The random directions used by the two-point ZO estimator define the raw shape. For query count 

q, draw z1, . . . , zq

> i.i.d.

∼ N (0, I) independently of g and H; all expectations below are over these directions. With smoothing radius μ > 0, the practical estimator and its zero-smoothing raw shape are 

bgμ ≜ q−1

> q

X

> r=1

f (θ + μzr ) − f (θ − μzr )2μ zr , Z ≜ q−1

> q

X

> r=1

zr z⊤ 

> r

. (6) The matrix Z is the leading zero-smoothing shape induced by the sampled directions. Taylor expansion along the queried line segments separates the practical estimator into this raw shape applied to the incoming gradient plus a finite-smoothing residual. Because the same estimator will later be inserted into a quadratic damage functional, the residual is controlled at both first and second moments: the first moment justifies the estimator-level reduction to Zg, and the second moment controls its contribution to forgetting damage. 

Lemma 3.1 (Finite-Smoothing Bridge) . Let f be L-smooth on the queried line segments. Then there exists a remainder vector rμ such that 

bgμ = Zg + rμ, E [∥rμ∥] = O(Lμd 3/2), E

h

∥rμ∥2i

= O(L2μ2d3). (7) Lemma 3.1 identifies Zg as the leading shaped incoming direction and keeps finite smoothing outside the zero-smoothing curvature mechanism. The raw shape still requires two calibrations: preservation of the incoming signal in expectation and a squared adaptation norm comparable with FO. 

3.2 From Raw Alignment to a Norm-Matched Comparator 

Raw ZO preserves the leading mean but inflates the quadratic scale. The fair forgetting comparator is therefore not the raw adaptation Zg itself, but the normalized shape Pg = κ−1/2Zg. This normalization matches the expected squared adaptation norm with FO and makes the induced mean shrinkage explicit. 

Lemma 3.2 (Raw Alignment, Norm Inflation, and Norm Matching) . For the raw ZO shape Z in (6) ,

E [Z] = I, E

h

∥Zg∥2i

= κ ∥g∥2 , κ = q + d + 1 

q . (8) 

For the norm-matched shape P = κ−1/2Z,

E[Pg] = 

r qq + d + 1 g, E

h

∥Pg∥2i

= ∥g∥2 . (9) Lemma 3.2 separates the two calibration roles. The identity E[Z] = I says that raw ZO preserves the FO incoming signal in the zero-smoothing leading term. The inflation factor κ = ( q + d + 1) /q 

comes from the Wishart-type fourth moment E[Z2] = κI, so it is not a tunable scale choice. Because raw ZO inflates E[∥Zg∥2], it cannot be the fair quadratic-forgetting comparator. The 4comparator is instead P = κ−1/2Z, which matches the FO squared norm exactly but shrinks the mean to pq/ (q + d + 1) g. This asymmetry is unavoidable: for any random shaped direction x,

E

h

∥x∥2i

= ∥E[x]∥2 + E

h

∥x − E[x]∥2i

, so exact mean alignment and exact expected squared-norm matching force zero shaping variance. Scaled-FO and covariance-matched noise controls are therefore needed before attributing a retention change to curvature shaping. After this calibration, the curvature theory compares only two shape operators: 

P = I (FO) , P = κ−1/2Z (ZO) . (10) Here Z justifies the raw first-order signal, while P = κ−1/2Z is the norm-matched ZO shape used in the retention comparison. The finite-smoothing residual remains outside the zero-smoothing curvature mechanism and reappears as the additive perturbation in (19) when the mean gap is translated to a practical finite-difference batch. With raw alignment, norm inflation, norm-matched mean shrinkage, and expected squared norm separated, the zero-smoothing retention mechanism reduces to the expected shaped retention curvature defined in (11). 

## 4 Why ZO May Forget Less: Randomized Gradient Shaping 

The calibration in Sec. 3 leaves FO and norm-matched ZO on a common local scale: raw ZO carries the first-order signal, finite smoothing is separated as a remainder, and norm matching fixes the expected squared adaptation norm while making mean shrinkage explicit. The remaining zero-smoothing difference is the retention curvature exposed by the shaped adaptation. 

4.1 Expected Shaped Retention Curvature 

For a shaped adaptation −ηPg, the historical loss sees P⊤HP rather than H. The expectation of this induced curvature matrix is the central object: 

¯Hq ≜ E P⊤HP  , ¯λ ≜ tr( H)

d . (11) Here ¯λ is the average retention curvature, and q is the number of ZO directions used by the norm-matched shape in (10) . If the retention curvature were known and positive definite, a whitening-like reference P⋆ = √¯λ H−1/2O with O⊤O = I would realize P⊤ 

> ⋆

HP ⋆ = ¯λI. This full-information reference only names the isotropic target; the actual ZO shape has no access to the eigensystem of H.Finite-query ZO can now be evaluated by whether it moves the exposed retention curvature toward this isotropic target without knowing H.

Theorem 4.1 (Expected Curvature Identity for Norm-Matched ZO) . For the norm-matched ZO shape in (10) , let τ = d/ (q + d + 1) . For any fixed symmetric H,

¯Hq = (1 − τ )H + τ ¯λI. (12) Theorem 4.1 is the mechanism backbone. It says that norm-matched ZO does not uniformly shrink retention curvature. Instead, it leaves the isotropic component ¯λI unchanged and multiplies only the traceless anisotropic component H − ¯λI by 1 − τ . The standard normal direction distribution supplies this closed-form moment identity; no symmetry of the neural network, loss landscape, or optimizer state is assumed. The query count enters only through τ = d/ (q + d + 1) : smaller q gives stronger mixing toward isotropic curvature, while larger q approaches the FO curvature. 

Corollary 4.2 (Spectral Equalization) . Let λ1, . . . , λ d be the eigenvalues of H, and let λ′

> 1

, . . . , λ ′

> d

be the eigenvalues of E P⊤HP . For the norm-matched ZO shape in (10) and any fixed symmetric 

H,

λi − λ′ 

> i

= τ  λi − ¯λ , λ′ 

> i

= (1 − τ )λi + τ ¯λ, i = 1 , . . . , d. (13) 

Consequently, the mean eigenvalue is fixed and every eigen-deviation from ¯λ is contracted by the factor 1 − τ .

Corollary 4.2 gives the eigen-level interpretation of the same mechanism. Every eigenvalue sheds a τ

fraction of its signed deviation from the mean. Large retention-curvature eigenvalues move down, small ones move up, and the average is preserved, so the spectrum becomes less anisotropic without changing its trace. 54.2 Quantifying the FO–ZO Forgetting Reduction 

The expected-curvature identity in Theorem 4.1 explains how norm-matched ZO reshapes retention curvature. Its forgetting effect is the projection of that shaped curvature onto the fixed incoming gradient. Define the directional retention curvature 

λ ≜ g⊤Hg

∥g∥2 , ¯λ = tr( H)

d . (14) The FO baseline quadratic damage and the norm-matched ZO quadratic damage are 

QFO ≜ Q(−ηg) = η2

2 ∥g∥2 λ, QZO  

> q

≜ Q(−ηPg) = η2

2 g⊤P⊤HP g. (15) These quantities use the same incoming gradient before shaping and matched expected squared adaptation norm after shaping. Thus the comparison removes smaller-step explanations, while the explicit mean shrinkage in (9) is handled by scaled-FO controls. Any remaining mean forgetting reduction comes from the shaped retention curvature in Theorem 4.1. 

Theorem 4.3 (FO–ZO Forgetting-Reduction Identity) . For the norm-matched ZO shape in (10) , let 

τ = d/ (q + d + 1) . For any fixed symmetric H,

QFO − E QZO 

> q

 = η2

2 τ ∥g∥2  λ − ¯λ , E QZO 

> q

 = η2

2 ∥g∥2  (1 − τ )λ + τ ¯λ . (16) 

If λ > 0, the corresponding relative reduction is 

QFO − E QZO 

> q



QFO = τ



1 − ¯λλ



. (17) When H ⪰ 0 and Q is read as forgetting damage, Theorem 4.3 gives the promised quantitative comparison: norm-matched ZO reduces mean quadratic forgetting exactly when the current gradient points through above-average retention curvature, λ > ¯λ. The amount saved is (η2/2) τ ∥g∥2 (λ − ¯λ),and the fraction of FO damage removed is τ (1 − ¯λ/λ ) whenever λ > 0. Equivalently, ¯λ ∥g∥2 is the isotropic forgetting floor, while ∥g∥2 (λ − ¯λ) = g⊤(H − ¯λI)g is the signed directional excess above that floor; ZO shaping leaves the floor unchanged and attenuates only this excess by the factor 1 − τ .If λ < ¯λ, the same expression is negative and norm-matched ZO increases the mean local damage, so the mechanism controls high-curvature exposure by giving up the lucky case in which FO already moves through below-average retention curvature. The result is therefore a geometry-dependent risk-control statement, not a uniform dominance theorem. 

Practical finite-query ZO. Theorem 4.3 is the mean zero-smoothing gap. A practical finite-difference ZO batch under the same norm-matched calibration adds two separate perturbations. With 

bxμ ≜ κ−1/2 bgμ and bQZO  

> q,μ

≜ Q(−ηbxμ), the accounting is 

Gq ≜ QFO − E QZO 

> q

 = η2

2 τ ∥g∥2  λ − ¯λ , (18) 

QFO − bQZO  

> q,μ

= Gq −  QZO  

> q

− E QZO 

> q

 −

 bQZO  

> q,μ

− Q ZO 

> q



. (19) Thus the practical ZO gap has the same mechanism signal as Theorem 4.3. The two parentheses in (19) are the centered finite-query deviation and the finite-smoothing perturbation, respectively; the zero-smoothing case drops the last parenthesis. A realized benefit requires Gq to dominate both perturbations. Proposition B.1 and Corollary B.2 in Appendix B give the corresponding high-probability one-batch deviation bound and expected finite-smoothing damage perturbation. 

4.3 When Curvature Is Unknown: ZO Moves Toward the Blind-Optimal Isotropic Exposure 

The fixed-H results in Secs. 4.1–4.2 explain the actual FO–ZO forgetting gap for a given local retention geometry. A complementary scope question arises when curvature information is absent. Scalar norm matching fixes only the total squared norm; the full exposure matrix matters once unknown curvature can choose directions. The benchmark below is retention-only and acts on second 6A. FO: H = λavg I + R       

> worst-axis alignment B. ZO:  ̄ Hq=λavg I+ (1 − τ)R
> aligned mean, lower excess
> C. Blind: λavg I
> orientation-free damage

Figure 1: Effective-curvature view of curvature-agnostic ZO shaping. All panels use the same quadratic-damage levels; darker contours indicate larger damage, and the arrow marks the shared incoming direction g. The contours show the curvature read by the shaped adaptation: FO reads 

H = ¯λI + R, norm-matched ZO reads ¯Hq = E[P⊤HP ] = ¯λI + (1 − τ )R, and the blind endpoint reads ¯λI. ZO therefore keeps the mean direction aligned while contracting anisotropic curvature toward the isotropic benchmark. moments, not a joint plasticity–retention optimum under exact mean constraints. Let x = Pg,

∆ = −ηx, and M = E xx ⊤. Then 

E [Q(∆)] = 12 tr  H E ∆∆ ⊤ = η2

2 tr( HM ). (20) This blind benchmark intentionally ignores higher distributional details and compares only how a fixed norm budget is exposed to unknown curvature. To define the curvature-agnostic benchmark, compare exposures under a fixed trace scale for retention curvature: 

H¯λ ≜ H ⪰ 0 : tr( H) = d¯λ , M ≜

n

M ⪰ 0 : tr( M) = ∥g∥2o

. (21) Here ¯λ denotes the unknown average curvature scale shared by the benchmark class, and M is the norm-matched exposure budget E

h

∥x∥2i

= ∥g∥2. If H is otherwise unknown, the worst case places its curvature trace on the most exposed direction of M, so the safest target is the exposure with no preferred direction. 

Theorem 4.4 (Curvature-Agnostic Isotropic Benchmark) . Fix ¯λ > 0 and g̸ = 0. For any M ∈ M ,let λmax (M) denote its largest eigenvalue. Then 

sup 

> H∈H ¯λ

η2

2 tr( HM ) = η2

2 d¯λ λ max (M), inf  

> M∈M

sup 

> H∈H ¯λ

η2

2 tr( HM ) = η2

2 ¯λ ∥g∥2 . (22) 

The unique second moment attaining the second value is M⋆ = ( ∥g∥2 /d )I.

Theorem 4.4 identifies the retention-only endpoint. Under no curvature information, the minimax exposure is isotropic. This target benchmarks exposure after the incoming direction has been fixed; it does not prescribe a new plasticity objective. Norm-matched ZO then sits between FO’s rank-one exposure and this isotropic endpoint. 

Corollary 4.5 (Norm-Matched ZO Exposure Interpolation) . Let τ = d/ (q + d + 1) , MFO ≜ gg ⊤,and M⋆ = ( ∥g∥2 /d )I. For the norm-matched ZO shape in (10) ,

MZO  

> q

≜ E (Pg)( Pg)⊤ = (1 − τ )gg ⊤ + τ ∥g∥2

d I = (1 − τ )MFO + τ M⋆. (23) 

Moreover, with R¯λ(M) ≜ sup H∈H ¯λ 

> η2
> 2

tr( HM ),

R¯λ

 MZO 

> q

 − R ¯λ(M⋆) = (1 − τ ) R¯λ

 MFO  − R ¯λ(M⋆) . (24) Corollary 4.5 supplies the curvature-agnostic interpretation. FO has rank-one exposure, the blind-optimal benchmark is isotropic, and norm-matched ZO closes exactly a τ -fraction of the FO-to-optimum exposure gap, leaving 1 − τ = ( q + 1) /(q + d + 1) . This benchmark explains why randomized shaping is principled when H is unknown, but it does not replace the fixed-H forgetting identity in Theorem 4.3, which governs the actual FO–ZO gap for a given retention geometry. Figure 1 visualizes the same mechanism in the equivalent shaped-curvature contour view. 7Algorithm 1: RISE ( Retention-Aware Isotropic Shaping for Exact-Gradient Adaptation )

Input: Exact gradient g, block partition {gb}Bb=1 , directions per block q, base optimizer O PT  

> 1

for each block b = 1 , . . . , B do  

> 2

Draw zb, 1, . . . , zb,q 

> i.i.d.

∼ N (0, Ib) 

> 3

Zb ← q−1 Pqi=1 zb,i z⊤ 

> b,i

and κb ← (q + db + 1) /q  

> 4

egb ← κ−1/2 

> b

Zbgb 

> 5

Concatenate eg ← (eg1, . . . , egB ) 

> 6

Pass eg to O PT in place of g

## 5 From Theory to RISE: Exact-Gradient Randomized Shaping 

The theory isolates the transferable part of low-query ZO: after the calibration in Lemmas 3.1 and 3.2, the retention-relevant operation is a randomized shape applied to a fixed incoming direction. RISE ( Retention-Aware Isotropic Shaping for Exact-Gradient Adaptation ) transfers this operation to exact-gradient adaptation. It computes the incoming FO gradient g, replaces it by a calibrated shaped vector eg, and lets the same base optimizer execute the adaptation. Thus RISE sits between FO and practical finite-difference ZO. Relative to FO, it changes second-moment exposure as in (23) under a matched expected squared norm. Relative to finite-difference ZO, it keeps the exact incoming signal, removes finite-μ bias from (7) , and replaces raw norm inflation with explicit norm matching from (9) .RISE uses the same shape blockwise because global low-query shaping has dimension-dependent deviation (Proposition B.1). For blocks of size db, q directions per block give Bq local shaping samples after one backward pass and no function-value queries. The block theory keeps cross-block curvature in the mean term and treats omitted couplings as a perturbation when small (Theorem C.3; Proposition C.4). Thus RISE acts as a stability–plasticity wrapper, importing ZO-style curvature mixing into exact-gradient adaptation at a norm-matched mean-shrinkage cost relative to FO. For a block partition g = ( g1, . . . , gB ), draw zb, 1, . . . , zb,q 

> i.i.d.

∼ N (0, Ib) and define, for each b,

Zb ≜ 1

q

> q

X

> i=1

zb,i z⊤

> b,i

, κb ≜ q + db + 1 

q , Pb ≜ κ−1/2 

> b

Zb, egb ≜ Pbgb. (25) By Lemma 3.2, each block is raw-aligned before normalization and norm-matched after normalization: 

E[Zbgb] = gb, E[∥Pbgb∥2] = ∥gb∥2, and E[Pbgb] = κ−1/2 

> b

gb. The shaped blocks replace the raw gradient inside the same FO optimizer. Blocks may be tensors, layers, or optimizer groups; Appendix C defines the blockwise scores used to interpret ablations. 

## 6 Experiments 

The main experiments test whether the local shaping mechanism survives inside continual-learning pipelines. Prior forward-pass and ZO work shows that ZO can reduce forgetting in ZeroFlow [ 6], forward-only continual adaptation [ 3 ], and ZO-FC [ 30 ]; here the question is whether the retention-relevant ZO shape can be applied to exact gradients without finite-difference plasticity loss. We evaluate ViT-B/16 on CIFAR100 [ 15 ], ImageNet-R [ 9], an ImageNet-derived rendition benchmark [ 23 ], and DomainNet [ 21 ], and T5 on task orders from the text-classification CL benchmark [ 31 ], GLUE [ 27 ], and SuperGLUE [ 28 ]. Metrics are Avg, Last, and Fgt; full settings and quadratic-sandbox evidence are in Appendix D. 

Vision results. In vision, RISE keeps the ZO-FC classifier/head design and replaces representation-side finite-difference ZO with exact-gradient norm-matched shaping. Table 1 shows that ZO-FC+RISE improves Last over ZO-FC by +1 .39 , +0 .94 , and +0 .39 points on CIFAR100, ImageNet-R, and DomainNet, while reducing Fgt by 1.01 , 1.72 , and 0.28 points. Higher Last indicates that ex-act gradients recover plasticity lost to gradient estimation; lower Fgt at comparable or higher Avg indicates that the ZO-derived shape still reduces retention exposure. Strong FO baselines and low-forgetting but low-plasticity ZO rows rule out one-axis dominance; the gain is the paired balance over the closest ZO-FC pipeline. 8Table 1: Vision continual-learning results (%). 

CIFAR100 Inc-5 ImageNet-R Inc-5 DomainNet Inc-69 Method avg ↑ last ↑ fgt ↓ avg ↑ last ↑ fgt ↓ avg ↑ last ↑ fgt ↓

FO Optimization 

Learnable Cls. [10] 85.61 78.33 5.17 60.60 52.12 3.98 75.85 69.88 3.70 

Adapter + Cls. [30] 88.41 83.64 9.58 74.71 69.62 8.49 78.67 72.85 4.64 APER Adapter [34] 87.90 81.68 6.01 71.52 63.42 7.48 71.24 64.93 6.72 EASE [33] 91.13 85.45 7.05 78.78 71.93 7.54 75.91 70.24 8.54 

ZO Optimization 

Learnable Cls. 62.85 43.55 3.99 25.60 17.08 9.18 52.70 46.21 7.37 Adapter + Cls. 79.45 70.46 8.72 34.87 27.33 6.36 48.24 44.29 4.56 APER Adapter 87.70 81.49 6.07 62.60 54.50 8.55 62.45 56.67 6.89 EASE 87.32 81.07 6.87 62.65 54.47 9.84 63.10 56.80 7.22 ZO-FC [30] 88.39 83.34 5.26 72.01 66.63 4.40 77.17 71.05 4.19 ZO-FC+RISE 89.75 84.73 4.25 72.05 67.57 2.68 77.57 71.44 3.91 Table 2: Additional language-model continual-learning results (%). Values report the mean with standard deviation shown after ± over repeated runs under the same task order. 

Standard CL Benchmark Order 1 Standard CL Benchmark Order 2 Large Number of Tasks Order 3 Method Type Avg ↑ Last ↑ Fgt ↓ Avg ↑ Last ↑ Fgt ↓ Avg ↑ Last ↑ Fgt ↓

SeqLoRA 

> [11]

FO 53.33 ±1.16 89.99 ±0.40 35.57 ±1.50 52.13 ±4.89 73.23 ±0.61 37.01 ±6.69 27.62 ±11 .26 52.43 ±3.81 50.62 ±12 .43 

ZO 46.24 ±3.02 62.05 ±5.49 3.67 ±0.62 42.07 ±3.62 57.51 ±1.31 7.36 ±1.31 25.60 ±8.24 53.61 ±10 .77 15.83 ±6.13 

RISE 74.85 ±1.95 89.46 ±0.09 7.01 ±2.21 75.15 ±0.67 73.16 ±0.23 6.58 ±1.45 62.65 ±5.13 56.66 ±1.42 17.77 ±5.16 

O-LoRA 

> [29]

FO 77.19 ±0.28 89.04 ±0.47 2.83 ±0.02 76.61 ±0.62 71.92 ±0.19 2.37 ±0.32 72.72 ±1.61 54.44 ±2.09 3.80 ±1.44 

ZO 62.42 ±1.47 61.70 ±3.18 0.98 ±1.16 60.38 ±1.32 61.72 ±0.86 2.09 ±2.59 56.48 ±2.61 52.19 ±6.54 3.76 ±2.57 

RISE 77.24 ±0.30 88.81 ±0.37 1.43 ±0.37 76.90 ±0.18 71.85 ±0.26 1.87 ±0.05 74.06 ±0.39 54.55 ±1.27 3.16 ±0.48 

Language-model transfer. Table 2 reports language transfer on SeqLoRA and O-LoRA. On SeqLoRA, RISE keeps Last close to FO while cutting FO forgetting from 35 .57 to 7.01 , from 37 .01 

to 6.58 , and from 50 .62 to 17 .77 . O-LoRA starts from a lower-forgetting FO baseline, yet RISE still lowers Fgt on all orders while maintaining or improving Avg. The contrast matches the mechanism: when FO forgets heavily, the shaped exact gradient can reduce retention exposure without losing the final-task signal; when FO is already stable, gains are smaller but consistent because the exact gradient carries the incoming signal while randomized shape changes its retention exposure. Together, the domains support the same conditional pattern: randomized shaping helps when it lowers shape-sensitive retention exposure without collapsing the incoming plasticity signal, and has less room when the base method already forgets little. The raw-ZO rows show why exact-gradient transfer matters: low forgetting can coexist with poor Last, so retention shape must be separated from finite-difference plasticity loss before attributing gains to ZO-style curvature mixing. 

## 7 Conclusion 

Low-query ZO retention is calibrated randomized shaping: raw ZO supplies the mean-aligned signal, norm matching fixes squared norm with explicit mean shrinkage, and the FO–ZO damage gap becomes a query-dependent fraction of directional curvature excess. RISE transfers this shape to exact gradients as a blockwise wrapper, yielding a local second-moment stability–plasticity tradeoff whose global trajectory and selection questions are handled by the appendix controls and blockwise analyses. 9References 

[1] Ang Bian, Wei Li, Hangjie Yuan, Chengrong Yu, Mang Wang, Zixiang Zhao, Aojun Lu, Pengliang Ji, and Tao Feng. Make continual learning stronger via C-Flat. In Proc. NeurIPS ,2024. [2] Arslan Chaudhry, Marc’Aurelio Ranzato, Marcus Rohrbach, and Mohamed Elhoseiny. Efficient lifelong learning with A-GEM. In Proc. ICLR , 2019. [3] Jiao Chen, Jiayi He, Fangfang Chen, Zuohong Lv, and Jianhua Tang. Forward-only continual learning. In Proc. ACM MM , 2025. [4] Yiming Chen, Yuan Zhang, Liyuan Cao, Kun Yuan, and Zaiwen Wen. Enhancing zeroth-order fine-tuning for language models with low-rank structures. In Proc. ICLR , 2025. [5] Mehrdad Farajtabar, Navid Azizan, Alex Mott, and Ang Li. Orthogonal gradient descent for continual learning. In Proc. AISTATS , 2020. [6] Tao Feng, Wei Li, Didi Zhu, Hangjie Yuan, Wendi Zheng, Dan Zhang, and Jie Tang. ZeroFlow: Overcoming catastrophic forgetting is easier than you think. In Proc. ICML , 2025. [7] Abraham D. Flaxman, Adam Tauman Kalai, and H. Brendan McMahan. Online convex optimization in the bandit setting: Gradient descent without a gradient. In Proc. SODA , 2005. [8] Pierre Foret, Ariel Kleiner, Hossein Mobahi, and Behnam Neyshabur. Sharpness-aware mini-mization for efficiently improving generalization. In Proc. ICLR , 2021. [9] Dan Hendrycks, Steven Basart, Norman Mu, Saurav Kadavath, Frank Wang, Evan Dorundo, Rahul Desai, Tyler Zhu, Samyak Parajuli, Mike Guo, Dawn Song, Jacob Steinhardt, and Justin Gilmer. The many faces of robustness: A critical analysis of out-of-distribution generalization. In Proc. ICCV , 2021. [10] Saihui Hou, Xinyu Pan, Chen Change Loy, Zilei Wang, and Dahua Lin. Learning a unified classifier incrementally via rebalancing. In Proc. CVPR , 2019. [11] Edward J. Hu, Yelong Shen, Phillip Wallis, Zeyuan Allen-Zhu, Yuanzhi Li, Shean Wang, Lu Wang, and Weizhu Chen. LoRA: Low-rank adaptation of large language models. In Proc. ICLR , 2022. [12] Zhongzhan Huang, Mingfu Liang, Senwei Liang, and Wei He. AlterSGD: Finding flat minima for continual learning by alternative training. arXiv:2107.05804, arXiv, 2021. [13] James Kirkpatrick, Razvan Pascanu, Neil Rabinowitz, Joel Veness, Guillaume Desjardins, An-drei A. Rusu, Kieran Milan, John Quan, Tiago Ramalho, Agnieszka Grabska-Barwinska, Demis Hassabis, Claudia Clopath, Dharshan Kumaran, and Raia Hadsell. Overcoming catastrophic forgetting in neural networks. Proc. Natl. Acad. Sci. U.S.A. , 2017. [14] Yajing Kong, Liu Liu, Huanhuan Chen, Janusz Kacprzyk, and Dacheng Tao. Overcoming catastrophic forgetting in continual learning by exploring eigenvalues of Hessian matrix. IEEE Trans. Neural Netw. Learn. Syst. , 2024. [15] Alex Krizhevsky and Geoffrey Hinton. Learning multiple layers of features from tiny images. Technical report, University of Toronto, 2009. [16] Zhizhong Li and Derek Hoiem. Learning without forgetting. In Proc. ECCV , 2016. [17] Yong Liu, Zirui Zhu, Chaoyu Gong, Minhao Cheng, Cho-Jui Hsieh, and Yang You. Sparse MeZO: Less parameters for better performance in zeroth-order LLM fine-tuning. In Proc. NeurIPS , 2025. [18] David Lopez-Paz and Marc’Aurelio Ranzato. Gradient episodic memory for continual learning. In Proc. NeurIPS , 2017. [19] Sadhika Malladi, Tianyu Gao, Eshaan Nichani, Alex Damian, Jason D. Lee, Danqi Chen, and Sanjeev Arora. Fine-tuning language models with just forward passes. In Proc. NeurIPS , 2023. 10 [20] Yurii Nesterov and Vladimir Spokoiny. Random gradient-free minimization of convex functions. 

Found. Comput. Math. , 2017. [21] Xingchao Peng, Qinxun Bai, Xide Xia, Zijun Huang, Kate Saenko, and Bo Wang. Moment matching for multi-source domain adaptation. In Proc. ICCV , 2019. [22] Sylvestre-Alvise Rebuffi, Alexander Kolesnikov, Georg Sperl, and Christoph H. Lampert. iCaRL: Incremental classifier and representation learning. In Proc. CVPR , 2017. [23] Olga Russakovsky, Jia Deng, Hao Su, Jonathan Krause, Sanjeev Satheesh, Sean Ma, Zhiheng Huang, Andrej Karpathy, Aditya Khosla, Michael Bernstein, Alexander C. Berg, and Fei-Fei Li. ImageNet large scale visual recognition challenge. Int. J. Comput. Vis. , 2015. [24] Gobinda Saha, Isha Garg, and Kaushik Roy. Gradient projection memory for continual learning. In Proc. ICLR , 2021. [25] Keisuke Sugiura and Hiroki Matsutani. ElasticZO: A memory-efficient on-device learning with combined zeroth- and first-order optimization. arXiv:2501.04287, arXiv, 2025. [26] Qitao Tan, Jun Liu, Zheng Zhan, Caiwei Ding, Yanzhi Wang, Xiaolong Ma, Jaewoo Lee, Jin Lu, and Geng Yuan. Harmony in divergence: Towards fast, accurate, and memory-efficient zeroth-order LLM fine-tuning. arXiv:2502.03304, arXiv, 2025. [27] Alex Wang, Amanpreet Singh, Julian Michael, Felix Hill, Omer Levy, and Samuel R. Bowman. GLUE: A multi-task benchmark and analysis platform for natural language understanding. In 

Proc. EMNLP Workshop BlackboxNLP , 2018. [28] Alex Wang, Yada Pruksachatkun, Nikita Nangia, Amanpreet Singh, Julian Michael, Felix Hill, Omer Levy, and Samuel R. Bowman. SuperGLUE: A stickier benchmark for general-purpose language understanding systems. In Proc. NeurIPS , 2019. [29] Xiao Wang, Tianze Chen, Qiming Ge, Han Xia, Rong Bao, Rui Zheng, Qi Zhang, Tao Gui, and Xuan-Jing Huang. Orthogonal subspace learning for language model continual learning. In 

Findings of EMNLP , 2023. [30] Wanhao Yu, Zheng Wang, Shuteng Niu, Sen Lin, and Li Yang. More than memory savings: Zeroth-order optimization mitigates forgetting in continual learning. arXiv:2510.21019, arXiv, 2025. [31] Xiang Zhang, Junbo Zhao, and Yann LeCun. Character-level convolutional networks for text classification. In Proc. NeurIPS , 2015. [32] Yihua Zhang, Pingzhi Li, Junyuan Hong, Jiaxiang Li, Yimeng Zhang, Wenqing Zheng, Pin-Yu Chen, Jason D. Lee, Wotao Yin, Mingyi Hong, Zhangyang Wang, Sijia Liu, and Tianlong Chen. Revisiting zeroth-order optimization for memory-efficient LLM fine-tuning: A benchmark. In 

Proc. ICML , 2024. [33] Da-Wei Zhou, Hai-Long Sun, Han-Jia Ye, and De-Chuan Zhan. Expandable subspace ensemble for pre-trained model-based class-incremental learning. In Proc. CVPR , 2024. [34] Da-Wei Zhou, Zi-Wen Cai, Han-Jia Ye, De-Chuan Zhan, and Ziwei Liu. Revisiting class-incremental learning with pre-trained models: Generalizability and adaptivity are all you need. 

Int. J. Comput. Vis. , 2025. 

## A Related Work and Positioning 

Continual Learning and Retention Geometry. Continual-learning methods have long controlled forgetting by restricting how a new-task adaptation may move the model. Learning without Forgetting preserves old behavior through distillation [ 16 ]; EWC penalizes movement in Fisher-important parameters [ 13 ]; iCaRL stores exemplars for replay [ 22 ]; GEM and A-GEM impose past-gradient constraints [ 2, 18 ]; and OGD and GPM project updates away from past-task subspaces [ 5 , 24 ]. More recent geometric work makes the same stability concern curvature-aware: AlterSGD searches 11 for flat continual-learning minima [ 12 ], SAM penalizes sharp solutions [ 8], C-Flat adapts flatness control to continual learning [ 1], and Hessian-eigenvalue analysis links forgetting to high-curvature directions [ 14 ]. These methods use explicit retention information to change the objective, gradient, or training rule. Our analysis isolates a smaller local object that exists before such retention channels are added: once the incoming-loss direction is fixed, historical loss only sees the induced adaptation displacement and the retention curvature exposed by that displacement. 

Pretrained-Model and PEFT Continual Adaptation. The empirical baselines in this paper sit on a newer line that adapts pretrained representations rather than training continual learners from scratch. Unified classifier rebalancing is an early class-incremental classifier baseline [ 10 ]. LoRA supplies the low-rank adapter parameterization used by many language-model continual-learning pipelines [ 11 ], while O-LoRA adds orthogonal subspace constraints for language-model continual learning [29 ]. For vision continual learning with pretrained models, EASE expands subspace ensembles [ 33 ], and APER emphasizes adaptive pretrained-model class-incremental learning [ 34 ]. These methods define strong host settings and baseline rows, but they do not by themselves explain why a ZO-shaped adaptation should expose less retention curvature than an ordinary FO displacement under the same norm budget. 

Zeroth-Order Optimization and Forward-Pass Adaptation. Classical ZO optimization estimates descent directions from function values, from bandit finite differences [ 7] to randomized gradient-free smoothing [ 20 ]. In modern language-model fine-tuning, MeZO shows that large models can be adapted with forward passes alone [ 19 ], and the ZO-LLM benchmark broadens this evidence across optimizers, model families, and tuning schemes [ 32 ]. Subsequent variants reduce the estimation burden by selecting sparse parameter subsets [ 17 ], using low-rank ZO structures [ 4 ], adapting layerwise ZO update scales [ 26 ], or combining ZO and FO optimization [ 25 ]. These works establish the feasibility and systems value of forward-pass fine-tuning. Their main object, however, is still gradient estimation or memory-efficient optimization, not the retention curvature exposed after an incoming gradient has already been fixed. 

Forward-Pass and ZO Continual Learning. The closest empirical continual-learning work moves from memory-efficient ZO to forgetting itself. ZeroFlow benchmarks gradient-free algorithms for continual learning and shows that forward passes can be sufficient in several forgetting settings [ 6]; Forward-Only Continual Learning explores gradient-free adaptation for pretrained models [ 3]; and the ZO-FC study shows that ZO optimization can reduce forgetting beyond memory savings, identifies a stability–plasticity tradeoff, and optimizes an adapter with ZO while keeping the classifier/head under FO gradients [ 30 ]. These papers establish the phenomenon that motivates this work. The open mechanistic question is what remains after finite-smoothing error, raw-ZO mean alignment, raw norm inflation, norm-matched mean shrinkage, and expected squared adaptation norm are separated. We answer that question with a direct norm-matched FO–ZO forgetting-gap identity, and RISE then transfers the same shape mechanism to exact gradients. The remaining appendix gives the finite-query account (Appendix B), the blockwise RISE analysis (Appendix C), the empirical settings and controls (Appendix D), and the proofs (Appendix E). 

## B Practical Finite-Query ZO Gap 

The mean zero-smoothing mechanism leaves one implementation question: whether a single finite-difference batch realizes the same sign and scale. The resulting realization-level account keeps the two perturbations separate. Finite-query randomness is a centered sampling deviation around the mean gap, while finite smoothing is an estimator remainder that perturbs the quadratic damage. 

Proposition B.1 (One-Batch Deviation around the Mean Gap) . Let Z = q−1 Pqr=1 zr z⊤ 

> r

with 

zr

> i.i.d.

∼ N (0, I), P = κ−1/2Z, κ = ( q + d + 1) /q , and τ = d/ (q + d + 1) . Assume H ⪰ 0 and let 

λmax denote its largest eigenvalue. Let Gq = ( η2/2) τ ∥g∥2 (λ − ¯λ) be the mean forgetting-reduction signal in (18) , where λ = g⊤Hg/ ∥g∥2 and ¯λ = tr( H)/d . For δ ∈ (0 , 1) , define 

ψq (δ) ≜

s

d + log(1 /δ )

q + d + log(1 /δ ) + 1 

q . (26) 12 There is a universal constant C > 0 such that, with probability at least 1 − δ,

 QFO − Q ZO 

> q

 − Gq = QZO  

> q

− E QZO 

> q

 ≤ C η2

2 ∥g∥2 λmax 

 ψq (δ) + ψq (δ)2 . (27) Proposition B.1 turns a sampled ZO batch into a signal–deviation comparison. The deterministic signal is Gq , while the right-hand side of (27) is the local sample-covariance scale multiplied by the largest retention curvature. A positive mean benefit is visible in one batch only when the signal dominates this deviation; a negative mean gap is visible by the same logic. The query count therefore has two roles: smaller q increases the mean mixing weight τ , but also increases the realization scale 

ψq (δ).

Corollary B.2 (Finite-Smoothing Perturbation to the Realized Gap) . Assume the conditions of Lemma 3.1. Also assume H ⪰ 0 and let λmax be its largest eigenvalue. Define the norm-matched finite-difference direction bxμ ≜ κ−1/2 bgμ and its quadratic damage bQZO  

> q,μ

≜ Q(−ηbxμ). Then the realized-gap accounting in (19) holds. Moreover, the same finite-smoothing residual controls both the damage perturbation and the small mismatch from exact zero-smoothing norm matching. For a universal constant C > 0,

E

h bQZO  

> q,μ

− Q ZO 

> q

i

≤ C η2

2 λmax 

h

κ−1/2Lμd 3/2 ∥g∥ + κ−1L2μ2d3i

. (28) 

The expected squared norm obeys 

E

h

∥bxμ∥2i

− ∥ g∥2 ≤ C

h

κ−1/2Lμd 3/2 ∥g∥ + κ−1L2μ2d3i

. (29) The finite-difference residual enters the quadratic damage in two ways: through a cross term with the zero-smoothing shaped direction and through the residual quadratic term. The expected-norm bound in Lemma 3.1 justifies the estimator-level reduction, while the second-moment bound E[∥rμ∥2]

controls the damage-level perturbation through Cauchy–Schwarz and the residual quadratic term. Thus (19) realizes the main-text mean mechanism when Gq dominates both the finite-query deviation and the finite-smoothing perturbation at the relevant scale. Equation (29) also makes explicit that norm matching is exact for the zero-smoothing shape and perturbed, at the same order, for a finite-difference batch. 

Corollary B.3 (Realized Gap Sign Certificate) . Under the conditions of Proposition B.1 and Corol-lary B.2, define 

Bq (δ) ≜ C η2

2 ∥g∥2 λmax 

 ψq (δ) + ψq (δ)2 , (30) 

Bμ ≜ C η2

2 λmax 

h

κ−1/2Lμd 3/2 ∥g∥ + κ−1L2μ2d3i

, (31) 

where C is a universal constant large enough for the two preceding results. For any δq , δ μ ∈ (0 , 1) ,with probability at least 1 − δq − δμ,



QFO − bQZO 

> q,μ



− Gq ≤ Bq (δq ) + Bμ

δμ

. (32) 

Consequently, if |Gq | > B q (δq ) + Bμ/δ μ, the practical realized gap has the same sign as the mean signal Gq . In the zero-smoothing setting, set Bμ = 0 .

If the two function values are observed with additive zero-mean scalar noise, the estimator gains an additional residual. For independent noises with conditional variance at most σ2 

> f

per function value, the residual nμ = q−1 Pqr=1 (( ξr, + − ξr, −)/(2 μ)) zr satisfies E[∥nμ∥2] ≤ σ2 

> f

d/ (2 μ2q). The same perturbation calculation applies after replacing rμ by rμ + nμ. This is the reason practical ZO experiments either use common mini-batches/common random numbers for the two function values or report the additional variance induced by stochastic function-value observations. 

## C Blockwise Support Theory for RISE 

RISE applies the calibrated ZO shape independently inside parameter blocks, so the global identity from Sec. 4 is localized rather than reused verbatim. The blockwise support does not claim that 13 vanilla ZO is intrinsically blockwise. A full-H oracle could exploit intra-block and inter-block curvature simultaneously; a blockwise informed rule would keep the within-block matrices Hbb 

and ignore cross-block couplings. RISE uses weaker information by shaping natural parameter blocks independently. The local calculation identifies how much of the global identity survives this localization and which term records the cost of ignoring cross-block curvature. Partition the parameter into B blocks and write the retention curvature in blocks Hbc . Let block b

have dimension db and identity Ib. For each block, draw zb, 1, . . . , zb,q b

> i.i.d.

∼ N (0, Ib) independently across blocks and define 

Zb ≜ 1

qbqbX

> i=1

zb,i z⊤

> b,i

, κb ≜ qb + db + 1 

qb

, Pb ≜ κ−1/2 

> b

Zb. (33) Let Pblk ≜ blkdiag( P1, . . . , PB ), ab ≜ κ−1/2 

> b

, and τb ≜ db/(qb + db + 1) .

Theorem C.1 (Blockwise Expected Curvature and Gap for RISE) . For the blockwise RISE shape Pblk 

in (33) , any fixed symmetric retention curvature H satisfies 

 E P⊤

> blk

HP blk 

  

> bb

= (1 − τb)Hbb + τb ¯λbIb,  E P⊤

> blk

HP blk 

  

> bc

= abacHbc (b̸ = c), (34) 

where ¯λb ≜ tr( Hbb )/d b. Consequently, with QRISE blk ≜ Q(−ηPblk g),

QFO − E QRISE blk 

 = η2

2

" BX

> b=1

τb g⊤ 

> b

(Hbb − ¯λbIb)gb + 2 X

> b<c

(1 − abac)g⊤ 

> b

Hbc gc

#

. (35) Theorem C.1 is the formal connection between the global mechanism and the blockwise wrapper. If 

H is block diagonal, the gap is exactly the sum of the global FO–ZO identity applied inside each block. If cross-block curvature is present, the second term in (35) is the explicit price of blockwise localization: it may help or hurt depending on the signed cross-block interaction g⊤ 

> b

Hbc gc. This is why the blockwise evaluation uses the blockwise mean-matched FO control Ag and the covariance-matched control in (46) ; otherwise shrinkage of cross-block mean terms could be mistaken for a shaping effect. 

Proposition C.2 (Blockwise One-Batch Deviation) . Assume the setting of Theorem C.1. For δb ∈

(0 , 1) define 

ψb(δb) ≜

s

db + log(1 /δ b)

qb

+ db + log(1 /δ b) + 1 

qb

. (36) 

There is a universal constant C > 0 such that, with probability at least 1 − PBb=1 δb,

QRISE blk − E QRISE blk 

 ≤ η2

2

> B

X

> b=1

C ∥gb∥2 ∥Hbb ∥op 

 ψb(δb) + ψb(δb)2

+ η2 X

> b<c

abac ∥gb∥ ∥ gc∥ ∥ Hbc ∥op (ψb(δb) + ψc(δc) + ψb(δb)ψc(δc)) . (37) Proposition C.2 explains the finite-query advantage and its limitation. The within-block sampling scale depends on db, not the full ambient dimension, so moderate blocks can make the mean-curvature signal easier to realize with low qb. The cross-block term does not vanish automatically; it is controlled by the off-diagonal curvature and by the shaped block norms. Thus blockwise shaping is a stable realization of the global mechanism only when the within-block anisotropic signal is large relative to both within-block sampling fluctuation and cross-block coupling. The same blockwise object can be rewritten as a mean adaptation plus centered random residual. This decomposition is useful for interpretation because it separates deterministic mean-step damage from the expected cost of randomized exposure. 

Theorem C.3 (Expected Forgetting Decomposition for Blockwise Randomized Shaping) . Under the local quadratic forgetting model in (3) , let a randomized adaptation decompose as ∆ = m + ϵ,where m = E[∆] and ϵ = ( ϵ1, . . . , ϵB ) is partitioned by blocks. Assume E[ϵb] = 0, E[ϵbϵ⊤ 

> c

] = 0

for b̸ = c, and E[ϵbϵ⊤ 

> b

] = Σb. With Σ = blkdiag( Σ1, . . . , ΣB ),

E [Q(∆)] = 12 m⊤Hm + 12 tr( HΣ ) = 12 m⊤Hm + 12

> B

X

> b=1

tr( Hbb Σb). (38) 14 Theorem C.3 is the expectation identity that matches the blockwise wrapper. The first term is the damage of the mean adaptation and may involve the full curvature H, including cross-block couplings. This term is why the empirical setup includes mean-scaled FO controls. After the mean adaptation is controlled, the expected damage introduced by the centered blockwise random residual depends only on Hbb and the block covariances Σb. Cross-block curvature is not irrelevant; it enters the stability of one sampled realization. For a zero-mean blockwise residual, 

Q(ϵ) − E [Q(ϵ)] = 12

> B

X

> b=1

 ϵ⊤ 

> b

Hbb ϵb − tr( Hbb Σb) + X

> b<c

ϵ⊤ 

> b

Hbc ϵc. (39) Thus the mean randomized damage is block-diagonal, while inter-block curvature controls run-to-run fluctuation and the reliability of a single sampled shaped adaptation. The same decomposition gives a trace-level measure when a block is shaped isotropically. If 

Σb = αbIb, then 

E [Q(ϵ)] = 12

> B

X

> b=1

αb tr( Hbb ), Db ≜ 1

db

tr( Hbb ). (40) The damage density Db is the mean forgetting cost per unit isotropic randomized exposure in block b.It is milder than a largest-eigenvalue score and matches the average behavior of randomized shaping. Directional block geometry gives a complementary score. For blocks with positive definite or damped curvature, define 

Rb(ρ) ≜ g⊤ 

> b

(Hbb + ρIb)−1gb, ρ ≥ 0. (41) Large Rb(ρ) means the incoming signal in block b can be carried through directions that are flat for the historical loss. A block is therefore forgetting-resistant not merely because Db is small, but because it combines low average retention curvature, signal alignment with flat directions, and weak coupling to the rest of the network. A shape-aware coupling score is 

Cb ≜ X

> c̸=b

Σ1/2 

> b

Hbc Σ1/2  

> cF

, (42) with the raw alternative P 

> c̸=b

∥Hbc ∥F when the block covariance is not yet specified. Blocks with modest Db but large Cb can look safe on average while showing unstable realized forgetting; these are fragile bridge blocks rather than uniformly robust layers. The distance from a full-H oracle is controlled by how large the omitted couplings are relative to the block diagonal. Let Hblk ≜ blkdiag( H11 , . . . , HBB ), E ≜ H − Hblk , and 

ϵblk ≜ H−1/2 

> blk

EH −1/2 

> blk op

. (43) 

Proposition C.4 (Block-Coupling Perturbation) . Let Hblk = blkdiag( H11 , . . . , HBB ), E = H −

Hblk , and ϵblk = H−1/2 

> blk

EH −1/2 

> blk op

. If Hblk ≻ 0 and ϵblk < 1, then 

(1 + ϵblk )−1H−1 

> blk

⪯ H−1 ⪯ (1 − ϵblk )−1H−1 

> blk

. (44) Proposition C.4 gives the intended status of the blockwise relaxation. When inter-block coupling is weak in the normalized sense of (43) , blockwise curvature information is close to the full-H oracle up to the displayed multiplicative factors. When the coupling is large, the blockwise wrapper is not invalid; it gives up coordinated use of off-diagonal curvature in exchange for a cheaper and more stable randomization rule. Finally, the standard norm-matched ZO forgetting-gap identity can still be read inside each block as a local shaping score, 

SRISE  

> b

≜ η2

2 τb ∥gb∥2  λb − ¯λb

 , λb ≜ g⊤ 

> b

Hbb gb

∥gb∥2 , ¯λb ≜ tr( Hbb )

db

, τb ≜ db

qb + db + 1 . (45) This score is not a pointwise decomposition of the full quadratic form. It is a block-local prediction of where the RISE wrapper has positive within-block anisotropic excess to attenuate after the mean step and scale have been controlled. These quantities have distinct roles: SRISE  

> b

tracks the within-block anisotropic benefit predicted by Theorem C.1; Db tracks the average cost of isotropic randomized exposure; Cb tracks sensitivity to omitted cross-block coupling and one-batch instability; and Rb(ρ) is an oracle-style flat-signal proxy for whether the incoming block signal lies in low-retention directions. None of these quantities is an unconditional selection threshold on its own. Their selection value is assessed through the completed query-count sweep and by additional scale controls when those controls are reported, as discussed in Appendix D.3. 15 D Extended Experimental Details 

The main experiments report compact transfer results, so the appendix separates the evidence roles. The quadratic-sandbox study measures theorem-level quantities directly; the end-to-end sections specify the visual and language continual-learning settings; and unreported controls are described as supporting analyses rather than evidence. 

D.1 Evidence Roles, Controls, and Settings 

Theory-facing experiments report operator residuals for E P⊤HP  against the prediction in (12) ,the empirical damage gap QFO − E QZO 

> q

 against (16) , centered finite-query deviations QZO  

> q

−

E QZO 

> q

, and the correlation between blockwise scores and observed RISE benefit. Algorithm-facing experiments report final average accuracy, average forgetting, new-task plasticity, backward transfer when applicable, training-time overhead, and additional memory overhead. This separation prevents final accuracy from becoming a proxy for theorem validation. Mechanism controls include FO, scaled FO with the same mean-step shrinkage induced by norm matching, FO plus isotropic Gaussian noise, FO plus covariance-matched additive noise, global exact-gradient shaping, and raw low-query ZO. For blockwise RISE, the mean-shrinkage control is blockwise rather than global: with ab ≜ pqb/(qb + db + 1) and A ≜ blkdiag( a1I1, . . . , a B IB ),the mean-matched FO control uses Ag. The covariance-matched noise control uses independent block noise with covariance 

Cb = 1

qb + db + 1 



gbg⊤ 

> b

+ ∥gb∥2 Ib



, (46) matching the centered covariance of Pbgb. These controls remove smaller-step, blockwise shrinkage, generic-noise, and gradient-estimation explanations before attributing a gain to randomized gradient shaping. Method comparisons use a compact set of related continual learning baselines: GPM as the projection-based geometry comparator [ 24 ], experience replay as a strong simple memory baseline [22 ], and a ZO-FC-style hybrid comparator in PEFT settings where the classifier or head is naturally optimized by FO gradients [ 30 ]. Because RISE is a post-processing rule on exact gradients, it can be attached to any host method whose adaptation exposes suitable gradient blocks. The reported vision instantiation uses ZO-FC as that host method, so the primary empirical claim is paired improvement over ZO-FC; other host-method gains are treated as transfer tests only when the corresponding RISE variant is explicitly evaluated. For visual continual learning, we use CIFAR100, ImageNet-R, and DomainNet. Following the settings in prior work, CIFAR100 and ImageNet-R are evaluated with 5-class and 10-class increments, and DomainNet is evaluated with 69 classes per task. The class or task order is fixed with seed 1993 for all methods and datasets. All visual experiments use a ViT-B/16 backbone. ZO-FC+RISE keeps the ZO-FC convention of using an FO classifier head for fast plasticity, but replaces the representation-side finite-difference ZO adaptation by the exact-gradient shaping rule in Eq. (25) . For the shaped representation-side component, we use learning rate 0.01 and q = 4 . The classifier head is optimized with FO SGD using learning rate 0.01 and a cosine decay schedule. In the ZO-FC host, the representation-side adapter uses SPSA with ZO-SGD, constant learning rate 0.01 , perturbation magnitude ϵ = 0 .001 , query budget q = 4 , and ℓ2 clipping threshold 1.0 for the estimated gradients; ZO-FC+RISE keeps the same host classifier training and replaces this finite-difference estimator by exact-gradient norm-matched shaping. The CIFAR100 budget trains the FO classifier head for 10 epochs and the representation-side adapter for 20 epochs; the ImageNet-R budget trains the FO classifier head for 20 epochs and the adapter for 40 epochs. Baselines that combine an adapter with a cosine classifier use the matched host budgets of 10 epochs on CIFAR100 and 20 epochs on ImageNet-R, while the other displayed baselines use their official implementations and recommended hyperparameters. This design makes Table 1 a paired ZO-FC ablation rather than a claim that RISE uniformly dominates all displayed continual-learning baselines. For language continual learning, we use the task pool summarized in Table 3, following the O-LoRA training setup [ 29 ]. The pool combines standard CL benchmark tasks, GLUE tasks, SuperGLUE-style tasks, and IMDB movie reviews. Our three task streams are listed in Table 4. Orders 1 and 2 are short standard CL streams over four classification tasks, while Order 3 is a heterogeneous 15-task stream spanning sentiment analysis, topic classification, natural language inference, paraphrase detection, 16 Table 3: Language continual-learning task pool used by the O-LoRA-style setting. The table restates the dataset coverage used for our language experiments with compact task and domain descriptions.                                                                 

> Dataset Source Task type Domain Metric
> Yelp CL benchmark Sentiment analysis Yelp reviews Accuracy Amazon CL benchmark Sentiment analysis Amazon reviews Accuracy DBpedia CL benchmark Topic classification Wikipedia Accuracy Yahoo CL benchmark Topic classification Yahoo Q&A Accuracy AG News CL benchmark Topic classification News Accuracy MNLI GLUE Natural language inference Mixed genres Accuracy QQP GLUE Paraphrase detection Quora questions Accuracy RTE GLUE Natural language inference News and Wikipedia Accuracy SST-2 GLUE Sentiment analysis Movie reviews Accuracy WiC SuperGLUE Word-sense disambiguation Lexical examples Accuracy CB SuperGLUE Natural language inference Mixed genres Accuracy COPA SuperGLUE Causal QA Blogs and encyclopedia Accuracy BoolQA SuperGLUE Boolean QA Wikipedia Accuracy MultiRC SuperGLUE Multi-sentence QA Mixed passages Accuracy IMDB Movie-review dataset Sentiment analysis Movie reviews Accuracy

Table 4: Language task streams used in our experiments. The third stream is the long heterogeneous order used for the large-task-sequence setting. 

Stream Task sequence 

Order 1 DBpedia → Amazon → Yahoo → AG News Order 2 DBpedia → Amazon → AG News → Yahoo Order 3 Yelp → Amazon → MNLI → CB → COPA → QQP → RTE → IMDB → SST-2 →

DBpedia → AG News → Yahoo → MultiRC → BoolQA → WiC 

question answering, and word-sense disambiguation. The experiments use T5 models; all variants use AdamW, all tasks are trained for one epoch, and reported results are means over three random seeds. We otherwise keep the same task streams, training budget, batching, optimizer settings, and regularization settings when comparing FO, ZO, and RISE variants. This keeps the language-model transfer test focused on whether exact-gradient randomized shaping recovers plasticity relative to finite-difference ZO while retaining forgetting benefits. Across all reported experiments, we use a single NVIDIA A800 GPU with 80 GB memory and keep the training batch size fixed at 48. 

D.2 Mechanism Evidence in a Quadratic Sandbox 

The quadratic-sandbox study uses a locally quadratic setting in which H, g, q, and d are directly specified. For a fixed spectrum and query count, we sample norm-matched ZO shapes, estimate 

E P⊤HP , and compare the empirical average with the closed form in (12) . The operator panel reports eigenvalue-level contraction toward ¯λ together with a Frobenius operator residual as the number of Monte Carlo samples increases. The main synthetic figure uses d = 64 , q = 4 , and 24 ,000 

Monte Carlo samples for the eigenvalue panel; the relative eigenvalue error is 1.17 × 10 −2. Repeating the same validation after a random orthogonal rotation of H gives a non-diagonal robustness test: Theorem 4.1 is a distributional isotropy statement, not a coordinate artifact. The direct damage panel rotates g relative to the eigenvectors of H so that δλ = λ − ¯λ sweeps through negative, near-zero, and positive regimes. The plotted scalar is QFO − E QZO 

> q

, with the predicted line from (16) overlaid. Negative and near-zero regimes are part of the validation, because they verify that the mechanism is geometry-dependent rather than a uniform protection guarantee. In the main synthetic run, the empirical-versus-predicted damage gaps have R2 = 0 .9999 .Finite-query validation compares the deterministic mean signal Gq in (18) together with the realized centered deviation across query batches, dimensions, and block sizes. Increasing q reduces realized variance, while large global dimensions make one-shot shaping unstable; replacing the global shape by smaller blockwise shapes lowers the realization scale. The main variance panel uses d = 256 

and compares global shaping with blockwise shaping at db = 16 ; at q = 2 , the blockwise standard deviation is 0.79 times the global standard deviation. This variance pattern is the controlled transition from the global ZO identity to the blockwise RISE wrapper. 17 0 10 20 30 40 50 60                    

> Eigenvalue index
> 2
> 4
> 6
> Retention curvature
> A Spectrum Equalization
> λi(H)
> predicted shaped
> Monte Carlo ̄
> λ
> 10 210 310 4
> Monte Carlo samples
> 10 −1
> Relative Frobenius residual
> B Operator Identity Residual
> −2 −1 01234
> λ−̄ λ
> −1
> 0
> 1
> 2
> FO  −  ZO
> q
> R2= 0.9999
> C Sign-Changing Damage Gap
> prediction
> Monte Carlo
> 124816
> Query count q
> 10 0
> 6 × 10 −1
> 2 × 10 0
> Std. of centered damage
> D Finite-Query Variance
> global
> blockwise, db= 16

Figure 2: Synthetic validation of randomized shaping. In a controlled quadratic sandbox, norm-matched ZO shaping contracts the retention-curvature spectrum toward ¯λ while preserving the mean curvature (A), and the Monte Carlo operator residual decreases with sample size (B). Rotating g

across eigendirections makes the direct damage gap change sign exactly as predicted by (16) (C, with 

R2 = 0 .9999 between empirical and predicted gaps). In a larger synthetic setting, blockwise shaping with db = 16 lowers the centered one-batch damage deviation relative to global shaping across query counts (D, where at q = 2 the standard deviation is 0.79 × the global value). An optional neural local validation connects the sandbox to ordinary training. At selected continual-learning checkpoints, historical batches define an empirical Fisher or Hessian-style retention curvature proxy, the current batch supplies the incoming exact gradient, and one-step or few-step measurements estimate the resulting change in historical loss. The comparison is local: predicted quadratic damage gaps and blockwise scores are correlated with observed local forgetting changes. This local validation tests whether the mechanism remains visible in neural-network neighborhoods without claiming a global nonconvex training theorem. When blockwise scores are reported, the proxy construction is fixed across methods. Diagonal and block-diagonal readings may use empirical Fisher or Gauss–Newton estimates from the same historical mini-batches; directional quantities such as g⊤ 

> b

Hbb gb may be estimated by Hessian-vector or Fisher-vector products; and damped inverses in Rb(ρ) use the same ρ across layers and methods. Off-diagonal coupling scores are reported only when the corresponding block-pair products are estimated explicitly; when they are omitted, the interpretation is restricted to within-block exposure. 

D.3 End-to-End Settings and Ablations 

The end-to-end experiments treat RISE as an exact-gradient transfer of the ZO shape, not as a generic ZO method, replay method, or memory-saving finite-difference optimizer. They are benchmark-transfer tests that complement the quadratic-sandbox evidence in Fig. 2. The reported vision table inserts RISE into ZO-FC and therefore tests the paired ZO-FC to ZO-FC+RISE change. The language table uses SeqLoRA and O-LoRA as additional hosts, illustrating that the same wrapper can be attached beyond ZO-FC when exact block gradients are available. For visual continual learning, the central comparison is ZO-FC versus ZO-FC+RISE. Both methods keep the classifier head optimized by FO gradients, so the head remains a fast-plastic component. The difference is on the representation side: ZO-FC uses finite-difference ZO adaptation, while ZO-FC+RISE applies the exact-gradient shaping rule in Eq. (25) . The classifier-only, FO-optimization, 18 Table 5: Ablation over query count q on ImageNet-R Inc-5. 

q Avg ↑ Last ↑ Fgt ↓

1 71.21 66.42 2.63 

4 72.05 67.57 2.68 8 72.98 68.27 3.48 16 73.92 68.73 4.29 32 74.94 69.27 5.08 64 75.06 69.03 6.46 and ZO-optimization rows in Table 1 provide context for this paired comparison. They are not used to claim generic dominance over all continual-learning methods. For language continual learning, Table 2 compares FO, finite-difference ZO, and RISE variants under the same task orders and training settings for SeqLoRA and O-LoRA. This design tests the specific confound raised by recent ZO continual-learning results: raw finite-difference ZO can reduce forgetting while losing plasticity. The reported comparison tests whether exact-gradient transfer recovers much of that plasticity while retaining some of the stability benefit associated with ZO shaping. These rows support the attachable-wrapper view, but the magnitude of the gain remains host-method dependent and is not inferred for methods without a reported RISE variant. Across both domains, the reported metrics are Avg, Last, and Fgt. Avg measures overall continual-learning performance, Last serves as the plasticity-sensitive readout, and Fgt measures retention. Efficiency breakdowns and memory overhead are useful system-level measurements, but they are not part of the reported evidence package. The completed end-to-end ablation is the query-count sweep in Table 5. It varies q ∈{1, 4, 8, 16 , 32 , 64 } on ImageNet-R Inc-5 while keeping the rest of the visual setting fixed. This ablation tests the finite-query prediction that low q gives stronger randomized shaping and lower forgetting, while larger q moves the adaptation closer to the exact-gradient direction and improves plasticity-sensitive accuracy. Additional controls remain important for stronger claims, but they are supporting analyses unless their results are reported. These include scaled FO, FO plus isotropic Gaussian noise, FO plus covariance-matched noise, global versus blockwise exact-gradient shaping, block-granularity sweeps, parameter-scope ablations, and score-correlation analyses using the blockwise scores in Appendix C. When such controls are reported, they use the same task streams, seeds, adaptation budgets, and optimizer placement as the corresponding end-to-end table. Their role is to separate randomized-shaping benefits from mean shrinkage, generic noise, implementation granularity, and score-selection effects. The blind-optimal exposure result in Sec. 4.3 remains a supporting scope analysis. A compact exposure panel comparing FO, norm-matched ZO, and the isotropic second-moment benchmark can clarify the unknown-curvature interpretation, but it does not replace the fixed-H operator identity, direct forgetting-gap validation, or the reported end-to-end transfer results. 

## E Proofs 

The proofs follow the dependency order used by the main text: finite-difference calibration, norm matching, expected shaped curvature, direct damage, one-batch corrections, curvature-agnostic exposure, and blockwise RISE support. 

E.1 Proof of Lemma 3.1 

By L-smoothness, for each queried direction zr there exist scalar remainders ϵr, + and ϵr, − such that 

f (θ + μzr ) = f (θ) + μ⟨g, zr ⟩ + ϵr, +, |ϵr, +| ≤ Lμ 2

2 ∥zr ∥2 , (47) 

f (θ − μzr ) = f (θ) − μ⟨g, zr ⟩ + ϵr, −, |ϵr, −| ≤ Lμ 2

2 ∥zr ∥2 . (48) 19 Thus f (θ + μzr ) − f (θ − μzr )2μ = ⟨g, zr ⟩ + ϵr , ϵr ≜ ϵr, + − ϵr, −

2μ , (49) with 

|ϵr | ≤ Lμ 

2 ∥zr ∥2 . (50) Substituting the two-point Taylor decomposition into (6) yields 

bgμ = 1

q

> q

X

> r=1

zr z⊤ 

> r

g + 1

q

> q

X

> r=1

ϵr zr = Zg + rμ, rμ ≜ 1

q

> q

X

> r=1

ϵr zr . (51) Conditioned on the queried directions, 

∥rμ∥ ≤ 1

q

> q

X

> r=1

|ϵr | ∥ zr ∥ ≤ Lμ 

2q

> q

X

> r=1

∥zr ∥3 . (52) Taking expectation gives 

E [∥rμ∥] ≤ Lμ 

2 Ez∼N (0,I)

h

∥z∥3i

. (53) The Gaussian fourth moment gives the simple dimension bound 

E

h

∥z∥3i

≤



E

h

∥z∥4i 3/4

= ( d(d + 2)) 3/4 = O



d3/2

. (54) For the second moment, Jensen’s inequality gives q−1 Pqr=1 ∥zr ∥3 squared no larger than 

q−1 Pqr=1 ∥zr ∥6. Hence the Gaussian sixth moment gives 

E

h

∥rμ∥2i

≤ L2μ2

4 Ez∼N (0,I)

h

∥z∥6i

= O  L2μ2d3 . (55) Together these bounds prove (7). 

E.2 Proof of Lemma 3.2 

Since E zr z⊤

> r

 = I, linearity gives 

E [Z] = 1

q

> q

X

> r=1

E zr z⊤

> r

 = I. (56) For the second moment, expand 

E

h

∥Zg∥2i

= 1

q2

> q

X

> r,s =1

E g⊤zr z⊤ 

> r

zsz⊤ 

> s

g . (57) For r̸ = s, independence gives 

E g⊤zr z⊤ 

> r

zsz⊤ 

> s

g = g⊤E zr z⊤

> r

 E zsz⊤

> s

 g = ∥g∥2 . (58) For r = s, rotational invariance and the Gaussian fourth moment give 

E

h

∥zr ∥2 (z⊤ 

> r

g)2i

= ( d + 2) ∥g∥2 . (59) Combining the q diagonal and q(q − 1) off-diagonal terms yields 

E

h

∥Zg∥2i

= q(d + 2) + q(q − 1) 

q2 ∥g∥2 = q + d + 1 

q ∥g∥2 . (60) Multiplying by κ−1 = q/ (q+d+1) gives E[∥Pg∥2] = ∥g∥2, and linearity gives E[Pg] = κ−1/2g =pq/ (q + d + 1) g. This proves (9). 20 E.3 Full-Information Isotropic Reference 

Let ¯λ = tr( H)/d and P⋆ = ptr( H)/d H−1/2O with O⊤O = I. Since H ≻ 0, this operator is well defined. Direct substitution gives 

P⊤ 

> ⋆

HP ⋆ = tr( H)

d O⊤H−1/2HH −1/2O = ¯λI. (61) Taking traces yields tr( P⊤ 

> ⋆

HP ⋆) = tr( ¯λI) = tr( H), which proves both the fixed-scale identity and the isotropic target. For any g̸ = 0,

Q(−ηP⋆g) = η2

2 g⊤  P⊤ 

> ⋆

HP ⋆

 g = η2

2 ¯λ ∥g∥2 . (62) Subtracting this expression from QFO = ( η2/2) ∥g∥2 λ gives 

QFO − Q (−ηP⋆g) = η2

2 ∥g∥2  λ − ¯λ ,

which is the full-information damage gap stated informally in Section 4.1, with λ = g⊤Hg/ ∥g∥2.

E.4 A General Isotropic-Moment Lemma 

Lemma E.1 (General Isotropic-Moment Form) . Let P be a random shape operator with finite second moments whose distribution is isotropic in a selected comparison space, meaning O⊤PO d

= P for every orthogonal matrix O, where d

= denotes equality in distribution. Define T (H) ≜ E P⊤HP 

and ¯λ = tr( H)/d . Then there exist scalars α and ρ, depending on the shape-operator distribution but not on H, such that for every symmetric H,

T (H) = αH + ρ¯λI. (63) 

Equivalently, if H = ¯λI + R with tr( R) = 0 , where R is the traceless anisotropic component of retention curvature, then 

T (H) = ( α + ρ)¯λI + αR. (64) The isotropic-moment assumption in Lemma E.1 implies that the linear map T (H) = E P⊤HP  is orthogonally equivariant: T (O⊤HO ) = O⊤T (H)O for every orthogonal O. The space of symmet-ric matrices decomposes into the direct sum of the identity span and the traceless symmetric subspace. These two subspaces are invariant under orthogonal conjugation. By the standard irreducibility of the traceless symmetric representation, an orthogonally equivariant linear map acts as a scalar on each component. Hence there exist scalars ω and α such that 

T (¯λI + R) = ω¯λI + αR, tr( R) = 0 . (65) Writing ρ = ω − α gives T (H) = αH + ρ¯λI, which is (63) ; the equivalent traceless form follows immediately. 

E.5 Proof of Theorem 4.1 

We expand 

E [ZHZ ] = 1

q2

> q

X

> r,s =1

E zr z⊤ 

> r

Hz sz⊤

> s

 . (66) For r̸ = s, independence gives H. For r = s, the Gaussian fourth-moment identity gives 

E zz ⊤Hzz ⊤ = tr( H)I + 2 H. (67) Therefore, 

E [ZHZ ] = 1

q2 (q(tr( H)I + 2 H) + q(q − 1) H) = q + 1 

q H + tr( H)

q I. (68) Since Z and P = κ−1/2Z are symmetric, 

E P⊤HP  = qq + d + 1 

 q + 1 

q H + tr( H)

q I



= q + 1 

q + d + 1 H + tr( H)

q + d + 1 I. (69) Using ¯λ = tr( H)/d and τ = d/ (q + d + 1) gives 

E P⊤HP  = (1 − τ )H + τ ¯λI. (70) 21 E.6 Proof of Corollary 4.2 

Since I commutes with H, the matrix E P⊤HP  = (1 − τ )H + τ ¯λI has the same eigenvectors as 

H, and its eigenvalues are exactly 

λ′ 

> i

= (1 − τ )λi + τ ¯λ, i = 1 , . . . , d. (71) The map λ 7 → (1 − τ )λ + τ ¯λ1 preserves the sum of eigenvalues and contracts every deviation from the mean by 1 − τ ∈ (0 , 1) .

E.7 Proof of Theorem 4.3 

By the definition of QZO  

> q

and Theorem 4.1, 

E QZO 

> q

 = η2

2 g⊤  (1 − τ )H + τ ¯λI g. (72) Using g⊤Hg = ∥g∥2 λ and g⊤Ig = ∥g∥2 gives 

E QZO 

> q

 = η2

2 ∥g∥2  (1 − τ )λ + τ ¯λ . (73) Since QFO = ( η2/2) ∥g∥2 λ, subtracting the last display from QFO gives 

QFO − E QZO 

> q

 = η2

2 τ ∥g∥2  λ − ¯λ , (74) which is the claimed forgetting-reduction identity. If λ > 0, then QFO = ( η2/2) ∥g∥2 λ > 0, and dividing the last display by QFO gives τ (1 − ¯λ/λ ).

E.8 Proof of Proposition B.1 

Let D ≜ Z − I and Ξq ≜ P⊤HP − ¯Hq . Also let λmax denote the largest eigenvalue of H. Since 

P = κ−1/2Z, Z = I + D, and H is positive semidefinite, 

P⊤HP = κ−1ZHZ = κ−1 (H + DH + HD + DHD ) . (75) The proof of Theorem 4.1 gives 

¯Hq = E P⊤HP  = κ−1



H + 1

q

 H + d¯λI

. (76) Subtracting (76) from (75) gives the exact decomposition 

Ξq = κ−1



DH + HD + DHD − 1

q

 H + d¯λI

. (77) The zero-smoothing realized damage identity used in Proposition B.1 follows from 

QZO  

> q

= η2

2 g⊤   ¯Hq + Ξ q

 g = E QZO 

> q

 + η2

2 g⊤Ξq g (78) and Theorem 4.3. Equivalently, 

 QFO − Q ZO 

> q

 − Gq = QZO  

> q

− E QZO 

> q

 = η2

2 g⊤Ξq g . (79) It remains to prove the high-probability operator-norm bound. A standard Gaussian sample-covariance concentration inequality gives a universal constant C > 0 such that, with probability at least 1 − δ,

∥D∥op ≤ ϵq , ϵq = C

"s 

d + log(1 /δ )

q + d + log(1 /δ )

q

#

. (80) Increasing C if necessary, ϵq ≤ Cψ q (δ) for ψq (δ) in (26) . On the same event, the random part in (77) satisfies 

∥DH + HD + DHD ∥op ≤  2ϵq + ϵ2

> q

 λmax . (81) 22 The deterministic centering term is absorbed by the same fluctuation scale: since H ⪰ 0, d¯λ =tr( H) ≤ dλ max , and therefore 

1

q

 H + d¯λI

> op

≤ λmax + d¯λq ≤ d + 1 

q λmax ≤ λmax ψq (δ). (82) Combining these estimates with κ−1 ≤ 1 gives 

∥Ξq ∥op ≤ κ−1



∥DH + HD + DHD ∥op + λmax + d¯λq



(83) 

≤ κ−1

 2ϵq + ϵ2

> q

 λmax + λmax + d¯λq



(84) 

≤ Cλ max 

 ψq (δ) + ψq (δ)2 . (85) Using |g⊤Ξq g| ≤ ∥ g∥2 ∥Ξq ∥op gives the main-text bound in (27). 

E.9 Proof of Corollary B.2 

Let bxμ = κ−1/2 bgμ, x = Pg = κ−1/2Zg, and b = κ−1/2rμ. Lemma 3.1 gives bxμ = x + b. By definition, QZO  

> q

= Q(−ηx) and bQZO  

> q,μ

= Q(−ηbxμ). Adding and subtracting QZO  

> q

in QFO − bQZO  

> q,μ

and then using Theorem 4.3 gives (19). It remains to bound the final perturbation term. Since H ⪰ 0 and λmax is its largest eigenvalue, 

|Q (−ηbxμ) − Q (−ηx)| = η2

2 2x⊤Hb + b⊤Hb

≤ η2

2 λmax 



2 ∥x∥ ∥ b∥ + ∥b∥2

.

(86) This pathwise inequality is the source of the expectation bound after substituting x = Pg and 

b = κ−1/2rμ.Taking expectations and applying Cauchy–Schwarz, 

E [∥Pg∥ ∥ rμ∥] ≤



E

h

∥Pg∥2i 1/2 

E

h

∥rμ∥2i 1/2

. (87) Lemma 3.2 gives E[∥Pg∥2] = ∥g∥2, and Lemma 3.1 gives the required second-moment remainder bound. The Cauchy–Schwarz estimate controls the cross term, while the residual quadratic term con-tributes κ−1E[∥rμ∥2] directly. Substituting these estimates gives E

h

| bQZO  

> q,μ

− Q ZO  

> q

|

i

at the displayed order, proving (28). For the squared-norm perturbation, use bxμ = x + b and E[∥x∥2] = ∥g∥2:

E

h

∥bxμ∥2i

− ∥ g∥2 = E

h

2x⊤b + ∥b∥2i

≤ 2E[∥x∥ ∥ b∥] + E[∥b∥2].

(88) The same Cauchy–Schwarz step and the same bounds on rμ give (29). 

E.10 Proof of Corollary B.3 

Proposition B.1 gives, with probability at least 1 − δq , the zero-smoothing centered-deviation bound 

|Q ZO  

> q

− E[QZO  

> q

]| ≤ Bq (δq ). Corollary B.2 gives E[| bQZO  

> q,μ

− Q ZO  

> q

|] ≤ Bμ, so Markov’s inequality gives 

bQZO  

> q,μ

− Q ZO  

> q

≤ Bμ

δμ

(89) with probability at least 1 − δμ. Applying a union bound and the exact accounting identity (19) proves (32) . If the right-hand side is smaller than |Gq |, the realized gap cannot cross zero, so its sign agrees with Gq .23 E.11 Proof of Theorem 4.4 

Fix M ⪰ 0 and let λmax (M) be its largest eigenvalue. Then M ⪯ λmax (M)I, so λmax (M)I − M ⪰

0. For every H ∈ H ¯λ, the trace of the product of two positive semidefinite matrices is nonnegative, because tr( H(λmax (M)I − M)) = tr( H1/2(λmax (M)I − M)H1/2) ≥ 0. Hence 

tr( HM ) ≤ λmax (M) tr( H) = d¯λ λ max (M). (90) This upper bound is attained by choosing H = d¯λ uu ⊤, where u is a unit top eigenvector of M.Hence 

sup 

> H∈H ¯λ

tr( HM ) = d¯λ λ max (M). (91) Multiplying by η2/2 proves (22) . The minimax problem is therefore equivalent to minimizing 

λmax (M) over positive semidefinite M with tr( M) = ∥g∥2. For any such M,

λmax (M) ≥ tr( M)

d = ∥g∥2

d . (92) Equality holds if and only if all eigenvalues of M are equal, which gives the unique optimizer 

M⋆ = ( ∥g∥2 /d )I. Multiplying by η2/2 proves the minimax equality in (22). 

E.12 Appendix-Only Aligned Curvature-Agnostic Benchmark 

Theorem E.2 (Aligned-Signal Curvature-Agnostic Benchmark) . Assume d ≥ 2, fix α ∈ [0 , 1] , and define Π⊥ ≜ I − gg ⊤/ ∥g∥2. Let 

Mα ≜

n

M ⪰ 0 : tr( M) = ∥g∥2 , M ⪰ α2gg ⊤o

. (93) 

For H¯λ in (21) ,

V ⋆α ≜ inf 

> M∈M α

sup 

> H∈H ¯λ

η2

2 tr( HM ) = η2

2 d¯λ ∥g∥2 max 



α2, 1

d



. (94) 

One optimal second moment is 

M⋆α =



∥g∥2

d I, α2 ≤ 1/d, α2gg ⊤ + (1 − α2) ∥g∥2

d − 1 Π⊥, α2 > 1/d. 

(95) The set Mα contains the second moments of all random directions x satisfying E[x] = αg and 

E

h

∥x∥2i

= ∥g∥2. The theorem records the aligned curvature-agnostic characterization that the main text uses only as positioning. 

Proof. The adversarial-curvature calculation from Theorem 4.4 gives 

sup 

> H∈H ¯λ

tr( HM ) = d¯λ λ max (M). (96) It remains to minimize λmax (M) over Mα. Every feasible M satisfies both 

λmax (M) ≥ tr( M)

d = ∥g∥2

d (97) and, by applying the constraint M ⪰ α2gg ⊤ to the unit vector g/ ∥g∥,

λmax (M) ≥ α2 ∥g∥2 . (98) Thus 

λmax (M) ≥ ∥ g∥2 max 



α2, 1

d



. (99) 24 When α2 ≤ 1/d , the isotropic moment (∥g∥2 /d )I is feasible because (∥g∥2 /d )I − α2gg ⊤ ⪰ 0,and it attains this lower bound. When α2 > 1/d , the moment in (95) has trace ∥g∥2 and satisfies 

M⋆α − α2gg ⊤ = ((1 − α2) ∥g∥2 /(d − 1))Π ⊥ ⪰ 0. Its eigenvalue along g is α2 ∥g∥2, and all perpendicular eigenvalues equal ((1 − α2) ∥g∥2)/(d − 1) , which is no larger because α2 > 1/d . It therefore attains the lower bound. Multiplying the optimal largest eigenvalue by d¯λη 2/2 gives (94). Finally, the condition M ⪰ α2gg ⊤ is the second-moment form of preserving the aligned mean. If 

M satisfies it, set C = M − α2gg ⊤ ⪰ 0 and take any zero-mean random vector ϵ with covariance 

C; then x = αg + ϵ has E[x] = αg and E[xx ⊤] = M.

E.13 Proof of Corollary 4.5 

Applying Theorem 4.1 with H replaced by gg ⊤ gives 

MZO  

> q

= E Pgg ⊤P = (1 − τ )gg ⊤ + τ ∥g∥2

d I, (100) which proves (23). The eigenvalue of MZO  

> q

along g is 

∥g∥2 

1 − τ + τd



, (101) and each perpendicular eigenvalue is τ ∥g∥2 /d , so the displayed value is the largest eigenvalue. The FO exposure MFO = gg ⊤ has largest eigenvalue ∥g∥2, and M⋆ has largest eigenvalue ∥g∥2 /d .Applying (22), 

R¯λ

 MZO 

> q

 − R ¯λ(M⋆) = η2

2 d¯λ ∥g∥2 (1 − τ )



1 − 1

d



, (102) while 

R¯λ

 MFO  − R ¯λ(M⋆) = η2

2 d¯λ ∥g∥2



1 − 1

d



. (103) The first display is therefore (1 − τ ) times the second, proving (24) . Subtracting E[Pg]E[Pg]⊤ from (23) also gives the centered covariance used for covariance-matched controls: 

Cov( Pg) = 1

q + d + 1 



gg ⊤ + ∥g∥2 I



. (104) 

E.14 Proof of Theorem C.1 

For a diagonal block, Theorem 4.1 applies inside the db-dimensional block with query count qb:

E[P⊤ 

> b

Hbb Pb] = (1 − τb)Hbb + τb ¯λbIb. (105) For an off-diagonal block, independence across blocks and E[Pb] = abIb give 

E[P⊤ 

> b

Hbc Pc] = E[Pb]⊤Hbc E[Pc] = abacHbc . (106) This proves (34). Expanding the quadratic form by blocks, 

QFO = η2

2

" BX

> b=1

g⊤ 

> b

Hbb gb + 2 X

> b<c

g⊤ 

> b

Hbc gc

#

, (107) and substituting (34) into E[QRISE blk ] gives the same expression with Hbb replaced by (1 − τb)Hbb +

τb ¯λbIb and Hbc replaced by abacHbc . Subtracting yields (35). 

E.15 Proof of Proposition C.2 

Let Db ≜ Zb − Ib. Applying the same Gaussian sample-covariance concentration used in (80) to each block and taking a union bound gives an event of probability at least 1 − P 

> b

δb on which 

∥Db∥op ≤ Cψ b(δb) for all b = 1 , . . . , B. (108) 25 Increasing C absorbs universal constants in the following bounds. On this event, the diagonal block fluctuation follows from the blockwise version of (77): 

P⊤ 

> b

Hbb Pb − E[P⊤ 

> b

Hbb Pb] = κ−1

> b



DbHbb + Hbb Db + DbHbb Db − 1

qb

(Hbb + tr( Hbb )Ib)



.

(109) Here Hbb is only assumed symmetric. Thus the centering term is bounded using | tr( Hbb )| ≤ 

db ∥Hbb ∥op , not a PSD trace bound: 

1

qb

(Hbb + tr( Hbb )Ib)

> op

≤ db + 1 

qb

∥Hbb ∥op ≤ ψb(δb) ∥Hbb ∥op . (110) Together with (108), this gives 

P⊤ 

> b

Hbb Pb − E[P⊤ 

> b

Hbb Pb] op ≤ C ∥Hbb ∥op 

 ψb(δb) + ψb(δb)2 . (111) For an off-diagonal block, use Pb = ab(Ib + Db) to write 

P⊤ 

> b

Hbc Pc − abacHbc = abac (DbHbc + Hbc Dc + DbHbc Dc) . (112) Therefore, 

P⊤ 

> b

Hbc Pc − abacHbc op ≤ Ca bac ∥Hbc ∥op (ψb(δb) + ψc(δc) + ψb(δb)ψc(δc)) . (113) Finally expand QRISE blk − E[QRISE blk ] by diagonal and off-diagonal blocks. Bound each diagonal scalar by 

∥gb∥2 times (111) , and each off-diagonal scalar by ∥gb∥ ∥ gc∥ times (113) . The factor η2/2 applies to diagonal terms and the factor η2 to b < c cross terms, giving (37). 

E.16 Proof of Theorem C.3 

Write ∆ = m + ϵ, where m = E[∆] and E[ϵ] = 0. By the trace identity, 

E [Q(∆)] = 12 E (m + ϵ)⊤H(m + ϵ) = 12 m⊤Hm + 12 tr  H E[ϵϵ ⊤] , (114) because the cross term has expectation zero. The block independence and zero cross-covariance assumptions give 

E[ϵϵ ⊤] = blkdiag( Σ1, . . . , ΣB ) = Σ. (115) Using the block trace identity then yields 

tr( HΣ ) = 

> B

X

> b=1

tr( Hbb Σb), (116) which proves (38). 

E.17 Proof of Proposition C.4 

Let 

K ≜ H−1/2 

> blk

EH −1/2 

> blk

. (117) Then 

H−1/2 

> blk

HH −1/2 

> blk

= I + K. (118) Since ∥K∥op = ϵblk < 1 and K is symmetric, 

(1 − ϵblk )I ⪯ I + K ⪯ (1 + ϵblk )I. (119) In particular, I + K ≻ 0, and congruence by H1/2 

> blk

gives H ≻ 0. Inverting the positive definite matrices reverses the Loewner order: 

(1 + ϵblk )−1I ⪯ (I + K)−1 ⪯ (1 − ϵblk )−1I. (120) Finally, 

H−1 = H−1/2 

> blk

(I + K)−1H−1/2 

> blk

, (121) and congruence by H−1/2 

> blk

gives (44). 26