---
title: Reward-Guided Semantic Evolution for Test-time Adaptive Object Detection
title_zh: 奖励引导的语义演化用于测试时自适应目标检测
authors: "Lihua Zhou, Mao Ye, Xiatian Zhu, Nianxin Li, Changyi Ma, Shuaifeng Li, Yitong Qin, Hongbin Liu, Jiebo Luo, Zhen Lei"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04531v1"
tags: ["query:tta"]
score: 9.5
evidence: 针对视觉语言模型测试时间自适应目标检测的免训练框架
tldr: 本研究针对开放词汇目标检测在测试阶段因分布偏移导致的性能下降问题，提出了奖励引导语义演化（RGSE）框架。RGSE是一种无需训练的方法，通过模拟进化搜索，利用余弦相似度作为奖励信号，对文本嵌入进行扰动、评估和加权融合，从而在不依赖反向传播的情况下实现文本与视觉特征的实时对齐，在多个基准测试中达到了SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04531v1/fig-001.webp\", \"caption\": \"TABLE I OBJECT DETECTION PERFORMANCE COMPARISON WITH THE STATE-OF-THE-ART METHODS ON FOGGYCITYSCAPES. METRIC: AVERAGE PRECISION@50(%) - THE BEST NUMBERS ARE DENOTED IN bold; BP-FREE: BACKPROPAGATION-FREE AT TEST TIME.\", \"page\": 6, \"index\": 1, \"width\": 1070, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2605.04531v1/fig-002.webp\", \"caption\": \"TABLE II OBJECT DETECTION PERFORMANCE COMPARISON WITH THE STATE-OF-THE-ART METHODS ON PASCAL-C. METRIC: MEAN AVERAGE PRECISION@50(%) - THE BEST NUMBERS ARE DENOTED IN bold.\", \"page\": 6, \"index\": 2, \"width\": 1070, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2605.04531v1/fig-003.webp\", \"caption\": \"TABLE III OBJECT DETECTION PERFORMANCE COMPARISON WITH THE STATE-OF-THE-ART METHODS ON COCO-C. METRIC: MEAN AVERAGE PRECISION@50(%) - THE BEST NUMBERS ARE DENOTED IN bold.\", \"page\": 7, \"index\": 3, \"width\": 1070, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2605.04531v1/fig-004.webp\", \"caption\": \"Fig. 3. Hyperparameter sensitivity on PASCAL-C. RGSE shows stable performance across a broad range of τbase, α, and σ.\", \"page\": 7, \"index\": 4, \"width\": 1074, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2605.04531v1/fig-005.webp\", \"caption\": \"Fig. 4. Qualitative results on PASCAL-C-Brit, PASCAL-C-Contrast, PASCAL-C-GaussNoise, and FoggyCityscapes (Swin-T). Green, red and blue boxes represent true positives, false negatives and false positives, respectively.\", \"page\": 8, \"index\": 5, \"width\": 857, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2605.04531v1/fig-006.webp\", \"caption\": \"Fig. 2. Overview of Reward-Guided Semantic Evolution (RGSE). Given a test image, we first obtain the initial outputs from Grounding DINO: region proposals {vm, bm,pinit m , lm}Mm=1, and text embeddings {tk}Kk=1. To address semantic misalignment under domain shift, RGSE refines the original text embeddings through four forward-pass stages: (1) Perturbation: Gaussian noise is added to active text embeddings; (2) Reward evaluation: each perturbed embedding is scored based on cosine similarity with current and historical high-confidence visual proposals; (3) Refinement: only candidates outperforming the parent are fused via reward-weighted averaging; (4) Memory update: high-confidence proposals are added to a class-specific cache for future adaptation.\", \"page\": 4, \"index\": 6, \"width\": 860, \"height\": 409}]"
motivation: 现有的测试时自适应目标检测方法要么计算成本高，要么无法直接解决文本与视觉嵌入之间的语义失配问题。
method: 提出一种无需训练的进化搜索框架，通过对文本嵌入进行扰动并利用视觉提议的相似度作为奖励进行加权融合，实现文本特征的动态优化。
result: 在多个目标检测基准测试中取得了最先进的性能，且计算开销极小，无需任何反向传播。
conclusion: RGSE证明了通过简单的语义演化策略即可在测试阶段高效解决跨模态对齐问题，为开放词汇检测的鲁棒性提升提供了新思路。
---

## 摘要
使用 Grounding DINO 等视觉语言模型（VLMs）的开放词汇目标检测在测试时分布偏移下会遭受性能下降，这主要是由于文本嵌入与区域建议（region proposals）的偏移视觉嵌入之间的语义失配造成的。虽然最近针对基于 VLM 的测试时自适应目标检测方法要么依赖于昂贵的反向传播，要么通过外部存储器绕过语义失配，但没有一种方法能以无训练的方式直接且高效地对齐文本和视觉。为了解决这个问题，我们提出了奖励引导的语义演化（RGSE），这是一个在测试时直接优化文本嵌入的无训练框架。受进化搜索的启发，RGSE 将文本嵌入自适应视为一个语义搜索过程：它将文本嵌入扰动为候选变体，通过与当前和历史高置信度视觉建议的余弦相似度作为奖励信号进行评估，并通过奖励加权平均将它们融合为优化后的嵌入。在没有任何反向传播的情况下，RGSE 在多个检测基准测试中实现了最先进的性能，同时增加了极小的计算开销。我们的代码将在发表后开源。

## Abstract
Open-vocabulary object detection with vision-language models (VLMs) such as Grounding DINO suffers from performance degradation under test-time distribution shifts, primarily due to semantic misalignment between text embeddings and shifted visual embeddings of region proposals. While recent test-time adaptive object detection methods for VLM-based either rely on costly backpropagation or bypass semantic misalignment via external memory, none directly and efficiently align text and vision in a training-free manner. To address this, we propose Reward-Guided Semantic Evolution (RGSE), a training-free framework that directly refines the text embeddings at test time. Inspired by evolutionary search, RGSE treats text embedding adaptation as a semantic search process: it perturbs text embeddings as candidate variants, evaluates them via cosine similarity with current and historical high-confidence visual proposals as a reward signal, and fuses them into a refined embedding through reward-weighted averaging. Without any backpropagation, RGSE achieves state-of-the-art performance across multiple detection benchmarks while adding minimal computational overhead. Our code will be open source upon publication.

---

## 论文详细总结（自动生成）

这是一份关于论文《Reward-Guided Semantic Evolution for Test-time Adaptive Object Detection》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：开放词汇目标检测模型（如 Grounding DINO）在面对测试环境与训练环境不一致（分布偏移，如恶劣天气、图像损坏）时，性能会显著下降。
*   **研究背景**：这种下降源于文本嵌入（Text Embeddings）与受损视觉嵌入（Visual Embeddings）之间的**语义失配**。
*   **动机**：现有的测试时自适应（TTA）方法要么依赖昂贵的反向传播（计算慢、不实时），要么通过外部缓存绕过失配问题（不直接修正文本特征）。本文旨在提出一种**无需训练（Training-free）**、能直接且高效修正语义失配的方法。

### 2. 方法论：奖励引导的语义演化 (RGSE)
RGSE 将文本嵌入的自适应建模为一个**进化搜索过程**，完全基于前向传播，包含四个关键阶段：
*   **扰动 (Perturbation)**：识别当前图像中的“活跃类别”，在原始文本嵌入上添加高斯噪声，生成 $N$ 个候选变体（子代）。
*   **奖励评估 (Reward Evaluation)**：定义“奖励信号”为候选文本嵌入与视觉提议（Proposals）之间的余弦相似度。奖励计算结合了**当前图像**的视觉特征和**历史高置信度**的视觉特征（存储在轻量级内存中）。
*   **精炼 (Refinement)**：采用类似自然选择的机制，剔除奖励低于原始嵌入的变体。对剩余的优良变体进行**奖励加权平均**，生成精炼后的文本嵌入，并更新全局文本库。
*   **内存更新 (Memory Update)**：将当前图像的高置信度视觉特征存入类别特定的队列（固定容量），通过相似度替换机制保持内存的代表性。

### 3. 实验设计
*   **数据集/场景**：
    *   **FoggyCityscapes**：模拟城市驾驶中的浓雾环境。
    *   **PASCAL-C**：对 PASCAL VOC 数据集施加 15 种不同类型的合成损坏（如模糊、噪声、雪天等）。
    *   **COCO-C**：对 MS-COCO 数据集施加同样的 15 种损坏。
*   **基准模型 (Backbone)**：采用 Grounding DINO，视觉骨干网络包括 Swin-T 和 Swin-B。
*   **对比方法**：
    *   传统闭集 TTA 方法：SHOT, T3A, TTAC, STFAR 等。
    *   基于 VLM 的 TTA 方法：TDA, HisTPT, BCA, BCA+, MPMT 等。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A100 GPU** 上进行。
*   **效率表现**：
    *   在 COCO-C 上的平均推理时间为 **182.4 ms/image**（$N=1000$ 时），显存占用约 **3823 MB**。
    *   相比于基于反向传播的方法（如 HisTPT 的 1240.8 ms），RGSE 在速度上快了约 7 倍，显存占用仅为其 1/3 左右。

### 5. 实验数量与充分性
*   **实验规模**：论文在三个主流基准数据集上进行了详尽测试，涵盖了数十种损坏场景和不同规模的骨干网络。
*   **消融实验**：针对扰动数量 $N$、内存容量 $M_{max}$、以及核心组件（历史内存、过滤机制、全局库）分别做了消融研究。
*   **评价**：实验设计非常充分且客观。通过与多种 SOTA 方法在相同设置下对比，并提供了超参数敏感性分析和 t-SNE 可视化，有力证明了方法的鲁棒性和公平性。

### 6. 主要结论与发现
*   **性能领先**：RGSE 在所有基准测试中均达到了 SOTA 性能。例如在 FoggyCityscapes 上比之前的最佳方法 BCA+ 提升了约 3.3% 的 mAP。
*   **无需梯度**：证明了通过简单的进化搜索策略，无需任何反向传播即可实现比梯度优化更好的对齐效果。
*   **实时性**：RGSE 增加的计算开销极小，能够满足实时处理的需求。
*   **演化轨迹**：可视化显示，精炼后的文本嵌入在语义空间中确实向受损的视觉聚类中心靠拢。

### 7. 优点与亮点
*   **创新视角**：将 TTA 问题转化为进化搜索问题，避开了复杂的损失函数设计和不稳定的梯度更新。
*   **高效简洁**：完全基于前向传播，易于集成到现有的 VLM 检测器中。
*   **即插即用**：不需要访问源域数据，也不需要对模型架构做任何修改。
*   **鲁棒性强**：对超参数（如噪声强度、激活阈值）不敏感，在多种不同类型的损坏下表现稳定。

### 8. 不足与局限
*   **类别扩展性**：虽然内存占用较低，但在处理拥有数万个类别的极端开放词汇场景时，维护类别特定的视觉内存和进行大规模扰动可能会面临扩展性挑战。
*   **依赖初始预测**：如果初始预测（Active Categories）完全错误，扰动过程可能会在错误的方向上进行，尽管过滤机制能缓解这一点。
*   **应用限制**：目前主要针对基于相似度匹配的检测器（如 Grounding DINO），对于非对齐架构的检测器适用性有待验证。

（完）
