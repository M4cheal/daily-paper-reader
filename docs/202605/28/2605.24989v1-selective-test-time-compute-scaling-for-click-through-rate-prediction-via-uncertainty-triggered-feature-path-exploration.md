---
title: Selective Test-Time Compute Scaling for Click-Through Rate Prediction via Uncertainty-Triggered Feature Path Exploration
title_zh: 通过不确定性触发的特征路径探索实现点击率预测的选择性测试时计算扩展
authors: "Moyu Zhang, Yun Chen, Yujun Jin, Jinxin Hu, Yu Zhang, Xiaoyi Zeng"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.24989v1"
tags: ["query:tta"]
score: 9.0
evidence: CTR预测的测试时计算缩放
tldr: "工业CTR预测中，训练充分与稀疏特征组合存在预测不对称性，现有训练时方法无法在部署时为每实例自适应。本文提出UTTSI，一种无需训练、模型无关的测试时选择性推理框架，通过模型置信度与频率先验的双信号估计区分不确定性，对不确定实例进行随机特征路径探索并一致性加权集成。在四个数据集和三种骨干网络上验证，在线A/B测试实现5.3%的CTR相对提升，表明测试时计算分配是训练时方法的有效补充。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24989v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1820, \"height\": 431}, {\"url\": \"assets/figures/arxiv/2605.24989v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1849, \"height\": 549}, {\"url\": \"assets/figures/arxiv/2605.24989v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1829, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2605.24989v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 868, \"height\": 1065}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24989v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 860, \"height\": 1644}, {\"url\": \"assets/tables/arxiv/2605.24989v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 205}, {\"url\": \"assets/tables/arxiv/2605.24989v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 903}, {\"url\": \"assets/tables/arxiv/2605.24989v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 742, \"height\": 207}]"
motivation: 现有训练时方法受稀疏性限制，无法为每个测试实例提供自适应推理，导致稀疏组合预测不可靠。
method: 提出UTTSI，通过双信号估计区分认知不确定性，自适应过滤特征，对不确定实例进行随机特征路径探索并一致性加权集成。
result: "在四个数据集和三种骨干网络上一致超越训练时基线，在线A/B测试CTR相对提升5.3%（p<0.01）。"
conclusion: 测试时选择性计算分配可作为训练时CTR预测的有效补充，首次证明了其在工业场景中的价值。
---

## 摘要
测试时计算扩展在语言模型中已被证明非常有效，但对于工业点击率（CTR）预测而言，这一机会尚未被充分探索。CTR模型存在一个根本的不对称性：在训练中充分表征的特征组合能产生可靠的预测，而稀疏观察到的组合则输出不可靠的结果。现有的训练阶段解决方案，如自适应门控，学习固定的选择函数，但受限于相同的稀疏性，在部署时无法为每个实例提供补救措施。我们提出UTTSI（不确定性触发的测试时选择性推理），一种无需训练、模型无关的框架，它根据每个实例的不确定性按比例扩展推理深度。一个双信号估计器结合模型logit置信度与数据级频率先验，区分认知不确定性和偶然模糊性。每个实例经过自适应特征过滤以去除不可靠的嵌入；不确定性高的实例额外接受随机特征路径探索，其预测通过一致性加权集成进行聚合。置信度高的实例完全跳过探索，平均开销约为基础模型成本的2.8倍，最坏情况延迟不变。在四个数据集和三种主干架构上的实验表明，与所有训练阶段基线相比，该方法带来了持续且统计显著的提升。为期七天的在线A/B测试进一步确认了5.3%的相对CTR提升（p < 0.01），从而将选择性测试时计算分配确立为CTR预测中训练阶段进展的实用补充。

## Abstract
Scaling test-time compute has proven highly effective for language models, yet this opportunity remains largely unexplored for industrial Click-Through Rate (CTR) prediction. CTR models suffer from a fundamental asymmetry: feature combinations well-represented in training yield confident predictions, while sparsely observed ones produce unreliable outputs. Existing training-phase solutions such as adaptive gating learn a fixed selection function subject to the same sparsity, offering no per-instance recourse at deployment.We propose UTTSI (Uncertainty-Triggered Test-Time Selective Inference), a training-free model-agnostic framework that scales inference depth proportionally to per-instance uncertainty. A dual-signal estimator combining model logit confidence with a data-level frequency prior distinguishes epistemic uncertainty from aleatoric ambiguity. Every instance undergoes adaptive feature filtering to remove unreliable embeddings; uncertain instances additionally receive stochastic feature-path explorations whose predictions are aggregated via consistency-weighted ensembling. Confident instances bypass exploration entirely, keeping average overhead at approximately $2.8\times$ base model cost with worst-case latency unchanged.Experiments on four datasets with three backbone architectures demonstrate consistent, statistically significant gains over all training-phase baselines. A seven-day online A/B test further confirms a 5.3% relative CTR gain ($p < 0.01$), establishing selective test-time compute allocation as a practical complement to training-phase advances for CTR prediction.

---

## 论文详细总结（自动生成）

# 论文结构化中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：工业点击率（CTR）预测模型在训练和推理之间存在根本不对称性：训练中特征组合充分时预测可靠，但推理时大量特征组合（尤其是长尾值）因稀疏观测导致预测不可靠。现有训练阶段方法（如自适应门控、特征交互架构）学习的是固定的选择函数，受制于同样的稀疏性，无法在部署时为每个实例提供自适应的补救措施。
- **整体含义**：本文认为合理的解决方案应在观察到实例后，按需决定每个实例应分配多少额外计算资源，而非对所有实例应用统一的推理过程。这一方向在LLM领域已被验证有效，但在CTR预测中尚属首次探索。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：UTTSI（不确定性触发的测试时选择性推理）是一个无需训练、模型无关的框架，基于每个实例的估计不确定性，按比例缩放推理深度。它包含三个协调的组件：
  - **频率先验估计**：使用Count-Min Sketch概率哈希结构，预计算每个特征值在训练数据中的出现频率，提供数据级可靠性信号。
  - **双信号不确定性估计**：结合模型内部logit置信度（预测是否接近决策边界）和频率先验（加权平均，以梯度归因权重），得到实例级不确定性分数 \( u(x) \)。
    - \( u(x) = 1 - [\alpha \cdot s_{\text{model}}(x) + (1-\alpha) \cdot s_{\text{freq}}(x)] \)
    - 其中 \( s_{\text{model}}(x) \) 基于logit绝对值归一化，\( s_{\text{freq}}(x) \) 为归因加权平均特征频率。
  - **特征过滤与路径探索**：
    - **所有实例**先进行自适应特征过滤：基于复合得分（频率+梯度归因）和每个特征域独立的阈值（由训练数据分位数决定），移除不可靠的嵌入。
    - **不确定性高的实例**额外生成 \( K(x) = \lfloor K_{\max} \cdot u(x) \rfloor \) 条随机特征路径：通过带权重的Bernoulli采样逐步构建特征子集，每条路径独立，最终通过一致性加权集成（指数权重）聚合所有路径（含过滤后的基础预测）的输出。
  - 置信度高的实例（\( K(x)=0 \)）直接输出过滤后的预测，无需多路径探索。

- **关键设计**：
  - 梯度归因（attr(f_i)）通过反向传播从logit得到，用于不确定性估计和特征探索权重，且不更新模型参数。
  - 探索路径的多样性来源于随机采样，而质量由归因和频率引导。
  - 一致性加权抑制离群路径。

## 3. 实验设计：数据集、场景、对比方法

- **数据集**：四个大规模数据集
  - Criteo（39个特征域，45M曝光，点击率26%）
  - Avazu（23个域，40M曝光，点击率17%）
  - KDD12（11个域，60M曝光，点击率4.5%）
  - 工业数据集（68个域，513M曝光，点击率2.5%）
- **对比方法**：涵盖三大类
  - 深度特征交互模型：FM、DNN、Wide&Deep、DeepFM、DCN、AutoInt、FiBiNet、GDCN、MaskNet、PEPNet、HSTU
  - 多专家架构：MMoE、PLE
  - 神经架构搜索：AutoCTR、OptFu
- **评价指标**：AUC和LogLoss
- **在线A/B测试**：在大型电商平台部署7天（2026年4月15-21日），与PEPNet基线对比CTR指标。

## 4. 资源与算力

- 训练硬件：8块NVIDIA A100 GPU
- 框架：TensorFlow，使用Adam优化器，Xavier初始化，ReLU激活
- 训练时长：文中未明确给出具体时长，仅说明超参数通过网格搜索确定。
- 推理开销：平均每实例约2.8倍基础模型调用（一次基础前向+一次过滤后前向+平均0.8条探索路径），但所有路径可完全并行化，最坏延迟等于一次前向。

## 5. 实验数量与充分性

- **实验组数**：
  - 主对比实验：三个骨干网络（HSTU、PLE、OptFu）+ UTTSI在四个数据集上，共12组主结果。
  - 消融实验：三个变体（w/o-Dual、w/o-Attr、w-Single）在三个骨干+四个数据集上，共36组。
  - 超参数敏感性：五个超参数（K_max, α, ρ, β, λ, T）在工业数据集+三个骨干上，每组取不同值，充分展示趋势。
  - 不确定性校准分析：Spearman相关系数、按分位数的误差趋势，以及按不确定性分层（低/中/高）的AUC对比。
  - 在线A/B测试：7天，基于用户级随机流量切分，统计检验。
- **充分性评价**：实验覆盖多个公共和工业数据集、多种骨干架构、全面消融和敏感性分析、在线验证，对比方法包括SOTA。严格实施统计显著性检验（p<0.05），公平性较好。

## 6. 主要结论与发现

- UTTSI在四个数据集和三个骨干网络上均一致优于所有训练阶段基线，其中OptFu+UTTSI达到最优，AUC提升+0.0037至+0.0048，LogLoss降低0.0126至0.0142。
- 消融验证了每个组件（双信号、归因引导、多路径集成）的必要性。
- 不确定性分数与预测误差的Spearman相关系数达0.88-0.91（双信号）vs 0.71-0.76（仅logit），说明频率先验至关重要。
- 在线A/B测试中CTR相对提升5.3%（p<0.01），证明实际工业价值。
- 平均推理开销可控（约2.8倍），且可并行化。

## 7. 优点

- **创新性**：首个为CTR预测设计的测试时计算扩展框架，填补了该方向空白。
- **通用性**：无需修改模型参数或架构，即插即用于任意已训练的CTR模型。
- **设计合理性**：双信号不确定性估计有效分离认知不确定性与偶然模糊性；特征过滤与多路径探索的偏置-方差分解明确。
- **效率友好**：仅对不确定实例投入额外计算，大部分实例无开销，且可并行，满足工业延迟要求。
- **实验充分**：涵盖公共/工业数据集、多种骨干、全面消融与敏感性分析、在线验证。

## 8. 不足与局限

- **计算开销**：梯度归一化的反向传播增加约0.5-1倍前向成本；多路径探索虽可并行但仍需资源，平均2.8倍调用可能对计算资源紧张的场景有压力。
- **依赖频率先验**：Count-Min Sketch提供上界估计，但极端哈希冲突可能误判；频率先验仅反映数据频率，无法捕捉交叉特征模式的稀疏性。
- **超参数较多**：K_max、α、β、ρ、T、λ等，尽管敏感性稳定但实际部署需调优。
- **验证范围**：骨干网络仅测试了HSTU、PLE、OptFu，未在其他架构（如DCN、AutoInt等）上进行UTTSI实验，泛化性有待扩展。
- **离线指标局限性**：AUC和LogLoss的提升在公/私数据集上虽显著，但在线A/B测试仅针对一种基线，更多场景下的鲁棒性需进一步验证。
- **理论分析不足**：未给出探索路径数K(x)与方差缩减之间的理论界限，偏置-方差分解仅凭直觉论证。

（完）
