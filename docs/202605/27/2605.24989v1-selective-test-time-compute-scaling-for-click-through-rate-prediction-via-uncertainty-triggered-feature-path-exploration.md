---
title: Selective Test-Time Compute Scaling for Click-Through Rate Prediction via Uncertainty-Triggered Feature Path Exploration
title_zh: 基于不确定性触发的特征路径探索的点击率预测选择性测试时计算扩展
authors: "Moyu Zhang, Yun Chen, Yujun Jin, Jinxin Hu, Yu Zhang, Xiaoyi Zeng"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.24989v1"
tags: ["query:tta"]
score: 8.0
evidence: 为点击率预测的选择性测试时计算扩展
tldr: "CTR预测中特征组合稀疏导致模型产生不可靠输出，现有训练时方案难以在部署时针对每个实例灵活调整。本文提出UTTSI，一种训练无关的模型无关框架，通过双信号估计器区分不确定类型，对不确定实例进行随机特征路径探索并一致性集成，而置信实例直接输出。在四个数据集和在线实验中，该方法一致超越训练时基线，CTR提升5.3%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24989v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1820, \"height\": 431, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24989v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1849, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24989v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1829, \"height\": 400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24989v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 868, \"height\": 1065, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24989v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 860, \"height\": 1644, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24989v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 746, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24989v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 903, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24989v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 742, \"height\": 207, \"label\": \"Table\"}]"
motivation: 现有训练时自适应门控受限于数据稀疏性，无法在部署时按实例分配计算资源，需测试时选择性推理。
method: 双信号估计器（模型logit置信度+数据频率先验）触发特征路径探索，对不确定实例进行随机子路径采样并一致性加权集成。
result: "在四个数据集和三种骨干网络上获得统计显著提升，在线A/B测试实现5.3%相对CTR增益（p<0.01）。"
conclusion: 选择性测试时计算分配可有效补充训练时进展，为工业CTR预测提供实用且即插即用的方案。
---

## 摘要
在语言模型中，扩展测试时计算已被证明非常有效，然而这一机会在工业点击率（CTR）预测中仍未得到充分探索。CTR模型存在一个根本的不对称性：在训练中充分表示的特征组合可产生有信心的预测，而稀疏观察到的特征组合则产出不可靠的输出。现有的训练阶段解决方案（如自适应门控）学习了受相同稀疏性限制的固定选择函数，在部署时无法提供按实例调整的补救措施。我们提出UTTSI（不确定性触发的测试时选择性推断），一种无需训练、模型无关的框架，能够按实例的不确定性比例扩展推理深度。一个结合模型logit置信度和数据级频率先验的双信号估计器能够区分认知不确定性与偶然模糊性。每个实例都经过自适应特征过滤以移除不可靠的嵌入；对于不确定的实例，额外进行随机特征路径探索，并通过一致性加权集成聚合其预测。有信心的实例完全跳过探索，平均开销约为基础模型成本的2.8倍，最坏情况延迟不变。在三个骨干架构的四个数据集上的实验表明，与所有训练阶段基线相比，UTTSI取得了一致且统计显著的提升。为期七天的在线A/B测试进一步证实了相对CTR提升5.3%（p<0.01），表明选择性测试时计算分配可作为CTR预测训练阶段进展的实用补充。

## Abstract
Scaling test-time compute has proven highly effective for language models, yet this opportunity remains largely unexplored for industrial Click-Through Rate (CTR) prediction. CTR models suffer from a fundamental asymmetry: feature combinations well-represented in training yield confident predictions, while sparsely observed ones produce unreliable outputs. Existing training-phase solutions such as adaptive gating learn a fixed selection function subject to the same sparsity, offering no per-instance recourse at deployment.We propose UTTSI (Uncertainty-Triggered Test-Time Selective Inference), a training-free model-agnostic framework that scales inference depth proportionally to per-instance uncertainty. A dual-signal estimator combining model logit confidence with a data-level frequency prior distinguishes epistemic uncertainty from aleatoric ambiguity. Every instance undergoes adaptive feature filtering to remove unreliable embeddings; uncertain instances additionally receive stochastic feature-path explorations whose predictions are aggregated via consistency-weighted ensembling. Confident instances bypass exploration entirely, keeping average overhead at approximately $2.8\times$ base model cost with worst-case latency unchanged.Experiments on four datasets with three backbone architectures demonstrate consistent, statistically significant gains over all training-phase baselines. A seven-day online A/B test further confirms a 5.3% relative CTR gain ($p < 0.01$), establishing selective test-time compute allocation as a practical complement to training-phase advances for CTR prediction.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：工业点击率（CTR）预测中，训练阶段充分观察的特征组合能产生可靠预测，但大量稀疏特征组合（长尾特征）导致预测不可靠。现有训练时方法（如自适应门控、注意力机制）学到的固定选择函数受限于同样的数据稀疏性，无法在推理时针对每个实例动态调整计算资源。
- **研究动机**：模仿语言模型中测试时计算扩展的成功经验，但直接将LLM的token级置信度方法迁移到CTR不可行（CTR是二分类任务，无生成似然）。需要为CTR设计专门的测试时优化框架，按实例的不确定性分配额外计算，从而弥补训练时与推理时的不对称性。
- **整体含义**：UTTSI首次提出在CTR预测中实现测试时选择性推理，通过双信号不确定性估计触发特征路径探索，让模型在推理时“思考更长”仅对真正困难的样本，从而在不修改模型参数的情况下显著提升性能。

### 2. 论文提出的方法论

- **核心思想**：在已训练好的CTR模型上，构建一个无需训练、模型无关的插件式框架。先评估每个实例的不确定性，然后对不确定实例进行自适应特征过滤和多次随机特征子集探索，最后通过一致性加权集成所有预测；对置信实例仅做一次特征过滤后直接输出。
- **关键技术细节**：
  - **频次先验估计**：采用Count-Min Sketch结构离线统计每个特征值的出现频次，归一化后作为数据级可靠性信号。
  - **双信号不确定性估计**：结合模型logit置信度（`s_model`，基于logit绝对值与阈值γ）和属性加权的频次置信度（`s_freq`，利用梯度范数`attr(f_i)`加权特征频次），得到最终不确定性`u(x)=1-[α·s_model+(1-α)·s_freq]`，并据此分配路径数`K(x)=⌊K_max·u(x)⌋`。
  - **特征过滤**：对所有实例，用复合分数`w(f_i)=β·s_freq(f_i)+(1-β)·attr(f_i)`与每个特征字段的预计算阈值`τ_i`比较，移除低分特征。阈值按训练数据各字段分数的ρ分位数设定。
  - **特征路径探索**（仅对不确定实例）：从过滤后的特征集出发，通过T步迭代Bernoulli采样（概率与`w(f_i)`成正比）构造K(x)条独立特征子集路径，每条路径生成一个预测。
  - **一致性加权聚合**：对所有路径预测（含过滤后基准预测）计算均值，然后每个预测的权重为`w_i=exp(-λ·|P_i - mean|)`，最终加权平均得到`P_final`。
- **算法流程**：输入实例 → 前向传播获logit及梯度属性 → 双信号估计不确定性 → 自适应特征过滤 → 若K(x)=0直接输出过滤后预测；否则并行生成K(x)条路径预测 → 一致性加权聚合输出。

### 3. 实验设计

- **数据集**：四个大规模CTR数据集：
  - Criteo（45M曝光，39个特征字段）
  - Avazu（40M曝光，23个字段）
  - KDD12（60M曝光，11个字段）
  - Industrial（工业数据集，513M曝光，68个字段，来自国际电商平台）
- **基准与对比方法**：
  - **深度特征交互模型**：FM、DNN、Wide&Deep、DeepFM、DCN、AutoInt、FiBiNet、GDCN、MaskNet、PEPNet、HSTU。
  - **多专家架构**：MMoE、PLE。
  - **神经架构搜索**：AutoCTR、OptFu。
  - **UTTSI变体**：在HSTU、PLE、OptFu三个骨干上应用UTTSI，并与原骨干及无UTTSI版本对比。
- **评估指标**：AUC（面积）和LogLoss（二分类交叉熵）。
- **消融实验**：三个变体：
  - w/o-Dual（去掉频次先验，仅用logit置信度）
  - w/o-Attr（路径采样改为均匀随机）
  - w-Single（每个不确定实例只用单条路径）
- **超参数敏感性**：对K_max、α、β、ρ、λ、T分别变化分析。
- **不确定性校准**：按不确定性分位计算预测误差相关性，以及分层AUC对比。
- **在线A/B测试**：在大型电商平台进行7天在线测试（2026年4月15-21日），对比生产级PEPNet基线。

### 4. 资源与算力

- 训练阶段所有模型在 **8块NVIDIA A100 GPU** 上使用TensorFlow实现，采用Adam优化器和Xavier初始化。但论文**未明确给出训练时长**（如每个模型训练多少小时）。
- 测试时UTTSI的额外开销：平均每实例约2.8次模型调用（含一次基础前向、一次过滤后前向、平均0.8次探索路径），因路径可并行化，最坏延迟等于一次基础前向。

### 5. 实验数量与充分性

- **实验数量丰富**：共计在4个数据集、3种骨干网络（HSTU、PLE、OptFu）上报告主要结果；对比了14种训练时基线模型；进行了3项消融实验（每组含4个数据集×3骨干=12个子实验）；5项超参数敏感性分析（在工业数据集上）；不确定性校准（含Spearman相关性统计）；在线A/B测试。
- **充分性与公平性**：
  - 所有基线使用网格搜索最佳超参数，采用相同评估协议。
  - 在线测试统计显著（p<0.01），离线结果标注了p值。
  - 消融实验设计合理，逐组件验证必要性。
  - 不确定性校准不仅报告工业数据集，还列出Criteo和Avazu的Spearman相关系数，泛化性有验证。
  - 不足之处：超参数敏感性仅在工业数据集上展示，未在其他数据集完全重复；在线测试仅对比PEPNet单一基线，未与其他测试时方法（如TTA）对比（因TTA不适合CTR任务）。

### 6. 论文的主要结论与发现

- **UTTSI在四个数据集上均一致超越所有训练时基线**，尤其是与OptFu结合时提升最大（AUC +0.0037~+0.0048，LogLoss下降）。
- **不确定性校准良好**：双信号估计器与预测误差的Spearman相关系数达0.88-0.91，远高于仅用logit（0.71-0.76）。
- **按不确定性分层**：高不确定性样本获得最大增益（+0.0088 AUC），而低不确定性样本也有收益（+0.0013），验证了过滤和探索互补设计。
- **在线A/B测试**：CTR相对提升5.3%（p<0.01），验证了实际价值。
- **计算开销可控**：2.8×平均开销，且可通过并行化维持延迟不变。

### 7. 优点

- **方法创新性**：首次将测试时计算扩展引入CTR预测，提出双信号不确定性估计器有效区分认知和偶然不确定性，区别于LLM方法。
- **模型无关与即插即用**：不修改原模型参数或架构，适用于任何已训练的CTR模型，实用性强。
- **实验全面**：覆盖四个不同规模的数据集（含工业级超5亿样本）、多种骨干架构、离线消融、在线部署，置信度高。
- **效率与效果平衡**：通过不确定性路由使平均开销仅2.8×，并利用并行化保证最坏延迟不增加，适应工业SLA要求。
- **开放性与可复现**：使用公开数据集（Criteo、Avazu、KDD12），关键超参数给定默认值，方法描述详细。

### 8. 不足与局限

- **超参数依赖性**：虽敏感度分析显示鲁棒，但仍需在部署前针对特定模型和数据进行调参（如K_max、α、β等），增加了应用门槛。
- **实验覆盖局限**：
  - 在线测试仅对比一个PEPNet基线，未与其他测试时方法（如TTA或早期退出网络）对比（尽管论文论证了它们不可直接迁移，但仍可设计适配版对比）。
  - 超参数敏感性仅在工业数据集上验证，其他公开数据集上未重复，可能存在数据相关性。
- **计算开销未完全透明**：作者报告平均2.8×模型调用，但未详细说明梯度计算（反向传播）在不确定估计中的额外耗时；虽然提及“约0.5-1×前向成本”，但未在实验中对总延迟进行端到端测量。
- **频次先验离线更新依赖**：频次索引需每日增量更新，若新增特征出现频率骤变，可能引入短期校准偏差；论文A/B测试7天未退化，但更长周期未验证。
- **路径探索的随机性**：虽采用一致性加权，但多路径随机采样可能对某些边缘实例产生不一致结果，缺乏理论保证收敛到最优。
- **可解释性不足**：不确定性得分与路径选择的联合效应难以解释，可能给工业调试带来困难。

（完）
