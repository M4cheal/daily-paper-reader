---
title: "COPRA: Conditional Parameter Adaptation with Reinforcement Learning for Video Anomaly Detection"
title_zh: COPRA：基于强化学习的视频异常检测条件参数自适应
authors: "Darryl Cherian Jacob, Xinyu Liu, Kai Wang, Pan He"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15325v1"
tags: ["query:tta"]
score: 9.0
evidence: 视觉语言模型在推理时的条件参数自适应
tldr: 针对视觉语言模型在视频异常检测中存在的训练与推理不匹配问题，本文提出了COPRA框架。该框架通过条件参数自适应机制，为每个视频片段动态生成特定的参数更新，而非使用固定提示或共享更新。实验证明，COPRA在标准VAD基准测试中表现优异，且在跨领域任务及视频问答等泛化任务上展现了强大的适应能力，为自适应视频理解提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15325v1/fig-001.webp\", \"caption\": \"Table 1: In-domain comparison with prior VAD methods on UCF-Crime and XD-Violence. Among explainable approaches, COPRA consistently improves over its frozen baseline and outperforms strong baselines such as VERA Ye et al. [2025]. On UCF-Crime, it raises InternVL2-8B from 84.31 to 87.14 AUC, and on XD-Violence from 74.32 to 76.52 AP.\", \"page\": 6, \"index\": 1, \"width\": 831, \"height\": 538}, {\"url\": \"assets/figures/arxiv/2605.15325v1/fig-002.webp\", \"caption\": \"Table 2: COPRA consistently outperforms frozen VLM baselines across models and in-domain datasets. For fair comparison, all models use VERA’s three-stage inference—(Step 1) initial scoring, (Step 2) retrieval refinement, and (Step 3) temporal smoothing/weighting—and we compare identical backbones with and without COPRA. COPRA improves AUC/AP at every stage for Qwen2-VL-7BInstruct and InternVL2-8B, demonstrating gains beyond post-processing alone.\", \"page\": 6, \"index\": 2, \"width\": 740, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2605.15325v1/fig-003.webp\", \"caption\": \"Figure 1: Illustration of the generalization gap in VAD caused by training–inference mismatch and train–test distribution shift. Existing methods rely on shared, static parameters, forcing diverse video patterns into a single compromise solution. As a result, the decision boundary learned from the training dataset often fails to generalize to unseen test distributions, causing some samples to fall on the wrong side of the boundary. We address this limitation through instance-conditioned parameter adaptation, where each video dynamically pulls the decision landscape toward a more suitable boundary via input-specific parameter updates, improving robustness and generalization.\", \"page\": 2, \"index\": 3, \"width\": 826, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2605.15325v1/fig-004.webp\", \"caption\": \"Figure 3: AUC (%) performance for cross-dataset generalization across train–test dataset pairs. From left to right: COPRA with Qwen2-VL-7B-Instruct, COPRA with InternVL2-8B, VERA with InternVL2-8B, and the AUC improvement of COPRA over VERA Ye et al. [2025].\", \"page\": 7, \"index\": 4, \"width\": 782, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2605.15325v1/fig-005.webp\", \"caption\": \"Figure 7: Illustration of COPRA’s segment-level reasoning process on an arrest video from UCFCrime. Each numbered group shows representative frames sampled from a temporal segment, where the visual context evolves from an initially empty room to suspicious human activity, physical confrontation, and the arrival of additional people. For each segment, the VLM produces a corresponding reasoning response, allowing the anomaly decision to be grounded in localized visual evidence and yielding a more reliable final anomaly score.\", \"page\": 18, \"index\": 5, \"width\": 829, \"height\": 449}, {\"url\": \"assets/figures/arxiv/2605.15325v1/fig-006.webp\", \"caption\": \"Figure 4: Cross-domain evaluation of natural language explanation quality on HIVAU-70K Zhang et al. [2025]. C, E, and V denote clip-, event-, and video-level evaluation, respectively. Compared with the InternVL2-8B baseline, COPRA improves most semantic and lexical overlap metrics across all temporal levels, including BLEU, CIDEr, METEOR, and ROUGE, demonstrating strong crossdomain generalization for natural language explanation generation.\", \"page\": 8, \"index\": 6, \"width\": 829, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2605.15325v1/fig-007.webp\", \"caption\": \"Figure 2: Overview of COPRA, an instance-conditioned parameter generation framework for VAD. The key advantage is a learned functional memory that maps each instance to tailored parameters, enabling improved robustness and cross-domain generalization compared to static adaptation methods.\", \"page\": 4, \"index\": 7, \"width\": 829, \"height\": 200}, {\"url\": \"assets/figures/arxiv/2605.15325v1/fig-008.webp\", \"caption\": \"Figure 6: Qualitative temporal anomaly scoring example on XD-Violence. The grey bars show the InternVL2-8B baseline, the green bars show COPRA, and the black dashed signal indicates the ground-truth anomaly interval. Although the baseline assigns high anomaly scores to normal frames before the annotated event and drops during much of the abnormal interval, COPRA better aligns its peak response with the ground-truth abnormal segment. These examples illustrate how instance-conditioned parameter generation improves temporal anomaly localization.\", \"page\": 17, \"index\": 8, \"width\": 829, \"height\": 550}]"
motivation: 现有的基于视觉语言模型的视频异常检测方法在训练和推理阶段存在数据分布与模型配置的不一致，限制了其在未知环境下的泛化能力。
method: 提出了一种条件参数自适应框架，通过为每个视频片段动态生成输入特定的参数更新，使冻结的视觉语言模型能够实时适应不同的上下文。
result: 在多个标准视频异常检测基准测试中，COPRA在域内和跨域设置下均优于静态基准模型，并成功扩展至视频问答和密集描述任务。
conclusion: COPRA证明了权重空间生成框架在实现可扩展、自适应且感知上下文的视频理解任务中的有效性。
---

## 摘要
视觉语言模型（VLM）在视频异常检测（VAD）中表现出强大的性能，同时提供了可解释的预测。然而，现有的基于 VLM 的 VAD 方法在数据分布和模型配置方面都存在训练与推理之间的根本不匹配。首先，大多数方法依赖于静态的训练后自适应，限制了在未见环境或异常类型等分布偏移下的泛化能力。其次，它们在长视频的稀疏帧上训练 VLM，但在密集采样的短片段上进行推理，导致训练和测试之间存在不一致。为了解决这些局限性，我们提出了 COPRA，一种用于基于 VLM 的 VAD 的条件参数自适应框架。COPRA 不使用固定的提示词或共享的参数更新，而是生成特定于输入的参数更新，以便在训练和推理过程中为每个视频片段动态调整冻结的 VLM。实验表明，COPRA 在标准 VAD 基准测试中表现强劲，在域内和跨域设置下均始终优于静态基准方法。此外，COPRA 还能泛化到 VAD 之外的未见任务，如多项选择视频问答和密集字幕生成。这些结果凸显了 COPRA 作为一个有效的权重空间生成框架，适用于可扩展、自适应且具有上下文感知能力的视频理解。代码将在 https://github.com/THE-MALT-LAB/COPRA 发布。

## Abstract
Vision-language models (VLMs) have shown strong performance in video anomaly detection (VAD) while providing interpretable predictions. However, existing VLM-based VAD methods suffer from a fundamental mismatch between training and inference in both data distribution and model configuration. First, most approaches rely on static post-training adaptation, limiting generalization under distribution shifts such as unseen environments or anomaly types. Second, they train VLMs on sparse frames from long videos, but perform inference on densely sampled short segments, creating inconsistencies between training and testing. To address these limitations, we propose COPRA, a conditional parameter adaptation framework for VLM-based VAD. Instead of fixed prompts or shared parameter updates, COPRA generates input-specific parameter updates to dynamically adapt a frozen VLM for each video segment during both training and inference. Experiments show strong performance on standard VAD benchmarks, consistently outperforming static baselines in both in-domain and cross-domain settings. Moreover, COPRA generalizes beyond VAD to unseen tasks such as multiple-choice Video Question Answering and Dense Captioning. These results highlight COPRA as an effective weight-space generation framework for scalable, adaptive, and context-aware video understanding. The code will be released at https://github.com/THE-MALT-LAB/COPRA

---

## 论文详细总结（自动生成）

以下是对论文《COPRA: Conditional Parameter Adaptation with Reinforcement Learning for Video Anomaly Detection》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于视觉语言模型（VLM）的视频异常检测（VAD）方法存在“共享参数瓶颈”。
*   **研究动机**：
    *   **训练-推理不匹配**：模型通常在长视频的稀疏帧上训练，但在密集采样的滑动窗口上推理。
    *   **静态自适应局限性**：现有方法在训练后使用固定的提示词（Prompts）或共享的参数更新（如 LoRA），导致模型容易过拟合训练分布，难以应对未见的场景、视角或新型异常。
    *   **异质性挑战**：不同的异常（如交通事故 vs. 商店盗窃）需要截然不同的视觉线索和推理逻辑，单一的静态参数集往往是各方妥协的结果，而非最优解。

### 2. 方法论：COPRA 框架
*   **核心思想**：将 VAD 建模为一个**实例条件自适应问题**。不再学习全局共享的参数，而是针对每个视频片段动态生成特定的参数更新（LoRA 权重），实现“一例一策”。
*   **关键技术细节**：
    *   **条件参数生成器**：引入一个轻量级的 Transformer 生成器 $g_{\phi}$。它接收视频帧的视觉特征作为条件信号，动态预测 VLM 语言主干网络的 LoRA 更新矩阵（A 和 B）。
    *   **全局潜验先验**：在生成的参数中注入一个可学习的全局潜验先验 $p_{global}$，以捕获数据集层面的通用模式。
    *   **强化学习微调（RLHF/GRPO）**：采用组相对策略优化（GRPO）算法，在仅有视频级弱监督（正常/异常标签）的情况下，通过奖励机制（准确性奖励和格式奖励）引导生成器产生有效的参数。
    *   **灵活的时间采样**：支持在不同时间粒度（如 10s 片段或 120s 块）上生成参数，平衡了计算开销与上下文感知能力。

### 3. 实验设计
*   **数据集**：
    *   **域内（In-Domain）**：UCF-Crime（监控场景）、XD-Violence（暴力事件）。
    *   **跨域（Cross-Domain）**：VRU-Accident（交通安全）、HIVAU-70K（开放式异常理解）。
*   **Benchmark 与对比方法**：
    *   对比了非解释性方法（如 RTFM, MGFN）和可解释性方法（如 LAVAD, Holmes-VAD, VERA）。
    *   基准模型包括 InternVL2-8B、InternVL3-8B 和 Qwen2-VL-7B。
*   **评估指标**：UCF-Crime 使用 AUC，XD-Violence 使用 AP，跨域任务使用准确率、BLEU、METEOR、CIDEr 等。

### 4. 资源与算力
*   **硬件**：单台 **NVIDIA A100-SXM4 GPU (80GB)**。
*   **训练时长**：在 UCF-Crime 或 XD-Violence 上训练一个 epoch 大约需要 **3 天**。
*   **模型规模**：生成器参数量约为 87.37M，而底座 VLM（如 8B 参数）保持冻结。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括：
    *   在两个主流 VAD 数据集上的性能对比。
    *   跨架构（Qwen, InternVL2/3）的验证。
    *   **消融实验**：验证了实例特定参数 vs. 静态参数、全局潜验的作用、不同时间粒度的影响。
    *   **泛化性测试**：直接将模型应用于视频问答（VideoQA）和密集描述（Dense Captioning）等非 VAD 任务。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集测试（在 A 上练，在 B 上测）有力地证明了该方法缓解了过拟合问题，而非仅仅依靠强大的底座模型。

### 6. 主要结论与发现
*   **动态胜过静态**：实例条件的参数自适应显著优于静态 LoRA 或提示词微调，AUC/AP 提升明显。
*   **强大的泛化力**：COPRA 学习到的不是简单的分类规则，而是某种“权重空间生成机制”，使其在未见过的交通场景和自然语言解释任务中依然有效。
*   **表征分离度更高**：t-SNE 可视化显示，COPRA 生成的特征空间中，正常与异常样本的边界更加清晰（Silhouette 分数更高）。

### 7. 优点与亮点
*   **创新范式**：首次将权重空间生成（Weight-space Generation）引入视频异常检测，解决了长期存在的训练-推理不匹配问题。
*   **端到端强化学习**：巧妙地利用 GRPO 算法在弱监督下训练参数生成器，无需复杂的伪标签生成过程。
*   **可解释性**：不仅提供异常分数，还能通过自适应后的 VLM 生成更准确、更具行为显著性的自然语言描述。

### 8. 不足与局限
*   **计算开销**：虽然参数生成器很轻量，但在 GRPO 训练期间需要多次采样响应，增加了训练成本。
*   **理论深度**：生成的 LoRA 权重本身缺乏直观的可解释性（即很难解释为什么这组权重能提升性能）。
*   **推理延迟**：虽然可以分块复用参数，但相比于完全冻结的模型，动态生成参数并注入权重仍会带来额外的推理延迟和显存占用。

（完）
