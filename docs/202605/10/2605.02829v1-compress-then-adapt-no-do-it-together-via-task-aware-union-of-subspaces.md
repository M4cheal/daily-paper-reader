---
title: "Compress Then Adapt? No, Do It Together via Task-aware Union of Subspaces"
title_zh: 先压缩再适配？不，通过任务感知的子空间并集共同完成
authors: "Jingze Ge, Yun Liu, Xue Geng, Wanqi Dong, Wang Zhe Mark, Min Wu, Xulei Yang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02829v1"
tags: ["query:tta"]
score: 7.0
evidence: 大型预训练模型的联合自适应与压缩框架
tldr: 针对大型预训练模型在下游任务中参数高效微调（PEFT）与低秩压缩通常分步进行导致的不对齐问题，本文提出JACTUS框架。该方法通过任务感知的子空间并集，将压缩与微调过程统一。它利用输入和梯度协方差估计，在保留权重的同时进行投影低秩近似和全局秩分配，仅训练紧凑的核心矩阵。实验证明，JACTUS在视觉和语言任务上，以更少的参数量超越了DoRA等全参数PEFT基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.02829v1/fig-001.webp\", \"caption\": \"Table 4: CommonsenseQA on Llama2-7B [44]. Accuracy (%). Results of LoRA and DoRA are taken from DoRA [25].\", \"page\": 13, \"index\": 1, \"width\": 716, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.02829v1/fig-002.webp\", \"caption\": \"Fig. 3: Layer-wise ranks selected by the greedy global allocator for ViT-Large [10] under 40%, 60%, and 80% retained-parameter budget. We plot the ranks of attention (Query/Key/Value/Projection) and MLP (Up/Down) projections across layers. The calibration dataset is extracted from CIFAR-100 [22] training split.\", \"page\": 8, \"index\": 2, \"width\": 725, \"height\": 690}, {\"url\": \"assets/figures/arxiv/2605.02829v1/fig-003.webp\", \"caption\": \"Table 1: Fine-tuning on ViT-Base and ViT-Large [10]. Top-1 accuracy (%).\", \"page\": 12, \"index\": 3, \"width\": 714, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2605.02829v1/fig-004.webp\", \"caption\": \"Table 2: Fine-tuning on Swin-Base and Swin-Large [26]. Top-1 accuracy (%).\", \"page\": 12, \"index\": 4, \"width\": 716, \"height\": 417}]"
motivation: 传统的先压缩后微调策略会导致压缩子空间与下游任务目标不匹配，且无法在全局范围内优化参数预算。
method: 提出JACTUS框架，通过输入与梯度协方差的子空间并集进行投影低秩近似，并按边际收益进行全局秩分配。
result: "在ViT和Llama2模型上，JACTUS在仅保留80%参数的情况下，性能优于保留100%参数的DoRA等主流PEFT方法。"
conclusion: 研究表明，将模型压缩与任务适配进行联合优化，能有效解决子空间错位问题，实现更高效且性能更优的模型部署。
---

## 摘要
将大型预训练模型适配到多样化任务已成为常规操作，然而参数高效微调（PEFT）和低秩压缩这两大主流策略通常是按顺序组合的。这种解耦的做法先进行压缩，然后再微调适配器，可能会导致压缩后的子空间与下游目标不匹配，并浪费全局参数预算。为了克服这一局限性，我们引入了 JACTUS（基于任务感知子空间并集的联合适配与压缩），这是一个统一了压缩与适配的单一框架。JACTUS 从一个小的校准集中估计输入和预激活梯度的协方差，将它们与预训练权重子空间形成正交并集，在该并集内执行投影低秩近似，根据每个参数的边际收益全局分配秩，并仅训练一个紧凑的核心矩阵。通过将为压缩保留的方向与适配所需的方向耦合，这显式地缓解了压缩子空间与下游目标之间潜在的不匹配，从而产生一个可部署的低秩模型，该模型在避免保留全部冻结权重的同时，实现了快速且稳健的微调。在视觉任务上，JACTUS 在保留 80% 参数的情况下，在八个数据集上的 ViT-Base 平均准确率达到 89.2%，超过了强大的 100% 参数 PEFT 基准（例如 DoRA 的 87.9%）。在语言任务上，在相同的 80% 保留参数预算下，JACTUS 在 Llama2-7B 常识问答中实现了 80.9% 的平均得分，优于 100% 参数的 PEFT（例如 DoRA 的 79.7%），并在相同保留参数预算下超过了先前的“先压缩后微调”流水线。我们将发布代码。

## Abstract
Adapting large pretrained models to diverse tasks is now routine, yet the two dominant strategies of parameter-efficient fine-tuning (PEFT) and low-rank compression are typically composed in sequence. This decoupled practice first compresses and then fine-tunes adapters, potentially misaligning the compressed subspace with downstream objectives and squandering a global parameter budget. To overcome this limitation, we introduce JACTUS (Joint Adaptation and Compression with a Task-aware Union of Subspaces), a single framework that unifies compression and adaptation. From a small calibration set, JACTUS estimates input and pre-activation gradient covariances, forms their orthogonal union with the pretrained weight subspace, performs a projected low-rank approximation inside this union, allocates rank globally by marginal gain per parameter, and trains only a compact core matrix. This explicitly mitigates the potential misalignment between the compressed subspace and downstream objectives by coupling the directions preserved for compression with those required for adaptation, yielding a deployable low-rank model that avoids retaining full frozen weights while enabling fast and robust tuning. On vision, JACTUS attains an average 89.2% accuracy on ViT-Base across eight datasets at 80% retained parameters, surpassing strong 100% PEFT baselines (e.g., DoRA 87.9%). On language, JACTUS achieves an 80.9% average on Llama2-7B commonsense QA at the same 80% retained-parameter budget, outperforming 100% PEFT (e.g., DoRA 79.7%) and exceeding prior compress-then-finetune pipelines under the same ratained-parameter budget. We will release code.