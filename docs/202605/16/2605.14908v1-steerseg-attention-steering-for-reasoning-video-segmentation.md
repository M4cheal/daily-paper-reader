---
title: "SteerSeg: Attention Steering for Reasoning Video Segmentation"
title_zh: SteerSeg：用于推理视频分割的注意力引导
authors: "Ali Cheraghian, Hamidreza Dastmalchi, Abdelwahed Khamis, Morteza Saberi, Aijun An, Lars Petersson"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14908v1"
tags: ["query:tta"]
score: 6.5
evidence: 使用冻结LVLM进行视频推理分割的免训练接地技术
tldr: 本研究针对视频推理分割中大视觉语言模型（LVLM）注意力图弥散且模糊的问题，提出了SteerSeg框架。该框架通过输入层面的调节来引导注意力，结合可学习的软提示词和思维链（CoT）提示，将注意力重塑为更集中的空间先验。SteerSeg在冻结模型参数的同时，仅通过少量参数学习显著提升了LVLM的空间定位能力，在多个基准测试中表现出优异的泛化性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14908v1/fig-001.webp\", \"caption\": \"Figure 1: (a) Diagnostic study on reasoning and grounding. While the LVLM often identifies the correct target object, the corresponding attention remains poorly localized, leading to inaccurate masks. (b) Effect of attention refinement on segmentation. Raw and contrast-based attention produce ambiguous localization, while soft prompts and CoT reasoning progressively improve attention quality and segmentation accuracy.\", \"page\": 2, \"index\": 1, \"width\": 820, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.14908v1/fig-002.webp\", \"caption\": \"Table 1: Comparison of LVLM-based text-conditioned VOS methods on Ref-DAVIS [19], ReasonVOS [5], and ReVOS [44] benchmarks. The upper gray rows correspond to methods trained end-to-end jointly with the LVLM and segmentation model, while the lower colored rows use frozen LVLMs. Methods marked with ⋆ are fully training-free.\", \"page\": 7, \"index\": 2, \"width\": 824, \"height\": 368}, {\"url\": \"assets/figures/arxiv/2605.14908v1/fig-003.webp\", \"caption\": \"Figure 6: GUI interface used for human evaluation of the generated reasoning attributes on ReasonVOS samples.\", \"page\": 14, \"index\": 3, \"width\": 810, \"height\": 535}, {\"url\": \"assets/figures/arxiv/2605.14908v1/fig-004.webp\", \"caption\": \"Figure 2: Given a referring expression and video frames, the frozen LVLM first performs CoT reasoning to extract discriminative attributes of the target object. These attributes are incorporated into a prompt for response-token generation, together with learnable soft prompts. Attention rollout from the response token produces spatial attention maps, which are reshaped into frame-level and videolevel localization cues. The resulting attention maps are used to guide SAM for mask propagation and candidate tracklet generation, followed by tracklet ranking to obtain the final segmentation mask.\", \"page\": 4, \"index\": 4, \"width\": 824, \"height\": 220}, {\"url\": \"assets/figures/arxiv/2605.14908v1/fig-005.webp\", \"caption\": \"Table 5: Hyperparameters used for soft-prompt training, rollout extraction, and SAM2 propagation.\", \"page\": 16, \"index\": 5, \"width\": 772, \"height\": 413}]"
motivation: 现有的基于LVLM的视频分割方法依赖于为文本生成优化的注意力图，导致空间定位信号弥散且存在歧义。
method: 提出通过可学习的软提示词重塑注意力分布，并结合思维链属性引导注意力指向正确实例，从而生成精准的点提示驱动分割模型。
result: 仅在Ref-YouTube-VOS上训练，SteerSeg就在多个多样化基准测试中显著提升了LVLM的空间定位精度和泛化能力。
conclusion: 通过在输入端引导注意力而非仅在后处理中利用，可以有效弥合LVLM生成能力与空间定位需求之间的鸿沟。
---

## 摘要
视频推理分割需要根据自然语言表达式在视频帧中定位对象，这通常涉及空间推理和隐式引用。最近的方法利用冻结的大型视觉语言模型（LVLM），通过提取注意力图并将其作为分割的空间先验，从而实现免训练的定位。然而，这些注意力图是为文本生成而非空间定位优化的，往往导致定位信号弥散且模糊。在这项工作中，我们介绍了 SteerSeg，这是一个轻量级框架，它将注意力失调识别为基于注意力的定位中的关键瓶颈，并提出通过输入级调节从源头引导注意力。SteerSeg 将可学习的软提示与推理引导的思维链（CoT）提示相结合。软提示重塑了注意力分布以产生空间上更集中的图，而源自 CoT 的属性通过将注意力引导至正确的实例，解决了相似对象之间的歧义。生成的注意力图被转换为跨关键帧的点提示以引导分割模型，同时使用基于相关性的评分对候选轨迹段进行排序和选择。我们的方法冻结了 LVLM 和分割模型的参数，仅学习一小部分软提示，在保留模型预训练推理能力的同时显著提高了定位性能。尽管仅在 Ref-YouTube-VOS 上进行训练，SteerSeg 在各种基准测试中都表现出良好的泛化能力，显著提升了 LVLM 的空间定位能力。项目页面：https://steerseg.github.io

## Abstract
Video reasoning segmentation requires localizing objects across video frames from natural language expressions, often involving spatial reasoning and implicit references. Recent approaches leverage frozen large vision-language models (LVLMs) by extracting attention maps and using them as spatial priors for segmentation, enabling training-free grounding. However, these attention maps are optimized for text generation rather than spatial localization, often resulting in diffuse and ambiguous grounding signals. In this work, we introduce SteerSeg, a lightweight framework that identifies attention misalignment as the key bottleneck in attention-based grounding and proposes to steer attention at its source through input-level conditioning. SteerSeg combines learnable soft prompts with reasoning-guided Chain-of-Thought (CoT) prompting. The soft prompts reshape the attention distribution to produce more spatially concentrated maps, while CoT-derived attributes resolve ambiguity among similar objects by guiding attention toward the correct instance. The resulting attention maps are converted into point prompts across keyframes to guide a segmentation model, while candidate tracklets are ranked and selected using correlation-based scoring. Our approach freezes the LVLM and segmentation model parameters and learns only a small set of soft prompts, preserving the model's pretrained reasoning capabilities while significantly improving grounding. Despite being trained only on Ref-YouTube-VOS, SteerSeg generalizes well across diverse benchmarks, significantly improving the spatial grounding capability of LVLMs. Project page: https://steerseg.github.io